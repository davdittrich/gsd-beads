name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7

      - name: Build allowlisted archive
        run: |
          zip -r gsd-beads.zip \
            .claude-plugin \
            hooks \
            .agents/skills \
            .gsd \
            ponytail-everywhere/.claude-plugin \
            ponytail-everywhere/hooks \
            ponytail-everywhere/.gsd \
            README.md \
            LICENSE

      - name: Publish release
        run: gh release create "$RELEASE_TAG" gsd-beads.zip --generate-notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.ref_name }}
