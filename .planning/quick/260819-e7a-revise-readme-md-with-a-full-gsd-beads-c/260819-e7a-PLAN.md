---
phase: quick-260819-e7a
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [gsd-beads-833]
files_modified:
  - README.md

estimate:
  tokens: 35000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`README.md` documents all four `beads.*` config keys (`enabled`, `sync_mode`, `ship_gate`, `epic_per`) with type, allowed values, shipped default, and effect."
    - "Every documented key, default, and env var in README.md is quotable verbatim from `capability.json`, the two hook scripts, or `sync.py` — no invented keys, no guessed defaults."
    - "README.md states that `sync_mode` values `mirror` and `off` are reserved and not yet implemented."
    - "README.md states the enabled-resolution rule the skills implement: disabled only when `.planning/config.json` has `beads.enabled` explicitly boolean `false`; missing file, missing `beads` object, and missing key all fall through to the `capability.json` default of `true`."
    - "README.md contains exactly one copy-pasteable `json` fenced block showing the `beads` object with all four keys at their shipped defaults, and that block parses as valid JSON."
    - "README.md states the `epic_per: milestone` forward-only caveat (does not retroactively fold existing per-phase epics)."
    - "README.md documents `CLAUDE_PLUGIN_ROOT`, `CLAUDE_CONFIG_DIR`, and `GSD_HOME` as the environment variables the plugin reads, each with its default."
    - "README.md separates `BEADS_DIR` and `bd config set` as bd-owned knobs that gsd-beads neither reads nor writes."
    - "The Uninstall block names the same plugin identifier the Install block installs."
    - "The Example workflow list names commands in the namespace gsd-core actually ships."
    - "`rumdl check` against the markdown-linting capability's curated ruleset reports zero violations for README.md."
    - "The change is committed on `main` with only README.md in the commit."
  artifacts:
    - README.md
  key_links:
    - "README.md `## Configuration` table rows ↔ `plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json` `config` object (single source of the four keys and their defaults)"
    - "README.md env-var table rows ↔ the literal `${VAR:-default}` expansions in `hooks/capability-auto-install.sh` and the `os.environ.get` calls in `scripts/sync.py`"
    - "README.md Uninstall plugin identifier ↔ README.md Install plugin identifier ↔ `.claude-plugin/marketplace.json` plugin `name`"
---

<objective>
Revise `README.md` with an exhaustive, evidence-grounded `## Configuration` section covering every
gsd-beads configuration surface, and fix two known accuracy defects in the existing prose.

Purpose: today a reader can discover `beads.enabled` from the README and nothing else. Three of the
four config keys, the enabled-resolution rule, and every environment variable the plugin reads are
undocumented outside source files.
Output: a revised `README.md`, linted and committed on `main`. No source or behaviour changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@README.md
@plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json

Read the beads ticket for intent, do not restate it in the README: `bd show gsd-beads-833`.

Grounding already established during planning — treat these as verified facts, they need no
re-discovery. Verify only what a task's `<action>` explicitly tells you to re-check:

- `capability.json` `config` object declares exactly four keys. Verbatim defaults and descriptions:
  - `beads.enabled` — boolean, default `true`, "Master toggle for the beads issue-tracking capability."
  - `beads.sync_mode` — enum `["authoritative","mirror","off"]`, default `"authoritative"`.
    Description: "authoritative (default): bd owns task status and task content (title/description)
    after first sync. Content originates in PLAN.md at first sync, but PLAN.md task text is never
    re-synced from later bd edits (D-01). 'mirror' and 'off' are reserved for later phases."
  - `beads.ship_gate` — boolean, default `true`, "When true, ship:pre blocks on blocking_open>0 or
    diverged>0 in BEADS.md."
  - `beads.epic_per` — enum `["phase","milestone"]`, default `"phase"`. Description: "phase
    (default): one epic per phase, as today. milestone: one epic shared across every phase in the
    current milestone (D-10: forward-only, does not retroactively fold existing per-phase epics;
    D-11: read fresh at each epic-creation call site)."
- `capability.json` also declares the two `ship:pre` gates as `when: "beads.ship_gate"`,
  `blocking: true`, each an `artifact-frontmatter-equals` predicate on `BEADS.md` (`blocking_open`
  equals 0, `diverged` equals 0), and gates all six lifecycle steps on `when: "beads.enabled"` with
  `onError: skip`.
- All four capability skills carry the identical Step 1 config gate, byte-for-byte (see
  `skills/beads-sync/SKILL.md:26-28` and the same lines in `beads-status`, `beads-recall`,
  `beads-migrate-todos`): read `.planning/config.json` with the Read tool; STOP only "If the file
  exists, `config.beads` is present, and `config.beads.enabled` is explicitly the boolean `false`";
  otherwise "the file is missing, `config.beads` is absent, or `config.beads` is present with no
  `enabled` key -- fall through to the shipped default (`beads.enabled: true` in `capability.json`)".
- `scripts/sync.py:618 read_epic_per()` re-reads `.planning/config.json` fresh at each
  epic-creation call site (D-11) and returns `"phase"` when the file is absent, raises
  `json.JSONDecodeError` (malformed), or carries no `beads.epic_per` key.
- `skills/beads-status/SKILL.md` Step 2c reads `BEADS.md`'s `blocking_open`/`diverged` frontmatter
  and `.planning/config.json`'s `beads.ship_gate`; when `ship_gate` is `false` **and**
  (`blocking_open > 0` **or** `diverged > 0`) it runs `sync.py ship-override`, which
  (`sync.py:1848 ship_override()`) records a `Beads-Override: ship_gate bypassed,
  blocking_open=…, diverged=…` git trailer via `git commit --amend --trailer` plus a best-effort
  `bd comment` on the phase epic; it refuses to amend when HEAD is already pushed.
- Environment variables the plugin actually reads, with their literal defaults:
  - `CLAUDE_PLUGIN_ROOT` — `hooks/session-start.sh:5` and `hooks/capability-auto-install.sh:18`,
    both `"${CLAUDE_PLUGIN_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}"`.
  - `GSD_HOME` — `hooks/capability-auto-install.sh:46`,
    `STATE_FILE="${GSD_HOME:-$HOME}/.gsd/capability-auto-install-$CAP_ID.hash"` (the per-capability
    bundle-drift hash sidecar).
  - `CLAUDE_CONFIG_DIR` — `hooks/capability-auto-install.sh:68`, gsd-tools resolver fallback
    `"${CLAUDE_CONFIG_DIR:-$HOME/.claude}/gsd-core/bin/gsd-tools.cjs"`; and `sync.py:1935` /
    `sync.py:1999`, `os.environ.get("CLAUDE_CONFIG_DIR", str(Path.home() / ".claude"))` joined with
    `gsd-core/workflows/ship.md` and `gsd-core/workflows/execute-plan.md` for the two local-patch
    checks. Both sync.py docstrings state the probe is scoped to the Claude runtime only —
    `CODEX_HOME`, `CURSOR_CONFIG_DIR` and other runtime homes are deliberately not probed.
  - `BD_TIMEOUT` at `sync.py:21` is a hard-coded module constant (`BD_TIMEOUT = 15  # seconds`),
    **not** an environment variable. Do not document it as one.
- bd-owned, not gsd-beads-owned: `BEADS_DIR`
  (`.agents/skills/beads/resources/WORKTREES.md:44` — points a worktree at a beads workspace
  outside the tree; `STEALTH_MODE.md:23,34`) and `bd config set no-git-ops true`
  (`STEALTH_MODE.md:21,33`). Nothing in gsd-beads reads or writes either.
- Command namespace ground truth: gsd-core's `plugin.json` declares `"commands": "./commands/gsd/"`
  and that directory contains `plan-phase.md`, `execute-phase.md`, `verify-work.md`, `ship.md`
  (71 files total), so the shipped plugin form is the colon namespace — matching README.md line 9's
  existing `/gsd:*` claim, and contradicting the hyphenated forms in the Example workflow list.
- Uninstall ground truth: `claude plugin uninstall|remove [options] <plugin>`;
  `~/.claude/plugins/installed_plugins.json` keys this plugin as `beads-lifecycle@gsd-beads`;
  `.claude-plugin/marketplace.json` names the plugin `beads-lifecycle`. There is no plugin named
  `beads`.
- Lint ruleset: no `.rumdl.toml` exists in this repo. The markdown-linting capability resolves its
  config to `<root>/.gsd/capabilities/markdown-linting/config/.rumdl.toml` (`lint.py:25`), which is
  not installed here; the user-scope copy at
  `$HOME/.gsd/capabilities/markdown-linting/config/.rumdl.toml` is the same file and enables exactly
  `MD001, MD003, MD009, MD012, MD022, MD024, MD040` — line-length (MD013), inline-HTML (MD033) and
  first-line-heading (MD041) are off by omission. `rumdl` is at `/usr/bin/rumdl`. `lint.py:23` lints
  `.planning`, `README.md`, `CLAUDE.md`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the exhaustive `## Configuration` section to README.md</name>
  <files>README.md</files>
  <read_first>
    plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json (the `config` object is the
    authoritative source for every key, type, enum value, default, and description — quote it, do
    not paraphrase the defaults)
  </read_first>
  <action>
    Insert one new `## Configuration` section into README.md, positioned after the `### Example
    workflow` subsection (which ends at the current "See `AGENTS.md` in this repo for the full
    command reference." line) and before `## Uninstall`. Do not restructure or rewrite any other
    section in this task.

    Open with one sentence stating that every gsd-beads setting lives in the project's
    `.planning/config.json` under a single `beads` object, and that the shipped defaults are declared
    in `plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json`.

    Then a markdown table with header `| Key | Type | Values | Default | Effect |` and exactly four
    rows, one per key, in `capability.json` declaration order: `beads.enabled`, `beads.sync_mode`,
    `beads.ship_gate`, `beads.epic_per`. Types and defaults must match `capability.json` exactly
    (`boolean`/`true`, `enum`/`"authoritative"`, `boolean`/`true`, `enum`/`"phase"`). The Values
    column lists the enum members for the two enums and `true` / `false` for the two booleans. The
    Effect column condenses each `capability.json` description into one clause; for `ship_gate` name
    the two `BEADS.md` frontmatter fields the gates read (`blocking_open`, `diverged`) and that both
    gates are blocking.

    Then exactly one `json`-tagged fenced block — the only such block in the file — holding a
    copy-pasteable `.planning/config.json` fragment: a single top-level object containing one
    `beads` object with all four keys set to their shipped defaults. It must parse as valid JSON
    (no comments, no trailing commas, double-quoted keys and string values).

    Then a `### How a value is resolved` subsection stating, in prose:
    (1) the enabled gate all four skills implement identically — disabled only when
    `.planning/config.json` exists AND has a `beads` object AND that object's `enabled` is
    explicitly the boolean `false`; a missing file, a missing `beads` object, and a present `beads`
    object with no `enabled` key all fall through to the `capability.json` default of `true`, so a
    fresh install runs with tracking on;
    (2) that a disabled capability makes every lifecycle step print a visible notice and no-op
    rather than fail, since all six steps are `onError: skip`;
    (3) that `epic_per` is re-read fresh from `.planning/config.json` at each epic-creation call
    site (not resolved once per session), and falls back to `phase` when the file is absent,
    malformed JSON, or missing the key;
    (4) that `ship_gate` is read at `ship:pre` alongside `BEADS.md`'s `blocking_open`/`diverged`,
    and that setting it to `false` while either field is non-zero does not silently skip the gate —
    the bypass is recorded as a `Beads-Override:` git trailer on the amended ship commit plus a
    best-effort `bd` comment on the phase epic, and the amend is refused when HEAD is already
    pushed.

    Then a `### Caveats` list (or equivalent bullets inside the section — do not add a second
    top-level `## Caveats` heading, the file already has one) covering: `mirror` and `off` are
    reserved for later phases and not yet implemented, so `authoritative` is the only working
    `sync_mode`; under `authoritative`, task content originates in PLAN.md at first sync and PLAN.md
    task text is never re-synced from later bd edits; `epic_per: milestone` is forward-only and does
    not retroactively fold existing per-phase epics into the milestone epic.

    Then a `### Environment variables` subsection: one table with header
    `| Variable | Read by | Default | Purpose |` and exactly three rows for `CLAUDE_PLUGIN_ROOT`,
    `CLAUDE_CONFIG_DIR`, and `GSD_HOME`, each Default column carrying the literal fallback from the
    source. State in one line that gsd-beads only reads these — it never sets them — and that every
    one has a working default, so none is required. Add one line noting the two `CLAUDE_CONFIG_DIR`
    consumers in `sync.py` probe the Claude runtime home only, and do not check other runtime homes.
    Add one line stating that `sync.py`'s 15-second bd subprocess timeout is a module constant, not
    a configurable environment variable.

    Close with a `### Not gsd-beads configuration` subsection: `BEADS_DIR` and `bd config set …`
    are `bd`-owned knobs that change `bd`'s own behaviour for every consumer, documented in the
    bundled `bd` skill resources; gsd-beads neither reads nor writes them, and they have no
    `.planning/config.json` equivalent. Name `BEADS_DIR` (points a worktree or a whole project at a
    beads workspace outside the tree) and `bd config set no-git-ops true` (keeps `bd prime` output
    in stealth mode) as the two examples. Do not reproduce AGENTS.md's bare-`bd` command reference
    here — the existing pointer to AGENTS.md stays as the single cross-reference.

    Every number, default, enum member, and variable name written in this section must be quotable
    from the sources listed in `<context>`. Invent nothing; if a claim is not in those sources, omit
    it rather than reasoning it out.
  </action>
  <verify>
    <automated>python3 - "$PWD/README.md" &lt;&lt;'PY'
import json, re, sys
t = open(sys.argv[1], encoding='utf-8').read()
for k in ('beads.enabled','beads.sync_mode','beads.ship_gate','beads.epic_per',
          'CLAUDE_PLUGIN_ROOT','CLAUDE_CONFIG_DIR','GSD_HOME','BEADS_DIR',
          'no-git-ops','authoritative','reserved','forward-only','## Configuration'):
    assert k in t, f'missing: {k}'
blocks = re.findall(r'```json\n(.*?)```', t, re.S)
assert len(blocks) == 1, f'expected exactly 1 json block, got {len(blocks)}'
cfg = json.loads(blocks[0])['beads']
assert cfg == {'enabled': True, 'sync_mode': 'authoritative',
               'ship_gate': True, 'epic_per': 'phase'}, cfg
print('OK')
PY</automated>
  </verify>
  <done>
    README.md has a `## Configuration` section documenting all four `beads.*` keys with types,
    values, defaults and effects; the resolution rule; the reserved-`sync_mode` and
    forward-only-`epic_per` caveats; the three environment variables with their defaults; and the
    bd-owned knobs as a clearly separated non-gsd-beads note. Its single `json` block parses and
    carries all four keys at their shipped defaults. The automated check prints OK.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix the two accuracy defects, lint, and commit</name>
  <files>README.md</files>
  <action>
    Two targeted corrections plus a lint pass. Keep the diff proportionate — do not rewrite prose
    that is already correct, do not restructure headings, do not touch the Caveats bullets or the
    comparison table.

    (a) Uninstall block. Re-confirm the identifier before editing: `.claude-plugin/marketplace.json`
    names the plugin `beads-lifecycle`, `plugins/beads-lifecycle/.claude-plugin/plugin.json` agrees,
    `claude plugin uninstall --help` takes a single `<plugin>` argument, and
    `~/.claude/plugins/installed_plugins.json` keys the install as `beads-lifecycle@gsd-beads`. No
    plugin named `beads` exists. Change the Uninstall command's plugin argument to
    `beads-lifecycle@gsd-beads`, so it matches the Install block's argument exactly.

    (b) Example workflow command names. Re-confirm that gsd-core's `plugin.json` declares
    `"commands": "./commands/gsd/"` and that the directory contains `plan-phase.md`,
    `execute-phase.md`, `verify-work.md` and `ship.md` — the shipped form is the colon namespace,
    which is what README.md line 9 already claims. Rewrite the three command names in the numbered
    Example workflow list to the colon form (`/gsd:plan-phase`, `/gsd:execute-phase`,
    `/gsd:verify-work`) and leave list item 4 ("Shipping") describing the ship gates as-is. Do not
    change the prose describing what each step does; only the command tokens.

    (c) In the Example workflow's opening paragraph (the one already naming `beads.enabled`), add a
    short pointer to the new `## Configuration` section for the full key list. One clause, not a
    rewrite; do not duplicate the table's contents there.

    (d) Scan the rest of README.md once for any statement that Task 1's evidence contradicts and fix
    only genuine errors. Verified-correct as of planning and NOT to be touched: the `gsd-core >=
    1.6.0` requirement (matches `capability.json` `engines.gsd`), the Install block, the
    `/gsd:*` claim in the What-it-does paragraph, the `bd prime --hook-json` SessionStart caveat,
    the plugin-cache caveat, and the one-capability caveat.

    (e) Lint. Run rumdl against the markdown-linting capability's curated ruleset (the user-scope
    copy is the same file the capability would install into this repo):
    `rumdl check --config "$HOME/.gsd/capabilities/markdown-linting/config/.rumdl.toml" README.md`.
    Only MD001, MD003, MD009, MD012, MD022, MD024, MD040 are enabled — expect zero violations; MD040
    (fenced-code-language) is the one most likely to bite the new json block. If the shell allowlist
    blocks `rumdl`, run `lean-ctx allow rumdl` once and re-run. Fix any reported violation in
    README.md rather than relaxing the config; never add a `.rumdl.toml` to this repo.

    (f) Commit README.md alone on the current branch (`main`). Conventional Commits, imperative
    subject, a body explaining why (the config surface was undocumented outside source files; two
    stale command identifiers). No AI attribution trailer, no emoji, no `Co-Authored-By`. Do not
    push — the orchestrator handles that.
  </action>
  <verify>
    <automated>python3 - "$PWD/README.md" &lt;&lt;'PY'
import re, subprocess, sys
t = open(sys.argv[1], encoding='utf-8').read()
assert 'claude plugin uninstall beads-lifecycle@gsd-beads -y' in t, 'uninstall arg not fixed'
for c in ('/gsd:plan-phase', '/gsd:execute-phase', '/gsd:verify-work'):
    assert c in t, f'missing colon-form command: {c}'
stale = re.findall(r'/gsd-(?:plan-phase|execute-phase|verify-work|ship)\b', t)
assert not stale, f'stale hyphenated commands remain: {stale}'
assert not re.search(r'uninstall\s+beads\s+-y', t), 'stale uninstall arg remains'
out = subprocess.run(['git', 'show', '--name-only', '--format=', 'HEAD'],
                     capture_output=True, text=True).stdout.split()
assert out == ['README.md'], f'HEAD must touch README.md only, got {out}'
msg = subprocess.run(['git', 'log', '-1', '--format=%B'],
                     capture_output=True, text=True).stdout
assert 'Co-Authored-By' not in msg and 'Claude' not in msg, 'attribution trailer present'
print('OK')
PY</automated>
    <automated>rumdl check --config "$HOME/.gsd/capabilities/markdown-linting/config/.rumdl.toml" README.md</automated>
  </verify>
  <done>
    The Uninstall block names `beads-lifecycle@gsd-beads`, the Example workflow list uses the colon
    command namespace with no hyphenated leftovers, the Example workflow paragraph points at
    `## Configuration`, rumdl reports zero violations against the curated ruleset, and HEAD on
    `main` is a single conventional commit touching only README.md with no attribution trailer.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public README | Published documentation; a wrong default or invented key misconfigures a reader's project |
| none (no code path) | Documentation-only change; no input parsing, no new dependency, no executable surface added |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-e7a-01 | Information disclosure | README.md config examples | low | mitigate | Snippet contains only shipped defaults from `capability.json`; no paths, tokens, or host-specific values are copied into the published file |
| T-e7a-02 | Tampering | README.md config documentation | medium | mitigate | Every documented key/default is quoted from `capability.json`, the hook scripts, or `sync.py`; Task 1's automated check asserts the json block equals the four shipped defaults exactly, so a drifted or invented default fails the gate |
| T-e7a-SC | Tampering | package installs | n/a | accept | No npm/pip/cargo install in this plan — no package-legitimacy audit applies |
</threat_model>

<verification>
- Task 1's check passes: all four keys, three env vars, both bd-owned knobs, and the reserved /
  forward-only caveats are present; exactly one json block, parsing to the four shipped defaults.
- Task 2's check passes: uninstall identifier matches install, colon-form commands present with no
  hyphenated leftovers, HEAD touches README.md only, no attribution trailer.
- `rumdl check --config "$HOME/.gsd/capabilities/markdown-linting/config/.rumdl.toml" README.md`
  reports zero violations.
- `git diff HEAD~1 --stat` shows README.md only; no source, hook, skill, or capability file changed.
</verification>

<success_criteria>
A reader who has never opened the source tree can configure gsd-beads entirely from README.md: they
know all four keys, their types, allowed values and shipped defaults; what happens when a key or the
whole file is absent; which two enum values are not yet implemented; that `epic_per: milestone` is
forward-only; which three environment variables the plugin reads and that all are optional; and that
`BEADS_DIR` / `bd config set` belong to `bd`, not here. The Install and Uninstall blocks name the
same plugin, the workflow commands are the ones actually shipped, and the whole file lints clean.
</success_criteria>

<output>
Create `.planning/quick/260819-e7a-revise-readme-md-with-a-full-gsd-beads-c/260819-e7a-SUMMARY.md` when done
</output>
