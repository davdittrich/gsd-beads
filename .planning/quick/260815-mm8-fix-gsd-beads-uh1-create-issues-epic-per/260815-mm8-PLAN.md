---
phase: quick-260815-mm8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gsd/capabilities/beads/scripts/sync.py
  - .gsd/capabilities/beads/tests/test_sync.py
autonomous: true
requirements: [gsd-beads-uh1, gsd-beads-bgb]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Two plans in the same phase, neither carrying a pre-set beads_epic, sync to exactly one shared epic (not two)."
    - "Two plans sharing one epic never close each other's already-synced issue as an orphan when the second plan syncs."
  artifacts:
    - .gsd/capabilities/beads/scripts/sync.py
    - .gsd/capabilities/beads/tests/test_sync.py
  key_links:
    - "create_issues -> resolve_epic -> resolve_phase_epic (phase-scoped epic reuse, gsd-beads-uh1)"
    - "create_issues -> collect_epic_task_ids -> find_orphans (epic-scoped orphan detection, gsd-beads-bgb)"
---

<objective>
Fix two pre-existing Phase 1 bugs in `.gsd/capabilities/beads/scripts/sync.py`, both discovered and documented (not fixed, out of scope) in `02-02-SUMMARY.md` Deviations 3 and 4: `create_issues` resolves each plan's epic independently instead of reusing a phase-shared epic (gsd-beads-uh1), and `find_orphans`'s `current_ids` is scoped to one plan instead of every plan sharing that plan's epic, so syncing a second plan on a shared epic auto-closes the first plan's already-synced issue (gsd-beads-bgb).

Purpose: Phase 3's divergence detection reads the same epic-resolution and close-state that these two bugs corrupt (per both tickets) -- fix before that phase builds on it.
Output: `resolve_epic` made phase-scoped via the already-existing `resolve_phase_epic` helper; a new `collect_epic_task_ids` helper scoping orphan-sweep `current_ids` to every plan sharing the epic being synced; two new RED->GREEN test classes in `test_sync.py`; both bd tickets closed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.gsd/capabilities/beads/scripts/sync.py
@.gsd/capabilities/beads/tests/test_sync.py
@.planning/phases/02-visibility/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix gsd-beads-uh1 -- phase-scoped epic resolution in resolve_epic</name>
  <files>.gsd/capabilities/beads/scripts/sync.py, .gsd/capabilities/beads/tests/test_sync.py</files>
  <behavior>
    - RED: add `TestPhaseScopedEpic` to `test_sync.py`. Test
      `test_second_plan_in_phase_reuses_first_plans_epic_when_neither_preset_one`:
      write two plan files into one phase_dir via `_write_plan_workspace`-style
      layout (adapt it inline, or add a second `_write_plan_workspace`-style
      helper call against the same `tmp_path`/phase_dir) -- `01-01-PLAN.md` from
      `_three_task_plan_text()`-style single-task fixture (reuse
      `FIXTURES_DIR / "plan-single.md"`, no `beads_epic` in frontmatter) and a
      second plan `01-02-PLAN.md` built by copying that same fixture text with
      `plan: 01` replaced by `plan: 02` (still no `beads_epic`). Run
      `sync.create_issues` on `01-01-PLAN.md` first, then on `01-02-PLAN.md`,
      both under one `mock.patch("subprocess.run", side_effect=_make_bd_side_effect())`.
      Assert: exactly one `bd create --type epic` argv total across both runs
      (reuse `TestCreateIssues._create_argvs` filtering pattern), and
      `sync.BEADS_EPIC_RE.search(...)` on both plans' post-sync text returns the
      same epic id. Run `python3 -m unittest
      gsd_beads.tests.test_sync.TestPhaseScopedEpic -v` (via the repo's existing
      test invocation, e.g. `cd .gsd/capabilities/beads && python3 -m unittest
      tests.test_sync.TestPhaseScopedEpic -v`) and confirm it fails against the
      current `resolve_epic` (two separate `bd create --type epic` calls).
    - GREEN: `resolve_epic` currently takes `(frontmatter, roadmap_path,
      phase_num)` and only checks this plan's own `BEADS_EPIC_RE` match before
      creating a fresh epic. Add a `phase_dir` parameter. After the existing
      own-frontmatter check fails (no match, or `bd show` on the stored id
      fails), call the module's existing `resolve_phase_epic(phase_dir)`
      helper (already defined, used today only by `regenerate_beads_md`) --
      if it returns a non-None id, `bd show` that id; on success return
      `(shared_epic_id, True)` (the `True` here means "needs writing to this
      plan's own frontmatter", matching the existing `epic_created` semantics
      already consumed by `rewrite_plan`'s conditional in `create_issues` --
      update `resolve_epic`'s docstring to state the second element means
      "needs frontmatter write", not literally "created in bd", since this
      path finds an existing epic without creating one). Only fall through to
      `bd create --type epic` when neither the own-frontmatter id nor
      `resolve_phase_epic`'s result resolves. Update the one call site in
      `create_issues` (`resolve_epic(frontmatter, roadmap_path, phase_num)`)
      to pass `plan_path.parent` as the fourth argument.
  </behavior>
  <action>
    Per gsd-beads-uh1: make epic resolution phase-scoped by reusing
    `resolve_phase_epic` inside `resolve_epic` before creating a new epic, per
    the RED/GREEN behavior above. This is the minimal fix -- no new epic-scan
    logic, since `resolve_phase_epic` already implements the exact "first
    plan-in-phase with a `beads_epic`" scan this bug needs. Do not touch
    `resolve_issue`, `find_orphans`, or any other Phase 1 function in this
    task -- Task 2 owns those.
  </action>
  <verify>
    <automated>cd .gsd/capabilities/beads && python3 -m unittest tests.test_sync.TestPhaseScopedEpic -v</automated>
  </verify>
  <done>TestPhaseScopedEpic's test passes: two plans in the same phase with no beads_epic pre-set on either sync to one shared epic, and exactly one bd create --type epic call happens across both syncs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix gsd-beads-bgb -- epic-scoped orphan detection in find_orphans</name>
  <files>.gsd/capabilities/beads/scripts/sync.py, .gsd/capabilities/beads/tests/test_sync.py</files>
  <behavior>
    - RED: add `TestEpicScopedOrphans` to `test_sync.py`. Test
      `test_syncing_second_plan_never_closes_first_plans_issue_under_shared_epic`:
      build a phase_dir with two plan files sharing one `beads_epic:
      shared-epic-01` in frontmatter -- plan A (`01-01-PLAN.md`) already
      synced, one task carrying `<beads-id>shared-epic-01.1</beads-id>`; plan
      B (`01-02-PLAN.md`) same `beads_epic` but its one task has no
      `<beads-id>` yet. Mock `subprocess.run` with a custom side_effect (follow
      the `test_orphaned_epic_child_closes_once_with_reason` pattern): `bd
      show shared-epic-01` succeeds; `bd create --type task ...` returns
      `shared-epic-01.2`; `bd list --parent shared-epic-01 --all --json`
      returns both `shared-epic-01.1` and `shared-epic-01.2` as `"status":
      "open"`. Run `sync.create_issues` on plan B's path. Assert no `["bd",
      "close", ...]` call names `shared-epic-01.1` (collect every close argv
      and assert `"shared-epic-01.1" not in` the flattened set). Confirm this
      test fails against the current `find_orphans`/`current_ids` (plan A's
      issue gets closed as a false orphan).
    - GREEN: `current_ids` in `create_issues` is currently `{tid for tid in
      task_ids if tid}` -- only this plan's own resolved ids. Add a new
      module-level function `collect_epic_task_ids(phase_dir, epic_id)`:
      iterate `discover_plan_files(phase_dir).values()`, `parse_plan` each,
      skip any whose `BEADS_EPIC_RE.search(frontmatter)` doesn't match
      `epic_id`, and union every task's `beads_id` (when truthy) into a
      returned `set()`. In `create_issues`, change `current_ids = {tid for tid
      in task_ids if tid}` to `current_ids = {tid for tid in task_ids if tid}
      | collect_epic_task_ids(plan_path.parent, epic_id)` immediately before
      the `find_orphans` call. Do not change `find_orphans`'s own signature or
      body -- the fix is entirely in what `current_ids` contains before it's
      passed in.
  </behavior>
  <action>
    Per gsd-beads-bgb: scope orphan detection to every plan sharing the epic
    being synced by unioning `collect_epic_task_ids` into `current_ids`
    before the `find_orphans` call, per the RED/GREEN behavior above. Reuses
    `discover_plan_files`/`parse_plan`/`BEADS_EPIC_RE`, all already defined in
    this module -- no new parsing logic.
  </action>
  <verify>
    <automated>cd .gsd/capabilities/beads && python3 -m unittest tests.test_sync.TestEpicScopedOrphans -v</automated>
  </verify>
  <done>TestEpicScopedOrphans's test passes: syncing plan B under a shared epic never closes plan A's already-synced issue. Full suite green: `cd .gsd/capabilities/beads && python3 -m unittest discover -s tests -v` shows zero failures. Both bd tickets closed: `bd close gsd-beads-uh1` and `bd close gsd-beads-bgb`.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLAN.md text -> sync.py | PLAN.md frontmatter/task text is authored by a different principal (planner/executor agents) than the process invoking `bd`; unchanged by this fix -- both new code paths (`resolve_phase_epic`, `collect_epic_task_ids`) only ever read already-parsed frontmatter fields and pass ids through `run_bd`'s typed argv, never through shell interpolation (module-level invariant, T-01-01, untouched). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-mm8-01 | Tampering | `collect_epic_task_ids` epic-id match | low | accept | Matching is an exact `BEADS_EPIC_RE` string-equality check against `epic_id` (never a substring/prefix match), so a plan with a similarly-named epic cannot leak into another epic's scope; no new external input surface introduced. |
| T-mm8-02 | Denial of Service | `resolve_phase_epic` phase-directory scan | low | accept | Bounded by `discover_plan_files` over one phase_dir (already the existing scan pattern used by `regenerate_beads_md`); no new unbounded loop introduced. |

</threat_model>

<verification>
Run the full existing suite plus the two new classes:
`cd .gsd/capabilities/beads && python3 -m unittest discover -s tests -v`
Confirm zero regressions in the 39 pre-existing tests (per 02-02-SUMMARY.md's "39/39" baseline) and both new classes pass.
</verification>

<success_criteria>
- Two plans in one phase, neither pre-set with `beads_epic`, sync to exactly one shared epic.
- Syncing a second plan under a shared epic never closes a sibling plan's already-synced issue.
- Full test suite green (pre-existing 39 + 2 new tests).
- `bd show gsd-beads-uh1` and `bd show gsd-beads-bgb` both report closed.
</success_criteria>

<output>
Create `.planning/quick/260815-mm8-fix-gsd-beads-uh1-create-issues-epic-per/260815-mm8-SUMMARY.md` when done
</output>
