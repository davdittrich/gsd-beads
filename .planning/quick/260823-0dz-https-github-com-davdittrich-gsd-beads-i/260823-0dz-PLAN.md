---
quick_id: 260823-0dz
slug: https-github-com-davdittrich-gsd-beads-i
date: 2026-08-23
phase: quick-260823-0dz
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-sync/SKILL.md
  - plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-recall/SKILL.md
  - plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-migrate-todos/SKILL.md
  - plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-status/SKILL.md
  - plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py
  - tasks/lessons.md
autonomous: true
beads_epic: gsd-beads-elt
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Every direct Beads lifecycle skill executes its documented sync.py subcommands when only the project-local, global, or plugin-bundled capability exists."
    - "Project-local sync.py wins over global and plugin candidates; global wins over plugin; only regular files are eligible."
    - "An absent candidate set fails the direct skill command with one stable stderr diagnostic and never invokes Python or installs anything."
    - "Every existing sync.py subcommand and argument sequence remains byte-for-byte equivalent after path resolution."
    - "The existing lifecycle hook, sync.py implementation, manifests, gsd-core, and auto-install behavior remain unchanged and their tests stay green."
    - "tasks/lessons.md contains only the requested correction that Codegraph and Serena are available and must lead structural exploration."
  artifacts:
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-sync/SKILL.md"
      provides: "Project/global/plugin resolver for create-issues direct dispatch"
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-recall/SKILL.md"
      provides: "Project/global/plugin resolver for recall and patch-check direct dispatches"
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-migrate-todos/SKILL.md"
      provides: "Project/global/plugin resolver for migrate-todos direct dispatch"
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-status/SKILL.md"
      provides: "Project/global/plugin resolver for every status lifecycle direct dispatch"
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py"
      provides: "Isolated path-selection, argv-spy, missing-candidate, and lifecycle regression coverage"
    - path: "tasks/lessons.md"
      provides: "Single correction about Codegraph/Serena availability"
  key_links:
    - from: "four direct skill SKILL.md files"
      to: "selected sync.py regular file"
      via: "ordered quoted candidate scan: CLAUDE_PROJECT_DIR, GSD_HOME-or-HOME, CLAUDE_PLUGIN_ROOT"
      pattern: "SYNC_PY=.*candidate.*-f"
    - from: "selected sync.py path"
      to: "existing direct-skill public command contracts"
      via: "python3 \"$SYNC_PY\" followed by the unchanged subcommand/argument suffix"
      pattern: "python3 \\\"\\$SYNC_PY\\\""
    - from: "TestDirectSkillSyncResolver"
      to: "all resolver-bearing Bash blocks in the four skills"
      via: "temporary candidate scripts append selected path and sys.argv to an exact-equality spy log"
      pattern: "TestDirectSkillSyncResolver"
---

- **Mechanism:** ordered regular-file resolver reusing lifecycle project-local -> `${GSD_HOME:-$HOME}` global -> plugin precedence
- **Forbidden:** new helper/dependency/second policy, gsd-core edits, auto-install or argv/subcommand changes
- **Audit:** path/argv spy strategy proving internal resolution plus public execution contract

<objective>
Make all four direct Beads lifecycle skills resolve the installed `sync.py` from the same ordered roots as lifecycle dispatch, without changing what any command asks `sync.py` to do.

Purpose: Direct skill invocation currently assumes a project-local `.gsd` installation and fails in global-only installations reported by GitHub issue 3, while the lifecycle hook already implements the required project/global/plugin precedence.
Output: Four corrected skill command surfaces, one standard-library regression class in the existing test module, and the requested one-entry lessons file.
</objective>

<execution_context>
@/home/dd/.codex/gsd-core/workflows/execute-plan.md
@/home/dd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/quick/260823-0dz-https-github-com-davdittrich-gsd-beads-i/260823-0dz-RESEARCH.md
@plugins/beads-lifecycle/hooks/lifecycle-dispatch.sh
@plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-sync/SKILL.md
@plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-recall/SKILL.md
@plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-migrate-todos/SKILL.md
@plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-status/SKILL.md
@plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py

Authoritative task state: `bd show gsd-beads-elt.4`. User selected “All clear”; no CONTEXT.md or D-XX decisions exist. Research confidence is HIGH and identifies no unresolved decision.
</context>

## Alternatives Considered

Ranks are ordinal, 1 = best. The repository-mandated decision order is performance, simplicity/LOC, ecosystem support, then maintenance overhead.

| Mechanism | Performance | Simplicity/LOC | Ecosystem | Maintenance | Dated evidence | Disposition |
|---|---:|---:|---:|---:|---|---|
| Repeat the lifecycle hook's bounded ordered `-f` scan inside each independently executed skill Bash block | 1 | 1 | 1 | 2 | Claude defines `${CLAUDE_PROJECT_DIR}` and `${CLAUDE_PLUGIN_ROOT}` as substituted/exported roots and requires quoting paths with spaces ([Claude Code Plugins Reference, accessed 2026-08-23](https://code.claude.com/docs/en/plugins-reference)); `-f` is the standard regular-file predicate ([POSIX.1-2024 `test`](https://pubs.opengroup.org/onlinepubs/9799919799.2024edition/utilities/test.html)). | **Selected.** It adds no process, file, dependency, or second policy and matches the already-tested lifecycle precedence. |
| Add and source a shared `resolve-sync.sh` wrapper | 3 | 3 | 2 | 1 | Shell scripts add another file that every direct command must locate before it can locate `sync.py` ([GNU Bash Manual, accessed 2026-08-23](https://www.gnu.org/software/bash/manual/bash.html)); bead `gsd-beads-elt.4` forbids a new helper (updated 2026-08-22). | Rejected: the helper creates the same bootstrap problem and an extra artifact/process boundary. |
| Put `sync.py` on `PATH` and use `command -v` | 2 | 2 | 2 | 3 | POSIX specifies `command -v`/PATH lookup but does not encode project-over-global-over-plugin policy ([POSIX.1-2024 `command`](https://pubs.opengroup.org/onlinepubs/9799919799.2024edition/utilities/command.html)); Claude only adds a plugin's `bin/` directory to PATH, not arbitrary capability scripts ([Claude Code Plugins Reference, accessed 2026-08-23](https://code.claude.com/docs/en/plugins-reference)). | Rejected: it cannot preserve the required three-root precedence and permits unrelated PATH entries to win. |
| Keep project-relative `.gsd/.../sync.py` commands | 1 | 1 | 3 | 3 | GitHub issue 3's maintainer-directed case demonstrates global-only failure ([issue comment 5382867657, 2026-08-22](https://github.com/davdittrich/gsd-beads/issues/3#issuecomment-5382867657)); Claude exposes project and plugin roots precisely for installed component resolution ([Claude Code Plugins Reference, accessed 2026-08-23](https://code.claude.com/docs/en/plugins-reference)). | Rejected: fast and short but functionally incomplete for the explicitly required installation modes. |

**Decision:** Performance first eliminates the wrapper's extra boundary. Among valid no-extra-process choices, simplicity/LOC decides the repeated bounded scan; ecosystem evidence confirms the roots, while maintenance drift is controlled by one normalized inventory/precedence regression across all four skills.

## Unchanged Components and Regressions Prevented

- `plugins/beads-lifecycle/hooks/lifecycle-dispatch.sh` remains unchanged; its lines 97-108 are the policy source reused by the direct skills, including regular-file selection and project -> global -> plugin order.
- `plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py`, capability manifests, hook registration, gsd-core, package/dependency state, auto-install behavior, and every public subcommand implementation remain unchanged.
- Exact inventory assertions prevent dropped, reordered, or rewritten arguments for `create-issues`, `beads-recall`, both `check-patch` calls, `migrate-todos`, `wave-status-block`, `reconcile-stale-closed`, `regenerate-beads-md`, `ship-override`, `status`, and `close-wave`.
- Dynamic isolated-root cases prevent recurrence of global-only failure, wrong precedence, plugin fallback failure, accepting directories as scripts, silent missing-candidate success, unquoted-space paths, and accidental Python invocation after resolution failure.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove and implement direct-skill resolver parity</name>
  <beads-id>gsd-beads-elt.4</beads-id>
  <reversibility rating="reversible">The change is confined to four internal skill command blocks and their existing unittest module; reverting those edits restores the prior behavior without migration.</reversibility>
  <files>plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-sync/SKILL.md, plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-recall/SKILL.md, plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-migrate-todos/SKILL.md, plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-status/SKILL.md, plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py, tasks/lessons.md</files>
  <behavior>
    - RED: A fixed ten-fence inventory bijectively selects every named executable raw Bash fence; the test fails on a missing, duplicate, or unlisted direct-sync fence before it can execute anything.
    - RED: Before placeholder substitution, each selected raw fence must contain the canonical resolver exactly once and its ordered raw command tails following `python3 "$SYNC_PY"` must equal the inventory exactly; current project-relative blocks fail this raw-source gate.
    - GREEN: With all three regular-file candidates present, every resolver-bearing block invokes the project-local spy; without project-local it invokes global; without project/global it invokes plugin.
    - GREEN: With `GSD_HOME` unset or empty, `${GSD_HOME:-$HOME}` selects the HOME-global candidate; candidate root paths containing spaces remain one argv item.
    - GREEN: With no regular-file candidate, every direct block exits non-zero, writes exactly `gsd-beads: sync.py not found in project, global, or plugin capability roots` to stderr, and leaves the argv-spy log empty.
    - GREEN: A closed placeholder-token map is the only permitted transformation; unknown or unreplaced placeholders fail, and Bash executes the otherwise byte-identical raw fence rather than a reconstructed surrogate.
    - GREEN: The spy receives the selected script path and exact pre-change argv for every documented invocation, including `status` once with its phase argument and once without it; the lifecycle hook regression class remains green.
    - GREEN: `tasks/lessons.md` has one lesson entry and no task status list: Codegraph and Serena are available and must precede manual structural search.
  </behavior>
  <action>Work strictly tests-first. RED: in `test_sync.py`, add one `TestDirectSkillSyncResolver` class using only the Python standard library and temporary directories. Declare a closed `RAW_FENCES` inventory with exactly ten named entries, each binding one skill path to its ordered raw command suffix tuple: `beads-sync:create-issues`; `beads-recall:recall`; `beads-recall:patch-checks`; `beads-migrate-todos:migrate`; `beads-status:wave-status`; `beads-status:reconcile-regenerate`; `beads-status:ship-override`; `beads-status:patch-check`; `beads-status:status`; and `beads-status:close-wave`. Encode their exact pre-change tails, including the PLAN path, phase directory, plan-id/repeated-plan-id placeholders, both recall patch targets, both reconcile/regenerate commands, and the optional status phase token. Read every raw `bash` fence in the four named skills that contains either the current hard-coded sync.py prefix or `python3 "$SYNC_PY"`; match those fences bijectively to `RAW_FENCES` by file and ordered command tails, fail if any named entry has zero or multiple matches, and fail if any executable direct-sync fence remains unmatched. Before any transformation, assert the raw fence contains one byte-exact canonical resolver prelude and assert the complete ordered suffix of every raw line after `python3 "$SYNC_PY"` equals that entry's expected tuple; do not normalize whitespace, parse/re-render shell, or accept equivalent rewritten source. Declare one closed placeholder map containing exactly `&lt;PLAN.md path&gt;`, `&lt;phase directory&gt;`, `&lt;plan id&gt;`, `[&lt;plan id&gt; ...]`, and `[phase directory]`, with the repeated-plan token replaced before the single-plan token. Discover placeholder-shaped tokens only in the already-asserted command tails; reject any token outside that map and reject any known or unknown placeholder left after substitution. Derive an executable block solely by ordered literal replacement of those tokens with `shlex.quote`d fixture values; make no other change to the selected raw fence, then pass that complete minimally derived fence directly to `bash -c` from an unrelated temporary cwd. For the `beads-status:status` entry, derive exactly two blocks from the raw optional token: one literal replacement with the quoted phase path and one literal removal of the whole optional token; assert spy argv equals `['status', phase_dir]` and `['status']` respectively. Place a tiny Python argv-spy at candidate `sync.py` paths under temporary project, global, and plugin roots; each spy appends its resolved file path and `sys.argv[1:]` as JSON so tests use exact equality. Cover all-candidates, explicit-GSD_HOME global-only, unset/empty-GSD_HOME HOME-global, plugin-only, directory-instead-of-regular-file, paths-with-spaces, and no-candidate cases; assert the focused class fails at the raw-source/global/plugin gates before skill edits. GREEN: in every inventoried fenced Bash block that directly invokes the hard-coded project-relative `sync.py`, insert the same ordered resolver shape already used by `lifecycle-dispatch.sh`: initialize `SYNC_PY`, scan the quoted fixed suffix under `${CLAUDE_PROJECT_DIR}`, `${GSD_HOME:-$HOME}`, and `${CLAUDE_PLUGIN_ROOT}` in that order, accept only `-f`, stop on the first match, and on exhaustion emit the behavior's exact stderr diagnostic and exit 1. Replace only the interpreter path prefix with `python3 "$SYNC_PY"`; preserve every existing subcommand, placeholder, argument order, number of Bash calls, lifecycle-specific fail-open prose, and surrounding behavior. Do not add a resolver file, dependency, PATH lookup, `eval`, auto-install branch, gsd-core change, hook change, manifest change, or `sync.py` change. REFACTOR: remove any test duplication that does not contribute to the single fixed-inventory/raw-source/closed-map/spy contract, then rerun the focused class, existing `TestLifecycleDispatchHook`, and canonical full capability suite. Finally create `tasks/lessons.md` with a heading and exactly one dated lesson entry recording only the user's correction that Codegraph and Serena are available and must lead codebase exploration before grep/manual reads; add no run logs, code snippets, or unrelated lesson.</action>
  <verify>
    <automated>(cd plugins/beads-lifecycle/.gsd/capabilities/beads &amp;&amp; python3 -m unittest tests.test_sync.TestDirectSkillSyncResolver tests.test_sync.TestLifecycleDispatchHook &amp;&amp; python3 -m unittest discover -s tests -t tests) &amp;&amp; test "$(grep -c 'Codegraph and Serena are available' tasks/lessons.md)" -eq 1 &amp;&amp; test "$(grep -Ev '^(#|[[:space:]]*$)' tasks/lessons.md | wc -l)" -eq 1 &amp;&amp; git diff --exit-code -- plugins/beads-lifecycle/hooks/lifecycle-dispatch.sh plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py gsd-core</automated>
  </verify>
  <done>All ten inventoried raw fences are selected exactly once, pass byte-exact resolver and command-tail assertions before a closed placeholder transformation, and execute only their minimally derived blocks; all four direct skills pass exact path/argv spy tests for project, global, HOME fallback, and plugin resolution; `status` passes with and without its optional phase argument; missing candidates fail explicitly without invocation; every prior subcommand/argument suffix is unchanged; the full capability suite passes; lifecycle dispatch, sync.py, gsd-core, manifests, dependencies, and auto-install are unchanged; tasks/lessons.md contains only the requested correction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Claude-provided/environment roots -> shell path construction | Project, global, and plugin root values may contain spaces or shell metacharacters. |
| Candidate filesystem entry -> Python interpreter | The resolver decides which file is executed as the trusted installed capability. |
| Skill Markdown -> agent-executed Bash | Documentation blocks are executable operational instructions, not inert prose. |
| Extracted Bash block -> regression harness | Test placeholder substitution must not conceal a changed public argv contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0DZ-01 | Tampering / Elevation of Privilege | Root interpolation in four skills | high | mitigate | Use only three fixed suffixes, quote every expanded candidate and selected path, use `-f`, and forbid `eval` or shell-built argv strings; path-with-spaces execution tests exercise the boundary. |
| T-0DZ-02 | Spoofing | Candidate precedence | high | mitigate | Preserve lifecycle's explicit project -> global -> plugin order and prove selected `__file__` by exact JSON spy for each candidate combination. |
| T-0DZ-03 | Tampering | Existing subcommand argv | high | mitigate | Snapshot the complete direct-command suffix inventory in the existing unittest module and assert exact `sys.argv[1:]` after executing extracted blocks. |
| T-0DZ-04 | Denial of Service | Missing capability file | medium | mitigate | Exit non-zero with one stable stderr diagnostic before Python invocation; assert empty spy log and no auto-install side effect. |
| T-0DZ-05 | Repudiation | User correction record | low | mitigate | Keep one dated, exact-scope entry in `tasks/lessons.md` and verify there is exactly one non-heading content line. |
| T-0DZ-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager command or dependency change is in scope; the selected mechanism uses Bash built-ins and the existing Python interpreter only. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Feature / Requirement | Covered by | Status | Notes |
|---|---|---|---|---|---|
| GOAL | GitHub issue 3 comment 5382867657 | Direct skill commands work when Beads capability exists globally rather than project-locally | Task 1 behavior and dynamic global/HOME cases | COVERED | Uses the authoritative maintainer-directed scope. |
| REQ | gsd-beads-elt.4 | Resolve project-local first, global second, plugin third; regular files only | Task 1 implementation and precedence spy | COVERED | Mirrors lifecycle-dispatch policy without modifying it. |
| REQ | gsd-beads-elt.4 | Cover beads-sync, beads-recall, beads-status, and beads-migrate-todos | Task 1 files and inventory | COVERED | Every direct invocation is enumerated. |
| REQ | gsd-beads-elt.4 | Preserve subcommands and arguments exactly | Task 1 exact inventory and argv spy | COVERED | Public execution contract is equality-tested. |
| REQ | gsd-beads-elt.4 | No gsd-core, dependency, auto-install, or lifecycle-dispatch changes | Task 1 action, verify, and done | COVERED | Targeted git diff gate protects unchanged components. |
| REQ | gsd-beads-elt.4 | Record Codegraph/Serena availability correction in tasks/lessons.md | Task 1 final RED/GREEN condition | COVERED | Exactly one scoped lesson, no unrelated content. |
| RESEARCH | 260823-0dz-RESEARCH.md | Reuse lifecycle ordered scan and use quoted `-f` checks | Alternatives and Task 1 GREEN action | COVERED | HIGH-confidence recommendation adopted. |
| RESEARCH | 260823-0dz-RESEARCH.md | Isolated table-driven path/argv tests plus existing lifecycle and full suite | Task 1 RED/GREEN/verify | COVERED | Standard library only; no new test file. |
| RESEARCH | 260823-0dz-RESEARCH.md | Stable non-zero direct-invocation failure with no candidate | Task 1 behavior and T-0DZ-04 | COVERED | Direct skill differs intentionally from hook fail-open semantics. |
| CONTEXT | — | User selected “All clear”; no locked decisions or deferred ideas | Objective and task boundaries | COVERED | No CONTEXT.md exists by workflow contract. |

<verification>
1. Run Task 1's automated command from repository root and require zero exit status.
2. Inspect the focused test's RED evidence in execution history: global-only or plugin-only must fail before skill modification, then pass after the resolver change.
3. Review the final diff once: only the six `files_modified` paths may change; each command suffix must match the inventory; lifecycle-dispatch.sh, sync.py, manifests, gsd-core, package files, and auto-install paths must have no diff.
4. Run GSD plan-check before execution approval and post-execution verification before closing `gsd-beads-elt.4`.
</verification>

<success_criteria>
- A user can invoke any of the four direct skills from an unrelated cwd with only project-local, global/HOME, or plugin capability installation and reach the intended existing sync.py subcommand.
- Project/global/plugin precedence, regular-file eligibility, quoting, and missing-candidate failure are exact-equality tested through the real Markdown Bash blocks.
- The public argv inventory is unchanged for every direct invocation, and existing lifecycle/full capability regressions pass.
- No new helper, dependency, resolver policy, auto-install behavior, argv/subcommand, gsd-core, lifecycle hook, manifest, or sync.py change exists.
- The only lessons artifact is the requested Codegraph/Serena correction.
</success_criteria>

<output>
Create `.planning/quick/260823-0dz-https-github-com-davdittrich-gsd-beads-i/260823-0dz-SUMMARY.md` when done, and keep bead `gsd-beads-elt.4` authoritative for execution state.
</output>
