---
quick_id: 260908-h0m
slug: ponytail-cleanup-of-sota-numerics-check-
date: 2026-09-08
phase: quick-260908-h0m
plan: 01
type: execute
wave: 1
depends_on: []
beads_epic: gsd-beads-25vc.21.4
autonomous: true
target_repo: /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013
target_remote: https://github.com/davdittrich/sota-numerics.git
target_branch: feat/extended-sota-definition
target_baseline_head: c8c8a1b
files_modified:
  # ALL PATHS BELOW ARE RELATIVE TO target_repo, NOT TO gsd-beads.
  - .gsd/capabilities/sota-numerics/scripts/check-alternatives.py
  - hooks/capability-auto-install.sh
  - tests/test-capability-auto-install.sh
  - tests/test_check_alternatives.py
must_haves:
  truths:
    - "Every behaviour the four test suites pin at baseline still holds: 125 Python tests pass, and all three shell suites print ALL PASS."
    - "The D0 doc-parity check still fails a hook refusal that README does not document, and still reports 9 refusals."
    - "No rationale that a test already pins survives in more than one place across code comment, test comment, NOTES.md and README."
    - "check-alternatives.py's no-argument path walks the filesystem for the project root once, not twice."
    - "P2-6 is recorded as declined-to-human and is NOT implemented; P2-7 and any already-landed item are recorded as already-resolved rather than re-deleted."
  artifacts:
    - "target_repo .gsd/capabilities/sota-numerics/scripts/check-alternatives.py (shrinks ~894 -> ~795)"
    - "target_repo hooks/capability-auto-install.sh (shrinks ~244 -> ~175)"
    - "target_repo tests/test-capability-auto-install.sh (shrinks ~894 -> ~805)"
    - "target_repo tests/test_check_alternatives.py (shrinks ~2010 -> ~1985)"
    - ".planning/quick/260908-h0m-ponytail-cleanup-of-sota-numerics-check-/260908-h0m-SUMMARY.md in the gsd-beads repo, carrying the per-item disposition table"
  key_links:
    - "validate_entry's return type and its single call site in check_plan must change together, or the caller indexes a string."
    - "The hook's refusal strings and README's refusal table are the D0 contract; the grep-loop replacement must keep failing when they diverge."
    - "The regex-constant comments in check-alternatives.py and the test comments in test_check_alternatives.py are the duplication pair; deleting from both sides loses the meaning entirely."
estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 3
  confidence: low
---

<objective>
Land the non-blocking ponytail simplifications itemised in beads `gsd-beads-25vc.21.4`
(source: `REVIEW-PONYTAIL-FINAL.md`) against the sota-numerics working tree: roughly
-290 lines of duplicated rationale comments, one embedded-interpreter test block, three
circular self-referential assertions, and four small dead-shape cleanups in the checker.

Purpose: the capability's own shipped rule — `planner-sota.md`'s "keep each meaning in
exactly one place" and `verifier-precision.md`'s "flag prose that says what the code
already says" — is violated by the capability's own source. This is pure simplification.

Output: four modified files in the target repo, each item either landed in a commit or
carrying an explicit recorded decision in the SUMMARY.
</objective>

<target_repo priority="read-this-first">
## THE WORK DOES NOT HAPPEN IN THE CURRENT REPO

**Target repo:** `/home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013`

That directory is a git worktree checkout of `https://github.com/davdittrich/sota-numerics.git`
on branch `feat/extended-sota-definition`. It is a **different repository** from
`gsd-beads`, which merely hosts this planning directory.

- **Every `<files>` path in every task below is relative to that target repo.**
  `tests/test_check_alternatives.py` means
  `/home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013/tests/test_check_alternatives.py`.
- **Every verify command runs with that directory as the working directory.**
- **Every commit in this plan is a commit in the sota-numerics repo**, on branch
  `feat/extended-sota-definition`.
- **The only files that belong in the `gsd-beads` repo** are this `PLAN.md` and the
  `SUMMARY.md` you write at the end, both under
  `.planning/quick/260908-h0m-ponytail-cleanup-of-sota-numerics-check-/`.

Confirm before your first edit:

```sh
cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013
git remote -v          # must show davdittrich/sota-numerics
git rev-parse --abbrev-ref HEAD   # must be feat/extended-sota-definition
git status --short     # must be clean before you start
```
</target_repo>

<hazard priority="read-this-first">
## The target worktree is a live plugin. Do not let it publish itself.

`.worktrees/sota-numerics-release-013` sits under the `gsd-beads` project root and carries
`.claude-plugin/plugin.json`, so Claude Code loads it as a plugin. Its own
`hooks/hooks.json` fires `capability-auto-install.sh` on `SessionStart` and on each
`gsd-executor` / `gsd-planner` / `gsd-verifier` subagent spawn, and that hook installs the
bundle at **global** scope. An uncommitted edit in this worktree can therefore be published
machine-wide.

The refusal guard built in phase 24 is exactly what stops this: the hook refuses when
`git status --porcelain --ignored` reports the bundle dirty, and refuses when the worktree
HEAD is not published. That guard is present at the baseline HEAD, so a dirty worktree
should refuse. Verify it actually did rather than assuming:

```sh
cat ~/.gsd/capability-auto-install-sota-numerics.hash
# MUST still read: da4da96a5d8327d5b6c38a7e6adfa7883f2b6dbac710e891c7236239f55ad498
# That is the plugin-cache bundle hash. If it changes to the worktree bundle hash
# at any point during this plan, STOP: the guard did not hold and uncommitted bytes
# were published globally.
```

Check it once before Task 1 and once after Task 3. Restoring `~/.gsd/capabilities/<id>`
does not disarm the hook — only a clean tree or a stopped session does.
</hazard>

<context>
@/home/dd/projects/gsd-beads/.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/REVIEW-PONYTAIL-FINAL.md
@/home/dd/projects/gsd-beads/CLAUDE.md

Full itemised detail, file:line targets and acceptance criteria: `bd show gsd-beads-25vc.21.4`.
Read `REVIEW-PONYTAIL-FINAL.md` **uncompressed** for the P1-2 and P2-8 sections — those two
items name specific comment blocks and a specific assertion, and a compressed read loses the
exact quoted text you need to find them.
</context>

<baseline priority="measured">
Measured 2026-09-08 in the target repo at HEAD `c8c8a1b`. All four suites are green. These
are the numbers your changes must preserve.

| command (run from target repo root) | baseline result |
| --- | --- |
| `python3 -m unittest tests/test_check_alternatives.py` | `Ran 125 tests` — `OK` |
| `bash tests/test-capability-auto-install.sh` | `ALL PASS`; emits `PASS: D0: the hook's 9 refusals and README's table of them are the same set` |
| `bash tests/test-gate-script-resolution.sh` | pass |
| `bash tests/test-session-start.sh` | pass |

Line counts at baseline: `check-alternatives.py` 894, `hooks/capability-auto-install.sh` 244,
`tests/test-capability-auto-install.sh` 894, `tests/test_check_alternatives.py` 2010,
`CHANGELOG.md` 92.

**The test count must stay exactly 125.** Every item in this plan removes assertions,
comments or helper functions — none removes a test method. A count other than 125 means you
deleted a test, which this plan does not authorise.
</baseline>

<line_number_warning priority="high">
`REVIEW-PONYTAIL-FINAL.md` was written at HEAD `1de851c`. **21 commits have landed since.**

- `check-alternatives.py` grew 675 -> 894 lines.
- `tests/test_check_alternatives.py` grew 1515 -> 2010 lines.

**The review's line numbers for those two files are stale.** Anchor on symbol names and
quoted content, not on the review's line numbers. `tests/test-capability-auto-install.sh`
drifted only 893 -> 894, so its D0 anchor still holds.

Verified current anchors are given inline in each task below.
</line_number_warning>

<tasks>

<task type="auto">
  <name>Task 1: Collapse the checker's duplicated rationale and its three dead shapes (P1-2 Python half, P2-4, P2-5, P2-9)</name>
  <files>.gsd/capabilities/sota-numerics/scripts/check-alternatives.py</files>
  <precondition>`~/.gsd/capability-auto-install-sota-numerics.hash` reads `da4da96a5d8327d5b6c38a7e6adfa7883f2b6dbac710e891c7236239f55ad498` and the target worktree is clean — see the hazard block.</precondition>
  <action>
Work only inside `check-alternatives.py`. Four independent edits, in this order (P2-5 before
P2-4 so the accumulator's shape is settled before its return type changes).

**P2-5 — inline the two single-use helpers.** `entry_has_citation` (currently around line
692) and `entry_years` (around 696) are each one expression, each called exactly once, ten
lines below, inside `validate_entry`. Neither is referenced by the test suite (grep confirms
zero occurrences in `tests/test_check_alternatives.py`). Delete both definitions and inline
their expressions at the single call site. Leave `entry_placeholder_violation` alone — it
carries a real docstring and is a genuine unit.

**P2-4 — `validate_entry` returns a reason, not a list.** It currently builds an `issues`
list and its only caller (in the plan-checking function, around line 791) immediately
indexes element zero, so plurality is documented but never reaches anyone. Keep the internal
accumulator — it is what lets an entry missing both a citation and a date report both at once
— but join it and return a single string, or `None` when the entry passes. Update the
docstring to describe the new return, and update the one call site so it stops indexing.
Grep for other callers before you change the signature; the review found one and so did the
planner, but verify rather than trust.

**P2-9 — walk for the project root once.** The no-argument path (the only path the gate
uses) calls `find_project_root` twice: `resolve_current_phase_dir` calls it internally and
discards the result, then a guard around line 819 calls it again on the resolved directory.
Move that guard into `main`'s explicit-argument branch, which is the only branch that has not
already established a project root. Trace both branches of `main` before moving it and
confirm the explicit-argument branch still refuses a directory outside a GSD project — that
refusal is the guard's whole purpose and a test pins it.

**P1-2 (Python half) — one copy of each rationale.** The comment blocks guarding the
module-level regex constants (`SECTION_HEADING_RE`, `NEXT_HEADING_RE`, `INTERNAL_HEADING_RE`,
`BULLET_RE`, `TABLE_ROW_RE`, `TABLE_SEPARATOR_RE`, around lines 96-145) restate rationale that
the corresponding test comments in `tests/test_check_alternatives.py` already carry
near-verbatim — the review quotes three matched pairs (H3-does-not-end-the-section, the bare
setext run being a horizontal rule, and the thematic-break-plus-frontmatter-fence case). The
test is the executable record; the comment is not. Collapse each code-side block to one line
that names the case and names the test method that pins it, in the shape the review gives.
Read the review's P1-2 section uncompressed to get the exact five blocks and their quoted
text — do not guess which blocks it means.

Delete from the code side only. Do not touch `tests/test_check_alternatives.py` in this task;
if a rationale exists *only* in the code comment and nowhere else, keep it — it is not a
duplicate.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && out=$(python3 -m unittest tests/test_check_alternatives.py 2>&1) && printf '%s\n' "$out" | grep -qE '^Ran 125 tests' && printf '%s\n' "$out" | tail -1 | grep -qx 'OK' && bash tests/test-gate-script-resolution.sh >/dev/null && test "$(wc -l < .gsd/capabilities/sota-numerics/scripts/check-alternatives.py)" -lt 860</automated>
  </verify>
  <done>125 Python tests pass, the gate-script-resolution suite passes, `check-alternatives.py` is under 860 lines (from 894), the two single-use helpers are gone, `validate_entry` returns a string or `None`, and the no-argument path resolves the project root once.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the embedded-interpreter doc-parity check and de-duplicate the hook's rationale (P1-1, P1-2 shell half)</name>
  <files>tests/test-capability-auto-install.sh, hooks/capability-auto-install.sh</files>
  <action>
**P1-1 — the D0 doc-parity check.** `tests/test-capability-auto-install.sh` lines 795-889
spend 96 lines running an interpreter inside a heredoc to hand-parse a Markdown table out of
README, set-compare it against refusal strings grepped out of the hook, print findings on a
private `D0-PROBLEM:` / `D0-TOTAL:` wire protocol, and demux that protocol back out in shell
— three languages for one assertion.

Replace the whole block with the six-line pure-shell loop the review specifies: grep the
refusal messages out of the hook, normalise the capability-id interpolation, and assert each
one appears in README. **Keep only the forward direction** (hook -> README). That is the
direction that actually went stale — the review quotes the block's own comment admitting the
hook grew from five refusals to eight while README was written against the old set. The
reverse direction, the duplicate check, and the header anchoring are the speculative half and
go with the block. Remove the surrounding comments that exist only to explain the deleted
mechanism (including the one around line 140 about the interpreter-missing skip path, and the
one around 871 explaining the read-versus-set choice) — they document machinery that will no
longer be there.

The replacement must still emit a `PASS: D0: ...` line naming the refusal count, and it must
still report **9** refusals. Prove the check is still load-bearing before you move on:
temporarily add a tenth refusal string to the hook, confirm the suite goes red, then revert.
A doc-parity check that cannot fail is worse than no check.

**P1-2 (shell half) — one copy of each rationale.** `hooks/capability-auto-install.sh` carries
five rationale blocks the shell test already records. The clearest pair: the hook's block
around lines 161-183 walks through why each `git status` flag is passed and appends the
`.gitattributes` clean-filter limitation with its ticket number, while
`tests/test-capability-auto-install.sh` lines 20-47 already tabulate the same flag-to-case-ID
map, already carry the same limitation, and already carry the ticket number. The hook's
`bundle_hash` rationale around lines 36-50 and the four-flag prose list around 165-175 are the
same story. Collapse the hook-side copies to the one-line pointer shape the review gives —
name the mechanism, point at the case IDs — and let the test remain the record. Read the
review's P1-2 section uncompressed for the exact five blocks.

Do not thin any refusal branch, any `exit 0` fail-closed path, the `unverifiable_repo` HEAD
refinement, or the capability-id pattern check. The review explicitly checked all of those and
found them load-bearing. This item deletes prose, never a branch.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && out=$(bash tests/test-capability-auto-install.sh 2>&1) && printf '%s\n' "$out" | grep -qF "PASS: D0: the hook's 9 refusals and README's table of them are the same set" && printf '%s\n' "$out" | grep -qx 'ALL PASS' && bash tests/test-session-start.sh >/dev/null && test "$(wc -l < tests/test-capability-auto-install.sh)" -lt 830 && test "$(wc -l < hooks/capability-auto-install.sh)" -lt 200</automated>
  </verify>
  <done>The capability-auto-install suite prints `ALL PASS` and still reports 9 refusals in its D0 line; the session-start suite passes; `tests/test-capability-auto-install.sh` is under 830 lines (from 894) and `hooks/capability-auto-install.sh` is under 200 lines (from 244); the D0 check was proven still able to fail.</done>
</task>

<task type="auto">
  <name>Task 3: Drop the circular self-match assertions, settle P2-8, and record the declined items (P1-3, P2-8, P2-6, P2-7)</name>
  <files>tests/test_check_alternatives.py</files>
  <action>
**P1-3 — drop the docstring self-match assertions.**
`TestDocumentedSyntax.test_readme_and_planner_name_exact_mixed_contract` (currently around
lines 493-531) ends with three assertions that carry no behaviour:

- an `assertNotIn` against README for a sentence someone once deleted — a tombstone that can
  only ever fire if that exact string is reintroduced;
- two `assertIn` checks that open `check-alternatives.py` and confirm its own module docstring
  still contains two phrases of its own prose — circular, and any reword of the docstring
  turns the suite red for no behavioural reason.

Delete those three. **Keep** everything above them: the control-plan run, and the two-file
contract check that asserts the marker heading and the mixed-entry contract sentence appear in
both `README.md` and the shipped `fragments/planner-sota.md` — that pair is a real
cross-artifact contract, is the test's stated purpose, and NOTES.md section 5 depends on it.
Keep the method and its name; only the trailing assertions go.

**P2-8 — verify before you delete.** The review names a redundant `returncode` assertion in
`TestMixedCompatibility` that a later line re-asserts two lines down. At the current HEAD the
planner found only one `bullet_result.returncode` occurrence, inside
`TestSupportedEntryShapes.test_table_accepts_same_semantics_as_bullets`, paired with a
`table_result` assertion — which is not a duplicate. Read the review's P2-8 section
uncompressed, locate the assertion it actually names, and delete it only if it is genuinely
re-asserted. **If it no longer exists, record P2-8 as already-resolved by a later commit and
delete nothing.** Do not manufacture a deletion to match the review's line count.

**P2-6 — DECLINED, do not implement.** The bullet-parser and table-row-parser merge is a
contract change, not a simplification: three tests pin the current keep-both-when-mixed
behaviour and README documents it. The review itself says the question is the author's to
answer. Record it in the SUMMARY as declined and deferred to the author, with the reason and
the three test names. Do not touch the parsers, `TABLE_SEPARATOR_RE`, or the transition walks.

**P2-7 — already resolved, do not re-delete.** The self-referential revision-history paragraph
the review quotes existed at `CHANGELOG.md:20-22` at HEAD `1de851c`; a later phase-24 commit
rewrote that paragraph and it is absent from the current `CHANGELOG.md`. Confirm its absence
and record it as already-resolved. Do not modify `CHANGELOG.md` in this plan.

**Then write the SUMMARY.** In the `gsd-beads` repo, at
`.planning/quick/260908-h0m-ponytail-cleanup-of-sota-numerics-check-/260908-h0m-SUMMARY.md`,
give a disposition table with one row per item — P1-1, P1-2, P1-3, P2-4, P2-5, P2-6, P2-7,
P2-8, P2-9 — each marked landed / already-resolved / declined, each with its commit SHA or its
reason, and each with the measured line delta. That table is the acceptance criterion of
`gsd-beads-25vc.21.4`, which requires every itemised finding to have either a landed commit or
an explicit recorded decision. Close or comment the ticket with it.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && out=$(python3 -m unittest tests/test_check_alternatives.py 2>&1) && printf '%s\n' "$out" | grep -qE '^Ran 125 tests' && printf '%s\n' "$out" | tail -1 | grep -qx 'OK' && python3 -m unittest tests.test_check_alternatives.TestDocumentedSyntax -v 2>&1 | grep -q 'test_readme_and_planner_name_exact_mixed_contract' && test "$(grep -v '^[[:space:]]*#' tests/test_check_alternatives.py | grep -c 'normalized_checker_text')" -eq 0 && test "$(wc -l < tests/test_check_alternatives.py)" -lt 1995 && git diff --name-only HEAD -- CHANGELOG.md | wc -l | grep -qx 0</automated>
  </verify>
  <done>125 Python tests still pass, `test_readme_and_planner_name_exact_mixed_contract` still exists and still checks both README and the planner fragment, `CHANGELOG.md` is untouched, and the SUMMARY carries a disposition row for all nine items with P2-6 marked declined-to-author.</done>
</task>

</tasks>

<stop_conditions>
Stop and report rather than proceeding if any of these hold:

1. **A test pins the behaviour you are about to simplify away** (beyond P2-6, already
   excluded). This plan authorises deleting comments, assertions and dead shapes — never a
   test method, and never a behaviour a test asserts. If making an item green requires editing
   a test's expectations, that item is a contract change, not a simplification: leave it,
   record it in the SUMMARY the way P2-6 is recorded, and move on.
2. **The Python test count moves off 125.**
3. **The D0 refusal count moves off 9**, or the D0 check can no longer be made to fail.
4. **`~/.gsd/capability-auto-install-sota-numerics.hash` changes** from the value in the
   hazard block — uncommitted bytes were published machine-wide and that outranks this task.
5. **A review item's target cannot be found** at the current HEAD. Record it as
   already-resolved or as not-found; do not substitute a nearby deletion for it.
</stop_conditions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target worktree -> global GSD capability scope | The in-tree plugin's `SessionStart`/`SubagentStart` hook installs this bundle at `--scope global`; uncommitted worktree bytes crossing this boundary land on every project on the machine. |
| hook refusal strings -> README refusal table | The D0 check is the only thing binding the code's behaviour to the documentation a user reads to understand a refusal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-h0m-01 | Elevation of Privilege | `hooks/capability-auto-install.sh` under an in-tree plugin worktree | high | mitigate | The phase-24 dirty/unpublished refusal guard must hold. Assert the stored hash before Task 1 and after Task 3 (hazard block); a change means the guard failed and uncommitted bytes were published. |
| T-h0m-02 | Tampering | `hooks/capability-auto-install.sh` refusal branches | high | mitigate | P1-2 deletes comment prose only. No `exit 0` fail-closed path, refusal branch, `unverifiable_repo` HEAD refinement, or capability-id pattern check is touched; the suite's `ALL PASS` and the 9-refusal D0 count gate this. |
| T-h0m-03 | Repudiation | D0 doc-parity check in `tests/test-capability-auto-install.sh` | medium | mitigate | The 6-line replacement is proven still able to fail (add a tenth refusal, confirm red, revert) before Task 2 completes. A silently-passing parity check would let refusal text and README diverge undetected. |
| T-h0m-04 | Tampering | `check-alternatives.py` `validate_entry` return-type change | medium | mitigate | Grep for every caller before changing the signature; a missed caller would index a string instead of a list and silently report one character as the violation reason. 125-test gate. |
| T-h0m-SC | Tampering | package installs | low | accept | This plan installs no packages and adds no dependency. No legitimacy gate applies. |
</threat_model>

<commits>
One commit per task, in the **target repo**, on `feat/extended-sota-definition`. Conventional
Commits, no AI attribution, no `Co-Authored-By`. Suggested subjects:

- Task 1 — `refactor(checker): keep each rationale in one place and drop three dead shapes`
- Task 2 — `refactor(hook): replace the embedded doc-parity check with a grep loop`
- Task 3 — `test(checker): drop the circular docstring self-match assertions`

Each commit body names the review item IDs it lands and the measured line delta.
</commits>

<success_criteria>
- All four suites green in the target repo: `Ran 125 tests` / `OK`, and `ALL PASS` from each
  of the three shell suites.
- The D0 line still reports 9 refusals.
- Measured line reduction in the region of -290 across the four files (P1-1 ~-90,
  P1-2 ~-155, P1-3 ~-25, P2-4 -4, P2-5 -8, P2-9 -3; P2-7's -3 already banked upstream,
  P2-8's -6 conditional). The number is a measurement to report, not a target to hit — do not
  pad the deletion to reach it.
- `CHANGELOG.md` unmodified.
- Every one of the nine review items has a disposition row in the SUMMARY, and P2-6 is
  recorded as declined and deferred to the author.
- `~/.gsd/capability-auto-install-sota-numerics.hash` unchanged.
</success_criteria>

<output>
Write `.planning/quick/260908-h0m-ponytail-cleanup-of-sota-numerics-check-/260908-h0m-SUMMARY.md`
in the **gsd-beads** repo when done, and record the disposition table on
`bd gsd-beads-25vc.21.4`.
</output>
