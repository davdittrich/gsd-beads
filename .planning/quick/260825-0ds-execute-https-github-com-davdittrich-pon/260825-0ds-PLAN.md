---
quick_id: 260825-0ds
slug: execute-https-github-com-davdittrich-pon
date: 2026-08-25
phase: quick-260825-0ds
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - ponytail-everywhere/hooks/hooks.json
  - ponytail-everywhere/hooks/session-start.sh
  - ponytail-everywhere/tests/test-session-start.sh
  - ponytail-everywhere/tests/test-plan-review-contribution.sh
  - ponytail-everywhere/README.md
  - ponytail-everywhere/.claude-plugin/plugin.json
  - ponytail-everywhere/.gsd/capabilities/ponytail/capability.json
autonomous: true
beads_epic: gsd-beads-7q5
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Claude SubagentStart independently delivers collaborator-output guidance to both gsd-code-reviewer and gsd-verifier through the existing session-start role seam."
    - "The guidance requires malformed collaborator output to be detected and reported instead of silently treated as success."
    - "Format, style, and quality findings may be non-blocking only when continuation preserves artifact integrity and every external contract; security, trust-boundary, data-loss, race, accessibility, source/document, constructor-divergence, ASVS, and TDD blockers remain non-waivable."
    - "Unhandled edge cases, ignored return values, swallowed errors, invalid boundary inputs, lazy structure, and plan-transcription code remain required findings; evidence-backed performance, testing, intent, and minor-style concerns are labeled suggestions."
    - "README states that SubagentStart delivery is Claude-only and links runtime-neutral follow-up issue #3."
    - "Planner, executor, generic, enabled-toggle, and lite/full/ultra behavior remain unchanged; no reconciliation, shared state, downstream agent_skills machinery, or dependency is added."
    - "The focused test fails before the guidance exists, then all four repository test scripts pass with plugin and capability versions synchronized at 0.5.0."
    - "Exactly three release-text substitutions occur in test-plan-review-contribution.sh: fixture expressions at live lines 33 and 145 change from 0.4.1 to 0.5.0, and line 146's diagnostic changes from `plugin version is not 0.4.1` to `plugin version is not 0.5.0`; every other byte and behavior remains unchanged."
    - "A valid Antigravity review through agy-bridge has no unresolved in-scope blocker before the release commit is pushed and GitHub CI succeeds."
    - "Claude and Codex marketplace installations report 0.5.0, their tracked payloads are byte-identical to pushed source, and the shared Ponytail capability is byte-identical to the source bundle."
  artifacts:
    - path: "ponytail-everywhere/hooks/hooks.json"
      provides: "Independent gsd-code-reviewer and gsd-verifier SubagentStart routing"
    - path: "ponytail-everywhere/hooks/session-start.sh"
      provides: "Runtime-neutral reviewer/verifier collaborator-output guidance on the existing role seam"
    - path: "ponytail-everywhere/tests/test-session-start.sh"
      provides: "Public hook-routing, guidance, preservation, and version contract proof"
    - path: "ponytail-everywhere/tests/test-plan-review-contribution.sh"
      provides: "Two synchronized 0.5.0 fixture expressions at live lines 33 and 145 plus the matching 0.5.0 failure diagnostic at line 146, with no other test change"
    - path: "ponytail-everywhere/README.md"
      provides: "User-visible finding taxonomy, explicit safety ceiling, Claude-only SubagentStart boundary, and runtime-neutral issue #3 link"
    - path: "ponytail-everywhere/.claude-plugin/plugin.json"
      provides: "Claude marketplace release version 0.5.0"
    - path: "ponytail-everywhere/.gsd/capabilities/ponytail/capability.json"
      provides: "Capability release version 0.5.0"
    - path: "/home/dd/.claude/plugins/cache/gsd-beads/ponytail-everywhere/0.5.0"
      provides: "Byte-verified Claude installation"
    - path: "/home/dd/.codex/plugins/cache/gsd-beads/ponytail-everywhere/0.5.0"
      provides: "Byte-verified Codex installation"
    - path: "/home/dd/.gsd/capabilities/ponytail"
      provides: "Byte-verified shared runtime capability"
  key_links:
    - from: "ponytail-everywhere/hooks/hooks.json"
      to: "ponytail-everywhere/hooks/session-start.sh"
      via: "SubagentStart matcher commands pass the existing verifier role"
      pattern: "gsd-code-reviewer|gsd-verifier"
    - from: "ponytail-everywhere/tests/test-session-start.sh"
      to: "ponytail-everywhere/hooks/hooks.json"
      via: "jq asserts exact matcher cardinality and command routing"
      pattern: "SubagentStart"
    - from: "ponytail-everywhere/tests/test-session-start.sh"
      to: "ponytail-everywhere/hooks/session-start.sh"
      via: "shell invocation asserts malformed-output, safe-continuation, required-finding, suggestion, and non-waivable blocker text"
      pattern: "malformed|required findings|suggestions|non-waivable"
    - from: "ponytail-everywhere/.claude-plugin/plugin.json"
      to: "ponytail-everywhere/.gsd/capabilities/ponytail/capability.json"
      via: "exact 0.5.0 version parity"
      pattern: "\"version\": \"0.5.0\""
    - from: "ponytail-everywhere/tests/test-plan-review-contribution.sh"
      to: "ponytail-everywhere/.claude-plugin/plugin.json"
      via: "only live lines 33 and 145 advance their release-version expectations and line 146 advances the companion failure diagnostic to 0.5.0"
      pattern: "0.5.0"
    - from: "ponytail-everywhere"
      to: "/home/dd/.claude/plugins/cache/gsd-beads/ponytail-everywhere/0.5.0"
      via: "native marketplace refresh/install followed by tracked-file SHA-256 comparison"
      pattern: "ponytail-everywhere@gsd-beads"
    - from: "ponytail-everywhere"
      to: "/home/dd/.codex/plugins/cache/gsd-beads/ponytail-everywhere/0.5.0"
      via: "native marketplace upgrade/add followed by tracked-file SHA-256 comparison"
      pattern: "ponytail-everywhere@gsd-beads"
---

## Mechanism

Reuse the existing Claude `SubagentStart -> hooks/session-start.sh <role>` mechanism. Add an exact `gsd-code-reviewer` matcher that invokes the same verifier role already used by `gsd-verifier`, and extend only that role's framing with the owner-approved collaborator-output and finding-classification contract. Keep the hook advisory, stateless, and explicitly Claude-only; runtime-neutral delivery remains tracked by issue #3. Release the backward-compatible feature as 0.5.0, then refresh both configured `gsd-beads` marketplaces and install through each runtime's native plugin command before comparing every tracked payload byte.

## Forbidden

Do not add a hook script, JSON/output parser, reconciliation, cache, approval/history state, shared state, downstream `agent_skills` wiring, Quick bridge change, lifecycle contribution, dependency, marketplace-manifest edit, tag, GitHub release, or direct `agy`/`agy --print` invocation. Do not widen issue #7 beyond the owner comment, alter planner/executor/generic/toggle/intensity behavior, weaken artifact or external-contract integrity, or waive security, trust-boundary, data-loss, race, accessibility, source/document, constructor-divergence, ASVS, or TDD blockers.

## Audit

Spy on the public hook boundary, not shell internals: parse `hooks.json` with `jq` to prove one exact matcher per target agent and the expected command argument, invoke `session-start.sh verifier` in the existing scratch configuration harness, and assert malformed handling, the complete required-finding and suggestion classifications, safe continuation, and non-waivable blockers. Retain every existing role, toggle, injection, path-with-spaces, and intensity case. Assert README says Claude-only and links issue #3. Validate Antigravity's JSON envelope before accepting its review, record the verdict on `gsd-beads-7q5`, resolve exactly one pushed-commit `CI` push run before watching its numeric run ID, and compare SHA-256 manifests generated from `git ls-files` for source, Claude cache, and Codex cache plus recursive byte identity for the shared capability.

## Alternatives Considered

Ranks are ordinal (`1` is best) and use the repository's decision order: performance, simplicity/LOC, ecosystem support, then maintenance overhead.

### Collaborator-guidance delivery

| Mechanism | Performance | Simplicity / LOC | Ecosystem | Maintenance | Current dated evidence | Disposition |
|---|---:|---:|---:|---:|---|---|
| Reuse `SubagentStart` and the existing verifier role in `session-start.sh` | 1 | 1 | 1 | 1 | Claude Code documents agent-type matching at `SubagentStart` and command-hook output at the native hook boundary ([official hooks reference, accessed 2026-08-25](https://code.claude.com/docs/en/hooks)); current Ponytail main already routes `gsd-verifier` through this seam ([hooks.json at `b9435ef`, 2026-08-23](https://github.com/davdittrich/ponytail-everywhere/blob/b9435efa8ba0552f508932e1fcd8f10cfcbf3320/hooks/hooks.json)). | **Selected.** One matcher, one framing edit, one existing test file; no second process or state surface. |
| Add a standalone collaborator hook script and parser | 1 | 3 | 1 | 3 | Claude's native command hook already accepts stdout context, so a parser is not required for advisory guidance ([official hooks reference, accessed 2026-08-25](https://code.claude.com/docs/en/hooks)); the owner requires malformed output to be reported but excludes new machinery ([issue #7 owner comment, 2026-08-24](https://github.com/davdittrich/ponytail-everywhere/issues/7#issuecomment-5402107885)). | Rejected: duplicates the existing role process and creates an unrequested output protocol. |
| Deliver through project/global `agent_skills` plus reconciliation state | 2 | 3 | 2 | 3 | Current Ponytail documents `agent_skills` only for the runtime-neutral Quick planner bridge ([README at `b9435ef`, 2026-08-23](https://github.com/davdittrich/ponytail-everywhere/blob/b9435efa8ba0552f508932e1fcd8f10cfcbf3320/README.md)); the issue comment explicitly excludes downstream `agent_skills`, reconciliation, shared state, and dependencies ([issue #7 owner comment, 2026-08-24](https://github.com/davdittrich/ponytail-everywhere/issues/7#issuecomment-5402107885)). | Rejected: wrong delivery layer and materially more configuration/state. |

**Decision:** Performance ties for the two native command-hook shapes, so simplicity/LOC decides: extend the existing role seam.

### Release and runtime synchronization

| Mechanism | Performance | Simplicity / LOC | Ecosystem | Maintenance | Current dated evidence | Disposition |
|---|---:|---:|---:|---:|---|---|
| 0.5.0 plus native Claude/Codex marketplace refresh and install, then tracked-file hashes | 1 | 1 | 1 | 1 | SemVer assigns a minor increment to backward-compatible functionality ([SemVer 2.0.0, accessed 2026-08-25](https://semver.org/spec/v2.0.0.html)); the configured marketplace entry is a versionless Git URL ([gsd-beads marketplace, accessed 2026-08-25](https://github.com/davdittrich/gsd-beads/blob/main/.claude-plugin/marketplace.json)); issue #7 requires version bump, marketplace synchronization, push, and byte-verified Claude/Codex installs ([owner comment, 2026-08-24](https://github.com/davdittrich/ponytail-everywhere/issues/7#issuecomment-5402107885)). | **Selected.** Matches the repository's version-metadata-plus-push convention and proves deployed bytes. |
| 0.4.2 patch release | 1 | 1 | 1 | 2 | The change adds observable reviewer/verifier functionality rather than only correcting existing behavior; SemVer's backward-compatible feature rule is minor, not patch ([SemVer 2.0.0, accessed 2026-08-25](https://semver.org/spec/v2.0.0.html)); 0.4.1 already denotes issue #6's patch release ([current main commit, 2026-08-23](https://github.com/davdittrich/ponytail-everywhere/commit/b9435efa8ba0552f508932e1fcd8f10cfcbf3320)). | Rejected: understates the public feature addition. |
| Manual copies into Claude/Codex caches or capability-only refresh | 2 | 2 | 3 | 3 | The marketplace already points to the source Git repository and the README directs native marketplace installation ([README at `b9435ef`, 2026-08-23](https://github.com/davdittrich/ponytail-everywhere/blob/b9435efa8ba0552f508932e1fcd8f10cfcbf3320/README.md)); issue #7 requires both local runtime installs byte-verified ([owner comment, 2026-08-24](https://github.com/davdittrich/ponytail-everywhere/issues/7#issuecomment-5402107885)). | Rejected: bypasses installer ownership and cannot prove both runtime registrations. |

**Decision:** Native marketplace synchronization wins performance and ecosystem support, and 0.5.0 is the smallest SemVer-correct release number.

<objective>
Ship Ponytail issue #7 from current remote main through tested Claude collaborator guidance, independent Antigravity review, synchronized 0.5.0 release metadata, pushed CI, and byte-identical Claude/Codex installations.

Purpose: Code-reviewer and verifier subagents must distinguish waivable presentation defects from malformed or safety/contract-blocking results without adding policy machinery or changing other Ponytail delivery paths.
Output: Seven synchronized source files, one pushed 0.5.0 release commit, successful CI, refreshed Claude/Codex plugin caches, a refreshed shared capability, and closed bead `gsd-beads-7q5`.
</objective>

<execution_context>
@/home/dd/.codex/gsd-core/workflows/execute-plan.md
@/home/dd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@ponytail-everywhere/hooks/hooks.json
@ponytail-everywhere/hooks/session-start.sh
@ponytail-everywhere/tests/test-session-start.sh
@ponytail-everywhere/tests/test-plan-review-contribution.sh
@ponytail-everywhere/README.md
@ponytail-everywhere/.claude-plugin/plugin.json
@ponytail-everywhere/.gsd/capabilities/ponytail/capability.json

Authoritative scope: `bd show gsd-beads-7q5` and `bd comments gsd-beads-7q5`. The sole owner comment observed 2026-08-24 defines all mutable scope and exclusions; the user-authorized live-evidence amendments add only three release-text substitutions in `tests/test-plan-review-contribution.sh`: the two 0.4.1 fixture expressions at live lines 33 and 145 and line 146's matching 0.4.1 failure diagnostic, all advanced to 0.5.0. Planning observed the clean nested checkout at local `7fd46c1` while remote `main` is `b9435efa8ba0552f508932e1fcd8f10cfcbf3320`; execution must fast-forward and re-read the seven target files before RED. No RESEARCH.md is authorized or required.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove, review, release, and install independent collaborator guidance</name>
  <beads-id>gsd-beads-7q5</beads-id>
  <precondition>`ponytail-everywhere` is on clean local `main` with no unpublished commits, `origin` is `https://github.com/davdittrich/ponytail-everywhere.git`, and `bd show gsd-beads-7q5` remains in progress with the single owner comment as scope authority.</precondition>
  <reversibility rating="costly">The public 0.5.0 push and two runtime cache updates require a follow-up release to supersede, but introduce no data migration or incompatible API.</reversibility>
  <files>ponytail-everywhere/hooks/hooks.json, ponytail-everywhere/hooks/session-start.sh, ponytail-everywhere/tests/test-session-start.sh, ponytail-everywhere/tests/test-plan-review-contribution.sh, ponytail-everywhere/README.md, ponytail-everywhere/.claude-plugin/plugin.json, ponytail-everywhere/.gsd/capabilities/ponytail/capability.json</files>
  <behavior>
    - RED/GREEN: `hooks.json` retains the existing exact `gsd-verifier` SubagentStart matcher and adds exactly one exact `gsd-code-reviewer` matcher; both invoke `session-start.sh verifier`, with no matcher affecting other agents.
    - RED/GREEN: The verifier-role output says malformed or missing collaborator output is detected and reported, never silently accepted as a passing result.
    - RED/GREEN: Format, style, and quality failures are non-blocking only when continuation preserves artifact integrity and all external contracts.
    - RED/GREEN: Unhandled edge cases, ignored return values, swallowed errors, invalid boundary inputs, lazy structure, and plan-transcription code are always emitted as required findings when evidenced.
    - RED/GREEN: Evidence-backed performance, testing, intent, and minor-style concerns are emitted as suggestions, not required findings, unless an independent non-waivable condition applies.
    - RED/GREEN: Security, trust-boundary, data-loss, race, accessibility, source/document, constructor-divergence, ASVS, and TDD blockers are explicitly non-waivable.
    - REGRESSION: Existing planner, executor, generic, enabled=false, config-failure, path-with-spaces, injection, and lite/full/ultra cases remain green and retain their current output contracts.
    - RELEASE: Plugin and capability manifests are both 0.5.0; exactly three release-text substitutions occur in `tests/test-plan-review-contribution.sh`—fixture expressions at live lines 33 and 145 advance from 0.4.1 to 0.5.0 and line 146's failure diagnostic advances from `plugin version is not 0.4.1` to `plugin version is not 0.5.0`—with every other byte unchanged; README names the complete finding taxonomy and safety ceiling, states SubagentStart delivery is Claude-only, and links runtime-neutral follow-up issue #3; no other tracked file changes.
    - REVIEW/DEPLOY: `agy-bridge --type review` returns a valid successful JSON envelope and no unresolved in-scope blocker; pushed CI succeeds; both runtime plugin registries report 0.5.0; source, runtime caches, and the installed capability compare byte-identically.
  </behavior>
  <action>Preflight and synchronize before editing: record `git status --short --branch`, `git ls-remote origin refs/heads/main`, the ticket/comment, and the current installed Claude/Codex versions; fetch `origin`, require the remote head to descend from local HEAD, and use `git merge --ff-only origin/main`. Re-read the seven target files after that fast-forward and pin the starting versions at 0.4.1. Confirm live lines 33 and 145 of `tests/test-plan-review-contribution.sh` are its two 0.4.1 release-version fixtures and line 146 contains the exact companion diagnostic `plugin version is not 0.4.1` before editing. If the checkout is dirty, diverged, the ticket/comment changed, or any of the three release-text targets is absent from its stated live line, halt without a partial edit.

RED: extend `tests/test-session-start.sh` first. Use `jq` against `hooks/hooks.json` to assert exact matcher cardinality and that `gsd-code-reviewer` and `gsd-verifier` each route to `bash "${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh" verifier`. Invoke the existing verifier-role scratch case and assert stable contract phrases for malformed-result reporting; the conditional format/style/quality exception; artifact and external-contract integrity; required findings for unhandled edge cases, ignored return values, swallowed errors, invalid boundary inputs, lazy structure, and plan-transcription code; suggestions for evidence-backed performance, testing, intent, and minor-style concerns; and every non-waivable blocker class. Add README assertions for the same classification, the literal Claude-only SubagentStart boundary, and the full `https://github.com/davdittrich/ponytail-everywhere/issues/3` runtime-neutral follow-up link. Retain all current cases and require the focused script to fail only because the new matcher/guidance/version contract is absent.

GREEN: add the exact `gsd-code-reviewer` SubagentStart matcher to `hooks/hooks.json`, pointing at the existing verifier role; do not create a role or script. Extend only the verifier framing in `hooks/session-start.sh` with the tested malformed-output, required-finding, suggestion, safe-continuation, and non-waivable contracts. Update README's Claude subagent list and document the same complete classification and safety ceiling; state plainly that SubagentStart delivery is Claude-only, do not claim Codex SubagentStart delivery, and link runtime-neutral follow-up issue #3. Bump `.claude-plugin/plugin.json` and `.gsd/capabilities/ponytail/capability.json` together from 0.4.1 to 0.5.0. In `tests/test-plan-review-contribution.sh`, replace 0.4.1 with 0.5.0 only in the fixture expressions at live lines 33 and 145, and change only line 146's failure message from `plugin version is not 0.4.1` to `plugin version is not 0.5.0`; prove the resulting file is byte-identical to its pre-task version after applying exactly those three substitutions, so no assertion, control flow, or other behavior changes. Run the focused script, the other three existing scripts, JSON parsing/version/cardinality checks, `git diff --check`, and an exact changed-path check for the seven declared files.

Review the complete candidate diff before committing. Pipe a narrow adversarial prompt to `AGY_SKIP_PERMISSIONS=1 agy-bridge --type review --add-dir /home/dd/projects/gsd-beads/ponytail-everywhere --json`; never call `agy` directly. Validate with `jq` that `success` is true, `response` is a non-empty string, and the response contains an explicit verdict. Treat an invalid envelope as malformed collaborator output: report it on the bead and halt. Require the review to report evidenced unhandled edge cases, ignored return values, swallowed errors, invalid boundary inputs, lazy structure, and plan-transcription code as findings; keep evidenced performance, testing, intent, and minor-style concerns as suggestions unless an independent non-waivable condition applies. Apply only in-scope blocker corrections; format/style/quality findings may remain non-blocking only under the tested integrity/contract condition, while every listed blocker class halts release until resolved. Rerun all gates and add a concise Antigravity verdict/evidence comment to `gsd-beads-7q5`.

Create one Conventional Commit with the implementation, tests, docs, and synchronized release metadata, with no AI attribution or co-author trailer. Push `main` normally and verify `origin/main` equals local HEAD. Capture the pushed commit as `RELEASE_SHA`; query `gh run list --repo davdittrich/ponytail-everywhere --workflow ci.yml --commit "$RELEASE_SHA" --event push --limit 10 --json databaseId,headSha,workflowName,status,conclusion`, require with `jq` exactly one result whose `headSha` equals `RELEASE_SHA` and whose `workflowName` is `CI`, extract its numeric `databaseId` as `RUN_ID`, then call exactly `gh run watch "$RUN_ID" --repo davdittrich/ponytail-everywhere --exit-status`. If zero or multiple intended runs resolve, halt and report the ambiguity; do not poll or select by recency. Do not tag or create a GitHub release because this repository releases by synchronized metadata plus pushed commit.

After CI succeeds, synchronize the existing versionless `gsd-beads` marketplace registrations without editing the outer marketplace manifest: run `claude plugin marketplace update gsd-beads`, `claude plugin install ponytail-everywhere@gsd-beads --scope user --yes`, `codex plugin marketplace upgrade gsd-beads --json`, and `codex plugin add ponytail-everywhere@gsd-beads --json`. Resolve each installed 0.5.0 cache path from the runtime registry, explicitly invoke the installed Claude plugin's `hooks/capability-auto-install.sh ponytail` with `CLAUDE_PLUGIN_ROOT` set to that cache, and require `/home/dd/.gsd/capabilities/ponytail` to report 0.5.0. Generate SHA-256 manifests for the exact `git ls-files` set in source and both runtime caches and require both diffs empty; require `diff -qr` between the source capability bundle and the installed shared capability to be empty. Only after remote head, CI, both runtime versions, both tracked-payload comparisons, and capability comparison pass, close `gsd-beads-7q5` with the release commit and verification summary.</action>
  <verify>
    <automated>cd ponytail-everywhere &amp;&amp; bash tests/test-session-start.sh &amp;&amp; bash tests/test-quick-planner-bridge.sh &amp;&amp; bash tests/test-plan-review-contribution.sh &amp;&amp; bash tests/test-proportionality-check.sh &amp;&amp; jq -e '.version == "0.5.0"' .claude-plugin/plugin.json &amp;&amp; jq -e '.version == "0.5.0"' .gsd/capabilities/ponytail/capability.json &amp;&amp; test "$(git show HEAD^:tests/test-plan-review-contribution.sh | sed -n '33p;145p' | grep -Fc '0.4.1')" = "2" &amp;&amp; test "$(git show HEAD^:tests/test-plan-review-contribution.sh | sed -n '146p' | grep -Fxc '  || fail "plugin version is not 0.4.1"')" = "1" &amp;&amp; test "$(sed -n '33p;145p' tests/test-plan-review-contribution.sh | grep -Fc '0.5.0')" = "2" &amp;&amp; test "$(sed -n '146p' tests/test-plan-review-contribution.sh | grep -Fxc '  || fail "plugin version is not 0.5.0"')" = "1" &amp;&amp; diff -u &lt;(git show HEAD^:tests/test-plan-review-contribution.sh | sed '33s/0.4.1/0.5.0/;145s/0.4.1/0.5.0/;146s/plugin version is not 0.4.1/plugin version is not 0.5.0/') tests/test-plan-review-contribution.sh &amp;&amp; jq -e '([.hooks.SubagentStart[] | select(.matcher == "gsd-code-reviewer") | .hooks[] | select(.command == "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh\" verifier")] | length == 1) and ([.hooks.SubagentStart[] | select(.matcher == "gsd-verifier") | .hooks[] | select(.command == "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh\" verifier")] | length == 1)' hooks/hooks.json &amp;&amp; grep -Fq 'Claude-only' README.md &amp;&amp; grep -Fq 'https://github.com/davdittrich/ponytail-everywhere/issues/3' README.md &amp;&amp; grep -Fq 'unhandled edge cases' README.md &amp;&amp; grep -Fq 'evidence-backed performance' README.md &amp;&amp; git diff --check &amp;&amp; git diff-tree --no-commit-id --name-only -r HEAD | sort | diff -u - &lt;(printf '%s\n' .claude-plugin/plugin.json .gsd/capabilities/ponytail/capability.json README.md hooks/hooks.json hooks/session-start.sh tests/test-plan-review-contribution.sh tests/test-session-start.sh | sort) &amp;&amp; test "$(git ls-remote origin refs/heads/main | cut -f1)" = "$(git rev-parse HEAD)" &amp;&amp; gh run list --repo davdittrich/ponytail-everywhere --workflow ci.yml --commit "$(git rev-parse HEAD)" --event push --limit 10 --json databaseId,headSha,workflowName,conclusion | jq -e --arg sha "$(git rev-parse HEAD)" 'length == 1 and .[0].headSha == $sha and .[0].workflowName == "CI" and .[0].conclusion == "success"' &amp;&amp; test "$(claude plugin list --json | jq -r '.[] | select(.id == "ponytail-everywhere@gsd-beads") | .version')" = "0.5.0" &amp;&amp; test "$(codex plugin list --json | jq -r '.installed[] | select(.pluginId == "ponytail-everywhere@gsd-beads") | .version')" = "0.5.0" &amp;&amp; diff -u &lt;(git ls-files -z | xargs -0 sha256sum) &lt;(git ls-files -z | (cd /home/dd/.claude/plugins/cache/gsd-beads/ponytail-everywhere/0.5.0 &amp;&amp; xargs -0 sha256sum)) &amp;&amp; diff -u &lt;(git ls-files -z | xargs -0 sha256sum) &lt;(git ls-files -z | (cd /home/dd/.codex/plugins/cache/gsd-beads/ponytail-everywhere/0.5.0 &amp;&amp; xargs -0 sha256sum)) &amp;&amp; diff -qr .gsd/capabilities/ponytail /home/dd/.gsd/capabilities/ponytail &amp;&amp; cd .. &amp;&amp; bd show gsd-beads-7q5 --json | jq -e '.[0].status == "closed"'</automated>
  </verify>
  <done>Remote main and local HEAD are the same reviewed 0.5.0 commit with exactly seven declared files and the unique pushed-commit `CI` run watched by numeric run ID to success. In `tests/test-plan-review-contribution.sh`, only three release-text substitutions occur: live lines 33 and 145 advance their fixture expressions from 0.4.1 to 0.5.0 and line 146 advances its diagnostic from `plugin version is not 0.4.1` to `plugin version is not 0.5.0`; comparison with the parent file after those three substitutions is byte-identical, so its behavior is otherwise unchanged. Claude starts both gsd-code-reviewer and gsd-verifier with the tested collaborator-output guidance; malformed output is reported; required findings include unhandled edge cases, ignored return values, swallowed errors, invalid boundary inputs, lazy structure, and plan-transcription code; evidence-backed performance, testing, intent, and minor-style concerns are suggestions; only safe low-impact presentation/quality failures can be non-blocking; every listed safety, integrity, contract, ASVS, and TDD class is non-waivable. README states SubagentStart delivery is Claude-only and links runtime-neutral follow-up issue #3. Existing planner/executor/generic/toggle/intensity behavior and all four test scripts remain green. Antigravity's validated bridge review has no unresolved in-scope blocker. Claude and Codex registries report 0.5.0, their tracked payload hashes equal source, the shared capability is byte-identical to source, no excluded machinery/file exists in the diff, and `gsd-beads-7q5` is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub `origin/main` -> clean nested checkout | Remote code crosses into the release worktree before any task edit. |
| Claude SubagentStart -> reviewer/verifier prompt | Static plugin guidance enters independent collaborator contexts and influences whether findings block continuation. |
| Antigravity JSON -> release decision | Untrusted external-model output may inform corrections but cannot redefine ticket scope or waive blockers. |
| Pushed source -> marketplace snapshots -> runtime caches | Release bytes cross Git and two installer-controlled cache boundaries. |
| Claude cache -> shared GSD capability | The installed plugin's auto-install hook updates the user-scoped capability bundle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0DS-01 | Tampering | Remote synchronization and push | high | mitigate | Require clean main, descendant check, ff-only merge, exact remote/local HEAD equality, and successful CI for the pushed commit. |
| T-0DS-02 | Spoofing | Subagent matcher routing | medium | mitigate | Assert exact matcher cardinality and exact command arguments with jq; invoke the public verifier-role boundary in the focused test. |
| T-0DS-03 | Elevation of Privilege | Non-blocking collaborator guidance | high | mitigate | Make safe continuation conditional on artifact/external-contract integrity and enumerate non-waivable security, concurrency, accessibility, source, constructor, ASVS, and TDD blockers under test. |
| T-0DS-04 | Repudiation | Antigravity review | medium | mitigate | Validate the agy-bridge JSON envelope, require an explicit verdict, and record verdict/evidence on the authoritative bead before release. |
| T-0DS-05 | Information Disclosure | Antigravity directory grant | medium | mitigate | Grant only the nested public repository with `--add-dir`; do not pass credentials, outer Beads data, runtime configs, or home directories. |
| T-0DS-06 | Tampering | Marketplace and capability installs | high | mitigate | Use native marketplace/install commands, assert both registry versions, compare SHA-256 manifests for every tracked file, and recursively compare the capability bundle. |
| T-0DS-07 | Denial of Service | Malformed collaborator output | medium | mitigate | Detect and report missing/invalid review output; halt release instead of silently converting malformed output into a pass. |
| T-0DS-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager dependency install is in scope; existing runtime marketplace installers fetch the already-declared Git source. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Feature / Requirement | Covered by | Status | Notes |
|---|---|---|---|---|---|
| GOAL | gsd-beads-7q5 | Implement Ponytail issue #7 through review, release, push, marketplace sync, and byte-verified Claude/Codex installs | Task 1 end-to-end tracer | COVERED | Publishing and local synchronization are terminal acceptance, not follow-up. |
| REQ | owner comment 2026-08-24 | Independent Claude SubagentStart guidance for gsd-code-reviewer and gsd-verifier | Task 1 RED/GREEN hook routing | COVERED | Reuses one role seam and preserves existing verifier delivery. |
| REQ | owner comment 2026-08-24 | Detect and report malformed collaborator output | Task 1 guidance, test, review-envelope gate | COVERED | No parser or state added. |
| REQ | owner comment 2026-08-24 | Low-impact format/style/quality may be non-blocking only under integrity and external-contract safety | Task 1 guidance and exact focused assertions | COVERED | Conditional exception is narrow and tested. |
| REQ | owner comment 2026-08-24 | Unhandled edge cases, ignored return values, swallowed errors, invalid boundary inputs, lazy structure, and plan-transcription code remain required findings | Task 1 must-have, behavior, RED/GREEN assertions, review contract, README, done | COVERED | Required findings are not silently downgraded or omitted. |
| REQ | owner comment 2026-08-24 | Evidence-backed performance, testing, intent, and minor-style concerns are suggestions | Task 1 must-have, behavior, RED/GREEN assertions, review contract, README, done | COVERED | Suggestions remain distinct from required findings unless an independent non-waivable condition applies. |
| REQ | owner comment 2026-08-24 | Security, trust-boundary, data-loss, race, accessibility, source/document, constructor-divergence, ASVS, and TDD blockers are non-waivable | Task 1 guidance, behavior, done, threat model | COVERED | Every named class remains blocking. |
| REQ | owner comment 2026-08-24 | Preserve planner/executor/generic/toggle/intensity behavior and runtime-neutral issue #3 delivery | Task 1 regression suite and forbidden scope | COVERED | Existing Quick bridge and lifecycle contributions are untouched. |
| REQ | checker revision | README and verification state SubagentStart delivery is Claude-only and link runtime-neutral follow-up issue #3 | Task 1 README assertions, automated verify, done | COVERED | Codex receives the installed plugin bytes, not Claude SubagentStart hook delivery. |
| REQ | owner comment 2026-08-24 | Add no reconciliation, shared state, downstream agent_skills machinery, or dependency | Forbidden and exact changed-path gate | COVERED | Seven declared files only, including the separately authorized three-substitution release synchronization. |
| REQ | user-authorized live evidence | `tests/test-plan-review-contribution.sh` fixture expressions at live lines 33 and 145 and the companion failure diagnostic at line 146 must advance from 0.4.1 to 0.5.0 with no other behavior change | Task 1 release behavior, GREEN action, parent-transform verify, done | COVERED | Seventh declared file; only the three exact substitutions are permitted. |
| REQ | task description | Antigravity review through approved bridge, version bump, marketplace sync, push, Claude/Codex byte verification | Task 1 review/release/install action and terminal verify | COVERED | Uses agy-bridge and native runtime installers. |
| RESEARCH | — | Research phase explicitly excluded by planning context | Entire plan | COVERED | Current primary-source checks support alternatives without a RESEARCH.md artifact. |
| CONTEXT | gsd-beads-7q5 comment | Live owner comment is the authoritative observed GitHub scope | Context, Forbidden, Task 1 | COVERED | No CONTEXT.md or D-XX decision exists; no deferred idea is planned. |

<verification>
1. Require Task 1's automated command to return zero after deployment; it rechecks tests, complete finding/suggestion classification, README's Claude-only SubagentStart boundary and issue #3 link, routing, versions, remote head, the unique successful `CI` push run, registry state, payload hashes, capability bytes, and bead closure.
2. Preserve TDD evidence in the summary: the focused test fails on absent reviewer routing/guidance before product edits and passes after GREEN.
3. Preserve the validated Antigravity JSON verdict and the bead comment reference; an invalid envelope or unresolved non-waivable blocker fails the release.
4. Inspect the pushed diff once and require exactly the seven declared files; the seventh file differs from its parent only by the two 0.4.1-to-0.5.0 fixture-expression substitutions at live lines 33 and 145 and the matching diagnostic substitution at line 146; the outer marketplace manifest, Quick bridge, lifecycle contributions, and dependencies remain unchanged.
</verification>

<success_criteria>
- Both target Claude subagents receive the exact reviewer/verifier contract through native SubagentStart routing and the existing script role.
- Malformed collaborator output is reported; the low-impact exception is conditional; every named blocker class is non-waivable.
- Unhandled edge cases, ignored return values, swallowed errors, invalid boundary inputs, lazy structure, and plan-transcription code remain required findings; evidence-backed performance, testing, intent, and minor-style concerns remain suggestions unless independently non-waivable.
- README and verification state that SubagentStart delivery is Claude-only and link runtime-neutral follow-up issue #3.
- Existing Ponytail roles, toggle, intensity, Quick delivery, and all four test scripts remain unchanged in behavior.
- Plugin and capability versions are 0.5.0 in pushed source; Antigravity review and GitHub CI pass.
- Exactly seven declared files change; `tests/test-plan-review-contribution.sh` is identical to its parent after applying only the three authorized release-text substitutions at live lines 33, 145, and 146.
- Claude and Codex marketplaces are refreshed, both plugin registries report 0.5.0, both tracked cache payloads match source byte-for-byte, and the installed capability matches its source bundle.
- No out-of-scope machinery or artifact is introduced and `gsd-beads-7q5` is closed only after terminal acceptance passes.
</success_criteria>

<output>
Create `.planning/quick/260825-0ds-execute-https-github-com-davdittrich-pon/260825-0ds-SUMMARY.md` when done and keep bead `gsd-beads-7q5` authoritative for execution and review evidence.
</output>
