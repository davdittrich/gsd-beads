---
phase: 11.1-beads-enabled-default-flip-to-true
plan: 02
type: execute
wave: 2
depends_on: ["11.1-01"]
files_modified:
  - README.md
  - .agents/skills/beads/PRIME.md
  - .beads/PRIME.md
  - docs/prd-beads-capability.md
  - CHANGELOG.md
autonomous: true
requirements: [D-01, D-03, D-04]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "README.md states beads is on by default and names the `beads.enabled: false` opt-out in the same paragraph (D-04)."
    - "No live doc still asserts that beads is off, opt-in, or defaults to false (D-01)."
    - "Both PRIME.md twins report the same default for `beads.enabled`, and their other three `beads.*` defaults are unchanged (D-01)."
    - "The PRD's embedded config-schema block reports the shipped default for `beads.enabled` (D-01)."
    - "CHANGELOG.md records the flip under the bumped capability version and states that an explicit `beads.enabled` value in a consumer's config is unaffected (D-03)."
  artifacts:
    - "CHANGELOG.md — NEW, repo root"
    - "README.md — opt-out framing at the Example workflow intro"
    - ".agents/skills/beads/PRIME.md — config reference line for beads.enabled"
    - ".beads/PRIME.md — config reference line for beads.enabled"
    - "docs/prd-beads-capability.md — config-schema block row for beads.enabled"
  key_links:
    - "The two PRIME.md files are unmanaged near-duplicates (no symlink, no generation step). Editing one does not propagate; both must be edited and then proven identical on the changed line."
    - "CHANGELOG.md keys its entry to capability.json's version (0.2.0 from plan 11.1-01), not to `.claude-plugin/plugin.json`'s independent plugin version — this repo has no other versioned artifact to anchor a changelog to."
---

<objective>
Bring every live document in line with the flipped default, and record the change in a new
repo-root CHANGELOG.md.

Purpose: plan `11.1-01` changed the behavior; five documents still describe the old opt-in
behavior, and a consumer with an existing install has no way to discover the change.
Output: four documents corrected and `CHANGELOG.md` created.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11.1-beads-enabled-default-flip-to-true/11.1-CONTEXT.md
@.planning/phases/11.1-beads-enabled-default-flip-to-true/11.1-PATTERNS.md
@.planning/phases/11.1-beads-enabled-default-flip-to-true/11.1-01-SUMMARY.md
</context>

## Artifacts this phase produces

| File | New/Modified | Change | Decision |
|---|---|---|---|
| `CHANGELOG.md` | **NEW** (repo root) | Created with the `0.2.0` default-flip entry | D-03 |
| `README.md` | Modified | Example-workflow intro reframed opt-in -> opt-out | D-04 |
| `.agents/skills/beads/PRIME.md` | Modified | `beads.enabled` config-reference line (line 47) | D-01 |
| `.beads/PRIME.md` | Modified | Same line, same content (unmanaged twin) | D-01 |
| `docs/prd-beads-capability.md` | Modified | `beads.enabled` row of the config-schema block (line 148) | D-01 |

**Explicitly out of scope** (per CONTEXT.md, Claude's Discretion): everything under
`.planning/milestones/v1.0-phases/**` and every prior phase's `SUMMARY.md` / `VERIFICATION.md`.
Those are historical records of decisions taken when the default genuinely was the old value;
rewriting them would falsify the project history. Leave them alone.

## Alternatives Considered

N/A — no mechanism choice.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reframe the README example-workflow intro from opt-in to opt-out</name>

  <files>README.md</files>

  <read_first>
    - `README.md` lines 45-72. The `### Example workflow` heading is at line 52; the paragraph to
      rewrite spans lines 54-56; line 58 begins `With it on, gsd-core's own lifecycle commands
      drive `bd` state directly:` and is followed by the numbered 1-4 lifecycle walkthrough.
    - `.planning/phases/11.1-beads-enabled-default-flip-to-true/11.1-CONTEXT.md` — D-04.
  </read_first>

  <reversibility rating="reversible">
    One paragraph of prose.
  </reversibility>

  <action>
    Rewrite the paragraph under `### Example workflow` (currently lines 54-56) so it leads with the
    new behavior and carries the opt-out instruction in the same breath, per D-04 — one paragraph,
    not a new subsection. It must say three things: that the capability is on by default, that
    `beads.enabled` defaults to the boolean true so a fresh install runs with issue tracking on,
    and that a project opts out by setting `beads.enabled: false` in its `.planning/config.json`.

    Keep the existing "every step below checks that gate first and no-ops with a visible notice"
    clause — it remains accurate, but rephrase its trailing condition so it describes the no-op
    happening when a project has opted out rather than when it has not yet opted in.

    Line 58 currently opens with a phrase that presupposes the reader has just switched the
    capability on. That presupposition is now false, so drop that opening clause and let the
    sentence begin directly with the subject `gsd-core's own lifecycle commands`. Leave the
    numbered 1-4 lifecycle walkthrough beneath it, the `## Install` block above it, and every other
    section of README.md untouched.
  </action>

  <verify>
    <automated><![CDATA[
set -euo pipefail
# New opt-out framing present.
test "$(grep -cF 'on by default' README.md)" -ge 1
test "$(grep -cF 'beads.enabled: false' README.md)" -ge 1
# Old opt-in framing gone.
test "$(grep -cF 'off until' README.md)" -eq 0
test "$(grep -cF 'With it on,' README.md)" -eq 0
# The lifecycle walkthrough survived.
test "$(grep -cF "gsd-core's own lifecycle commands drive" README.md)" -eq 1
test "$(grep -cF '/gsd-plan-phase' README.md)" -ge 1
test "$(grep -cF '/gsd-verify-work' README.md)" -ge 1
echo "OK: README reframed opt-out, walkthrough intact"
]]></automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF 'on by default' README.md` prints `1` or more.
    - `grep -cF 'beads.enabled: false' README.md` prints `1` or more.
    - `grep -cF 'off until' README.md` prints `0`.
    - `grep -cF 'With it on,' README.md` prints `0`.
    - `grep -cF "gsd-core's own lifecycle commands drive" README.md` prints `1` — the sentence survived, only its opening clause was dropped.
    - `grep -cF '/gsd-plan-phase' README.md` and `grep -cF '/gsd-verify-work' README.md` each print `1` or more, proving the numbered 1-4 walkthrough was not disturbed.
  </acceptance_criteria>

  <done>
    A reader of README.md learns that beads runs by default and how to turn it off, from a single
    paragraph, without needing to read the capability manifest.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Correct the stated default in both PRIME.md twins and the PRD config schema</name>

  <files>
    .agents/skills/beads/PRIME.md
    .beads/PRIME.md
    docs/prd-beads-capability.md
  </files>

  <read_first>
    - `.agents/skills/beads/PRIME.md` lines 44-52 — a four-item config-reference list. Line 47 is
      the `beads.enabled` entry; lines 48, 49 and 50 are the `beads.sync_mode`, `beads.ship_gate`
      and `beads.epic_per` entries. Line 36 separately documents `beads.ship_gate` in prose and is
      not part of this change.
    - `.beads/PRIME.md` lines 44-52 — byte-identical to the above at these lines.
    - `docs/prd-beads-capability.md` lines 145-155 — a fenced block holding the capability config
      schema. Line 148 is the `beads.enabled` row; lines 149-152 are the `sync_mode`, `epic_per`,
      `ship_gate` and `recall_scope` rows. Lines 155-165 are the `steps`/`contributions` arrays
      whose `"when": "beads.enabled"` predicates are correct as written.
    - `.planning/phases/11.1-beads-enabled-default-flip-to-true/11.1-01-SUMMARY.md` — confirm the
      value plan 01 actually shipped before asserting it in prose.
  </read_first>

  <reversibility rating="reversible">
    Three single-line value corrections.
  </reversibility>

  <action>
    In both `.agents/skills/beads/PRIME.md` and `.beads/PRIME.md`, on the `beads.enabled` entry of
    the config-reference list (line 47 in each), change the parenthetical default annotation so it
    names the boolean true that plan 01 shipped. Leave the entry's trailing description
    (`master toggle for this whole integration.`) and the list's other three entries on lines
    48-50 exactly as they are — `sync_mode`, `ship_gate` and `epic_per` defaults are unchanged by
    this phase. Leave line 36's separate `beads.ship_gate` prose paragraph untouched.

    These two files are unmanaged near-duplicates: there is no symlink and no generation step
    between them, so editing one does not propagate. Edit both, then confirm the changed line is
    identical in each.

    In `docs/prd-beads-capability.md`, inside the fenced config-schema block, change the
    `beads.enabled` row (line 148) so its default value reads as the boolean true. Preserve the
    row's existing column alignment — the block aligns the value braces across all five rows, so
    the shorter replacement value needs one extra space of padding to keep the block visually
    aligned. Leave the `sync_mode`, `epic_per`, `ship_gate` and `recall_scope` rows on lines
    149-152 untouched, and leave the `steps`/`contributions` arrays below them untouched.

    Do not touch line 248 of the PRD. Its remark about a milestone-level epic layer being off
    concerns the optional `epic_per: milestone` decision, not `beads.enabled`, and is still
    accurate.
  </action>

  <verify>
    <automated><![CDATA[
set -euo pipefail

# Both twins now state the new default, and neither retains the old one.
for f in .agents/skills/beads/PRIME.md .beads/PRIME.md; do
  test "$(grep -cF '`beads.enabled` (default `true`)'  "$f")" -eq 1
  test "$(grep -cF '`beads.enabled` (default `false`)' "$f")" -eq 0
  # Neighbouring config-reference list entries untouched. NOTE: the ship_gate assertion
  # anchors on the list-item form ("- ... — see Ship gate above."), because the bare
  # "`beads.ship_gate` (default `true`)" substring also occurs in line 36's prose paragraph
  # and would count 2.
  # NOTE: patterns that begin with "-" MUST be passed via `-e`, or grep parses them as options.
  test "$(grep -cF '`beads.sync_mode` (default `authoritative`)' "$f")" -eq 1
  test "$(grep -cF -e '- `beads.ship_gate` (default `true`) — see Ship gate above.' "$f")" -eq 1
  test "$(grep -cF -e '- `beads.epic_per` (default `phase`) — see Phase epics above.' "$f")" -eq 1
done

# The twins did not drift apart.
diff <(grep -F '`beads.enabled` (default' .agents/skills/beads/PRIME.md) \
     <(grep -F '`beads.enabled` (default' .beads/PRIME.md)

# PRD schema row flipped; siblings and the unrelated line 248 remark intact.
grep -F '"beads.enabled"' docs/prd-beads-capability.md | grep -qF '"default": true'
test "$(grep -F '"beads.enabled"' docs/prd-beads-capability.md | grep -cF '"default": false')" -eq 0
test "$(grep -cF '"beads.recall_scope"' docs/prd-beads-capability.md)" -eq 1
test "$(grep -cF 'two epic layers is usually noise' docs/prd-beads-capability.md)" -eq 1
test "$(grep -cF '"when": "beads.enabled"' docs/prd-beads-capability.md)" -eq 9

echo "OK: both twins and the PRD schema report the shipped default"
]]></automated>
  </verify>

  <acceptance_criteria>
    - In each of `.agents/skills/beads/PRIME.md` and `.beads/PRIME.md`: ``grep -cF '`beads.enabled` (default `true`)'`` prints `1`, and ``grep -cF '`beads.enabled` (default `false`)'`` prints `0`.
    - In each twin, the `beads.sync_mode`, `beads.ship_gate` and `beads.epic_per` config-reference list entries each still appear exactly once with their original default values (assert the `ship_gate` and `epic_per` entries on their full list-item form including the trailing cross-reference — the bare `` `beads.ship_gate` (default `true`) `` substring also occurs in line 36's prose and would count 2).
    - `diff` of the `beads.enabled` reference line between the two twins exits 0 (they did not drift apart).
    - The `"beads.enabled"` row of `docs/prd-beads-capability.md`'s config-schema block matches `"default": true` and does not match `"default": false`.
    - `grep -cF '"beads.recall_scope"' docs/prd-beads-capability.md` prints `1` and `grep -cF '"when": "beads.enabled"' docs/prd-beads-capability.md` prints `9` — the sibling schema rows and the steps/contributions arrays were not disturbed.
    - `grep -cF 'two epic layers is usually noise' docs/prd-beads-capability.md` prints `1` — the unrelated line-248 remark was not collaterally edited.
  </acceptance_criteria>

  <done>
    No live reference document reports a default for `beads.enabled` that disagrees with the
    shipped manifest.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create CHANGELOG.md with the default-flip entry</name>

  <files>CHANGELOG.md</files>

  <read_first>
    - `.planning/phases/11.1-beads-enabled-default-flip-to-true/11.1-CONTEXT.md` — D-03, which
      requires the entry to state explicitly that existing explicit `beads.enabled` values are
      untouched and only installs that never set the key pick up the new default.
    - `.planning/phases/11.1-beads-enabled-default-flip-to-true/11.1-PATTERNS.md` — the
      "CHANGELOG.md (new file, repo root)" section, which records that this repo has no existing
      changelog and excerpts the Keep-a-Changelog-style external analog chosen as the shape.
    - `.planning/phases/11.1-beads-enabled-default-flip-to-true/11.1-01-SUMMARY.md` — take the
      version number for the entry heading from what plan 01 actually wrote to `capability.json`,
      not from this plan's prose.
  </read_first>

  <reversibility rating="reversible">
    A new standalone file; deleting it fully reverts.
  </reversibility>

  <action>
    Create `CHANGELOG.md` at the repository root. It does not exist yet — this file establishes the
    convention, so keep it minimal and mechanical: an `# Changelog` title, one short orienting line
    stating that the versions in this file track
    `.gsd/capabilities/beads/capability.json`, then a single `## 0.2.0` release section. Use
    Keep-a-Changelog-style `### Changed` grouping, matching the external analog recorded in
    11.1-PATTERNS.md. Do not add an unreleased-placeholder heading, a date, a diff link, or
    back-filled entries for prior phases — there is no release history to reconstruct and no
    tooling that consumes this file.

    Under `### Changed`, record two things: that `beads.enabled` now defaults to the boolean true
    so a fresh install runs with issue tracking on out of the box, and that opting out is now the
    explicit action, done by setting `beads.enabled: false` in a project's `.planning/config.json`.
    Mention that the four beads skills' config gates were inverted to match, so an absent key
    resolves to the shipped default rather than stopping at the gate — a reader debugging why
    beads suddenly runs needs that detail to find the mechanism.

    Then, per D-03, state the no-regression guarantee in its own labelled group: a project that
    already sets `beads.enabled` explicitly in `.planning/config.json` keeps its current behavior,
    because an explicit value always wins over the shipped default; only installs that never set
    the key pick up the new default. This sentence is the point of the entry — it is what tells an
    existing consumer they need to do nothing.

    Key the `0.2.0` heading to the capability version. Do not use, or mention, the separate and
    unrelated version string carried by `.claude-plugin/plugin.json` — this phase does not change
    that one, and conflating the two would misdate the entry.
  </action>

  <verify>
    <automated><![CDATA[
set -euo pipefail
test -f CHANGELOG.md
test "$(grep -cF '# Changelog'  CHANGELOG.md)" -eq 1
test "$(grep -cF '## 0.2.0'     CHANGELOG.md)" -eq 1
test "$(grep -cF '### Changed'  CHANGELOG.md)" -eq 1
# The entry names the key, the new behaviour and the opt-out.
test "$(grep -cF 'beads.enabled'       CHANGELOG.md)" -ge 1
test "$(grep -cF 'beads.enabled: false' CHANGELOG.md)" -ge 1
# D-03's no-regression guarantee is stated, not implied.
grep -qiF 'explicit' CHANGELOG.md
grep -qiF '.planning/config.json' CHANGELOG.md
# The version tracked is the capability's, not the plugin's.
test "$(grep -cF '1.2.0' CHANGELOG.md)" -eq 0
test "$(grep -cF 'capability.json' CHANGELOG.md)" -ge 1
# No speculative scaffolding.
test "$(grep -cF '## Unreleased' CHANGELOG.md)" -eq 0
echo "OK: CHANGELOG.md created with the 0.2.0 default-flip entry"
]]></automated>
  </verify>

  <acceptance_criteria>
    - `CHANGELOG.md` exists at the repository root.
    - `grep -cF '# Changelog' CHANGELOG.md` prints `1`; `grep -cF '## 0.2.0' CHANGELOG.md` prints `1`; `grep -cF '### Changed' CHANGELOG.md` prints `1`.
    - `grep -cF 'beads.enabled: false' CHANGELOG.md` prints `1` or more — the opt-out is spelled out concretely.
    - `CHANGELOG.md` contains the word `explicit` and the string `.planning/config.json`, covering D-03's requirement that existing explicit values be called out as unaffected.
    - `grep -cF 'capability.json' CHANGELOG.md` prints `1` or more, and `grep -cF '1.2.0' CHANGELOG.md` prints `0` — the entry is keyed to the capability version, not the plugin version.
    - `grep -cF '## Unreleased' CHANGELOG.md` prints `0` — no empty scaffolding section.
  </acceptance_criteria>

  <done>
    A consumer upgrading an existing install can read one file and learn that the default changed,
    what it changed to, how to opt out, and that their explicit setting is safe.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository documentation -> reader | Documentation is the only channel through which an operator learns a security-relevant default changed. No executable surface is touched by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.1-04 | Repudiation | `CHANGELOG.md`, `README.md` | medium | mitigate | An undocumented default flip means an operator cannot tell whether beads started running by their choice or by an upgrade. Task 3 creates `CHANGELOG.md` recording the flip against capability version `0.2.0` and stating that explicit `beads.enabled` values are unaffected; Task 1 puts the opt-out instruction in `README.md` beside the on-by-default statement. Both are asserted by grep-based acceptance criteria, so the plan cannot pass with the disclosure missing. |
| T-11.1-05 | Information Disclosure | doc sweep scope | low | accept | The sweep deliberately excludes `.planning/milestones/v1.0-phases/**` and prior-phase summaries, which still describe the old default. Accepted: those are dated historical records, not operator-facing guidance, and rewriting them would falsify the project's decision history. The five live operator-facing documents are all corrected. |

This plan changes no code, adds no dependency, and runs no package-manager install, so the
package-legitimacy gate does not apply and no supply-chain threat entry is warranted.

No `high` or `critical` severity threat is present; the ASVS L1 `block_on: high` gate does not fire.
</threat_model>

<verification>
1. `grep -cF 'off until' README.md` prints `0`; `grep -cF 'on by default' README.md` and `grep -cF 'beads.enabled: false' README.md` each print `1` or more.
2. Both PRIME.md twins report the new default exactly once and the old default zero times, and their `beads.enabled` lines `diff` clean against each other.
3. The PRD's `"beads.enabled"` schema row matches `"default": true`; its four sibling rows and nine `"when": "beads.enabled"` predicates are unchanged.
4. `CHANGELOG.md` exists with a `## 0.2.0` / `### Changed` entry naming the key, the opt-out, and the unaffected-explicit-value guarantee.
5. Repo-wide sanity: no live (non-`.planning/`) document still asserts that beads is off or opt-in.
</verification>

<success_criteria>
- Every operator-facing document describes beads as on by default with an explicit opt-out.
- The two unmanaged PRIME.md twins remain in sync.
- `CHANGELOG.md` exists and satisfies D-03's explicit no-regression statement.
- No historical `.planning/milestones/**` record was rewritten.
</success_criteria>

<output>
Create `.planning/phases/11.1-beads-enabled-default-flip-to-true/11.1-02-SUMMARY.md` when done.
</output>
