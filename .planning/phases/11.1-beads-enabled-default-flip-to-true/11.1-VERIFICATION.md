---
phase: 11.1-beads-enabled-default-flip-to-true
verified: 2026-08-17T13:11:29Z
status: passed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 11.1: beads.enabled default flip to true Verification Report

**Phase Goal:** Flip the beads capability's shipped default `beads.enabled` from `false` to `true`, so a fresh install runs with beads issue tracking on out of the box instead of requiring a manual opt-in — and make the four beads skills' own config gates honor that default, so the flip actually reaches them. Covers the config default, the skill gates that duplicate it, the live documentation, and a CHANGELOG entry.
**Verified:** 2026-08-17T13:11:29Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `capability.json` is at version `0.2.0` with `beads.enabled` defaulting to `true`; sibling `beads.*` defaults untouched | VERIFIED | `jq -e '.version == "0.2.0"'` and `jq -e '.config."beads.enabled".default == true'` both pass; `sync_mode`="authoritative", `epic_per`="phase", `ship_gate`=true all unchanged. Seven `"when": "beads.enabled"` hooks confirmed present. |
| 2 | A fresh project (no `beads` key in `.planning/config.json`) resolves all 7 `beads.enabled`-gated hooks as `configured` and `active` | VERIFIED (behavioral, live-run, not just presence) | Ran the plan's actual tracer probe against `gsd-tools capability state` in a throwaway scratch dir — not merely re-stated from SUMMARY. Live output: `SILENT_PASS` (jq assertion `length == 7 and all(.configured and .active)` returned `true`). |
| 3 | A project with explicit `beads.enabled: false` still stops every skill's Step 1 gate / all 7 hooks report `configured == false` | VERIFIED (behavioral, live-run) | Same live probe, opt-out case: `OPTOUT_PASS` (jq assertion `all(.configured | not)` returned `true`). |
| 4 | All four beads skills (`beads-recall`, `beads-sync`, `beads-status`, `beads-migrate-todos`) invert their Step 1 gate to opt-out polarity, stopping only on explicit boolean `false` | VERIFIED | Read full Step 1 sections of `beads-recall` and `beads-migrate-todos` directly — gate text: "If ... `config.beads.enabled` is explicitly the boolean `false`: ... STOP. Otherwise ... fall through to the shipped default ... and proceed." `grep -cF 'enabled !== true'` = 0 and `grep -cF 'shipped default'` >= 1 across all four files. |
| 5 | README.md states beads is on by default and names the `beads.enabled: false` opt-out in the same paragraph | VERIFIED | `grep -cF 'on by default'` = 1, `grep -cF 'beads.enabled: false'` = 1, `grep -cF 'off until'` = 0, `grep -cF 'With it on,'` = 0. Lifecycle walkthrough sentence intact (`grep -cF "gsd-core's own lifecycle commands drive"` = 1). |
| 6 | Both PRIME.md twins (`.agents/skills/beads/PRIME.md`, `.beads/PRIME.md`) report `beads.enabled` default `true`, stay in sync with each other, and leave sibling entries untouched | VERIFIED | Both files: `` `beads.enabled` (default `true`)`` = 1, `` `beads.enabled` (default `false`)`` = 0. `diff` on the `beads.enabled` line between the two files exits 0 (identical). |
| 7 | The PRD's embedded config-schema block reports the shipped default for `beads.enabled`; sibling rows and `"when"` predicates untouched | VERIFIED | `docs/prd-beads-capability.md` shows `"beads.enabled": { "type": "boolean", "default": true }`. Count of `"when": "beads.enabled"` predicates = 9 (matches plan's asserted count). |
| 8 | `CHANGELOG.md` exists at repo root, keyed to `capability.json` 0.2.0, states the flip, the opt-out mechanism, and the explicit-value no-regression guarantee | VERIFIED | File exists. `# Changelog` = 1, `## 0.2.0` = 1, `### Changed` = 1, `beads.enabled: false` >= 1 present, contains `explicit` and `.planning/config.json`. |
| 9 | No live (non-`.planning/`) document still asserts beads is off/opt-in by default | VERIFIED | Repo-wide grep for `off by default`, `off until`, `opt-in`, and `default.*false` phrasing outside `.planning/` and `.git/` returned only one unrelated match (`AGENTS.md:92`, about git-push team permissions, not beads). |

**Score:** 9/9 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.gsd/capabilities/beads/capability.json` | version 0.2.0, `beads.enabled` default true | VERIFIED | Confirmed live via `jq`. |
| `.gsd/capabilities/beads/skills/beads-recall/SKILL.md` | opt-out Step 1 gate | VERIFIED | Read in full; correct gate logic. |
| `.gsd/capabilities/beads/skills/beads-sync/SKILL.md` | opt-out Step 1 gate | VERIFIED | grep-confirmed identical gate wording. |
| `.gsd/capabilities/beads/skills/beads-status/SKILL.md` | opt-out Step 1 gate | VERIFIED | grep-confirmed identical gate wording. |
| `.gsd/capabilities/beads/skills/beads-migrate-todos/SKILL.md` | opt-out Step 1 gate | VERIFIED | Read in full; correct gate logic (disabled-message wording issue noted below, does not affect gate correctness). |
| `README.md` | opt-out framing | VERIFIED | grep-confirmed. |
| `.agents/skills/beads/PRIME.md` | config-reference default corrected | VERIFIED | grep-confirmed. |
| `.beads/PRIME.md` | same, kept identical, gitignored | VERIFIED | Confirmed listed in `.gitignore` (comment: "Runtime copy of the bd prime override... regenerated by the SessionStart hook") and `git ls-files` returns no match — genuinely untracked, consistent with SUMMARY's claim. |
| `docs/prd-beads-capability.md` | config-schema row corrected | VERIFIED | grep-confirmed. |
| `CHANGELOG.md` | new file, 0.2.0 entry | VERIFIED | Exists, content confirmed. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `capability.json` `.config."beads.enabled".default` | gsd-core `resolveConfigKey()` schema-default fallthrough | Level-4 config resolution ladder | WIRED | `resolveConfigKey` located in `~/.claude/gsd-core/bin/lib/config.cjs` (and `capability-activation.cjs`, `loop-resolver.cjs`) — a real mechanism, not a fabricated claim. Live tracer probe (`gsd-tools capability state`) exercised this exact path end-to-end in a throwaway project and returned 7/7 hooks configured+active on silence, 0/7 on explicit false. |
| Each `SKILL.md` Step 1 gate | `.planning/config.json` (read directly, independent of the schema default) | Direct file read in skill prompt | WIRED | Confirmed by reading `beads-recall` and `beads-migrate-todos` gate text in full: both ends (manifest default and skill's own duplicated gate) now agree on opt-out polarity. |
| `capability.json` version bump | `CHANGELOG.md` heading | Version-keyed changelog entry | WIRED | `CHANGELOG.md`'s `## 0.2.0` heading matches `capability.json`'s `"version": "0.2.0"`. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Fresh project (no `beads` key) activates all 7 gated hooks | Live-run scratch-dir probe via `gsd-tools capability state` + jq assertion | `SILENT_PASS` | PASS |
| Project with `beads.enabled: false` deactivates all 7 gated hooks | Live-run scratch-dir probe via `gsd-tools capability state` + jq assertion | `OPTOUT_PASS` | PASS |
| Global-scope capability ledger points at this working tree (precondition for the above probes to be meaningful) | `jq -r '.entries.beads.source' ~/.gsd-capabilities.json` | Matched `/home/dd/projects/gsd-beads/.gsd/capabilities/beads` | PASS |

Note: these two probes were re-run independently by the verifier in fresh scratch directories — not read from the SUMMARY's reported output — to satisfy the "SUMMARY claims are not evidence" mandate.

### Requirements Coverage

This project has no `REQUIREMENTS.md` entries for D-01 through D-04 (confirmed: `grep -n "D-0[1-4]" .planning/REQUIREMENTS.md` returns no matches). Per the phase's own frontmatter and ROADMAP.md, traceability is intentionally routed through `.planning/phases/11.1-.../11.1-CONTEXT.md`'s decision log instead. No orphaned requirement IDs exist because none were ever declared in REQUIREMENTS.md for this phase — this is documented, not a gap.

| Decision ID | Description | Status | Evidence |
|---|---|---|---|
| D-01 | Full doc sweep + all 4 skill gates inverted | SATISFIED | Truths 1-9 above. |
| D-02 | Version bump 0.1.0 -> 0.2.0 | SATISFIED | Truth 1. |
| D-03 | CHANGELOG with no-regression guarantee for explicit values | SATISFIED | Truth 8; live probe (Truth 3) proves the guarantee is real behavior, not just documented prose. |
| D-04 | README opt-out wording in the same paragraph as "on by default" | SATISFIED | Truth 5. |

### Anti-Patterns Found

Scanned all 9 files this phase modified for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers. No debt markers found (one false-positive grep hit on the literal string "TODOS" inside "MIGRATE TODOS" banner text, not a `TODO` marker).

Two pre-existing quality issues were already surfaced by this phase's own `11.1-REVIEW.md` code-review artifact and independently confirmed by the verifier by reading the affected files directly:

| File | Issue | Severity | Impact on phase goal |
|------|-------|----------|----------------------|
| `.gsd/capabilities/beads/skills/beads-migrate-todos/SKILL.md:35` | Disabled message says "Beads sync is disabled" instead of "Beads migrate-todos is disabled" | Info (cosmetic, pre-existing wording bug this phase inherited but did not introduce — commit `e4ac490` only touched the gate condition, not this message body) | None — does not affect the config-gate flip behavior; the gate itself correctly stops on `beads.enabled: false` regardless of this string's wording. |
| All four `SKILL.md` gates | New opt-out condition matches only the exact boolean `false`; a malformed explicit value (e.g. string `"false"`, `0`, `null`) silently falls through to enabled rather than failing closed | Info (design observation, not a must-have regression — the plan's declared truth is "explicitly the boolean false", which is exactly what was verified; this is an edge case beyond the phase's declared scope) | None against this phase's declared must-haves — worth a follow-up ticket, not a blocker for this phase's stated goal. |

Neither issue causes any of this phase's 9 observable truths to fail, and neither was claimed as covered by this phase's must-haves.

### Human Verification Required

None. All must-haves were verifiable programmatically, including live re-execution of the behavioral probes (not just static grep).

### Gaps Summary

No gaps. All 9 observable truths derived from ROADMAP.md's phase goal and the two plans' `must_haves` frontmatter are verified against the actual codebase, including live re-execution (not SUMMARY-trusted) of the tracer probe that proves the config-default flip actually reaches the skill gates end-to-end in both directions (silent-project-enabled and explicit-false-disabled). All 5 documented commits (`252984f`, `e4ac490`, `3ac7a94`, `7e9053b`, `b69b335`) exist in git history and match their claimed content.

---

_Verified: 2026-08-17T13:11:29Z_
_Verifier: Claude (gsd-verifier)_
