---
phase: 23-extend-sota-definition-in-sota-numerics-and-refactor-plugin
plan: 03
type: execute
wave: 3
depends_on:
  - 23-02
target_repo: /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013
files_modified:
  - .gsd/capabilities/sota-numerics/capability.json
  - .claude-plugin/plugin.json
  - .gsd/capabilities/sota-numerics/scripts/check-alternatives.py
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-05
  - D-17
  - D-19
  - D-20
  - D-21
  - D-23
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Both `.claude-plugin/plugin.json` and `.gsd/capabilities/sota-numerics/capability.json` carry `\"version\": \"0.2.0\"` (D-19, D-23)."
    - "Both manifests' `description` strings state the extended definition of SOTA and still state that exactly one gate exists, at `plan:post` (D-02, D-17)."
    - "`capability.json` differs from `origin/main` only at `version`, `description`, `gates[0].description`, and the `sota-numerics.enabled` config description — every other key path is byte-identical, including the whole `contributions` array and `gates[0].check` (D-01, D-05)."
    - "`check-alternatives.py`'s abstract syntax tree, with docstrings removed, is identical to `origin/main`'s — the prose changed and the logic did not (D-01)."
    - "No prose in the gate script or in either manifest points at a document that does not ship with the capability (D-17)."
    - "The three existing suites pass unchanged (D-20)."
  prohibitions:
    - "No new key, no removed key, and no changed value anywhere in `capability.json` outside the four allowed paths — in particular `gates[0].check.predicate.command`, `gates[0].blocking`, `gates[0].onError`, `gates[0].when`, and every `contributions[]` entry stay byte-identical (D-01, D-02, D-05)."
    - "No executable line of `check-alternatives.py` changes: not a regex, not a constant, not a branch (D-01)."
    - "The instruction not to revert `onError` from halt back to skip stays present in `gates[0].description` (NOTES.md section 1)."
    - "No file under `hooks/` or `tests/` is edited (D-19, D-21)."
    - "The globally installed capability at `${GSD_HOME:-$HOME}/.gsd/capabilities/sota-numerics` is not written to (recursion guard)."
  artifacts:
    - .gsd/capabilities/sota-numerics/capability.json
    - .claude-plugin/plugin.json
    - .gsd/capabilities/sota-numerics/scripts/check-alternatives.py
  key_links:
    - "The two `version` fields have no tooling that cross-checks them; nothing in gsd-core reads one to validate the other. They stay in sync only because one task edits both and one command asserts both."
    - "`gates[0].description` is the shipped copy of the reasoning NOTES.md section 1 records at length. Refactoring the description must not delete the do-not-revert instruction, which is the only part of that reasoning a contributor reading only the manifest will see."
---

<objective>
Bring the capability's own manifests and its gate script's prose up to the extended definition, and bump both version fields to `0.2.0` — while freezing every byte of the gate contract those manifests declare.

Purpose: the phase's credibility rests on the plugin complying with what it now asks of others. The manifests describe a capability whose definition of SOTA has grown, and the gate script's docstring and comments are agent-facing prose full of pointers to planning documents that do not ship with the capability. Both are in the self-compliance scope. Neither may change what the gate does.

Output: two edited manifests at `0.2.0` and one gate script whose prose changed and whose logic did not.

**Repository:** every path below is relative to the `sota-numerics` clone at `/home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013`, NOT to `gsd-beads`. Work continues on the branch `feat/extended-sota-definition`.

**Recursion guard:** the `plan:post` gate that checks these very plan files executes the INSTALLED copy at `${GSD_HOME:-$HOME}/.gsd/capabilities/sota-numerics` when no project-root copy exists. That mirror is not source. No task here writes to it, and no task here changes gate behavior in the clone either. Mirror tree digest on 2026-09-06: `3ea50c85d28641b249ab71ce11374468652c9ff0388b64c464205a10e99e413e`.
</objective>

<validation_choices>
No SPEC.md and no REQ-IDs exist for this phase, so the spec-less probe fallback is skipped by design and `requirements` carries decision IDs from 23-CONTEXT.md. TDD mode is enabled in `.planning/config.json`, but every artifact in this plan is a JSON metadata string or a Python docstring — configuration and documentation, which the TDD contract exempts and D-21 independently forbids adding test infrastructure for. The regression guard is the pre-existing suite plus two exactness checks written for this plan and proven green against the unmodified tree on 2026-09-06: a structural JSON diff with an allowed-key allowlist, and a docstring-stripped AST comparison.
</validation_choices>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-CONTEXT.md
@.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-RESEARCH.md
@.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-PATTERNS.md
@.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite capability.json's description strings and bump it to 0.2.0</name>
  <reversibility rating="reversible">A manifest string and a version number are revertible by a single commit revert until the branch merges; the merge itself is the one-way step and it lives in Plan 05.</reversibility>
  <files>.gsd/capabilities/sota-numerics/capability.json</files>
  <read_first>
    - .gsd/capabilities/sota-numerics/capability.json (the file being modified — read all 91 lines, including the whole `gates[0]` object you must not change)
    - .gsd/capabilities/sota-numerics/fragments/planner-sota.md, executor-numerics.md, verifier-precision.md, ship-precision-advisory.md as Plans 01 and 02 left them — the description must describe what these four now say, and the four must read as one standard
    - .gsd/capabilities/sota-numerics/NOTES.md section 1 — the shipped record of why `gates[0].onError` is halt; `gates[0].description` is the abbreviated copy of it and must keep its operative instruction
  </read_first>
  <action>
Change exactly four values in `.gsd/capabilities/sota-numerics/capability.json` and nothing else (D-01, D-05, D-23).

1. `version`: from `0.1.3` to `0.2.0`. A minor bump: the instruction surface grows and prompt behavior changes, while the gate contract does not (D-23).
2. `description`: rewrite so it states the extended definition rather than only numerical stability. It must name what the capability now steers for — code that goes with the grain of the project, agent-facing prose that is legible, agent-invoked code that is quiet, and completeness and efficiency alongside numerical stability — and it must still end by stating that the capability declares exactly one gate and that gate is the blocking `plan:post` Alternatives Considered check. Write it as one sentence of at most 400 characters. Do not imply that any added dimension is enforced (D-02).
3. `gates[0].description`: rewrite for legibility without changing the claim. Keep the three things a contributor reading only this manifest needs: that `onError` governs the check command failing to run rather than the block decision, that the block decision comes from the exit code through gsd-core's generic `command-exit-zero` evaluator, and the explicit instruction not to revert this field to skip. Cut the restatement of which other capabilities use skip; NOTES.md section 1 carries that at length and ships alongside. Target at most 500 characters, down from the current 573.
4. `config` → `sota-numerics.enabled` → `description`: delete the trailing parenthetical citing decision-record identifiers. Those name a planning document that does not ship with the capability, so a reader of the manifest cannot resolve them. Keep the rest of the sentence, including the claim that this is the only config key the capability declares.

Do not touch `id`, `role`, `title`, `tier`, `requires`, `engines`, `runtimeCompat`, `skills`, `agents`, `hooks`, `steps`, the `contributions` array in any part, or anything inside `gates[0]` other than its `description`. Preserve the file's existing two-space indentation and its trailing newline.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && python3 - <<'PY'
import json, subprocess, sys
P = ".gsd/capabilities/sota-numerics/capability.json"
a = json.loads(subprocess.run(["git","show","origin/main:"+P],capture_output=True,text=True,check=True).stdout)
b = json.load(open(P))
ALLOWED = {("version",), ("description",), ("gates",0,"description"), ("config","sota-numerics.enabled","description")}
def walk(x, y, p=()):
    if type(x) is not type(y):
        yield p; return
    if isinstance(x, dict):
        for k in sorted(set(x) | set(y)):
            if k not in x or k not in y: yield p + (k,)
            else: yield from walk(x[k], y[k], p + (k,))
    elif isinstance(x, list):
        if len(x) != len(y): yield p; return
        for i, (u, v) in enumerate(zip(x, y)): yield from walk(u, v, p + (i,))
    elif x != y:
        yield p
diff = sorted(set(walk(a, b)), key=repr)
extra = [p for p in diff if p not in ALLOWED]
print("changed:", diff)
print("version:", b["version"], "| description-chars:", len(b["description"]), "| gate-description-chars:", len(b["gates"][0]["description"]))
ok = not extra and b["version"] == "0.2.0" and len(b["description"]) <= 400 and len(b["gates"][0]["description"]) <= 500
print("MANIFEST-OK" if ok else "MANIFEST-DIVERGED " + repr(extra))
sys.exit(0 if ok else 1)
PY</automated>
    <fails_when>non-zero exit; or the output prints `MANIFEST-DIVERGED` with a key path outside the four allowed ones; or `version:` is not `0.2.0`; or either character count exceeds its stated ceiling; or the literal `MANIFEST-OK` is absent.</fails_when>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && bash tests/test-session-start.sh >/dev/null && bash tests/test-gate-script-resolution.sh >/dev/null && python3 -m unittest tests/test_check_alternatives.py 2>&1 | tail -3 && grep -qF 'command-exit-zero' .gsd/capabilities/sota-numerics/capability.json && grep -qiF 'back to' .gsd/capabilities/sota-numerics/capability.json && echo GATE-CONTRACT-INTACT</automated>
    <fails_when>non-zero exit; or the unittest tail is not `OK` after `Ran 50 tests`; or the literal `GATE-CONTRACT-INTACT` is absent, meaning the manifest lost either the evaluator name or the do-not-revert instruction.</fails_when>
  </verify>
  <acceptance_criteria>
    - Test command: the structural-freeze command prints `MANIFEST-OK` and `changed:` lists only paths drawn from `('version',)`, `('description',)`, `('gates', 0, 'description')`, and `('config', 'sota-numerics.enabled', 'description')`.
    - Source assertion: `python3 -c "import json;print(json.load(open('.gsd/capabilities/sota-numerics/capability.json'))['version'])"` prints `0.2.0`.
    - Source assertion: `capability.json`'s `description` is at most 400 characters and contains the literals `with the grain`, `legible`, `quiet`, and `plan:post`.
    - Source assertion: `capability.json`'s `gates[0].description` is at most 500 characters and contains the literal `command-exit-zero`.
    - Behavior assertion: `git diff origin/main -- .gsd/capabilities/sota-numerics/scripts .gsd/capabilities/sota-numerics/fragments hooks tests` produces no output for this task's commit range.
    - Test command: all three suites exit 0, with `Ran 50 tests` and `OK`.
  </acceptance_criteria>
  <done>capability.json reads `0.2.0`, describes the extended definition without implying new enforcement, carries a shorter gate description that keeps the do-not-revert instruction, and is byte-identical to origin/main at every other key path.</done>
</task>

<task type="auto">
  <name>Task 2: Bump .claude-plugin/plugin.json to 0.2.0 with a matching description</name>
  <reversibility rating="reversible">A marketplace-facing string and a version number, revertible by a single commit revert until the branch merges.</reversibility>
  <files>.claude-plugin/plugin.json</files>
  <read_first>
    - .claude-plugin/plugin.json (the file being modified — all 10 lines)
    - .gsd/capabilities/sota-numerics/capability.json as Task 1 left it — this file's `description` must make the same claim for a different audience, the marketplace listing rather than the capability internals
  </read_first>
  <action>
Change exactly two values in `.claude-plugin/plugin.json` and nothing else (D-17, D-19, D-23).

1. `version`: from `0.1.3` to `0.2.0`, matching `capability.json` exactly. Nothing in gsd-core cross-checks these two fields; they stay in sync only because this task edits both repositories' worth of manifests in one wave and one command asserts both.
2. `description`: rewrite so it makes the same claim as `capability.json`'s new description for a marketplace reader — the extended definition of SOTA that the four advisory prompts now steer for, plus exactly one blocking `plan:post` gate. The two strings need not be byte-identical; they must not disagree. Write it as one sentence of at most 400 characters.

Do not touch `name`, `author`, or `license`. Preserve the file's existing two-space indentation and its trailing newline.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && python3 - <<'PY'
import json, subprocess, sys
P = ".claude-plugin/plugin.json"
a = json.loads(subprocess.run(["git","show","origin/main:"+P],capture_output=True,text=True,check=True).stdout)
b = json.load(open(P))
allowed = {"version", "description"}
changed = {k for k in set(a) | set(b) if a.get(k) != b.get(k)}
cap = json.load(open(".gsd/capabilities/sota-numerics/capability.json"))
print("changed:", sorted(changed), "| plugin-version:", b["version"], "| capability-version:", cap["version"], "| description-chars:", len(b["description"]))
ok = changed <= allowed and b["version"] == "0.2.0" == cap["version"] and len(b["description"]) <= 400
print("VERSIONS-IN-SYNC" if ok else "VERSION-DRIFT")
sys.exit(0 if ok else 1)
PY</automated>
    <fails_when>non-zero exit; or `changed:` lists any key other than `version` and `description`; or the two printed versions differ from each other or from `0.2.0`; or `description-chars:` exceeds 400; or the literal `VERSIONS-IN-SYNC` is absent.</fails_when>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && grep -c '"version": "0.1.3"' .claude-plugin/plugin.json .gsd/capabilities/sota-numerics/capability.json</automated>
    <fails_when>either file reports a count above `0`, meaning a stale `0.1.3` survived; note that `grep -c` exits 1 when both counts are zero, which is the passing case here, so read the printed counts rather than the exit status.</fails_when>
  </verify>
  <acceptance_criteria>
    - Test command: the version-sync command prints `VERSIONS-IN-SYNC`, with `plugin-version:` and `capability-version:` both `0.2.0`.
    - Source assertion: `grep -c '"version": "0.1.3"' .claude-plugin/plugin.json .gsd/capabilities/sota-numerics/capability.json` prints `0` for both files.
    - Source assertion: `plugin.json` still contains the literals `"name": "sota-numerics"`, `davdittrich@gmail.com`, and `"license": "MIT"`.
    - Source assertion: `plugin.json`'s `description` is at most 400 characters and contains the literal `plan:post`.
    - Behavior assertion: `python3 -m json.tool .claude-plugin/plugin.json >/dev/null` exits 0.
  </acceptance_criteria>
  <done>Both manifests read `0.2.0`, both descriptions state the extended definition and the single gate without contradicting each other, and no other field in plugin.json changed.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor check-alternatives.py's docstrings and comments, freezing its logic</name>
  <reversibility rating="reversible">Comment and docstring text only; the AST-equivalence check makes any accidental logic change detectable and revertible before commit.</reversibility>
  <files>.gsd/capabilities/sota-numerics/scripts/check-alternatives.py</files>
  <read_first>
    - .gsd/capabilities/sota-numerics/scripts/check-alternatives.py (the file being modified — read the whole file; you must know which line is a claim about behavior and which line is behavior)
    - .gsd/capabilities/sota-numerics/fragments/executor-numerics.md as Plan 01 left it — its definitions of legible and quiet are the standard this refactor is measured against
    - .gsd/capabilities/sota-numerics/NOTES.md, all five sections — several comments currently point at planning documents; NOTES.md is the one reasoning document that ships with the capability and is therefore the only in-repo target a comment may point at
    - tests/test_check_alternatives.py — confirm for yourself that no test asserts on docstring or comment text before changing any of it
  </read_first>
  <action>
Rewrite only the module docstring, the function and class docstrings, and the inline comments of `check-alternatives.py`. Change no executable line: no regex, no constant, no branch, no name (D-01, D-19, D-20).

Apply the legible standard the executor fragment now defines. Three concrete moves:

1. Replace every parenthetical that cites a planning document not shipped with the capability with the claim it was standing in for. The module docstring's opening reference chain becomes a plain statement of what the script validates. The comment on the plan-filename pattern states that the phase segment must match both a plain and a dotted phase number, instead of citing where that was discovered. The comment on the anchored single-line scans states that they avoid nested quantifiers so matching stays linear, instead of citing a research section. The threat identifiers attached to the untrusted-input and path-confinement claims are replaced by the claims themselves. Where reasoning genuinely belongs in a shipped document, point at the NOTES.md section number instead — the recency comment is the case that qualifies, and NOTES.md section 5 is its target.
2. Preserve, in claim and not merely in spirit, the two contracts other prose depends on: the exit-code contract, which must still say that exit 0 is a pass, exit 1 prints one `<plan_path>: <reason>` line per violation followed by exactly one `remediation: ...` line, and exit 2 is a usage or IO error; and the security claim, which must still say that PLAN.md text is authored by a different principal, is treated as untrusted input throughout, is never evaluated and never passed to a shell, and that no child process is spawned anywhere in the module. State that claim directly rather than by comparison to a script in a different repository, which a reader of this capability cannot open.
3. Delete any comment restating what the adjacent line already says. A comment says why; the code says what.

Keep the shebang line and the `if __name__` guard exactly as they are. Preserve the file's trailing newline.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && python3 - <<'PY'
import ast, subprocess, sys
P = ".gsd/capabilities/sota-numerics/scripts/check-alternatives.py"
D = (ast.Module, ast.FunctionDef, ast.AsyncFunctionDef, ast.ClassDef)
def norm(src):
    t = ast.parse(src)
    for x in ast.walk(t):
        if isinstance(x, D) and x.body and isinstance(x.body[0], ast.Expr) \
           and isinstance(x.body[0].value, ast.Constant) and isinstance(x.body[0].value.value, str):
            x.body = x.body[1:] or [ast.Pass()]
    return ast.dump(t)
base = subprocess.run(["git","show","origin/main:"+P],capture_output=True,text=True,check=True).stdout
ok = norm(base) == norm(open(P).read())
print("AST-IDENTICAL" if ok else "AST-DIVERGED")
sys.exit(0 if ok else 1)
PY</automated>
    <fails_when>non-zero exit, or the output reads `AST-DIVERGED`, meaning an executable line changed and not just prose.</fails_when>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && bash tests/test-session-start.sh >/dev/null && bash tests/test-gate-script-resolution.sh >/dev/null && python3 -m unittest tests/test_check_alternatives.py 2>&1 | tail -3 && grep -qF 'remediation:' .gsd/capabilities/sota-numerics/scripts/check-alternatives.py && grep -qF 'untrusted' .gsd/capabilities/sota-numerics/scripts/check-alternatives.py && grep -cE 'RESEARCH|REVIEWS|\bD-[0-9]+|\bT-[0-9]+-[0-9]+' .gsd/capabilities/sota-numerics/scripts/check-alternatives.py; C=$(grep -cE 'RESEARCH|REVIEWS|\bD-[0-9]+|\bT-[0-9]+-[0-9]+' .gsd/capabilities/sota-numerics/scripts/check-alternatives.py); [ "$C" -eq 0 ] && echo REFS-RESOLVED</automated>
    <fails_when>the unittest tail is not `OK` after `Ran 50 tests`; or the printed count of unresolvable document references is above zero; or the literal `REFS-RESOLVED` is absent; or either of the two preserved-contract greps found nothing.</fails_when>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && python3 .gsd/capabilities/sota-numerics/scripts/check-alternatives.py tests/fixtures 2>&1 | tail -3; echo "exit=${PIPESTATUS[0]}"</automated>
    <fails_when>the printed `exit=` is not `1`, or the output does not contain exactly one line beginning `remediation:` — the fixtures directory holds deliberately non-compliant plans, so a passing exit code here means the gate stopped detecting them.</fails_when>
  </verify>
  <!-- planner-discipline-allow: D- -->
  <!-- planner-discipline-allow: RESEARCH -->
  <acceptance_criteria>
    - Test command: the AST-equivalence command prints `AST-IDENTICAL` and exits 0.
    - Test command: the reference-audit command prints `REFS-RESOLVED` and a count of `0`.
    - Source assertion: the file still contains the literals `remediation:`, `untrusted`, `command-exit-zero`, and `#!/usr/bin/env python3` on line 1.
    - Source assertion: the file no longer contains the literal `sync.py` — the cross-repository pointer is replaced by the claim it stood for.
    - Test command: `python3 -m unittest tests/test_check_alternatives.py` reports `Ran 50 tests` and `OK`; both smoke scripts print `ALL PASS`.
    - CLI assertion: running the script against `tests/fixtures` exits 1 and emits exactly one `remediation:` line.
    - Behavior assertion: `git diff origin/main --stat -- .gsd/capabilities/sota-numerics/scripts/check-alternatives.py` shows changed lines confined to docstrings and comments, corroborated by the AST check above.
  </acceptance_criteria>
  <done>The gate script's prose meets the legible standard and points at nothing a reader of the capability cannot open, while its docstring-stripped AST is identical to origin/main and all 50 tests plus both smoke scripts pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan author → `check-alternatives.py` | `PLAN.md` text authored by a different principal is parsed here. This plan edits the file's prose while that parser stays byte-identical in behavior. |
| capability author → marketplace consumer | `.claude-plugin/plugin.json` is the string a marketplace listing shows. A description that overstates enforcement misleads an installer about what the capability blocks. |
| manifest → gsd-core gate evaluator | `gates[0].check.predicate.command` is a shell string gsd-core executes. Editing the surrounding object risks touching it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-02 | Tampering | `scripts/check-alternatives.py` | high | mitigate | A prose refactor that touches a regex or constant would silently weaken a parser of untrusted input. Task 3 asserts docstring-stripped AST equality against `origin/main` and re-runs the 50-test suite and the fixture-directory negative path. |
| T-23-09 | Tampering | `capability.json` `gates[0].check` | high | mitigate | Editing `gates[0].description` puts the adjacent `predicate.command` shell string one line away. Task 1 asserts a structural JSON diff against `origin/main` with a four-path allowlist; any other changed key path fails the task. |
| T-23-10 | Spoofing | `plugin.json` `description` | medium | mitigate | A description claiming the added dimensions are enforced would misrepresent an advisory capability to installers. Both descriptions must state that exactly one gate exists, at `plan:post`; Task 1 and Task 2 assert the `plan:post` literal and forbid enforcement language for the added dimensions. |
| T-23-11 | Repudiation | `gates[0].description` | medium | mitigate | The do-not-revert instruction is the only part of NOTES.md section 1's reasoning visible to someone reading the manifest alone. Task 1 asserts it survives the shortening. |
| T-23-03 | Elevation of Privilege | installed global mirror | high | mitigate | No task writes to `${GSD_HOME:-$HOME}/.gsd/capabilities/sota-numerics`; its digest was recorded 2026-09-06 and Plan 05 re-verifies it before publish. |
| T-23-04 | Information Disclosure | `gsd-beads` working tree | medium | mitigate | Every git command is `git -C <clone>`; bd issue gsd-beads-9tg tracks the containment fix. |
| T-23-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan or in Phase 23; the phase adds no dependency, so package legitimacy is not applicable rather than skipped. |

ASVS L1 assessment: V2, V3, V4, and V6 have no surface in this plan. V5 applies to `check-alternatives.py` and is held constant by the AST-equivalence assertion rather than re-argued.
</threat_model>

## Alternatives Considered

- **Structural JSON diff against `origin/main` with an explicit allowed-key allowlist**: parse both revisions of `capability.json`, walk every key path, and fail on any difference outside `version`, `description`, `gates[0].description`, and the one config description. Paired with a docstring-stripped AST comparison for the Python file. Citation: the gate object being frozen, `` `.gsd/capabilities/sota-numerics/capability.json` `` lines 75-89, read directly and verified 2026-09-06; both commands were run green against the unmodified tree on 2026-09-06 before this plan was written, so the check is known to pass on a no-op rather than assumed to.
- **Text diff review plus the existing smoke tests**: read `git diff` by eye and trust `tests/test-gate-script-resolution.sh` and `tests/test-session-start.sh` to catch a broken gate. Citation: `` `.github/workflows/ci.yml` `` — the three CI steps, read and verified 2026-09-06. Rejected on coverage: those two scripts exercise script-path resolution and the session-start hook, not the manifest's predicate string, so a reformatted or subtly altered `command` passes both while changing what the gate runs.
- **JSON Schema validation of the manifest**: let gsd-core's loader validate `capability.json` on install and treat a clean load as proof. Citation: `` `/home/dd/.claude/gsd-core/bin/lib/capability-validator.cjs` `` lines 2672-2735, read and verified 2026-09-06. Rejected on the same ground, more sharply: shape validity is not contract identity. A manifest with a completely different `predicate.command` is still schema-valid, so the check cannot distinguish the change this plan forbids from the change it permits.

Decided by: simplicity. Performance is a wash — all three checks complete in well under a second on a 91-line manifest and a 340-line script, so the ranked criterion above simplicity does not separate them and the decision falls to the next one. The structural diff and the AST comparison each express the invariant directly in a single self-contained command with no new file added to the repository, which matters under a decision that forbids new test infrastructure; the two alternatives need either sustained human diff-reading discipline or a new schema artifact to approach the same coverage, and still would not reach it.

### Internal design alternatives

- **Docstring-stripped AST comparison versus a comment-stripped text diff**: the AST comparison is exact and ignores formatting, blank lines, and comment placement by construction, where a text filter would need its own rules for line continuations and multi-line strings. Comments never appear in a Python AST, so stripping only docstrings is sufficient and complete.
- **One task for both manifests versus one task each**: one each. The version fields must agree, and the cross-check belongs in the second task where both files are final; splitting also keeps each task's freeze assertion scoped to a single file's allowlist.
- **Rewriting `gates[0].description` versus leaving it alone**: rewriting. It is agent- and contributor-facing prose inside the self-compliance scope, and it currently spends most of its length restating which other capabilities use skip — reasoning NOTES.md carries in full and ships alongside.

## Artifacts this phase produces

This plan's additions to the phase-wide artifact list in `23-01-PLAN.md`:

| Symbol | Kind | Location |
|---|---|---|
| `"version": "0.2.0"` | changed value | `.gsd/capabilities/sota-numerics/capability.json` |
| `"version": "0.2.0"` | changed value | `.claude-plugin/plugin.json` |
| capability `description` | rewritten string, at most 400 characters, names the extended definition | `.gsd/capabilities/sota-numerics/capability.json` |
| `gates[0].description` | rewritten string, at most 500 characters, keeps the do-not-revert instruction | `.gsd/capabilities/sota-numerics/capability.json` |
| `sota-numerics.enabled` config `description` | trailing decision-record parenthetical removed | `.gsd/capabilities/sota-numerics/capability.json` |
| plugin `description` | rewritten string, at most 400 characters, marketplace audience | `.claude-plugin/plugin.json` |
| module and function docstrings, inline comments | rewritten prose, unresolvable document pointers replaced by their claims | `.gsd/capabilities/sota-numerics/scripts/check-alternatives.py` |

No new file path is created, no key is added or removed from either manifest, and no executable line of the gate script changes.

<verification>
- `capability.json` differs from `origin/main` only at the four allowed key paths; the structural-freeze command prints `MANIFEST-OK` (D-01, D-05).
- `plugin.json` differs only at `version` and `description`; both manifests read `0.2.0` (D-19, D-23).
- `check-alternatives.py`'s docstring-stripped AST is identical to `origin/main`; the command prints `AST-IDENTICAL` (D-01).
- The reference audit prints `REFS-RESOLVED` with a count of zero (D-17).
- All three suites exit 0 with `Ran 50 tests ... OK`, and the script still exits 1 with exactly one `remediation:` line against the non-compliant fixtures (D-20).
</verification>

<success_criteria>
Both manifests read `0.2.0` and describe the extended definition without claiming enforcement it does not have; the gate script's prose is legible and self-contained; and every mechanical check confirms that the gate contract, the contributions array, and the parser's logic are unchanged from `origin/main`.
</success_criteria>

<output>
Create `.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-03-SUMMARY.md` when done.
</output>
