---
phase: 23-extend-sota-definition-in-sota-numerics-and-refactor-plugin
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
target_repo: /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013
files_modified:
  - .gsd/capabilities/sota-numerics/fragments/planner-sota.md
  - .gsd/capabilities/sota-numerics/fragments/verifier-precision.md
  - .gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md
  - README.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-04
  - D-05
  - D-07
  - D-08
  - D-09
  - D-10
  - D-17
  - D-20
  - D-21
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "planner-sota.md is 12 lines or fewer and still teaches every rule a planner must follow to pass the gate: section heading, two named alternatives, citation, in-window year, `Decided by:` line, the internal-alternatives escape hatch, the no-mechanism-choice exemption, and the foundational-pairing rule (D-08)."
    - "A planner reading planner-sota.md learns one definition of **with the grain** (project consistency) and one instruction that every task in a plan carries a bound the executor can check (D-07, D-09)."
    - "The verifier and ship fragments flag and confirm the planner-side dimensions using the bare token, without redefining it (D-04, D-09)."
    - "The four fragments total 45 lines or fewer after every task (D-08)."
    - "The three existing suites pass unchanged after every task (D-20)."
  prohibitions:
    - "`check-alternatives.py` is not edited by this plan; its `TestFoundationalCitationPairing` expectations must remain satisfiable by the pruned fragment (D-01, NOTES.md §5)."
    - "No rule is deleted from planner-sota.md — only restatement of how the checker implements a rule is deleted (D-08)."
    - "No fragment gains a heading, bullet list, or code fence; the register stays dense prose, one sentence per line."
    - "No new test infrastructure for prose (D-21)."
    - "The globally installed capability at `${GSD_HOME:-$HOME}/.gsd/capabilities/sota-numerics` is not written to (recursion guard)."
  artifacts:
    - .gsd/capabilities/sota-numerics/fragments/planner-sota.md
    - .gsd/capabilities/sota-numerics/fragments/verifier-precision.md
    - .gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md
    - README.md
  key_links:
    - "planner-sota.md teaches the foundational-citation-pairing rule that `TestFoundationalCitationPairing` in `tests/test_check_alternatives.py` enforces. NOTES.md §5 records that these two seats must keep teaching the same rule; compressing the fragment's wording must not change which citations pass."
    - "**with the grain** is defined once in planner-sota.md and appears as a bare token in verifier-precision.md and ship-precision-advisory.md — the same shape `quiet` and `legible` use from executor-numerics.md."
---

<objective>
Take the planner role slice of D-07 — project consistency and plan completeness — and pay for it out of `planner-sota.md`'s own restatement of the checker rather than out of the 45-line budget.

Purpose: `planner-sota.md` is 13 of the 27 current fragment lines and spends several of them re-teaching how `check-alternatives.py` implements rules the planner does not need to know to follow them. The script already prints its own `remediation: ...` line on every failure, so the planner learns the mechanics from the failure, not from the prompt. Pruning that restatement funds the two new dimensions without growing the file.

Output: a pruned and extended `planner-sota.md`, matching verifier and ship clauses, and the README planner row.

**Repository:** every path below is relative to the `sota-numerics` clone at `/home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013`, NOT to `gsd-beads`. Work continues on the branch `feat/extended-sota-definition` created in Plan 01.

**Recursion guard:** the installed global mirror at `/home/dd/.gsd/capabilities/sota-numerics` is what the running `plan:post` gate executes. It is not source and no task writes to it. Its tree digest on 2026-09-06 was `3ea50c85d28641b249ab71ce11374468652c9ff0388b64c464205a10e99e413e`.
</objective>

<validation_choices>
Same three choices as Plan 01, restated so this file stands alone: no SPEC.md / no REQ-IDs, so `requirements` carries D-NN decision IDs and the spec-less probe fallback is skipped by design; TDD mode is on but every artifact here is prose, which the TDD contract exempts and D-21 independently forbids adding test infrastructure for; the measured green baseline is `ALL PASS` from both smoke scripts and `Ran 50 tests ... OK` in 1.6s from the Python suite, taken 2026-09-06 before any Phase 23 edit.
</validation_choices>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-CONTEXT.md
@.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-RESEARCH.md
@.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-PATTERNS.md
@.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prune planner-sota.md's restatement of checker internals from 13 lines to 10</name>
  <files>.gsd/capabilities/sota-numerics/fragments/planner-sota.md</files>
  <read_first>
    - .gsd/capabilities/sota-numerics/fragments/planner-sota.md (the file being modified — read all 13 lines)
    - .gsd/capabilities/sota-numerics/fragments/executor-numerics.md, verifier-precision.md, ship-precision-advisory.md (the three siblings, as Plan 01 left them)
    - .gsd/capabilities/sota-numerics/scripts/check-alternatives.py, the whole file — you must know exactly which rules it enforces before deciding which sentence is a rule and which is a restatement of implementation
    - .gsd/capabilities/sota-numerics/NOTES.md section 5 — it records that this fragment and `TestFoundationalCitationPairing` are two seats teaching one rule and must stay in step
    - tests/test_check_alternatives.py, the `TestFoundationalCitationPairing` class
  </read_first>
  <action>
Reduce `planner-sota.md` from 13 lines to 10 by merging and trimming, deleting no rule. Every one of these must still be learnable from the fragment alone: the `## Alternatives Considered` heading name; at least two bold-named alternatives as bullets or framed table rows; a URL or backticked doc-ref citation per alternative; a four-digit year per alternative with at least one inside the six-year window; a `Decided by:` line naming performance, simplicity, LOC, ecosystem, or maintenance; the `### Internal design alternatives` escape hatch and the fact that internal entries do not count toward the two; the `N/A — no mechanism choice` exemption; that a phase with a SPEC.md still needs the section; that placeholder citations are rejected; the foundational-pairing rule; that two is a floor not a target; and that a pick trading performance away must name the lower-ranked criterion that won.

What comes out is the implementation detail behind those rules — the parts that describe how the checker resolves a case rather than what the planner should write. Three specific reductions, in this order:

1. Merge the three consecutive lines that teach the foundational-citation-pairing rule into one line. The surviving line must still say that at least one cited year per alternative falls inside the window, that a canonical old year is neither rejected nor counted toward the window, and that pairing a canonical source with a current one is the correct move. This is the line NOTES.md section 5 names; keep it teaching exactly the behavior `TestFoundationalCitationPairing` asserts. Net minus two lines.
2. Merge the internal-design-alternatives line and the line about internal entries not counting into one line, dropping the sentence describing the heading marker's whitespace and case handling. Net minus one line.
3. Trim in place, without changing the line count: from the section-shape line, drop the clause describing bullet-versus-table precedence and how the checker falls back between the two shapes; from the placeholder-citation line, drop the clause about which authorities the filter does and does not match, keeping the instruction to cite something real.

Everything deleted here is a mechanic the planner meets through the gate's own stderr instead: on any failure the script prints one `<plan_path>: <reason>` line per violation naming the exact failing condition, followed by exactly one `remediation: ...` line.

Add nothing in this task. The register stays dense prose, one sentence per line, no headings, no bullets, no fences.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && python3 -m unittest tests/test_check_alternatives.py 2>&1 | tail -3 && python3 -m unittest -k FoundationalCitationPairing tests.test_check_alternatives 2>&1 | tail -3 && L=$(wc -l < .gsd/capabilities/sota-numerics/fragments/planner-sota.md) && echo "planner-lines=$L" && [ "$L" -eq 10 ] && for t in 'Alternatives Considered' 'Decided by:' 'Internal design alternatives' 'no mechanism choice' 'SPEC.md' 'Kahan'; do grep -qF "$t" .gsd/capabilities/sota-numerics/fragments/planner-sota.md || { echo "LOST RULE: $t"; exit 1; }; done && echo PRUNE-OK</automated>
    <fails_when>non-zero exit; or the unittest tail is not `OK` after `Ran 50 tests`; or `planner-lines=` is anything other than 10; or a `LOST RULE:` line appears; or the literal `PRUNE-OK` is absent.</fails_when>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && ! grep -qE 'case-sensitive|never combine|containing a port' .gsd/capabilities/sota-numerics/fragments/planner-sota.md && echo MECHANICS-PRUNED</automated>
    <fails_when>the literal `MECHANICS-PRUNED` is absent, meaning at least one of the checker-implementation clauses survived the prune.</fails_when>
  </verify>
  <acceptance_criteria>
    - Test command: `wc -l < .gsd/capabilities/sota-numerics/fragments/planner-sota.md` prints exactly `10`.
    - Source assertion: the fragment still contains each of the literals `Alternatives Considered`, `Decided by:`, `Internal design alternatives`, `no mechanism choice`, `SPEC.md`, and `Kahan`.
    - Source assertion: `grep -cE 'case-sensitive|never combine|containing a port' .gsd/capabilities/sota-numerics/fragments/planner-sota.md` is `0`.
    - Source assertion: `grep -cE '^(#|\s*[-*] |```)' .gsd/capabilities/sota-numerics/fragments/planner-sota.md` is `0`.
    - Test command: `python3 -m unittest tests/test_check_alternatives.py` reports `Ran 50 tests` and `OK`; the `TestFoundationalCitationPairing` subset also reports `OK`.
    - Behavior assertion: `git diff origin/main -- .gsd/capabilities/sota-numerics/scripts/check-alternatives.py tests/` produces no output.
  </acceptance_criteria>
  <done>planner-sota.md is exactly 10 lines, every gate rule is still teachable from it alone, and the 50-test suite is green with the pairing tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Define "with the grain" and plan completeness in planner-sota.md</name>
  <files>.gsd/capabilities/sota-numerics/fragments/planner-sota.md, README.md</files>
  <read_first>
    - .gsd/capabilities/sota-numerics/fragments/planner-sota.md as Task 1 left it (the file being modified)
    - .gsd/capabilities/sota-numerics/fragments/executor-numerics.md, verifier-precision.md, ship-precision-advisory.md (the three siblings — `quiet` and `legible` are already defined in the executor fragment and this file must read as the same standard, not a second one)
    - README.md lines 1-18 (the `## What it changes` table, `plan:pre` row)
  </read_first>
  <action>
Add exactly two lines to `planner-sota.md`, taking it from 10 to 12.

Line one defines the leading word **with the grain** once, as a target behavior: a plan goes with the grain when the code it asks for matches the conventions already in the project — its naming, its error shape, its module layout, its test placement — so a reader cannot tell from the code which file is new. State it as the target, never as a list of divergences to avoid (D-10).

Line two states plan completeness as a target: every task in a plan names a bound the executor can check, so "done" is decidable without asking the author. Phrase this as the completion-criterion rule, and keep it in the same register as the surrounding sentences.

Place both lines so the fragment reads as one standard that grew, not as two standards sharing a file: the added dimensions belong in the flow of the existing prose, not appended after a break. Introduce them with no phrase framing them as new, additional, or separate.

In `README.md`'s `## What it changes` table, append one clause to the `plan:pre` Behavior cell naming both added planner behaviors. Keep it inside that one row.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && bash tests/test-session-start.sh >/dev/null && bash tests/test-gate-script-resolution.sh >/dev/null && python3 -m unittest tests/test_check_alternatives.py 2>&1 | tail -3 && L=$(wc -l < .gsd/capabilities/sota-numerics/fragments/planner-sota.md) && echo "planner-lines=$L" && [ "$L" -le 12 ] && T=$(awk 'END{print NR}' .gsd/capabilities/sota-numerics/fragments/*.md) && echo "fragment-lines=$T" && [ "$T" -le 45 ] && grep -qF 'with the grain' .gsd/capabilities/sota-numerics/fragments/planner-sota.md && grep -qF 'with the grain' README.md && echo GRAIN-OK</automated>
    <fails_when>non-zero exit; or the unittest tail is not `OK` after `Ran 50 tests`; or `planner-lines=` exceeds 12; or `fragment-lines=` exceeds 45; or the literal `GRAIN-OK` is absent.</fails_when>
  </verify>
  <acceptance_criteria>
    - Test command: `wc -l < .gsd/capabilities/sota-numerics/fragments/planner-sota.md` prints a value `<= 12`.
    - Test command: `awk 'END{print NR}' .gsd/capabilities/sota-numerics/fragments/*.md` prints a value `<= 45`.
    - Source assertion: `grep -cF 'with the grain' .gsd/capabilities/sota-numerics/fragments/planner-sota.md` is at least `1`.
    - Source assertion: `README.md` contains the literal `with the grain`.
    - Source assertion: `grep -cE '^(#|\s*[-*] |```)' .gsd/capabilities/sota-numerics/fragments/planner-sota.md` is `0`.
    - Test command: all three suites exit 0, with `Ran 50 tests` and `OK`.
  </acceptance_criteria>
  <done>planner-sota.md defines `with the grain` once and states plan completeness as a checkable bound, at 12 lines or fewer, with the README `plan:pre` row updated and the suites green.</done>
</task>

<task type="auto">
  <name>Task 3: Echo the planner dimensions in the verifier and ship fragments</name>
  <files>.gsd/capabilities/sota-numerics/fragments/verifier-precision.md, .gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md, README.md</files>
  <read_first>
    - .gsd/capabilities/sota-numerics/fragments/verifier-precision.md (a file being modified — read the three existing `Flag ...` clauses; the new clause must match their shape)
    - .gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md (a file being modified — read the `confirm` sentence Plan 01 extended)
    - .gsd/capabilities/sota-numerics/fragments/planner-sota.md as Task 2 left it (this is where `with the grain` is defined; the echo must use the bare token)
    - .gsd/capabilities/sota-numerics/fragments/executor-numerics.md (the fourth sibling)
    - README.md lines 1-18
  </read_first>
  <action>
In `verifier-precision.md`, add one `Flag ...` clause in the exact shape of the existing three, naming code that does not go **with the grain** of the project — a new naming scheme where one already exists, an error shape that differs from the module's neighbours, a test placed outside where this project's tests live. Use the bare token; the definition stays in `planner-sota.md`.

In `ship-precision-advisory.md`, extend the same `confirm` sentence so it also covers with-the-grain claims: a claim that the change follows project convention is backed by the convention it names, not asserted.

Keep the opening disclaimer clause of both fragments byte-identical to what it says now — the sentence stating that this capability declares no gate at that point and everything below is a finding. That sentence is what keeps the advisory fragments from reading as new gates (D-02, D-03).

In `README.md`'s `## What it changes` table, append the with-the-grain clause to the `execute:wave:post` and `ship:pre` Behavior cells.

Then confirm the four fragments read as one grown standard by reading all four in sequence: `quiet` and `legible` are defined once in the executor fragment, `with the grain` once in the planner fragment, and every other appearance of all three is a bare token.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && bash tests/test-session-start.sh >/dev/null && bash tests/test-gate-script-resolution.sh >/dev/null && python3 -m unittest tests/test_check_alternatives.py 2>&1 | tail -3 && for f in planner-sota executor-numerics verifier-precision ship-precision-advisory; do printf '%s ' "$f"; wc -l < ".gsd/capabilities/sota-numerics/fragments/$f.md"; done && T=$(awk 'END{print NR}' .gsd/capabilities/sota-numerics/fragments/*.md) && echo "fragment-lines=$T" && [ "$T" -le 45 ] && [ "$(wc -l < .gsd/capabilities/sota-numerics/fragments/verifier-precision.md)" -le 9 ] && [ "$(wc -l < .gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md)" -le 7 ] && grep -qF 'with the grain' .gsd/capabilities/sota-numerics/fragments/verifier-precision.md && grep -qF 'with the grain' .gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md && echo PLANNER-SLICE-OK</automated>
    <fails_when>non-zero exit; or the unittest tail is not `OK` after `Ran 50 tests`; or `fragment-lines=` exceeds 45; or either per-file `wc -l` exceeds its ceiling; or the literal `PLANNER-SLICE-OK` is absent.</fails_when>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && head -1 .gsd/capabilities/sota-numerics/fragments/verifier-precision.md | grep -qF 'declares no gate at execute:wave:post' && head -1 .gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md | grep -qF 'declares no gate at ship:pre' && git diff --name-only origin/main && echo DISCLAIMERS-INTACT</automated>
    <fails_when>the literal `DISCLAIMERS-INTACT` is absent, meaning an advisory fragment lost the sentence that distinguishes it from a gate; or `git diff --name-only` lists any path outside `.gsd/capabilities/sota-numerics/fragments/` and `README.md`.</fails_when>
  </verify>
  <acceptance_criteria>
    - Source assertion: `verifier-precision.md` and `ship-precision-advisory.md` each contain the literal `with the grain`, and neither contains a second definition of it — `grep -cF 'cannot tell' .gsd/capabilities/sota-numerics/fragments/*.md` totals `1` across all four files.
    - Source assertion: line 1 of `verifier-precision.md` contains `declares no gate at execute:wave:post`; line 1 of `ship-precision-advisory.md` contains `declares no gate at ship:pre`.
    - Test command: `wc -l` prints `<= 9` for `verifier-precision.md` and `<= 7` for `ship-precision-advisory.md`; `awk 'END{print NR}'` across the four prints `<= 45`.
    - Source assertion: `git diff --name-only origin/main` lists only the four fragment files and `README.md`.
    - Test command: all three suites exit 0, with `Ran 50 tests` and `OK`.
    - Source assertion: `README.md` `## What it changes` table still has exactly five data rows — `grep -c '^| \`' README.md` is `5`.
  </acceptance_criteria>
  <done>All six added dimensions are present across the four fragments as one grown standard, each leading word defined exactly once and reused bare, the advisory disclaimers intact, the fragment total at or under 45 lines, and README's five-row table updated in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan author → `check-alternatives.py` | `PLAN.md` text from a different principal is parsed by the gate. Not modified by this plan. |
| capability author → every consumer's planner context | `planner-sota.md` is injected verbatim into a planner's prompt at `plan:pre`. Pruning it wrongly removes a rule a planner needs to pass a blocking gate. |
| `sota-numerics` clone → `gsd-beads` working tree | The clone is untracked inside `gsd-beads`; a repo-wide git command from the wrong root crosses projects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-06 | Denial of Service | `fragments/planner-sota.md` | high | mitigate | Pruning a rule rather than a restatement would leave planners unable to satisfy a `blocking: true, onError: halt` gate, halting every downstream planning run. Task 1's verify greps for each surviving rule token by literal and fails the task on a `LOST RULE:` line; the 50-test suite plus the `TestFoundationalCitationPairing` subset run on the same command. |
| T-23-07 | Tampering | `scripts/check-alternatives.py`, `tests/` | high | mitigate | The temptation when compressing the pairing rule is to "simplify" the test alongside it. Task 1 asserts `git diff origin/main -- scripts/check-alternatives.py tests/` is empty. NOTES.md §5 records why both seats exist. |
| T-23-08 | Spoofing | advisory fragment reading as a gate | medium | mitigate | Adding findings to the verifier and ship fragments risks losing the opening clause that states no gate exists at that point. Task 3 asserts both first lines by literal. |
| T-23-04 | Information Disclosure | `gsd-beads` working tree | medium | mitigate | Every git command is `git -C <clone>`; bd issue gsd-beads-9tg tracks the underlying containment fix. |
| T-23-03 | Elevation of Privilege | installed global mirror | high | mitigate | No task writes to `${GSD_HOME:-$HOME}/.gsd/capabilities/sota-numerics`; Plan 01 established and verified its digest and Plan 05 re-verifies it before publish. |
| T-23-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan or in Phase 23; the phase adds no dependency. |

ASVS L1 assessment: V2/V3/V4/V6 have no surface here. V5 is unchanged — the untrusted-input parser is not modified.
</threat_model>

## Alternatives Considered

- **Prune the fragment's restatement of checker internals**: the planner learns the mechanics from the gate's own failure output instead of from the prompt. The script prints one `<plan_path>: <reason>` line per violation naming the exact failing condition and exactly one `remediation: fix the plans above, then re-run /gsd-plan-phase <phase> --force` line, at `` `.gsd/capabilities/sota-numerics/scripts/check-alternatives.py:323` `` (read directly and verified 2026-09-06). Every deleted clause is reproduced there at the moment it matters, and only then.
- **Grow the fragment and let the total rise past 45 lines**: gsd-core enforces no line, token, or KB ceiling on fragments below an 8 MiB DoS backstop (`` `/home/dd/.claude/gsd-core/bin/lib/capability-validator.cjs:2672-2724` ``, verified 2026-09-06), so nothing mechanical would stop it. Rejected because the cost is paid model-side, not host-side: [Anthropic, "Effective context engineering for AI agents" (2025)](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents) frames context as a finite resource whose quality degrades as it grows, and [Chroma Research, "Context Rot" (2025)](https://www.morphllm.com/context-rot) measured that degradation across all 18 models tested, monotonically with input length and independent of whether the window is nominally full.
- **Move the planner detail into README.md and point the fragment at it**: the fragment shrinks to a pointer and the rules live once, in the README. Rejected on correctness before cost: gsd-core materializes `fragment.inline` into the prompt verbatim with nothing that reads a sibling file (`` `/home/dd/.claude/gsd-core/references/loop-hook-dispatch.md` ``, verified 2026-09-06), so the planner would receive a pointer to a file it has no guaranteed access to, and the meaning would then live in two places that drift.

Decided by: performance. The quantity being minimized is per-invocation prompt tokens on the planner's context, the highest-ranked criterion, and pruning restatement is the only one of the three options that reduces it without deleting a rule or introducing an unresolvable pointer.

### Internal design alternatives

- **Prune before adding versus add before pruning**: prune first (Task 1, landing at exactly 10 lines) so the budget headroom is proven before it is spent. Adding first would leave the fragment temporarily over its own ceiling and make a failed prune look like a budget failure rather than a rule-loss failure.
- **Merging the three pairing lines versus deleting two of them**: merge, not delete. NOTES.md §5 records that this fragment and `TestFoundationalCitationPairing` teach one rule from two seats and must stay in step; deleting the canonical-pairing guidance would let a foundational citation fail a gate the test says should pass it.

## Artifacts this phase produces

This plan's additions to the phase-wide artifact list in `23-01-PLAN.md`:

| Symbol | Kind | Location |
|---|---|---|
| `with the grain` | new leading word, defined once | `fragments/planner-sota.md` |
| plan-completeness sentence | new instruction line | `fragments/planner-sota.md` |
| merged foundational-pairing line | three lines collapsed to one, rule preserved | `fragments/planner-sota.md` |
| merged internal-alternatives line | two lines collapsed to one, rule preserved | `fragments/planner-sota.md` |
| with-the-grain finding clause | new `Flag ...` clause | `fragments/verifier-precision.md` |
| extended `confirm` sentence | modified to cover convention claims | `fragments/ship-precision-advisory.md` |
| `plan:pre` / `execute:wave:post` / `ship:pre` Behavior cells | modified table cells | `README.md` `## What it changes` |

No new file path is created. Phase-wide, the six added dimensions are complete after this plan; Plans 03 through 05 refactor the capability's own prose to comply with them and publish `0.2.0`.

<verification>
- planner-sota.md is exactly 10 lines after Task 1 and 12 or fewer after Task 2, with every gate rule still present by literal (D-08).
- The four fragments total 45 lines or fewer after every task (D-08).
- All three pre-existing suites exit 0 after every task (D-20, D-21).
- `git diff origin/main -- scripts/check-alternatives.py tests/` is empty (D-01).
- Both advisory fragments retain their opening no-gate-at-this-point clause (D-02, D-03).
</verification>

<success_criteria>
All six added dimensions are present across the four fragments, `with the grain` / `quiet` / `legible` are each defined exactly once and reused as bare tokens, the fragment total is at or under 45 lines, the three suites are green, and README's five-row `## What it changes` table documents the added behavior in place.
</success_criteria>

<output>
Create `.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-02-SUMMARY.md` when done.
</output>
