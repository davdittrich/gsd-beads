---
beads_epic: gsd-beads-sac
phase: 23-extend-sota-definition-in-sota-numerics-and-refactor-plugin
plan: 04
type: execute
wave: 3
depends_on:
  - 23-02
target_repo: /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013
files_modified:
  - .gsd/capabilities/sota-numerics/NOTES.md
  - README.md
autonomous: true
requirements:
  - D-02
  - D-10
  - D-16
  - D-17
  - D-18
  - D-20
  - D-21
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every behavioral claim in README.md resolves to a value in capability.json or a line in check-alternatives.py, proven by a single command that names each claim (D-20)."
    - "README.md's `## What it changes` table describes what the four fragments actually say after Plans 01 and 02 (D-17)."
    - "NOTES.md contains no passage that README.md already states independently, and every anti-regression entry survives (D-18)."
    - "README.md's prose meets the legible standard the executor fragment defines: a claim is stated once, in one place, with a checkable bound (D-16, D-17)."
    - "The three existing suites pass unchanged (D-20)."
  prohibitions:
    - "NOTES.md gains no new section, no new entry, and no new sentence — it is prune-only (D-18)."
    - "No reasoning is deleted from NOTES.md that README.md does not already state; a bulk cut is forbidden, the prune is line by line against README's final text (D-18)."
    - "README gains no section that restates the fragments' content; the added dimensions are documented inside the existing five-row table (D-17)."
    - "No capability behavior changes: no manifest, no fragment, no script, no hook, no test is edited by this plan (D-01, D-19, D-21)."
    - "The globally installed capability at `${GSD_HOME:-$HOME}/.gsd/capabilities/sota-numerics` is not written to (recursion guard)."
  artifacts:
    - .gsd/capabilities/sota-numerics/NOTES.md
    - README.md
  key_links:
    - "README.md and NOTES.md are the capability's only two shipped reasoning documents. Every other prose pointer in the repository must resolve to one of them or to a line of code; a pointer to a planning artifact is dead for every consumer."
    - "README's `## What it changes` table row and the fragment it describes are the two halves of one claim. They live in different files with no tooling between them, so the claim-trace command is what keeps them honest."
---

<objective>
Make the capability's two shipped documents comply with the standard the capability now teaches, and prove that every behavioral claim README makes is backed by a line of code or config rather than by an adjective.

Purpose: the phase's whole argument is that a plugin asking others to write complete, unambiguous, project-consistent prose should demonstrably do so itself. README is the document a consumer reads before installing; NOTES.md is the document a future contributor reads before "fixing" a deliberate divergence. Both are agent-facing prose in the self-compliance scope, and README additionally carries the burden of accuracy about what the four fragments now say.

Output: a pruned NOTES.md with its anti-regression entries intact, and a README whose behavioral claims all resolve mechanically.

**Repository:** every path below is relative to the `sota-numerics` clone at `/home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013`, NOT to `gsd-beads`. Work continues on the branch `feat/extended-sota-definition`.

**Recursion guard:** the installed global mirror at `${GSD_HOME:-$HOME}/.gsd/capabilities/sota-numerics` is what the running `plan:post` gate executes. It is not source and no task writes to it. Its tree digest on 2026-09-06 was `3ea50c85d28641b249ab71ce11374468652c9ff0388b64c464205a10e99e413e`.
</objective>

<validation_choices>
No SPEC.md and no REQ-IDs exist for this phase, so the spec-less probe fallback is skipped by design and `requirements` carries decision IDs from 23-CONTEXT.md. TDD mode is enabled in `.planning/config.json`, but this plan produces only Markdown documentation, which the TDD contract exempts and D-21 independently forbids adding test infrastructure for. The claim-trace command in Task 2 is a verification command, not a test file: it lives in this plan and in the summary, and nothing is added to the repository. It was run green against the unmodified tree on 2026-09-06, so it is known to pass on a no-op rather than assumed to.
</validation_choices>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-CONTEXT.md
@.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-RESEARCH.md
@.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-PATTERNS.md
@.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tracker-id="beads:gsd-beads-sac.10">
  <name>Task 1: Prune NOTES.md line by line against what README already states</name>
  <beads-id>gsd-beads-sac.10</beads-id>
  <files>.gsd/capabilities/sota-numerics/NOTES.md</files>
  <!-- beads: content synced to bd -- see `bd show gsd-beads-sac.10` -->
</task>

<task type="auto" tracker-id="beads:gsd-beads-sac.11">
  <name>Task 2: Trace every behavioral claim in README to code or config</name>
  <beads-id>gsd-beads-sac.11</beads-id>
  <files>README.md</files>
  <!-- beads: content synced to bd -- see `bd show gsd-beads-sac.11` -->
</task>

<task type="auto" tracker-id="beads:gsd-beads-sac.12">
  <name>Task 3: Apply the legible standard to README's own prose</name>
  <beads-id>gsd-beads-sac.12</beads-id>
  <files>README.md</files>
  <!-- beads: content synced to bd -- see `bd show gsd-beads-sac.12` -->
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capability author → marketplace reader | README is the document a consumer reads before installing. A claim it makes that no code backs is an unearned assurance about what the capability blocks. |
| capability author → future contributor | NOTES.md is the only shipped record of why several deliberate divergences exist. Pruning it wrongly removes the guard against a well-meaning regression. |
| `sota-numerics` clone → `gsd-beads` working tree | The clone is untracked inside `gsd-beads`; a repo-wide git command from the wrong root crosses projects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-12 | Spoofing | `README.md` behavioral claims | high | mitigate | A README asserting behavior the code does not implement is an unearned security and correctness assurance to every installer. Task 2 resolves twelve claims mechanically against `capability.json` and `check-alternatives.py` and fails on any `FAIL` line; the claim-to-source map goes in the summary. |
| T-23-13 | Repudiation | `NOTES.md` anti-regression entries | high | mitigate | Deleting the recency-rule warning or the do-not-revert instruction would remove the only shipped record of two deliberate divergences, inviting a regression a cross-AI review already caught once. Task 1 asserts all five headings survive, asserts three anchor literals by name, and asserts zero added lines. |
| T-23-14 | Tampering | `README.md` reference list | medium | mitigate | A prose-shortening pass is the natural moment to lose citations. Task 3 asserts all seven author surnames survive. |
| T-23-03 | Elevation of Privilege | installed global mirror | high | mitigate | No task writes to `${GSD_HOME:-$HOME}/.gsd/capabilities/sota-numerics`; Plan 05 re-verifies its digest before publish. |
| T-23-04 | Information Disclosure | `gsd-beads` working tree | medium | mitigate | Every git command is `git -C <clone>`; bd issue gsd-beads-9tg tracks the containment fix. |
| T-23-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan or in Phase 23; the phase adds no dependency, so package legitimacy is not applicable rather than skipped. |

ASVS L1 assessment: V2, V3, V4, V5, and V6 have no surface in this plan — it edits two Markdown documents and changes no code, no manifest, and no hook.
</threat_model>

## Alternatives Considered

- **Extend the existing five `## What it changes` rows**: document each added dimension inside the row for the lifecycle point whose fragment carries it, so a fragment and its documentation stay in the same row of the same table. Citation: `` `README.md` `` lines 9-15 and the four-entry `contributions` array in `` `.gsd/capabilities/sota-numerics/capability.json` ``, both read directly and verified 2026-09-06. The table already has exactly one row per contribution point plus one for the gate, so the mapping is already established and needs no new structure.
- **Add a new section listing the six dimensions**: give the extended definition its own level-two section with one entry per dimension. Rejected because it creates a second place the definition lives, so a later fragment edit silently desynchronizes the README — the same duplication this repository is already paying down elsewhere, since `` `.gsd/capabilities/sota-numerics/NOTES.md` `` is being pruned in this very plan precisely on the grounds that README states part of it (read and verified 2026-09-06). Cost is real too: [Anthropic, "Effective context engineering for AI agents" (2025)](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents) and [Chroma Research, "Context Rot" (2025)](https://www.morphllm.com/context-rot) both make the case that added length degrades comprehension monotonically, and a section restating four fragments is pure addition.
- **Point README at the fragment files instead of describing them**: replace the Behavior cells with references to `fragments/*.md`. Rejected on reachability. The marketplace entry serves this repository by URL with no ref — `source.url` is `https://github.com/davdittrich/sota-numerics.git` in `` `.claude-plugin/marketplace.json` `` (verified 2026-09-06) — so README is frequently read as a rendered listing page where the four fragment files are not one click away, and a reader deciding whether to install would have to open four files to learn what the plugin does.

Decided by: maintenance. Performance is a wash across all three: README is not injected into any prompt, so its length costs a human reader's attention rather than an agent's context budget, and the criterion that normally decides this class of question does not separate the options. Simplicity and LOC slightly favor the pointer option, which is the shortest, but it fails on reachability rather than on cost. What separates the three is which one stays true without anyone remembering to update it, and only the table row does: the row and its fragment are the two halves of one claim, sitting in one place, with the claim-trace command holding them honest.

### Internal design alternatives

- **Line-by-line prune of NOTES.md versus deleting whole sections**: line by line. Three of the five sections have a README-covered half and an uncovered half; deleting either whole section would lose an anti-regression entry, and keeping either whole section would leave the duplication the decision asks to remove.
- **Auditing README claims before editing versus after**: before. Running the claim-trace command first distinguishes a claim that Plans 01 through 03 invalidated from a claim that was never backed, and the twelve-claim baseline was proven green on the unmodified tree so a red line is signal rather than noise.
- **Recording the claim-to-source map in the summary versus in a repository file**: the summary. A file in the repository would be new documentation infrastructure for prose, which is exactly what the decision forbids, and it would immediately become a third place the same facts live.

## Artifacts this phase produces

This plan's additions to the phase-wide artifact list in `23-01-PLAN.md`:

| Symbol | Kind | Location |
|---|---|---|
| pruned sections 1, 2, 3 | shortened prose, headings and anti-regression entries retained | `.gsd/capabilities/sota-numerics/NOTES.md` |
| corrected behavioral claims | edited sentences, each resolving to a file line or JSON key path | `README.md` |
| legible-standard prose pass | edited sentences across every section except the reference list | `README.md` |
| claim-to-source map | new table, one row per audited claim | `23-04-SUMMARY.md` (planning artifact, not shipped) |

No new file path is created in the capability. No level-two section is added to or removed from either document.

<verification>
- NOTES.md is shorter than 115 lines, keeps all five level-two headings, adds zero lines, and retains its anti-regression anchors (D-18).
- The claim-trace command prints `README-CLAIMS-TRACED` with twelve `ok` lines (D-20).
- README is at most 260 lines, keeps its five-row table and its seven citations (D-17).
- All three pre-existing suites exit 0 with `Ran 50 tests ... OK` (D-20, D-21).
- The full branch diff against `origin/main` lists exactly the nine files Phase 23 is scoped to change.
</verification>

<success_criteria>
The capability's two shipped documents meet the standard it now teaches: every behavioral claim in README resolves to code or config by a single command, NOTES.md carries no passage README already states while keeping every anti-regression entry, and no capability behavior changed.
</success_criteria>

<output>
Create `.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-04-SUMMARY.md` when done.
</output>
