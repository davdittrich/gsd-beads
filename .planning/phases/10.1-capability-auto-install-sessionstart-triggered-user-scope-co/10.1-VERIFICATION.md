---
phase: 10.1-capability-auto-install-sessionstart-triggered-user-scope-co
verified: 2026-08-17T09:07:33Z
status: passed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 10.1: Capability Auto-Install Verification Report

**Phase Goal:** A shared SessionStart-triggered mechanism grants `capability install --scope
global --yes` automatically for a capability the first time it's needed — no manual command
required. Applied to both `beads` (Phase 6) and `ponytail` (Phase 10). Must close the
silent-invalidation gap discovered debugging Phase 10 via detect-and-re-grant, not grant-once.
**Verified:** 2026-08-17T09:07:33Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | First run with no prior sidecar hash installs `beads` at global scope and prints exactly one notice line | ✓ VERIFIED | `bash tests/test-capability-auto-install.sh` case1 PASS; live re-run confirms `Auto-installed capability: <id> (user scope)` string in script |
| 2 | Second run against a byte-identical bundle is completely silent and never invokes `capability install` | ✓ VERIFIED | test case2 PASS — zero stdout/stderr, zero recorded stub invocations |
| 3 | Any file edit inside the bundle dir between runs re-invokes `capability install` and reprints the notice (detect-and-re-grant, not grant-once — closes the silent-invalidation gap) | ✓ VERIFIED | test case3 PASS |
| 4 | `gsd-tools` unresolvable (exit 127) or `capability install` failing nonzero prints one visible warning to stderr and exits 0 (fail-open-but-loud) | ✓ VERIFIED | test case4 (install-failure) and case5 (no-tool) PASS, distinguishable stderr text, no sidecar write on failure |
| 5 | Hook behaves identically whether `CLAUDE_PLUGIN_ROOT` is exported or derived from `dirname $0` | ✓ VERIFIED | test case6 PASS — byte-identical stdout/stderr/argv |
| 6 | `ponytail-everywhere` ships its own `.gsd/capabilities/ponytail/` bundle so a ponytail-only install has something to install | ✓ VERIFIED | `diff -r .gsd/capabilities/ponytail ponytail-everywhere/.gsd/capabilities/ponytail` exits 0, no output — 5 files present |
| 7 | Session start in the `ponytail-everywhere` plugin auto-installs `ponytail` with one notice, silent thereafter | ✓ VERIFIED | Real machine evidence: `~/.gsd-capabilities.json` lists `ponytail` entry (source = `ponytail-everywhere/.gsd/capabilities/ponytail`), `~/.gsd/capability-auto-install-ponytail.hash` exists, `~/.claude/plugins/cache/gsd-beads/ponytail-everywhere/0.1.0/.gsd/capabilities/ponytail/capability.json` present in real plugin cache from a genuine `claude plugin install` cycle |
| 8 | The two vendored `capability-auto-install.sh` copies are byte-identical, and CI fails if they ever diverge | ✓ VERIFIED | `diff hooks/capability-auto-install.sh ponytail-everywhere/hooks/capability-auto-install.sh` exits 0, no output; `.github/workflows/ci.yml` contains a `cmp`/`diff -r` parity step in job `checks` on `push`+`pull_request` |
| 9 | The GitHub Release zip carries `.gsd/capabilities/` for **both** plugins so the release-artifact install path and marketplace-cache path agree on bundle presence | ✓ VERIFIED | `.github/workflows/release.yml` zip allowlist includes `ponytail-everywhere/.claude-plugin`, `ponytail-everywhere/hooks`, `ponytail-everywhere/.gsd` — confirmed present via `git show 383bbc2` (the CR-01 post-review fix commit) and by reading the current file content directly |

**Score:** 9/9 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `hooks/capability-auto-install.sh` | id-validated, hash-gated auto-install script | ✓ VERIFIED | 90+ lines, substantive logic read in full; `bash -n` exits 0 |
| `hooks/session-start.sh` | modified: calls auto-install before `exec bd prime` | ✓ VERIFIED | line 13 calls script, line 15 is `exec`; auto-install strictly precedes exec |
| `tests/test-capability-auto-install.sh` | 6-case stdlib bash smoke test | ✓ VERIFIED | ran directly: 6/6 PASS, exit 0 |
| `ponytail-everywhere/.gsd/capabilities/ponytail/` | 5 files, byte-identical to repo root bundle | ✓ VERIFIED | `diff -r` exit 0 |
| `ponytail-everywhere/hooks/capability-auto-install.sh` | byte-identical vendored copy | ✓ VERIFIED | `diff` exit 0 |
| `ponytail-everywhere/hooks/session-start.sh` | modified: calls auto-install early, before `ponytail.enabled` gate | ✓ VERIFIED | line 6 calls script, before line 8's `gsd-tools.sh` source and before the enabled-gate exit further down |
| `.github/workflows/ci.yml` | new job `checks`, parity + both test suites, push+PR triggers | ✓ VERIFIED | file read in full; 3 steps match plan spec exactly |
| `.github/workflows/release.yml` | `.gsd` (both plugins) added to zip allowlist | ✓ VERIFIED | current file content confirms 3-line addition for `ponytail-everywhere/*`, matching CR-01 fix commit `383bbc2` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `hooks/session-start.sh` | `hooks/capability-auto-install.sh` | invoked with literal `beads`, before `exec` | ✓ WIRED | grep confirms line order |
| `ponytail-everywhere/hooks/session-start.sh` | `ponytail-everywhere/hooks/capability-auto-install.sh` | invoked with literal `ponytail`, before `ponytail.enabled` early exit | ✓ WIRED | grep confirms line order (line 6 vs. later gate) |
| `capability-auto-install.sh` | `gsd-core capability install` CLI | inlined `gsd_tools()` resolver, absolute `$BUNDLE_DIR` spec, `--scope global --yes` | ✓ WIRED | read in full; absolute path construction confirmed, `--scope user` string count is 0 (Pitfall 1 guarded) |
| `.github/workflows/ci.yml` | vendored copies | `cmp`/`diff -r` parity step | ✓ WIRED | present in workflow, both bash suites also invoked |
| `.github/workflows/release.yml` | both plugin dirs | zip allowlist | ✓ WIRED | confirmed post-CR-01 fix |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Auto-install script test suite (beads mechanism) | `bash tests/test-capability-auto-install.sh` | 6/6 `PASS:` lines, `ALL PASS`, exit 0 | ✓ PASS |
| ponytail session-start test suite (unaffected by this phase's edit) | `bash ponytail-everywhere/tests/test-session-start.sh` | 11/11 `PASS:` lines, `ALL PASS`, exit 0 | ✓ PASS |
| Vendored script byte-identity | `diff hooks/capability-auto-install.sh ponytail-everywhere/hooks/capability-auto-install.sh` | exit 0, no output | ✓ PASS |
| Vendored bundle byte-identity | `diff -r .gsd/capabilities/ponytail ponytail-everywhere/.gsd/capabilities/ponytail` | exit 0, no output | ✓ PASS |
| Both scripts pass shell syntax check | `bash -n` on all 4 hook scripts | exit 0 | ✓ PASS |
| Real marketplace install left live evidence on this machine | inspect `~/.gsd-capabilities.json`, `~/.gsd/capability-auto-install-ponytail.hash`, `~/.claude/plugins/cache/gsd-beads/ponytail-everywhere/0.1.0/.gsd/capabilities/ponytail/capability.json` | ledger entry present, sidecar hash file present (mtime 11:05), cached bundle file present | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CAP-01 | 01, 02 | Single authored script vendored per capability plugin | ✓ SATISFIED | Both scripts exist and are byte-identical |
| CAP-02 | 01, 02 | SessionStart wiring runs synchronously every session | ✓ SATISFIED | Both `session-start.sh` files wire the call before their respective exit/exec points |
| CAP-03 | 01 | Whole-bundle-directory hash drift detection | ✓ SATISFIED | `bundle_hash()` function read in full; cases 2/3 exercise it |
| CAP-04 | 01 | One-line grant/re-grant notice, silence otherwise | ✓ SATISFIED | `printf 'Auto-installed capability: %s (user scope)\n'` exact-match test assertion |
| CAP-05 | 01 | Visible warning + fail-open on install failure | ✓ SATISFIED | cases 4/5 in test suite, distinguishable text |
| CAP-06 | 02 | ponytail bundle vendored into shipping plugin dir | ✓ SATISFIED | `diff -r` confirms 5-file bundle present |
| CAP-07 | 02 | Automated byte-identity check across vendored copies | ✓ SATISFIED | `.github/workflows/ci.yml` `cmp`/`diff -r` step |

**Cross-reference against REQUIREMENTS.md:** `grep -n 'CAP-0[1-7]' .planning/REQUIREMENTS.md`
returned no matches. This is **consistent, not a gap** — confirmed by:
1. ROADMAP.md's Phase 10.1 entry explicitly states "minted at planning 2026-08-17" and carries
   the full `Requirements:` line with all 7 IDs written back, per the plan's own instruction.
2. The existing D-01..D-05 precedent cited in the task (other ad-hoc-inserted-phase requirement
   IDs) is **also absent** from REQUIREMENTS.md (`grep -n 'D-0[1-5]' .planning/REQUIREMENTS.md`
   returned no matches either) — confirming this project's established pattern is to track
   ad-hoc-phase requirement IDs only in ROADMAP.md/PLAN.md frontmatter, not to backfill
   REQUIREMENTS.md. No orphaned or missing requirement IDs.

### Anti-Patterns Found

No debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) in any file modified by this phase.

Three advisory findings from `10.1-REVIEW.md` remain open (by design — the task brief for this
verification explicitly notes them as "still open/advisory," distinct from CR-01 which was
fixed). None contradict a phase must-have truth; each is a latent edge case or a pre-existing
inconsistency outside this phase's stated scope:

| File | Finding | Severity | Status |
|------|---------|----------|--------|
| `hooks/capability-auto-install.sh:36-40` (+ vendored twin) | WR-01: `bundle_hash()`'s `find` uses default `-type f -o -type d`, so a symlink anywhere under a bundle is invisible to drift detection (no symlink currently exists in either shipped bundle — latent, not triggered) | ⚠️ Warning | Open, advisory |
| `tests/test-capability-auto-install.sh:55-59,62,80,89,170-176` | WR-02: cases 1-4 and 6 run with cwd inside this real git repo; today no `gsd-core/bin/gsd-tools.cjs` exists here so the PATH stub still wins, but the isolation is incidental rather than enforced (case 5 already neutralizes this correctly) | ⚠️ Warning | Open, advisory |
| `ponytail-everywhere/hooks/session-start.sh:61` vs. fragment files | WR-03: `ponytail.level=lite` advisory text differs between the `SubagentStart` banner ("YAGNI, reuse, stdlib, shortest diff") and the capability fragments ("rungs 1-2 only") — a pre-existing Phase 10 inconsistency, not introduced by this phase's vendoring (verbatim copy) | ⚠️ Warning | Open, advisory, out of this phase's scope (content of fragments unchanged by 10.1) |

### Human Verification Required

None. The one human-verify checkpoint in this phase (Plan 02 Task 3 — real marketplace install
to confirm Assumption A2, the subdirectory-sourced plugin cache layout) was already resolved:
this verification independently re-confirmed the live evidence still present on this machine
(`~/.gsd-capabilities.json` ponytail entry, sidecar hash file, and cached bundle file under
`~/.claude/plugins/cache/gsd-beads/ponytail-everywhere/0.1.0/`), rather than trusting the
SUMMARY's narrative alone.

Assumption A3 (macOS `shasum -a 256` fallback) remains an accepted, explicitly documented gap
per the plan's own pre-approved fallback instruction ("if none available, say so and record it
as an accepted, documented gap") — not an open question requiring a decision, so it is not
routed here as a human-verification item.

### Gaps Summary

None. All 9 derived must-have truths (roadmap goal + both plans' `must_haves.truths`) verified
against the live codebase, not SUMMARY claims: both bash test suites were re-run directly by
this verification (not trusted from SUMMARY's reported pass counts), byte-identity of vendored
copies was re-diffed independently, the CR-01 release.yml fix was confirmed present by reading
the current file content and its introducing commit, and the real-install evidence for
`ponytail` (Assumption A2) was independently inspected on the live filesystem rather than taken
on the SUMMARY's word.

---

_Verified: 2026-08-17T09:07:33Z_
_Verifier: Claude (gsd-verifier)_
