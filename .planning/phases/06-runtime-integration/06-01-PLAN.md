---
phase: 06-runtime-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/hooks.json
  - .claude/settings.json
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [PUB-03, PUB-06]
user_setup: []

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A project that installs beads@gsd-beads gets a SessionStart hook running `bd prime --hook-json` without the user creating or editing any `.claude/settings.json` (PUB-06, ROADMAP criterion 2)."
    - "A session started inside gsd-beads itself fires `bd prime --hook-json` exactly once — never twice (PUB-06, ROADMAP criterion 3)."
    - "A session started with `bd` absent from PATH still completes successfully, emitting one non-blocking hook notice and zero `bd prime` context (PUB-06, ROADMAP criterion 4)."
    - "From a project with no prior gsd-beads state, one written-down command makes gsd-core's capability loader report `beads` as installed and active (PUB-03, ROADMAP criterion 1)."
    - "The bridge command is recorded verbatim, with its real observed output, so Phase 8's README (PUB-07) can transcribe it rather than invent it."
    - "`.gsd/capabilities/beads/` is bit-for-bit unchanged by this phase, so the project-scope consent hash stays valid."
  artifacts:
    - "hooks/hooks.json — SessionStart block, canonically equal to the block at 2b09c1b7fed7c4a89c1bbb8f38d889d792375fff:.claude/settings.json"
    - ".claude/settings.json — absent from the working tree and from the index"
    - ".planning/PROJECT.md — Key Decisions row recording the PUB-03 manual-bridge decision and its three reasons"
    - ".planning/STATE.md — Blockers/Concerns: the Phase-6 open decision closed, the PUB-04 allowlist gap opened for Phase 7/8"
    - ".planning/phases/06-runtime-integration/06-01-SUMMARY.md — verbatim transcripts of the bridge command, the fail-open notice, and the auto-load answer"
  key_links:
    - ".claude-plugin/plugin.json (no `hooks` key) -> Claude Code's convention discovery of hooks/hooks.json — if convention discovery does not apply, the hook ships but never registers"
    - "Claude Code plugin cache copy of hooks/hooks.json -> the running session — the cache is written at install time, so a post-install edit to hooks/hooks.json is invisible until reinstall"
    - "`capability install` spec path -> `<spec>/capability.json` — the spec must name the bundle directory, not the plugin root"
    - "this repo's own dev sessions -> the locally installed plugin — after `.claude/settings.json` is deleted, the plugin staying installed is the only thing that still primes this repo"
    - "`claude plugin validate . --strict` -> marketplace.json presence — with marketplace.json present the validator inspects the marketplace manifest only and never reads hooks/hooks.json"
  prohibitions:
    - "No PATH-probe guard clause, wrapper, or conditional around the packaged hook command; the shipped command is the bare invocation and nothing else."
    - "`.claude/settings.json` must not survive, in whole or in part, once hooks/hooks.json ships."
    - "No postinstall hook, session hook, or script that runs a capability install with consent pre-granted."
    - "The capability-install spec argument must not name the plugin root or the repository root."
    - "No file under `.gsd/capabilities/beads/` may be created, edited, or deleted by this phase."
    - "The marketplace and the plugin must not be added or installed at project scope during this phase."
  flagged_assumptions:
    - "PUB-03 returned `unclassified`/`unresolved` from the deterministic edge-coverage probe. No SPEC.md exists for this phase, so no edge-coverage acceptance criterion was derived for it. Surfaced here rather than resolved: the plan treats PUB-03's edge behavior (what happens when the bridge command is run twice, or against a project that already has a global-scope `beads` capability) as UNVERIFIED. Task 3 records observed behavior; it does not claim coverage."
    - "PUB-06 returned `unclassified`/`unresolved` from the deterministic edge-coverage probe. No SPEC.md exists for this phase. Surfaced rather than resolved: edge behavior beyond the four ROADMAP criteria (e.g. a hook that hangs, a `bd` on PATH that is a different binary, concurrent sessions) is UNVERIFIED by this plan."
    - "RESEARCH.md Assumption A1 (whether hooks/hooks.json auto-loads for a merely-checked-out repo with no `/plugin install`) is UNRESOLVED at plan time. Task 1 answers it empirically before the answer is depended on; the plan does not assume either outcome."
    - "RESEARCH.md's Validation Architecture declares SessionStart hook firing `manual-only — not scriptable via a non-interactive CLI call`. Planner probe on 2026-08-16 contradicted this: `claude -p <prompt> --debug hooks --debug-file <path>` fires SessionStart hooks and writes one countable success line per hook. This plan uses that automated path and keeps one human-check as a TTY-vs-headless backstop."
---

<objective>
Make a plugin install deliver a working session hook, and make the gsd-core capability
reachable from a clean project by one written-down command.

Purpose: after `/plugin install beads@gsd-beads`, a user gets `bd prime` on every session
start with zero hand-editing of their own settings (PUB-06), and knows the exact second
command that activates the gsd-core `beads` capability their plugin install does not
activate (PUB-03). This closes STATE.md's "[Phase 6, open decision]" entry.

Output: `hooks/hooks.json` created, `.claude/settings.json` deleted, the manual bridge
proven from a clean scratch project and recorded verbatim for Phase 8's README.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-runtime-integration/06-RESEARCH.md
@.planning/phases/06-runtime-integration/06-PATTERNS.md
</context>

## Artifacts this phase produces

| Artifact | Change | Owner task |
|----------|--------|-----------|
| `hooks/hooks.json` | **created** — new file, new `hooks/` directory at repo root; one `SessionStart` matcher, one `command`-type hook | Task 1 |
| `.claude/settings.json` | **deleted** — entire file removed from working tree and index (it holds only the `hooks.SessionStart` block) | Task 1 |
| `.claude/settings.local.json` | **untouched** — machine-local, untracked, carries an unrelated `headroom` SessionStart hook | — |
| `.claude-plugin/plugin.json` | **untouched** — no `hooks` key added; convention discovery is what is being proven | Task 1 (asserted unchanged) |
| `.gsd/capabilities/beads/` | **untouched** — asserted clean by `git status --porcelain` | Task 3 (asserted unchanged) |
| `.planning/PROJECT.md` | **modified** — one new row in `## Key Decisions` (line 127) recording the PUB-03 manual-bridge decision | Task 3 |
| `.planning/STATE.md` | **modified** — `### Blockers/Concerns` (line 89): Phase-6 open decision closed, PUB-04 allowlist gap opened | Task 3 |
| `.planning/phases/06-runtime-integration/06-01-SUMMARY.md` | **created** — carries three verbatim transcripts (bridge command + output, fail-open notice, auto-load answer) that Phase 8's PUB-07 README transcribes | executor, on completion |

No new CLI, wrapper, script, or runtime dependency is produced. No package is installed in any
ecosystem.

## Review findings and their disposition

Source: `06-REVIEWS.md` (single reviewer, antigravity, 2026-08-16 — the only reviewer configured in
`.planning/config.json`; the gemini and opencode lanes were skipped by explicit user instruction, so
there is no cross-reviewer consensus and no divergence to adjudicate). Overall risk assessed LOW; no
finding is above LOW severity and none changes `must_haves`, the threat model, or requirements
coverage.

| # | Finding | Severity | Disposition | Where it landed |
|---|---------|----------|-------------|-----------------|
| C1 | Task 3's verification script hardcodes an absolute repository path in the `capability install` spec argument and in the return `cd`, so it fails in another clone, a worktree, or a CI container | LOW | **incorporated** | Task 3 `<action>` (capture `git rev-parse --show-toplevel` before leaving the repo), `<verify>` (`R=$(git rev-parse --show-toplevel)`, spec `"$R/.gsd/capabilities/beads"`, `cd "$R"`), `<acceptance_criteria>` (no checkout-specific path constant), `<output>` (the SUMMARY must mark the root as the reader's own clone root so PUB-07 does not publish this machine's path) |
| C2 | Assumption A1's probe does not gate on its own result, and after `.claude/settings.json` is deleted this repo's dev sessions depend on the plugin staying installed | LOW | **acknowledged — no plan change** | The dependency is already disclosed in four places and stays disclosed: `must_haves.key_links` entry 4, `must_haves.flagged_assumptions` entry 3, Task 1 `<action>` ("record that as a disclosed consequence"), and `<output>` transcript 3. Not gating on the value is deliberate and the reviewer agrees it is correct — Probe A is a measurement, not an assertion, and gating it would force the plan to presume an answer it exists to obtain. Nothing to change |
| S1 | Parameterize the Task 3 path dynamically | LOW | **incorporated** | Same change as C1 — one fix covers both |
| S2 | Pre-flight `claude plugin list` before Probe A so a stale `beads` install cannot contaminate the auto-load baseline | LOW | **incorporated** | Task 1 `<precondition>` (halt if `beads@gsd-beads` is installed at any scope), `<action>` (re-assert at the point of use, since Probe A is the only measurement whose meaning depends on absence), `<verify>` (gate line immediately before Probe A), `<acceptance_criteria>` (baseline was clean when Probe A ran). The `name@marketplace` list format was confirmed against a live `claude plugin list` run on 2026-08-16, so the gate matches a real line rather than a guessed one |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — a plugin install delivers the SessionStart hook, fired exactly once</name>

  <files>hooks/hooks.json (create), .claude/settings.json (delete)</files>

  <read_first>
    - `.claude/settings.json` — the literal content source; read it in full before deleting it
    - `.claude/settings.local.json` — confirm it is a separate file with an unrelated SessionStart hook that must survive
    - `.claude-plugin/plugin.json` — confirm there is no `hooks` key and that none is to be added
    - `.claude-plugin/marketplace.json` — the file that must be moved aside and restored for the second validate run
    - `.planning/phases/06-runtime-integration/06-PATTERNS.md` — "Exact content to copy verbatim" and the "Do NOT add" note
    - `.planning/phases/06-runtime-integration/06-RESEARCH.md` — Pattern 2, Pitfall 3, Pitfall 4, Assumption A1
  </read_first>

  <precondition>
    `claude` resolves on PATH at version 2.1.x, `bd` resolves at `/usr/bin/bd`, `jq` resolves on
    PATH, `claude plugin marketplace list` does NOT already list a `gsd-beads` marketplace, and
    `claude plugin list` shows no `beads@gsd-beads` entry at any scope. Halt if any is unmet —
    Phase 5's round trip is expected to have removed both. A stale install left behind at any scope
    would make Probe A report a fire that came from that install rather than from convention
    auto-load, silently converting Assumption A1's empirical answer into a false positive;
    uninstall it before starting rather than working around it.
  </precondition>

  <reversibility rating="reversible">
    Deleting `.claude/settings.json` is undone by `git checkout 2b09c1b7fed7c4a89c1bbb8f38d889d792375fff -- .claude/settings.json`.
    Creating `hooks/hooks.json` is undone by deleting it. No published contract, no migration.
  </reversibility>

  <action>
    Create `hooks/hooks.json` at the repository root (creating the `hooks/` directory). Its content
    is the SessionStart block that lives today in `.claude/settings.json`, moved container-for-container
    with nothing added and nothing renamed: a top-level `hooks` object, a `SessionStart` array, one
    entry whose `matcher` is the empty string and whose `hooks` array holds exactly one element with
    `type` set to `command` and `command` set to the bare `bd` prime invocation with the `--hook-json`
    flag — the identical string already in `.claude/settings.json`. Ship that invocation bare: no
    PATH-probe guard, no `&&`/`||` chaining, no shell wrapper, no `timeout` key, no extra matcher.
    Claude Code's own SessionStart exit-code contract already fails open with one non-blocking notice
    when the binary is missing, which is what ROADMAP criterion 4 asks for (06-RESEARCH.md Pitfall 4),
    and divergence from the already-proven dev-hook string is what would break it.

    Do NOT add a `hooks` key to `.claude-plugin/plugin.json`. Claude Code discovers a plugin's hooks
    at the conventional `hooks/hooks.json` path; leaving `plugin.json` untouched is precisely what
    this tracer proves. Verified on 2026-08-16: plugins with no manifest `hooks` field are logged as
    `Read hooks.json for plugin <name>`, while a plugin that declares one is logged as
    `Read manifest hooks for plugin <name>` — the convention path is live.

    Then delete `.claude/settings.json` outright. That file's entire content is the one
    `hooks.SessionStart` block being moved; removing the key would leave an empty object with no
    consumer. Leaving the file in place double-fires `bd prime` the moment the plugin is also
    installed locally, because Claude Code deduplicates an identical handler only across settings
    files and explicitly keeps a plugin's copy separate (06-RESEARCH.md Pitfall 3). Leave
    `.claude/settings.local.json` alone — it is untracked, machine-local, and carries an unrelated
    `headroom` SessionStart hook.

    Validate in both modes (Phase 5's D-09 discipline). Run `claude plugin validate . --strict` with
    `.claude-plugin/marketplace.json` in place, then move that file aside, run the same command
    again so the validator inspects the plugin manifest and its components instead of the marketplace
    manifest, and restore it unconditionally. Verified on 2026-08-16: with `marketplace.json`
    present the validator prints `Validating marketplace manifest: ...` and reads nothing else — the
    first run alone cannot see `hooks/hooks.json` at all. The root `CLAUDE.md` warning is the one
    documented permanent exception (D-10, Phase 5); nothing the validator says about hooks is
    acceptable.

    Then prove the runtime behavior in three ordered headless probes, each a
    `claude -p` run with `--debug hooks --debug-file <log>` and stdin redirected from `/dev/null`.
    Probe A runs with the plugin NOT installed and `.claude/settings.json` already gone: it answers
    RESEARCH.md Assumption A1 empirically — if the bd-prime success line count is 0, a merely
    checked-out repo does not auto-load `hooks/hooks.json`, and this repo's own dev sessions now
    depend on the plugin staying installed. Record the observed count either way; do not assume an
    outcome. Immediately before Probe A, re-assert the clean baseline the precondition demands by
    listing the installed plugins and confirming no `beads@gsd-beads` entry is present; Probe A is
    the only measurement in this phase whose meaning depends on the plugin being absent, so the
    baseline is asserted at the point of use and not merely at task entry (addresses the review's
    pre-flight local-plugin-state suggestion). The list line is `name@marketplace`-formatted, so
    that exact pair is what identifies a stale install. Then add the local marketplace with `claude plugin marketplace add ./ --scope local` and
    install with `claude plugin install beads@gsd-beads -s local -y`. Use local scope for both:
    project scope writes the marketplace and plugin records into `.claude/settings.json`, recreating
    the tracked file this task just deleted. Probe B then runs with the plugin installed and must
    show the plugin's hooks.json read and exactly one bd-prime success line. Do NOT uninstall at the
    end — leave the plugin installed at local scope as this repo's dogfooding setup, and record that
    as a disclosed consequence of deleting the settings hook.
  </action>

  <verify>
    <automated>
BASE=2b09c1b7fed7c4a89c1bbb8f38d889d792375fff
L=$(mktemp -d)
# 1. hooks/hooks.json is canonically the old settings.json block, and settings.json is gone
diff <(git show $BASE:.claude/settings.json | jq -S .) <(jq -S . hooks/hooks.json) || exit 1
test ! -e .claude/settings.json || exit 1
test -z "$(git ls-files .claude/settings.json)" || exit 1
# 2. exactly one SessionStart hook entry, bare command, plugin.json untouched
test "$(jq '[.hooks.SessionStart[].hooks[]] | length' hooks/hooks.json)" = 1 || exit 1
test "$(jq -r '.hooks.SessionStart[].hooks[].command' hooks/hooks.json)" = "$(git show $BASE:.claude/settings.json | jq -r '.hooks.SessionStart[].hooks[].command')" || exit 1
git diff --exit-code $BASE -- .claude-plugin/plugin.json || exit 1
# 3. double-run validate; neither run may say anything about hooks
claude plugin validate . --strict 2>&1 | tee $L/v1.txt
mv .claude-plugin/marketplace.json $L/mp.bak; claude plugin validate . --strict 2>&1 | tee $L/v2.txt; mv $L/mp.bak .claude-plugin/marketplace.json
grep -i hook $L/v1.txt $L/v2.txt && exit 1
# 4. probe A — plugin NOT installed (answers Assumption A1; record, do not gate on the value)
claude plugin list 2>&1 | grep -q 'beads@gsd-beads' && exit 1   # a stale install would fake the answer
claude -p "reply with the single word ok" --debug hooks --debug-file $L/a.log --allowedTools "" < /dev/null
echo "A1 auto-load fires: $(grep -c 'Hook SessionStart (bd prime --hook-json) provided additionalContext' $L/a.log)"
# 5. install at local scope, probe B — must be exactly one fire, from the plugin
claude plugin marketplace add ./ --scope local
claude plugin install beads@gsd-beads -s local -y
test ! -e .claude/settings.json || exit 1
claude -p "reply with the single word ok" --debug hooks --debug-file $L/b.log --allowedTools "" < /dev/null
grep -q 'Read hooks.json for plugin beads' $L/b.log || exit 1
test "$(grep -c 'Hook SessionStart (bd prime --hook-json) provided additionalContext' $L/b.log)" = 1 || exit 1
echo PASS
    </automated>
    <human-check>
      Start one real interactive Claude Code session in this repo (a TTY session, not `-p`) and
      confirm the beads context block appears exactly once. This is the backstop for the headless
      probes: it catches any divergence between the `-p` path and the TTY path.
    </human-check>
  </verify>

  <acceptance_criteria>
    - `diff <(git show 2b09c1b7fed7c4a89c1bbb8f38d889d792375fff:.claude/settings.json | jq -S .) <(jq -S . hooks/hooks.json)` produces no output and exits 0.
    - `test ! -e .claude/settings.json` exits 0 and `git ls-files .claude/settings.json` prints nothing.
    - `jq '[.hooks.SessionStart[].hooks[]] | length' hooks/hooks.json` prints `1`.
    - `jq -r '.hooks.SessionStart[].hooks[].command' hooks/hooks.json` prints exactly the command string returned by the same jq expression against `git show 2b09c1b7fed7c4a89c1bbb8f38d889d792375fff:.claude/settings.json` — no prefix, no suffix, no chaining.
    - `git diff --exit-code 2b09c1b7fed7c4a89c1bbb8f38d889d792375fff -- .claude-plugin/plugin.json` exits 0.
    - `.claude/settings.local.json` still exists and still contains its `headroom` SessionStart hook.
    - Both `claude plugin validate . --strict` runs complete, `.claude-plugin/marketplace.json` is back in place afterwards, and `grep -i hook` over both captured outputs matches nothing.
    - Probe B's debug log contains a line matching `Read hooks.json for plugin beads`.
    - `grep -c 'Hook SessionStart (bd prime --hook-json) provided additionalContext'` over probe B's debug log equals exactly `1`.
    - `claude plugin list` printed no `beads@gsd-beads` entry at the moment immediately before Probe A ran; if it did, the task halted rather than recording a meaningless Probe A count.
    - Probe A's count for the same pattern is recorded verbatim in the SUMMARY together with the resulting statement about whether a checked-out repo auto-loads plugin hooks.
    - `claude plugin list` shows `beads` installed at local scope at task end (not uninstalled).
    - Interactive TTY session in this repo shows the beads context block exactly once.
  </acceptance_criteria>

  <done>
    `hooks/hooks.json` ships the SessionStart hook, `.claude/settings.json` is gone from tree and
    index, the plugin is installed locally, and a session in this repo primes exactly once from the
    plugin's hook — ROADMAP criteria 2 and 3 demonstrated, PUB-06 half-satisfied (criterion 4 is
    Task 2).
  </done>
</task>

<task type="auto">
  <name>Task 2: Fail-open — a session with `bd` off PATH still starts, with one notice</name>

  <files>hooks/hooks.json (under test, unmodified); transcript recorded in 06-01-SUMMARY.md</files>

  <read_first>
    - `hooks/hooks.json` — the file under test, as written by Task 1
    - `.planning/phases/06-runtime-integration/06-RESEARCH.md` — Pitfall 4 (the exit-code contract this task is proving, not implementing)
    - `.planning/ROADMAP.md` — Phase 6 success criterion 4
  </read_first>

  <precondition>
    Task 1 is complete: the plugin is installed at local scope and `hooks/hooks.json` exists.
    `bd` resolves only at `/usr/bin/bd` (confirm with `command -v bd`); if it also resolves
    elsewhere on PATH, extend the shim construction to shadow every location before running.
  </precondition>

  <action>
    Prove ROADMAP criterion 4 without changing any shipped file. Build a throwaway PATH shim
    directory holding a symlink to every entry in `/usr/bin`, then remove the single `bd` symlink
    from it, so `claude`, `node`, `git` and `jq` still resolve while `bd` does not. Run one headless
    `claude -p` session with `PATH` set to that shim only, with `--debug hooks` and a debug file,
    stdin from `/dev/null`.

    Three things must hold and be recorded: the session exits 0 (fail-open — the missing binary did
    not block startup), the debug log carries zero bd-prime success lines (the hook genuinely could
    not run, so this is not a false green), and the log carries at least one notice naming the failed
    hook. Capture that notice line VERBATIM into the SUMMARY — its exact wording is what Phase 8's
    README caveats section (PUB-07) has to transcribe rather than paraphrase, and the plan does not
    guess it in advance. Delete the shim directory afterwards; it lives under a mktemp path and
    touches nothing in the repository.

    Do not add a guard to `hooks/hooks.json` if the notice looks ugly. The notice IS the deliverable:
    criterion 4 asks for one visible notice, and 06-RESEARCH.md's Don't Hand-Roll table records that
    a hand-rolled probe would duplicate a platform guarantee.
  </action>

  <verify>
    <automated>
S=$(mktemp -d); L=$(mktemp -d)
ln -s /usr/bin/* $S/ 2>/dev/null; rm -f $S/bd
env -u BD_HOME PATH="$S" sh -c 'command -v bd' && exit 1   # bd must be unreachable in the shim
env PATH="$S" claude -p "reply with the single word ok" --debug hooks --debug-file $L/c.log --allowedTools "" < /dev/null; RC=$?
test $RC -eq 0 || exit 1
test "$(grep -c 'Hook SessionStart (bd prime --hook-json) provided additionalContext' $L/c.log)" = 0 || exit 1
grep -in 'bd prime' $L/c.log | head -20   # capture the verbatim failure notice for the SUMMARY
rm -rf $S
git status --porcelain hooks/ .claude-plugin/ | grep . && exit 1
echo PASS
    </automated>
  </verify>

  <acceptance_criteria>
    - `command -v bd` under the shim PATH exits non-zero (the test setup is real — the shim actually hides `bd`).
    - The `claude -p` process exits 0 under the shim PATH.
    - `grep -c 'Hook SessionStart (bd prime --hook-json) provided additionalContext'` over that run's debug log equals `0`.
    - At least one debug-log line naming the `bd prime` hook and a failure/non-blocking status is captured, and its verbatim text is pasted into 06-01-SUMMARY.md.
    - `git status --porcelain hooks/ .claude-plugin/` prints nothing — this task modified no shipped file.
    - `hooks/hooks.json`'s command string is unchanged from Task 1 (re-assert with the Task 1 jq equality check).
  </acceptance_criteria>

  <done>
    ROADMAP criterion 4 demonstrated: with `bd` unreachable, the session still starts, no beads
    context is injected, and exactly the platform's own non-blocking notice appears — with its
    wording recorded for the README. PUB-06 fully satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 3: PUB-03 — prove the manual capability bridge from a clean project and record the decision</name>

  <files>.planning/PROJECT.md, .planning/STATE.md</files>

  <read_first>
    - `.planning/phases/06-runtime-integration/06-RESEARCH.md` — Pattern 1 (the exact command and why the spec path ends at the bundle directory), Pitfall 1, Pitfall 2, Pitfall 5
    - `.planning/PROJECT.md` — the `## Key Decisions` table at line 127; match its three-column row format exactly
    - `.planning/STATE.md` — the `### Blockers/Concerns` list at line 89; match the existing `[Phase N, label]` entry style
    - `.gsd/capabilities/beads/capability.json` — confirm this is the directory the spec argument must name (it is the directory holding `capability.json`); read only, do not edit
    - `.planning/REQUIREMENTS.md` — PUB-03's text and PUB-04's ship allowlist, which omits `.gsd/`
  </read_first>

  <precondition>
    `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs"` exists and is executable, and the scratch
    directory used for the clean-project test is created fresh outside this repository (under
    `/tmp`), with no `.gsd/` and no `.planning/` in it or in any parent.
  </precondition>

  <reversibility rating="costly">
    Choosing the documented manual bridge over an automated postinstall bridge is not one-way — no
    published contract is created — but reversing it later means writing the automation AND rewriting
    the README section Phase 8 will have transcribed from this task's output. Flagged, not gated.
  </reversibility>

  <action>
    Satisfy PUB-03 by the manual-alternative path and prove it, then write the decision down.

    First capture this repository's root into a shell variable with `git rev-parse --show-toplevel`,
    run from inside the repository and BEFORE changing directory anywhere else. Every later
    reference to the capability bundle is formed from that variable. Do not write this machine's
    checkout path into the command: the same script must run from a different clone, a worktree, or
    a CI container, and once the shell has moved into the scratch project `git rev-parse` no longer
    resolves to this repository, so capturing it late is not an option (addresses the review's
    absolute-path finding).

    Create a fresh scratch project under `/tmp` with nothing in it, `cd` into it, and run gsd-core's
    existing capability installer: `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" capability install`
    with the spec argument formed as the captured repository root joined to
    `.gsd/capabilities/beads`, plus
    `--scope project --yes`. The spec must name that bundle directory, because gsd-core's
    `resolveLocal()` reads `capability.json` from the spec root directly; naming the repository root
    or the plugin root fails with a missing manifest. Then query
    `capability state --raw` from that same scratch directory and confirm the `beads` entry reports
    both `installed` and `active` true. Capture the full command line and the full state output
    verbatim — this transcript is the deliverable Phase 8's PUB-07 README transcribes.

    Build nothing. Do not write a wrapper script, a postinstall hook, or a SessionStart hook that
    runs this command with consent pre-granted: gsd-core's CB-3 consent gate is deliberately
    human-gated, an automated grant defeats it, and the bundle path such automation would target is
    absent from PUB-04's Phase-8 ship allowlist, so it would work today and silently stop working at
    first public release (06-RESEARCH.md Alternatives Considered, Pitfall 1, Pitfall 2).

    Return to the captured repository root and remove the scratch directory once the transcript is
    captured.

    Then record two entries. In `.planning/PROJECT.md`'s `## Key Decisions` table, append one row in
    the existing Decision/Rationale/Outcome format: the decision is that PUB-03 is satisfied by the
    documented manual `capability install` step rather than an automatic bridge; the rationale names
    the three converging reasons (REQUIREMENTS.md already defers postinstall-hook research to Future
    Requirements; an automatic consent grant defeats CB-3; PUB-04's allowlist omits the bundle so
    automation would break at first release); the outcome names the verified command and the fact
    that it was executed successfully from a clean scratch project on this date. In
    `.planning/STATE.md`'s `### Blockers/Concerns`, mark the existing `[Phase 6, open decision]`
    entry resolved with a pointer to that row, and add a new entry addressed to Phase 7/8: PUB-04's
    ship allowlist omits `.gsd/capabilities/beads/`, so the README's bridge step must direct users to
    clone the repository rather than rely on the release archive — unless Phase 8 amends the
    allowlist. Do not decide the allowlist question here; Phase 6 cannot fix Phase 8's requirement.

    Touch nothing under `.gsd/capabilities/beads/`. Reading it is required; any write invalidates the
    project-scope consent hash and silently deactivates the capability in this repository.
  </action>

  <verify>
    <automated>
G="$HOME/.claude/gsd-core/bin/gsd-tools.cjs"
R=$(git rev-parse --show-toplevel) || exit 1   # capture repo root while still inside the repo
T=$(mktemp -d); cd $T
node "$G" capability install "$R/.gsd/capabilities/beads" --scope project --yes
node "$G" query capability state --raw > $T/state.json 2>/dev/null || node "$G" capability state --raw > $T/state.json
jq -e '[.capabilities[] | select(.id=="beads") | select(.installed==true and .active==true)] | length == 1' $T/state.json || exit 1
cd "$R"; rm -rf $T
# source bundle must be untouched (consent-hash invariant)
test -z "$(git status --porcelain .gsd/capabilities/beads/)" || exit 1
# no automation was built
test -z "$(git status --porcelain --untracked-files=all | grep -v '^.M\? *\.planning/')" || echo "REVIEW: unexpected non-.planning change"
# both records written
grep -q 'capability install' .planning/PROJECT.md || exit 1
grep -q 'PUB-04' .planning/STATE.md || exit 1
echo PASS
    </automated>
    <human-check>
      Read the appended PROJECT.md row and STATE.md entries and confirm the recorded command is the
      one actually run, character for character, and that no aspirational step was added.
    </human-check>
  </verify>

  <acceptance_criteria>
    - `capability install` run from a freshly created `/tmp` directory exits 0.
    - The spec argument was derived at runtime from `git rev-parse --show-toplevel`, captured before the shell left the repository; neither the task's script nor its action text carries a checkout-specific path constant, so the same steps run unchanged from another clone, a worktree, or a CI container.
    - The SUMMARY's transcript of the command marks the repository-root portion as the reader's own clone root, so Phase 8's README (PUB-07) transcribes a path the reader can substitute rather than this machine's.
    - `jq -e '[.capabilities[] | select(.id=="beads") | select(.installed==true and .active==true)] | length == 1'` over that project's `capability state --raw` output exits 0.
    - The full command line and full `capability state --raw` output are pasted verbatim into 06-01-SUMMARY.md.
    - `git status --porcelain .gsd/capabilities/beads/` prints nothing.
    - `git status --porcelain --untracked-files=all` shows changes only under `.planning/` — no new script, hook, or manifest file was created anywhere in the shipped tree.
    - `.planning/PROJECT.md`'s `## Key Decisions` table has one new row naming the manual bridge, its three reasons, and the verified command.
    - `.planning/STATE.md`'s `### Blockers/Concerns` marks the `[Phase 6, open decision]` entry resolved and contains a new entry naming PUB-04's allowlist omission and the clone-versus-archive consequence.
    - The scratch directory no longer exists.
  </acceptance_criteria>

  <done>
    ROADMAP criterion 1 demonstrated by the manual path: the bridge command is written down, executed
    successfully from a project with no prior gsd-beads state, and its transcript is available for
    Phase 8's README. PUB-03 satisfied; STATE.md's Phase-6 open decision closed and the Phase-8
    allowlist gap handed forward.
  </done>
</task>

</tasks>

<threat_model>
ASVS level 1. Blocking threshold: `high`. Scope: a hook file that ships to third-party machines,
and a capability-consent gate.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gsd-beads plugin -> installer's machine | `hooks/hooks.json` is executed by Claude Code on every session start on someone else's machine. Everything in it crosses this boundary. |
| Claude Code plugin runtime -> gsd-core capability loader | No code path connects them. The gap is bridged only by a human running a command; automating that crossing is what the high-severity threat below describes. |
| `bd`'s local database -> session context | `bd prime` output is injected as `additionalContext` into every session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | an automated postinstall/SessionStart bridge running `capability install --yes` | high | mitigate | Do not build it (Task 3). gsd-core's CB-3 consent gate stays human-run. Enforced by Task 3's criterion that `git status --porcelain --untracked-files=all` shows changes only under `.planning/` — no new invoking file may exist. |
| T-06-02 | Tampering | `hooks/hooks.json`'s command string — arbitrary shell executed on every session start on every installer's machine | high | mitigate | The shipped command is asserted byte-equal (canonical-JSON diff, Task 1) to the string already proven across Phases 1-5: a bare invocation of a binary the user installed themselves, with no shell chaining, no interpolation of untrusted data, no network fetch, no `${...}` expansion. Task 1's criteria pin both the command string and the hook-entry count at 1, so no second command can be smuggled in. |
| T-06-03 | Denial of Service | a malformed or oversized `hooks/hooks.json` shipped to installers | low | mitigate | `claude plugin validate . --strict` double-run (Task 1) with `marketplace.json` moved aside — verified on 2026-08-16 that the single-run mode reads the marketplace manifest only and never sees `hooks/hooks.json`. Plus a `jq` parse on every criterion touching the file. Degraded-UX only: Claude Code's SessionStart contract already fails open (Task 2 proves it). |
| T-06-04 | Information Disclosure | `bd prime` output injected into session context | low | accept | The data is the user's own local issue database, injected into the user's own session. This phase adds no transport, no persistence, and no new reader. Unchanged from v1.0, where the same hook already ran from `.claude/settings.json`. |
| T-06-05 | Spoofing | marketplace source resolution — `marketplace.json` names `"source": "./"` | low | accept | Local relative path only; no remote source is fetched this phase. Re-evaluate in Phase 8 when the source is re-pointed at a release archive URL (PUB-04). |
| T-06-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package manager runs in this phase in any ecosystem (06-RESEARCH.md, Package Legitimacy Audit: "Not applicable"). No `[ASSUMED]`/`[SUS]`/`[SLOP]` package exists to gate. |

No threat is dispositioned `accept` at `high` severity, so nothing here trips the `block_on: high`
gate.
</threat_model>

<verification>
Run after all three tasks, from the repository root:

1. `test ! -e .claude/settings.json && test -f hooks/hooks.json` — the swap happened.
2. `diff <(git show 2b09c1b7fed7c4a89c1bbb8f38d889d792375fff:.claude/settings.json | jq -S .) <(jq -S . hooks/hooks.json)` — the shipped hook is the proven hook.
3. `claude plugin validate . --strict` twice (marketplace.json present, then moved aside and restored) — no hook-related output in either.
4. Headless probe with the plugin installed: exactly one bd-prime success line, and a `Read hooks.json for plugin beads` line.
5. Headless probe under a `bd`-free PATH shim: exit 0, zero bd-prime success lines, one recorded notice.
6. `capability state --raw` from a fresh `/tmp` project after the documented bridge command: `beads` installed and active.
7. `git status --porcelain .gsd/capabilities/beads/` — empty.
8. One interactive TTY session in this repo primes exactly once.
</verification>

<success_criteria>
- ROADMAP Phase 6 criterion 1 (PUB-03): the bridge command executed successfully from a clean project and its transcript recorded verbatim.
- ROADMAP Phase 6 criterion 2 (PUB-06): the hook ships in `hooks/hooks.json` and fires with no user-side `.claude/settings.json`.
- ROADMAP Phase 6 criterion 3 (PUB-06): exactly one fire per session inside this repo, proven by a counted debug log and one TTY session.
- ROADMAP Phase 6 criterion 4 (PUB-06): `bd`-free session exits 0 with one recorded notice and no injected context.
- STATE.md's `[Phase 6, open decision]` entry is closed; a new Phase-7/8 entry carries the PUB-04 allowlist gap forward.
- Every prohibition in `must_haves.prohibitions` holds at phase end.
</success_criteria>

<output>
Create `.planning/phases/06-runtime-integration/06-01-SUMMARY.md` when done.

It MUST carry three verbatim transcripts, because Phase 8's PUB-07 README transcribes from them
rather than re-deriving them:
1. The `capability install` command line as run, and its `capability state --raw` output. Mark the
   repository-root portion of the spec argument as the reader's own clone root — it is captured from
   `git rev-parse --show-toplevel` at run time, and Phase 8's README must not publish this machine's
   checkout path as if it were a constant.
2. The `bd`-absent session's failure notice, exactly as the debug log wrote it.
3. Probe A's bd-prime fire count with the plugin uninstalled, and the resulting one-sentence answer
   to RESEARCH.md Assumption A1 (whether a checked-out repo auto-loads plugin hooks), including the
   disclosed consequence for this repo's own dev sessions.
</output>
