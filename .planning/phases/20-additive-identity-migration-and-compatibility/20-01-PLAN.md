---
beads_epic: gsd-beads-5jk
phase: 20-additive-identity-migration-and-compatibility
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py
  - plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py
autonomous: true
requirements: [ID-01, ID-02]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every successfully resolved exact auto or tracer task carries one canonical native tracker identity derived from its still-present legacy Beads identity."
    - "A second synchronization of the same canonical plan performs no plan write and no Beads create/update, and leaves plan bytes identical."
    - "A wrong, duplicate, or authority-free native tracker identity halts before any plan write or Beads create."
    - "Stale or malformed legacy identity and unavailable/failing Beads never produce a native projection."
    - "Checkpoint, missing-type, partial-type, and unknown-type task blocks remain byte-identical beside migrated eligible tasks."
  artifacts:
    - path: plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py
      provides: "One additive native-identity projection in the existing parse_plan -> create_issues -> rewrite_plan flow."
    - path: plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py
      provides: "Public-boundary, one-factor migration, conflict, idempotence, native-parser, and preservation proofs."
  key_links:
    - from: plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py
      to: "live bd show/create result"
      via: "existing resolve_issue typed-argv seam"
      pattern: "resolve_issue"
    - from: "safe live legacy Beads identity"
      to: "tracker-id=beads:<id>"
      via: "opening-tag-local insertion immediately after the type attribute"
      pattern: "TASK_TYPE_RE"
    - from: "native identity updates"
      to: plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py
      via: "the existing rewrite_plan descending-offset writer and write predicate"
      pattern: "rewrite_plan"
    - from: "migrated plan syntax"
      to: /home/dd/.codex/gsd-core/bin/lib/plan-document.cjs
      via: "parsePlanDocument reads tracker-id from eligible opening tags and returns null for checkpoints"
      pattern: "parsePlanDocument"
---

<!-- rumdl-disable MD013 MD022 MD032 MD033 MD041 MD058 -->

**Mechanism:** Extend the existing `parse_plan -> resolve_issue -> rewrite_plan` seam with one opening-tag-local `tracker-id="beads:<id>"` splice after successful authoritative Beads resolution.

**Forbidden:** XML/HTML reserialization, a second migration command/pass, title matching, legacy-identity replacement, conflict repair/overwrite, cache, retry, registry, tracker abstraction, new dependency, gsd-core edit, installed cutover, Patch 2 retirement, and scope from `gsd-beads-2e2`.

**Audit:** Public `create_issues` calls; exact `bd` argv history; `Path.write_text` spy for negative/idempotent arms; raw plan/task-block bytes; installed gsd-core `parsePlanDocument` recognition; exact checkpoint/unknown coexistence.

**Beads binding invariant:** This pre-sync plan intentionally contains no Beads identity child or `beads_epic` field. The `plan:post` Beads sync is the sole binder and must create one ticket and insert exactly one resolved identity child per task; a literal empty/TBD placeholder would either be treated as authority or cause a duplicate insertion.

<objective>
Make native task identity an additive, deterministic projection of live legacy Beads identity for exact `auto` and `tracer` tasks, without changing legacy consumers or any human/unknown task bytes (D-01 through D-09; ID-01 and ID-02).

Purpose: gsd-core can recognize eligible tasks through its native resolver seam while Beads remains the only identity authority and every ambiguous identity fails closed.

Output: Surgical changes to the existing synchronizer and its existing unittest module only; no new module, fixture framework, dependency, migration surface, or runtime cutover artifact.

Ponytail whole-plan outcome: rung 2 (reuse what already exists). `parse_plan`, `resolve_issue`, `rewrite_plan`, `create_issues`, `TestIdentityBinding`, `_make_bd_side_effect`, and `_write_plan_workspace` already cover every required layer. The plan exists because ID-01/ID-02 cannot be satisfied by deletion or a no-op; it rejects serializers, a separate migrator, and a generic tracker registry because they add traversal/write/failure surfaces without improving the locked contract. If the mandatory live caller trace finds an incompatible exact-dict consumer, the bounded fallback is one local `TASK_RE` opening-tag offset pass inside the same `create_issues`/`rewrite_plan` seam—not a second migration flow. Confidence: 98/100.
</objective>

<execution_context>
@/home/dd/.codex/gsd-core/workflows/execute-plan.md
@/home/dd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-additive-identity-migration-and-compatibility/20-CONTEXT.md
@.planning/phases/20-additive-identity-migration-and-compatibility/20-RESEARCH.md
@.planning/phases/20-additive-identity-migration-and-compatibility/20-PATTERNS.md
@.planning/phases/20-additive-identity-migration-and-compatibility/20-VALIDATION.md
@.planning/phases/19-native-resolver-contract-and-failure-boundary/19-01-SUMMARY.md
@.planning/phases/19-native-resolver-contract-and-failure-boundary/19-02-SUMMARY.md
@plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py
@plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py
@/home/dd/.codex/gsd-core/bin/lib/plan-document.cjs
@/home/dd/.codex/gsd-core/bin/lib/task-content-resolution.cjs
</context>

## Dependency and Wave Model

| Task | Wave | Needs | Creates | Ordering reason |
|---|---:|---|---|---|
| 20-01-01 | 1 | Phase 19 epic closed; authoritative capability suite green | Safe existing-bound migration, conflict preflight, parser recognition | Tracer proves the hardest trust-boundary path first. |
| 20-01-02 | 1 (sequential within plan) | 20-01-01's parse/write seam | Newly-created same-write projection plus complete negative/idempotence/preservation matrix | Same two files; sequential ownership prevents overlapping edits. |

## Alternatives Considered

| Mechanism | Primary dated source | Performance | Simplicity / LOC | Ecosystem support | Maintenance | Outcome |
|---|---|---|---|---|---|---|
| **Existing regex match spans + string slicing inside the current writer** | [Python 3.14.7 `re.Match.start/end/span`](https://docs.python.org/3/library/re.html#match-objects), accessed 2026-08-31; current tracked `sync.py` `parse_plan`/`rewrite_plan`, inspected 2026-08-31 | One existing parse plus descending local splices; no second traversal or reconstruction | Extends two existing records/collections and one write predicate | Python stdlib already used; gsd-core already consumes the attribute | One authoritative mutation seam | **Chosen.** It is the only option that directly preserves every unrelated byte while reusing the live Beads resolution boundary. |
| **`xml.etree.ElementTree` parse and serialize** | [Python 3.14.7 ElementTree API](https://docs.python.org/3/library/xml.etree.elementtree.html), accessed 2026-08-31 | Adds tree construction and whole-document serialization | More code to restore non-XML PLAN frontmatter and authored lexical form | Mature stdlib XML API | Owns serializer/version lexical drift | Rejected: semantic XML correctness cannot prove D-05 raw-byte/order/body preservation. |
| **`xml.dom.minidom` parse and `writexml`** | [Python 3.14.7 `xml.dom.minidom`](https://docs.python.org/3/library/xml.dom.minidom.html), accessed 2026-08-31 | Adds DOM construction and full output generation | Requires document wrapping plus whitespace/output controls | Mature stdlib DOM API | Creates a second representation and formatting surface | Rejected: `writexml` is a renderer, while the contract requires one local insertion and no unrelated rendering. |

**Decided by:** performance first, then simplicity/lines of code, ecosystem support, and maintenance overhead. The local existing-writer splice wins at the first criterion and remains best at every later criterion.

Numerical-mechanism audit: not applicable. This phase performs no floating-point computation, accumulation, tolerance comparison, statistical estimator, or numerical algorithm; adding Kahan or IEEE 754 citations would be irrelevant evidence rather than a mechanism comparison.

## Scientific Resolution of Unresolved Questions

### Q1 — Can the shared `parse_plan` task record safely gain opening-tag metadata?

1. **Argument map:** Adding source offsets/counts is safe if all callers consume named fields and no caller asserts exact task-dict equality.
2. **Evidentiary inventory:** Serena found ten production call sites plus `create_issues`; each accesses selected keys such as `beads_id`, `files`, and order. Direct `parse_plan` source shows an ordinary dict with no closed schema. Evidence is direct current source, inspected 2026-08-31.
3. **Logic audit:** Additive dict keys do not alter existing key values or tuple return shape; therefore current named-key consumers remain behaviorally unchanged.
4. **Bias/confound audit:** Index/caller omission is the main risk. Task 1 must run the complete capability suite, not infer safety from the indexed call list alone.
5. **Alternatives:** (a) add the minimum fields to the existing dict, accepted only while the live caller trace confirms named-key consumption; (b) if that evidence changes, make one bounded local `TASK_RE.finditer(text)` opening-tag offset pass in `create_issues` and feed its results to the same `rewrite_plan`, accepted as the compatibility fallback; (c) create a second parsed task type or migration command, rejected as a new abstraction/pipeline.
6. **Integrated appraisal:** **Strong; Accept with a mandatory pre-edit caller trace and full-suite condition; 97/100.** No exact-dict consumer was observed on 2026-08-31, but the executor must re-establish that fact from current CodeGraph/Serena evidence before selecting the dict-extension mechanism. Any incompatible caller selects the bounded same-seam fallback rather than a workaround or scope change.

### Q2 — Is Phase 20 executable merely because the Phase 19 child prerequisites are closed?

1. **Argument map:** Phase 20 depends on completed Phase 19, not only on two closed child tasks.
2. **Evidentiary inventory:** Live `bd show` on 2026-08-31 reports `gsd-beads-0y4.1` and `gsd-beads-c5l` closed but parent epic `gsd-beads-0y4` open; Phase 19 summaries record earlier green suites but are historical evidence.
3. **Logic audit:** Child closure does not entail epic completion, and an earlier suite result does not entail a future clean baseline.
4. **Bias/confound audit:** Completion-by-proxy and stale-green bias would admit execution while the authoritative epic remains open. `STATE.md` is also historical, not a substitute for live Beads.
5. **Alternatives:** (a) accept child closure alone, rejected; (b) accept a green suite alone, rejected because dependency state remains open; (c) require live epic closure and rerun the exact suite under the proven outside-ancestry scratch root, accepted.
6. **Integrated appraisal:** **Strong; Modify execution with a blocking precondition; 100/100 for current open state.** Planning may complete, but Task 1 must halt before mutation until both live gates pass.

### Q3 — How should native parser compatibility be proved without stealing Phase 21 cutover scope?

1. **Argument map:** D-09 needs syntax recognition; Phase 21 owns installed resolver execution and byte-parity cutover.
2. **Evidentiary inventory:** Current gsd-core `plan-document.cjs` exports `parsePlanDocument`, reads `tracker-id` from eligible opening tags, and returns `trackerId: null` for checkpoints; the active `gsd_run` launcher resolves its own real path and delegates to the sibling gsd-core `bin` tree; `task-command-router.cjs` performs the later exact-id route.
3. **Logic audit:** Calling only `parsePlanDocument` proves Phase 20 syntax consumption without invoking a capability resolver or asserting installed-source parity.
4. **Bias/confound audit:** Reimplementing the attribute regex in Python would test a facsimile, while invoking `task resolve-content` would conflate identity with Phase 21 installation and live resolver behavior.
5. **Alternatives:** (a) Python-only regex assertion, rejected as circular; (b) full public resolver invocation, rejected as Phase 21 scope; (c) hard-coded Codex/GSD home, rejected as host-specific; (d) resolve `command -v gsd_run`, canonicalize it, derive its sibling parser module, and invoke the export through bounded typed Node argv, accepted.
6. **Integrated appraisal:** **Strong; Accept active-launcher parser-only proof; 98/100.** The test must hard-fail—never skip or search an alternate home—when `gsd_run` is absent, path canonicalization fails, the derived parser is missing/unreadable, `require()` fails, or `parsePlanDocument` is not callable.

<tasks>

<task type="tracer" tdd="true">
  <name>Task [existing-bound identity splice through current sync seam] ! [no serializer, second migrator, conflict repair, or gsd-core edit]</name>
  <beads-id>gsd-beads-5jk.1</beads-id>
  <reversibility rating="costly">D-02/D-04 persist native identities into already-synced plans; undo requires coordinated removal while conflicting bindings must never be overwritten.</reversibility>
  <files>plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py, plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py</files>
  <ponytail>
    <first_rung>2 — reuse existing codebase mechanisms</first_rung>
    <why_necessary>ID-01 requires persisted native identity, and D-04 makes conflict preflight inseparable from safely enabling the first path.</why_necessary>
    <reuses>Current CodeGraph/Serena caller evidence, `parse_plan`, `TASK_RE`, live `resolve_issue`, `rewrite_plan`, `create_issues`, `TestIdentityBinding`, existing workspace helpers, active `gsd_run`, typed subprocess argv, and gsd-core's exported parser.</reuses>
    <rejects>Deletion/no-op (does not satisfy ID-01), stdlib XML/DOM serialization (cannot prove lexical identity), installed dependency (none needed), separate migrator (duplicates the write boundary), and generic tracker model (YAGNI).</rejects>
    <minimum_runnable_check>`TMPDIR=/dev/shm PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_sync.TestIdentityBinding -v`.</minimum_runnable_check>
  </ponytail>
  <critical_thinking>
    <argument>One local insertion after successful legacy resolution is sufficient for the existing-bound path.</argument>
    <evidence>Direct current source shows one resolver and one descending-offset writer; current gsd-core reads the exact opening-tag attribute.</evidence>
    <logic>Preflight before epic resolution prevents partial creation; post-resolution queuing prevents projection from stale authority.</logic>
    <bias_confound>Semantic parser success alone could hide whitespace/order drift, while a create-path fixture could confound migration with new issue allocation; raw bytes and pre-bound controls isolate both.</bias_confound>
    <alternatives>Whole-document serialization and a post-sync migration command are rejected because they add reconstruction or a second failure/write boundary; if additive task keys are incompatible, the single local `TASK_RE` offset pass is the bounded same-seam fallback.</alternatives>
    <appraisal>Strong; accept with byte, spy, and full-suite gates; confidence 97/100.</appraisal>
  </critical_thinking>
  <precondition>Before any mutation, `bd show gsd-beads-0y4 --json | jq -er '.[0].status == "closed"'` must exit 0 from `/home/dd/projects/gsd-beads`, then `TMPDIR=/dev/shm PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -t tests` must exit 0 from `plugins/beads-lifecycle/.gsd/capabilities/beads`. Closed `gsd-beads-0y4.1` and `gsd-beads-c5l` alone do not satisfy this gate.</precondition>
  <action>Implement D-01, D-02, D-04, D-05, and the existing-bound portion of D-06 test-first. BEFORE RED OR ANY FILE EDIT: use CodeGraph/Serena reference tracing on `parse_plan`, enumerate every current production caller and direct test caller, inspect each caller body, and record in the task/SUMMARY whether it consumes named keys only or depends on exact dict keys/iteration/serialization/equality. If every caller is additive-key compatible, select the minimum task-dict metadata extension. If any incompatible caller exists, do not add dict keys: make one bounded local `TASK_RE.finditer(text)` pass over the already-read original text inside `create_issues` to calculate opening-tag type/native cardinality/value/insertion offsets, then feed those offsets into the existing `rewrite_plan` descending-offset writer. This fallback stays inside the same command and write seam; it is not permission for another migrator, cache, command, or file. RED: extend `TestIdentityBinding` at the public `sync.create_issues(plan_path)` boundary with one-factor existing-bound `auto`, existing-bound `tracer`, already-canonical, wrong-value, duplicate-attribute, and native-without-legacy controls. Reuse `_write_plan_workspace` and `_make_bd_side_effect`; retain raw pre/post bytes; inspect exact `mock_run.call_args_list`; and spy on `Path.write_text` for conflict/no-op arms. Make native-parser proof executable and portable: from the test, run bounded `bash -lc 'command -v gsd_run'`; require one nonempty absolute result; canonicalize it with bounded typed argv `readlink -f -- &lt;resolved-path&gt;`; derive `&lt;canonical-launcher-parent&gt;/lib/plan-document.cjs`; require that file to exist and be readable; then invoke bounded typed Node argv with the parser path and migrated plan path as separate arguments. The Node program must require the derived module, require callable `parsePlanDocument`, parse the migrated bytes, assert the exact eligible tracker value and checkpoint null value, and exit nonzero on any failure. Absence, unreadability, require/export failure, timeout, or wrong values are hard failures—never skip, hard-code an alternate configuration home, or invoke full Phase 21 resolver cutover. Require RED before production edits. GREEN: under the selected compatible mechanism, expose only the minimum opening-tag metadata: exact native-attribute cardinality/value and the absolute insertion point immediately after the matched `type` attribute. Preflight every eligible task immediately after `parse_plan` and before `resolve_epic`, `resolve_issue`, or any write/create: reject duplicate, noncanonical, or authority-free native identity with one precise nonzero diagnostic. After a successful live resolution of a pre-bound exact `auto`/`tracer`, queue one native insertion; add that collection to the existing `rewrite_plan` descending-position splice and existing write predicate. Preserve the legacy identity child and all existing callers/keys; do not modify gsd-core, call a title matcher, reserialize a task, or introduce a second writer. REFACTOR only if the implementation creates three genuinely identical checks; otherwise keep the local branches in the existing deep functions. Commit RED and GREEN separately using conventional commits; omit attribution trailers.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; cd /home/dd/projects/gsd-beads/plugins/beads-lifecycle/.gsd/capabilities/beads; TMPDIR=/dev/shm PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_sync.TestIdentityBinding -v'</automated>
    <fails_when>The pre-edit CodeGraph/Serena trace omits a current `parse_plan` caller or an exact-dict consumer is ignored instead of selecting the bounded local fallback; the focused class exits nonzero; an eligible bound task lacks exactly one canonical native attribute or loses its legacy identity; insertion moves unrelated bytes; any existing-bound control calls `bd create`; active `gsd_run` cannot be resolved/canonicalized, the derived parser is absent/unreadable/non-callable, the bounded Node invocation times out/fails, gsd-core reports a wrong value, a parser arm skips/falls back to a hard-coded home, or any conflict/duplicate/authority-free arm reaches a create or write.</fails_when>
  </verify>
  <done>D-01/D-02/D-04/D-05 are proven through one production-quality existing-bound path: safe eligible tasks migrate locally, canonical input is unchanged, ambiguous identity fails before mutation, and gsd-core recognizes the emitted syntax.</done>
  <!-- beads: content synced to bd -- see `bd show gsd-beads-5jk.1` -->
</task>

<task type="auto" tdd="true">
  <name>Task [newly-resolved and preservation matrix through same seam] ! [no cache, retry, tracker abstraction, skip, or relaxed byte assertion]</name>
  <beads-id>gsd-beads-5jk.2</beads-id>
  <reversibility rating="costly">D-02 extends persisted projection to newly created eligible tasks, but remains within the same deterministic writer and can be removed only by coordinated migration.</reversibility>
  <files>plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py, plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py</files>
  <ponytail>
    <first_rung>2 — extend the already-proven seam and tests</first_rung>
    <why_necessary>ID-01 explicitly includes newly resolved tasks and repeat idempotence; ID-02 and D-09 require coexistence and discriminating negative proof, so Task 1 alone is insufficient.</why_necessary>
    <reuses>Task 1's native update collection, existing legacy `task_updates`, the sole `rewrite_plan`, current B6 branch, `TestIdentityBinding`/`TestIdempotency` byte controls, and checkpoint block helpers.</reuses>
    <rejects>A second migration flow, cache/retry, title repair, generic registry, new fixture framework, and standalone documentation task; each adds surface without satisfying a missing behavior.</rejects>
    <minimum_runnable_check>`TMPDIR=/dev/shm PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_sync.TestIdentityBinding -v` plus the authoritative discovery suite.</minimum_runnable_check>
  </ponytail>
  <critical_thinking>
    <argument>The same writer can cover new allocation and all preservation boundaries without a second migration system.</argument>
    <evidence>Current `create_issues` already receives both `created` and `issue_id`, owns the one write predicate, and has existing stale/idempotence/checkpoint analog tests.</evidence>
    <logic>Adding the returned id to the existing native queue is sufficient; exact-type filtering and negative controls demonstrate exclusions rather than assuming them.</logic>
    <bias_confound>A fresh second-pass fixture, reset critical history, standalone checkpoint fixture, or semantic comparison could falsely pass idempotence/preservation; same-path bytes, write spies, retained second-pass history, and neighboring blocks control these confounds.</bias_confound>
    <alternatives>E2E-only real-bd testing is rejected because it cannot isolate one factor; broad table abstraction is rejected because nearby explicit unittest arms are shorter and clearer at this scale.</alternatives>
    <appraisal>Strong; accept only if every one-factor arm and the full suite pass; confidence 96/100.</appraisal>
  </critical_thinking>
  <action>Complete D-02, D-03, D-06, D-07, D-08, and D-09 through the same seam. RED: add a newly-unbound eligible control to `TestIdentityBinding` that fails until the issue id returned by the existing single `bd create` is queued for both identity insertions in the same writer call. Add one-factor arms for a second invocation on the same path, stale legacy lookup, malformed legacy shape, unavailable/failing `bd`, and checkpoint/missing/partial/unknown coexistence. Preserve second-pass call history needed to distinguish first from second invocation; use `read_bytes()` equality and `Path.write_text` spies rather than semantic parsing for no-write claims; compare excluded task blocks exactly before/after. GREEN: extend Task 1's native-update queue to the created-issue outcome without adding another traversal or writer. Keep existing `task_updates` ownership of the legacy child, merge native and legacy source-position insertions in descending order, include native updates in the sole write predicate, and leave `newly_created_ids`/stripping semantics tied only to actual issue creation. A divergent/stale/malformed result must never queue a native update; only exact `auto`/`tracer` types enter native logic. Do not absorb `gsd-beads-2e2`, broaden B6, change checkpoint creation/content behavior, reset a fixture between idempotence passes, or weaken exact-byte assertions. REFACTOR only by deleting duplication introduced in this task; no new helper/module without three identical business-logic occurrences. Commit RED and GREEN separately using conventional commits; omit attribution trailers.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; cd /home/dd/projects/gsd-beads/plugins/beads-lifecycle/.gsd/capabilities/beads; TMPDIR=/dev/shm PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_sync.TestIdentityBinding -v; TMPDIR=/dev/shm PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -t tests'</automated>
    <fails_when>The focused or full suite exits nonzero; a newly created eligible task does not receive both identities in one write; second sync changes bytes or calls create/update/write; stale/malformed/unavailable/failing controls gain native identity; any checkpoint/missing/partial/unknown block changes; any comparison changes more than its declared factor; or any test is skipped/xfail/relaxed to semantic equality.</fails_when>
  </verify>
  <done>ID-01 and ID-02 are complete: both existing and newly resolved eligible tasks migrate through one writer, repeats are byte/no-create/no-write idempotent, every unsafe authority case stays unmigrated, and all human/unknown task blocks remain exact.</done>
  <!-- beads: content synced to bd -- see `bd show gsd-beads-5jk.2` -->
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| PLAN opening tag -> synchronizer parser | Plan-authored type and identity text is untrusted input controlling whether migration is allowed. |
| synchronizer -> Beads CLI | A legacy id crosses into fixed typed argv and live Beads establishes whether it is authoritative. |
| synchronizer -> PLAN write | Native identity changes a durable document consumed by legacy and native runtimes. |
| migrated PLAN -> gsd-core parser | gsd-core interprets the inserted attribute as external task-content routing identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-01 | Tampering | eligible task opening tag | high | mitigate | Preflight exact type, legacy authority, native cardinality, and canonical equality before epic/task creation or any write; assert conflict no-create/no-write with spies. |
| T-20-02 | Tampering | legacy Beads id -> native projection | high | mitigate | Retain `SAFE_BD_ID_RE`/typed argv boundary and queue projection only after live `resolve_issue` success; stale/malformed controls remain unmigrated. |
| T-20-03 | Elevation of Privilege | checkpoint and unknown task types | high | mitigate | Exact `auto`/`tracer` allow-list only; compare decision/human-verify/missing/partial/unknown blocks byte-for-byte beside a migrated sibling. |
| T-20-04 | Repudiation | repeat synchronization | medium | mitigate | Same-path second-pass test proves raw-byte equality and zero create/update/write, preserving an auditable no-op. |
| T-20-05 | Denial of Service | unavailable/failing Beads | medium | accept | Preserve the already-approved B6 visible fail-open/no-write degradation; this phase introduces no retry or alternate authority. |
| T-20-06 | Information Disclosure | diagnostic text | low | mitigate | Diagnostics name the conflict category/task but never serialize task body or invoke shell interpolation. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Eligible tasks gain native identity without changing legacy consumers | 20-01-01, 20-01-02 | COVERED | Existing-bound tracer plus new-task expansion use one seam. |
| REQ | ID-01 | Eligible auto/tracer keeps legacy identity, gains canonical native identity, repeats byte-identically with no duplicate issue | 20-01-01, 20-01-02 | COVERED | Direct public-boundary and second-pass proofs. |
| REQ | ID-02 | Checkpoints never migrate and preserve human behavior | 20-01-02 | COVERED | Exact coexistence block comparisons. |
| CONTEXT | D-01 | Legacy Beads identity remains authority; native identity is deterministic projection | 20-01-01 | COVERED | Post-live-resolution projection only. |
| CONTEXT | D-02 | Migrate existing and newly resolved exact eligible tasks | 20-01-01, 20-01-02 | COVERED | Tracer then expansion. |
| CONTEXT | D-03 | Stale/unavailable/failing authority produces no native migration | 20-01-02 | COVERED | One-factor negative arms and B6 preservation. |
| CONTEXT | D-04 | Wrong/duplicate/authority-free native identity halts without write/create | 20-01-01 | COVERED | All-task preflight before epic resolution. |
| CONTEXT | D-05 | Local opening-tag splice immediately after type; no reserialization | 20-01-01 | COVERED | Source offset plus raw-byte proof. |
| CONTEXT | D-06 | Canonical repeat is byte/no-create/no-write identical | 20-01-01, 20-01-02 | COVERED | Canonical no-op and same-path second pass. |
| CONTEXT | D-07 | All checkpoint variants remain exact | 20-01-02 | COVERED | Decision and human-verify coexistence controls. |
| CONTEXT | D-08 | Missing/partial/unknown types remain exact | 20-01-02 | COVERED | Exact allow-list and block equality. |
| CONTEXT | D-09 | One-factor public/native-parser/no-write verification with no skips or weakened assertions | 20-01-01, 20-01-02 | COVERED | Explicit controlled matrix and full suite. |
| RESEARCH | — | Reuse `parse_plan -> create_issues -> rewrite_plan`; no second pipeline/dependency | 20-01-01, 20-01-02 | COVERED | Plan header and actions forbid expansion. |
| RESEARCH | — | Phase 19 epic completion plus exact outside-ancestry suite before mutation | 20-01-01 | COVERED | Direct blocking precondition. |
| RESEARCH | — | gsd-core recognizes the inserted attribute without Phase 21 cutover | 20-01-01 | COVERED | Parser-only Node proof. |
| BEADS-RECALL | gsd-beads-2e2 | Overlay PLUGIN_ROOT bug | NONE | EXCLUDED | Explicitly out of Phase 20 scope; not absorbed. |

<verification>
1. Before implementation, the Task 1 precondition proves live Phase 19 epic closure and a clean authoritative baseline under `TMPDIR=/dev/shm` outside repository ancestry.
2. After each task, run `TMPDIR=/dev/shm PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_sync.TestIdentityBinding -v` from the capability root.
3. At wave/phase close, run `TMPDIR=/dev/shm PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -t tests` from the capability root and require zero failures/errors/skips introduced by Phase 20.
4. Run `git diff --check -- plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py` and require no whitespace errors.
5. Inspect `git diff --name-only` and require Phase 20 implementation to modify only the two declared files; plan:post's plan binding is lifecycle metadata, not implementation scope.
6. Re-run the focused native-parser assertion by resolving `command -v gsd_run`, canonicalizing it with `readlink -f`, deriving the sibling `lib/plan-document.cjs`, and invoking callable exported `parsePlanDocument` through bounded typed Node argv. Require exact `beads:<id>` for eligible tasks and null for checkpoints; absence/unreadability/non-callability is a hard failure, and installed resolver cutover remains out of scope.
7. Require plan-post Beads sync to add exactly one resolved identity child to each plan task and one phase epic binding; the pre-sync file must not carry placeholders.
</verification>

<success_criteria>
- Exact eligible existing and newly resolved tasks contain one canonical native attribute and retain their one authoritative legacy identity child (ID-01; D-01/D-02/D-05).
- Canonical second synchronization is byte-identical with zero create/update/write (ID-01; D-06).
- Conflicting, duplicate, or authority-free native identity returns nonzero before any Beads create or plan write (D-04).
- Stale/malformed/unavailable/failing authority produces no native projection and preserves the established degradation boundary (D-03).
- Checkpoint, missing, partial, and unknown task blocks remain exact and never enter native routing (ID-02; D-07/D-08).
- gsd-core's native parser recognizes the exact projection, every one-factor arm passes, and no skip/xfail/relaxed byte assertion exists (D-09).
- No dependency, new file, second migration surface, gsd-core source change, Phase 21 cutover work, or `gsd-beads-2e2` change exists.
</success_criteria>

<output>
Create `.planning/phases/20-additive-identity-migration-and-compatibility/20-01-SUMMARY.md` when done.
</output>
