---
phase: 11-sota-numerics-capability-plugin-sota-efficiency-numerical-st
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - sota-numerics/.gsd/capabilities/sota-numerics/fragments/planner-sota.md
  - sota-numerics/.gsd/capabilities/sota-numerics/fragments/executor-numerics.md
  - sota-numerics/.gsd/capabilities/sota-numerics/fragments/verifier-precision.md
  - sota-numerics/.gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md
  - sota-numerics/.gsd/capabilities/sota-numerics/capability.json
  - sota-numerics/hooks/session-start.sh
  - .gsd/capabilities/sota-numerics/fragments/planner-sota.md
  - .gsd/capabilities/sota-numerics/fragments/executor-numerics.md
  - .gsd/capabilities/sota-numerics/fragments/verifier-precision.md
  - .gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md
  - .gsd/capabilities/sota-numerics/capability.json
autonomous: true
requirements: [D-05, D-11, D-12, D-13]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Four stage-tailored fragment files exist, one per lifecycle point in D-12's spread, and no two share the same body text (D-13)."
    - "`gsd_run loop render-hooks plan:pre --raw` lists a sota-numerics contribution targeting the planner whose fragment resolves and materialises (D-12)."
    - "`gsd_run loop render-hooks execute:wave:pre --raw`, `execute:wave:post --raw`, and `ship:pre --raw` each list a sota-numerics contribution (D-12)."
    - "Every contribution is gated on the single key `sota-numerics.enabled` and no contribution declares `configValues` (D-11)."
    - "The planner fragment states the exact Alternatives Considered section format the plan:post gate script accepts, including the `Decided by:` line and the D-03 exemption text."
    - "The planner fragment states that a phase with a SPEC.md still requires PLAN.md's own Alternatives Considered section (D-05)."
    - "The ship:pre fragment states it is advisory with no gate at ship time, and the verifier fragment states its findings are not blockers (D-12)."
    - "`bash sota-numerics/tests/test-session-start.sh` still passes with the stage-tailored role framings substituted in."
  artifacts:
    - sota-numerics/.gsd/capabilities/sota-numerics/fragments/planner-sota.md
    - sota-numerics/.gsd/capabilities/sota-numerics/fragments/executor-numerics.md
    - sota-numerics/.gsd/capabilities/sota-numerics/fragments/verifier-precision.md
    - sota-numerics/.gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md
    - .gsd/capabilities/sota-numerics/fragments/
  key_links:
    - "`capability.json` `contributions[].fragment.path` → each `fragments/*.md` file, resolved relative to the capability bundle root."
    - "`contributions[3].into` → `orchestrator`, the ONLY agent role the loop-host contract declares for the ship step — `orchestrator`, not `reviewer` or `shipper`."
    - "planner fragment's documented section format → the literal patterns `check-alternatives.py` matches, so the fragment and the gate cannot drift apart."
---

<objective>
Expand from Plan 01's proven spine to D-12's full four-point advisory spread: write the four
stage-tailored steering fragments, wire them as `contributions[]` entries, and give
`session-start.sh` its real per-role framings.

Purpose: the gate alone only punishes; the fragments are what actually steer the planner into
producing a compliant, genuinely researched Alternatives Considered section in the first place, and
the executor/verifier/ship fragments carry the numerical-stability and efficiency half of this
capability that the gate does not cover at all.

Output: a fully wired capability whose contributions render at all four lifecycle points.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sota-numerics-capability-plugin-sota-efficiency-numerical-st/11-CONTEXT.md
@.planning/phases/11-sota-numerics-capability-plugin-sota-efficiency-numerical-st/11-PATTERNS.md
@.planning/phases/11-sota-numerics-capability-plugin-sota-efficiency-numerical-st/11-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan creates the four fragment files (in both the plugin bundle and the repo-root dogfood
copy) and the four `contributions[]` entries in `capability.json`. It introduces no new config key
— `sota-numerics.enabled` (declared in Plan 01) governs every contribution, per D-11.

Full phase-wide artifact list, including the capability id, config key, gate id, and
`command-exit-zero` predicate usage: see the "Artifacts this phase produces" section of
`11-01-PLAN.md`.

**New files (this plan):**
- `sota-numerics/.gsd/capabilities/sota-numerics/fragments/planner-sota.md`
- `sota-numerics/.gsd/capabilities/sota-numerics/fragments/executor-numerics.md`
- `sota-numerics/.gsd/capabilities/sota-numerics/fragments/verifier-precision.md`
- `sota-numerics/.gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md`
- the byte-identical dogfood copies of all four under `.gsd/capabilities/sota-numerics/fragments/`

**Modified files (this plan):** `capability.json` (`contributions` array, both copies),
`sota-numerics/hooks/session-start.sh` (per-role `FRAMING` strings).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write the four stage-tailored steering fragments</name>
  <files>sota-numerics/.gsd/capabilities/sota-numerics/fragments/planner-sota.md, sota-numerics/.gsd/capabilities/sota-numerics/fragments/executor-numerics.md, sota-numerics/.gsd/capabilities/sota-numerics/fragments/verifier-precision.md, sota-numerics/.gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md</files>
  <read_first>
    - `.gsd/capabilities/ponytail/fragments/planner-ladder.md`, `executor-ladder.md`, `verifier-ladder.md` — the exact shape to copy: 3-5 lines of plain prose, no headings, no code fences, first line naming the discipline and whether it gates.
    - `.gsd/capabilities/beads/fragments/recall-pointer.md` — the single-audience advisory fragment precedent for the `ship:pre` file, which has no ponytail analog.
    - `~/.claude/CLAUDE.md` sections "Architectural Integrity & Framework Selection" (Alternatives Mandatory, SOTA Verification, Mechanism Justification ranking, Gate Failure) and the opening line ("SOTA, elegant, mathematic correctness and precision, no cancelations, avoid propagation of errors") — the literal source of the rules these fragments encode; read directly rather than relying on CONTEXT.md's paraphrase (CONTEXT.md canonical_refs).
    - `sota-numerics/.gsd/capabilities/sota-numerics/scripts/check-alternatives.py` — the exact literal patterns the gate accepts, so the planner fragment documents a format the gate will actually pass.
  </read_first>
  <action>
Write four short prose fragments, each 4-7 lines, each with distinct body text (D-13 — no shared
generic reminder). Match ponytail's fragment register: plain sentences, no markdown headings, no
fenced code blocks, no bullet lists longer than the format spec the planner fragment needs.

`planner-sota.md` (rendered into the planner at `plan:pre`) — the only fragment whose framing must
be qualified rather than "advisory only", because D-12 pairs it with a real blocking gate later in
the same lifecycle: state that this steering is advisory but that a blocking gate fires at
`plan:post` and will halt planning if the plan does not comply. Then state the requirement: for
each non-trivial mechanism or library choice, name at least 2 current alternatives, each with an
attached source (a URL or a doc reference) carrying a discoverable date, and name which ranked
criterion decided the pick, in the order performance, then simplicity and lines of code, then
ecosystem support, then maintenance overhead. Document the exact accepted section shape, matching
what `check-alternatives.py` parses: a `## Alternatives Considered` heading, one bulleted list item
per alternative whose name is bold-wrapped, each carrying a URL or backticked doc-ref and a 4-digit
year, and one `Decided by:` line naming one of performance, simplicity, LOC, ecosystem, or
maintenance. State the D-03 exemption verbatim — a plan that is pure config, documentation, or a
mechanical rename with no real mechanism choice may write `N/A — no mechanism choice` as the whole
section body instead of fabricating a comparison. State D-05: a phase that already has a SPEC.md
still needs this section in PLAN.md, because SPEC.md locks what is built and this section covers
how the mechanism was chosen. State that placeholder citations and years far outside the current
window are rejected mechanically.

`executor-numerics.md` (executor, `execute:wave:pre`) — numerical-stability and no-cancellation
framing: derive numeric parameters from first principles rather than tuning values until tests
pass; prefer numerically stable formulations over convenient ones; avoid catastrophic cancellation
and error propagation; efficiency and speed before simplicity where the two conflict; a
simplification that trades precision must be labelled with its ceiling, not left silent. State that
this fragment is advisory at execute time and that the capability's only gate already fired at
`plan:post`.

`verifier-precision.md` (verifier, `execute:wave:post`) — flag unjustified simplification or
precision loss as findings, not blockers: check that a mechanism the plan justified is the one that
shipped; flag silent scope or precision reductions, hardcoded constants standing in for derived
values, and superlative claims about performance or overhead that no measurement backs. State
explicitly that this capability declares no gate here, so these are findings only.

`ship-precision-advisory.md` (orchestrator, `ship:pre`) — confirm before shipping that precision
and efficiency claims in the milestone's summaries are backed by a measurement or a cited source,
and that any accepted simplification carries a named ceiling. State explicitly that this is
advisory only with no `ship:pre` gate declared by this capability (D-12).
  </action>
  <verify>
    <automated>for f in planner-sota executor-numerics verifier-precision ship-precision-advisory; do p="sota-numerics/.gsd/capabilities/sota-numerics/fragments/$f.md"; test -s "$p" || { echo "MISSING $p"; exit 1; }; done; md5sum sota-numerics/.gsd/capabilities/sota-numerics/fragments/*.md | awk '{print $1}' | sort -u | wc -l | grep -qx 4 && echo "four distinct fragments"</automated>
  </verify>
  <acceptance_criteria>
    - All four fragment files exist, are non-empty, and have four distinct md5 sums (D-13: no shared text).
    - No fragment file contains a fenced code block: `grep -c '```' sota-numerics/.gsd/capabilities/sota-numerics/fragments/*.md` reports 0 for every file.
    - `grep -l 'Alternatives Considered' sota-numerics/.gsd/capabilities/sota-numerics/fragments/planner-sota.md` matches, and the same file contains the strings `Decided by:` and `N/A — no mechanism choice`.
    - `planner-sota.md` names all four ranked criteria (performance, simplicity, ecosystem, maintenance) and mentions `plan:post`.
    - `planner-sota.md` mentions `SPEC.md` (D-05 restatement).
    - `verifier-precision.md` and `ship-precision-advisory.md` each contain the word `advisory` and state that no gate is declared at their point.
    - Each fragment is between 4 and 12 lines: `wc -l` on each reports a value in that range.
  </acceptance_criteria>
  <done>Four distinct, stage-tailored fragments exist whose planner entry documents a section format the Plan 01 gate script actually accepts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the four contributions and the per-role session-start framings</name>
  <files>sota-numerics/.gsd/capabilities/sota-numerics/capability.json, sota-numerics/hooks/session-start.sh, .gsd/capabilities/sota-numerics/capability.json, .gsd/capabilities/sota-numerics/fragments/planner-sota.md, .gsd/capabilities/sota-numerics/fragments/executor-numerics.md, .gsd/capabilities/sota-numerics/fragments/verifier-precision.md, .gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md</files>
  <read_first>
    - `sota-numerics/.gsd/capabilities/sota-numerics/capability.json` — the manifest as Plan 01 left it, with `contributions` still empty and the gate already declared.
    - `.gsd/capabilities/ponytail/capability.json` — the exact `contributions[]` entry field set to copy: `point`, `into`, `produces`, `consumes`, `fragment.path`, `when`, `onError`. Note ponytail also carries `configValues`; sota-numerics must NOT, since D-11 declares no second key.
    - `~/.claude/gsd-core/bin/lib/loop-host-contract.cjs` — the per-step `agentRoles` arrays that constrain `into`: the plan step allows `researcher`/`planner`/`checker`, the execute step allows `executor`/`verifier`, and the ship step allows only `orchestrator`.
    - `sota-numerics/hooks/session-start.sh` — the placeholder `FRAMING` strings Plan 01 left in the `case "$ROLE"` block.
    - `sota-numerics/tests/test-session-start.sh` — the assertions the new framings must keep passing.
  </read_first>
  <action>
Add four entries to `contributions[]` in `capability.json`, each with `produces: []`,
`consumes: []`, `when: "sota-numerics.enabled"`, `onError: "skip"`, and NO `configValues` key:

1. `point` `plan:pre`, `into` `planner`, `fragment.path` `fragments/planner-sota.md`
2. `point` `execute:wave:pre`, `into` `executor`, `fragment.path` `fragments/executor-numerics.md`
3. `point` `execute:wave:post`, `into` `verifier`, `fragment.path` `fragments/verifier-precision.md`
4. `point` `ship:pre`, `into` `orchestrator`, `fragment.path` `fragments/ship-precision-advisory.md`

`into: "orchestrator"` for the `ship:pre` entry is not a guess: the loop-host contract's ship step
declares `agentRoles: ["orchestrator"]` and nothing else, so any other value fails validation.

`onError: "skip"` on all four contributions is correct and is NOT the divergence Plan 01 recorded —
that divergence applies only to `gates[0].onError`, which stays `halt`. Do not change the gate's
value while editing this file.

Replace the placeholder `FRAMING` strings in `session-start.sh`'s `case "$ROLE"` block with
one-line condensations of the corresponding fragments: the planner line names the 2-cited-dated-
alternatives-plus-ranked-criterion requirement and warns that a blocking gate fires at `plan:post`;
the executor line names first-principles numeric parameters, stable formulations, and avoiding
cancellation and error propagation; the verifier line names flagging unjustified simplification and
unmeasured precision or performance claims; the generic default line states the SOTA/efficiency/
numerical-stability discipline in one sentence. Keep the `case` fall-through to `generic` and add
no `eval`.

Copy all four fragment files and the updated `capability.json` to `.gsd/capabilities/sota-numerics/`
so the dogfood install (D-04) stays byte-identical to the plugin bundle.
  </action>
  <verify>
    <automated>bash sota-numerics/tests/test-session-start.sh && jq -e '([.contributions[].point]|sort) == ["execute:wave:post","execute:wave:pre","plan:pre","ship:pre"] and ([.contributions[]|has("configValues")]|any|not) and .gates[0].onError == "halt" and (.config|length) == 1' .gsd/capabilities/sota-numerics/capability.json && diff -r sota-numerics/.gsd/capabilities/sota-numerics .gsd/capabilities/sota-numerics</automated>
  </verify>
  <acceptance_criteria>
    - `capability.json` parses and its `contributions` array has exactly 4 entries with points `plan:pre`, `execute:wave:pre`, `execute:wave:post`, `ship:pre`.
    - The `ship:pre` contribution's `into` is exactly `orchestrator`; the others are `planner`, `executor`, `verifier` respectively.
    - No contribution entry has a `configValues` key (D-11).
    - Every contribution's `when` is `sota-numerics.enabled` and `onError` is `skip`.
    - `gates[0].onError` is still `halt` and `gates[0].blocking` is still `true` — unchanged by this task.
    - `node ~/.claude/gsd-core/bin/gsd-tools.cjs loop render-hooks plan:pre --raw` output contains `sota-numerics`, and the same for `execute:wave:pre`, `execute:wave:post`, and `ship:pre`.
    - `bash sota-numerics/tests/test-session-start.sh` exits 0 with the new framings in place.
    - `grep -v '^#' sota-numerics/hooks/session-start.sh | grep -c 'eval '` reports 0.
    - `diff -r sota-numerics/.gsd/capabilities/sota-numerics .gsd/capabilities/sota-numerics` reports no differences.
    - Note for the executor: inline interpreter flags (`node -e`, `python3 -c`) are blocked by this environment's shell policy. Use `jq` for JSON assertions and a real script file for anything longer.
  </acceptance_criteria>
  <done>All four lifecycle points render a sota-numerics contribution through gsd-core's existing dispatch, with no new dispatch machinery and no gsd-core patch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fragment markdown → agent prompt | Fragment text is materialised verbatim into planner/executor/verifier/orchestrator prompts by gsd-core's contribution renderer. |
| `.planning/config.json` → `session-start.sh` `case` dispatch | Project config controls whether the banner prints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08 | Tampering | fragment content injected into agent prompts | low | mitigate | Fragments are static repo-tracked files with no interpolation, no `configValues`, and no reference to runtime data; changes are reviewable in git diff. Acceptance criteria forbid fenced code blocks, keeping fragments plain prose that cannot masquerade as executable instructions. |
| T-11-09 | Spoofing | a fragment drifting out of sync with what the gate script accepts, so the planner is steered toward a format that then blocks | medium | mitigate | Task 1 requires reading `check-alternatives.py` before writing the planner fragment, and acceptance criteria assert the fragment contains the literal `Decided by:` and exemption strings the script parses. |
| T-11-10 | Elevation of Privilege | argv-sourced role string in `session-start.sh` `case` dispatch | low | mitigate | Unchanged from Plan 01: fall-through to `generic`, no `eval`, asserted by the injection-shaped-role case in `test-session-start.sh` and the `eval` grep. |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this plan — markdown and JSON only. |

No threat is rated `high`; `security_block_on` is `high`, so nothing here blocks.
</threat_model>

<verification>
- `capability.json` validates and declares 4 contributions plus the 1 gate from Plan 01.
- `gsd_run loop render-hooks` names sota-numerics at all four of D-12's points.
- `bash sota-numerics/tests/test-session-start.sh` passes.
- The plugin bundle and the repo-root dogfood copy are identical trees.
</verification>

<success_criteria>
D-12's full four-point spread is live: a planner, executor, verifier, and ship-time orchestrator
each receive distinct, stage-tailored sota-numerics steering text through gsd-core's existing
contribution dispatch, gated on the single `sota-numerics.enabled` key, with the planner's fragment
documenting exactly the section format the `plan:post` gate accepts.
</success_criteria>

<output>
Create `.planning/phases/11-sota-numerics-capability-plugin-sota-efficiency-numerical-st/11-02-SUMMARY.md` when done.
Record in it: the four `into:` values used, and whether `render-hooks` was observed to materialise
each fragment or only to list the hook.
</output>