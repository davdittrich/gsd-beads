---
phase: 13-markdown-linting-capability-dogfood
plan: 04
type: execute
wave: 4
depends_on: ["13-02", "13-03"]
files_modified:
  - .gsd/capabilities/markdown-linting/scripts/lint.py
  - .gsd/capabilities/markdown-linting/tests/test_lint.py
autonomous: true
gap_closure: true
requirements: [MDL-04]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The capability's own documented no-stale-report guarantee (plan 02 `must_haves.prohibitions`; `TestFailOpen`'s class docstring: 'the report is never left stale/untouched') holds for **any** rumdl subprocess failure, not just the tool-absent/timeout/OSError cases the tests cover — verified by a mocked `subprocess.run(returncode=101, stdout=\"\")` run against a phase dir carrying a pre-existing `violation_count: 0` report, which must exit 0, print exactly one NOTICE, and leave the report rewritten with the `unavailable` sentinel rather than byte-identical (MDL-04, VERIFICATION.md Truth #6, 13-REVIEW.md CR-02)."
    - "`lint.py`'s documented `count` CLI subcommand degrades safely (no unhandled exception) when both `rumdl` and `uvx` are absent from PATH, matching the guard its sibling `fix` subcommand already has — verified by `lint.main(['count'])` under `shutil.which` mocked to `None` for both tools raising `RuntimeError('neither rumdl nor uvx is available on PATH')`, not `TypeError` (D-04 absent tier, VERIFICATION.md Truth #7, 13-REVIEW.md CR-01)."
    - "The `count` guard fires before any subprocess is spawned: with `subprocess.run` mocked to raise on call, the tool-absent `count` invocation still raises the RuntimeError and never touches the mock."
    - "A rumdl returncode of 2 (deliberate config/runtime error) still propagates uncaught and still writes no report — the CR-02 widening must not launder a broken ruleset into a fail-open sentinel (regression guard on `test_config_error_raises`)."
    - "The stdlib suite runs green at 12 tests under `python3 -m unittest discover` (10 pre-existing + the two new regression tests), on a machine with or without rumdl installed."
  artifacts:
    - .gsd/capabilities/markdown-linting/scripts/lint.py
    - .gsd/capabilities/markdown-linting/tests/test_lint.py
  key_links:
    - "unexpected rumdl exit code -> count_violations() raises CalledProcessError -> verify_post()'s widened except -> _write_report() sentinel branch -> artifact-frontmatter-equals reports block:true (never a stale clean number)"
    - "resolve_rumdl_invocation() returning None -> main()'s `count` branch guard -> RuntimeError (same failure shape fix() already produces), never `None + list`"
  prohibitions:
    - statement: "A LINT-REPORT.md must never present a number that was not measured on this run. This plan closes the last two paths by which that could happen; no task may narrow the returncode-2 raise into the fail-open branch to make an assertion easier."
      status: enforced-by-test
---

<objective>
Close the two reproduced, already-diagnosed defects in `scripts/lint.py` that VERIFICATION.md
marked FAILED (Truths #6 and #7), by applying 13-REVIEW.md's CR-01 and CR-02 patches verbatim and
pinning each with a regression test.

Purpose: the advisory gate's whole value is that its number is honest. Today a rumdl crash leaves a
prior clean count in place, unmeasured and presented as current — the exact failure MDL-04 exists
to prevent — and the `count` subcommand crashes with a raw `TypeError` on the same tool-absent
condition its sibling `fix` handles cleanly (D-04's absent tier).
Output: two guard clauses in `lint.py`, two regression tests in `test_lint.py`, suite green at 12.

This is a closure task against this phase's own findings. The fixes are already specified with
working code in `13-REVIEW.md`; do not redesign them, do not widen scope, do not re-open the
mechanism choice.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-markdown-linting-capability-dogfood/13-VERIFICATION.md
@.planning/phases/13-markdown-linting-capability-dogfood/13-REVIEW.md
@.planning/phases/13-markdown-linting-capability-dogfood/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin CR-01 and CR-02 with failing regression tests (RED)</name>
  <files>.gsd/capabilities/markdown-linting/tests/test_lint.py</files>
  <read_first>
    - .gsd/capabilities/markdown-linting/tests/test_lint.py (the suite as it stands: helpers `_write_phase_dir` / `_make_fake_project_root`, and the existing `TestFailOpen` and `TestToolResolution` classes these two tests join)
    - .gsd/capabilities/markdown-linting/scripts/lint.py (the code under test, unmodified — `count_violations` lines 71-83, `verify_post` lines 111-182, `fix` lines 185-207, `main`'s `count` branch lines 236-242)
    - .planning/phases/13-markdown-linting-capability-dogfood/13-VERIFICATION.md (gaps[] — the `missing` field of each gap names exactly the two tests this task writes, and the Behavioral Spot-Checks table records the exact exception each defect currently produces)
    - .planning/phases/13-markdown-linting-capability-dogfood/13-REVIEW.md (CR-01 and CR-02 — the target post-fix behavior these tests assert)
  </read_first>
  <behavior>
    - `test_count_cli_tool_absent_raises_runtime_error` (joins `TestToolResolution`): with `shutil.which` returning `None` for both tools and `subprocess.run` mocked to raise on any call, `lint.main(["count"])` raises `RuntimeError` whose message is `neither rumdl nor uvx is available on PATH`, and the `subprocess.run` mock is never invoked. Against unmodified `lint.py` this test FAILS with `TypeError: unsupported operand type(s) for +: 'NoneType' and 'list'`.
    - `test_unexpected_exit_code_fail_open_overwrites_stale_report` (joins `TestFailOpen`): with `rumdl` resolvable on PATH but `subprocess.run` returning `CompletedProcess(returncode=101, stdout="", stderr=<panic text>)`, `lint.verify_post(phase_dir)` returns 0, prints `lint.NOTICE` exactly once, and rewrites a pre-existing `violation_count: 0` report so its content differs from what was there before, contains `violation_count: unavailable`, does not contain `violation_count: 0`, and names `CalledProcessError` in `unavailable_reason`. Against unmodified `lint.py` this test FAILS with an uncaught `json.decoder.JSONDecodeError` and the report left byte-identical.
  </behavior>
  <action>
    Add exactly two test methods. No new test class, no new helper, no new fixture file — both
    reuse helpers the suite already has (`_write_phase_dir`, `_make_fake_project_root`) and the
    mocking idiom the existing `TestFailOpen` tests already use.

    **Test 1 — `test_count_cli_tool_absent_raises_runtime_error`, added to `TestToolResolution`.**
    Build a scratch project root with `_make_fake_project_root(tmp_path)` so `find_project_root`
    resolves against a tempdir rather than the live repo (the suite's standing rule: no test reads
    a real path under `.planning/`). Patch `lint.Path.cwd` to return that tmp_path via
    `mock.patch.object(lint.Path, "cwd", return_value=tmp_path)` — `main()`'s `count` branch calls
    `Path.cwd()` directly, so cwd must be controlled or the test result depends on where the runner
    was launched. Inside that, patch `shutil.which` to `return_value=None` and patch
    `subprocess.run` with `side_effect=AssertionError("rumdl/uvx must not be invoked when absent")`
    — the same sentinel `test_tool_absent_fail_open` uses, so the test proves the guard fires
    before any process is spawned rather than merely that some exception was raised. Assert with
    `self.assertRaises(RuntimeError)` as a context manager and then assert the substring
    `neither rumdl nor uvx is available on PATH` appears in `str(ctx.exception)`, so a future
    unrelated RuntimeError cannot satisfy this test.

    Extend `TestToolResolution`'s class docstring by one clause noting the class now also covers
    that every CLI subcommand guards the both-absent case rather than crashing — the class is D-04
    tier ordering plus its absent tier, and this test is the absent tier for `count`.

    **Test 2 — `test_unexpected_exit_code_fail_open_overwrites_stale_report`, added to
    `TestFailOpen`.** Use `_write_phase_dir(Path(tmp))`, write a pre-existing report at
    `phase_dir / "13-LINT-REPORT.md"` containing `violation_count: 0` (reuse the exact stale-report
    literal `test_tool_absent_overwrites_stale_zero_report_sentinel` already writes), and capture
    its text into a local before-value. Patch `shutil.which` with the
    `side_effect=lambda name: "/usr/bin/rumdl" if name == "rumdl" else None` idiom already used by
    the timeout/OSError tests, and patch `subprocess.run` with `return_value` set to a
    `subprocess.CompletedProcess` whose `returncode` is `101`, whose `stdout` is the empty string,
    and whose `stderr` carries a short Rust-panic-shaped string — this is the exact shape
    VERIFICATION.md reproduced live. Capture stdout into an `io.StringIO` via
    `mock.patch("sys.stdout", captured)` as the sibling tests do.

    Assert, in order: exit code is 0; `captured.getvalue().count(lint.NOTICE)` is 1; the report
    text now differs from the before-value (this is the staleness assertion proper — assert on the
    inequality, not only on the substrings, because a no-op write would satisfy substring checks on
    a differently-seeded report); the text contains `violation_count: unavailable`; the text does
    not contain a zero count line; the text names `CalledProcessError` in its `unavailable_reason`
    field.

    Write both tests against the POST-fix behavior. They are expected to fail now — that is this
    task's deliverable. Do not touch `lint.py` in this task, and do not weaken either assertion to
    make it pass early.
  </action>
  <verify>
    <automated>python3 -m unittest discover -s .gsd/capabilities/markdown-linting/tests -k count_cli_tool_absent -k unexpected_exit_code -v > /tmp/13-04-red.txt 2>&1; grep -q TypeError /tmp/13-04-red.txt &amp;&amp; grep -q JSONDecodeError /tmp/13-04-red.txt &amp;&amp; echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `RED-CONFIRMED` and exits 0 — meaning both new tests ran, both failed, and they failed with the two exception types VERIFICATION.md recorded (`TypeError` for CR-01, `JSONDecodeError` for CR-02), not with a setup error, import error, or `AssertionError` from the never-invoke sentinel. (`/tmp/13-04-red.txt` is a throwaway capture; it is not an artifact and must not be committed.)
    - That same run reports `Ran 2 tests` — the two `-k` filters select exactly the two new methods and nothing else (`-k` is repeatable and OR-matched since Python 3.7).
    - `python3 -m unittest discover -s .gsd/capabilities/markdown-linting/tests -v` reports `Ran 12 tests` with exactly 2 failures and 0 errors — the 10 pre-existing tests are untouched and still green.
    - `git diff --name-only` lists only `.gsd/capabilities/markdown-linting/tests/test_lint.py` — `lint.py` is not modified in this task.
    - `grep -v '^\s*#' .gsd/capabilities/markdown-linting/tests/test_lint.py | grep -c 'rumdl/uvx must not be invoked when absent'` returns `2` — the new `count` test reuses the existing never-invoke sentinel string rather than inventing a second one.
    - The new tests import nothing outside the standard library and add no module-level import that was not already present.
  </acceptance_criteria>
  <done>Both defects are pinned by named, currently-failing tests that reproduce the exact exceptions VERIFICATION.md recorded, with the rest of the suite still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply CR-01 and CR-02 guards to lint.py (GREEN)</name>
  <files>.gsd/capabilities/markdown-linting/scripts/lint.py</files>
  <read_first>
    - .gsd/capabilities/markdown-linting/scripts/lint.py (`count_violations` lines 71-83 and `verify_post`'s try/except at lines 164-174 are CR-02's two edit sites; `main`'s `count` branch lines 236-242 is CR-01's; `fix` lines 195-197 is the guard being mirrored, and `resolve_rumdl_invocation`'s docstring lines 60-68 is the one stale doc claim this creates)
    - .planning/phases/13-markdown-linting-capability-dogfood/13-REVIEW.md (CR-01 and CR-02 fix blocks — copy these, do not re-derive them)
    - .gsd/capabilities/markdown-linting/tests/test_lint.py (after Task 1 — the two tests this task turns green, plus `test_config_error_raises`, which must stay green and is the regression guard on the returncode-2 path)
  </read_first>
  <behavior>
    - `count_violations()` with a rumdl `returncode` of `0` or `1`: unchanged — parses stdout as a JSON array and returns its length. (Measured live in this repo 2026-08-18: rumdl exits `0` on a clean file and `1` on a file with violations, so these two are the entire success surface and the guard below cannot fire on a normal run.)
    - `count_violations()` with `returncode == 2`: unchanged — still raises `RuntimeError`, still uncaught by `verify_post()`, still writes no report.
    - `count_violations()` with any other returncode: raises `subprocess.CalledProcessError` carrying the returncode, argv, stdout and stderr, before reaching `json.loads`.
    - `verify_post()` when `count_violations()` raises that `CalledProcessError`: takes the existing fail-open branch — prints `NOTICE` once, fully overwrites the report with the `unavailable` sentinel and an `unavailable_reason` naming the exception, returns 0.
    - `main()`'s `count` branch with both tools absent: raises `RuntimeError("neither rumdl nor uvx is available on PATH")` before calling `count_violations()`.
    - `fix()`: unchanged in behavior on the absent path; on the post-fix count path a crash exit code now surfaces as `CalledProcessError` instead of `JSONDecodeError`.
  </behavior>
  <action>
    Apply 13-REVIEW.md's two fixes verbatim. Three edit sites, roughly 10-15 lines total.

    **CR-01, in `main()`'s `count` branch.** Immediately after `rumdl_argv = resolve_rumdl_invocation()`
    and before the `print(count_violations(...))` call, insert the identical two-line guard `fix()`
    already carries: `if rumdl_argv is None:` raising `RuntimeError` with the message
    `neither rumdl nor uvx is available on PATH`. Same message string as `fix()`'s, character for
    character — the two subcommands must fail identically on an identical condition. Do not
    refactor `fix()`, do not relocate the guard into `count_violations()`, do not convert either
    site into a shared helper: the reviewed decision is call-site symmetry with `fix()`, and
    re-opening it is out of scope for a closure task.

    **CR-02, part 1, in `count_violations()`.** After the existing `returncode == 2` check and
    before the `json.loads(result.stdout)` return, insert a check that the returncode is one of the
    two rumdl actually uses on a completed run, raising `subprocess.CalledProcessError` with
    `result.returncode`, `argv`, `result.stdout` and `result.stderr` when it is not. Order matters:
    the `== 2` check stays first so a config error keeps producing `RuntimeError`, which is
    deliberately NOT in `verify_post()`'s except tuple. Passing `argv` (not the bare tool name)
    means the exception message names the exact invocation that failed, which is what lands in the
    report's `unavailable_reason`.

    **CR-02, part 2, in `verify_post()`.** Widen the existing except tuple around the
    `count_violations()` call to also catch `subprocess.CalledProcessError` alongside
    `subprocess.TimeoutExpired` and `OSError`. The body of that branch is already correct — do not
    edit it. `CalledProcessError` is not an `OSError` subclass, so it genuinely has to be named.

    **Docstrings, same commit.** Three claims go stale the moment the code changes and must be
    corrected in the same edit:
    (a) `count_violations()`'s docstring — note that a returncode outside the completed-run pair is
    treated as a crash and surfaced to the caller as a subprocess error rather than being parsed;
    (b) `verify_post()`'s docstring — its MDL-04 paragraph currently enumerates the fail-open
    triggers as tool-absent plus timeout plus OS error; add the crash-exit-code case to that
    enumeration, and keep the existing sentence explaining why returncode 2 stays outside it;
    (c) `resolve_rumdl_invocation()`'s docstring — its trailing note that a `None` return "may
    propagate as a raise in this task's callers" is now wrong in the way that matters: after this
    change all three callers guard it explicitly, so restate it as such.
    Describe the new behavior in prose; do not paste the new source lines into the docstrings.

    Leave everything else alone. Specifically do NOT: touch the README (its tier list is about
    tool availability, and its standing "never a stale count" claim becomes true rather than stale
    as a result of this change); address WR-01's duplicated-argv drift or WR-02's YAML escaping
    (both are recorded, non-blocking, and out of this closure's scope); add a STATE.md blocker
    append on the new fail-open path (plan 02 deliberately declined that for the sibling paths and
    the reasoning is unchanged); or wrap the `CalledProcessError` that now escapes `fix()` — `fix()`
    is a developer-invoked tool and a rumdl crash there should stay loud.
  </action>
  <verify>
    <automated>python3 -m unittest discover -s .gsd/capabilities/markdown-linting/tests -v</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m unittest discover -s .gsd/capabilities/markdown-linting/tests -v` exits 0 and reports `Ran 12 tests` with `OK` — the two Task 1 regression tests are now green and all 10 pre-existing tests still pass.
    - `test_config_error_raises` is among the passing tests: a rumdl returncode of 2 still raises out of `verify_post()` and still leaves no report file, so the widened except tuple did not swallow the config-error path.
    - `grep -v '^\s*#' .gsd/capabilities/markdown-linting/scripts/lint.py | grep -c 'if rumdl_argv is None'` returns `2` — the guard now exists at both call sites (`fix()` and `main()`'s `count` branch).
    - `grep -v '^\s*#' .gsd/capabilities/markdown-linting/scripts/lint.py | grep -c 'raise subprocess.CalledProcessError'` returns at least `1`.
    - `grep -v '^\s*#' .gsd/capabilities/markdown-linting/scripts/lint.py | grep -c 'subprocess.CalledProcessError)'` returns at least `1` — the widened except tuple is present.
    - `python3 .gsd/capabilities/markdown-linting/scripts/lint.py count` run from the repo root prints `0` (the live tree is at zero violations per 13-03; the guard must not disturb the happy path).
    - `python3 .gsd/capabilities/markdown-linting/scripts/lint.py verify-post .planning/phases/13-markdown-linting-capability-dogfood` exits 0 and the regenerated `13-LINT-REPORT.md` still carries `violation_count: 0`.
    - `git diff --stat .gsd/capabilities/markdown-linting/scripts/lint.py` shows fewer than 30 changed lines — this is a guard-clause patch plus three docstring corrections, not a refactor.
    - `git diff --name-only` lists no file under `.gsd/capabilities/markdown-linting/` other than `scripts/lint.py`.
  </acceptance_criteria>
  <reversibility rating="reversible">Two added guard clauses in one file; `git revert` restores prior behavior exactly, and no artifact format, config key or gate predicate changes.</reversibility>
  <done>Both VERIFICATION.md truths that failed are now true and machine-asserted: a rumdl crash exit code leaves a sentinel report rather than a stale one, and `count` fails identically to `fix` when neither tool is on PATH.</done>
</task>

</tasks>

## Artifacts this phase produces

**None new.** This plan modifies two existing files in place and introduces no new symbol, file,
config key, frontmatter field, gate, or public interface.

| Symbol / path | Kind | Status |
|---|---|---|
| `.gsd/capabilities/markdown-linting/scripts/lint.py` | existing module | modified (two guard clauses, three docstring corrections) |
| `.gsd/capabilities/markdown-linting/tests/test_lint.py` | existing test module | modified (two test methods added to existing classes) |
| `test_count_cli_tool_absent_raises_runtime_error` | test method added to the existing `TestToolResolution` | new method on an existing class, not a new artifact |
| `test_unexpected_exit_code_fail_open_overwrites_stale_report` | test method added to the existing `TestFailOpen` | new method on an existing class, not a new artifact |

## Alternatives Considered

N/A — no mechanism choice.

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crashing rumdl subprocess -> LINT-REPORT.md -> ship:pre gate | an unmeasured tree must not reach the gate as a previously-measured number |

No new boundary is introduced. This plan adds no input surface, no network or auth path, no new
file write, and no new external invocation — it adds two defensive guards on paths that already
exist. Both entries below are re-dispositions of already-registered threats that this patch moves
from partially-mitigated to mitigated.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-17 | Spoofing | `verify_post()` / `count_violations()` — a stale `violation_count` surviving a rumdl crash and being read by the gate as a current, clean measurement (the residual of T-13-07, which plan 02 mitigated only for the tool-absent/timeout/OSError paths) | high | mitigate | `count_violations()` raises `CalledProcessError` on any returncode outside the completed-run pair, routing it into `verify_post()`'s existing sentinel branch; asserted by `test_unexpected_exit_code_fail_open_overwrites_stale_report`, which fails if the pre-existing report is left byte-identical. The returncode-2 config-error path stays uncaught (T-13-08 unchanged), asserted by `test_config_error_raises`. |
| T-13-18 | Denial of Service | `main()`'s `count` branch — an unhandled `TypeError` aborting a lifecycle-adjacent invocation on a machine with no rumdl, instead of the clean documented failure `fix()` produces | low | mitigate | The `None`-guard `fix()` already carries, applied to the `count` branch; asserted by `test_count_cli_tool_absent_raises_runtime_error`, which also asserts no subprocess is spawned first. |

</threat_model>

<verification>
- `python3 -m unittest discover -s .gsd/capabilities/markdown-linting/tests -v` exits 0, reporting `Ran 12 tests ... OK` (stdlib runner only, no third-party test dependency).
- The two tests named in VERIFICATION.md's `gaps[].missing` fields exist by name and pass:
  `test_count_cli_tool_absent_raises_runtime_error` (CR-01) and
  `test_unexpected_exit_code_fail_open_overwrites_stale_report` (CR-02).
- Both tests were observed FAILING before Task 2 and PASSING after — recorded in
  `13-04-SUMMARY.md` with the two exception types seen in the red run, so the tests are proven to
  actually exercise the defects rather than passing vacuously.
- `test_config_error_raises` still passes: returncode 2 remains an uncaught `RuntimeError` writing
  no report.
- `lint.py count` from the repo root still prints `0`, and `lint.py verify-post` on this phase dir
  still regenerates `13-LINT-REPORT.md` with `violation_count: 0` — the live happy path is
  undisturbed.
- `git diff --name-only` for the whole plan lists exactly two files: `scripts/lint.py` and
  `tests/test_lint.py`.
</verification>

<success_criteria>
- VERIFICATION.md Truth #6 is satisfiable: for any rumdl subprocess failure — tool absent, timeout,
  OS error, or an unexpected exit code — `LINT-REPORT.md` is fully overwritten with the
  non-numeric `unavailable` sentinel, never left stale, and the gate reads it as `block: true`.
- VERIFICATION.md Truth #7 is satisfiable: `lint.py count` with both `rumdl` and `uvx` absent
  raises the same clear `RuntimeError` `fix()` raises, with no subprocess spawned.
- MDL-04's "never a stale count presented as current" guarantee is now true in general rather than
  only for the three failure modes plan 02 enumerated.
- The suite is green at 12 tests, and the diff is confined to two already-existing files with no
  new abstraction, helper, class, fixture, or config key.
</success_criteria>

<output>
Create `.planning/phases/13-markdown-linting-capability-dogfood/13-04-SUMMARY.md` when done.
Record in it: the two exception types observed in the Task 1 red run, the before/after test counts
(10 green -> 12 green), and confirmation that `test_config_error_raises` stayed green across the
change.
</output>
