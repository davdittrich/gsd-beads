---
phase: 03-enforcement
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - $HOME/.claude/gsd-core/workflows/ship.md
  - .gsd/capabilities/beads/GSD-CORE-PATCH.md
  - .gsd/capabilities/beads/scripts/sync.py
  - .gsd/capabilities/beads/tests/test_sync.py
  - .gsd/capabilities/beads/skills/beads-status/SKILL.md
autonomous: true
requirements: [B9, B10]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "With capability.json's two ship:pre gates (blocking_open==0, diverged==0) active and BEADS.md's blocking_open field nonzero, gsd_run check predicate invoked exactly as ship.md's new generic gate-dispatch step specifies returns block:true naming the field and its actual/expected values -- the same command a real /gsd-ship run would execute."
    - "With beads.ship_gate set to false in .planning/config.json, gsd_run loop render-hooks ship:pre --raw excludes both beads kind==\"gate\" entries from activeHooks while still returning the beads kind==\"step\" entry (ref.skill==\"beads-status\") -- the override path stays dispatched even when the blocking gates are inactive."
    - "A phase directory with no *-BEADS.md yet, under a gate whose onError==\"skip\", is skipped by ship.md's new dispatch loop before the generic predicate evaluator ever runs -- proven by showing the evaluator itself returns block:true (fail-closed) on that same missing artifact, which is exactly the outcome the pre-check exists to prevent (B6, PROJECT.md fail-open constraint)."
    - "ship.md's new generic ship:pre step-dispatch (mirrored from ship_post_capability_dispatch's contract) is the only path that can invoke Skill(gsd-beads-status) at ship:pre -- ship_override's git trailer / bd comment primitive (Plan 02) is otherwise unreachable from a real /gsd-ship run."
    - "check-shipmd-patch reports the patch present when ship.md contains the exact marker string, and prints a one-line reapply pointer to GSD-CORE-PATCH.md when it does not, proven against both a marker-containing and marker-stripped fixture copy of ship.md."
  artifacts:
    - $HOME/.claude/gsd-core/workflows/ship.md
    - .gsd/capabilities/beads/GSD-CORE-PATCH.md
    - .gsd/capabilities/beads/scripts/sync.py
    - .gsd/capabilities/beads/tests/test_sync.py
    - .gsd/capabilities/beads/skills/beads-status/SKILL.md
  key_links:
    - "ship.md preflight_checks new step 8 -> gsd_run check predicate -> gate-predicate-evaluator.cjs artifact-frontmatter-equals -> BEADS.md frontmatter (Plan 01's real blocking_open/diverged)"
    - "ship.md preflight_checks new step 9 -> Skill(gsd-beads-status) -> beads-status/SKILL.md Step 2c -> sync.py ship_override -> git trailer + bd comment (Plan 02)"
    - "beads-status/SKILL.md new Step 2d -> sync.py check_shipmd_patch -> ship.md's own patch marker (self-detecting staleness after a gsd-core update)"
  prohibitions:
    - "Never call bd directly from ship.md's new dispatch loop or from check_shipmd_patch -- both read only BEADS.md/ship.md text, matching the locked PROJECT.md gate-predicate constraint (no gate.check.predicate may query bd live)."
    - "Never auto-edit ship.md programmatically from check_shipmd_patch -- warn with the exact reapply pointer only (GSD-CORE-PATCH.md); an automated markdown patcher against a file this project does not own is a correctness risk this plan explicitly declines."
    - "Never remove, duplicate, or re-process the existing hardcoded security/broken-windows ship:pre handling (steps 6/7) inside the new generic loop -- step 8 explicitly excludes capId=='security' and capId=='broken-windows'."
    - "Never touch 03-01-PLAN.md's or 03-02-PLAN.md's own tasks, frontmatter, or must_haves -- only 03-02-PLAN.md's Known Limitation section is revised, per this planning run's explicit instruction, and only to point forward at this plan."
  flagged_assumptions:
    - "ship.md's preflight_checks step structure -- specifically the anchor line 'If no active `broken-windows` `ship:pre` gate hook is present...skip this check silently.' immediately followed by </step> -- is assumed stable between planning and execution of this plan; guarded by a <precondition> that re-reads the anchor before editing, not independently verified against a newer gsd-core release that might restructure preflight_checks."
    - "gate-predicate-evaluator.cjs's artifact-frontmatter-equals treating a missing artifact as block:true (fail-closed) rather than a step-1 command error is read directly from its source (gsd-core/bin/lib/gate-predicate-evaluator.cjs) during planning, not from documentation -- assumed unchanged at execution time; the fail-open pre-check in step 8 exists specifically because this was independently confirmed, not assumed."
---

<objective>
Make the `ship:pre` enforcement Plan 01 and Plan 02 declared actually fire on a real `/gsd-ship`
run, by adding a generic `ship:pre` gate-and-step dispatch loop to the installed
`$HOME/.claude/gsd-core/workflows/ship.md` -- the machine-local patch the user explicitly
authorized as in-scope Phase 3 work, overriding the project's N2 no-fork-patch constraint for
this one file. Also add a self-detecting staleness check so a future `gsd-core` update or
capability reinstall that silently drops the patch is caught and reported, not discovered the
next time a phase ships with an open blocking issue that nobody was warned about.

Purpose: Plan 01 makes `blocking_open`/`diverged` real, Plan 02 declares the two `ship:pre` gates
plus `ship_override`, but the installed `ship.md`'s `preflight_checks` step only special-cases
`capId == "security"` and `capId == "broken-windows"` -- there is no generic enumeration loop
over `loop render-hooks ship:pre --raw`'s `activeHooks`. Without this plan, ROADMAP.md's Phase 3
Success Criteria 1 and 2 ("ship:pre blocks and names it", "beads.ship_gate=false allows the ship
and records the override") are false at ship time regardless of how correct Plan 01/02's own
code is. An upstream feature request (open-gsd/gsd-core#3554) asks for this generically, natively;
if it ships, this local patch becomes unnecessary and should be reverted (see
`GSD-CORE-PATCH.md`'s Revert Condition).

Output: two new numbered steps inside `ship.md`'s `preflight_checks`, bracketed by an
idempotency-detectable patch marker; `GSD-CORE-PATCH.md` holding the verbatim reapply source;
`sync.py`'s `check_shipmd_patch` + `check-shipmd-patch` CLI subcommand wired into
`beads-status`'s `ship:pre` dispatch; new live (non-mocked) test coverage proving the exact
command sequences the patch specifies actually block, allow, and skip correctly against the real
`gsd_run` CLI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-enforcement/03-CONTEXT.md
@.planning/phases/03-enforcement/03-PATTERNS.md
@.planning/phases/03-enforcement/03-02-SUMMARY.md

@.gsd/capabilities/beads/scripts/sync.py
@.gsd/capabilities/beads/tests/test_sync.py
@.gsd/capabilities/beads/capability.json
@.gsd/capabilities/beads/skills/beads-status/SKILL.md
@$HOME/.claude/gsd-core/workflows/ship.md
@$HOME/.claude/gsd-core/references/loop-hook-dispatch.md
@$HOME/.claude/gsd-core/bin/lib/gate-predicate-evaluator.cjs
</context>

## Artifacts this phase produces (this plan)

- `ship.md` `preflight_checks` step 8: "Generic `ship:pre` gate dispatch (capability-driven,
  beyond security/broken-windows)" -- two-step gate contract via `gsd_run check predicate`,
  reused from the identical pattern already live at `verify:pre`/`execute:wave:post`/`execute:post`
- `ship.md` `preflight_checks` step 9: "Generic `ship:pre` step dispatch (capability-driven)" --
  mirrors `ship_post_capability_dispatch`'s existing "Generic step hook dispatch contract"
- Patch marker comment convention: `<!-- gsd-beads-patch:ship-pre-generic-dispatch v1 -->` /
  `<!-- /gsd-beads-patch:ship-pre-generic-dispatch v1 -->`, bracketing steps 8+9 in `ship.md`
- `.gsd/capabilities/beads/GSD-CORE-PATCH.md` -- new file, the verbatim reapply source + rationale
  + revert condition (upstream #3554)
- `check_shipmd_patch(ship_md_path_override=None)` -- new function, `sync.py`
- `SHIP_MD_PATCH_MARKER` -- new module constant, `sync.py`
- `check-shipmd-patch [--ship-md-path <path>]` -- new CLI subcommand, `sync.py`
- `beads-status/SKILL.md`: "Step 2d -- ship:pre: verify the local ship.md patch (reapply check)"
  section; "## Known Gap" section revised to "## Patch Status (gap closed locally, 03-03)"
- `test_sync.py`: `TestShipPreGenericDispatch`, `TestCheckShipmdPatch` classes

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Patch ship.md's preflight_checks with the generic ship:pre gate+step dispatch loop; prove it live</name>
  <files>$HOME/.claude/gsd-core/workflows/ship.md, .gsd/capabilities/beads/GSD-CORE-PATCH.md, .gsd/capabilities/beads/tests/test_sync.py</files>
  <read_first>
    $HOME/.claude/gsd-core/workflows/ship.md (preflight_checks step 50-156 in full, especially
    steps 6-7's SECURITY_FILE/WINDOWS_GATE_ACTIVE pattern and the SHIP_PRE_HOOKS_JSON resolution
    at line 99; ship_post_capability_dispatch step 462-510 in full -- this is the exact contract
    to mirror for kind=="step")
    $HOME/.claude/gsd-core/workflows/verify-work.md (verify_pre_hooks step, ~63-99 -- the exact
    "two-step gate contract" wording and the `gsd_run check predicate --predicate ... --phase-dir
    "${PHASE_DIR}" --raw` invocation form to reuse verbatim)
    $HOME/.claude/gsd-core/workflows/execute-phase.md (lines ~1005-1047, ~1254-1261 -- two more
    instances of the same two-step gate contract, confirming the exact onError/blocking branching
    text)
    $HOME/.claude/gsd-core/bin/lib/gate-predicate-evaluator.cjs (evaluateArtifactFrontmatterEquals,
    ~104-146 -- confirms a missing artifact returns block:true/artifactNotFound:true on a normal
    exit, NOT a command failure; this is why the fail-open pre-check below is necessary, not
    optional)
    .gsd/capabilities/beads/tests/test_sync.py (_bd_on_path ~23-24, TestLiveDependencies ~176-260
    -- the skipUnless-guarded live-subprocess pattern to mirror for gsd_run-based tests)
    .planning/phases/03-enforcement/03-02-PLAN.md (the exact gates[]/config/steps[] shape 03-02
    declares -- this task's dispatch loop must correctly process that exact shape once 03-02 has
    landed)
  </read_first>
  <precondition>
    $HOME/.claude/gsd-core/workflows/ship.md's preflight_checks step still ends with the line "If
    no active `broken-windows` `ship:pre` gate hook is present (gate disabled via
    `workflow.windows_enforce=false`, the default -- tracking continues but the gate is opt-in),
    skip this check silently." immediately followed by `</step>` -- re-read this exact anchor
    before editing; if gsd-core has restructured preflight_checks since planning, halt and
    re-derive the insertion point rather than blindly appending after a stale anchor.
  </precondition>
  <behavior>
    - test_predicate_blocks_on_nonzero_blocking_open: a temp phase_dir with a fixture
      `{phase}-BEADS.md` whose frontmatter has blocking_open: 1 -- invoking the real
      `gsd_run check predicate --predicate '{"kind":"artifact-frontmatter-equals","artifact":
      "BEADS.md","field":"blocking_open","equals":0}' --phase-dir <phase_dir> --raw` as a
      subprocess returns exit 0 and JSON with block=true and a message containing "blocking_open".
    - test_predicate_passes_on_zero_blocking_open_and_diverged: same fixture but
      blocking_open: 0 / diverged: 0 -- both field checks (run separately, one per field) return
      block=false.
    - test_fail_open_precheck_skips_missing_artifact_before_evaluator_would_block: an empty temp
      phase_dir (no `*-BEADS.md` at all) -- (a) a bash glob `ls <phase_dir>/*-BEADS.md` (the exact
      pre-check ship.md's step 8 specifies) returns no match, proving the pre-check correctly
      detects "nothing to evaluate yet"; (b) calling the real `gsd_run check predicate` anyway
      against that same missing artifact independently confirms it returns block=true (fail-closed
      by the evaluator's own design) -- together these prove the pre-check is load-bearing, not
      redundant: without it, a phase with no BEADS.md yet would incorrectly block shipping.
    - test_beads_gate_hooks_excluded_step_hook_retained_when_ship_gate_false: skipped unless
      capability.json (real file, this project) already declares a `ship:pre` gate with
      `check.predicate.artifact == "BEADS.md"` (i.e. Plan 02 has landed). Reads the current
      `beads.ship_gate` value from `.planning/config.json`, writes `false` in its place, runs the
      real `gsd_run loop render-hooks ship:pre --raw` from the project root, asserts
      `activeHooks` contains zero entries with `capId=="beads" and kind=="gate"` and at least one
      entry with `capId=="beads" and kind=="step" and ref.skill=="beads-status"`, then restores
      the original `beads.ship_gate` value in a `finally`/`addCleanup` block regardless of
      assertion outcome.
  </behavior>
  <action>
    Add a module-level helper `_gsd_tools_path()` to test_sync.py (near `_bd_on_path`): resolve
    `Path(os.environ.get("CLAUDE_CONFIG_DIR", str(Path.home() / ".claude"))) / "gsd-core" / "bin"
    / "gsd-tools.cjs"`, return it if it exists else None. Guard every new live test with
    `@unittest.skipUnless(_gsd_tools_path() is not None and shutil.which("node") is not None,
    "gsd-tools.cjs / node not found")`, invoking it as
    `subprocess.run(["node", str(_gsd_tools_path()), "check", "predicate", "--predicate",
    <json>, "--phase-dir", str(phase_dir), "--raw"], capture_output=True, text=True, timeout=30)`
    (and the analogous `["node", str(_gsd_tools_path()), "loop", "render-hooks", "ship:pre",
    "--raw"]` invocation for the fourth test, run with `cwd=<project root, resolved the same way
    find_project_root resolves it from this test file's own path>`). Add class
    TestShipPreGenericDispatch implementing the four behaviors above exactly, each parsing
    `json.loads(result.stdout)` and asserting on `block`/`message`/`activeHooks` fields.

    In `$HOME/.claude/gsd-core/workflows/ship.md`, insert two new preflight_checks list items (8
    and 9) immediately before the step's closing `</step>` tag (after step 7's existing final
    line), the whole insertion bracketed top and bottom by the literal HTML comments
    `<!-- gsd-beads-patch:ship-pre-generic-dispatch v1 -->` and
    `<!-- /gsd-beads-patch:ship-pre-generic-dispatch v1 -->` on their own lines.

    **Step 8** ("Generic `ship:pre` gate dispatch (capability-driven, beyond
    security/broken-windows)."): state that `SHIP_PRE_HOOKS_JSON` (already resolved in step 6)
    carries every active `ship:pre` gate, not only `security`/`broken-windows`; inspect
    `activeHooks` in-context for every entry where `kind == "gate"` and `capId` is neither
    `"security"` nor `"broken-windows"` (those two stay exclusively handled by steps 6/7 -- do not
    reprocess them here). For each such entry, in array order: **(a) fail-open pre-check** --
    when `hook.check.predicate.kind == "artifact-frontmatter-equals"` and `hook.onError ==
    "skip"`, resolve `ls "${PHASE_DIR}"/*-"${hook.check.predicate.artifact}" 2>/dev/null | head
    -1` (the same glob form `SECURITY_FILE` already uses at step 6); if that file does not exist,
    skip this gate entry entirely and move to the next -- do NOT call `gsd_run check predicate`
    for it. State explicitly why: the generic evaluator's `artifact-frontmatter-equals` kind fails
    CLOSED (returns `block: true`) on a missing artifact by design, which is correct for an
    `onError: "halt"` gate like `security`'s own but wrong for a capability that declared
    `onError: "skip"` specifically to mean "not yet computed, never block on it" (PROJECT.md's
    fail-open constraint) -- this one-line existence check preserves that promise generically,
    without naming any specific `capId`. A gate whose predicate kind is not
    `artifact-frontmatter-equals`, or whose `onError` is `"halt"`, skips this pre-check and goes
    straight to evaluation. **(b) evaluate**: for a `check.query` gate run
    `gsd_run check ${hook.check.query} "${PHASE_NUMBER}" --raw`; for a `check.predicate` gate run
    `gsd_run check predicate --predicate '<hook.check.predicate as JSON>' --phase-dir
    "${PHASE_DIR}" --phase-number "${PHASE_NUMBER}" --raw`, capturing `GATE_RESULT`/`CHECK_EXIT`.
    **(c) two-step gate contract**, identical wording to `verify:pre`/`execute:wave:post`/
    `execute:post`: Step 1 -- non-zero `CHECK_EXIT`, empty output, or unparseable JSON routes by
    `hook.onError` (`"halt"` blocks shipping with a command-error message; `"skip"` logs a warning
    and continues, never reading `GATE_RESULT.block`). Step 2 -- only reached on command success:
    `hook.blocking == true` and `GATE_RESULT.block == true` blocks shipping and presents a
    two-line message -- line 1: `⚠ Ship gate blocked ({hook.capId}): {GATE_RESULT.message}`; line
    2: `See {PHASE_DIR}/*-{hook.check.predicate.artifact} for detail (if the predicate is
    artifact-frontmatter-equals), or set {hook.when} to false in .planning/config.json to
    override, if this capability supports one and records it. Then re-run /gsd-ship.` -- this halt
    is not bypassed by `onError` (that governs step 1 only). `hook.blocking == false` or
    `GATE_RESULT.block == false` continues silently (or prints an advisory line for a non-blocking
    gate with `block == true`). If `activeHooks` has no qualifying `kind == "gate"` entry, skip
    step 8 silently.

    **Step 9** ("Generic `ship:pre` step dispatch (capability-driven)."): state this runs here --
    before `push_branch` -- specifically so a step that amends the not-yet-pushed HEAD commit
    (e.g. an override-audit trailer) lands before the push. For each active `SHIP_PRE_HOOKS_JSON`
    entry where `kind == "step"`: honor `consumes` exactly as `ship_post_capability_dispatch`
    already does (`ls "${PHASE_DIR}"/*-<name> 2>/dev/null | head -1` per consumed name; if any
    consumed artifact is absent, skip that hook entirely); if `ref.agent` is set, reuse
    `ship_post_capability_dispatch`'s existing spawn-banner, runtime-aware-dispatch,
    `#2684`-model-resolution, and input-validation rules verbatim (point at that section by name
    rather than duplicating ~30 lines -- "identical to the ship:post Generic step hook dispatch
    contract above, substituting ship:pre banner wording"); if `ref.skill` is set, dispatch
    `Skill(skill="gsd-${hook.ref.skill}", args="${PHASE_NUMBER} --auto ${GSD_WS}")`. Each dispatch
    is best-effort (`onError: "skip"` -- the only value the beads-status entry declares): a
    failure is recorded as a warning and preflight continues, never re-raised. If `activeHooks`
    has no `kind == "step"` entry, skip step 9 silently.

    Create `.gsd/capabilities/beads/GSD-CORE-PATCH.md`: a heading naming the target file
    (`$HOME/.claude/gsd-core/workflows/ship.md`, machine-local, shared across every gsd-core
    project on this machine), the exact insertion anchor (the line ending "...skip this check
    silently." after step 7, before `</step>`), the N2-override rationale (cite PROJECT.md's
    Constraints section, "Overridden 2026-08-15" entry), the upstream tracking issue
    (open-gsd/gsd-core#3554) with an explicit "Revert Condition" subsection stating this patch
    should be deleted from `ship.md` once #3554 ships a native generic `ship:pre` dispatch loop,
    the patch marker strings, and a "## Patch Content (verbatim)" section containing the exact
    same steps-8-and-9 text just written into `ship.md`, fenced as a markdown code block, byte-for
    -byte identical, so a human (or `check_shipmd_patch`'s eventual reader) can paste it back in
    after a gsd-core update strips it.
  </action>
  <verify>
    <automated>python3 -m pytest .gsd/capabilities/beads/tests/test_sync.py -k ShipPreGenericDispatch -q</automated>
  </verify>
  <acceptance_criteria>
    - All four TestShipPreGenericDispatch tests pass (the fourth self-skips only if Plan 02's
      gates are not yet present in capability.json).
    - `grep -c 'gsd-beads-patch:ship-pre-generic-dispatch v1' "$HOME/.claude/gsd-core/workflows/ship.md"`
      returns exactly 2 (opening and closing marker, each on its own line).
    - `GSD-CORE-PATCH.md` exists and its "## Patch Content (verbatim)" section's fenced block is
      byte-for-byte identical to the text between the two markers in the live `ship.md`.
    - Steps 6 and 7's existing security/broken-windows text in `ship.md` is unmodified (diff the
      file's lines 50-156 range before/after the marker insertion; only new content was appended).
  </acceptance_criteria>
  <reversibility rating="costly">
    ship.md is shared, machine-global, and outside this project's git repository -- a future
    gsd-core update or capability/plugin reinstall can silently overwrite it and drop this patch
    with no error (Task 2's check-shipmd-patch exists specifically to catch that). The patch is
    reversible by deleting the marker-bracketed block, but there is no automatic rollback and no
    version-controlled history for this specific file the way there is for files inside this
    repo -- GSD-CORE-PATCH.md is the durable record. Revert once open-gsd/gsd-core#3554 ships a
    native equivalent.
  </reversibility>
  <done>ship.md's preflight_checks step contains steps 8 and 9, bracketed by the patch marker;
  GSD-CORE-PATCH.md holds the verbatim reapply source and revert condition; four live
  (non-mocked) tests against the real gsd_run CLI prove the block/allow/skip/override-dispatch
  behavior the new steps specify.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Self-detecting staleness check (check-shipmd-patch), wired into beads-status's ship:pre dispatch</name>
  <files>.gsd/capabilities/beads/scripts/sync.py, .gsd/capabilities/beads/tests/test_sync.py, .gsd/capabilities/beads/skills/beads-status/SKILL.md</files>
  <read_first>
    .gsd/capabilities/beads/scripts/sync.py (module-level constants near FRONTMATTER_RE line 28,
    main() CLI argparse wiring lines 935-976, find_project_root lines 71-84)
    .gsd/capabilities/beads/skills/beads-status/SKILL.md (Step 1.5 branch, Step 2c and "## Known
    Gap" -- both added by Plan 02, full file)
    .planning/phases/03-enforcement/03-03-PLAN.md Task 1 (this plan's own GSD-CORE-PATCH.md
    content and the SHIP_MD_PATCH_MARKER string, just written)
  </read_first>
  <behavior>
    - check_shipmd_patch(ship_md_path_override=None): resolves the target path from
      ship_md_path_override if given, else
      Path(os.environ.get("CLAUDE_CONFIG_DIR", str(Path.home()/".claude"))) / "gsd-core" /
      "workflows" / "ship.md". If the file does not exist at all, prints a "ship.md not found at
      <path> -- cannot verify the local ship:pre dispatch patch" notice and returns 1. If it
      exists and its text contains SHIP_MD_PATCH_MARKER, prints "ship.md ship:pre patch: present
      (v1)" and returns 0. If it exists but does not contain the marker, prints the exact one-line
      warning "⚠ ship.md's ship:pre generic gate/step dispatch patch (beads) is missing -- the two
      ship:pre gates and the ship_override step will not fire. Reapply:
      see .gsd/capabilities/beads/GSD-CORE-PATCH.md" and returns 1.
    - test_reports_present_when_marker_found: a temp file containing SHIP_MD_PATCH_MARKER anywhere
      in its text -- check_shipmd_patch(that path) returns 0 and its stdout contains "present".
    - test_reports_missing_with_reapply_pointer_when_marker_absent: a temp file with unrelated
      text (no marker) -- returns 1, stdout contains both "GSD-CORE-PATCH.md" and "ship_override".
    - test_reports_missing_with_reapply_pointer_when_file_absent: a path that does not exist --
      returns 1, stdout names the missing path.
  </behavior>
  <action>
    In sync.py, add `SHIP_MD_PATCH_MARKER = "&lt;!-- gsd-beads-patch:ship-pre-generic-dispatch v1
    --&gt;"` as a module constant near the other regex/constant definitions. Add
    `check_shipmd_patch(ship_md_path_override=None)` implementing the behavior above exactly,
    using `Path(...).exists()` and a plain substring `in` check against `Path(...).read_text(
    encoding="utf-8")` -- no regex needed for a literal marker. Add a new argparse subparser
    `check-shipmd-patch` with an optional `--ship-md-path` argument, dispatching to
    `check_shipmd_patch(args.ship_md_path)` inside `main()`, following the exact registration
    style the existing `ship-override`/`regenerate-beads-md` subparsers already use. In
    test_sync.py, add class TestCheckShipmdPatch implementing the three tests above, each writing
    a temp file (or omitting one) and calling `sync.check_shipmd_patch` directly (no subprocess --
    this is a pure Python function, unlike Task 1's live CLI smoke tests), capturing stdout via
    `contextlib.redirect_stdout(io.StringIO())` (the same capture pattern this file already uses
    elsewhere).

    In `beads-status/SKILL.md`, add "Step 2d -- ship:pre: verify the local ship.md patch (reapply
    check)" immediately after the existing Step 2c ("record an override if one occurred"), stating
    this step always runs (independent of whether Step 2c did anything): run
    `python3 .gsd/capabilities/beads/scripts/sync.py check-shipmd-patch`; if its output contains
    the "⚠" warning line, surface it to the user verbatim -- never swallow it -- but never block
    shipping on it (this is diagnostic only, matching the `onError: skip` this entire beads-status
    ship:pre dispatch already runs under). Replace the existing "## Known Gap (verified during
    Phase 3 planning)" heading and its body (added by Plan 02) with a new heading "## Patch Status
    (gap closed locally, Phase 3 plan 03-03)" whose body states: the two hardcoded capId checks
    (`security`/`broken-windows`) that made this gap true during Plan 02's planning are now joined
    by a generic `ship:pre` gate+step dispatch loop, patched locally into the installed
    `$HOME/.claude/gsd-core/workflows/ship.md` (marked with the `gsd-beads-patch:
    ship-pre-generic-dispatch v1` comment, reapply source in `GSD-CORE-PATCH.md`, detected by this
    Step 2d on every ship:pre dispatch); the underlying gap is also filed upstream as
    open-gsd/gsd-core#3554 -- once that lands natively, this local patch (and this Step
    2d/GSD-CORE-PATCH.md) should be deleted, not kept as permanent duplication.
  </action>
  <verify>
    <automated>python3 -m pytest .gsd/capabilities/beads/tests/test_sync.py -k CheckShipmdPatch -q</automated>
  </verify>
  <acceptance_criteria>
    - All three TestCheckShipmdPatch tests pass.
    - `grep -c "Step 2d -- ship:pre" .gsd/capabilities/beads/skills/beads-status/SKILL.md` returns
      1, and `grep -c "## Patch Status (gap closed locally, 03-03)" .gsd/capabilities/beads/skills/beads-status/SKILL.md`
      returns 1.
    - `grep -c "## Known Gap (verified during Phase 3 planning)" .gsd/capabilities/beads/skills/beads-status/SKILL.md`
      returns 0 -- the old heading is fully replaced, not left alongside the new one.
    - `python3 .gsd/capabilities/beads/scripts/sync.py check-shipmd-patch` run for real against
      the actual patched `$HOME/.claude/gsd-core/workflows/ship.md` (no `--ship-md-path` override)
      exits 0 and prints "present".
  </acceptance_criteria>
  <reversibility rating="reversible">
    check_shipmd_patch is a pure read-and-warn function with no side effects on ship.md itself;
    changing its marker string or message text later has no migration cost.
  </reversibility>
  <done>check-shipmd-patch correctly detects the patch's presence/absence against a real or
  fixture ship.md and is wired into beads-status's ship:pre dispatch as an always-run diagnostic;
  beads-status/SKILL.md's Known Gap section is replaced with a Patch Status section naming the
  revert condition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `$HOME/.claude/gsd-core/workflows/ship.md` (machine-global, shared across every gsd-core project on this machine) -> this repo's `ship:pre` enforcement | A single file edit changes behavior for every project using this gsd-core install, not only gsd-beads |
| Third-party capability's `check.predicate`/`check.query` JSON -> `gsd_run check predicate`/`gsd_run check` dispatch | Step 8's generic loop now dispatches ANY active `ship:pre` gate's declared check, not only beads's own -- the same trust boundary already accepted at `verify:pre`/`execute:wave:post`/`execute:post`/`plan:*` |
| `ref.agent`/`ref.skill` in an active `kind=="step"` hook -> Skill/Agent dispatch | Step 9 dispatches whatever capability manifest declared the hook; reuses `ship_post_capability_dispatch`'s existing input-validation rule verbatim rather than inventing a second one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | `$HOME/.claude/gsd-core/workflows/ship.md` (shared, global, outside this repo's git history) | high | mitigate | Costly-reversibility patch to a file this project does not own: a future gsd-core update/reinstall can silently drop it with no error. Mitigated by the patch marker + `check-shipmd-patch`'s always-run reapply warning (Task 2), `GSD-CORE-PATCH.md` as the durable, version-controlled reapply source, and the upstream fix already filed (open-gsd/gsd-core#3554) with an explicit revert condition once it lands. |
| T-03-09 | Elevation of Privilege / Tampering | Step 8 now dispatches ANY capability's `ship:pre` gate predicate, not only beads's | low | accept | Identical trust boundary already accepted at `verify:pre`/`execute:wave:post`/`execute:post`/`plan:*` -- same `gsd_run check predicate` CLI, same capability-manifest provenance. This plan extends an already-accepted mechanism to one more lifecycle point; it does not create a new one. |
| T-03-10 | Denial of Service / False Assurance | A missing or stale patch silently returns `ship.md` to hardcoded-only dispatch -- an operator could believe `blocking_open`/`diverged` are enforced when they are not | medium | mitigate | `check-shipmd-patch` runs on every `ship:pre` dispatch when beads is enabled (Step 2d), not only at capability install time -- catches staleness the very next ship attempt, not silently forever. |
| T-03-11 | Tampering | Step 9 dispatches `ref.agent`/`ref.skill` from an active step hook | low | accept | Reuses `ship_post_capability_dispatch`'s existing `^[A-Za-z0-9][A-Za-z0-9._-]*$` `ref.agent` validation verbatim -- no new validation logic invented, same mitigated boundary as ship:post already has. |

No new package-manager installs in this plan (stdlib `pathlib`/`os` + the existing `node`/`gsd_run`
CLI + `git` + `bd` only, N5) -- the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
Run the full suite: `python3 -m pytest .gsd/capabilities/beads/tests/test_sync.py -q` (the
`bd`-binary-required and `gsd_run`-required live tests each skip independently if their tool is
absent, unchanged from the Phase 1/2/Plan-01/02 precedent). Confirm the total pass count is at
least the pre-Plan-03 baseline plus TestShipPreGenericDispatch's three unconditional tests (the
fourth self-skips pre-Plan-02) and TestCheckShipmdPatch's three tests. Separately, run
`python3 .gsd/capabilities/beads/scripts/sync.py check-shipmd-patch` for real against the live
patched `ship.md` and confirm it exits 0.
</verification>

<success_criteria>
A real `/gsd-ship` run on a phase with an open blocking beads issue now actually blocks and names
it (via `ship.md`'s new step 8, reading Plan 01's real `blocking_open`); `beads.ship_gate=false`
actually allows the ship and actually records the override (via step 8's gate exclusion plus step
9's dispatch of Plan 02's `ship_override`); the patch is self-detecting -- a future gsd-core
update that drops it is caught and reported on the very next ship attempt, not discovered by
surprise.
</success_criteria>

<output>
Create `.planning/phases/03-enforcement/03-03-SUMMARY.md` when done
</output>
