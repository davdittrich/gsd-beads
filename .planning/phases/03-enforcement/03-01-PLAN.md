---
phase: 03-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gsd/capabilities/beads/scripts/sync.py
  - .gsd/capabilities/beads/tests/test_sync.py
  - .gsd/capabilities/beads/capability.json
  - .gsd/capabilities/beads/skills/beads-status/SKILL.md
autonomous: true
requirements: [B9, B10]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "BEADS.md's blocking_open frontmatter field equals the live count of non-closed issues under the phase's shared epic (bd list --parent <epic> --all), with no priority/type filtering (D-01/D-02)."
    - "BEADS.md's diverged frontmatter field equals the count of synced issues (linked via <beads-id> to a task) whose bd status and linked task's completion state disagree in either direction (D-04)."
    - "BEADS.md's issue table names both sides of every considered row via a Task Status column (done/incomplete) alongside the existing bd Status column, so a diverged row is readable without cross-referencing PLAN.md/SUMMARY.md (D-06)."
    - "BEADS.md is regenerated -- and blocking_open/diverged recomputed -- at verify:post in addition to the existing execute:wave:pre/execute:wave:post points (D-03)."
  artifacts:
    - .gsd/capabilities/beads/scripts/sync.py
    - .gsd/capabilities/beads/tests/test_sync.py
    - .gsd/capabilities/beads/capability.json
    - .gsd/capabilities/beads/skills/beads-status/SKILL.md
  key_links:
    - "regenerate_beads_md -> _resolve_completed_task_ids / _compute_diverged -> BEADS.md frontmatter (blocking_open, diverged) and table (Task Status column)"
    - "capability.json steps[] verify:post entry -> beads-status skill -> sync.py regenerate-beads-md CLI subcommand (already exists, unchanged call shape)"
  prohibitions:
    - "Never auto-reconcile a diverged issue (close/reopen it in bd, or edit PLAN.md's task) from this computation -- divergence is reported only, resolved by the operator (B10)."
    - "This plan's regeneration code is the sole live bd query point for blocking_open/diverged -- no gate's check.predicate may ever call bd directly (locked PROJECT.md decision, enforced by Plan 02's gates)."
    - "Never fork or patch gsd-core's own workflow files to change how BEADS.md is consumed -- extend only files under .gsd/capabilities/beads/ (N2)."
  flagged_assumptions:
    - "B9/empty: What is the result for empty, single-element, or null input? -- blocking_open on an epic with zero synced issues is assumed to be 0 (exercised by a dedicated empty-epic test in Task 2), not independently verified against a written spec."
    - "B9/encoding: Whose definition of length/equality applies -- bytes, code points, grapheme clusters, or normalized form? -- blocking_open is an integer issue-count, not a string comparison; encoding/normalization semantics are assumed not applicable to this requirement's domain, not independently verified."
    - "B10/adjacency: When two things are exactly equal or just touch, do they merge, collide, or separate? -- divergence is a per-issue boolean disagreement between two independent status signals, not an interval/adjacency merge; each issue is assumed evaluated independently, never merged with a neighboring issue, not independently verified."
    - "B10/empty: What is the result for empty, single-element, or null input? -- diverged on a phase with zero synced issues is assumed to be 0 (exercised by a dedicated empty-epic test in Task 2); a task with no <beads-id> is assumed excluded from both the completed-id set and the divergence count (mirrors find_completed_task_ids' existing skip semantics), not independently verified against a written spec."
    - "B10/ordering: When elements compare equal, is output order specified and stable? -- BEADS.md's table row order is assumed to follow bd list's own JSON response order (no independent sort/stability guarantee); diverged rows are not grouped or resorted, not independently verified against a bd stability guarantee."
---

<objective>
Make BEADS.md's `blocking_open` and `diverged` frontmatter fields real, live-computed values
(replacing Phase 2's `0` placeholders), extend its issue table with a `Task Status` column so a
diverged row is readable without cross-referencing another file, and wire this same regeneration
into `verify:post` so the projection is fresh going into ship (D-01, D-02, D-03, D-04, D-06).

Purpose: this is the artifact-projection half of Enforcement -- Plan 02 (wave 2, depends on this
plan) declares the `ship:pre` gates and the override-audit trail that read the fields this plan
computes. Neither gate means anything until this plan makes `blocking_open`/`diverged` real.
Output: `regenerate_beads_md` computes and writes real values; the beads-status skill also fires
at `verify:post`; new/updated test coverage proves both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-enforcement/03-CONTEXT.md
@.planning/phases/03-enforcement/03-PATTERNS.md

@.gsd/capabilities/beads/scripts/sync.py
@.gsd/capabilities/beads/tests/test_sync.py
@.gsd/capabilities/beads/capability.json
@.gsd/capabilities/beads/skills/beads-status/SKILL.md
</context>

## Artifacts this phase produces (this plan)

- `_resolve_completed_task_ids(phase_dir)` -- new function, `sync.py`
- `_compute_diverged(rows, ordinal_map, completed_ids)` -- new function, `sync.py`
- `_render_beads_md_table(rows, ordinal_map, task_status_by_id)` -- extended signature (was
  `(rows, ordinal_map)`), `sync.py`
- `blocking_open` / `diverged` BEADS.md frontmatter fields -- now real computed integers, not the
  literal `0` Phase 2 shipped as a placeholder
- BEADS.md table's new `Task Status` column (6th column, inserted after `Status`)
- `capability.json` `steps[]` entry: `{"point": "verify:post", "ref": {"skill": "beads-status"}}`
- `beads-status/SKILL.md` "Step 2b -- verify:post" section
- `test_sync.py`: `TestBlockingOpen`, `TestDivergence` classes

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer -- compute blocking_open + diverged end-to-end through regenerate_beads_md</name>
  <files>.gsd/capabilities/beads/scripts/sync.py, .gsd/capabilities/beads/tests/test_sync.py</files>
  <read_first>
    .gsd/capabilities/beads/scripts/sync.py (regenerate_beads_md ~810-874, _render_beads_md_table
    ~786-807, _resolve_task_ordinal_map ~771-783, find_completed_task_ids ~362-385,
    discover_plan_files ~145-157)
    .gsd/capabilities/beads/tests/test_sync.py (TestBeadsMdRegeneration ~1332-1415,
    _regen_two_task_plan_text ~1250-1301, _write_wave_workspace ~768-789,
    _make_beads_md_bd_side_effect ~1304-1316, _find_table_row ~1319-1329)
    .planning/phases/03-enforcement/03-CONTEXT.md (D-01, D-02, D-04, D-06)
  </read_first>
  <behavior>
    - _resolve_completed_task_ids(phase_dir) returns the union of every &lt;beads-id&gt; across
      every plan in phase_dir whose SUMMARY.md exists; an empty phase_dir (no plans) returns an
      empty set.
    - _compute_diverged(rows, ordinal_map, completed_ids) returns (diverged_count,
      task_status_by_id): a row whose id is a key in ordinal_map is diverged when
      (row.status == "closed") != (issue_id in completed_ids); a row absent from ordinal_map is
      skipped entirely (not counted, not present in task_status_by_id); every considered
      issue_id maps to the literal string "done" or "incomplete" in task_status_by_id.
    - regenerate_beads_md's blocking_open frontmatter value equals open_count (len(rows) minus
      closed_count) with no filtering -- D-01/D-02: every open issue under the epic counts, full
      stop, no priority/type filter.
    - regenerate_beads_md's diverged frontmatter value equals _compute_diverged's diverged_count.
    - A zero-row rows list (epic exists, zero children) yields blocking_open=0 and diverged=0.
  </behavior>
  <action>
    In sync.py, add _resolve_completed_task_ids(phase_dir): iterate discover_plan_files(phase_dir)
    keys as plan_id, call the existing find_completed_task_ids(phase_dir, plan_id) for each, union
    every returned id into one set, return it. Add _compute_diverged(rows, ordinal_map,
    completed_ids): for each row in rows whose str(row.get("id","")) is a key in ordinal_map,
    compare row.get("status") == "closed" against issue_id in completed_ids; record
    task_status_by_id[issue_id] as "done" if issue_id in completed_ids else "incomplete"
    (regardless of agreement); on disagreement increment a counter; return (count,
    task_status_by_id). In regenerate_beads_md, immediately after the existing
    `ordinal_map = _resolve_task_ordinal_map(phase_dir)` line, add
    `completed_ids = _resolve_completed_task_ids(phase_dir)` and
    `diverged_count, task_status_by_id = _compute_diverged(rows, ordinal_map, completed_ids)`.
    Change the `table = _render_beads_md_table(rows, ordinal_map)` call to also pass
    task_status_by_id as a third positional argument. In _render_beads_md_table(rows, ordinal_map,
    task_status_by_id), extend the header/separator from the current 5 columns to 6: insert
    "Task Status" immediately after "Status" and before "Plan Task" -- header becomes
    "| Issue | Title | Status | Task Status | Plan Task | Blocked By |"; each data row inserts
    task_status_by_id.get(issue_id, "") in that same position, keeping Blocked By as the last cell
    unchanged. In regenerate_beads_md's frontmatter block, replace the literal "blocking_open: 0\n"
    with an f-string using open_count (blocking_open IS open_count per D-01/D-02, no separate
    filtered variable -- name it explicitly `blocking_open = open_count` with a one-line comment
    citing D-01/D-02 for anyone reading it later) and replace "diverged: 0\n" with an f-string
    using diverged_count. Delete the body placeholder line
    "blocking_open/diverged: not yet computed, Phase 3\n\n" entirely -- the body becomes the H1
    title line followed directly by the table. In test_sync.py's existing
    test_frontmatter_matches_mocked_bd_response_counts (TestBeadsMdRegeneration), update the
    assertions for the new real values: with the existing fixture (regen-epic.1 status=open,
    regen-epic.2 status=closed, both tasks in a plan whose SUMMARY.md exists so both are
    task-done), blocking_open is now 1 (one open row) and diverged is now 1 (regen-epic.1 is
    open-but-task-done, a real disagreement; regen-epic.2 is closed-and-task-done, consistent).
    Change `self.assertIn("blocking_open: 0", text)` to assert "blocking_open: 1", change the
    diverged assertion to "diverged: 1", and delete the
    `self.assertIn("not yet computed, Phase 3", text)` line entirely (the placeholder no longer
    exists).
  </action>
  <verify>
    <automated>python3 -m pytest .gsd/capabilities/beads/tests/test_sync.py -k "BeadsMdRegeneration or WaveStatusBlock" -q</automated>
  </verify>
  <acceptance_criteria>
    - TestBeadsMdRegeneration.test_frontmatter_matches_mocked_bd_response_counts passes with
      "blocking_open: 1" and "diverged: 1" asserted, and no assertion references
      "not yet computed, Phase 3".
    - TestBeadsMdRegeneration.test_blocked_by_column_excludes_parent_child_includes_blocks still
      passes unmodified -- row[-1] still resolves to the Blocked By cell after the new Task
      Status column insertion.
    - TestWaveStatusBlock's two existing tests still pass unmodified.
  </acceptance_criteria>
  <reversibility rating="costly">
    D-01/D-02 fix blocking_open's scope to the entire epic with no priority/type filtering, and
    D-04 fixes diverged's per-issue predicate; Plan 02's gates[] read these two field names and
    semantics directly, so narrowing either definition after Plan 02 ships means redefining what
    an already-declared ship:pre gate silently checks, with no migration path for BEADS.md history
    already committed under the old meaning.
  </reversibility>
  <done>regenerate_beads_md computes and writes real blocking_open/diverged values from live bd
  data through a 6-column table, with the Phase-2 placeholder line removed and the existing
  regression test updated to match the new real values.</done>
</task>

<task type="auto">
  <name>Task 2: Wire BEADS.md regeneration into verify:post (D-03); dedicated blocking_open/diverged test coverage</name>
  <files>.gsd/capabilities/beads/capability.json, .gsd/capabilities/beads/skills/beads-status/SKILL.md, .gsd/capabilities/beads/tests/test_sync.py</files>
  <read_first>
    .gsd/capabilities/beads/capability.json (steps[] array, full file)
    .gsd/capabilities/beads/skills/beads-status/SKILL.md (Step 1.5 branch pattern, full file)
    .planning/phases/03-enforcement/03-CONTEXT.md (D-03)
  </read_first>
  <action>
    In capability.json's steps[] array, add a fourth entry after the execute:wave:post entry and
    before contributions[]: {"point": "verify:post", "ref": {"skill": "beads-status"},
    "produces": ["BEADS.md"], "consumes": ["UAT.md"], "when": "beads.enabled",
    "onError": "skip"} -- matches the PRD's own target manifest for this point exactly. In
    beads-status/SKILL.md's "Step 1.5 -- Lifecycle-point branch (D-11)" section, name the third
    point ("verify:post") alongside the existing two ("execute:wave:pre", "execute:wave:post"),
    and add a new subsection "Step 2b -- verify:post: Regenerate BEADS.md only" directly after
    the existing "Step 2a" section: at verify:post (after UAT records zero issues, before the
    phase-completion predicate), run
    `python3 .gsd/capabilities/beads/scripts/sync.py regenerate-beads-md <phase directory>` --
    the identical read-only call Step 2a already uses, but with no wave-status-block and no
    plan-id list (there is no wave/plan-id context at this lifecycle point) -- then stop; do not
    call close-wave from this branch. Extend the existing "Anti-Patterns" list with one new item:
    do not collapse the verify:post branch into either of the two execute:wave branches --
    verify:post fires once per phase (not once per wave) and never dispatches close-wave. In
    test_sync.py, add a new TestBlockingOpen class (a zero-row epic via
    _make_beads_md_bd_side_effect("[]\n") asserts "blocking_open: 0"; a 3-row fixture with 2 open
    rows + 1 closed row asserts "blocking_open: 2") and a new TestDivergence class (reuse
    _regen_two_task_plan_text/_write_wave_workspace with has_summary=False and a rows fixture
    where regen-epic.1 is "closed" -- asserts "diverged: 1" and the table's regen-epic.1 row shows
    Task Status "incomplete"; a separate zero-row epic case asserts "diverged: 0").
  </action>
  <verify>
    <automated>python3 -m pytest .gsd/capabilities/beads/tests/test_sync.py -k "BlockingOpen or Divergence" -q && python3 -c "import json; d=json.load(open('.gsd/capabilities/beads/capability.json')); steps=[s for s in d['steps'] if s['point']=='verify:post']; assert len(steps)==1 and steps[0]['ref']['skill']=='beads-status' and steps[0]['when']=='beads.enabled' and steps[0]['onError']=='skip', steps"</automated>
  </verify>
  <acceptance_criteria>
    - capability.json's steps[] contains exactly one entry with point == "verify:post",
      ref.skill == "beads-status", when == "beads.enabled", onError == "skip".
    - TestBlockingOpen and TestDivergence each pass with at least one zero-issue case and at
      least one nonzero case.
    - beads-status/SKILL.md contains a "Step 2b -- verify:post" heading and names all three
      lifecycle points beads-status now branches on.
  </acceptance_criteria>
  <done>verify:post regenerates BEADS.md via the same CLI subcommand execute:wave:pre already
  uses, and blocking_open/diverged have dedicated, independently-readable test coverage beyond
  the single regression fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bd JSON response -> BEADS.md table | issue title/status text originates from whoever filed the bd issue, a different principal than this script |
| PLAN.md/SUMMARY.md presence -> completed_ids | SUMMARY.md existence is treated as a trusted local filesystem fact, not adversarial input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `_render_beads_md_table`'s new Task Status column | low | accept | Column value is one of two internal literals ("done"/"incomplete"), never issue-sourced text -- no new escaping surface beyond the existing `_escape_table_cell` already applied to bd-sourced cells. |
| T-03-02 | Information Disclosure | `diverged` count + Task Status column exposing bd/PLAN.md mismatches in a committed markdown file | low | accept | This is BEADS.md's intended purpose (D-06); the same information is already derivable by anyone with `bd` access. |
| T-03-03 | Denial of Service | `_compute_diverged`/`_resolve_completed_task_ids` iterating every plan file in phase_dir on every regeneration (now 3x per phase: execute:wave:pre/post, verify:post) | low | accept | Bounded by the phase's own plan count, never attacker-controlled; same complexity class as the existing `_resolve_task_ordinal_map`. |
</threat_model>

<verification>
Run the full suite: `python3 -m pytest .gsd/capabilities/beads/tests/test_sync.py -q` (the one
`bd`-binary-required tracer test skips if `bd` is absent, unchanged from Phase 1/2 behavior).
Confirm no regression: total pass count is at least the pre-Phase-3 baseline plus the new tests
this plan adds (TestBlockingOpen, TestDivergence), and every existing test in
TestBeadsMdRegeneration/TestWaveStatusBlock still passes.
</verification>

<success_criteria>
BEADS.md's frontmatter reflects real blocking_open/diverged counts at every regeneration point
(execute:wave:pre, execute:wave:post, verify:post), and its table names both sides of a
considered row via the Task Status column.
</success_criteria>

<output>
Create `.planning/phases/03-enforcement/03-01-SUMMARY.md` when done
</output>
