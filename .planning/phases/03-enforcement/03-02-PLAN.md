---
phase: 03-enforcement
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .gsd/capabilities/beads/scripts/sync.py
  - .gsd/capabilities/beads/tests/test_sync.py
  - .gsd/capabilities/beads/capability.json
  - .gsd/capabilities/beads/skills/beads-status/SKILL.md
autonomous: true
requirements: [B9, B10]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "capability.json declares two ship:pre gates via the locked artifact-frontmatter-equals predicate: BEADS.md's blocking_open field equals 0, and BEADS.md's diverged field equals 0 -- both blocking: true, onError: skip, active only when beads.ship_gate is true (D-01..D-06's canonical gate shape, PRD Section 5.3)."
    - "capability.json declares the beads.ship_gate config key (boolean, default true)."
    - "Invoking `sync.py ship-override <phase_dir>` against a phase whose BEADS.md shows a nonzero blocking_open or diverged writes a `Beads-Override: ship_gate bypassed, blocking_open=N, diverged=N` git trailer onto the current HEAD commit (verifiable via `git log -1 --format=%(trailers)`), and best-effort posts a matching `bd comment` on the phase epic that fails open (never raises, never blocks) when bd is unavailable (D-05)."
    - "ship_override reads its blocking_open/diverged/epic values from BEADS.md's own generated frontmatter only -- it never issues a fresh live bd query to decide what to record."
  artifacts:
    - .gsd/capabilities/beads/scripts/sync.py
    - .gsd/capabilities/beads/tests/test_sync.py
    - .gsd/capabilities/beads/capability.json
    - .gsd/capabilities/beads/skills/beads-status/SKILL.md
  key_links:
    - "capability.json gates[] -> BEADS.md frontmatter fields blocking_open/diverged (the artifact-frontmatter-equals predicate's read target, computed by Plan 01)"
    - "ship_override -> BEADS.md frontmatter (read) + git HEAD (write, load-bearing) + bd epic comment (write, best-effort/fail-open)"
  prohibitions:
    - "Never call bd live from within a gate's check.predicate -- both new gates read only BEADS.md frontmatter (locked PROJECT.md decision)."
    - "Never let the bd comment write block or fail ship_override's own exit status -- it degrades exactly like every other bd write in this codebase (B6): print a skip notice, continue; only the git trailer half determines ship_override's exit code."
    - "Never patch gsd-core's ship.md (or any other first-party workflow file) to force ship:pre dispatch of this capability's gate/step -- raise it upstream instead (N2); this plan documents the gap, it does not work around it locally."
---

<objective>
Declare the two `ship:pre` gates and the `beads.ship_gate` config key exactly per PRD Section 5.3
(reading Plan 01's now-real `blocking_open`/`diverged` fields), and build `ship_override` -- the
D-05 override-audit primitive that writes a durable git trailer plus a best-effort `bd comment`
when `beads.ship_gate=false` bypasses what would otherwise have blocked.

Purpose: complete the Enforcement phase's declared artifact contract (B9/B10) within what an
overlay-only capability can actually build. Depends on Plan 01 (wave 1) for real
`blocking_open`/`diverged` values -- these gates are meaningless against the Phase-2-era `0`
placeholders.
Output: `capability.json`'s `gates[]`/`config`/`steps[]` extended; `sync.py` gains
`ship_override`/`_read_beads_md_frontmatter` plus a `ship-override` CLI subcommand; the
beads-status skill documents dispatch of the override check.
</objective>

## Known Limitation (verified during Phase 3 planning) -- closed by plan 03-03

gsd-core's currently-installed `/gsd-ship` workflow's `preflight_checks` step only dispatched
`ship:pre` gates for two hardcoded capability ids (`security`, `broken-windows`), so this plan's
`gates[]`/`ship:pre` `steps[]` declarations were correct and schema-valid but inert on a real
`/gsd-ship` run. That gap is closed by `03-03-PLAN.md` (a machine-local, user-authorized patch to
the installed `ship.md`) -- see `03-03-PLAN.md` for the fix, the live-behavior proof, and the
upstream tracking issue (open-gsd/gsd-core#3554).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-enforcement/03-CONTEXT.md
@.planning/phases/03-enforcement/03-PATTERNS.md
@.planning/phases/03-enforcement/03-01-SUMMARY.md

@.gsd/capabilities/beads/scripts/sync.py
@.gsd/capabilities/beads/tests/test_sync.py
@.gsd/capabilities/beads/capability.json
@.gsd/capabilities/beads/skills/beads-status/SKILL.md
</context>

## Artifacts this phase produces (this plan)

- `_read_beads_md_frontmatter(phase_dir)` -- new function, `sync.py`
- `ship_override(phase_dir_arg)` -- new function, `sync.py`
- `ship-override <phase_dir>` -- new CLI subcommand, `sync.py`
- `capability.json` `gates[]`: two `ship:pre` entries (`blocking_open == 0`, `diverged == 0`)
- `capability.json` `config`: `beads.ship_gate` key (boolean, default `true`)
- `capability.json` `steps[]` entry: `{"point": "ship:pre", "ref": {"skill": "beads-status"}}`
- `beads-status/SKILL.md` "Step 2c -- ship:pre" section and "## Known Gap" callout
- `test_sync.py`: `TestShipOverride` class
- Git commit trailer key: `Beads-Override`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ship_override -- BEADS.md-sourced git trailer + best-effort bd comment</name>
  <files>.gsd/capabilities/beads/scripts/sync.py, .gsd/capabilities/beads/tests/test_sync.py</files>
  <read_first>
    .gsd/capabilities/beads/scripts/sync.py (FRONTMATTER_RE line 28, run_bd lines 34-37,
    bd_available lines 40-50, find_project_root lines 71-84, close_wave's fail-open shape lines
    449-453, main() CLI wiring lines 935-976)
    .planning/phases/03-enforcement/03-CONTEXT.md (D-05, Claude's Discretion)
    .planning/phases/03-enforcement/03-PATTERNS.md ("new override-audit function" section)
  </read_first>
  <precondition>
    project_root (the .planning/ ancestor) is a git repository with at least one existing commit
    -- `git commit --amend` requires a HEAD to amend, not create; true for any phase reaching
    ship, since execute-phase already committed wave work before ship runs.
  </precondition>
  <behavior>
    - _read_beads_md_frontmatter(phase_dir) returns {} when {padded_phase}-BEADS.md does not
      exist; otherwise returns a dict of every top-level "key: value" line inside the frontmatter
      block (reusing FRONTMATTER_RE), values as raw strings.
    - ship_override(phase_dir_arg) returns 1 and makes zero subprocess calls when BEADS.md is
      absent, or is missing any of "epic"/"blocking_open"/"diverged".
    - ship_override always attempts the git trailer write first -- argv
      ["git", "commit", "--amend", "--allow-empty", "--no-edit", "--trailer",
      f"Beads-Override: ship_gate bypassed, blocking_open={blocking_open}, diverged={diverged}"],
      cwd=project_root -- regardless of bd's availability.
    - On git amend success (returncode 0): prints the recorded trailer line; on failure
      (nonzero): prints a failure message naming stderr, and ship_override's own return value is 1.
    - The bd comment (run_bd(["bd", "comment", epic_id, comment_text])) is attempted
      independently of the git result -- git failing does not skip the bd comment attempt, and
      bd failing/unavailable does not change the git half's outcome; when bd_available() is
      False, print the B6-style skip notice and never call run_bd.
  </behavior>
  <action>
    Add BEADS_MD_FIELD_RE = re.compile(r"^(\w+):\s*(.*)$", re.MULTILINE) near the other
    module-level regexes. Add _read_beads_md_frontmatter(phase_dir): resolve
    {padded_phase}-BEADS.md the same way regenerate_beads_md does, return {} if the file is
    absent, else match FRONTMATTER_RE against its text and return
    {m.group(1): m.group(2).strip() for m in BEADS_MD_FIELD_RE.finditer(fm_match.group(1))}. Add
    ship_override(phase_dir_arg): resolve phase_dir = Path(phase_dir_arg).resolve();
    fields = _read_beads_md_frontmatter(phase_dir); if "epic" not in fields or
    "blocking_open" not in fields or "diverged" not in fields, print
    "ship-override: BEADS.md missing or incomplete -- nothing recorded" and return 1. Otherwise
    resolve project_root via find_project_root(phase_dir) (catch ValueError the same way other
    functions do -- print a notice and return 1 on failure). Build
    trailer = f"Beads-Override: ship_gate bypassed, blocking_open={fields['blocking_open']}, diverged={fields['diverged']}";
    run subprocess.run(["git", "commit", "--amend", "--allow-empty", "--no-edit", "--trailer",
    trailer], cwd=str(project_root), capture_output=True, text=True, timeout=BD_TIMEOUT) directly
    (not via run_bd, which is bd-specific) -- print success/failure per the behavior spec above,
    track git_ok. Then, independently: if bd_available(), build
    comment_text = f"ship_gate override: blocking_open={fields['blocking_open']}, diverged={fields['diverged']}"
    and call run_bd(["bd", "comment", fields["epic"], comment_text]); print a failure notice on
    nonzero returncode, never raise. Else print
    "ship-override: bd unavailable -- comment mirror skipped (B6)". Return 0 if git_ok else 1.
    Add a new argparse subparser "ship-override" taking a single "phase_dir" positional argument,
    dispatch to ship_override(args.phase_dir) inside main(). In test_sync.py, add a new
    TestShipOverride class with a subprocess.run mock whose side_effect dispatches on argv[0]
    ("git" vs "bd"): (a) a full BEADS.md fixture (epic/blocking_open=2/diverged=1) asserts the
    git call's argv contains exactly
    "Beads-Override: ship_gate bypassed, blocking_open=2, diverged=1" and the bd comment call
    names that same epic id, exit code 0; (b) the git call returns nonzero -- asserts exit code 1
    and that the bd comment call still happens; (c) the bd_available() probe fails (mock the
    "bd list --json -n 1" call nonzero) -- asserts the git call still happens (exit 0) and no
    "bd comment" call is made; (d) no BEADS.md file present -- asserts zero subprocess calls and
    exit code 1.
  </action>
  <verify>
    <automated>python3 -m pytest .gsd/capabilities/beads/tests/test_sync.py -k ShipOverride -q</automated>
  </verify>
  <acceptance_criteria>
    - TestShipOverride covers all four cases (full success, git failure, bd unavailable, missing
      BEADS.md) and all pass.
    - The trailer string asserted in case (a) is exactly
      "Beads-Override: ship_gate bypassed, blocking_open=2, diverged=1" for the fixture's values.
    - The argv used for the git call is `["git", "commit", "--amend", "--allow-empty",
      "--no-edit", "--trailer", <trailer>]` -- this exact form was smoke-tested during planning
      against a real temporary git repository and confirmed to produce a trailer that
      `git log -1 --format=%(trailers)` parses correctly.
  </acceptance_criteria>
  <reversibility rating="reversible">
    The exact trailer key name/format is Claude's Discretion per CONTEXT.md; changing it later
    only affects how future overrides are grepped, with no migration cost for already-written
    history.
  </reversibility>
  <done>ship_override reliably records the override via a real git trailer (load-bearing) plus a
  best-effort bd comment (fail-open), independent of bd's availability, sourced only from
  BEADS.md's own generated frontmatter.</done>
</task>

<task type="auto">
  <name>Task 2: Declare ship:pre gates, beads.ship_gate config, ship:pre step; document the verified ship.md dispatch gap</name>
  <files>.gsd/capabilities/beads/capability.json, .gsd/capabilities/beads/skills/beads-status/SKILL.md</files>
  <read_first>
    .gsd/capabilities/beads/capability.json (full file)
    .gsd/capabilities/beads/skills/beads-status/SKILL.md (full file)
    $HOME/.claude/gsd-core/workflows/ship.md (preflight_checks step, lines ~94-156 -- the two
    hardcoded capId=="security"/"broken-windows" gate checks; confirms no generic ship:pre
    dispatch exists for other capability ids in the installed version)
    .planning/phases/03-enforcement/03-CONTEXT.md (D-05, canonical_refs)
  </read_first>
  <action>
    In capability.json, replace the empty "gates": [] with two entries, both shaped
    {"point": "ship:pre", "check": {"predicate": {"kind": "artifact-frontmatter-equals",
    "artifact": "BEADS.md", "field": &lt;field&gt;, "equals": 0}}, "when": "beads.ship_gate",
    "blocking": true, "onError": "skip"} -- one with field "blocking_open", one with field
    "diverged" -- matching PRD Section 5.3's canonical shape exactly. Add
    "beads.ship_gate": {"type": "boolean", "default": true, "description": "When true, ship:pre
    blocks on blocking_open>0 or diverged>0 in BEADS.md."} to the existing config object,
    alongside beads.enabled/beads.sync_mode. Add a fifth steps[] entry:
    {"point": "ship:pre", "ref": {"skill": "beads-status"}, "produces": [],
    "consumes": ["BEADS.md"], "when": "beads.enabled", "onError": "skip"}. In
    beads-status/SKILL.md, extend the Step 1.5 branch list to name a fourth point ("ship:pre")
    and add "Step 2c -- ship:pre: record an override if one occurred" directly after Step 2b:
    read {padded_phase}-BEADS.md's blocking_open/diverged frontmatter fields and
    .planning/config.json's beads.ship_gate value; when beads.ship_gate is false AND
    (blocking_open &gt; 0 OR diverged &gt; 0), run
    `python3 .gsd/capabilities/beads/scripts/sync.py ship-override <phase directory>` and report
    its printed summary; otherwise this branch is a no-op (print nothing). Immediately below Step
    2c, add a "## Known Gap (verified during Phase 3 planning)" heading stating, in substance:
    the installed gsd-core `/gsd-ship` workflow's preflight_checks step only special-cases
    capId == "security" and capId == "broken-windows" at ship:pre (confirmed by reading
    $HOME/.claude/gsd-core/workflows/ship.md directly) -- there is no generic ship:pre
    hook-dispatch loop for other capability ids in that file. This capability's ship:pre
    gates[]/steps[] entries are therefore declarative and forward-compatible, not yet
    live-enforced by /gsd-ship; making them live requires either an upstream gsd-core change
    (generic ship:pre dispatch, or a beads-specific block analogous to security/broken-windows)
    or a local patch to ship.md, and the latter is out of scope per this project's N2 constraint
    (overlay-only, no gsd-core fork/patch) -- raise the gap upstream instead of working around it
    here. Extend the Anti-Patterns list with one item: do not assume this SKILL.md registration
    alone makes /gsd-ship block -- verify against the installed ship.md before relying on it.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('.gsd/capabilities/beads/capability.json')); g=d['gates']; assert len(g)==2, g; fields={x['check']['predicate']['field'] for x in g}; assert fields=={'blocking_open','diverged'}, fields; assert all(x['point']=='ship:pre' and x['blocking'] is True and x['onError']=='skip' and x['when']=='beads.ship_gate' for x in g), g; assert 'beads.ship_gate' in d['config'] and d['config']['beads.ship_gate']['default'] is True; assert any(s['point']=='ship:pre' and s['ref']['skill']=='beads-status' for s in d['steps'])"</automated>
  </verify>
  <acceptance_criteria>
    - The python3 verify command above exits 0 against the modified capability.json.
    - beads-status/SKILL.md contains a "Step 2c -- ship:pre" heading and a "## Known Gap"
      heading whose text names both capId=="security" and capId=="broken-windows" as the only
      two currently-dispatched ship:pre capabilities.
    - `python3 -m json.tool .gsd/capabilities/beads/capability.json` exits 0 (valid JSON).
  </acceptance_criteria>
  <done>capability.json declares the two ship:pre gates and the beads.ship_gate config key
  exactly per PRD Section 5.3, and the beads-status skill documents the override-audit dispatch
  plus the verified, current non-enforcement gap in gsd-core's installed ship.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BEADS.md frontmatter -> ship_override's trailer/comment values | BEADS.md is this capability's own generated artifact (Plan 01), not third-party text, but its blocking_open/diverged values still originate from a live bd query one step removed |
| beads.ship_gate config -> gate activation | `.planning/config.json` is operator-controlled, trusted local configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | `git commit --amend --trailer` argv | low | mitigate | Trailer value is built only from internally-computed integers parsed out of BEADS.md's own generated frontmatter plus a fixed string template -- no PLAN.md/issue-title/free-text ever enters the trailer string; argv-list `subprocess.run` call (never a shell string), matching T-01-01. |
| T-03-05 | Tampering | Amending HEAD immediately before push (ship:pre fires before push_branch in ship.md) | low | accept | Rewrites the tip commit sha of a not-yet-pushed branch; safe under ship.md's existing "clean working tree" preflight check. Single-operator local workflow; a concurrent push race is theoretical, not observed. |
| T-03-06 | Repudiation | Best-effort `bd comment` silently failing (bd absent/locked at ship time) | low | accept | Mitigated by design: the commit trailer is the load-bearing, always-attempted record (D-05); the bd comment is a convenience mirror only, fail-open per the established B6 convention. |
| T-03-07 | Denial of Service / False Assurance | Declared `ship:pre` `gates[]`/`steps[]` entries are not dispatched by the currently-installed gsd-core `ship.md` (verified: only `capId == "security"`/`"broken-windows"` are special-cased) -- an operator could believe the gate is enforced when it is not | medium | mitigate | Explicit, prominent documentation in SKILL.md's "## Known Gap" section, this plan's objective, and must_haves -- cannot code-fix without patching gsd-core (out of scope, N2); tracked for upstream follow-up. |

No new package-manager installs in this plan (stdlib `subprocess`/`re` + `git` + `bd` only, N5) --
the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
Run `python3 -m pytest .gsd/capabilities/beads/tests/test_sync.py -q` -- full suite, including
Plan 01's and this plan's new classes, all green (the one `bd`-binary-required tracer test skips
if `bd` is absent, unchanged from Phase 1/2). Run the two acceptance-criteria python3 one-liners
from Task 1 and Task 2 directly against the real repo files (not just inside the test suite) to
confirm capability.json and the trailer format are correct outside of mocks.
</verification>

<success_criteria>
`ship_override` is a correct, independently-testable primitive that records an override via a
real git trailer plus a best-effort bd comment; `capability.json` declares the exact PRD
Section 5.3 gate/config shape; the gap between "declared" and "live-enforced by /gsd-ship" is
explicit and documented, not silently assumed away.
</success_criteria>

<output>
Create `.planning/phases/03-enforcement/03-02-SUMMARY.md` when done
</output>