---
phase: 18-address-tech-debt-patch-check-doc-accuracy-changelog
plan: 04
type: execute
wave: 3
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - CHANGELOG.md
  - plugins/beads-lifecycle/.claude-plugin/plugin.json
autonomous: true
requirements: []
# Phase 18 maps to no REQUIREMENTS.md requirement ID (`phase_req_ids: null`). Scope traces to
# 17-REVIEW.md WR-03, ROADMAP Phase 17 "Ship-step checks" #1 and #2, and CONTEXT.md D-04/D-05/D-06.

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-04 — the 0.4.0 section documents TRUTH-03: awk-scoping CHANGELOG.md from the `## 0.4.0` heading to the `## 0.3.1` heading and grepping that region for `check_native_step_dispatch` returns >= 1 match. It returns 0 today — the 0.4.0 section documents TRUTH-04, TRUTH-01 and TRUTH-02 but omits TRUTH-03 entirely, which is exactly WR-03's finding."
    - "D-04 — both module constants are named in that entry: the same awk-scoped region contains `NATIVE_STEP_DISPATCH_WORKFLOW_FILES` and `NATIVE_STEP_DISPATCH_REGION_LINES`."
    - "D-04 — the entry describes the stand-down mechanism and names the upstream PR the way GSD-CORE-PATCH.md already does: the region contains `open-gsd/gsd-core#3687`, `plan:post` and `verify:post`."
    - "D-05 — the 120 s hook-timeout text is no longer filed under 0.3.1's `### Performance` heading: awk-scoping from `### Performance` to the following `### Changed` and grepping for `120 s` returns 0. It returns 1 today (the trailing sentences of the Performance bullet at CHANGELOG.md:70-75)."
    - "D-05 — the entry is relocated, not deleted: awk-scoping from the `## 0.3.1` heading to the `## 0.3.0` heading and grepping for `120 s` returns >= 1, and the surviving text still states that the value is a reduction from the platform's default rather than a throughput optimization."
    - "D-05 — the Performance bullet keeps its measured numbers: the same Performance region still contains `13.00 ms`, `0.91 ms` and `LC_ALL=C`."
    - "D-06 — `plugins/beads-lifecycle/.claude-plugin/plugin.json` declares version `1.4.0`. It declares `1.3.1` today, unchanged since the `v1.3.1` tag, while `main` carries `966315a`'s behavioral SHIP_MD_PATCH_MARKER v1-to-v2 change plus all of Phase 17 and Phase 18."
    - "D-06 — the file stays valid and otherwise untouched: `python3 -m json.tool` parses it, and its `name`, `description`, `author`, `license` and `skills` values are unchanged."
    - "ROADMAP Ship-step check #1 becomes satisfiable: `git diff --quiet v1.3.1..HEAD -- plugins .claude-plugin README.md` exits non-zero (changes exist), both version declarations now differ from the tag's — `capability.json` at `0.4.0` from plan 17-01, `plugin.json` at `1.4.0` from this plan — and CHANGELOG.md carries a `## 0.4.0` section for the new capability version."
    - "Phase 18's own user-visible change is documented: the 0.4.0 region names the patch-check message-marker change and the exit-code-keyed surfacing rule that plan 18-03 shipped."
  prohibitions:
    - "No CHANGELOG section for a version that does not exist. This file's version headings track `capability.json`, which stays at `0.4.0` through Phase 18, so no `## 1.4.0` heading is created."
    - "No rewriting of the 120 s entry's reasoning — it is relocated, with at most the light rewording needed for it to stand alone as its own bullet."
    - "No edits to any 0.3.0 or earlier section, and no reordering of the `### Fixed` / `### Changed` / `### Breaking` subsections already inside 0.4.0."
    - "No version change to `capability.json` — it is not touched by this plan."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "A complete 0.4.0 section including TRUTH-03 and Phase 18's own changes, and a 0.3.1 section that files the hook-timeout reduction under the right heading"
      contains: "check_native_step_dispatch"
    - path: "plugins/beads-lifecycle/.claude-plugin/plugin.json"
      provides: "The cumulative shipped-since-v1.3.1 version bump"
      contains: "1.4.0"
  key_links:
    - from: "plugins/beads-lifecycle/.claude-plugin/plugin.json"
      to: ".claude-plugin/marketplace.json"
      via: "marketplace installs resolve ./plugins/beads-lifecycle from the branch, so this version string is what a consumer already sees for work sitting on main"
      pattern: "\\./plugins/beads-lifecycle"
    - from: "CHANGELOG.md"
      to: "plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json"
      via: "the file header declares that its version headings track capability.json, which is why the plugin.json bump gets no section of its own"
      pattern: "## 0.4.0"
---

<objective>
Close the CHANGELOG's two accuracy gaps and bump the plugin version that has been stale since the
`v1.3.1` tag.

Purpose: `17-REVIEW.md` WR-03 found the 0.4.0 section documenting three of Phase 17's four
requirements — TRUTH-03 is missing outright. D-05 widens that to the adjacent defect in the same
file: 0.3.1's hook-timeout text sits under `### Performance` while its own sentences argue it is a
deliberate reduction from the platform default and explicitly not a throughput optimization. D-06
closes ROADMAP Ship-step check #1: `main` has carried a behavioral change since `v1.3.1` while
`plugin.json` still declares `1.3.1`, and marketplace installs resolve from the branch, so
consumers already run code the declared version denies.

Output: a 0.4.0 section that matches what actually shipped across Phases 17 and 18, a 0.3.1 section
that files its own entries correctly, and `plugin.json` at `1.4.0`.

**Tracer-first does not apply to this plan.** Three prose and config edits across two files with no
execution path between them and no layers to slice. Recorded per TRACER_MODE=true rather than
silently skipped.

**Why this plan is wave 3.** It documents what the other three plans shipped, so it runs last.
Task 1's Phase 18 entries describe 18-03's message and surfacing changes and 18-01's
reapply-verification documentation. And if 18-02's `checkpoint:decision` was answered `option-c`
(keep the `v1.3.0` tag), this plan additionally owns the explicit withdrawal line ROADMAP Ship-step
check #3 requires in that branch — so read 18-02's SUMMARY before starting Task 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-address-tech-debt-patch-check-doc-accuracy-changelog/18-CONTEXT.md
@.planning/phases/18-address-tech-debt-patch-check-doc-accuracy-changelog/18-PATTERNS.md
@.planning/phases/17-config-code-truth/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give 0.4.0 the entry it is missing, plus this phase's own changes</name>
  <read_first>
    - `CHANGELOG.md` lines 1-45 — the header sentence declaring that versions track `capability.json`, and the whole 0.4.0 section: its `### Fixed`, `### Changed` and `### Breaking` subsections, and the bold-one-sentence-summary-then-supporting-prose shape every bullet uses.
    - `.planning/phases/17-config-code-truth/17-02-SUMMARY.md` — what TRUTH-03 actually shipped, in the words of the plan that shipped it. Write the entry from this, not from the ROADMAP's forward-looking description.
    - `plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py` lines 195-206 — the two module constants and the comment above them, so the entry names them exactly.
    - `plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py` lines 2342-2400 — `check_native_step_dispatch`'s signature and docstring: the region-scoped probe, the fail-open-to-double-dispatch contract, and the stderr-diagnostics rationale.
    - `plugins/beads-lifecycle/.gsd/capabilities/beads/GSD-CORE-PATCH.md` lines 245-285 — the "Probe (not a patch)" section: the upstream-PR naming convention D-04 says to follow, and the region-scoped-versus-whole-file rationale.
    - `.planning/phases/18-address-tech-debt-patch-check-doc-accuracy-changelog/18-01-SUMMARY.md` and `18-03-SUMMARY.md` — what this phase actually shipped, for the Phase 18 entries.
    - `.planning/phases/18-address-tech-debt-patch-check-doc-accuracy-changelog/18-02-SUMMARY.md` — specifically the recorded answer to that plan's `checkpoint:decision`, which determines whether a withdrawal line is needed here.
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
Add to the existing `## 0.4.0` section. Do not create a new version heading — this file's version
headings track `capability.json`, which stays at `0.4.0` through Phase 18.

**D-04, the TRUTH-03 entry.** Add an `### Added` subsection to the 0.4.0 section, placed before
`### Fixed`. The section currently has `### Fixed`, `### Changed` and `### Breaking` and no
`### Added`; do not reorder those three. The entry documents:

- `check_native_step_dispatch(point, workflow_path_override=None)` — the probe that reads the
  installed gsd-core workflow file a lifecycle point maps onto and reports whether a generic,
  unqualified `kind == "step"` arm already exists inside that point's own region.
- its two module constants by name: `NATIVE_STEP_DISPATCH_WORKFLOW_FILES` (the point-to-filename
  map) and `NATIVE_STEP_DISPATCH_REGION_LINES` (the 120-line region bound).
- the stand-down mechanism: `lifecycle_dispatch` now gates its `plan:post` and `verify:post`
  branches on that probe, standing down rather than double-dispatching alongside gsd-core's own
  native loop once the upstream change ships. Say explicitly that `execute:wave:pre` and
  `execute:wave:post` are deliberately NOT gated — no upstream work covers either point — and that
  `plan:pre` is likewise ungated.
- why the probe is region-scoped rather than a whole-file scan: a whole-file `kind == "step"` grep
  is a verified false positive on both shipped 1.11.0 workflow files, and a false native-dispatch
  verdict would make the hook stand down and silently miss the sync, which is the one failure
  direction the design forbids. Every miss reports not-detected, degrading to the working double
  dispatch.
- the upstream PR named as `GSD-CORE-PATCH.md` names it: `open-gsd/gsd-core#3687` with its full
  URL, merged to `next` on 2026-08-19 and unreleased at the time of writing.

Match the section's established entry shape: bold one-sentence summary, then supporting prose.

**Phase 18's own entries.** Add to the existing `### Changed` subsection of 0.4.0:

- the patch-check message change and the surfacing rule that no longer depends on it: all four
  previously-unmarked `PATCH_CHECKS` templates — `not_found_msg` and `could_not_read_msg`, for both
  the `ship-md` and `execute-plan` targets — now carry the same leading marker the `missing_msg`
  templates already used, and both consuming skills now key their surfacing rule off
  `check-patch`'s exit code (0 present, 1 absent) or the absence of `present` in its output, so a
  future template that forgets the marker is a cosmetic miss rather than a silent one.
- the reapply-verification documentation: `GSD-CORE-PATCH.md` now names `verify-reapply-patches.cjs`
  and the `check-patch` verb as the two-part path for confirming a machine-local patch reapply, and
  records that the two runtime homes are separately templated so neither a patched file nor a
  backup may be copied across them.

**Conditional on 18-02's checkpoint answer.** If 18-02's SUMMARY records `option-c` (keep the
`v1.3.0` tag rather than delete it), also add the explicit withdrawal line ROADMAP Ship-step check
#3 requires in that branch: that `v1.3.0` is withdrawn, that its GitHub Release was deleted, and
that the real exposure was the branch range `55855cd`..`049da5b`. If the answer was `option-a` or
`option-b`, add no such line — the tag is gone and that ROADMAP item is closed by the deletion
instead. Record which branch was taken in this plan's SUMMARY either way.
  </action>
  <verify>
    <automated>awk '/^## 0.4.0$/,/^## 0.3.1$/' CHANGELOG.md | grep -c check_native_step_dispatch</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^## 0.4.0$/,/^## 0.3.1$/' CHANGELOG.md | grep -c check_native_step_dispatch` returns >= 1 (returns 0 today).
    - The same awk-scoped region contains `NATIVE_STEP_DISPATCH_WORKFLOW_FILES`, `NATIVE_STEP_DISPATCH_REGION_LINES`, `open-gsd/gsd-core#3687`, `plan:post`, `verify:post`, `execute:wave:pre` and `execute:wave:post`.
    - `grep -c '^### Added$' CHANGELOG.md` returns >= 1, and `awk '/^## 0.4.0$/,/^## 0.3.1$/' CHANGELOG.md | grep -n '^### '` lists `### Added` before `### Fixed`, with `### Fixed`, `### Changed` and `### Breaking` still in their existing relative order.
    - The same awk-scoped region contains `PATCH_CHECKS` and `check-patch`, documenting plan 18-03's message and surfacing change.
    - The same awk-scoped region contains `verify-reapply-patches.cjs`, documenting plan 18-01's addition.
    - `grep -c '^## 1.4.0' CHANGELOG.md` returns 0 — no version heading was invented for the plugin bump.
    - `grep -c '^## 0.3.1$' CHANGELOG.md` returns 1 and `grep -c '^## 0.3.0$' CHANGELOG.md` returns 1 — no existing version heading was disturbed.
    - If 18-02's SUMMARY records `option-c`, `grep -c '55855cd' CHANGELOG.md` returns >= 1; otherwise it returns 0. The SUMMARY states which branch applied.
  </acceptance_criteria>
  <done>The 0.4.0 section documents all four of Phase 17's requirements plus Phase 18's own user-visible changes, in the file's existing entry style, with no invented version heading.</done>
</task>

<task type="auto">
  <name>Task 2: File the hook-timeout reduction under a heading that matches what it is</name>
  <read_first>
    - `CHANGELOG.md` lines 60-90 — the whole 0.3.1 section: its `### Fixed`, `### Performance`, `### Changed` and `### Known issues` subsections, and specifically the Performance bullet at lines 68-75 whose trailing sentences are the text to relocate.
    - `.planning/ROADMAP.md` lines 289-295 — Ship-step check #2, the item this closes, and its statement that the configured hook timeout is a reduction from the command-hook default rather than added protection.
    - `.planning/phases/18-address-tech-debt-patch-check-doc-accuracy-changelog/18-CONTEXT.md` D-05 — the locked decision, including its instruction to move the text rather than rewrite its reasoning.
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
Split the hook-timeout sentences out of 0.3.1's `### Performance` bullet and refile them as their
own bullet under that same section's existing `### Changed` subsection. Use the existing
subsection rather than creating a new heading — D-05 permits Fixed or Changed, and `### Changed`
already exists in 0.3.1 while `### Fixed` there is occupied by the data-loss entry that has
nothing to do with this.

The text to move is the passage beginning `Separately, the hook's own timeout is set explicitly to`
and running to the end of that bullet — the only sentence in the file that opens that way. Move it
verbatim into its new home, or with only the light rewording
needed to stand alone as its own bullet — the sentences already carry their own argument for why
the current heading is wrong, and rewriting that argument would erase the evidence that the entry
was miscategorized rather than merely misplaced. Drop the now-redundant trailing clause that
points back at the Performance heading's per-call numbers, since after the move there is no
heading above it to disclaim.

What must remain in the `### Performance` bullet, untouched: the bash-builtin pre-filter
description, the measured 13.00 ms to 0.91 ms per-non-matching-call figures, the `LC_ALL=C`
rationale with the 4 MB payload cost, and the merged-JSON-parses sentence. Those are genuine
throughput measurements and this task must not disturb them.

Give the relocated bullet the section's bold-summary-then-prose shape so it reads as a peer of the
other `### Changed` entries rather than an orphaned fragment.
  </action>
  <verify>
    <automated>awk '/^### Performance$/,/^### Changed$/' CHANGELOG.md | grep -c '120 s'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^### Performance$/,/^### Changed$/' CHANGELOG.md | grep -c '120 s'` returns 0 (returns 1 today).
    - `awk '/^## 0.3.1$/,/^## 0.3.0$/' CHANGELOG.md | grep -c '120 s'` returns >= 1 — the text is relocated within the same version section, not deleted.
    - `awk '/^### Performance$/,/^### Changed$/' CHANGELOG.md` still contains `13.00 ms`, `0.91 ms`, `LC_ALL=C` and `4 MB`.
    - The relocated text still contains the word `reduction` and still names the platform default it reduces from.
    - `grep -c '^### Performance$' CHANGELOG.md` returns 1 — the heading itself survives and no duplicate was created.
    - `grep -c '^## 0.3.1$' CHANGELOG.md` returns 1 and the 0.3.1 section's subsection headings, in order, are still `### Fixed`, `### Performance`, `### Changed`, `### Known issues`.
  </acceptance_criteria>
  <done>The hook-timeout reduction is filed under a heading its own text does not contradict, and the Performance bullet keeps every measurement that belongs there.</done>
</task>

<task type="auto">
  <name>Task 3: Bump the plugin version that has been stale since v1.3.1</name>
  <read_first>
    - `plugins/beads-lifecycle/.claude-plugin/plugin.json` in full — nine lines; the `version` field is the only one this task changes.
    - `plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json` — its `version` field, at `0.4.0`, for the precedent on bump size for comparable scope.
    - `.planning/ROADMAP.md` lines 279-288 — Ship-step check #1 and its exact check command, which this task closes.
    - `.claude-plugin/marketplace.json` — the `"source": "./plugins/beads-lifecycle"` declaration, which is why this version string is consumer-visible from the branch rather than only at a tag.
  </read_first>
  <files>plugins/beads-lifecycle/.claude-plugin/plugin.json</files>
  <action>
Change `plugin.json`'s `version` field from `1.3.1` to `1.4.0`. Change nothing else in the file:
`name`, `description`, `author`, `license` and `skills` all stay byte-identical.

**Why `1.4.0` and not `1.3.2` or `2.0.0`** — D-06 leaves the number to the planner, and this is the
locked value, not a suggestion for the executor to revisit:

- Not a patch bump. What has accumulated since the `v1.3.1` tag is behavioral, not corrective-only:
  `966315a` moved `SHIP_MD_PATCH_MARKER` v1 to v2, flipping `check_shipmd_patch`'s verdict on every
  machine still carrying v1; Phase 17 added decimal-phase support at every lifecycle point, made
  `beads.sync_mode` do something for the first time, and added the native-dispatch stand-down probe.
- Not a major bump, despite 0.4.0's `### Breaking` subsection. That break is the retirement of two
  subprocess CLI verbs, and plan 17-04's checkpoint established by grep that they had no README
  exposure and no caller outside this repository. Every caller was updated in the same release. The
  plugin's own consumer-facing surface — its skills, its hooks, and its declared lifecycle steps —
  is additive across this range.
- `1.4.0` also mirrors the sibling declaration's shape: `capability.json` went `0.3.1` to `0.4.0`
  for this same body of work.

Do not touch `capability.json`. Do not add a `## 1.4.0` heading to CHANGELOG.md — that file's
headings track the capability version, which stays at `0.4.0`.
  </action>
  <verify>
    <automated>python3 -m json.tool plugins/beads-lifecycle/.claude-plugin/plugin.json &gt; /dev/null &amp;&amp; grep -c '"version": "1.4.0"' plugins/beads-lifecycle/.claude-plugin/plugin.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"version": "1.4.0"' plugins/beads-lifecycle/.claude-plugin/plugin.json` returns 1 and `grep -c '"version": "1.3.1"' plugins/beads-lifecycle/.claude-plugin/plugin.json` returns 0.
    - `python3 -m json.tool plugins/beads-lifecycle/.claude-plugin/plugin.json` exits 0.
    - `git diff --numstat plugins/beads-lifecycle/.claude-plugin/plugin.json` reports exactly `1` line added and `1` line removed.
    - `grep -c '"version": "0.4.0"' plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json` returns 1 — the capability version is untouched.
    - `git diff --quiet v1.3.1..HEAD -- plugins .claude-plugin README.md` exits non-zero, and both version declarations now differ from the values at that tag (`plugin.json` 1.3.1 to 1.4.0, `capability.json` 0.3.1 to 0.4.0), and `grep -c '^## 0.4.0$' CHANGELOG.md` returns 1 — the three conditions ROADMAP Ship-step check #1 requires together.
  </acceptance_criteria>
  <done>`plugin.json` declares `1.4.0`, the file is otherwise byte-identical, and ROADMAP Ship-step check #1's three conditions all hold.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates no new code symbols — nothing here is a function, class, flag or field that
`.planning/intel/API-SURFACE.md` could carry. Its outputs are:

- `CHANGELOG.md` — a new `### Added` subsection heading inside the existing `## 0.4.0` section, new
  bullets under 0.4.0's `### Added` and `### Changed`, and one bullet relocated from 0.3.1's
  `### Performance` into 0.3.1's `### Changed`
- `plugins/beads-lifecycle/.claude-plugin/plugin.json` — the `version` field's new value, `1.4.0`

## Alternatives Considered

N/A — no mechanism choice. Two of the three tasks are prose edits to an existing document in that
document's established entry style, and the third changes one string in a nine-line JSON file. The
one judgment call, the size of the version bump, is a semver reading of an existing change set
rather than a selection among tools or libraries; its reasoning is written out inline in Task 3's
action, where the executor will actually read it, rather than being deferred to this section.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → marketplace consumer | `.claude-plugin/marketplace.json` resolves this plugin from the branch, so `plugin.json`'s version string is what an installing consumer sees for code already on `main`. |
| CHANGELOG → future maintainer | The changelog is the only durable record of why a behavioral change shipped once the phase directory ages out of working memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04-01 | Repudiation | A shipped behavioral change with no changelog record | medium | mitigate | This plan is the mitigation: it closes the TRUTH-03 omission and adds Phase 18's own entries, with awk-region-scoped acceptance criteria naming the exact identifiers that must appear. The `git diff --quiet v1.3.1..HEAD` criterion ties the version declarations and the changelog section together as the single check ROADMAP Ship-step check #1 specifies. |
| T-18-04-02 | Tampering | Version string misdeclaring what a consumer installs | medium | mitigate | The bump size is argued from the actual change set rather than chosen by habit, and locked in the action so the executor does not re-derive it. A `git diff --numstat` criterion bounds the edit to exactly one line changed, so no other declared field can drift in the same commit. |
| T-18-04-03 | Tampering | An editing pass silently dropping or reordering existing changelog history | medium | mitigate | Explicit prohibitions against touching 0.3.0-and-earlier sections and against reordering 0.4.0's subsections, plus positive acceptance criteria asserting that `## 0.3.1` and `## 0.3.0` still each appear exactly once and that 0.3.1's four subsection headings remain in order. |
| T-18-04-04 | Information Disclosure | Changelog text naming internal identifiers and an upstream PR | low | accept | The repository is public by design, `GSD-CORE-PATCH.md` already names the same PR and identifiers, and a changelog whose entries cannot name what changed is not a changelog. |

**Package Legitimacy Gate: not applicable.** No `npm`/`pip`/`cargo` install runs in this plan and
no dependency is added, so no `T-18-04-SC` row exists.
</threat_model>

<verification>
- The 0.4.0 region names `check_native_step_dispatch`, both `NATIVE_STEP_DISPATCH_*` constants,
  `open-gsd/gsd-core#3687`, and both gated and both deliberately-ungated lifecycle points.
- The 0.4.0 region also documents Phase 18's message-marker change and the reapply-verification
  documentation.
- 0.3.1's `### Performance` region no longer contains `120 s`; the 0.3.1 section still does; the
  measured throughput figures are intact.
- `plugin.json` parses and declares `1.4.0`, with a one-line diff.
- No new version heading exists in CHANGELOG.md and no pre-0.3.1 section was edited.
</verification>

<success_criteria>
`17-REVIEW.md` WR-03 and ROADMAP Phase 17 Ship-step checks #1 and #2 are all closed: the 0.4.0
section documents every requirement Phase 17 shipped plus what Phase 18 changed, the 0.3.1 hook
timeout is filed as the deliberate reduction its own text says it is, and both version
declarations differ from the `v1.3.1` tag with a matching CHANGELOG section behind them.
</success_criteria>

<output>
Create `.planning/phases/18-address-tech-debt-patch-check-doc-accuracy-changelog/18-04-SUMMARY.md` when done.
</output>
