---
beads_epic: gsd-beads-0y4
phase: 19-native-resolver-contract-and-failure-boundary
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py
  - plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py
  - plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json
  - CHANGELOG.md
  - README.md
autonomous: true
requirements:
  - RES-01
  - RES-02
  - RES-03
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Tracked source declares one schema-valid beads task-content resolver, executes the approved Python stdlib bootstrap with exact argv transfer, and truthfully documents capability 0.5.0 without claiming installed cutover or byte parity (RES-01, D-17)."
    - "A valid raw-array or versioned-envelope bd result becomes exactly the five native fields without duplicating or losing authored content (RES-02, D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-16)."
    - "Every specified resolver defect returns nonzero, emits one bounded stderr diagnostic, emits no stdout, and cannot expose inline PLAN.md task prose (RES-03, D-15)."
    - "The public CLI seam and its exact typed bd argv are proved by one-factor TDD fixtures (D-18, D-19)."
    - "The resolver-specific bd timeout is strictly below the native resolver's 10000 ms outer timeout, so the adapter's bounded timeout diagnostic is reachable and the ordering is mechanically asserted (RES-03, D-15, D-17, D-19)."
    - "The live SPEC-less edge probe remains visibly 3 applicable, 0 resolved, 3 unresolved; RES-01, RES-02, and RES-03 each remain category=unclassified, status=unresolved, and flagged through phase close."
  artifacts:
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      provides: "resolve_task_content(issue_id) and resolve-task-content CLI dispatch"
      exports: ["resolve_task_content", "main"]
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py"
      provides: "TestResolveTaskContent and TestTaskContentResolverManifest public-seam contract tests"
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json"
      provides: "version 0.5.0 and the sole taskContentResolver declaration for trackerPrefix beads"
      contains: "taskContentResolver"
    - path: "CHANGELOG.md"
      provides: "0.5.0 release entry for the resolver contract"
    - path: "README.md"
      provides: "source-tree resolver behavior, SessionStart auto-install-but-inert boundary before Phase 20, and Phase 21 byte-parity/cutover ownership"
  key_links:
    - from: "plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json"
      to: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      via: "python3 -c bootstrap resolves GSD_HOME/Path.home(), then os.execv runs resolve-task-content with the whole-element id"
      pattern: "taskContentResolver|resolve-task-content|\\{\\{id\\}\\}"
    - from: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      to: "bd show"
      via: "existing run_bd typed subprocess seam with resolver-specific timeout=8, strictly below the 10000 ms outer bound"
      pattern: "run_bd\\(\\[\"bd\", \"show\""
    - from: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      to: "gsd-core task-content resolver"
      via: "one JSON object on stdout with description/read_first/verify/acceptance_criteria/done"
      pattern: "description|read_first|verify|acceptance_criteria|done"
  assumptions:
    - statement: "Live SPEC-less probe row RES-01 is category=unclassified, status=unresolved, probe='unclassified — review manually'; it remains flagged through phase close because no phase SPEC supplied Edge Coverage."
      status: unresolved
      flagged: true
    - statement: "Live SPEC-less probe row RES-02 is category=unclassified, status=unresolved, probe='unclassified — review manually'; D-01 through D-14 remain authoritative without fabricating an edge classification."
      status: unresolved
      flagged: true
    - statement: "Live SPEC-less probe row RES-03 is category=unclassified, status=unresolved, probe='unclassified — review manually'; every locked failure class remains explicit and fail-closed without fabricating resolution."
      status: unresolved
      flagged: true
  prohibitions:
    - statement: "Never expose PLAN.md task prose as fallback content after native resolution is attempted."
      status: unverified
      flagged: true
    - statement: "Never silently reinterpret, synthesize, duplicate, or discard authored Beads task prose or structured acceptance criteria."
      status: unverified
      flagged: true
    - statement: "Never create a second task-content source, resolver interface, or tracker abstraction alongside the native beads resolver."
      status: unverified
      flagged: true
---

<!-- rumdl-disable MD013 MD022 MD032 MD033 MD041 MD058 -->

**Mechanism:** existing `sync.py` adapter + Python stdlib bootstrap/native gsd-core `taskContentResolver` seam

**Forbidden:** Phase 20 `tracker-id` work; Phase 21 installed cutover or Patch 2 retirement; gsd-core edits; new dependencies; PATH shims; cache; retry; telemetry; raw Beads passthrough; general Markdown parser; another interface

**Audit:** public CLI boundary plus exact subprocess spy; isolated fixtures differ in only one variable

<objective>
Establish the complete Phase 19 resolver contract: one fail-closed public adapter verb and one sole native manifest declaration, both test-first and limited to tracked plugin source.

Purpose: Live Beads task content must cross gsd-core's native seam losslessly before Phase 20 adds identity and Phase 21 cuts over the installed runtime.

Output: A tested `resolve-task-content` command, a schema-valid `taskContentResolver` declaration, and no new module, dependency, runtime mirror, or downstream migration work.
</objective>

<execution_context>
@/home/dd/.codex/gsd-core/workflows/execute-plan.md
@/home/dd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-native-resolver-contract-and-failure-boundary/19-CONTEXT.md
@.planning/phases/19-native-resolver-contract-and-failure-boundary/19-RESEARCH.md
@.planning/phases/19-native-resolver-contract-and-failure-boundary/19-PATTERNS.md
@.planning/phases/19-native-resolver-contract-and-failure-boundary/19-VALIDATION.md
@plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py
@plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py
@plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json
@CHANGELOG.md
@README.md

<interfaces>
Existing tracked interfaces to reuse:

- `SAFE_BD_ID_RE = re.compile(r"[A-Za-z0-9][A-Za-z0-9._-]*")`; use `fullmatch` (D-13).
- `run_bd(argv, timeout=BD_TIMEOUT)` calls `subprocess.run` with typed argv, captured text streams, and no shell.
- `_task_description(task)` is the canonical producer for ordered H2 task sections and the primary round-trip oracle (D-18).
- `main(argv=None)` owns all `sync.py` subparser registration and return-code dispatch.
- Installed `validateCapability(cap, folderId)` returns an error-string array; `[]` means the feature manifest is valid.
- Installed `splitCriteria` splits CRLF/LF, trims, drops blanks, strips one optional `-` or `*` marker, then drops resulting blanks (D-10).
</interfaces>

<known_baseline>
On 2026-08-30, `TestTaskDescription` passed 3/3, while the authoritative capability suite ran 263 tests and failed with eight failures plus one error in lifecycle-dispatch/native-step-detection tests after installed gsd-core began dispatching `plan:post` and `verify:post` natively. Phase 19 does not edit that subsystem. The validated distinct prerequisite ticket `gsd-beads-byp` owns that repair outside this phase; its existence resolves ownership with 100/100 confidence but does not establish execution readiness. Before Task 1 starts, `bash -lc "set -euo pipefail; cd /home/dd/projects/gsd-beads; bd show gsd-beads-byp --json | jq -e '.[0].status == \"closed\"'; cd plugins/beads-lifecycle/.gsd/capabilities/beads; python3 -m unittest discover -s tests -t tests"` must exit 0, proving both live ticket closure and the exact green full capability suite. The executor must halt before any Phase 19 mutation when either condition is unmet, rather than repair lifecycle dispatch inside Phase 19 or downgrade the suite to a baseline report. Readiness confidence is 100/100 only after both live observations pass; at planning time the ticket is open, so readiness is 0/100.
</known_baseline>
</context>

<assumption_delta_decision noun="task content source" decision="no-change">
Native Beads remains the sole content source; PLAN fallback is prohibited, not added. Confidence: 99/100.
</assumption_delta_decision>

## Artifacts this phase produces

- New module-level symbol: `resolve_task_content(issue_id)` in tracked `sync.py`.
- New public CLI verb: `sync.py resolve-task-content <id>`.
- New test classes: `TestResolveTaskContent` and `TestTaskContentResolverManifest` in the existing tracked test module.
- New top-level manifest field: the sole `taskContentResolver`, with `trackerPrefix: "beads"`, `invoke.binary: "python3"`, a whole-element `{{id}}`, and `timeoutMs: 10000`.
- New files: none; existing `CHANGELOG.md` and `README.md` are updated inside Task 2 with the code-bearing capability change.

<tasks>

<task type="tracer" tdd="true">
  <name>Task [lossless fail-closed resolver via existing sync.py CLI] ! [no broad Markdown parser, fallback, retry, or new dependency]</name>
  <beads-id>gsd-beads-0y4.1</beads-id>
  <originating_ticket>gsd-beads-xy2 — plan:post must create exactly one distinct Beads task ticket for this PLAN task; PLAN.md remains the intent source.</originating_ticket>
  <requirements>RES-02, RES-03</requirements>
  <reversibility rating="reversible">The unused adapter verb is local to one existing module and can be removed without migration before Phase 20/21 consume it.</reversibility>
  <precondition>`bd show gsd-beads-byp --json` reports live status `closed`, the exact full capability suite exits 0, and `git ls-files -- plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py` prints both paths; otherwise halt before any Phase 19 mutation.</precondition>
  <files>plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py, plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py</files>
  <read_first>
    - `.planning/phases/19-native-resolver-contract-and-failure-boundary/19-CONTEXT.md` D-01 through D-16 and D-18/D-19
    - `.planning/phases/19-native-resolver-contract-and-failure-boundary/19-REVIEWS.md` timeout-ordering consensus and zero-exit error-envelope finding
    - `.planning/phases/19-native-resolver-contract-and-failure-boundary/19-VALIDATION.md` blocking Wave-0 gate
    - `plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py` `BD_TIMEOUT`, `SAFE_BD_ID_RE`, `run_bd`, `_task_description`, and `main`
    - `plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py` `_completed`, `TestEndToEndTracer`, `TestTaskDescription`, and stdout/stderr capture patterns
    - `/home/dd/.codex/gsd-core/bin/lib/plan-document.cjs` `splitCriteria`
    - `/home/dd/.codex/gsd-core/bin/lib/task-content-resolution.cjs` `coerceStringArray`, `mapResolverOutput`, and resolver failure semantics
  </read_first>
  <behavior>
    - RED canonical public path (D-01/D-03/D-08/D-12/D-16/D-18): `_task_description` produces the control; `main(["resolve-task-content", id])` returns 0 and writes exactly one five-key JSON object. `read_first` and `acceptance_criteria` are JSON arrays, while `description`, `verify`, and `done` preserve the locked scalar semantics.
    - RED preservation arms (D-02/D-06/D-07/D-19): leading prose, one unknown H2, and one near-match H2 each vary alone from the canonical control and preserve authored bytes/order; no test claims `_task_description` emits those authored variants.
    - RED typed subprocess path (D-13/D-15/D-17/D-18/D-19): the spy observes exactly one `run_bd` call reaching `subprocess.run` with argv `["bd", "show", id, "--json"]` and resolver-specific `timeout=8`; assert `8 * 1000 < 10000` without changing the shared `BD_TIMEOUT = 15` used by unrelated commands.
    - RED containers (D-13/D-14/D-19): one-row raw arrays and versioned `{data: [...]}` envelopes succeed. Separate one-factor arms cover cardinality, row type/id, and a return-code-zero `{"error": "not found"}` object, which must fail as `invalid envelope` rather than fall through or raise an unclassified exception.
    - RED normalization (D-09/D-10/D-11): Read First preserves canonical list order/duplicates; criteria matches gsd-core LF/CRLF/optional-marker/blank/order/duplicate semantics; missing/null criteria becomes `[]`; wrong types fail closed; successful stdout explicitly proves both list-valued fields are arrays.
    - RED failure matrix (D-04/D-05/D-07/D-14/D-15/D-19): invalid id, unavailable/nonzero/timed-out `bd`, invalid UTF-8/JSON, invalid envelope, row count/type/id mismatch, wrong source-field type, duplicate reserved heading, malformed Read First, and blank source/retained description each return nonzero with empty stdout and one diagnostic of at most 2000 characters carrying its stable category token.
  </behavior>
  <action>Addresses review concerns: preserve the open `gsd-beads-byp` ticket as a hard Wave-0 gate; make the adapter timeout reachable before the 10000 ms outer kill; assert array wire types; and add the isolated zero-exit error-envelope arm. RED: add `TestResolveTaskContent` at the public `main([...])` seam before production code. Build the canonical control with `_task_description`; vary only one authored or failure feature per arm; assert exact JSON/exit/stream behavior, exact five-key equality, list types for `read_first` and `acceptance_criteria`, stable diagnostic categories, and the exact `bd` argv plus `timeout=8`. The timeout assertion must prove the inner milliseconds are strictly below 10000; do not lower or otherwise change module-wide `BD_TIMEOUT`, because non-resolver commands retain their existing behavior. Make the zero-exit non-data object differ from a valid response only in its top-level envelope and require `invalid envelope`. Run the focused class and require RED because the command/implementation is absent, then commit `test(19-01): specify native beads content resolution`. GREEN: add one `resolve_task_content(issue_id)` implementation and one `resolve-task-content` parser/dispatch arm in existing `sync.py`. Reuse `SAFE_BD_ID_RE.fullmatch`, the existing `run_bd` typed-argv seam with resolver-only `timeout=8`, `_task_description`'s finite grammar, stdlib JSON/regex/subprocess exceptions, and the existing `main` interface. Validate all D-13/D-14 containers, cardinality, id, field types, and usability before the sole stdout write; catch every D-15 defect and emit one bounded stderr diagnostic. Do not route through `bd_available`, PLAN parsing, lifecycle dispatch, shell text, cache, retry, telemetry, a parser dependency, or another interface. Run the focused class and authoritative suite green, then commit `feat(19-01): resolve beads task content through sync adapter`. Refactor only if three genuinely identical business-logic occurrences exist; otherwise retain the one deep adapter.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; cd /home/dd/projects/gsd-beads; bd show gsd-beads-byp --json | jq -e '\''.[0].status == "closed"'\''; cd plugins/beads-lifecycle/.gsd/capabilities/beads; python3 -m unittest tests.test_sync.TestResolveTaskContent -v; python3 -m unittest discover -s tests -t tests'</automated>
    <fails_when>`gsd-beads-byp` is absent or not closed; the class is missing; inner timeout is not 8 seconds or is not strictly below 10000 ms; array-valued fields are not JSON arrays; the zero-exit error object does not produce `invalid envelope`; any success mapping/retained bytes differ; any isolated failure returns zero, writes stdout, emits multiple/over-2000-character diagnostics, or omits its stable category token; the exact bd argv differs or repeats; or either focused/full suite exits nonzero.</fails_when>
  </verify>
  <acceptance_criteria>
    - RES-02 and D-01 through D-14/D-16: every valid documented input maps losslessly to exactly five keys; `read_first` and `acceptance_criteria` are arrays on stdout.
    - RES-03 and D-15/D-17: the resolver calls the unchanged typed `run_bd` seam with exact argv and `timeout=8`, and a test proves `8 * 1000 < 10000` while shared `BD_TIMEOUT = 15` remains untouched for other commands.
    - D-14/D-19: a return-code-zero `{"error": "not found"}` fixture differs from its valid control only by envelope and exits nonzero with empty stdout plus the `invalid envelope` signal.
    - D-18/D-19: canonical round trip, independent authored variants, exact subprocess spy, JSON-only stdout, and every one-factor failure arm pass at the public seam.
    - No file outside the task's two tracked paths changes.
  </acceptance_criteria>
  <done>The tracked adapter exposes one tested public resolver path satisfying RES-02/RES-03 and D-01 through D-16/D-18/D-19; its own timeout diagnostic is reachable before the outer resolver timeout, and no fallback or new surface exists.</done>
  <ponytail>
    <first_rung>2 — already in this codebase</first_rung>
    <why>No higher rung suffices: the capability lacks the resolver verb, but existing `sync.py` already owns the writer, id grammar, timeout-parameterized typed subprocess seam, JSON imports, CLI, and test harness; stdlib fence state completes the missing inverse.</why>
    <deliberately_skipped>New module, fixture file, Markdown package, third-party Beads client library, shell wrapper, raw passthrough, PLAN fallback, cache, retry, telemetry, private-helper tests, Phase 20 identity, and Phase 21 cutover.</deliberately_skipped>
    <minimum_runnable_check>`python3 -m unittest tests.test_sync.TestResolveTaskContent -v` from the tracked capability root.</minimum_runnable_check>
  </ponytail>
</task>

<task type="auto" tdd="true">
  <name>Task [sole native beads resolver manifest and bootstrap] ! [no tracker-id migration, installed cutover, or Patch 2 retirement]</name>
  <beads-id>gsd-beads-0y4.2</beads-id>
  <originating_ticket>gsd-beads-xy2 — plan:post must create exactly one distinct Beads task ticket for this PLAN task; PLAN.md remains the intent source.</originating_ticket>
  <requirements>RES-01, RES-03</requirements>
  <reversibility rating="reversible">SessionStart may auto-install the changed bundle globally, but before Phase 20 emits tracker-id no task selects this declaration; removing the source field and re-running the existing drift installer restores the prior registry without data migration.</reversibility>
  <precondition>The Task 1 focused class is green, and the gsd-tools path resolved by the existing test helper has sibling `bin/lib/capability-validator.cjs` and `bin/lib/task-content-resolution.cjs` files exporting the cited public contracts.</precondition>
  <files>plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json, plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py, CHANGELOG.md, README.md</files>
  <read_first>
    - `.planning/phases/19-native-resolver-contract-and-failure-boundary/19-CONTEXT.md` D-15, D-17, D-18, D-19, and Phase 20/21 boundary
    - `.planning/phases/19-native-resolver-contract-and-failure-boundary/19-REVIEWS.md` auto-install inertness, byte-parity boundary, and manifest invocation findings
    - `plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json` current feature-role manifest
    - `plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py` `CAPABILITY_PATH`, `_gsd_tools_path`, and `TestSyncModeDeclarationParity`
    - `plugins/beads-lifecycle/hooks/capability-auto-install.sh` drift hash and global-scope installation contract
    - `plugins/beads-lifecycle/hooks/session-start.sh` SessionStart call into capability auto-install
    - `/home/dd/.codex/gsd-core/bin/lib/capability-validator.cjs` `validateCapability`, `validateTaskContentResolverFields`, and merged tracker-prefix uniqueness
    - `/home/dd/.codex/gsd-core/bin/lib/task-content-resolution.cjs` `buildInvocation`, real `spawnSync`, `timeoutMs`, `resolveTaskContent`, and array coercion
    - `CHANGELOG.md` capability-version ownership and current `0.4.0` entry
    - `README.md` task-content source, `GSD_HOME`, auto-install, and installed-runtime caveat sections
  </read_first>
  <behavior>
    - RED declaration (RES-01/D-17): tracked manifest version is `0.5.0` and contains exactly one `taskContentResolver` with prefix `beads`, binary `python3`, exact args `["-c", bootstrap, "{{id}}"]`, and timeout 10000 ms; `{{id}}` is a standalone element, JSON decoding preserves the single-line bootstrap, and installed validation returns no errors.
    - RED argv/execv success (D-17/D-18): Python `-c` observes `sys.argv == ["-c", id]`; installed public `resolveTaskContent` executes the tracked manifest against a scratch `GSD_HOME` probe and observes exact execv argv `[script, "resolve-task-content", id]` before valid five-field JSON.
    - RED failure isolation (RES-03/D-15/D-17/D-19): missing-script and sleeping-probe arms share the same public-resolver wrapper and differ only by the probe state; separately assert the thrown `ResolverFailedError`/`ResolverTimeoutError` and wrapper-level nonzero/empty-stdout/single-bounded-stderr observables.
    - RED timeout composition (D-15/D-17): Task 1's public spy proves the adapter passes inner timeout 8; the literal manifest test proves outer timeout 10000 and asserts `8 * 1000 < manifest.taskContentResolver.invoke.timeoutMs`.
    - RED source/runtime boundary: tracked hook source proves the next SessionStart may auto-install bundle drift globally, but before Phase 20 there is no `tracker-id="beads:..."` selection path. README must call the globally copied declaration inert, must not call it cutover proof, and must assign exact tracked/project/global byte parity plus public installed resolution to Phase 21.
    - RED release/docs: `0.5.0` records only the tracked source contract; README names the exact five fields, fail-closed/no-PLAN-fallback rule, locator, auto-install inertness, and Phase 21 parity/cutover ownership without claiming activation, installed equivalence, Patch 2 retirement, or source/global byte identity.
  </behavior>
  <action>Addresses review concerns: document that SessionStart can globally auto-install bundle drift while the declaration remains inert before Phase 20; constrain Phase 19 proof to tracked manifest plus isolated temporary layouts; reserve exact tracked/project/global byte parity and installed cutover for Phase 21. RED: add adjacent `TestTaskContentResolverManifest` tests before changing JSON. Load the tracked `CAPABILITY_PATH` with normal JSON decoding; assert the exact sole declaration, standalone whole-element `{{id}}`, valid one-line bootstrap escaping, 10000 ms timeout, `sys.argv[1]` contract, and installed validator result. Reuse one public `resolveTaskContent` wrapper and scratch-scoped `GSD_HOME` probes for successful execv, missing script, and sleeping timeout cases; keep failure fixtures one-factor and distinguish thrown error objects from wrapper process observables. Tie the layers together by asserting Task 1's observed inner timeout in milliseconds is strictly below the manifest's outer timeout. Add documentation assertions that quote the existing SessionStart drift installer accurately: source drift may be copied to global scope before Phase 20, but no eligible task carries the `beads:` tracker identity yet, so the resolver is unselected and inert; this fact is not evidence of byte parity or cutover. Run the focused class and require RED because the field/version/docs are absent, then commit `test(19-01): specify beads resolver declaration`. GREEN: bump tracked capability source from `0.4.0` to `0.5.0` and add only the D-17 top-level resolver. Use `python3`, exact args `["-c", bootstrap, "{{id}}"]`, `GSD_HOME` or `Path.home()`, a missing-script bounded failure, `os.execv`, and `timeoutMs: 10000`; do not modify the auto-install hooks. Update `CHANGELOG.md` in the same commit to record source availability only. Update README in that commit with the five-field contract, no-PLAN-fallback rule, locator, auto-install-but-inert explanation, and the explicit statement that Phase 21 alone proves exact tracked/project/global byte parity, active-registry/public-command resolution, installed cutover, and Patch 2 retirement. Do not install/copy a capability, compare runtime bytes, mutate registry state, change identity, alter Patch 2 inventory, edit gsd-core, or add a dependency/interface. Run both focused classes and the authoritative full suite green, then commit `feat(19-01): declare native beads task resolver`.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; cd /home/dd/projects/gsd-beads/plugins/beads-lifecycle/.gsd/capabilities/beads; python3 -m unittest tests.test_sync.TestResolveTaskContent tests.test_sync.TestTaskContentResolverManifest -v; python3 -m unittest discover -s tests -t tests; cd /home/dd/projects/gsd-beads; rg -F "## 0.5.0" CHANGELOG.md; awk "/^## 0\\.5\\.0/{in_section=1; next} /^## /{in_section=0} in_section" CHANGELOG.md | rg -F "taskContentResolver"; if awk "/^## 0\\.5\\.0/{in_section=1; next} /^## /{in_section=0} in_section" CHANGELOG.md | rg -i "activat(ed|ion)|cut[ -]?over|retir(ed|ement)|byte[- ]ident"; then exit 1; fi; rg -F "description, read_first, verify, acceptance_criteria, and done" README.md; rg -F "fails closed with no PLAN.md fallback" README.md; rg -F "GSD_HOME" README.md; rg -F "Path.home()" README.md; rg -F "auto-install" README.md; rg -F "inert until Phase 20 adds tracker-id" README.md; rg -F "Phase 21 owns exact tracked, project-installed, and global-installed byte parity, installed cutover, and Patch 2 retirement" README.md'</automated>
    <fails_when>The manifest/adapter class is missing or failing; JSON decoding or literal bootstrap/argv/version/declaration differs; `{{id}}` is embedded rather than standalone; inner timeout is not strictly below the actual 10000 ms manifest bound; real execv success fails; missing-script/outer-timeout arms conflate thrown errors with process evidence or violate one-factor isolation; validator errors are nonempty; full suite exits nonzero; source docs imply that global auto-install is cutover evidence; changelog overclaims activation/cutover/retirement/byte identity; README omits the exact fields, fail-closed rule, locator, inert-before-Phase-20 rule, or Phase-21 parity/cutover ownership; or any command exits nonzero.</fails_when>
  </verify>
  <acceptance_criteria>
    - RES-01/D-17: tracked capability source is `0.5.0` with one validator-clean resolver, exact `["-c", bootstrap, "{{id}}"]` args, standalone id, correct Python argv contract, and 10000 ms outer bound.
    - D-15/D-17: Task 1's observed 8-second inner bound is asserted strictly below the actual manifest timeout; missing script and outer timeout remain independently proved at the installed public resolver seam.
    - The raw tracked manifest JSON decodes and the literal bootstrap reaches exact execv argv through isolated scratch `GSD_HOME` layouts; no active registry or installed bundle is mutated.
    - README states that existing SessionStart drift detection may auto-install the source bundle globally but the declaration remains unselected and inert until Phase 20 adds tracker-id; this installed presence is not byte-parity or cutover evidence.
    - README assigns exact tracked/project/global byte parity, active installed public resolution, cutover, and Patch 2 retirement to Phase 21; changelog records only Phase 19 source availability.
    - No Phase 20/21 artifact, runtime mirror, gsd-core file, auto-install hook, dependency, PATH state, or extra interface changes.
  </acceptance_criteria>
  <done>The tracked manifest declares exactly one schema-valid native beads resolver reaching Task 1's public adapter by construction; auto-install inertness is documented, while byte parity and installed cutover remain explicitly unproved until Phase 21.</done>
  <ponytail>
    <first_rung>4 — native platform feature</first_rung>
    <why>gsd-core's existing `taskContentResolver` is the required seam; Python stdlib at rung 3 supplies the cwd-independent locator. No custom registry or wrapper is needed.</why>
    <deliberately_skipped>Second resolver, capability-root extension, PATH shim, console script, dependency, shell interpolation, manual installed-runtime copy, byte-parity check, config key, Phase 20 identity, Phase 21 cutover/Patch 2 inventory work, and a standalone documentation task.</deliberately_skipped>
    <minimum_runnable_check>`python3 -m unittest tests.test_sync.TestTaskContentResolverManifest -v` from the tracked capability root.</minimum_runnable_check>
  </ponytail>
</task>

</tasks>

## Alternatives Considered

| Mechanism | Primary dated source | Outcome | Decided by |
|---|---|---|---|
| **Native gsd-core `taskContentResolver` + Python stdlib `os.execv` bootstrap** | [Python `os.execv` documentation](https://docs.python.org/3/library/os.html#os.execv), accessed 2026-08-30; [official gsd-core resolver guide](https://github.com/open-gsd/gsd-core/blob/next/docs/how-to/develop-a-task-content-resolver-capability.md), current source 2026-08-30 | **Chosen.** Reuses the native seam and replaces the bootstrap with one installed adapter process. | Performance wins first (one bounded exec), then simplicity/LOC (stdlib only), ecosystem (native gsd-core), and maintenance (no executable lifecycle). |
| **Direct raw `bd show --json` resolver output** | [Beads JSON schema](https://github.com/gastownhall/beads/blob/main/docs/reference/json-schema.md), reviewed 2026-08-07 | Rejected: current array/versioned envelopes, scalar criteria, and Markdown sections do not satisfy the five-field resolver object. | Simplicity/LOC loses at system level because tracker-specific coercion would leak into every consumer. |
| **General CommonMark parser in the adapter** | [CommonMark 0.31.2](https://spec.commonmark.org/0.31.2/), published 2024-01-28 | Rejected: only exact reserved H2 recognition plus fence protection is in contract; a parser cannot infer duplicate-heading intent. | Performance, simplicity/LOC, and maintenance all lose without an acceptance-criterion gain. |

**Decided by:** performance first, then simplicity/LOC, ecosystem support, and maintenance overhead; the native resolver plus stdlib bootstrap wins in that exact order.

Numerical-mechanism audit: not applicable. This phase introduces no floating-point computation or numeric accumulation, so Kahan/IEEE 754 citations would not support a real mechanism choice; the only number is the locked integer timeout validated by gsd-core.

### Scientific resolution of open questions

#### Detector signal: “fallback”

1. **Argument map:** The signal occurs in a prohibition; it does not introduce a second source.
2. **Evidentiary inventory:** ROADMAP, REQUIREMENTS, CONTEXT D-15/D-16, and installed gsd-core resolver source independently require hard failure.
3. **Logic audit:** Treating a prohibited fallback as a pluralization would invert the sentence's meaning.
4. **Bias/confound audit:** The detector was rerun with only `fallback` as the pluralization term; that one token caused the signal.
5. **Competing explanations:** (a) PLAN becomes a second task-content source — rejected by explicit prohibition; (b) fallback names an error-policy branch — accepted as the actual usage.
6. **Appraisal:** **Strong; Accept no-change; 99/100.** `<assumption_delta_decision>` records noun `task content source`, decision `no-change`.

#### Edge probe: RES-01, RES-02, RES-03 all unclassified/unresolved

1. **Argument map:** The live SPEC-less probe returned 3 applicable, 0 resolved, and 3 unresolved. RES-01, RES-02, and RES-03 each report `category=unclassified`, `status=unresolved`, and `probe='unclassified — review manually'`; this does not invalidate the requirements or locked D-01 through D-19 contract.
2. **Evidentiary inventory:** Direct requirement text, CONTEXT decisions, tracked producer code, and installed resolver code are stronger implementation evidence than an absent taxonomy label.
3. **Logic audit:** “Unclassified” cannot be converted into either “resolved” or “irrelevant” without evidence.
4. **Bias/confound audit:** Keep each requirement as its own flagged assumption; do not collapse three detector misses into one invented classification.
5. **Competing explanations:** (a) block the phase for a new SPEC — rejected because the user explicitly directed preservation rather than re-specification; (b) auto-resolve from CONTEXT — rejected because the probe protocol forbids auto-resolving unclassified rows.
6. **Appraisal:** **Strong; Modify by preserving all three flagged assumptions through phase close; 98/100.** Every requirement is still task-covered, and none receives fabricated edge metadata. The executor must not flip any row to resolved without a phase SPEC that supplies the missing classification.

#### Validation gate: current full-suite failures

1. **Argument map:** Phase 19 needs focused resolver proofs and the repository's authoritative capability suite; a known-red full suite cannot simultaneously be mandatory and informational.
2. **Evidentiary inventory:** The exact full-suite command reproduced 263 tests with eight failures and one error on 2026-08-30; every failure is in lifecycle-dispatch/native-step-detection behavior triggered by current installed gsd-core, while Phase 19's CONTEXT/ROADMAP exclude that subsystem. This is direct empirical evidence. Confidence: 100/100.
3. **Logic audit:** Calling the suite non-gating would weaken the explicit Nyquist contract; repairing unrelated lifecycle dispatch inside this plan would violate the phase fence. Both are invalid resolutions.
4. **Bias/confound audit:** Do not attribute the failures to absent resolver code, and do not let adding resolver tests mask or numerically dilute them. Require the same full command green before and after Phase 19.
5. **Competing explanations:** (a) downgrade the full suite to a report—rejected because it shrinks verification; (b) repair lifecycle dispatch in Task 1—rejected because it changes an unrelated subsystem and confounds Phase 19; (c) require separate Beads-tracked prerequisite work to restore green before execution—accepted because it preserves both contracts.
6. **Appraisal:** **Strong; Modify with blocking prerequisite.** Live existence of validated ticket `gsd-beads-byp` resolves repair ownership at 100/100 confidence; only its live `closed` status plus the exact full-suite command exiting 0 resolve empirical readiness at 100/100. `19-VALIDATION.md`, Task 1's precondition, task actions, and overall verification enforce both observations without adding the repair to Phase 19.

#### Capability version and documentation ownership

1. **Argument map:** Adding a public manifest resolver/CLI behavior changes the capability source contract, so its version and source documentation must change with the code; installed cutover and Patch 2 retirement remain later work.
2. **Evidentiary inventory:** Live `gsd-beads-xy2` explicitly requires a version bump plus same-commit changelog/docs; `CHANGELOG.md` states that its versions track `capability.json`; the manifest is `0.4.0`; AGENTS.md requires docs with code. Direct source and tracker evidence. Confidence: 100/100.
3. **Logic audit:** The Phase 21 fence excludes installed activation and Patch 2 retirement, not truthful documentation of the Phase 19 source contract. Treating all documentation as cutover would conflate source availability with installed rollout.
4. **Bias/confound audit:** Keep `GSD-CORE-PATCH.md` and two-patch inventory unchanged in Phase 19 so documentation cannot imply Patch 2 is retired early; update only `CHANGELOG.md` and `README.md` inside Task 2.
5. **Competing explanations:** (a) leave version/docs unchanged—rejected by live issue and repository version ownership; (b) defer all docs to Phase 21—rejected because source behavior would be undocumented between phases; (c) update Patch 2 inventory now—rejected as a false cutover claim; (d) bump to `0.5.0` and document source behavior plus the Phase 21 boundary—accepted.
6. **Appraisal:** **Strong; Accept `0.5.0` + integrated changelog/README updates; 99/100.** The residual 1% is release timing, which does not change source-version ownership.

### Scientific appraisal of Cycle 1 review findings

| Review claim / premise | Evidence inventory, logic, and bias/confound audit | Competing explanations | Decision and confidence |
|---|---|---|---|
| `gsd-beads-byp` is open, so Phase 19 is not execution-ready. | Live `bd show` reports `open`; the same exact full-suite gate is already the Nyquist baseline. Treating plan approval as readiness would substitute authority-based evidence for live state. | Weaken the suite to informational; absorb lifecycle repair into Task 1. Both would either reduce verification or confound an excluded subsystem. | **Condition:** retain the fail-closed Wave-0 gate and sequence the separate ticket first. Strong, 100/100. Citation: `19-REVIEWS.md`, reviewed 2026-08-31. |
| Shared `BD_TIMEOUT=15` exceeds the native 10000 ms bound, making the adapter timeout diagnostic unreachable on that path. | Tracked `sync.py` defines 15 seconds; installed gsd-core passes `timeoutMs` to `spawnSync`; causal ordering is direct. A sleeping-probe outer-timeout test alone would confirm only supervisor behavior. | Rely on the outer error only; lower the global timeout for every `bd` call. Both violate D-15's adapter diagnostic or widen unrelated behavior. | **Accept:** resolver-only `run_bd(..., timeout=8)` plus public spy and `8*1000 < 10000` assertion. Strong, 99/100. Citation: `19-REVIEWS.md`, reviewed 2026-08-31. |
| Global auto-install can copy the changed bundle before Phase 21. | Tracked SessionStart hook hashes bundle drift and installs at global scope. Selection still requires a `beads:` tracker identity, which Phase 20 owns. Conflating presence with selection is the central logic risk. | The manifest is fully active immediately; installation is deferred until Phase 21. Both are false: it may be installed yet remain unselected. | **Modify docs only:** describe auto-install as possible but inert before Phase 20. Strong, 99/100. Citation: `19-REVIEWS.md`, reviewed 2026-08-31. |
| Temporary bootstrap proof is not tracked/project/global byte-parity proof. | Task 2 executes tracked manifest semantics against isolated scratch layouts; it neither hashes nor compares installed copies. Inferring parity from behavior would be an overgeneralization. | Treat the temporary copy as parity evidence; add parity/install work now. The first overclaims, the second violates CUT-01 sequencing. | **Condition:** Phase 19 proves agreement by construction; Phase 21 proves exact bytes and active cutover. Strong, 100/100. Citation: `19-REVIEWS.md`, reviewed 2026-08-31. |
| Three SPEC-less `must_haves` rows remain unresolved. | Live probe output is explicit and the missing classification source is known. Confirmation bias would convert strong implementation evidence into invented taxonomy metadata. | Auto-resolve from CONTEXT; block all work until a new SPEC. Neither follows the probe protocol or the user's preservation instruction. | **Accept unresolved:** keep all three rows flagged through phase close. Strong, 98/100. Citation: `19-REVIEWS.md`, reviewed 2026-08-31. |
| `read_first` and `acceptance_criteria` must be arrays on resolver stdout. | Installed `coerceStringArray` silently maps non-arrays to `[]`; therefore key presence alone is insensitive to wrong types. | Scalar output is rejected loudly; scalar output is normalized by core. Current source supports neither—it silently drops. | **Accept:** exact type assertions in the public success test. Strong, 100/100. Citation: `19-REVIEWS.md`, reviewed 2026-08-31. |
| The machine-local sota-numerics command is unrelated and non-portable. | The plan's own alternatives table is already checked by plan-phase policy; `/home/dd/.gsd/...` is outside the Phase 19 artifact/interface contract. Keeping it creates a machine-state confound. | Guard it when absent; retain it as defense in depth. Both add executor branching without strengthening Phase 19 behavior. | **Reject command:** remove it; the orchestrator's plan-post gate remains authoritative. Strong, 99/100. Citation: `19-REVIEWS.md`, reviewed 2026-08-31. |
| Bare `git ls-files --` cannot prove the five tracked targets. | With no pathspec it enumerates the whole index, so the observation cannot equal the claimed five-path set. | Infer membership from a long listing; inspect each path separately. Both are weaker or noisier than one explicit pathspec/set comparison. | **Accept:** pass all five exact pathspecs and compare sorted output to the same five literals. Strong, 100/100. Citation: `19-REVIEWS.md`, reviewed 2026-08-31. |
| A zero-exit non-data object needs its own invalid-envelope fixture. | D-14 permits only a raw array or object with list-valued `data`; a `{"error": ...}` object can arrive with exit 0 and otherwise bypass process-failure coverage. Combining it with a nonzero exit would confound process and envelope guards. | Assume error envelopes always exit nonzero; let a missing `data` key raise generically. Neither proves the stable envelope contract. | **Accept:** one-factor return-code-zero error-object arm requiring `invalid envelope`. Strong, 97/100. Citation: `19-REVIEWS.md`, reviewed 2026-08-31. |

Integrated logic audit: the accepted changes either reuse an existing seam or tighten evidence at a declared public boundary. None adds a module, dependency, installed mutation, Phase 20 identity, or Phase 21 parity/cutover work. Selection bias is controlled by applying the same one-factor rule to supporting and failing fixtures; confidence is lower only where the external `bd` error-envelope behavior is a permitted defensive case rather than a reproduced live emission.

## Ponytail whole-plan audit

- **Plan rung:** 2 holds for the adapter and 4 holds for the manifest. The existing deep `sync.py` module and native gsd-core seam already own every required responsibility.
- **Minimum plans/tasks:** One plan and two tasks are sufficient. Task 1 proves the full public adapter path; Task 2 can run only after that verb exists and then declares it. Splitting creates cross-plan overlap on `test_sync.py`; merging removes the independent RED gate for the manifest contract.
- **Minimum files:** Five already-tracked files. `test_sync.py` is shared sequentially inside one plan; `CHANGELOG.md` and `README.md` are mandatory same-task truth maintenance for the `0.5.0` source contract. No fixture, helper, wrapper, runtime-mirror file, or standalone docs task is added.
- **Deletion/reuse choices:** Reuse `SAFE_BD_ID_RE`, `run_bd`, `_task_description`, `main`, existing stdout/stderr capture, and the installed validator. No existing production code is deleted because Patch 2 retirement is Phase 21.
- **Why neither task can be removed:** Without Task 1, RES-02/RES-03 and the actual adapter verb do not exist. Without Task 2, RES-01 and native resolver selection do not exist.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Live Beads content resolves losslessly through the stdlib adapter | 1, 2 | COVERED | Adapter contract first, native declaration second. |
| REQ | RES-01 | Approved bootstrap and separate id | 2 | COVERED | Exact literal, real temporary-layout execv proof, outer-timeout proof, and installed validator. |
| REQ | RES-02 | Lossless five-field normalization | 1 | COVERED | Writer round trip and normalization matrix. |
| REQ | RES-03 | Every invalid path halts without PLAN fallback | 1 | COVERED | One-factor fail-closed matrix. |
| RESEARCH | — | Existing deep adapter; no new module/dependency | 1 | COVERED | Ponytail rung 2. |
| RESEARCH | — | Exact writer inverse and gsd-core criteria semantics | 1 | COVERED | D-01 through D-12. |
| RESEARCH | — | Raw and versioned Beads envelopes, one matching row | 1 | COVERED | D-13/D-14. |
| RESEARCH | — | Sole native resolver declaration, stdlib locator, capability version, and truthful source docs | 2 | COVERED | D-17 plus live issue/version ownership. |
| RESEARCH | — | Public CLI tests, exact argv spy, isolated faults | 1 | COVERED | D-18/D-19. |
| CONTEXT | D-01 | No duplicated extracted prose in description | 1 | COVERED | Exact mapping assertions. |
| CONTEXT | D-02 | Preserve retained prose/sections/order | 1 | COVERED | Canonical producer control plus independent leading-prose/unknown/near-match arms. |
| CONTEXT | D-03 | No metadata synthesis | 1 | COVERED | Exact key/value assertions. |
| CONTEXT | D-04 | Source and retained descriptions nonblank | 1 | COVERED | Unusable-content arms. |
| CONTEXT | D-05 | Smallest writer inverse | 1 | COVERED | Local fence-aware scan. |
| CONTEXT | D-06 | Canonical H2 delimiters; near matches retained | 1 | COVERED | Isolated heading fixtures. |
| CONTEXT | D-07 | Any order, duplicate halt, fence protection | 1 | COVERED | Isolated heading/fence fixtures. |
| CONTEXT | D-08 | Structured criteria stays separate | 1 | COVERED | Exact five-field output. |
| CONTEXT | D-09 | Canonical Read First list only | 1 | COVERED | Valid and malformed list arms. |
| CONTEXT | D-10 | Match splitCriteria semantics | 1 | COVERED | LF/CRLF/marker/blank/order/duplicate cases. |
| CONTEXT | D-11 | Criteria type and optional section rules | 1 | COVERED | Missing/null/string/wrong-type cases. |
| CONTEXT | D-12 | Preserve Verify/Done internal newlines | 1 | COVERED | Scalar assertions. |
| CONTEXT | D-13 | Existing id grammar and fixed argv | 1 | COVERED | Fullmatch plus exact spy. |
| CONTEXT | D-14 | Two containers, exactly one matching object row | 1 | COVERED | Independent envelope/cardinality/type/id arms. |
| CONTEXT | D-15 | Named failures nonzero, bounded stderr, empty stdout | 1 | COVERED | Public failure matrix. |
| CONTEXT | D-16 | Exactly five success keys, no extra interface | 1 | COVERED | Exact set equality. |
| CONTEXT | D-17 | Approved manifest bootstrap and timeout | 2 | COVERED | Exact `[-c, bootstrap, id]` argv, direct `sys.argv[1]` proof, real execv transfer, missing-script, timeout, and validator assertions. |
| CONTEXT | D-18 | Writer round trip, argv spy, JSON-only stdout | 1 | COVERED | Primary tracer oracle. |
| CONTEXT | D-19 | One-variable negative fixtures | 1 | COVERED | Test organization and acceptance gate. |

Deferred ideas: none. Phase 20 identity and Phase 21 installed cutover/Patch 2 retirement are other-phase exclusions, not missing rows.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| gsd-core resolver → Python bootstrap | Capability-authored executable and argv cross into a subprocess; the id must remain one typed element. |
| sync.py → bd CLI | Untrusted tracker id reaches an external process; no shell or option injection is allowed. |
| bd stdout/stderr → resolver JSON | External bytes and authored content cross into executable task instructions and protocol output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-19-01 | Tampering / Elevation | `resolve_task_content` id and `run_bd` argv | high | mitigate | Reuse `SAFE_BD_ID_RE.fullmatch`; invoke exactly `bd show <id> --json` as typed argv with no shell; spy the full call. |
| T-19-02 | Tampering | Beads JSON/envelope/content | high | mitigate | Strict UTF-8/JSON, container, cardinality, plain-object, matching-id, source-type, heading, list, and usability validation before emitting stdout. |
| T-19-03 | Information disclosure | Resolver stdout/stderr protocol | medium | mitigate | Success stdout is exactly one five-key JSON object; failure stdout is empty and the single stderr diagnostic is bounded to 2000 characters. |
| T-19-04 | Denial of service | Hung/failing `bd` or resolver process | high | mitigate | Resolver-only `run_bd(..., timeout=8)` fires before the native 10000 ms outer bound; assert the strict ordering, propagate both failure layers nonzero, and never retry or fall back. |
| T-19-SC | Tampering | Package/install surface | low | accept | No npm/pip/cargo install occurs; existing Python stdlib, bd, and installed gsd-core are the complete stack. |
</threat_model>

## Pre-mortem

| Likely failure | Early catch | Mitigation |
|---|---|---|
| Parser drops or duplicates authored sections | Task 1's `_task_description` round trip fails RED/GREEN | Preserve slices verbatim and compare exact five-field output. |
| A malformed/failing tracker path leaks a partial success | One-factor negative arm writes stdout or returns zero | Build the object fully before the sole success print; centralize bounded error return. |
| Outer resolver timeout kills the adapter before its own diagnostic | Task 1's spy/order assertion fails | Reuse the existing timeout parameter with resolver-only value 8 seconds; keep shared 15-second behavior unchanged elsewhere. |
| Manifest is schema-valid but points at the wrong command/path | Task 2 literal assertions fail even if validator passes | Assert exact prefix, bootstrap tokens, whole-element id, verb, and timeout in addition to `validateCapability`. |

<verification>
1. Before Task 1, `bash -lc "set -euo pipefail; cd /home/dd/projects/gsd-beads; bd show gsd-beads-byp --json | jq -e '.[0].status == \"closed\"'; cd plugins/beads-lifecycle/.gsd/capabilities/beads; python3 -m unittest discover -s tests -t tests"` exits 0; `set -euo pipefail` makes either an absent/non-closed `gsd-beads-byp` or a nonzero exact full-suite result halt autonomous execution before any Phase 19 mutation. After each task, rerun the exact full capability suite and require exit 0; lifecycle-dispatch repair remains owned only by `gsd-beads-byp` outside Phase 19.
2. `bash -lc 'set -euo pipefail; cd /home/dd/projects/gsd-beads; tracked_phase19_paths=$(git ls-files -- CHANGELOG.md README.md plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py | LC_ALL=C sort); expected_phase19_paths=$(printf "%s\n" CHANGELOG.md README.md plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py | LC_ALL=C sort); test "$tracked_phase19_paths" = "$expected_phase19_paths"'` exits 0; the audit is explicitly scoped to the exact five frontmatter paths and no runtime mirror.
3. `bash -lc 'set -euo pipefail; cd /home/dd/projects/gsd-beads/plugins/beads-lifecycle/.gsd/capabilities/beads; python3 -m unittest tests.test_sync.TestResolveTaskContent tests.test_sync.TestTaskContentResolverManifest -v'` exits 0, including array type assertions, resolver-only inner-timeout ordering, the zero-exit error-envelope arm, real temporary-layout bootstrap success, missing-script, and outer-timeout arms.
4. `git diff --check -- plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json CHANGELOG.md README.md` exits 0.
</verification>

<success_criteria>
- RES-01, RES-02, and RES-03 each have a passing focused automated proof.
- Every D-01 through D-19 decision is implemented by the cited task with no deferred or reduced behavior.
- The exact public adapter seam, typed subprocess call, and tracked manifest invocation agree by construction; exact tracked/project-installed/global-installed byte parity and active installed cutover remain explicitly unproved until Phase 21.
- The live SPEC-less edge result remains 3 applicable, 0 resolved, and 3 unresolved; RES-01/02/03 retain their unclassified/unresolved flagged rows through phase close.
- Only the five tracked files in frontmatter change; changelog/README stay integrated with Task 2, while no identity, installed cutover, Patch 2 inventory/wiring, dependency, PATH, cache, retry, telemetry, or gsd-core work appears.
- Plan:post creates one Beads ticket per task; the two tasks are never bundled into one issue.
</success_criteria>

<output>
Create `.planning/phases/19-native-resolver-contract-and-failure-boundary/19-01-SUMMARY.md` when done.
</output>
