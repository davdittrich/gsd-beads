---
phase: 17-config-code-truth
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py
  - plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py
  - plugins/beads-lifecycle/.gsd/capabilities/beads/GSD-CORE-PATCH.md
autonomous: true
requirements: [TRUTH-03]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On a gsd-core install whose `plan-phase.md` plan:post region contains no generic `kind == \"step\"` dispatch arm — the shipped 1.11.0 shape — `sync.py lifecycle-dispatch plan:post` still creates bd issues exactly as it does today. Detection failure never removes a dispatch."
    - "On a gsd-core install whose `plan-phase.md` plan:post region DOES contain a generic step-dispatch arm, `sync.py lifecycle-dispatch plan:post` performs no sync and prints a skip notice naming the exact workflow file path it probed."
    - "The probe returns not-detected against a fixture reproducing the shipped 1.11.0 `verify-work.md`, whose verify:post region already contains a `kind == \"step\"` line filtered to `ref.skill == \"secure-phase\"`. A whole-file grep would report detected there; that false positive would silently disable a required dispatch, and the probe must not have it."
    - "`execute:wave:pre`, `execute:wave:post` and `plan:pre` dispatch unconditionally regardless of probe result — PR #3687 covers neither wave point, and no upstream issue, PR or release covers them anywhere."
    - "`sync.py create-issues <plan>` invoked directly computes `allow_strip` from `beads.sync_mode`, while `lifecycle_dispatch`'s plan:post branch keeps passing the literal `False` regardless of config (D-03/D-06 asymmetry)."
    - "idempotency — `check_native_step_dispatch` is read-only and pure: two consecutive calls against an unchanged workflow file return the same exit code and print the same line, and neither call writes to any file."
    - statement: "concurrency — when the hook path and a native gsd-core dispatch both reach plan:post for the same phase, at most one of them performs a strip decision on a given PLAN.md, and the probe holds no cross-call state."
      verification: backstop
    - "Every added code path is fail-open: a missing workflow file, an unreadable workflow file, an unresolvable region, or any unexpected exception leaves `lifecycle_dispatch` returning 0 with at most one notice line."
  prohibitions:
    - statement: "MUST NOT let a failed or ambiguous native-dispatch detection enable the destructive strip. A detection error must degrade to the conservative side — keep dispatching, keep `allow_strip=False` on the hook path — never to a path where PLAN.md task prose is deleted because a probe could not read a file."
      category: safety
      status: resolved
      verification: judgment
  artifacts:
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      provides: "check_native_step_dispatch probe, its lifecycle_dispatch gate, read_sync_mode, and the config-governed allow_strip at the native CLI call site"
      contains: "def check_native_step_dispatch"
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py"
      provides: "Probe detection tests including the 1.11.0 false-positive guard, and the D-03/D-06 asymmetry pins"
      contains: "class TestNativeStepDispatchProbe"
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/GSD-CORE-PATCH.md"
      provides: "The probe mechanism, the exact text it looks for, and its revert condition"
      contains: "#3687"
  key_links:
    - from: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      to: "$CLAUDE_CONFIG_DIR/gsd-core/workflows/plan-phase.md"
      via: "check_native_step_dispatch reads the installed workflow file and scans the point's own dispatch region"
      pattern: "plan-phase\\.md"
    - from: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      to: ".planning/config.json"
      via: "main()'s create-issues branch computes allow_strip from read_sync_mode"
      pattern: "read_sync_mode"
---

<objective>
Close TRUTH-03: the `lifecycle-dispatch` PostToolUse hook stays correct when gsd-core ships PR
**#3687**, which adds generic `kind == "step"` dispatch at `plan:post` and `verify:post`. Merged
to `next` at commit `ea59430` on 2026-08-19, 6h50m after the v1.11.0 release cut — so it is
unreleased today and lands at the next cut, and this plan must be correct both before and after.

Two distinct problems, both answered here:

1. **Double dispatch.** Once native dispatch exists at those two points, the hook fires *and*
   gsd-core dispatches. `create_issues` is idempotent so no duplicate issues appear, but the work
   is wasted and the `additionalContext` output doubles.
2. **The `allow_strip` protection is bypassed, silently.** Native dispatch invokes the
   `beads-sync` *skill*, which runs `sync.py create-issues <plan>` — a CLI verb with no strip
   flag, so `create_issues`' `allow_strip=True` default applies and `strip_task_bodies` returns
   via the native path even though the hook path deliberately disables it. **The ruling this
   requirement must record, recorded:** an explicit registry dispatch IS a different principal
   from a string-matched hook, so the strip is permitted on the native path and governed by
   `beads.sync_mode` (D-06), while the hook path stays permanently conservative (D-03).

`execute:wave:pre` and `execute:wave:post` are **not** covered by #3687 and have no upstream fix
released, merged, or in an open PR. The hook remains necessary there and must not be deleted.

Purpose: this is time-boxed by a release that can land at any cut. Re-run the upstream status
check immediately before the milestone ships.

Output: a read-only, fail-open native-dispatch probe; a gate on two of the five dispatch points;
`beads.sync_mode` wired to the native path's strip decision; and `GSD-CORE-PATCH.md` recording the
mechanism and its revert condition.

bd parent issue: `gsd-beads-he1` (P1).

**Cross-decision note, stated so no reader has to infer it:** D-06 needs a `beads.sync_mode`
reader, and this plan lands before `17-03` (TRUTH-01), which owns the `capability.json`
declaration change and the doc sweep. This plan therefore adds the two-line `read_sync_mode`
accessor as a prerequisite of D-06's wiring; `17-03` owns everything else about the key. The
intermediate state is safe: until `17-03` lands, `capability.json` still declares `off`, and an
on-disk `"off"` yields `allow_strip = ("off" != "mirror")` = `True`, which is exactly the
`allow_strip=True` default that call site has today. No behavior regresses mid-phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-config-code-truth/17-CONTEXT.md
@.planning/phases/17-config-code-truth/17-PATTERNS.md
@.planning/phases/17-config-code-truth/17-RESEARCH.md
@.planning/phases/17-config-code-truth/17-01-SUMMARY.md
</context>

## Alternatives Considered

Mechanism choice: how the hook learns that the installed gsd-core now dispatches `kind == "step"`
hooks natively at `plan:post` and `verify:post`.

- **Region-scoped marker probe on the installed workflow files (CHOSEN).** Read
  `$CLAUDE_CONFIG_DIR/gsd-core/workflows/{plan-phase,verify-work}.md`, isolate the region that
  belongs to the point's own `render-hooks <point> --raw` call, and look for a generic step arm
  there. Source: the PR itself — <https://github.com/open-gsd/gsd-core/pull/3687> (merged to
  `next`, commit `ea59430`, 2026-08-19; re-verified live 2026-08-20) — plus the installed
  1.11.0 files themselves, `` `~/.claude/gsd-core/workflows/plan-phase.md` `` and
  `` `~/.claude/gsd-core/workflows/verify-work.md` `` (read 2026-08-20), which supply the exact
  pre-#3687 shape the probe must classify as not-detected. **Decided by:** simplicity/LOC — it is
  the function shape already in this file twice (`check_shipmd_patch`'s read-only, fail-open,
  name-the-path-checked discipline), and it is the only option whose failure mode is bounded and
  pre-declared: a false negative degrades to today's working double dispatch.

- **Version-sniffing the installed gsd-core.** Compare the installed version against a hardcoded
  "ships in 1.12.0" threshold. Source: `` `~/.claude/gsd-core/VERSION` `` reads `1.11.0`
  (checked 2026-08-20), and the release that will carry #3687 does not exist yet — so the
  threshold constant cannot be filled in today without guessing. **Decided by:** maintenance
  overhead — a stale or wrong threshold makes the whole design wrong *silently*, with nothing in
  the system re-checking it, which is precisely the failure class that forced this milestone's
  roadmap revision. Explicitly rejected by CONTEXT.md D-05.

- **Accept permanent double dispatch and add no detection at all.** Zero new code; duplicate
  issues are already impossible because `create_issues` is idempotent, a property pinned by
  `` `tests/test_sync.py` `` `class TestIdempotency` and documented in `` `CHANGELOG.md` ``
  §0.3.1 (2026-08-19). **Decided by:** it does not meet the requirement. TRUTH-03 has two
  problems and this option touches neither — the requirement asks the hook to stay *correct*,
  not merely safe, and the `allow_strip` bypass (problem 2) is untouched by doing nothing.
  It also leaves no signal at the moment the hook becomes redundant.

**Sub-choice within the chosen mechanism — region-scoped versus whole-file grep.** This is
settled by evidence, not preference: a whole-file `kind == "step"` grep is a **false positive on
the shipped 1.11.0 tree today**. `plan-phase.md` carries three such occurrences (at the plan:pre
generic contract, the auto-chain UI branch, and the intel step read) and `verify-work.md` carries
one at its verify:post region, filtered to `ref.skill == "secure-phase"`. A whole-file grep would
report "native dispatch present" on an install that has none, the hook would stand down, and the
sync would be **missed** — the one failure direction D-05 forbids. Region scoping plus a
capId/skill-filter exclusion classifies both shipped files correctly as not-detected.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `$CLAUDE_CONFIG_DIR` -> filesystem read | An environment variable selects which runtime home's workflow files the probe reads |
| installed gsd-core workflow prose -> dispatch decision | Third-party file content decides whether this capability performs a lifecycle dispatch |
| `.planning/config.json` -> strip decision | A user-editable config value decides whether irreversible PLAN.md prose deletion is permitted on the native path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-02-01 | Denial of Service | `check_native_step_dispatch` -> `lifecycle_dispatch` | high | mitigate | A false-positive detection silently disables a required dispatch — the phase's own reporter already lost 6 plans / 16 tasks of tickets to a silent skip. Mitigated by region scoping plus excluding `capId ==` / `ref.skill ==` qualified lines, and pinned by a test fixture that reproduces the shipped 1.11.0 shape of BOTH workflow files and asserts not-detected. |
| T-17-02-02 | Tampering | native `create-issues` path -> `strip_task_bodies` | high | mitigate | Irreversible PLAN.md prose deletion must never be reachable from a weak principal. The hook path keeps its literal `allow_strip=False` (D-03) and only the explicit CLI/native path consults config (D-06); the existing live `check_execute_plan_patch() == 0` re-gate stays untouched as the last line of defence. Task 3 pins both halves. |
| T-17-02-03 | Elevation of Privilege | `$CLAUDE_CONFIG_DIR` path resolution | low | accept | A caller who can set `CLAUDE_CONFIG_DIR` can point the probe at a crafted file and suppress a dispatch. Accepted: that caller already controls the process environment and could simply not run the hook, so no privilege is gained. Residual risk reduced by reusing the existing WR-03 discipline — every probe message names the exact path it read, so a wrong runtime home is visible in the output rather than silent. |
| T-17-02-04 | Information Disclosure | probe output | low | accept | The probe prints an absolute path under the user's own runtime home into the hook's `additionalContext`. Same exposure the two existing patch checkers already have; no new class of data is surfaced. |
| T-17-02-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed — stdlib only. RESEARCH.md's Package Legitimacy Audit records "not applicable"; there is no `[ASSUMED]`/`[SUS]` entry to gate. |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end — the hook stands down at plan:post on a native-dispatch gsd-core, and only then</name>
  <precondition>`plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json` declares version `0.4.0` and `diff -rq .gsd/capabilities/beads/ plugins/beads-lifecycle/.gsd/capabilities/beads/` is silent (plan 17-01 Task 3) — otherwise any claim this task makes about hook behavior describes code the hook does not run.</precondition>
  <reversibility rating="reversible">One new read-only function plus one branch in `lifecycle_dispatch`; undo is deleting both.</reversibility>
  <files>
    plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py,
    plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py,
    plugins/beads-lifecycle/.gsd/capabilities/beads/GSD-CORE-PATCH.md
  </files>
  <read_first>
    - plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py — lines 2049-2111 (`check_shipmd_patch`: the exact three-case return contract, `CLAUDE_CONFIG_DIR` resolution idiom, and the WR-03 discipline of naming the probed path), 679-760 (`lifecycle_dispatch`, especially the `plan:post` branch at 739-749 and the outer `except Exception`), 108-127 (the marker constants and `LIFECYCLE_DISPATCH_POINTS`)
    - ~/.claude/gsd-core/workflows/plan-phase.md — lines 1359-1402 (`## 13e.` — the whole plan:post region as shipped in 1.11.0: `render-hooks plan:post --raw` at 1367, then a `kind == "gate"` loop only, no step arm) and lines 449, 488, 644 (the three `kind == "step"` occurrences that live OUTSIDE that region and are exactly why a whole-file grep is wrong)
    - ~/.claude/gsd-core/workflows/verify-work.md — lines 529-545 (`render-hooks verify:post --raw` at 529, then the `kind == "step"` line at 533 filtered to `ref.skill == "secure-phase"` — the second false-positive source)
    - plugins/beads-lifecycle/.gsd/capabilities/beads/GSD-CORE-PATCH.md — the "Scope" section and Patch 1's revert-condition prose, for the shape this task's new entry must match
    - plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py — lines 2937-2993 (`class TestCheckShipmdPatch`: the tempfile + `contextlib.redirect_stdout` shape) and 3747-3832 (`class TestLifecycleDispatchRouting`: how dispatch branches are asserted)
  </read_first>
  <behavior>
    - RED first. Add `class TestNativeStepDispatchProbe`. Cases:
      - A fixture workflow file whose point region contains an unqualified generic step arm ->
        probe reports detected (exit code distinct from not-detected) and its printed line names
        the fixture's absolute path.
      - A fixture reproducing the shipped 1.11.0 `plan-phase.md` plan:post region (gate loop
        only) plus the three out-of-region `kind == "step"` lines -> probe reports NOT detected.
        This is the whole-file-grep false positive, pinned.
      - A missing file path -> NOT detected, one message naming the path, no exception.
      - A file that cannot be decoded as UTF-8 -> NOT detected, one message naming the path, no
        exception.
      - A fixture whose region ends before any step arm (the arm appears only after the next
        `##` heading) -> NOT detected. Failing to find it must not be a failure to dispatch.
    - Then `class TestLifecycleDispatchNativeGate` (or extend the existing routing class):
      `lifecycle-dispatch plan:post` against a probe stubbed to detected performs no
      `create_issues` call and prints a skip notice; against a probe stubbed to not-detected it
      performs exactly the calls it performs today.
  </behavior>
  <action>
    Add `check_native_step_dispatch(point, workflow_path_override=None)` to `scripts/sync.py`,
    placed beside the existing patch checkers and following their contract exactly: read-only,
    never raises, resolves its default path via
    `os.environ.get("CLAUDE_CONFIG_DIR", str(Path.home() / ".claude")) / "gsd-core" / "workflows"`,
    and names the exact path it probed in every message it prints (WR-03 — a report must never
    read as "checked everywhere" when only one runtime home was checked).

    Keep it a SEPARATE function, not an entry in the patch table that plan `17-04` builds. The
    two answer different questions with opposite polarity: the patch table asks "does our local
    patch still exist" (missing is bad), this asks "does upstream now do this natively" (present
    is good). Sharing one table would produce a misleading unified pass/fail semantic.

    Map the point to its workflow file: `plan:post` -> `plan-phase.md`, `verify:post` ->
    `verify-work.md`. Any other point is not probed at all.

    Detection rule — region-scoped, because a whole-file scan is a verified false positive on the
    shipped 1.11.0 tree:
    - Anchor: the first line containing the literal `render-hooks <point> --raw`.
    - Region: from the anchor to the earlier of (a) the next line starting a level-two markdown
      heading that is not inside a fenced block, and (b) 120 lines past the anchor. The fence
      awareness matters — `verify-work.md` prints heading-shaped lines inside fenced output
      templates, so a naive heading scan terminates the region far too early.
    - Detected when the region contains a line mentioning a `kind` equal to the step kind that
      does NOT also carry a `capId ==` or `ref.skill ==` qualifier on the same line. The
      qualifier exclusion is what correctly classifies `verify-work.md`'s existing
      secure-phase-only arm as not-generic.
    - Every miss — no anchor, no region, no file, unreadable file — returns not-detected. That
      degrades to today's working double dispatch, which is the only acceptable failure
      direction (D-05).

    Gate the `plan:post` branch of `lifecycle_dispatch` on the probe: when native dispatch is
    detected, skip the `create_issues` loop and print one notice to stderr (benign skips go to
    stderr so they stay out of `additionalContext`; the hook promotes stdout only). When not
    detected, behave exactly as today. Do not touch `plan:pre` or either `execute:wave` branch.

    Record the mechanism in `GSD-CORE-PATCH.md` in the same commit: what the probe looks for,
    which two points it gates, that the two wave points are deliberately ungated because no
    upstream work covers them anywhere, and the revert condition — when the probe reports
    detected on a released gsd-core for both points, the corresponding hook entries can be
    retired, but the hook itself cannot be deleted while the wave points remain uncovered.
  </action>
  <verify>
    <automated>cd plugins/beads-lifecycle/.gsd/capabilities/beads &amp;&amp; python3 -m unittest tests.test_sync.TestNativeStepDispatchProbe -v 2>&amp;1 | grep -qE '^OK'</automated>
    <automated>cd plugins/beads-lifecycle/.gsd/capabilities/beads &amp;&amp; python3 -m unittest discover -s tests -t tests 2>&amp;1 | grep -qE '^OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def check_native_step_dispatch' scripts/sync.py` is 1.
    - `python3 -m unittest tests.test_sync.TestNativeStepDispatchProbe -v` reports OK with at
      least five test methods and zero skips.
    - Behavior assertion against the REAL installed 1.11.0 tree: calling
      `sync.check_native_step_dispatch("plan:post")` and
      `sync.check_native_step_dispatch("verify:post")` with no override on this machine both
      return the not-detected result, and each prints a line containing the absolute path of the
      workflow file it read.
    - Behavior assertion, live: `python3 .../sync.py lifecycle-dispatch plan:post` run in a
      fixture project on this machine still creates bd issues — the probe must not have disabled
      it on a gsd-core that does not dispatch natively.
    - Fail-open assertion: `sync.check_native_step_dispatch("plan:post", "/nonexistent/x.md")`
      returns without raising and its output names `/nonexistent/x.md`.
    - `grep -c '3687' GSD-CORE-PATCH.md` is at least 1, and the surrounding text names both
      gated points and states the revert condition.
    - Full suite exits 0, reports `OK`, and the test count is strictly greater than at the end of
      plan 17-01.
  </acceptance_criteria>
  <done>The hook skips plan:post only when the installed workflow file genuinely carries a generic step arm in that point's own region; every other outcome — including both shipped 1.11.0 files — leaves dispatch exactly as it is today, and GSD-CORE-PATCH.md records why.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate verify:post, and pin the four points that must stay unconditional</name>
  <files>
    plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py,
    plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py
  </files>
  <read_first>
    - plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py — `lifecycle_dispatch` as edited by Task 1, and `LIFECYCLE_DISPATCH_POINTS` at 121-127 with its comment requiring the hook's own list to mirror it
    - plugins/beads-lifecycle/hooks/lifecycle-dispatch.sh — the embedded `POINTS` tuple (read-only for this task; no edit is required because the hook passes the point through and every gating decision lives in `sync.py`)
    - ~/.claude/gsd-core/workflows/verify-work.md — lines 529-545, the exact pre-#3687 shape the false-positive guard fixture must reproduce
    - .planning/ROADMAP.md — Phase 17 Success Criterion 7
  </read_first>
  <behavior>
    - `verify:post` gated identically to `plan:post`: detected -> skip `regenerate_beads_md`
      with one stderr notice; not-detected -> today's behavior.
    - Regression pins, one test each: `plan:pre`, `execute:wave:pre` and `execute:wave:post`
      perform their existing call regardless of what the probe would return. Assert this with
      the probe stubbed to detected, so a future edit that widens the gate goes red.
    - False-positive guard: a fixture reproducing the shipped 1.11.0 `verify-work.md` verify:post
      region — including its `kind == "step"` line qualified by a specific skill — classifies as
      not-detected. Assert the same fixture WOULD match a naive whole-file scan, so the test
      documents what it is protecting against rather than merely passing.
    - `LIFECYCLE_DISPATCH_POINTS` still contains exactly the same five points in the same order,
      and the hook script's own embedded list still contains the same five. The invariant the
      module comment declares must hold after this plan.
  </behavior>
  <action>
    Extend the probe gate to the `verify:post` branch of `lifecycle_dispatch` using the same
    helper and the same skip-notice discipline as Task 1.

    Add the four regression pins above. These are the load-bearing half of Success Criterion 7:
    `execute:wave:pre` and `execute:wave:post` have no upstream fix released, merged, or in an
    open PR anywhere, so they must dispatch unconditionally, and a test — not a comment — is what
    keeps a future reader from "simplifying" the gate to cover all five points.

    Add the false-positive guard test for the shipped `verify-work.md` shape. Build the fixture
    from the region text rather than reading the installed file, so the test is deterministic on
    a machine with a different gsd-core installed.

    Add an assertion that the five-point list in `LIFECYCLE_DISPATCH_POINTS` and the five-point
    list embedded in `hooks/lifecycle-dispatch.sh` still agree — the module comment already
    declares that invariant, and this phase changes dispatch behavior without changing the list,
    so the invariant is worth pinning while the file is open. Do not edit the hook: reworking the
    hook matcher is out of scope for this milestone, and every gating decision this plan makes
    lives inside `sync.py`, which the hook already calls with the point as its only argument.
  </action>
  <verify>
    <automated>cd plugins/beads-lifecycle/.gsd/capabilities/beads &amp;&amp; python3 -m unittest tests.test_sync.TestNativeStepDispatchProbe tests.test_sync.TestLifecycleDispatchRouting -v 2>&amp;1 | grep -qE '^OK'</automated>
    <automated>cd plugins/beads-lifecycle/.gsd/capabilities/beads &amp;&amp; python3 -m unittest discover -s tests -t tests 2>&amp;1 | grep -qE '^OK'</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: with the probe stubbed to report detected, `lifecycle_dispatch("plan:pre")`
      still calls `beads_recall`, `lifecycle_dispatch("execute:wave:pre")` still calls
      `render_wave_status_block`, and `lifecycle_dispatch("execute:wave:post")` still calls
      `reconcile_stale_closed` — three separate passing tests.
    - Behavior assertion: with the probe stubbed to report detected,
      `lifecycle_dispatch("verify:post")` does not call `regenerate_beads_md` and returns 0.
    - Behavior assertion: the shipped-1.11.0 `verify-work.md` fixture classifies as not-detected,
      and the same test also asserts that a naive scan of the whole fixture WOULD find a step-kind
      mention — documenting the false positive the region scoping prevents.
    - Source assertion: `python3 -c "import sync;assert sync.LIFECYCLE_DISPATCH_POINTS == ('plan:pre','plan:post','execute:wave:pre','execute:wave:post','verify:post'), sync.LIFECYCLE_DISPATCH_POINTS"` exits 0.
    - `lifecycle_dispatch` still returns 0 on every path: a test invoking it for each of the five
      points with the probe raising an exception asserts a 0 return every time.
    - `git diff --quiet -- plugins/beads-lifecycle/hooks/lifecycle-dispatch.sh` — the hook is
      unchanged by this plan.
  </acceptance_criteria>
  <done>Both natively-covered points are gated, the three uncovered points are pinned unconditional by tests, and the shipped 1.11.0 false-positive shape is pinned as not-detected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-06 wiring — config governs the native CLI strip, never the hook strip</name>
  <reversibility rating="reversible">One accessor and one expression at a single dispatch line; undo is restoring the bare default.</reversibility>
  <files>
    plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py,
    plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py
  </files>
  <read_first>
    - plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py — lines 641-677 (`read_beads_config` and the two existing accessors `read_epic_per` / `read_beads_enabled`, including the comment explaining why each shipped default is written down once beside its key), 1291-1303 (`create_issues`' signature and the docstring paragraph that currently states the strip is not gated on the config key), 1370-1390 (the live `check_execute_plan_patch() == 0` re-gate), 2180-2286 (`main()`, especially the `create-issues` dispatch line and the `create_p` subparser)
    - plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-sync/SKILL.md — Step 3, which shells out `create-issues <plan path>` with no strip-related flag; this is why the flag must be computed internally rather than added to the CLI
    - plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py — lines 739-749, the hook path's literal `allow_strip=False` that must NOT change
    - plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py — lines 744-807 (`class TestCreateIssuesStripGate`) and 3711-3746 (`class TestReadBeadsEnabled`), the two analogs this task's tests extend
  </read_first>
  <behavior>
    - `read_sync_mode(project_root)` returns the stored `beads.sync_mode` string, defaulting to
      `"authoritative"` when the config file is absent, unreadable, malformed, carries no `beads`
      object, carries no such key, or carries a non-string value.
    - `sync.py create-issues <plan>` in a project configured to the mirror value leaves every
      task body in PLAN.md intact even when the read-path patch is present.
    - The same command in a project configured to the authoritative value, with the patch
      present, strips newly-created task bodies exactly as it does today.
    - The same command with no config file at all behaves as the authoritative case — the
      shipped default is unchanged.
    - `lifecycle_dispatch`'s plan:post branch leaves task bodies intact in a project configured
      to the authoritative value. The hook path is not governed by config; this is the D-03
      asymmetry and it is deliberate.
    - The live `check_execute_plan_patch() == 0` re-gate still suppresses the strip when the
      patch is absent, regardless of config value — config can only ever subtract permission,
      never add it.
  </behavior>
  <action>
    Add `read_sync_mode(project_root)` next to `read_epic_per` and `read_beads_enabled`, in the
    same one-line-over-`read_beads_config` shape, with the shipped default written down beside
    the key exactly as the neighbouring comment requires. Default is the authoritative value.

    Change `main()`'s `create-issues` dispatch to compute the strip permission from the config
    value rather than accepting `create_issues`' default: resolve the project root from the plan
    path's parent using the existing `find_project_root`, read the mode, and pass
    `allow_strip=` the result of comparing that mode against the mirror value. Do NOT add a CLI
    flag — `beads-sync/SKILL.md` Step 3 invokes this verb with no strip argument, and a flag
    would add an untested cross-product (flag present / absent / config value) for no caller that
    needs it. Resolve the project root inside the same fail-open posture the rest of the file
    uses: an unresolvable root falls back to the shipped default rather than raising.

    Leave `lifecycle_dispatch`'s plan:post call passing the literal `False`. This asymmetry is
    D-03 and it is the answer to the v1.3.0 incident: the hook's trigger is a substring match
    against a shell command, a materially weaker principal than an explicit dispatch, and
    irreversible prose deletion must never be authorized by pattern matching. Update the
    `create_issues` docstring paragraph that currently states the strip is not gated on the
    config key — it becomes false with this change — replacing it with a statement of the
    asymmetry: which paths consult config, which path never does, and why. That docstring is part
    of this code change and lands in this commit.
  </action>
  <verify>
    <automated>cd plugins/beads-lifecycle/.gsd/capabilities/beads &amp;&amp; python3 -m unittest tests.test_sync.TestCreateIssuesStripGate -v 2>&amp;1 | grep -qE '^OK'</automated>
    <automated>cd plugins/beads-lifecycle/.gsd/capabilities/beads &amp;&amp; python3 -m unittest discover -s tests -t tests 2>&amp;1 | grep -qE '^OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def read_sync_mode' scripts/sync.py` is 1.
    - Behavior assertion: a fixture project whose `.planning/config.json` sets the beads sync
      mode to the mirror value, with the read-path patch present, runs
      `python3 .../sync.py create-issues <plan>` and the resulting PLAN.md still contains the
      task body elements that the authoritative run removes. The authoritative arm of the same
      test shows them removed. The two arms differ only in the config value.
    - Behavior assertion: with no `.planning/config.json` at all, the run behaves identically to
      the authoritative arm.
    - Behavior assertion: `lifecycle_dispatch("plan:post")` in the authoritative fixture leaves
      task bodies intact — proving the hook path did not inherit the config gating.
    - Source assertion: the plan:post branch of `lifecycle_dispatch` still passes a literal
      false value to `create_issues`, checked by
      `sed -n '/elif point == "plan:post"/,/elif point == "execute:wave:pre"/p' scripts/sync.py | grep -c 'allow_strip=False'` returning 1.
    - Source assertion: `grep -c 'add_argument' scripts/sync.py` is unchanged from before this
      task — no CLI flag was added.
    - Doc assertion: the `create_issues` docstring no longer claims the strip is ungated by the
      config key, verified by
      `! sed -n '/^def create_issues/,/^    if not bd_available/p' scripts/sync.py | grep -Eq 'read by nothing'`.
    - Full suite exits 0 and reports `OK` with a test count strictly greater than at the end of
      Task 2.
  </acceptance_criteria>
  <done>The explicit CLI path's strip decision is governed by `beads.sync_mode`; the hook path's is not, and both halves of that asymmetry are pinned by tests and stated in the docstring.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created or changed by THIS plan (absent from `.planning/intel/API-SURFACE.md`
because they do not exist yet):

- `check_native_step_dispatch(point, workflow_path_override=None)` — new function in `scripts/sync.py`
- `read_sync_mode(project_root)` — new function in `scripts/sync.py`
- `class TestNativeStepDispatchProbe` — new test class in `tests/test_sync.py`
- `class TestLifecycleDispatchNativeGate` — new test class in `tests/test_sync.py` (or new methods on the existing `TestLifecycleDispatchRouting`)
- New `GSD-CORE-PATCH.md` section documenting the native-dispatch probe and its revert condition

<verification>
- Full suite green, test count strictly above plan 17-01's ending count.
- Against the real installed 1.11.0 tree, the probe reports not-detected for BOTH points.
- A live `lifecycle-dispatch plan:post` on this machine still creates bd issues.
- The hook script is byte-unchanged by this plan.
- `diff -rq` between the runtime overlay and the tracked source stays silent after the
  capability re-sync at the end of this plan.
</verification>

<success_criteria>
ROADMAP.md Phase 17 Success Criterion 7 is met: `plan:post` and `verify:post` dispatch from the
hook only on a gsd-core that does not dispatch them natively, gated on a probe of the installed
workflow file rather than on a date or a version constant; `execute:wave:pre` and
`execute:wave:post` dispatch unconditionally; the gate is exercised by tests simulating both an
old and a new gsd-core; and the `allow_strip` ruling is recorded in the plan, in the code, and in
`GSD-CORE-PATCH.md` rather than discovered in production.
</success_criteria>

<output>
Create `.planning/phases/17-config-code-truth/17-02-SUMMARY.md` when done.
</output>
