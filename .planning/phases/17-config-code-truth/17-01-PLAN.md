---
beads_epic: gsd-beads-u67
phase: 17-config-code-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py
  - plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py
  - plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json
  - CHANGELOG.md
autonomous: true
requirements: [TRUTH-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A decimal-numbered phase resolves at plan:pre: in a fixture tree whose phase directory is `01.5-<slug>` and whose STATE.md carries `current_phase: 01.5`, `sync.py lifecycle-dispatch plan:pre` reports `1 matched, 0 unscoped` for an open bd issue whose description contains a path token present in that phase's ROADMAP section. Today the identical fixture reports `0 matched, 1 unscoped` while the integer-phase control arm reports `1 matched, 0 unscoped`."
    - "A decimal-numbered phase resolves at plan:post: `discover_plan_files` returns a non-empty mapping for a directory containing `01.5-01-PLAN.md`, and `get_phase_header` returns the `### Phase 1.5:` header text without raising."
    - "boundary — the phase-number normalizer strips leading zeros from the integer part ONLY and never from the fractional part: `01.5`->`1.5`, `07`->`7`, `10.1`->`10.1`, `010.1`->`10.1`, `1.05`->`1.05` (the `05` fraction is preserved verbatim)."
    - "adjacency — phase `1.5` and phase `15` never collide: the header pattern built for `1.5` does not match `### Phase 15:`, and the pattern built for `15` does not match `### Phase 1.5:`. Phase `1.5` and `1.50` are distinct inputs producing distinct patterns."
    - "empty — an empty or all-zero phase-number string does not raise out of the normalizer: `get_phase_header` still raises its existing semantic `ValueError` naming the absent header (the error `beads_recall` already catches), never a `ValueError` from a numeric conversion."
    - "encoding — phase-number matching is code-point string equality with no numeric or Unicode normalization applied; `PLAN_FILE_RE` keeps Python's Unicode-aware `\\d` class, preserving today's exact matching semantics (narrowing to `[0-9]` is deliberately NOT done — it is a behavior change outside TRUTH-04's scope)."
    - "ordering — `discover_plan_files` keys sort stably in lexicographic order and a decimal phase sorts between its base and the next integer (`01-01` < `01.5-01` < `02-01`), because `-` (0x2D) precedes `.` (0x2E)."
    - "precision — no `int()`, `float()`, or `Decimal()` is applied to a phase number anywhere on the fixed path; `10.1` and `10.10` therefore remain distinct strings rather than collapsing to one numeric value."
    - "idempotency — running `sync.py lifecycle-dispatch plan:pre` twice against an unchanged decimal-phase fixture tree produces a BEADS-RECALL.md identical apart from its `generated_at` frontmatter value."
    - statement: "concurrency — two lifecycle points dispatched in parallel against the same decimal phase directory must not interleave two writes to the same generated artifact; the phase-number normalizer itself holds no state and performs no I/O."
      verification: backstop
    - "The runtime mirror `.gsd/capabilities/beads/` and the git-tracked source `plugins/beads-lifecycle/.gsd/capabilities/beads/` are byte-identical after this plan, proven by `diff -rq` returning no output."
  prohibitions: []
  artifacts:
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      provides: "Widened PLAN_FILE_RE plus the two phase-number normalizers wired at all four break sites"
      contains: "def phase_regex_token"
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py"
      provides: "End-to-end decimal-phase tracer test plus boundary/adjacency/empty/ordering/precision coverage"
      contains: "class TestDecimalPhase"
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json"
      provides: "Version 0.4.0 — the bump that makes the runtime mirror re-sync"
      contains: "\"version\": \"0.4.0\""
    - path: "CHANGELOG.md"
      provides: "0.4.0 section opened for this milestone"
      contains: "## 0.4.0"
  key_links:
    - from: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      to: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      via: "get_phase_header and extract_phase_mentions both build their pattern from phase_regex_token instead of a numeric conversion"
      pattern: "phase_regex_token\\(phase_num\\)"
    - from: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      to: ".planning/phases/<decimal>-<slug>/"
      via: "_resolve_default_phase_dir matches STATE.md's current_phase against the directory prefix through phase_dir_prefix"
      pattern: "phase_dir_prefix"
---

<objective>
Close TRUTH-04: a phase with a decimal number (`1.5`, `01.5`, `10.1`, `11.1` — the form
`/gsd-phase --insert` produces) works at every beads lifecycle point instead of failing silently.

Purpose: this is the phase's own insurance policy. If upstream PR #3687 ships mid-phase and
TRUTH-03 has to jump the queue, the mechanism for that is `/gsd-phase --insert`, which emits
exactly the decimal phase number that is broken today. It is also the first commit to touch
`sync.py`, so it carries the `capability.json` 0.3.1 -> 0.4.0 bump every later plan in this phase
depends on for a non-stale runtime mirror (ROADMAP.md Phase 17, "Edit the tracked source" —
`capability update beads` reports `"upgraded"` for an equal-version no-op and copies nothing).

Output: a widened `PLAN_FILE_RE`, two small phase-number normalizers wired at all four break
sites, a real end-to-end regression test on a decimal fixture tree, `capability.json` at 0.4.0,
and a proven-identical runtime mirror.

bd parent issue: `gsd-beads-bzl` (P1).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-config-code-truth/17-CONTEXT.md
@.planning/phases/17-config-code-truth/17-PATTERNS.md
@.planning/phases/17-config-code-truth/17-RESEARCH.md
</context>

<assumption_delta_decision>
The `assumption-delta scan` fired (`detected: true`; signals `pluralization` on *also / second /
alongside*, `chosen` on *parameterized / chosen*). The genuine delta in this phase is **not** in
this plan — it is TRUTH-02's collapse of two named CLI verbs into one parameterized verb over a
table, recorded in `17-04-PLAN.md`.

For this plan the relevant question is narrower: **the phase number stops being an integer.**

- **Primary noun:** the *phase number as an opaque string token* (previously: an integer that
  happened to be spelled as a string).
- **Decision:** `promote`. The string is promoted to the primary representation everywhere on this
  path; the integer interpretation is demoted to what it always actually was — a leading-zero
  strip. No numeric type is introduced alongside the string (D-07 forbids it), so there is no
  add-alongside asymmetry to accumulate.
- **Rationale:** the value's only consumer is regex construction. A numeric type would buy nothing
  and would reintroduce both a parse that can raise and a formatting risk (`1.50`, `1.5000000001`).
- **Invariant test (accepted, Task 2):** every phase-number form this repo has ever produced —
  `01`, `07`, `17`, `10.1`, `11.1`, `01.5` — round-trips through `phase_regex_token` and
  `phase_dir_prefix` and resolves its own ROADMAP header, so a future phase reintroducing a
  numeric conversion goes red immediately.
</assumption_delta_decision>

## Alternatives Considered

Mechanism choice: how to make a decimal phase number usable in the three regexes and the one
directory-prefix comparison that currently break.

- **String leading-zero strip + `re.escape` (CHOSEN).** The value is only ever interpolated into a
  regex, so it never needs to be a number. `re.escape("1.5")` yields `1\.5`, keeping the literal
  dot literal; the existing `0*` prefix in the header pattern keeps `### Phase 01.5:` matching.
  The sibling `sota-numerics` capability shipped the equivalent widening for its own copy of this
  regex with a comment naming beads' pattern as the too-narrow one
  (`~/.gsd/capabilities/sota-numerics/scripts/check-alternatives.py:34-37`, read 2026-08-20;
  ReDoS discipline documented in the same file). Python's `re.escape` semantics confirmed against
  the current stdlib docs — <https://docs.python.org/3/library/re.html#re.escape> (Python 3.13,
  2026). **Decided by:** simplicity/LOC — two helper functions of two lines each, no parse that
  can raise, no formatting surface.

- **`decimal.Decimal` parsing.** Exact base-10 arithmetic with no binary rounding error, which is
  the standard answer whenever a decimal literal must survive a round trip
  (<https://docs.python.org/3/library/decimal.html>, Python 3.13, 2026). Rejected: it is the right
  tool for arithmetic and this path performs none. It also *loses* information the string carries
  — `Decimal("10.10") == Decimal("10.1")` is `True`, which would silently merge two distinct phase
  directories — and reintroduces an `InvalidOperation` raise on malformed input. **Decided by:**
  performance is a tie, so simplicity/LOC decides, and Decimal costs a parse plus a formatting
  step to get back to the string the regex actually needs.

- **`packaging.version.Version` / a semver-style parser.** Purpose-built for dotted numeric
  identifiers with correct ordering and normalization
  (<https://packaging.pypa.io/en/stable/version.html>, packaging 24.x, 2026). Rejected on
  **ecosystem support**: `sync.py` is deliberately pure stdlib (RESEARCH.md "Standard Stack": no
  new external libraries this phase), and adding a runtime dependency to a capability script that
  ships inside a plugin bundle is a distribution change out of all proportion to a two-line
  leading-zero strip. Its normalization would also collapse `10.10` to `10.1`, the same
  information loss as Decimal.

Not a mechanism choice, recorded for completeness: `PLAN_FILE_RE`'s widened shape copies the
sibling capability verbatim apart from preserving beads' existing capture group.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> regex construction | A phase-number token derived from a directory name or a `PLAN.md` filename is interpolated into a compiled regex |
| `.planning/STATE.md` -> directory resolution | `current_phase` frontmatter selects which phase directory every lifecycle point operates on |
| filesystem -> `discover_plan_files` | Directory entries are matched against `PLAN_FILE_RE` and become dict keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01-01 | Tampering | `get_phase_header`, `extract_phase_mentions` | high | mitigate | `re.escape()` the phase-number token before interpolation, so a literal `.` stays literal instead of becoming a wildcard that matches an unrelated header (`Phase 11X1:`). Task 1 wires it; Task 2 pins it with an explicit negative assertion. |
| T-17-01-02 | Denial of Service | `PLAN_FILE_RE` | medium | mitigate | The widened pattern `^(\d+(?:\.\d+)?-\d+)-PLAN\.md$` is anchored at both ends and every repeated group is separated by a mandatory literal (`.`, `-`), so no two quantifiers can match the same input span. No nested quantifiers, no catastrophic backtracking. Same discipline the sibling `sota-numerics` pattern documents. |
| T-17-01-03 | Elevation of Privilege | `_resolve_default_phase_dir` | medium | mitigate | The resolved directory is still selected by iterating `confined(project_root, ".planning", "phases")` and comparing name prefixes — never by joining `current_phase` onto a path. Widening the prefix comparison must not introduce a path join; Task 2 asserts a `current_phase` value containing a path separator resolves to `None`. |
| T-17-01-04 | Information Disclosure | `beads_recall` output | low | accept | A wider `PLAN_FILE_RE` makes more `*-PLAN.md` files discoverable within the already-confined `.planning/phases/` root. All discovered content is the user's own planning artifacts inside their own project; no new read surface is opened outside the confined root. |
| T-17-01-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs zero packages — `sync.py` is pure stdlib (`re`, `argparse`, `json`, `os`, `sys`, `pathlib`, all already imported). RESEARCH.md's Package Legitimacy Audit records "not applicable" for the same reason, so there is no `[ASSUMED]`/`[SUS]` package to gate. |
</threat_model>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end — one decimal phase resolves at plan:pre, wired through every break site</name>
  <beads-id>gsd-beads-u67.1</beads-id>
  <reversibility rating="reversible">A regex literal and two two-line helpers in one file; undo is a revert of that file.</reversibility>
  <files>
    plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py,
    plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py,
    plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json,
    CHANGELOG.md
  </files>
  <!-- beads: content synced to bd -- see `bd show gsd-beads-u67.1` -->
</task>

<task type="auto" tdd="true">
  <name>Task 2: Boundary, adjacency, precision and ordering coverage for the decimal path</name>
  <beads-id>gsd-beads-u67.2</beads-id>
  <files>
    plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py,
    plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py
  </files>
  <!-- beads: content synced to bd -- see `bd show gsd-beads-u67.2` -->
</task>

<task type="auto">
  <name>Task 3: Prove the runtime mirror re-synced from the tracked source</name>
  <beads-id>gsd-beads-u67.3</beads-id>
  <files>plugins/beads-lifecycle/.gsd/capabilities/beads/</files>
  <!-- beads: content synced to bd -- see `bd show gsd-beads-u67.3` -->
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created or changed by THIS plan (absent from `.planning/intel/API-SURFACE.md`
because they do not exist yet):

- `phase_regex_token(phase_num)` — new module-level function in `scripts/sync.py`
- `phase_dir_prefix(phase_num)` — new module-level function in `scripts/sync.py`
- `class TestDecimalPhase` — new test class in `tests/test_sync.py`
- `PLAN_FILE_RE` — existing constant, new pattern value `^(\d+(?:\.\d+)?-\d+)-PLAN\.md$`
- `capability.json` `version` — new value `0.4.0`
- `CHANGELOG.md` `## 0.4.0` — new section

<verification>
- Full suite green from BOTH trees, `Ran N tests` with N >= 172 (164 baseline + at least 8 new).
- `diff -rq` between the overlay and the tracked source is silent.
- A live decimal-phase `lifecycle-dispatch plan:pre` run matches its integer control arm.
- No `int()`/`float()`/`Decimal()` conversion of a phase number survives on the fixed path.
</verification>

<success_criteria>
ROADMAP.md Phase 17 Success Criterion 6 is met: a decimal-numbered phase resolves at all three
beads lifecycle points, exercised against this repo's own history forms (`10.1`, `11.1`) and the
`/gsd-phase --insert` form (`01.5`), with the fix pinned by tests rather than asserted in prose.
</success_criteria>

<output>
Create `.planning/phases/17-config-code-truth/17-01-SUMMARY.md` when done.
</output>
