---
beads_epic: gsd-beads-u67
phase: 17-config-code-truth
plan: 04
type: execute
wave: 4
depends_on: ["17-03"]
files_modified:
  - plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py
  - plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py
  - plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-recall/SKILL.md
  - plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-status/SKILL.md
  - plugins/beads-lifecycle/.gsd/capabilities/beads/GSD-CORE-PATCH.md
  - .planning/intel/API-SURFACE.md
  - CHANGELOG.md
autonomous: false
requirements: [TRUTH-02]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One reader serves both patch checks: the ~39-line body that exists twice today exists once, parameterized by target filename, marker constant, marker version and consequence text, and `sync.py` is measurably shorter than its 2286-line baseline."
    - "Both existing in-file call sites behave identically: the `plan:pre` pair still prints the ship-md report first and the execute-plan report second, and the live `strip_task_bodies` re-gate still compares the execute-plan check against zero with its semantics preserved verbatim."
    - "The assertions in `TestCheckShipmdPatch` and `TestCheckExecutePlanPatch` pass unedited — both Python function names survive as thin wrappers over the shared reader, because the four existing test mocks and the two in-file call sites bind to those names."
    - "The suite gains coverage rather than merely staying green: it reports strictly more tests than the 164 baseline, including at least one assertion on each marker's literal string and each present-message's version suffix — assertions that do not exist today, which is how commit `966315a` moved one marker from v1 to v2 with the suite still reporting 164/164 OK."
    - "The ship-md target is reachable and asserted from the CLI. That coverage lands before the CLI is changed, not after."
    - "adjacency — the table's two entries never collide: their keys are distinct, their marker constants are distinct literals, and a test asserts both, so a refactor that collapses to one shared marker constant goes red."
    - "empty — an empty-string path override falls back to the default resolved path exactly as it does today; a zero-byte target file reports the marker as absent rather than raising; a missing file and an undecodable file each report through the existing not-found and could-not-be-read paths."
    - "encoding — the target file is decoded as UTF-8 and the marker test is a plain code-point substring check, never a regex; an undecodable file degrades to the could-not-be-read report rather than raising out of the function."
    - "ordering — the merged reader is total: an unrecognized table key returns the same fail-open result as an unreadable file rather than raising, because both checks run inside `lifecycle_dispatch`'s single try/except alongside `beads_recall` and an exception there would take out the recall too."
  prohibitions:
    - statement: "MUST NOT report a patch as verified against a path the code did not actually read, and MUST NOT let the merged reader's message imply a broader check than was performed. A diagnostic that overstates what it verified is worse than no diagnostic — every message must continue to name the exact single path probed, so a report never reads as 'checked everywhere' when only one runtime home was checked."
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      provides: "PATCH_CHECKS table plus one parameterized reader; both wrapper functions retained"
      contains: "PATCH_CHECKS"
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py"
      provides: "Pre-merge coverage for the ship-md CLI path, both literal markers and both version suffixes"
      contains: "class TestPatchChecksTable"
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-status/SKILL.md"
      provides: "Step 2d invoking the surviving CLI verb"
      contains: "sync.py"
    - path: ".planning/intel/API-SURFACE.md"
      provides: "Regenerated CLI surface reflecting the collapsed verb"
      contains: "sync.py"
  key_links:
    - from: "plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-status/SKILL.md"
      to: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      via: "Step 2d shells out to the patch-check CLI verb by name at ship:pre"
      pattern: "scripts/sync\\.py"
    - from: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      to: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      via: "check_shipmd_patch and check_execute_plan_patch both delegate to the one parameterized reader"
      pattern: "PATCH_CHECKS"
---

<objective>
Close TRUTH-02: `check_shipmd_patch` and `check_execute_plan_patch` are served by one table-driven
reader. They are structural clones — ~39 body lines each, identical control flow, differing only
in filename, marker constant, marker version and four message strings, with roughly 50 of 78 total
body lines byte-identical. `lifecycle_dispatch` already calls them back-to-back at `plan:pre`,
which is exactly the table shape.

This lands last deliberately. It is the riskiest-per-unit-value change in the phase, it is P3
where the other three are P1, and it lands on a `sync.py` the other three plans have already
stabilised. It is also safe to do at all only because the prior worry resolved: gsd-core #3646 is
open, has no PR, and is gated behind #3647 which is also open with no PR — so
`check_execute_plan_patch` has a future, and this merge collapses duplication *between* two live
checkers rather than touching one scheduled for deletion.

Purpose: the duplication is not the real hazard. The real hazard is what the duplication hides —
the two markers are at **different versions** (ship at v2, execute-plan at v1) and **no test
asserts either marker's literal string**, which is exactly how commit `966315a` moved one from v1
to v2 with the suite still reporting 164/164 green. That edit could have been a typo and nothing
would have caught it. The merge is the occasion to close that blind spot, not the point of it.

Output: one parameterized reader over a small table of literals with a per-entry version field;
both Python function names retained as thin wrappers so every existing test and both in-file call
sites keep working unedited; one collapsed CLI verb with every caller updated in the same commit;
and five specific coverage additions that land *before* the CLI changes.

bd parent issue: `gsd-beads-t7a` (P3).

**Release-hygiene note:** ROADMAP.md Phase 17 lists five mechanical ship-step checks (version bump
and CHANGELOG entry for `966315a`, the changelog timeout mis-filing — closed by plan 17-03, the
withdrawn `v1.3.0` tag, the stale `~/.claude/gsd-local-patches/` backup, and the two-tree identity
plus test-count assertion). Per CONTEXT.md that debt is Claude's discretion to place; it is placed
in the ship step, where ROADMAP already enumerates it as commands, rather than as a fifth plan.
This plan does not duplicate it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-config-code-truth/17-CONTEXT.md
@.planning/phases/17-config-code-truth/17-PATTERNS.md
@.planning/phases/17-config-code-truth/17-RESEARCH.md
@.planning/phases/17-config-code-truth/17-03-SUMMARY.md
</context>

<!-- planner-discipline-allow: check-shipmd-patch -->
<!-- planner-discipline-allow: check-execute-plan-patch -->

<source_conflict>
## ROADMAP Success Criterion 4 versus CONTEXT.md D-08 — surfaced, not silently resolved

These two source artifacts disagree, and the disagreement is load-bearing enough that it gates a
task rather than being settled in prose.

- **ROADMAP.md Phase 17, Success Criterion 4** (written 2026-08-19) states that **both CLI
  subcommands and both flag spellings survive** the refactor, and calls this "load-bearing and
  untested today".
- **CONTEXT.md D-08** (written 2026-08-20, after the roadmap, in `/gsd-discuss-phase`) states the
  opposite: collapse to **one parameterized CLI verb, hard break, no aliases**, every caller
  updated in the same commit.

The discussion log records this as an informed, deliberate override: the "keep both verbs"
position was the *recommendation*, it was made **before** the callers were grepped, and it was
withdrawn once the grep established no README exposure and no caller outside this repo. Locked
user decisions supersede an earlier roadmap criterion, so this plan implements D-08.

**The substance of Criterion 4's concern is preserved, not discarded.** Its stated worry was that
unifying the CLI would keep the suite green while silently breaking `beads-status/SKILL.md` Step
2d, which invokes the ship-md verb by name. That worry is answered twice over: D-09 lands the
missing CLI coverage *before* the merge, and D-08 requires every caller updated in the same
commit. Criterion 4's literal "both subcommands survive" clause is what does not survive.

**Criterion 5 is untouched and fully honored** — both *Python function names* survive as thin
wrappers, so the assertions in `TestCheckShipmdPatch` and `TestCheckExecutePlanPatch` pass
unedited, and both in-file call sites keep their exact current spelling. D-08 is scoped to the
public CLI verbs; every caller it enumerates is a CLI call site.

Task 2 is a `checkpoint:decision` that puts this in front of the developer before the door is
walked through, because D-08 is rated `one-way`.
</source_conflict>

## Alternatives Considered

Mechanism choice: how two structurally identical detectors become one, and what happens to the
public CLI surface.

- **Table of literals + one parameterized reader, both wrappers retained (CHOSEN).** A
  module-level dict keyed by target name, each entry carrying filename, marker constant, marker
  version and its consequence text; one reader walks it; the two existing function names stay as
  one-line wrappers. Sources: the repo's own idiom for a small fixed variant set is a plain
  module-level tuple/dict of literals plus a direct dispatch — see `` `sync.py` `` lines 121-127
  (`LIFECYCLE_DISPATCH_POINTS`, 2026) and the analysis in
  `` `.planning/phases/17-config-code-truth/17-PATTERNS.md` `` (2026-08-20); the parameterized
  verb rides Python's standard subparser mechanism,
  <https://docs.python.org/3/library/argparse.html#sub-commands> (Python 3.13, 2026).
  **Decided by:** simplicity/LOC — it removes roughly 50 duplicated body lines, it is the only
  option that gives D-10's per-entry version field a home, and retaining the wrappers costs two
  lines while preserving four existing test mocks and two call sites unedited.

- **Shared private helper, both CLI verbs kept.** Extract the common body into one private
  function; the two public verbs and both flag spellings stay. Sources:
  `` `.planning/ROADMAP.md` `` Phase 17 Success Criterion 4 (2026-08-19) requires exactly this,
  and `` `.planning/phases/17-config-code-truth/17-DISCUSSION-LOG.md` `` Area 4 (2026-08-20)
  records it as the standing recommendation until the caller grep. **Decided by:** maintenance
  overhead — it removes the same duplicated lines but leaves two subparsers, two flag spellings
  and two dispatch branches permanently, each needing its own test, for a CLI surface with zero
  callers outside this repository. Superseded by CONTEXT.md D-08; surfaced to the developer at
  Task 2 rather than dismissed.

- **Collapse with a one-release alias window.** Ship the new verb, keep the old two as deprecated
  aliases for one release, then delete. Source: gsd-core's own removal precedent,
  `runtime.hostBehaviors.reviewerCli` — <https://github.com/open-gsd/gsd-core/issues/2801>
  (2026) — one release as a derived alias, then deletion, warning never error. **Decided by:**
  maintenance overhead — that precedent exists for an *externally visible* contract. A grep of
  this repo establishes the two verbs have no README exposure and no caller outside it, so an
  alias window would carry deprecation machinery through a release to protect callers that do not
  exist. Explicitly considered and not followed, per D-08.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `$CLAUDE_CONFIG_DIR` / `--path` override -> filesystem read | A path from the environment or from a CLI argument selects which file the reader opens |
| installed workflow file content -> reported verdict | Third-party file content decides whether a patch is reported present, and that verdict gates the destructive strip at `sync.py`'s live re-gate |
| CLI verb name -> skill callers | Two `SKILL.md` files invoke the verb by name; a rename that misses one produces a silent capability failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-04-01 | Spoofing | merged reader's present/absent verdict | high | mitigate | A false "present" verdict on the execute-plan target unlocks `strip_task_bodies` at the live re-gate — the irreversible path. Mitigated by keeping the check a plain code-point substring test against a version-labeled literal constant (never a regex, never a fuzzy match), by the per-entry version field so a stale marker cannot be matched by a newer entry, and by new tests asserting each marker's literal string and each present-message's version suffix. |
| T-17-04-02 | Denial of Service | merged reader inside `lifecycle_dispatch` | high | mitigate | Both checks run back-to-back inside one `try/except Exception` that also wraps `beads_recall`, so an exception raised by a merged reader on an unknown table key would silently take out the recall as well. The reader must be total: an unrecognized key returns the same fail-open result as an unreadable file. Pinned by a test calling the reader with an unknown key. |
| T-17-04-03 | Tampering | CLI verb rename across two SKILL.md callers | high | mitigate | A rename that updates one caller and misses the other produces a silent capability failure at `ship:pre` — the failure mode Success Criterion 4 was written to prevent. Mitigated by D-09's pre-merge CLI coverage for the ship-md target, by updating every caller in the same commit, and by a post-merge repo-wide grep asserting zero surviving references to either retired verb outside `.planning/`. |
| T-17-04-04 | Elevation of Privilege | `--path` override | low | accept | The path override lets a caller point the reader at an arbitrary readable file. Accepted: the reader is strictly read-only (an existing test already asserts it never writes to its target), it emits only a present/absent verdict plus the path it read, and any caller able to pass the flag already controls the process. Residual risk bounded by the existing WR-03 discipline of naming the exact path probed. |
| T-17-04-05 | Information Disclosure | verdict messages | low | accept | Messages disclose an absolute path under the user's own runtime home into their own session context — unchanged from today's two functions. |
| T-17-04-SC | Tampering | npm/pip/cargo installs | low | accept | Zero packages installed — stdlib refactor plus markdown edits. RESEARCH.md's Package Legitimacy Audit records "not applicable". |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close the coverage gaps BEFORE the CLI can change</name>
  <beads-id>gsd-beads-u67.9</beads-id>
  <files>plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py</files>
  <!-- beads: content synced to bd -- see `bd show gsd-beads-u67.9` -->
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm the one-way CLI contract change (D-08 versus ROADMAP Criterion 4)</name>
  <beads-id>gsd-beads-u67.10</beads-id>
  <decision>Collapse the two patch-check CLI verbs into one parameterized verb, as a hard break with no alias window?</decision>
  <context>
CONTEXT.md D-08 rates this decision **one-way**: it is a published-plugin CLI contract change with
no deprecation window. Undoing it means re-adding both verbs and a second round of doc updates,
and anyone who scripted against `.planning/intel/API-SURFACE.md` breaks with no warning. That
rating is why execution stops here rather than proceeding straight from Task 1 to Task 3.

Two things need confirming before the door is walked through.

**1. The source-artifact conflict.** ROADMAP.md Phase 17 Success Criterion 4 (2026-08-19) states
that both CLI subcommands and both flag spellings survive. CONTEXT.md D-08 (2026-08-20) states the
opposite. The discussion log records the collapse as an informed override made *after* a caller
grep established no README exposure and no caller outside this repo — the "keep both" position was
the recommendation and was withdrawn. If option A is chosen, Criterion 4's "both subcommands
survive" clause is amended in the merge commit to record that D-08 supersedes it. Criterion 5 is
unaffected either way: both *Python function names* survive as thin wrappers, so
`TestCheckShipmdPatch` and `TestCheckExecutePlanPatch` pass unedited and both in-file call sites
keep their current spelling.

**2. The verb shape**, which D-08 leaves to Claude's discretion. Recommended: a single verb taking
the target as a positional argument (the table key — `ship-md` or `execute-plan`) plus one
`--path` override replacing the two per-target flag spellings. That is one verb, one positional
and one flag, replacing two verbs and two flags — the smallest surface that still reaches both
targets and keeps the path override testable, which D-09 requires. Say so here if you want a
different spelling; changing it later is another contract change.

Callers updated in the same commit under any collapse option: `beads-recall/SKILL.md` lines
72-73, `beads-status/SKILL.md` line 146, `GSD-CORE-PATCH.md` (four mentions), the CLI-level tests
Task 1 just added, and a regenerated `.planning/intel/API-SURFACE.md`.
  </context>
  <options>
    <option id="option-a">
      <name>Collapse, hard break, no aliases — implement D-08 and amend Criterion 4 (RECOMMENDED)</name>
      <pros>Implements the developer's explicit, post-grep decision. Smallest permanent CLI surface: one verb, one positional, one flag. Gives D-10's per-entry version field an unambiguous home. The break is fully contained — grep confirms no README exposure and no caller outside this repo. Task 1's coverage already landed, so the break cannot be silent.</pros>
      <cons>ROADMAP Success Criterion 4's literal text must be amended in the same commit, which is a roadmap edit inside an execution plan. Anyone with an unrecorded local script calling either retired verb breaks with no deprecation warning.</cons>
    </option>
    <option id="option-b">
      <name>Merge internals only — keep both CLI verbs and both flag spellings</name>
      <pros>Honors ROADMAP Criterion 4 verbatim; zero external contract change; no roadmap amendment needed. Still removes the ~50 duplicated body lines and still gives the version field a home.</pros>
      <cons>Contradicts locked decision D-08. Leaves two subparsers, two flag spellings and two dispatch branches permanently, each needing its own test, to protect callers that a grep shows do not exist.</cons>
    </option>
    <option id="option-c">
      <name>Collapse with a one-release alias window (the `reviewerCli` precedent)</name>
      <pros>Reaches D-08's end state while giving any unknown caller one release to migrate; follows gsd-core's own documented removal pattern.</pros>
      <cons>D-08 considered and explicitly rejected this — that precedent exists for externally visible contracts and this one is not. Carries deprecation machinery plus its tests through a release for callers that do not exist, and defers the same break rather than avoiding it.</cons>
    </option>
  </options>
  <resume-signal>Reply with the option id, plus the verb spelling if you want something other than the recommended shape.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: One reader, one verb, every caller updated in the same commit</name>
  <beads-id>gsd-beads-u67.11</beads-id>
  <reversibility rating="one-way">A published-plugin CLI contract change with no alias window: undoing it means re-adding both verbs plus a second round of doc updates, and anything scripted against the published API surface breaks with no deprecation warning. Gated by the checkpoint immediately above.</reversibility>
  <files>
    plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py,
    plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py,
    plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-recall/SKILL.md,
    plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-status/SKILL.md,
    plugins/beads-lifecycle/.gsd/capabilities/beads/GSD-CORE-PATCH.md,
    .planning/intel/API-SURFACE.md,
    CHANGELOG.md
  </files>
  <!-- beads: content synced to bd -- see `bd show gsd-beads-u67.11` -->
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created or changed by THIS plan (absent from `.planning/intel/API-SURFACE.md`
because they do not exist yet, or because the surface has not been regenerated):

- `PATCH_CHECKS` — new module-level table constant in `scripts/sync.py`
- The new parameterized patch-check reader function backing both wrappers in `scripts/sync.py`
- The new collapsed CLI verb and its `--path` override, replacing two verbs and two flags
- `class TestPatchChecksTable` — new test class in `tests/test_sync.py`
- New CLI-level and never-writes test methods on the existing `TestCheckShipmdPatch`
- `CHANGELOG.md` 0.4.0 breaking-change entry for the retired verbs
- A regenerated `.planning/intel/API-SURFACE.md`

<verification>
- Full suite green from both trees, `Ran N tests` with N >= 179 (164 baseline + at least 15).
- `sync.py` is shorter than its 2286-line baseline.
- The `plan:pre` pair's six message cases are byte-identical to pre-merge.
- Zero surviving references to either retired verb outside `.planning/`.
- `diff -rq` between the runtime overlay and the tracked source is silent.
- ROADMAP Success Criterion 4 reflects the shipped contract.
</verification>

<success_criteria>
ROADMAP.md Phase 17 Success Criteria 4 (as amended at the checkpoint) and 5 are met: one reader
serves both patch checks, every existing call site behaves identically with `TestCheckShipmdPatch`
and `TestCheckExecutePlanPatch` assertions unedited, and the suite reports strictly more than 164
tests including the marker-literal and version-token assertions that did not exist before.
</success_criteria>

<output>
Create `.planning/phases/17-config-code-truth/17-04-SUMMARY.md` when done.
</output>
