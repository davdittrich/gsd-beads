---
phase: 09-beads-content-depth
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .agents/skills/beads/resources/DEPENDENCIES.md
  - .agents/skills/beads/resources/WORKTREES.md
  - .agents/skills/beads/resources/ASYNC_GATES.md
  - .agents/skills/beads/resources/RESUMABILITY.md
  - .agents/skills/beads/resources/STEALTH_MODE.md
  - .agents/skills/beads/resources/TROUBLESHOOTING.md
  - .agents/skills/beads/SKILL.md
autonomous: true
requirements: [PUB-11]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An agent needing dependency, worktree, async-gate, resumability, git-free-mode, or troubleshooting guidance finds it inside the shipped skill instead of guessing (PUB-11)"
    - "SKILL.md stays a short entry point and routes to each resource by relative path (D-03)"
    - "Every resource path named in SKILL.md resolves to a file that exists"
    - "No resource file freezes a bd flag table; each points at the live help output instead"
    - "Adapted upstream content carries its MIT attribution line"
  artifacts:
    - path: ".agents/skills/beads/resources/DEPENDENCIES.md"
      provides: "bd dep usage, the four dependency types, ready-queue effects"
      contains: "bd dep"
      min_lines: 40
    - path: ".agents/skills/beads/resources/WORKTREES.md"
      provides: "bd worktree usage and when a worktree is worth it"
      contains: "bd worktree"
      min_lines: 40
    - path: ".agents/skills/beads/resources/ASYNC_GATES.md"
      provides: "gate issues that block until an external event resolves"
      min_lines: 35
    - path: ".agents/skills/beads/resources/RESUMABILITY.md"
      provides: "recovering work state after context reset, compaction, or handoff"
      min_lines: 35
    - path: ".agents/skills/beads/resources/STEALTH_MODE.md"
      provides: "git-free operation via bd init --stealth, bd prime --stealth, BEADS_DIR"
      contains: "BEADS_DIR"
      min_lines: 25
    - path: ".agents/skills/beads/resources/TROUBLESHOOTING.md"
      provides: "symptom/cause/fix entries with a table of contents"
      min_lines: 50
    - path: ".agents/skills/beads/SKILL.md"
      provides: "entry point plus a Deeper Topics index into resources/"
      contains: "resources/"
  key_links:
    - from: ".agents/skills/beads/SKILL.md"
      to: ".agents/skills/beads/resources/"
      via: "Deeper Topics index lists each resource by relative path"
      pattern: "resources/[A-Z_]+\\.md"
---

<objective>
Give the shipped skill the six conceptual resource documents PUB-11 names, and route to them from SKILL.md so they are reachable rather than dead files in the archive.

Purpose: PUB-11. Phase 8 UAT found the shipped skill materially thinner than upstream — an agent working a gsd-beads project has no in-skill guidance for dependencies, worktrees, async gates, resumability, git-free operation, or common failures.
Output: `.agents/skills/beads/resources/` with six documents, plus a Deeper Topics index in SKILL.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-beads-content-depth/09-CONTEXT.md
@.planning/phases/09-beads-content-depth/09-PATTERNS.md
@.agents/skills/beads/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependencies, worktrees, and async gates</name>
  <precondition>The upstream beads skill is readable at `/home/dd/.claude/plugins/marketplaces/beads-marketplace/plugins/beads/skills/beads/resources/` — it is the adaptation source, not a file this repo owns.</precondition>
  <files>.agents/skills/beads/resources/DEPENDENCIES.md, .agents/skills/beads/resources/WORKTREES.md, .agents/skills/beads/resources/ASYNC_GATES.md</files>
  <read_first>
    - Upstream `resources/DEPENDENCIES.md`, `resources/WORKTREES.md`, `resources/ASYNC_GATES.md` — adapt and trim these; the mechanics are project-neutral.
    - Upstream `resources/CLI_REFERENCE.md` (13 lines) — read it for the discipline it states, and apply that discipline here.
    - `.planning/phases/09-beads-content-depth/09-PATTERNS.md` §"resources/..." — the recurring shape: a table-driven "when to use" section, fenced bash examples, a short architecture note.
    - `.gsd/capabilities/beads/capability.json` `gates[]` — the ship-gate condition ASYNC_GATES.md relates gate issues to.
  </read_first>
  <action>
    Create three resource documents under `.agents/skills/beads/resources/`, each opening with an H1, a blockquote attribution line naming the upstream beads skill and its MIT license, and then the adapted body. Each document describes stable mechanisms and gsd-core framing; none transcribes a flag table, because the command surface comes from the installed binary and drifts between releases — route the reader to the binary's own help output instead. Target 60-140 substantive lines each (D-04 covers these topics fully; trimming upstream prose is expected, dropping topics is not).

    DEPENDENCIES.md: `bd dep add`, `bd dep tree`, and dependency listing; the four dependency types (blocks, related, parent-child, discovered-from) as a table with the effect each has on the ready queue; how a hard blocker removes an issue from `bd ready`; cycle rejection; and the gsd framing — a phase epic is the parent of its per-task issues, and unresolved blockers are what `BEADS.md`'s `blocking_open` counts.

    WORKTREES.md: `bd worktree create`; a "when is a worktree worth it" decision table (long-lived parallel branch versus a single wave of gsd executors sharing one checkout); the `redirect` file that points a worktree at the main repo's `.beads/` and why it must not be committed; and `BEADS_DIR` for pointing at a workspace outside the tree.

    ASYNC_GATES.md: modelling an external wait (a review, a deploy, a third-party turnaround) as an issue that blocks its dependents; creating one, resolving it, and inspecting what it holds up; why polling a gate beats blocking an agent turn; and how a gate issue that is still open participates in gsd's ship gate through the same blocking count.
  </action>
  <verify>
    <automated>
cd .agents/skills/beads/resources
for f in DEPENDENCIES.md WORKTREES.md ASYNC_GATES.md; do test -f "$f"; done
test "$(grep -c . DEPENDENCIES.md)" -ge 40
test "$(grep -c . WORKTREES.md)" -ge 40
test "$(grep -c . ASYNC_GATES.md)" -ge 35
grep -qF 'bd dep' DEPENDENCIES.md
grep -qF 'discovered-from' DEPENDENCIES.md
grep -qF 'bd ready' DEPENDENCIES.md
grep -qF 'bd worktree' WORKTREES.md
grep -qF 'BEADS_DIR' WORKTREES.md
grep -qiF 'blocking_open' ASYNC_GATES.md
for f in DEPENDENCIES.md WORKTREES.md ASYNC_GATES.md; do grep -qi 'MIT' "$f"; done
! grep -qE '^\|[[:space:]]*.?-{2}[a-z]' DEPENDENCIES.md
! grep -qE '^\|[[:space:]]*.?-{2}[a-z]' WORKTREES.md
! grep -qE '^\|[[:space:]]*.?-{2}[a-z]' ASYNC_GATES.md
echo RESOURCES_A_OK
    </automated>
  </verify>
  <done>Three resource documents exist, cover their topics with gsd framing, carry upstream attribution, and contain no frozen flag table.</done>
</task>

<task type="auto">
  <name>Task 2: Resumability, git-free mode, and troubleshooting</name>
  <files>.agents/skills/beads/resources/RESUMABILITY.md, .agents/skills/beads/resources/STEALTH_MODE.md, .agents/skills/beads/resources/TROUBLESHOOTING.md</files>
  <read_first>
    - Upstream `resources/RESUMABILITY.md` and `resources/TROUBLESHOOTING.md` — adapt; TROUBLESHOOTING.md's table-of-contents plus symptom/cause/fix shape is the structure to reuse.
    - Live `bd init --help` and `bd prime --help` output — the authoritative source for the two distinct `--stealth` flags. Run both; do not write this topic from an upstream file, because upstream has no dedicated document for it (09-RESEARCH.md Pitfall 4).
    - `hooks/session-start.sh` and `hooks/hooks.json` from plan 09-01 — the SessionStart behaviour TROUBLESHOOTING.md documents failures of.
  </read_first>
  <action>
    Create three more resource documents in the same shape as Task 1 (H1, upstream MIT attribution blockquote, adapted body, no frozen flag tables).

    RESUMABILITY.md: why `bd` state survives a thread reset, compaction, or a handoff to another agent while a scratch checklist does not; the recovery sequence at the start of a cold session; finding work already claimed and deciding whether to resume or release it; using issue comments as the durable handoff channel for findings; and the gsd framing that a phase's plans and summaries record intent while `bd` records status.

    STEALTH_MODE.md: git-free and invisible operation. State explicitly that `bd init --stealth` and `bd prime --stealth` are two different flags on two different subcommands with different effects — the former configures per-repository git settings so beads files are never committed (including the per-repo exclude file and the `bd setup <tool> --stealth` follow-up), the latter suppresses git operations for a single prime invocation. Cover `BEADS_DIR` for keeping the workspace entirely outside the repository, and when each is the right choice. Source every claim from the live help output read above.

    TROUBLESHOOTING.md: a table of contents followed by one section per failure, each with a symptom (the command and what it printed), a cause, and a fix. Cover at least: `bd` reports no workspace; dependencies that appear not to persist; a stale claim keeping an issue out of the ready queue; interactive-editor commands hanging an agent turn; nothing injected at session start; and `bd prime` printing generic output because the `.beads/` override is absent — with the fix being that the SessionStart hook restores it, and the check being whether that hook ran.
  </action>
  <verify>
    <automated>
cd .agents/skills/beads/resources
for f in RESUMABILITY.md STEALTH_MODE.md TROUBLESHOOTING.md; do test -f "$f"; done
test "$(grep -c . RESUMABILITY.md)" -ge 35
test "$(grep -c . STEALTH_MODE.md)" -ge 25
test "$(grep -c . TROUBLESHOOTING.md)" -ge 50
grep -qF 'BEADS_DIR' STEALTH_MODE.md
grep -qF 'bd init' STEALTH_MODE.md
grep -qF 'bd prime' STEALTH_MODE.md
grep -qF 'stealth' STEALTH_MODE.md
grep -qF 'bd prime' RESUMABILITY.md
grep -qiF 'symptom' TROUBLESHOOTING.md
grep -qF 'PRIME.md' TROUBLESHOOTING.md
test "$(grep -c '^## ' TROUBLESHOOTING.md)" -ge 6
for f in RESUMABILITY.md STEALTH_MODE.md TROUBLESHOOTING.md; do grep -qi 'MIT' "$f"; done
! grep -qE '^\|[[:space:]]*.?-{2}[a-z]' RESUMABILITY.md
! grep -qE '^\|[[:space:]]*.?-{2}[a-z]' STEALTH_MODE.md
! grep -qE '^\|[[:space:]]*.?-{2}[a-z]' TROUBLESHOOTING.md
echo RESOURCES_B_OK
    </automated>
  </verify>
  <done>All six resource documents exist; the git-free topic distinguishes the two `--stealth` flags; troubleshooting has at least six symptom sections including the missing-override case.</done>
</task>

<task type="auto">
  <name>Task 3: Route SKILL.md to the resources</name>
  <files>.agents/skills/beads/SKILL.md</files>
  <action>
    Append a `## Deeper Topics` section to `.agents/skills/beads/SKILL.md` after the existing `## Rules` section, keeping everything above it byte-identical — the frontmatter, First Step, Preferred Route, Core CLI Workflow, What Belongs In Beads, and Rules sections are the existing entry point and stay as they are (D-03 keeps SKILL.md short; detail loads on demand).

    Under a `### Resources` subsection, list the six documents as a bullet per file: the relative path `resources/NAME.md` followed by a one-line statement of when to read it. Add one further bullet stating that `.beads/PRIME.md` carries this project's `bd prime` override, that the plugin's SessionStart hook restores it whenever it is missing, and that it can be added to a project's `.beads/.gitignore` if an untracked file there is unwanted. Do not restate the override's contents here — that file speaks for itself and duplicating it would put the same guidance in two places that drift apart (D-06).
  </action>
  <verify>
    <automated>
S=.agents/skills/beads/SKILL.md
grep -q '^## Deeper Topics' "$S"
grep -q '^### Resources' "$S"
grep -q '^## Rules' "$S"
grep -q '^name: beads' "$S"
LINKS=$(grep -oE 'resources/[A-Z_]+\.md' "$S" | sort -u)
test "$(printf '%s\n' "$LINKS" | wc -l)" -eq 6
MISSING=0; for f in $LINKS; do test -f ".agents/skills/beads/$f" || MISSING=1; done; test "$MISSING" -eq 0
grep -qF '.beads/PRIME.md' "$S"
test "$(grep -c 'execute:wave:post' "$S")" -eq 0
echo SKILL_INDEX_OK
    </automated>
  </verify>
  <done>SKILL.md indexes all six resources by resolvable relative path, documents where `.beads/PRIME.md` comes from, and does not duplicate the override's lifecycle content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped skill text → agent behaviour | Documentation this plugin ships is read by agents as instructions and acted on in the installer's repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Tampering | Resource docs recommending destructive commands | medium | mitigate | Command examples in these six files stay read/append shaped. Where a command mutates or removes state, the document names the guard and the inspection step that precedes it rather than offering a copy-pasteable force flag. |
| T-09-06 | Repudiation | Stale flag tables presented as authoritative | low | mitigate | No frozen flag tables; each document routes to the installed binary's own help output. Enforced by an automated check in Tasks 1 and 2. |
| T-09-10 | Repudiation | Adapted upstream text shipped without attribution | low | mitigate | Every adapted document carries an MIT attribution blockquote naming the upstream beads skill; checked automatically. |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No packages are installed by this plan — RESEARCH.md's Package Legitimacy Audit records "Not applicable". |
</threat_model>

<verification>
- Six files exist under `.agents/skills/beads/resources/` with the required topic tokens and line floors.
- Every `resources/*.md` path referenced by SKILL.md resolves.
- No resource file contains a markdown table row that begins with a bd flag.
- SKILL.md's pre-existing sections and frontmatter are unchanged.
</verification>

<success_criteria>
- Six of PUB-11's thirteen named topics (dependencies, worktrees, async gates, resumability, git-free mode, troubleshooting) are covered in the shipped skill.
- D-03's progressive-disclosure split exists and is navigable from the entry point.
- D-06's non-duplication holds between SKILL.md and PRIME.md.
</success_criteria>

<output>
Create `.planning/phases/09-beads-content-depth/09-02-SUMMARY.md` when done
</output>
