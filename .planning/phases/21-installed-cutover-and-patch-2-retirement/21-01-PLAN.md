---
phase: 21-installed-cutover-and-patch-2-retirement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py
  - plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py
  - plugins/beads-lifecycle/.gsd/capabilities/beads/GSD-CORE-PATCH.md
  - plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-recall/SKILL.md
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [CUT-01, CUT-02]

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The first naturally synced Phase 21 tracer task has one exact tracker-id=beads:<id>, the same <beads-id>, and one matching live Beads row before any cutover proof runs (D-06, D-07)."
    - "At one freshly observed source state, the complete tracked, project-active, and global-active Beads capability trees are byte-identical and the non-project registry selects the global bundle (D-01 through D-03)."
    - "The public task resolve-content command resolves the natural Phase 21 task from the live Beads database and returns its authored action, verify, acceptanceCriteria, readFirst, and done fields (D-03, D-08, CUT-01)."
    - "Unknown native identity, a real legacy beads-id-only task, unavailable bd, and malformed resolver stdout each independently exit nonzero with no resolved object and no task-content substitution (D-09 through D-11)."
    - "Patch 2 retirement begins only after the ordered pre-removal gate passes; every post-removal proof repeats, and any post-removal failure restores only the captured workflow bytes before SPEC_FAILURE (D-04, D-05)."
    - "No active Patch 2 marker, checker, table row, wrapper, call site, detector, test, or operational instruction remains, while Patch 1 remains installed and independently passes its existing checker (D-12 through D-15, CUT-02)."
    - "Missing or empty bundle trees, identities, Beads rows, or resolved content are failures; tree equality never merges the proof and mutation stages, and the D-04 order is stable and repeated after removal."
  artifacts:
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      provides: "Native content strip path plus the surviving Patch 1 checker surface; Patch 2-owned symbols and calls deleted"
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py"
      provides: "Patch 1/current native-contract regression coverage with Patch 2-specific tests deleted"
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/GSD-CORE-PATCH.md"
      provides: "Patch 1-only active operational patch contract"
    - path: "plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-recall/SKILL.md"
      provides: "Plan-time Patch 1 detector only"
    - path: "README.md"
      provides: "Current installed native-resolution contract"
    - path: "CHANGELOG.md"
      provides: "Append-only Patch 2 retirement history"
  key_links:
    - from: ".planning/phases/21-installed-cutover-and-patch-2-retirement/21-01-PLAN.md"
      to: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      via: "normal plan:post adds matching tracker-id and beads-id; public resolver dispatches the installed sync.py against that live id"
      pattern: "tracker-id=\"beads:.*\".*<beads-id>"
    - from: "gsd_run task resolve-content"
      to: "live Beads database"
      via: "active global taskContentResolver invokes fixed argv bd show <id> --json with BEADS_DIR derived from bd where --json"
      pattern: '\["bd", "show", .* "--json"\]'
    - from: "plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py"
      to: "plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-recall/SKILL.md"
      via: "check-patch keeps only the ship-md target and the recall step invokes only that surviving target"
      pattern: "check-patch ship-md"
    - from: "runtime-derived execute-plan workflow"
      to: "captured exact workflow bytes"
      via: "marker-bounded deletion after the pre-gate; exact-byte restore on any post-gate failure"
      pattern: "gsd-beads-patch:execute-plan-bd-task-read"
  prohibitions:
    - "No synthetic proof task, copied/imported/replayed Beads data, project-overlay positive proof, or stale prior-run evidence."
    - "No new dependency, library, framework, persisted harness, helper abstraction, alias, cache, retry, compatibility branch, tombstone, or fallback path."
    - "No hard-coded runtime home or installed workflow path; derive runtimeConfigDir, registry sources, and Beads location at execution time."
    - "No Patch 1 redesign or cleanup; preserve its marker, workflow block, table row, wrapper, tests, docs, installer wiring, and behavior."
    - "No standalone documentation task, external API capability matrix, schema push, raw database parsing, or direct adapter success substituted for the public oracle."
---

Mechanism: Fresh exact-state public resolver transaction through the existing installed GSD and Beads CLI seams, followed by marker-bounded Patch 2 deletion.

Forbidden: Project-overlay proof, synthetic/replayed data, stale evidence, partial retirement, Patch 1 edits, or any new cutover abstraction/dependency/alias/cache/retry/fallback.

Audit: Public `task resolve-content` is the behavioral oracle; existing exact `bd show <id> --json` argv spies prove mechanism; full-tree parity and exact-byte rollback control runtime drift.

<objective>
Prove installed native task-content resolution against the first naturally synced Phase 21 task, then retire every active Patch 2 surface without changing Patch 1 (D-01 through D-15).

Purpose: CUT-01 must be falsifiable before the duplicate machine-local read path disappears; CUT-02 then removes only the redundant path and repeats the same controls.
Output: One live installed-cutover proof, six narrowly updated tracked files, one marker-bounded runtime workflow deletion, and no new files, symbols, flags, dependencies, or APIs.
</objective>

<execution_context>
@/home/dd/.codex/gsd-core/workflows/execute-plan.md
@/home/dd/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-installed-cutover-and-patch-2-retirement/21-CONTEXT.md
@.planning/phases/21-installed-cutover-and-patch-2-retirement/21-RESEARCH.md
@.planning/phases/21-installed-cutover-and-patch-2-retirement/21-PATTERNS.md
@.planning/phases/21-installed-cutover-and-patch-2-retirement/21-VALIDATION.md
@.planning/phases/21-installed-cutover-and-patch-2-retirement/21-BEADS-RECALL.md
@.planning/phases/20-additive-identity-migration-and-compatibility/20-01-SUMMARY.md
@plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json
@plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py
@plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py
@plugins/beads-lifecycle/.gsd/capabilities/beads/GSD-CORE-PATCH.md
@plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-recall/SKILL.md
@plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-status/SKILL.md
@plugins/beads-lifecycle/hooks/capability-auto-install.sh
@README.md
@CHANGELOG.md

<interfaces>
Current installed public router contract (exact source quote, confidence 100/100):
`const registry = capabilityLoaderMod.loadRegistry({ includeInstalled: true, cwd });`
`output({ resolved: true, content: result.content }, raw, undefined);`

Current adapter mechanism (exact source quote, confidence 100/100):
`result = run_bd(["bd", "show", issue_id, "--json"], timeout=8)`

Current focused spy (exact source quote, confidence 100/100):
`self.assertEqual(run.call_args.args[0], ["bd", "show", self.ISSUE_ID, "--json"])`

Current retirement seam (exact source quote, confidence 100/100):
`EXECUTE_PLAN_PATCH_MARKER = "<!-- gsd-beads-patch:execute-plan-bd-task-read v1 -->"`
`if check_execute_plan_patch() == 0: new_text = strip_task_bodies(new_text, newly_created_ids)`

Current Patch 1 seam (exact source quote, confidence 100/100):
`SHIP_MD_PATCH_MARKER = "<!-- gsd-beads-patch:ship-pre-generic-dispatch v2 -->"`
`return check_patch("ship-md", ship_md_path_override)`
</interfaces>
</context>

## Artifacts this phase produces

No new tracked file, module, symbol, configuration flag, schema, dependency, or external API is created.

Changed existing surfaces: `sync.py`, `test_sync.py`, `GSD-CORE-PATCH.md`, `beads-recall/SKILL.md`, `README.md`, and `CHANGELOG.md`.

Deleted existing Patch 2 surfaces: its marker constant, checker-table row, wrapper/call sites, strip precondition, tests, active detector/documentation, and the runtime-derived marker-bracketed workflow block. Patch 1 remains.

## Alternatives Considered

- **Retain the Patch 2 dual-read path:** Keep the current machine-local task read beside the native resolver. Primary source: `plugins/beads-lifecycle/.gsd/capabilities/beads/GSD-CORE-PATCH.md` (reviewed 2026-09-01). Rejected because the duplicate path can satisfy execution while the installed resolver is broken, making CUT-01 unfalsifiable and increasing work per task.
- **Use project-overlay or direct-adapter success as the cutover oracle:** Exercise the tracked adapter or the project-scoped capability without proving global selection. Primary sources: https://github.com/open-gsd/gsd-core/blob/next/docs/reference/capability-manifest.md and https://github.com/gastownhall/beads/blob/main/docs/reference/index.md (accessed 2026-09-01). Rejected because neither mechanism traverses the installed global registry path required by CUT-01.
- **Introduce a persistent blue-green cutover harness:** Maintain parallel workflow variants and switch between them. Primary source: https://nvlpubs.nist.gov/nistpubs/SpecialPublications/NIST.SP.800-40r4.pdf (accessed 2026-09-01). Rejected because this is one shared local workflow block with an exact byte restore, not a traffic-bearing deployment; the additional harness would add code and another state surface without improving rollback.

Decided by: performance. The selected path invokes the already-shipped public resolver once per proof, removes the duplicate per-task read, and adds no coordinating layer; simplicity/LOC, ecosystem support, and maintenance all point the same way.

### Internal design alternatives

- One proof-only task plus one retirement task is the minimum shape that preserves the irreversible dependency and gives `plan:post` a natural exact `tracer` issue for D-06.
- Combining proof and deletion in one task would make the natural task usable but hide the terminal pre-removal boundary inside one ticket; splitting code, tests, and docs would violate D-15 and permit partial retirement.
- A reusable proof script was rejected: the volatile paths and live state must be derived fresh, and the existing CLI/tests already form the deeper module interface.

## Assumption Delta

Decision: `no-change`. The primary noun remains native tracker identity `beads:<id>` backed by the live Beads issue. Fallback is prohibited, not an alternative identity. The detector match on that word is therefore not model pluralization and changes no architecture.

## API Coverage Declaration

No external network API, SDK, or service is integrated. This phase uses existing local `gsd_run` and `bd` CLI contracts and local filesystem state; an API capability matrix would fabricate scope. Confidence: 100/100 from the current router, manifest, adapter, and requirement boundary.

## Schema Push Declaration

No database or application schema is added or changed. The live Beads database is read-only during the cutover transaction, so schema push does not apply. Confidence: 100/100.

## Spec-less Edge Review

| Requirement | Edge category | Resolution |
|---|---|---|
| CUT-01 | unclassified | Flagged assumption: SPEC is absent, but CUT-01 is an installed integration transaction rather than a collection operation; public positive plus four named trust-boundary failures are the complete source-defined classification. Confidence 96/100. |
| CUT-02 | adjacency | Exact tree equality is a gate result, not a merge: proof and mutation remain separate stages and retirement cannot touch the proof stage. |
| CUT-02 | empty | Missing/empty bundle, identity, live row, or resolved content fails before mutation; after retirement zero Patch 2 surfaces and exactly one Patch 1 target must remain. |
| CUT-02 | ordering | D-04 is strict and stable: parity/registry, live DB, positive, four negatives, removal, repeat, full suite, Patch 1. Equal or duplicate evidence never changes that order. |

<tasks>

<task type="tracer">
  <name>Task [active-global public resolution transaction] ! [project-overlay proof, synthetic task, stale evidence, or pre-gate mutation]</name>
  <precondition>Normal plan:post has completed after plan pre-check and added exactly one phase epic plus this tracer's one tracker-id, one equal beads-id, and one matching live Beads row; all tracked retirement targets are free of unrelated edits; `gsd_run`, `bd`, and Python 3 are callable; and bounded `/dev/shm` scratch can be created. These identities are plan:post postconditions and execution preconditions, not inputs to plan pre-check.</precondition>
  <read_first>
    .planning/phases/21-installed-cutover-and-patch-2-retirement/21-CONTEXT.md, .planning/phases/21-installed-cutover-and-patch-2-retirement/21-RESEARCH.md, .planning/phases/21-installed-cutover-and-patch-2-retirement/21-VALIDATION.md, .planning/phases/21-installed-cutover-and-patch-2-retirement/21-01-PLAN.md, plugins/beads-lifecycle/.gsd/capabilities/beads/capability.json, plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py, plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py, plugins/beads-lifecycle/hooks/capability-auto-install.sh
  </read_first>
  <files>None — this is a read-only live proof task; it creates or modifies no tracked file.</files>
  <behavior>
    - Baseline/RED: before installation or retirement mutation, the current active-global proof is run and must expose any stale global bundle rather than accepting the project overlay; an unexpected pass is investigated, never made artificially red.
    - GREEN: the existing capability installer seam may synchronize the runtime-derived global bundle, after which complete-tree parity, active-global selection, live identity, and public authored content all pass at one recorded source state.
    - Negative controls: each of the four D-09 arms changes exactly one factor and produces nonzero exit, no resolved object, and no task-content substitution.
  </behavior>
  <action>
Execute one bounded read-only cutover experiment per D-01 through D-11. Record `git rev-parse HEAD`; assert the six Task 2 tracked targets have no pre-existing diff; create scratch with `mktemp -d -p /dev/shm phase21-cutover.XXXXXX`, validate it with `namei`, install an EXIT cleanup trap, and never write temporary data elsewhere. If any required assertion fails, print `SPEC_FAILURE`, clean scratch, and stop before source or workflow mutation (D-05).

Parse this task's opening block from the real absolute `21-01-PLAN.md`. Require type exactly `tracer`, exactly one `tracker-id="beads:<id>"`, exactly one equal `<beads-id><id>`, and no conflicting candidate. Run `bd where --json`, take its `path` as execution-time `BEADS_DIR`, and require `BEADS_DIR="$BEADS_DIR" bd --readonly show "$ID" --json` to return exactly that live row (D-06, D-07). Do not create, copy, import, replay, or edit Beads data.

Derive the tracked bundle from the repository and the project-active `beads` source from `gsd_run capability list --raw` at the repository. Walk ancestors of the real absolute plan nearest-first; accept a proof root only when the plan remains router-contained, the registry contains exactly one active global `beads` and no active project `beads`, and that global manifest declares exactly one resolver. If no root qualifies, emit `SPEC_FAILURE` before mutation. If parity is absent, invoke only the existing install seam, `gsd_run capability install "$SOURCE_BUNDLE" --scope global --yes`, then re-derive both registries. Use `diff -qr --` in both directions through the complete trees and reject missing, empty, source-only, install-only, or non-identical paths (D-01, D-02, D-03).

From that non-project proof root, derive `BEADS_DIR` only from the supported `bd where --json` `path`, require `bd --readonly show "$ID" --json` to return the one matching live row, and run `gsd_run task resolve-content --plan "$REAL_PLAN" --task-id "beads:$ID" --raw`. Require exit 0, `resolved: true`, exactly the public `action`, `verify`, `acceptanceCriteria`, `readFirst`, and `done` fields, and every authored value carried by this task. Direct `sync.py`, parser-only, mock-only, or project-root success does not pass CUT-01 (D-07, D-08).

Run four separate public commands from the identical known-good baseline (D-09, D-10): (1) change only the task id to an unknown native id; (2) use the current real Phase 20 legacy task after asserting its block has one `<beads-id>` and no native tracker attribute; (3) change only PATH to a bounded scratch bin containing runtime-derived `node` and real `python3` but no `bd`; (4) change only resolver stdout by placing a scratch `python3` executable that emits malformed JSON ahead of PATH while invoking `gsd_run` by its resolved absolute path. Each arm gets separate stdout/stderr files, must exit nonzero, must not contain `resolved: true` or this task's authored content, and must not modify the live install/database. Do not combine faults, skip, xfail, downgrade to a warning, or classify a failure as environmental.

Finally run the existing focused adapter spy `python3 -m unittest tests.test_sync.TestResolveTaskContent.test_round_trip_emits_exact_five_key_json_and_typed_lists -v` from the capability directory; it must still assert exact argv `["bd", "show", id, "--json"]` and timeout 8 (D-11). Confirm HEAD and every tracked/runtime workflow byte are unchanged and the scratch directory is gone.
  </action>
  <acceptance_criteria>
    - The naturally synced first task has equal native and legacy Beads identity and the same live read-only Beads row.
    - Complete tracked/project/global trees are non-empty and byte-identical; the public proof registry names only the active global Beads bundle.
    - Public resolution returns `resolved: true` with the live task's five authored content fields.
    - All four isolated public negative arms exit nonzero, emit no resolved object/content substitution, and the exact internal bd argv spy passes.
    - No tracked source, installed workflow, live Beads data, or residual scratch changes during this task.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c '
import hashlib, importlib.util, json, os, re, shutil, subprocess, sys, tempfile
from pathlib import Path

sys.dont_write_bytecode = True

def run(argv, cwd, env=None):
    return subprocess.run(argv, cwd=cwd, env=env, text=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)

def require(condition, message):
    if not condition:
        raise AssertionError(message)

def digest_tree(root):
    require(root.is_dir(), "missing tree: " + str(root))
    digest = hashlib.sha256()
    count = 0
    for path in sorted(root.rglob("*"), key=lambda item: item.as_posix()):
        relative = path.relative_to(root).as_posix().encode()
        if path.is_symlink():
            digest.update(b"L" + relative + os.readlink(path).encode())
            count += 1
        elif path.is_file():
            digest.update(b"F" + relative + path.read_bytes())
            count += 1
    require(count != 0, "empty tree: " + str(root))
    return digest.hexdigest()

def registry(gsd, cwd):
    result = run([gsd, "capability", "list", "--raw"], cwd)
    require(result.returncode == 0, "registry failed: " + str(cwd))
    value = json.loads(result.stdout)
    require(isinstance(value, list), "invalid registry")
    return value

def source_path(entry, cwd):
    value = Path(entry["source"])
    return (value if value.is_absolute() else cwd / value).resolve()

repo = Path(subprocess.check_output(["git", "rev-parse", "--show-toplevel"], text=True).strip()).resolve()
plan = (repo / ".planning/phases/21-installed-cutover-and-patch-2-retirement/21-01-PLAN.md").resolve()
legacy_plan = (repo / ".planning/phases/20-additive-identity-migration-and-compatibility/20-01-PLAN.md").resolve()
tracked = (repo / "plugins/beads-lifecycle/.gsd/capabilities/beads").resolve()
sync_path = tracked / "scripts/sync.py"
gsd = shutil.which("gsd_run")
require(gsd is not None, "gsd_run unavailable")
gsd = str(Path(gsd).resolve())
scratch = Path(tempfile.mkdtemp(prefix="phase21-cutover.", dir="/dev/shm"))
failure = None
try:
    require(run(["namei", str(scratch)], repo).returncode == 0, "scratch namei failed")
    head_before = subprocess.check_output(["git", "rev-parse", "HEAD"], cwd=repo, text=True).strip()
    status_before = subprocess.check_output(["git", "status", "--porcelain=v1", "--untracked-files=all"], cwd=repo)
    plan_text = plan.read_text()
    epic_ids = re.findall(r"(?m)^beads_epic:\s*(\S+)\s*$", plan_text)
    require(len(epic_ids) == 1, "phase epic identity count")
    spec = importlib.util.spec_from_file_location("phase21_sync", sync_path)
    module = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(module)
    _, _, tasks = module.parse_plan(plan)
    require(len(tasks) == 2, "task count")
    task = tasks[0]
    require(task["type"] == "tracer", "first task type")
    require(len(task["tracker_ids"]) == 1, "tracker identity count")
    require(bool(task["beads_id"]), "missing beads identity")
    tracker = task["tracker_ids"][0]
    bead = task["beads_id"]
    require(tracker == "beads:" + bead, "identity mismatch")
    where = run(["bd", "where", "--json"], repo)
    require(where.returncode == 0, "bd where failed")
    beads_dir = Path(json.loads(where.stdout)["path"]).resolve()
    require(beads_dir.is_dir() and beads_dir.is_absolute(), "invalid beads path")
    env = os.environ.copy()
    env["BEADS_DIR"] = str(beads_dir)
    for issue_id in [epic_ids[0], bead]:
        shown = run(["bd", "--readonly", "show", issue_id, "--json"], repo, env)
        require(shown.returncode == 0, "live row missing: " + issue_id)
        rows = json.loads(shown.stdout)
        rows = rows if isinstance(rows, list) else [rows]
        require(len(rows) == 1 and rows[0].get("id") == issue_id, "live row mismatch: " + issue_id)
    beads_before = digest_tree(beads_dir)
    project_entries = [item for item in registry(gsd, repo) if item.get("id") == "beads" and item.get("status") == "active" and item.get("scope") == "project"]
    require(len(project_entries) == 1, "project resolver count")
    project_source = source_path(project_entries[0], repo)
    proof_root = None
    global_source = None
    for candidate in plan.parents:
        if candidate == repo or repo in candidate.parents:
            continue
        try:
            plan.relative_to(candidate)
        except ValueError:
            continue
        entries = registry(gsd, candidate)
        global_entries = [item for item in entries if item.get("id") == "beads" and item.get("status") == "active" and item.get("scope") == "global"]
        project_overlays = [item for item in entries if item.get("id") == "beads" and item.get("status") == "active" and item.get("scope") == "project"]
        if len(global_entries) != 1 or project_overlays:
            continue
        candidate_source = source_path(global_entries[0], candidate)
        manifest = json.loads((candidate_source / "capability.json").read_text())
        if not isinstance(manifest.get("taskContentResolver"), dict):
            continue
        proof_root = candidate
        global_source = candidate_source
        break
    require(proof_root is not None and global_source is not None, "no global-only contained root")
    tracked_before = digest_tree(tracked)
    project_before = digest_tree(project_source)
    global_before = digest_tree(global_source)
    require(run(["diff", "-qr", "--", str(tracked), str(project_source)], repo).returncode == 0, "tracked/project drift")
    require(run(["diff", "-qr", "--", str(tracked), str(global_source)], repo).returncode == 0, "tracked/global drift")
    state = run([gsd, "capability", "state", "--raw"], repo)
    require(state.returncode == 0, "capability state failed")
    runtime = Path(json.loads(state.stdout)["runtimeConfigDir"]).resolve()
    execute_workflow = runtime / "gsd-core/workflows/execute-plan.md"
    ship_workflow = runtime / "gsd-core/workflows/ship.md"
    require(execute_workflow.is_file() and ship_workflow.is_file(), "runtime workflow missing")
    workflows_before = (hashlib.sha256(execute_workflow.read_bytes()).hexdigest(), hashlib.sha256(ship_workflow.read_bytes()).hexdigest())
    positive = run([gsd, "task", "resolve-content", "--plan", str(plan), "--task-id", tracker, "--raw"], proof_root, env)
    require(positive.returncode == 0, "public positive failed")
    payload = json.loads(positive.stdout)
    content = payload.get("content")
    require(payload.get("resolved") is True and isinstance(content, dict), "public positive unresolved")
    require(set(content) == {"action", "verify", "acceptanceCriteria", "readFirst", "done"}, "public five-field shape")
    expected_read = [item.strip() for item in task["read_first"].split(",") if item.strip()]
    expected_acceptance = [line.strip().removeprefix("-").removeprefix("*").strip() for line in task["acceptance_criteria"].splitlines() if line.strip()]
    require(content["readFirst"] == expected_read, "authored readFirst mismatch")
    require(content["verify"] == task["verify"], "authored verify mismatch")
    require(content["acceptanceCriteria"] == expected_acceptance, "authored acceptance mismatch")
    require(content["done"] == task["done"], "authored done mismatch")
    for authored in [task["precondition"], task["behavior"], task["action"]]:
        require(authored in content["action"], "authored action mismatch")
    authored_marker = task["done"]
    _, _, legacy_tasks = module.parse_plan(legacy_plan)
    legacy = [item for item in legacy_tasks if item.get("beads_id") and not item.get("tracker_ids")]
    require(len(legacy) == 2, "real legacy control drift")
    no_bd = scratch / "no-bd"
    malformed = scratch / "malformed"
    no_bd.mkdir()
    malformed.mkdir()
    for binary in ["sh", "dirname", "readlink", "node", "python3"]:
        resolved = shutil.which(binary)
        require(resolved is not None, "missing runtime binary: " + binary)
        os.symlink(resolved, no_bd / binary)
    malformed_python = malformed / "python3"
    malformed_python.write_text("#!/bin/sh\nprintf %s {malformed-json\n")
    malformed_python.chmod(0o700)
    negative_specs = [
        ("unknown", plan, "beads:phase21-unknown-native-id", env),
        ("legacy", legacy_plan, "beads:" + legacy[0]["beads_id"], env),
        ("bd-unavailable", plan, tracker, dict(env, PATH=str(no_bd))),
        ("malformed-stdout", plan, tracker, dict(env, PATH=str(malformed) + os.pathsep + env.get("PATH", ""))),
    ]
    for label, arm_plan, arm_tracker, arm_env in negative_specs:
        result = run([gsd, "task", "resolve-content", "--plan", str(arm_plan), "--task-id", arm_tracker, "--raw"], proof_root, arm_env)
        require(result.returncode != 0, label + " unexpectedly succeeded")
        require(result.stdout.strip() == "", label + " emitted fallback stdout")
        require("resolved" not in result.stdout and authored_marker not in result.stdout + result.stderr, label + " substituted content")
    require(subprocess.check_output(["git", "rev-parse", "HEAD"], cwd=repo, text=True).strip() == head_before, "HEAD mutated")
    require(subprocess.check_output(["git", "status", "--porcelain=v1", "--untracked-files=all"], cwd=repo) == status_before, "tracked tree mutated")
    require(digest_tree(beads_dir) == beads_before, "live Beads mutated")
    require(digest_tree(tracked) == tracked_before and digest_tree(project_source) == project_before and digest_tree(global_source) == global_before, "bundle tree mutated")
    workflows_after = (hashlib.sha256(execute_workflow.read_bytes()).hexdigest(), hashlib.sha256(ship_workflow.read_bytes()).hexdigest())
    require(workflows_after == workflows_before, "runtime workflow mutated")
except Exception as error:
    failure = str(error)
finally:
    shutil.rmtree(scratch, ignore_errors=True)
if scratch.exists():
    failure = "scratch cleanup failed"
if failure is not None:
    print("SPEC_FAILURE: " + failure, file=sys.stderr)
    raise SystemExit(1)
print("PHASE21_CUTOVER_OK " + head_before)
'</automated>
    <fails_when>The self-contained transaction does not print `PHASE21_CUTOVER_OK` with the unchanged exact SHA; either plan:post identity or either live row is absent; the global-only ancestor, manifest resolver, full-tree byte parity, five authored public fields, any isolated negative's nonzero/empty/no-substitution result, mutation check, or cleanup assertion fails. It emits `SPEC_FAILURE` and permits no retirement mutation.</fails_when>
    <automated>bash -lc 'set -euo pipefail; cd /home/dd/projects/gsd-beads/plugins/beads-lifecycle/.gsd/capabilities/beads; TMPDIR=/dev/shm PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_sync.TestResolveTaskContent.test_round_trip_emits_exact_five_key_json_and_typed_lists -v'</automated>
    <fails_when>The existing focused spy does not prove exact internal argv `['bd', 'show', id, '--json']`, timeout 8, and the adapter's exact five raw fields.</fails_when>
  </verify>
  <done>The naturally synced Phase 21 task resolves through the active global bundle and all four isolated public failures remain fail-closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task [marker-bounded Patch 2 retirement with exact-byte rollback] ! [partial deletion, Patch 1 edits, compatibility residue, or adjacent cleanup]</name>
  <precondition>Immediately before the first drift-prone write, Task 1's complete D-01 through D-11 transaction passes again at the current HEAD and runtime state, and the runtime-derived execute-plan workflow contains exactly one balanced Patch 2 marker block.</precondition>
  <reversibility rating="costly">The runtime workflow is shared by multiple projects; post-removal failure requires restoring its exact captured bytes before halting.</reversibility>
  <read_first>
    .planning/phases/21-installed-cutover-and-patch-2-retirement/21-CONTEXT.md, .planning/phases/21-installed-cutover-and-patch-2-retirement/21-PATTERNS.md, plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py, plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py, plugins/beads-lifecycle/.gsd/capabilities/beads/GSD-CORE-PATCH.md, plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-recall/SKILL.md, plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-status/SKILL.md, README.md, CHANGELOG.md
  </read_first>
  <files>plugins/beads-lifecycle/.gsd/capabilities/beads/scripts/sync.py, plugins/beads-lifecycle/.gsd/capabilities/beads/tests/test_sync.py, plugins/beads-lifecycle/.gsd/capabilities/beads/GSD-CORE-PATCH.md, plugins/beads-lifecycle/.gsd/capabilities/beads/skills/beads-recall/SKILL.md, README.md, CHANGELOG.md</files>
  <behavior>
    - Tests first/RED: a read-only assertion that the checker table already contains only Patch 1 and the active docs already describe completed cutover must fail before any mutation; unexpected green is runtime/source drift and stops the task.
    - GREEN: delete only Patch 2-owned production, tests, active docs, detector, and runtime marker block; the existing native strip behavior and Patch 1 interface remain.
    - REFACTOR: none beyond deletion and mechanically inseparable assertion/framing edits; no table collapse or adjacent cleanup.
    - Rollback: any post-removal failure restores only the captured runtime workflow bytes, verifies their hash, emits SPEC_FAILURE, and leaves the tracked diff visible for diagnosis.
  </behavior>
  <action>
Follow D-01 through D-05 again before editing: record the current HEAD, rerun Task 1's parity/registry/live-row/public-positive/four-negative/argv-spy transaction, and stop on any miss. Then run a read-only RED assertion against current `sync.py`, `test_sync.py`, and README requiring the post-retirement state; it must fail specifically because Patch 2 is still active. If it passes, emit `SPEC_FAILURE` because the planned deletion set drifted. This tests-first ordering precedes every source and workflow mutation.

Create one fresh bounded `/dev/shm/phase21-retirement.XXXXXX` evidence directory and refuse a pre-existing deterministic pointer `/dev/shm/phase21-retirement-$BASE_HEAD.path`. Derive `runtimeConfigDir` from `gsd_run capability state --raw`; derive the active paths exactly as `$runtimeConfigDir/gsd-core/workflows/execute-plan.md` and `$runtimeConfigDir/gsd-core/workflows/ship.md` rather than naming a runtime home. Require both readable; write `base-head`, `runtime-paths.json`, `execute-plan.before`, `execute-plan.before.sha256`, `ship-md.before`, `ship-md.before.sha256`, and `patch1-regions.json` into the evidence directory. Build `patch1-regions.json` with the same AST/section extraction used by verification: hash exact `SHIP_MD_PATCH_MARKER`, `PATCH_CHECKS["ship-md"]`, `check_patch`, `check_shipmd_patch`, `TestCheckShipmdPatch`, every Patch-1-only `test_ship_md_*` method except the mixed execute-plan assertion scheduled for deletion, the bytes from `## Patch 1:` up to `## Patch 2:`, and full `beads-status/SKILL.md`, `capability.json`, and `capability-auto-install.sh`. Atomically publish only the pointer after every capture validates. Require exactly one opening and closing Patch 2 marker in correct order before any write. These are execution-time preconditions immediately before the costly mutation (D-05, D-14).

Use the existing seams only (D-12, D-15). In `sync.py`, delete the Patch 2 marker constant, the `execute-plan` table entry, `check_execute_plan_patch`, both direct call sites, and the Patch-2-only gate/diagnostic around `strip_task_bodies`; retain `allow_strip=False` hook protection and make the already-allowed branch call `strip_task_bodies` directly after native proof. Keep `PATCH_CHECKS`, `check_patch`, the `ship-md` row, `check_shipmd_patch`, and Patch 1 messages byte-identical; the CLI choices narrow automatically from the retained table. Add no retired-target branch, alias, marker, tombstone, fallback, or replacement framework.

In `test_sync.py`, delete `TestCreateIssuesStripGate` and `TestCheckExecutePlanPatch`; delete every execute-plan-specific method/assertion from `TestPatchChecksTable` and change only its mechanically inseparable table/wrapper invariant to exactly one `ship-md` target; keep `TestCheckShipmdPatch` byte-identical. In `TestLifecycleDispatchRouting`, update `test_plan_pre_runs_recall_then_all_three_diagnostics` to recall plus the two surviving diagnostics, removing the execute-plan mock and order assertion, and delete the Patch-2-only unmocked ship-before-execute ordering test around current lines 5971-6014. Remove the `check_execute_plan_patch` mock from `test_plan_pre_dispatches_regardless_of_probe_result` around current line 6627. In `TestDirectSkillSyncResolver.RAW_FENCES` around current line 7130, narrow the `beads-recall` command contract to `check-patch ship-md` only. Update `TestTaskContentResolverManifest.test_release_docs_keep_source_availability_distinct_from_cutover` to completed-cutover wording. Delete every other current active Patch 2 reference found in `test_sync.py`; do not reproduce Phase 19's failure matrix.

Delete the current active Patch 2 comments/docstrings in `sync.py` around the native-strip gate, lifecycle diagnostics, and CLI table as part of their owning code deletion. In `GSD-CORE-PATCH.md`, delete the active Patch 2 section, marker block, reapply/check instructions, and shared two-patch framing while leaving Patch 1 operational content intact. In `beads-recall/SKILL.md`, remove the execute-plan detector command and its Patch-2-only explanation/anti-pattern, leaving the existing ship-md detector. Update README's existing native-resolver caveat from future ownership to completed installed cutover. Add a current 0.5.0 retirement entry to CHANGELOG without rewriting any older Patch 2 history (D-13). Keep all six tracked changes in this task and one commit.

Only after every tracked edit is ready, remove exactly the runtime-derived marker-bounded Patch 2 block by byte slicing; refuse zero, duplicate, unbalanced, or reordered markers. Do not touch the runtime ship workflow. Synchronize the edited tracked capability into the active global scope only through `gsd_run capability install "$SOURCE_BUNDLE" --scope global --yes`, then re-derive the registries and require complete tracked/project/global byte parity. Run focused tests, route `python3 -m py_compile scripts/sync.py` into the execution-local evidence directory, run the scoped active-surface residue check and exact Patch 1 region/hash comparison, then repeat Task 1's positive and four negative public proofs. Run the full capability suite and the runtime-derived `check-patch ship-md --path "$SHIP_MD"` independently (D-04, D-14).

If any post-removal command fails, atomically restore only the captured execute workflow bytes, verify its hash equals the capture, remove the scratch directory, print `SPEC_FAILURE`, and halt without inventing another path; leave tracked changes uncommitted for diagnosis. If all pass, confirm scratch is empty, inspect the complete diff for the unoptimized/legacy paths, and make one Conventional Commit containing code, tests, and operational docs together (D-15).
  </action>
  <acceptance_criteria>
    - The strict pre-removal gate passes at the current source/runtime state immediately before mutation; otherwise no Patch 2 byte is removed.
    - `sync.py` retains native `strip_task_bodies` behavior but has no Patch 2 marker, table row, wrapper, checker call, or diagnostic; `check-patch` exposes only `ship-md`.
    - Patch 2-specific tests and active operational instructions are gone; README states completed cutover and CHANGELOG preserves old history plus the retirement entry.
    - The runtime-derived execute workflow has exactly the Patch 2 marker block removed; its captured bytes are restored exactly on any post-gate failure.
    - Patch 1 source/test/docs/installer wiring and runtime workflow bytes match their captured baseline, and `check-patch ship-md` returns 0.
    - The post-removal public positive, four isolated negatives, exact bd argv spy, Python compile, and full capability suite all pass; scratch is empty.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c '
import ast, hashlib, json, os, shutil, subprocess, sys
from pathlib import Path

def require(condition, message):
    if not condition:
        raise AssertionError(message)

def sha(data):
    return hashlib.sha256(data).hexdigest()

def node_text(text, node):
    value = ast.get_source_segment(text, node)
    require(value is not None, "AST source segment missing")
    return value.encode()

def current_patch1_regions(repo):
    capability = repo / "plugins/beads-lifecycle/.gsd/capabilities/beads"
    sync_path = capability / "scripts/sync.py"
    test_path = capability / "tests/test_sync.py"
    source = sync_path.read_text()
    source_tree = ast.parse(source)
    regions = {}
    for node in source_tree.body:
        if isinstance(node, ast.Assign) and any(isinstance(target, ast.Name) and target.id == "SHIP_MD_PATCH_MARKER" for target in node.targets):
            regions["sync:SHIP_MD_PATCH_MARKER"] = sha(node_text(source, node))
        if isinstance(node, ast.Assign) and any(isinstance(target, ast.Name) and target.id == "PATCH_CHECKS" for target in node.targets):
            require(isinstance(node.value, ast.Dict), "PATCH_CHECKS is not a dict")
            matches = [(key, value) for key, value in zip(node.value.keys, node.value.values) if isinstance(key, ast.Constant) and key.value == "ship-md"]
            require(len(matches) == 1, "ship-md table entry count")
            regions["sync:PATCH_CHECKS:ship-md"] = sha(node_text(source, matches[0][0]) + b"=" + node_text(source, matches[0][1]))
        if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)) and node.name in {"check_patch", "check_shipmd_patch"}:
            regions["sync:" + node.name] = sha(node_text(source, node))
    tests = test_path.read_text()
    test_tree = ast.parse(tests)
    for node in test_tree.body:
        if isinstance(node, ast.ClassDef) and node.name == "TestCheckShipmdPatch":
            regions["tests:TestCheckShipmdPatch"] = sha(node_text(tests, node))
        if isinstance(node, ast.ClassDef) and node.name == "TestPatchChecksTable":
            for method in node.body:
                if isinstance(method, (ast.FunctionDef, ast.AsyncFunctionDef)) and method.name.startswith("test_ship_md_") and "execute_plan" not in method.name:
                    regions["tests:TestPatchChecksTable:" + method.name] = sha(node_text(tests, method))
    doc = (capability / "GSD-CORE-PATCH.md").read_bytes()
    start = doc.find(b"## Patch 1:")
    end = doc.find(b"## Patch 2:", start)
    if end == -1:
        end = doc.find(b"## Reapply verification", start)
    require(start != -1 and end != -1 and start != end, "Patch 1 doc boundaries")
    regions["docs:Patch1"] = sha(doc[start:end])
    full_files = {
        "skills:beads-status": capability / "skills/beads-status/SKILL.md",
        "manifest:capability": capability / "capability.json",
        "installer:capability-auto-install": repo / "plugins/beads-lifecycle/hooks/capability-auto-install.sh",
    }
    for label, path in full_files.items():
        regions[label] = sha(path.read_bytes())
    return regions

def restore_and_clean(evidence, pointer, execute_path):
    captured = evidence / "execute-plan.before"
    expected = (evidence / "execute-plan.before.sha256").read_text().strip()
    require(sha(captured.read_bytes()) == expected, "rollback capture hash")
    replacement = evidence / "execute-plan.restore"
    shutil.copy2(captured, replacement)
    os.replace(replacement, execute_path)
    require(sha(execute_path.read_bytes()) == expected, "rollback live hash")
    shutil.rmtree(evidence)
    pointer.unlink()

repo = Path(subprocess.check_output(["git", "rev-parse", "--show-toplevel"], text=True).strip()).resolve()
base = subprocess.check_output(["git", "rev-parse", "HEAD"], cwd=repo, text=True).strip()
pointer = Path("/dev/shm/phase21-retirement-" + base + ".path")
require(pointer.is_file() and not pointer.is_symlink(), "retirement evidence pointer missing")
evidence = Path(pointer.read_text().strip()).resolve()
require(evidence.parent == Path("/dev/shm") and evidence.name.startswith("phase21-retirement."), "invalid evidence path")
paths = json.loads((evidence / "runtime-paths.json").read_text())
execute_path = Path(paths["execute"]).resolve()
ship_path = Path(paths["ship"]).resolve()
try:
    require((evidence / "base-head").read_text().strip() == base, "base SHA drift")
    require(execute_path.is_file() and ship_path.is_file(), "runtime workflow missing")
    execute_bytes = execute_path.read_bytes()
    require(b"gsd-beads-patch:execute-plan-bd-task-read" not in execute_bytes, "runtime Patch 2 marker remains")
    capability = repo / "plugins/beads-lifecycle/.gsd/capabilities/beads"
    active_roots = [capability / "scripts", capability / "tests", capability / "skills", repo / "plugins/beads-lifecycle/hooks"]
    active_files = [capability / "GSD-CORE-PATCH.md", capability / "capability.json", repo / "README.md"]
    for root in active_roots:
        active_files.extend(path for path in root.rglob("*") if path.is_file() and not path.is_symlink() and path.suffix in {".py", ".md", ".json", ".sh"})
    residues = [
        b"gsd-beads-patch:execute-plan-bd-task-read",
        b"EXECUTE_PLAN_PATCH_MARKER",
        b"check_execute_plan_patch",
        b"check-patch execute-plan",
        b"TestCreateIssuesStripGate",
        b"TestCheckExecutePlanPatch",
        b"test_plan_pre_prints_ship_report_before_execute_report_unmocked",
        b"test_plan_pre_runs_recall_then_all_three_diagnostics",
        b"bd-task-read patch",
        b"Patch 2",
    ]
    hits = []
    for path in sorted(set(active_files)):
        data = path.read_bytes()
        for residue in residues:
            if residue in data:
                hits.append(path.relative_to(repo).as_posix() + ":" + residue.decode())
    require(not hits, "active Patch 2 residue: " + ", ".join(hits))
    expected_ship = (evidence / "ship-md.before.sha256").read_text().strip()
    require(sha((evidence / "ship-md.before").read_bytes()) == expected_ship, "Patch 1 runtime capture hash")
    require(sha(ship_path.read_bytes()) == expected_ship, "Patch 1 runtime bytes changed")
    expected_regions = json.loads((evidence / "patch1-regions.json").read_text())
    require(current_patch1_regions(repo) == expected_regions, "Patch 1 tracked region changed")
    check = subprocess.run(["python3", str(capability / "scripts/sync.py"), "check-patch", "ship-md", "--path", str(ship_path)], cwd=capability, text=True, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
    require(check.returncode == 0 and "present" in check.stdout.lower(), "independent Patch 1 checker failed")
    probe = evidence / "rollback-probe"
    replacement = evidence / "rollback-replacement"
    probe.write_bytes(execute_bytes)
    shutil.copy2(evidence / "execute-plan.before", replacement)
    os.replace(replacement, probe)
    require(sha(probe.read_bytes()) == (evidence / "execute-plan.before.sha256").read_text().strip(), "rollback hash behavior")
    probe.unlink()
except Exception as error:
    try:
        restore_and_clean(evidence, pointer, execute_path)
    except Exception as rollback_error:
        print("SPEC_FAILURE: " + str(error) + "; rollback: " + str(rollback_error), file=sys.stderr)
        raise SystemExit(1)
    print("SPEC_FAILURE: " + str(error), file=sys.stderr)
    raise SystemExit(1)
print("PHASE21_RETIREMENT_SURFACE_OK " + base)
'</automated>
    <fails_when>The exact execution-local pointer/evidence contract is absent; current HEAD differs from `base-head`; the runtime marker or any scoped production/test/skill/operational Patch 2 residue remains; the ship workflow, any protected Patch 1 AST/test/doc/full-file hash, independent `check-patch ship-md` result, or isolated atomic rollback probe differs. The command restores `execute-plan.before` atomically, verifies its hash, removes only its evidence/pointer, prints `SPEC_FAILURE`, and exits nonzero.</fails_when>
    <automated>python3 -c '
import hashlib, html, json, os, re, shlex, shutil, subprocess, sys
from pathlib import Path

def sha(data):
    return hashlib.sha256(data).hexdigest()

def restore_and_clean(evidence, pointer, execute_path):
    captured = evidence / "execute-plan.before"
    expected = (evidence / "execute-plan.before.sha256").read_text().strip()
    if sha(captured.read_bytes()) != expected:
        raise AssertionError("rollback capture hash")
    replacement = evidence / "execute-plan.restore"
    shutil.copy2(captured, replacement)
    os.replace(replacement, execute_path)
    if sha(execute_path.read_bytes()) != expected:
        raise AssertionError("rollback live hash")
    shutil.rmtree(evidence)
    pointer.unlink()

repo = Path(subprocess.check_output(["git", "rev-parse", "--show-toplevel"], text=True).strip()).resolve()
base = subprocess.check_output(["git", "rev-parse", "HEAD"], cwd=repo, text=True).strip()
pointer = Path("/dev/shm/phase21-retirement-" + base + ".path")
evidence = Path(pointer.read_text().strip()).resolve()
paths = json.loads((evidence / "runtime-paths.json").read_text())
execute_path = Path(paths["execute"]).resolve()
try:
    plan = repo / ".planning/phases/21-installed-cutover-and-patch-2-retirement/21-01-PLAN.md"
    task_open = "<" + "task type=\"tracer\">"
    task_close = "<" + "/task>"
    automated_open = "<" + "automated>"
    automated_close = "<" + "/automated>"
    tracer = plan.read_text().split(task_open, 1)[1].split(task_close, 1)[0]
    commands = re.findall(re.escape(automated_open) + r"(.*?)" + re.escape(automated_close), tracer, re.DOTALL)
    if len(commands) != 2:
        raise AssertionError("Task 1 automated command count")
    public = subprocess.run(shlex.split(html.unescape(commands[0])), cwd=repo)
    if public.returncode != 0:
        raise AssertionError("post-removal public transaction")
    capability = repo / "plugins/beads-lifecycle/.gsd/capabilities/beads"
    env = os.environ.copy()
    env["TMPDIR"] = "/dev/shm"
    env["PYTHONDONTWRITEBYTECODE"] = "1"
    spy = subprocess.run(["python3", "-m", "unittest", "tests.test_sync.TestResolveTaskContent.test_round_trip_emits_exact_five_key_json_and_typed_lists", "-v"], cwd=capability, env=env)
    if spy.returncode != 0:
        raise AssertionError("post-removal exact argv spy")
except Exception as error:
    try:
        restore_and_clean(evidence, pointer, execute_path)
    except Exception as rollback_error:
        print("SPEC_FAILURE: " + str(error) + "; rollback: " + str(rollback_error), file=sys.stderr)
        raise SystemExit(1)
    print("SPEC_FAILURE: " + str(error), file=sys.stderr)
    raise SystemExit(1)
print("PHASE21_POST_MATRIX_OK " + base)
'</automated>
    <fails_when>Re-executing Task 1's exact self-contained command after retirement fails its exact-SHA, live identity/row, full parity/global-only selection, public five-field positive, any of the same four isolated nonzero/no-output/no-substitution controls, no-mutation, or cleanup assertion; or the exact internal argv spy fails. The command atomically restores and hash-verifies the captured workflow, removes only execution evidence, prints `SPEC_FAILURE`, and exits nonzero.</fails_when>
    <automated>python3 -c '
import hashlib, json, os, shutil, subprocess, sys
from pathlib import Path

def sha(data):
    return hashlib.sha256(data).hexdigest()

def restore_and_clean(evidence, pointer, execute_path):
    captured = evidence / "execute-plan.before"
    expected = (evidence / "execute-plan.before.sha256").read_text().strip()
    if sha(captured.read_bytes()) != expected:
        raise AssertionError("rollback capture hash")
    replacement = evidence / "execute-plan.restore"
    shutil.copy2(captured, replacement)
    os.replace(replacement, execute_path)
    if sha(execute_path.read_bytes()) != expected:
        raise AssertionError("rollback live hash")
    shutil.rmtree(evidence)
    pointer.unlink()

repo = Path(subprocess.check_output(["git", "rev-parse", "--show-toplevel"], text=True).strip()).resolve()
base = subprocess.check_output(["git", "rev-parse", "HEAD"], cwd=repo, text=True).strip()
pointer = Path("/dev/shm/phase21-retirement-" + base + ".path")
evidence = Path(pointer.read_text().strip()).resolve()
paths = json.loads((evidence / "runtime-paths.json").read_text())
execute_path = Path(paths["execute"]).resolve()
capability = repo / "plugins/beads-lifecycle/.gsd/capabilities/beads"
env = os.environ.copy()
env["TMPDIR"] = "/dev/shm"
env["PYTHONDONTWRITEBYTECODE"] = "1"
env["PYTHONPYCACHEPREFIX"] = str(evidence / "pycache")
try:
    compile_result = subprocess.run(["python3", "-m", "py_compile", "scripts/sync.py"], cwd=capability, env=env)
    if compile_result.returncode != 0:
        raise AssertionError("Python compile")
    suite = subprocess.run(["python3", "-m", "unittest", "discover", "-s", "tests", "-t", "tests"], cwd=capability, env=env)
    if suite.returncode != 0:
        raise AssertionError("full capability suite")
except Exception as error:
    try:
        restore_and_clean(evidence, pointer, execute_path)
    except Exception as rollback_error:
        print("SPEC_FAILURE: " + str(error) + "; rollback: " + str(rollback_error), file=sys.stderr)
        raise SystemExit(1)
    print("SPEC_FAILURE: " + str(error), file=sys.stderr)
    raise SystemExit(1)
shutil.rmtree(evidence)
pointer.unlink()
if evidence.exists() or pointer.exists():
    print("SPEC_FAILURE: retirement scratch cleanup", file=sys.stderr)
    raise SystemExit(1)
print("PHASE21_RETIREMENT_OK " + base)
'</automated>
    <fails_when>Python compilation (with pycache redirected into the evidence directory), the inherited full capability suite, or final evidence/pointer cleanup fails. A compile/suite failure atomically restores and hash-verifies the captured workflow, removes only execution evidence, prints `SPEC_FAILURE`, and exits nonzero; success must print `PHASE21_RETIREMENT_OK` with the unchanged base SHA.</fails_when>
  </verify>
  <done>Patch 2 is absent from every active runtime/tracked surface, native resolution and all four public failure controls still pass, and Patch 1 remains byte-identical and independently verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Non-project proof root → installed capability registry | Project and global overlays can expose different bundles; only the runtime-derived active global source is allowed to satisfy CUT-01. |
| Public GSD router → resolver subprocess | Third-party manifest argv and stdout cross into gsd-core's schema/hard-failure boundary. |
| Resolver subprocess → live Beads database | A plan-authored identifier reaches a local CLI/data authority; argv must stay typed and the database read-only. |
| Tracked retirement source → machine-local workflow | A committed deletion is paired with a shared mutable workflow edit that must be exact and recoverable. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-21-01 | Spoofing/Tampering | Capability overlay selection | high | mitigate | Task 1 derives registries from project and non-project roots, requires one active global Beads resolver, and compares complete trees before public proof (D-01 through D-03). |
| T-21-02 | Tampering | Resolver stdout | high | mitigate | Task 1's malformed-stdout public arm must exit nonzero with empty/unresolved output; Task 2 repeats it after retirement (D-09 through D-11). |
| T-21-03 | Tampering/Elevation | Tracker/Beads identifier | high | mitigate | Require exact equal `beads:<id>`/`<beads-id>`, the existing SAFE_BD_ID_RE boundary, fixed argv `bd show <id> --json`, and the focused argv spy. |
| T-21-04 | Denial of Service | Partial Patch 2 retirement | high | mitigate | One retirement task/commit deletes code, tests, docs, detector, and marker block together only after the ordered pre-gate; residue and full-suite checks block completion (D-04, D-12, D-15). |
| T-21-05 | Tampering/Denial of Service | Rollback integrity | high | mitigate | Capture exact workflow bytes/hash immediately before mutation; on any post-gate failure atomically restore only those bytes and verify the hash before SPEC_FAILURE (D-05). |
| T-21-06 | Repudiation | Stale or combined evidence | medium | mitigate | Record current HEAD and one-factor command outputs; rerun the same transaction after removal instead of reusing Phase 19/20 results. |
</threat_model>

## Discussion Area Coverage

| Discussion area | Decisions | Task coverage |
|---|---|---|
| Cutover transaction | D-01–D-05 | Tasks 1 and 2 both run the ordered exact-state transaction; Task 2 owns capture/restore. |
| Live proof task | D-06–D-08 | Task 1 is exact `tracer`, uses its normal plan:post identity/live row, and accepts only public output. |
| Negative-path matrix | D-09–D-11 | Task 1 runs four one-factor public arms plus the existing argv spy; Task 2 repeats them. |
| Patch retirement/history | D-12–D-15 | Task 2 performs the complete same-task/commit deletion, preserves history, and independently preserves Patch 1. |

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan/Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Installed resolution proven before Patch 2 removal; Patch 1 remains | 21-01 T1/T2 | COVERED | Public proof precedes retirement and repeats afterward. |
| REQ | CUT-01 | Live natural task resolves publicly with complete bundle parity | 21-01 T1 | COVERED | D-01–D-11 transaction. |
| REQ | CUT-02 | Patch 2 removed only after proof; Patch 1 independently verified | 21-01 T2 | COVERED | D-04/D-05 and D-12–D-15 transaction. |
| RESEARCH | — | Public router/active registry is the only behavioral oracle | 21-01 T1/T2 | COVERED | Direct adapter/mock success is supporting evidence only. |
| RESEARCH | — | Runtime-derived complete-tree install/parity and BEADS_DIR locator | 21-01 T1 | COVERED | Existing install seam, capability list, bd where. |
| RESEARCH | — | Four one-variable public failures; Phase 19 retains exhaustive matrix | 21-01 T1/T2 | COVERED | Exactly four Phase 21 arms, no duplicated matrix. |
| RESEARCH | — | Patch 2 inventory deleted; Patch 1 table/checker retained | 21-01 T2 | COVERED | Six tracked files plus runtime marker block. |
| RESEARCH | — | ASVS L1 overlay/output/id/partial-retirement/rollback controls | 21-01 T1/T2 | COVERED | T-21-01 through T-21-06 mitigated. |
| CONTEXT | D-01 | Fresh exact-state transaction | 21-01 T1/T2 | COVERED | Both sides of mutation re-observe state. |
| CONTEXT | D-02 | Runtime-derived complete bundle paths/parity | 21-01 T1 | COVERED | No hard-coded install path. |
| CONTEXT | D-03 | Non-project public positive with live DB | 21-01 T1 | COVERED | Absolute plan plus BEADS_DIR. |
| CONTEXT | D-04 | Binding gate order | 21-01 T1/T2 | COVERED | Explicit action/acceptance order. |
| CONTEXT | D-05 | SPEC_FAILURE and exact-byte restore | 21-01 T1/T2 | COVERED | No pre-failure mutation; post-failure restore. |
| CONTEXT | D-06 | First synced auto/tracer task and both ids | 21-01 T1 | COVERED | First task type is exact tracer. |
| CONTEXT | D-07 | Natural Phase 21 data, not Phase 20 proof | 21-01 T1 | COVERED | Phase 20 used only as real legacy negative. |
| CONTEXT | D-08 | Public authored-content success | 21-01 T1/T2 | COVERED | Positive validated twice. |
| CONTEXT | D-09 | Four public negative arms | 21-01 T1/T2 | COVERED | Exact named matrix. |
| CONTEXT | D-10 | One-factor isolated failures | 21-01 T1/T2 | COVERED | Separate scratch/output per arm. |
| CONTEXT | D-11 | Public what plus internal how | 21-01 T1/T2 | COVERED | Public command plus argv spy. |
| CONTEXT | D-12 | Complete Patch 2 active-surface deletion | 21-01 T2 | COVERED | Production/test/doc/detector/runtime inventory. |
| CONTEXT | D-13 | Preserve history; add retirement entry | 21-01 T2 | COVERED | CHANGELOG append-only. |
| CONTEXT | D-14 | Patch 1 preservation | 21-01 T2 | COVERED | Exact captured regions/bytes plus checker. |
| CONTEXT | D-15 | Same retirement task/commit | 21-01 T2 | COVERED | No docs-only task or utility. |

<verification>
1. Re-run Task 1's self-contained active-global positive and four one-factor public negatives from a derived non-project proof root; any unexpected exit direction is `SPEC_FAILURE`.
2. Run the exact inherited suite command: `bash -lc 'set -euo pipefail; cd /home/dd/projects/gsd-beads/plugins/beads-lifecycle/.gsd/capabilities/beads; TMPDIR=/dev/shm PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -t tests'`.
3. Derive `runtimeConfigDir` from `gsd_run capability state --raw`; run tracked `sync.py check-patch ship-md --path <derived ship.md>`, requiring exit 0 and `present`.
4. Prove the runtime execute workflow lacks the Patch 2 marker, the active tracked surfaces lack Patch 2 wiring, historical CHANGELOG entries remain, Patch 1 captures compare byte-for-byte, and `/dev/shm` contains no phase scratch.
</verification>

<success_criteria>
- CUT-01: a real naturally synced Phase 21 task resolves through the active global bundle from live Beads with complete-tree parity.
- CUT-02: the ordered negative controls pass before and after complete Patch 2 retirement.
- Patch 1 remains installed, byte-preserved, documented, tested, and independently verified.
- No new artifact/dependency/API/schema/harness survives; two XML tasks map to two Beads issues under one phase epic.
</success_criteria>

<output>
Create `.planning/phases/21-installed-cutover-and-patch-2-retirement/21-01-SUMMARY.md` when done.
</output>
