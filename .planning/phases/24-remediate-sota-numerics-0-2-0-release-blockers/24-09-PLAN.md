---
phase: 24-remediate-sota-numerics-0-2-0-release-blockers
plan: 09
type: execute
wave: 8
depends_on: [24-08]
target_repo: /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013
files_modified: []
autonomous: false
requirements: [D-16, D-17, D-19, D-20, D-21, D-22, D-23, D-24]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "The release question is put to the developer fresh at the moment of decision and answered before anything is merged (D-22)."
    - "Merging to main is the publish, because the marketplace source carries no ref, so nothing is released before that merge and everything is released by it (D-19)."
    - "The cross-repository description agreement is re-proved at the instant of merge, not inherited from wave 1 (D-12)."
    - "After the release, the global mirror holds the released bytes rather than a working-tree bundle (D-17)."
  artifacts:
    - .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-09-SUMMARY.md
  key_links:
    - "The marketplace entry points at the repository with no ref, so whatever main holds at install time is what an installer gets; the merge is therefore the release event and the tag is only a record of it."
    - "The install hook re-installs from a bundle on every session start, so the mirror has to be brought back to released bytes explicitly or the next session re-publishes the working tree locally."
---

<objective>
Publish 0.2.0, in the order that makes each irreversible step follow the evidence for it rather than precede it.

Everything before this plan is reversible on a branch. This plan contains the one action that is not: merging to main, which is the publish, because the marketplace entry carries no ref and installers therefore read whatever main holds. The decision to take that action is put to the developer at the moment of taking it, with the state of the branch in front of them, and not inferred from the fact that seven plans completed.

Purpose: a release whose go-ahead was implied by the planner rather than given by the developer is not a decision, and this capability's whole subject is the difference.
Output: 0.2.0 on main, tagged, with the global mirror holding released bytes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/DISPOSITION.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-08-SUMMARY.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-02-SUMMARY.md

Two repositories are in play. The release branch `feat/extended-sota-definition` lives in the
worktree named by `target_repo` and is the head of pull request 4 on
`davdittrich/sota-numerics`. The marketplace entry that installers read lives in
`/home/dd/projects/gsd-beads` and was corrected in wave 1 by plan 24-02.

Measured on 2026-09-07, as the starting point rather than as the answer: the branch was 110
commits ahead of `origin/main` and 0 behind it, and 11 ahead of its own remote branch and 0
behind; the manifest and the capability both already declare version 0.2.0; the repository
carries no tags at all; continuous integration is a single workflow. Every one of these is
re-derived at execution time, because waves 1 through 8 push commits and because a
behind-count in particular is only meaningful at the instant it is read.

The pull request is already open and has already been reviewed externally. The standing
ordering rule is therefore satisfied by pushing this phase's accepted fixes to the existing
head and letting the reviewer see the completed diff, not by opening anything new. Do not close
and reopen the pull request to trigger a fresh review pass.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Put the release question to the developer</name>
  <beads-id>gsd-beads-sac.14</beads-id>
  <read_first>
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/DISPOSITION.md`, every row
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-08-SUMMARY.md`, the regression re-run and the hash comparison
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md` decisions D-19 through D-24
  </read_first>
  <decision>Whether to merge `feat/extended-sota-definition` into `main` now, which publishes 0.2.0 to every installer.</decision>
  <context>
    Ask the question fresh. The phase context's decisions recorded that a release decision would be taken here and on what terms; they did not take it. The completion of plans 24-01 through 24-08 is not consent either. Automatic advance does not apply to this checkpoint under any configuration.

    Read each of the following live, at the moment of asking, and put it in front of the developer rather than quoting a summary: the branch's ahead-and-behind counts against main; every review thread still open, with its disposition; every finding recorded as deferred, with its ticket; every finding recorded as declined, with its reason; any scope escalation the previous plan raised; and whether the two install hash values matched.

    State plainly what merging does: the marketplace entry names no ref, so installers begin receiving these bytes immediately on merge. There is no staging step between this and every consumer.
  </context>
  <options>
    <option id="merge">
      <name>Merge now and publish 0.2.0</name>
      <pros>The blockers this phase was opened for are closed and evidenced; the branch is zero behind main; holding a finished branch accrues rebase risk for no gain.</pros>
      <cons>Irreversible in effect: a revert becomes a second published state rather than an undo, and anything that already installed keeps the merged bytes.</cons>
    </option>
    <option id="hold">
      <name>Hold, and name what must change first</name>
      <pros>Costs nothing but time; the branch is unaffected and the evidence stays valid until the base moves.</pros>
      <cons>The regression evidence and the hash comparison were measured now, so a long hold means the next attempt re-measures them.</cons>
    </option>
    <option id="hold-for-listed-items">
      <name>Hold specifically for named deferrals or declines to be reopened</name>
      <pros>Turns a general reservation into a concrete list, which the next planning pass can act on directly.</pros>
      <cons>Reopens dispositions that already have tickets, so the ledger has to be amended rather than appended to.</cons>
    </option>
  </options>
  <resume-signal>Answer `merge`, `hold`, or `hold` followed by the finding identifiers to reopen. Record the answer's exact wording in the summary; the next task is irreversible and the authorisation should not be a paraphrase.</resume-signal>
  <acceptance_criteria>
    - The presented figures were read live, not copied from a summary.
    - Every deferral shown carries a ticket identifier and every decline carries a reason.
    - The developer's answer is recorded verbatim in the summary.
    - No merge, push to main, or tag has occurred before this answer.
  </acceptance_criteria>
  <done>The developer has decided, in their own words, whether 0.2.0 ships.</done>
</task>

<task type="auto">
  <name>Task 2: Re-prove the merge preconditions at the merge instant, then merge</name>
  <beads-id>gsd-beads-sac.15</beads-id>
  <reversibility rating="one-way">Merging to main publishes to every installer immediately, because the marketplace entry names no ref; a revert would be a second published state, not an undo.</reversibility>
  <read_first>
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-02-SUMMARY.md`, the description comparison command and the commits it was run against
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md` decisions D-12, D-19 and D-20
  </read_first>
  <files>.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-09-SUMMARY.md</files>
  <precondition>Task 1's checkpoint returned an affirmative answer, recorded verbatim; without it this task does not run.</precondition>
  <action>
    Push this phase's commits to the pull request's head branch first, and let continuous integration run against the completed diff. Do not merge on a green result from an earlier commit: the check that matters is the one whose head equals what will be merged.

    Push the marketplace commit in the other repository too, if it is not already on that repository's main, and then re-run the description comparison between the marketplace entry and the branch manifest. Wave 1 proved they agreed at wave 1; the branch head has moved since, and the agreement that matters is the one holding when the merge happens. If they differ, stop: correcting the marketplace after the merge means a window during which installers read a description the plugin does not match.

    Immediately before merging, and with nothing between the reading and the merge, re-read the behind-count against main. It must be zero. This is the one property that cannot be established in advance, because the base can move at any moment, and a behind-count read ten minutes earlier is a claim about a state that no longer exists.

    Then merge. Use the pull request rather than a local push to main, so the merge is recorded against the review. Do not bypass any hook, and do not force anything.

    Record what was merged: the merge commit, the head commit it carried, and the continuous integration run identifier that was green for that head.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && git fetch origin --quiet && git rev-list --left-right --count origin/main...HEAD</automated>
    <fails_when>the second number is greater than `0` after the merge, which shows commits remain unmerged, or the first number is greater than `0`, which shows main moved past the merge.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads && A=$(jq -r '.plugins[]|select(.name=="sota-numerics").description' .claude-plugin/marketplace.json) && B=$(git -C .worktrees/sota-numerics-release-013 show origin/main:.claude-plugin/plugin.json | jq -r .description) && [ "$A" = "$B" ] && echo MATCH || echo DIFFER</automated>
    <fails_when>the output is `DIFFER`, which shows the published manifest and the marketplace entry disagree after the merge.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && gh pr view 4 --json state,mergeCommit --jq '"\(.state) \(.mergeCommit.oid // "none")"'</automated>
    <fails_when>the state is not `MERGED`, or the merge commit reads `none`.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads && grep -cE 'behind-count|behind count' .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-09-SUMMARY.md || true</automated>
    <fails_when>the printed count is `0`, which shows the merge-instant reading was not recorded.</fails_when>
  </verify>
  <acceptance_criteria>
    - Continuous integration was green for the exact head commit that was merged, and its run identifier is recorded.
    - The description comparison was re-run against the branch head immediately before merging and matched.
    - The behind-count was read immediately before the merge, with nothing between, and was zero.
    - The pull request state is merged, and the merge commit and head commit are recorded.
    - No hook was bypassed and nothing was force-pushed.
  </acceptance_criteria>
  <done>0.2.0 is on main, and every precondition for putting it there was measured at the moment it was put there.</done>
</task>

<task type="auto">
  <name>Task 3: Tag the released commit</name>
  <beads-id>gsd-beads-sac.16</beads-id>
  <reversibility rating="costly">A pushed tag can be deleted, but anything that already fetched it keeps the old target, so a moved tag is worse than a wrong one; retagging costs a version number rather than a command.</reversibility>
  <read_first>
    - `CHANGELOG.md` on main after the merge, the 0.2.0 section
    - `.claude-plugin/plugin.json` and `.gsd/capabilities/sota-numerics/capability.json` on main, the declared versions
  </read_first>
  <files>.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-09-SUMMARY.md</files>
  <precondition>The merge has completed and main carries the released bytes; the tag names the merge result, not the branch head that preceded it.</precondition>
  <action>
    Confirm first that the two declared versions on main agree with each other and with the changelog's newest section. The repository carries no tags at all today, so this is the first one and there is no precedent to match; choose the plain version form with a leading letter v, and say in the summary that the convention starts here.

    Create an annotated tag on the merge commit, with a message naming what the release changes rather than repeating the version. Push it. Do not tag the pre-merge branch head: it is an ancestor of the merge commit but it is not what main holds, and a tag pointing at something other than the released state is the exact kind of claim this phase exists to remove.

    Do not create a release object beyond the tag unless asked. The marketplace does not read tags, so a release page would be documentation, and documentation nobody asked for is the thing the reviews kept finding.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && git fetch origin --tags --quiet && git rev-parse v0.2.0^{commit} && git rev-parse origin/main</automated>
    <fails_when>the two printed commit identifiers differ, or the first command exits non-zero because the tag is absent.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && git cat-file -t v0.2.0</automated>
    <fails_when>the output is `commit` rather than `tag`, which shows a lightweight tag was pushed instead of an annotated one.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && P=$(git show origin/main:.claude-plugin/plugin.json) && C=$(git show origin/main:.gsd/capabilities/sota-numerics/capability.json) && echo "plugin=$(printf '%s' "$P" | jq -r .version) capability=$(printf '%s' "$C" | jq -r .version)" || echo GITFAIL</automated>
    <fails_when>the output is `GITFAIL`, or the two printed versions differ from each other, or either differs from `0.2.0`.</fails_when>
  </verify>
  <acceptance_criteria>
    - An annotated tag exists on the remote and points at the merge commit that main holds.
    - Both declared versions on main read 0.2.0 and match the changelog's newest section.
    - The summary records that the tag naming convention begins with this tag.
    - No release object was created.
  </acceptance_criteria>
  <done>The released commit is named, and the name points at what was released.</done>
</task>

<task type="auto">
  <name>Task 4: Bring the global mirror back to released bytes</name>
  <read_first>
    - `hooks/capability-auto-install.sh`, the bundle selection and the recorded-hash logic
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md` decision D-17
    - the recorded hash values in the summaries of plans 24-01 through 24-08
  </read_first>
  <files>.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-09-SUMMARY.md</files>
  <precondition>The tag is pushed and main holds the released bytes, so a released bundle exists to restore from; before that there is nothing correct to restore to.</precondition>
  <action>
    Throughout this phase the global mirror has been at risk of holding bytes from the working tree rather than from a release, because the install hook re-installs on session start from whichever bundle it finds. Now that a release exists, put the mirror back on it.

    Install from the released bundle, meaning the one the plugin host materialises from the published repository, not the worktree directory that has been edited all phase. Then read the recorded hash and confirm it equals the released bundle's hash rather than the worktree bundle's hash; that equality is the whole check, and comparing against the wrong side is how this went wrong before.

    Collect the hash values every plan in this phase recorded, put them in one place in the summary in order, and say where they changed and whether each change was intended. A phase that touched a self-installing plugin nine times should be able to account for every time its own installed state moved.

    If the mirror cannot be restored from a released bundle, do not fall back to installing the worktree. Say so and leave it, because installing the worktree is the failure being guarded against, not a workaround for it.
  </action>
  <verify>
    <automated>cat ~/.gsd/capability-auto-install-sota-numerics.hash 2>/dev/null || echo ABSENT</automated>
    <fails_when>the printed value equals the hash of the worktree bundle, or the output reads `ABSENT`.</fails_when>
  </verify>
  <verify>
    <automated>M=$(cd ~/.gsd/capabilities/sota-numerics 2>/dev/null && find . -type f ! -path '*/__pycache__/*' -exec sha256sum {} + | sort -k2 | sha256sum | cut -c1-16) && W=$(cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013/.gsd/capabilities/sota-numerics && find . -type f ! -path '*/__pycache__/*' -exec sha256sum {} + | sort -k2 | sha256sum | cut -c1-16) && echo "mirror=$M worktree=$W"</automated>
    <fails_when>the two printed digests are equal, which shows the mirror is byte-identical to the edited worktree and so was installed from it rather than from a release.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads && grep -cE '^\| *24-0[1-9] *\|' .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-09-SUMMARY.md || true</automated>
    <fails_when>the printed count is below `8`, which shows the per-plan hash ledger does not cover every plan in the phase.</fails_when>
  </verify>
  <acceptance_criteria>
    - The mirror was installed from a released bundle and the recorded hash matches that bundle.
    - The mirror is not byte-identical to the edited worktree bundle.
    - The summary carries a row per plan in the phase with the hash values that plan recorded, and names every point at which the value changed.
    - No fallback install from the worktree occurred.
  </acceptance_criteria>
  <done>The installed capability on this machine is the released one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| branch to published main | Merging crosses from a private branch to bytes every installer fetches, with no ref pinning in between. |
| planner to developer | The release decision belongs to the developer; the plan may prepare it and must not imply it. |
| released bundle to local mirror | The install hook writes machine-wide state from whichever bundle it is pointed at. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-36 | Elevation of Privilege | merge to main without an explicit go-ahead | critical | mitigate | Task 1 is a blocking decision checkpoint that automatic advance cannot skip, and Task 2 will not run without the recorded affirmative answer. |
| T-24-37 | Tampering | merging on stale evidence | high | mitigate | Task 2 re-reads the behind-count with nothing between the reading and the merge, and requires the green run to be for the exact merged head. |
| T-24-38 | Spoofing | marketplace description diverging from published manifest | high | mitigate | Task 2 re-runs the comparison against the branch head before merging and the checks re-run it against published main afterwards. |
| T-24-39 | Repudiation | a tag pointing at something other than what shipped | medium | mitigate | Task 3 requires an annotated tag whose target equals published main, checked by comparing the two commit identifiers. |
| T-24-40 | Elevation of Privilege | mirror restored from the edited worktree | high | mitigate | Task 4 forbids the worktree fallback, and its checks fail both when the recorded hash is the worktree's and when the mirror is byte-identical to it. |
| T-24-41 | Denial of Service | force-push or hook bypass during publish | high | mitigate | Task 2 forbids both, and the merge goes through the pull request so the action is recorded against the review. |
</threat_model>

## Alternatives Considered

N/A — no mechanism choice

## Artifacts phase produces

- `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-09-SUMMARY.md` — the developer's answer verbatim, the merge-instant readings, the merge and head commits with the green run identifier, the tag target, and the per-plan hash ledger.
- `origin/main` on `davdittrich/sota-numerics` carrying 0.2.0, with an annotated tag on the merge commit.
- A global mirror installed from the released bundle.

<verification>
- The checkpoint was answered by the developer before anything merged.
- Continuous integration was green for the merged head, the descriptions matched at the merge instant, and the behind-count was zero when read immediately before merging.
- An annotated tag points at published main, and both declared versions there read 0.2.0.
- The recorded install hash is the released bundle's, and the mirror is not the worktree's bytes.
</verification>

<success_criteria>
0.2.0 is published because the developer said to publish it, every irreversible step followed evidence measured at the moment of the step, and the machine that built it is running the released artefact rather than the workspace.
</success_criteria>

<output>
Create `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-09-SUMMARY.md` when done.
</output>
