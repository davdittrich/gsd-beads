---
beads_epic: gsd-beads-25vc
phase: 24-remediate-sota-numerics-0-2-0-release-blockers
plan: 04
type: tdd
wave: 3
depends_on: [24-03]
target_repo: /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013
files_modified:
  - .gsd/capabilities/sota-numerics/scripts/check-alternatives.py
  - tests/test_check_alternatives.py
autonomous: true
requirements: [D-01, D-05, D-07, D-08, D-17]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "No diagnostic line the gate writes to stderr exceeds the recorded bound, whatever the plan document contains (D-07)."
    - "Every quoted span in a diagnostic that was truncated carries an explicit elision marker, so a reader can tell truncation from absence (D-08)."
    - "Each violation reports as one plan path, line number and reason, and a run emits exactly one remediation line (D-08)."
    - "The bounded diagnostics change no verdict: every corpus exit code is unchanged (D-05)."
  artifacts:
    - .gsd/capabilities/sota-numerics/scripts/check-alternatives.py
    - tests/test_check_alternatives.py
    - .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-04-SUMMARY.md
  key_links:
    - "validate_plan returns one reason string per plan and main() prints it, so a line number has to travel out of validate_plan with the reason rather than being recovered by the printer."
    - "entry_years collects every four-digit year in an entry span, so an entry span that grows to document size is what turns one diagnostic into thousands of characters."
---

<objective>
Bound every diagnostic the gate emits, and give each violation the plan path, line and reason shape the capability already documents.

One failure path joins a whole entry span into a single stderr line, measured by the prose review at 12,280 characters and 8,099 tokens on one line. A gate that reports a violation by flooding the context window defeats the purpose it was added for (D-07).

Purpose: the gate is `blocking: true` with `onError: halt`, so its stderr is what an author reads at the moment planning stops. Output: bounded, elision-marked, line-numbered diagnostics with the run's single remediation line preserved (D-08).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/REVIEW-PROSE-TOKENS.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-03-SUMMARY.md

All source edits land in the worktree named by `target_repo`, on branch
`feat/extended-sota-definition`. Never edit `~/.gsd/capabilities/sota-numerics/...`; that is
the install mirror, not source.

D-17: record `~/.gsd/capability-auto-install-sota-numerics.hash` before the first edit and
after the last one, in `24-04-SUMMARY.md`.

The D-05 corpus baseline for this plan is the post-edit table in `24-03-SUMMARY.md`. These
changes touch message text and not verdict logic, so every corpus exit code must be identical
before and after; a changed exit code means verdict logic moved and is a finding.

D-08 fixes the shape but leaves the widths to this plan (D-24 discretion). The widths chosen
here are: any quoted span from the document truncated to 80 characters, any list of found
values truncated to its first 5 distinct values, and any one stderr line held under 200
characters. Record them in the summary so a later reader can see the bound was picked rather
than inherited.
</context>

<tasks>

<task type="auto" tracker-id="beads:gsd-beads-25vc.9" tdd="true">
  <name>Task 1: Bound every quoted span a diagnostic can carry, with an explicit elision marker</name>
  <beads-id>gsd-beads-25vc.9</beads-id>
  <files>.gsd/capabilities/sota-numerics/scripts/check-alternatives.py, tests/test_check_alternatives.py</files>
  <!-- beads: content synced to bd -- see `bd show gsd-beads-25vc.9` -->
</task>

<task type="auto" tracker-id="beads:gsd-beads-25vc.10" tdd="true">
  <name>Task 2: Report each violation as plan path, line number and reason, with one remediation line per run</name>
  <beads-id>gsd-beads-25vc.10</beads-id>
  <files>.gsd/capabilities/sota-numerics/scripts/check-alternatives.py, tests/test_check_alternatives.py</files>
  <!-- beads: content synced to bd -- see `bd show gsd-beads-25vc.10` -->
</task>

<task type="auto" tracker-id="beads:gsd-beads-25vc.11">
  <name>Task 3: Measure the diagnostic budget and record the widths chosen</name>
  <beads-id>gsd-beads-25vc.11</beads-id>
  <files>.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-04-SUMMARY.md</files>
  <!-- beads: content synced to bd -- see `bd show gsd-beads-25vc.11` -->
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan document to gate stderr | Attacker-influenced document text is interpolated into diagnostics an agent then reads. |
| gate stderr to the session context window | Whatever the gate prints is consumed by the agent that triggered the halt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-13 | Denial of Service | unbounded diagnostic interpolation | high | mitigate | A crafted document can flood the reader's context through one stderr line; Task 1 bounds every document-derived span and pins the bound with worst-case fixtures. |
| T-24-14 | Spoofing | truncated spans read as complete | medium | mitigate | Truncation without a marker lets a cut span look like the whole value; the helper appends an elision marker only when it cut, and a test asserts short messages carry none. |
| T-24-15 | Tampering | newline injection into diagnostics | medium | mitigate | Spans are truncated to a bounded width, so a document cannot forge extra violation lines at scale; the per-run remediation count check pins that exactly one remediation line is emitted. |
| T-24-16 | Denial of Service | diagnostics change moving a verdict | medium | mitigate | Task 1 and Task 2 both re-run the corpus sweep against the plan 24-03 table, so a message-only change that alters an exit code stops the commit. |
</threat_model>

## Alternatives Considered

- **Slice with an explicit elision marker**: one small helper that cuts a span at a fixed width and appends a marker only when it cut, applied at each interpolation site. Stdlib only, and the marker distinguishes truncation from absence, which is what makes a bounded diagnostic still trustworthy. The GSD capability's own precedent already specifies this shape for its diagnostics (`.gsd/capabilities/sota-numerics/NOTES.md`, 2026).
- **`textwrap.shorten` from the standard library**: collapses whitespace and truncates on a word boundary with a configurable placeholder (`https://docs.python.org/3/library/textwrap.html`, 2025). Nicer output for prose, but it normalises whitespace, so a quoted heading no longer matches the document byte for byte and a reader cannot search for what was quoted.
- **Emit full diagnostics at a lower verbosity and a summary by default**: keep the whole span behind a verbosity flag rather than cutting it. Standard logging practice (`https://docs.python.org/3/howto/logging.html`, 2025), but the gate has one stderr stream and no caller that could pass a flag, since the gate command is constant by design, so the flag would be unreachable.

Decided by: simplicity. Option one is a handful of lines with no whitespace surprises and no unreachable configuration; option two rewrites the quoted bytes it is meant to quote; option three adds a control surface the gate's constant command cannot use. Performance is irrelevant at these sizes.

### Internal design alternatives

- **Deduplicate and sort the year list before truncating versus truncating in document order**: sorting makes the message stable when the same years appear in a different order, so the same defect always reads the same way.
- **Carry the line number in the violation tuple versus recovering it in the printer**: only `validate_plan` knows which offset the reason is about, so recovering a line in the printer would guess.

## Artifacts phase produces

- `.gsd/capabilities/sota-numerics/scripts/check-alternatives.py` — bounded, elision-marked, line-numbered diagnostics with one remediation line per run.
- `tests/test_check_alternatives.py` — worst-case length fixtures and violation-shape assertions.
- `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-04-SUMMARY.md` — the measured diagnostic budget and the widths chosen.

<verification>
- The Python suite passes.
- The worst-case stderr line is under the recorded ceiling.
- A violating phase directory yields plan-path, line and reason lines plus exactly one remediation line.
- Corpus A exit codes are unchanged from plan 24-03.
</verification>

<success_criteria>
The gate names where a plan is wrong, in bounded output, and no document can turn a diagnostic into a context flood.
</success_criteria>

<output>
Create `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-04-SUMMARY.md` when done.
</output>
