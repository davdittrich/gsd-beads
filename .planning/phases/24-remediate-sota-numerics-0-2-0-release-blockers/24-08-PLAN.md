---
phase: 24-remediate-sota-numerics-0-2-0-release-blockers
plan: 08
type: execute
wave: 7
depends_on: [24-07]
target_repo: /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013
files_modified:
  - .gsd/capabilities/sota-numerics/scripts/check-alternatives.py
  - .gsd/capabilities/sota-numerics/capability.json
  - .gsd/capabilities/sota-numerics/NOTES.md
  - CHANGELOG.md
  - README.md
  - tests/test-gate-script-resolution.sh
autonomous: true
requirements: [D-15, D-16, D-17]
estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every review thread on the pull request is enumerated from the API and carries a disposition, rather than a count taken from prose (D-15)."
    - "Every finding in every internal review artifact appears exactly once in a disposition ledger, as fixed, tracked or declined with a reason (D-16)."
    - "No finding is closed by asserting the phase already handled it; the handling is named by commit or by check."
    - "The branch still passes the phase's own regression evidence after the last accepted fix."
  artifacts:
    - .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/DISPOSITION.md
    - .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-08-SUMMARY.md
  key_links:
    - "Several open threads name files that waves 1 through 5 rewrote, so a thread can be answerable by work already done, but only if the answering commit is named."
    - "This is the last plan before the publish decision, so a finding left without a disposition here ships without one."
---

<objective>
Close the review loop before the branch is offered for merge: every thread on the pull request and every finding in every internal review artifact gets a disposition on the record.

The standing rule is that internal review passes and their accepted fixes land on the branch before anything triggers external review, and that no finding is deferred without explicit approval and a tracking ticket. This plan is where that rule is discharged for the phase.

Purpose: the release decision in the next plan is only meaningful if the set of known-and-unhandled findings is empty by construction rather than by nobody having counted.
Output: a disposition ledger covering every finding from every source, and a pull request whose threads are each answered.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/REVIEW-CRITICAL-FINAL.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/REVIEW-PONYTAIL-FINAL.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/REVIEW-AGY-FINAL.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/REVIEW-PROSE-TOKENS.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-07-SUMMARY.md

All edits land in the worktree named by `target_repo`, on branch
`feat/extended-sota-definition`, which is the head of pull request 4 on
`davdittrich/sota-numerics`. Never edit `~/.gsd/capabilities/sota-numerics/...`.

D-17: record `~/.gsd/capability-auto-install-sota-numerics.hash` before the first edit and
after the last one, in `24-08-SUMMARY.md`.

Measured against the live API on 2026-09-07, as the starting point rather than as the answer:
the pull request carried 12 review threads, 7 resolved and 5 unresolved, the open ones touching
the capability manifest, the gate-script resolution test, the gate script at two separate
lines, and the changelog. The phase context describes this set as roughly eight; the live
number is what counts. Re-query rather than reusing any of these numbers, since waves 1 through
7 push commits that may move threads or add new ones.

The `files_modified` list bounds what an accepted fix may touch. A finding whose fix falls
outside that list is escalated rather than absorbed: at this point in the phase a surprise edit
to an unlisted file is a scope change, not a fix.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate every review thread from the API and give each one a disposition</name>
  <read_first>
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md` decisions D-15 and D-16
    - the summaries of plans 24-01 through 24-07, for what the phase already changed and in which commit
  </read_first>
  <files>.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/DISPOSITION.md</files>
  <precondition>The `gh` CLI is authenticated against `davdittrich/sota-numerics` with permission to read review threads and post comments; confirm with a read of the pull request before answering any thread.</precondition>
  <action>
    Record the pre-edit hash value first.

    Query the pull request's review threads through the API, taking resolved and unresolved alike, and write the total and the split into the ledger. Do not take the count from any prose description, the phase context's included: that number predates the pushes of waves 1 through 7, and the decision's own wording is approximate where the API is exact.

    For each thread decide one of three things and say which. Either the phase already answered it, in which case name the commit that answered it and quote the line of the fix, then reply on the thread with that reference and resolve it. Or it is right and unanswered, in which case fix it now, within the bounded file list, and reply with the fix commit. Or it is wrong or out of scope, in which case reply saying why, on the thread, so the reasoning sits where the reviewer can contest it instead of only in a local file.

    Do not resolve a thread whose fix has not landed. Resolving is a claim that the concern is addressed on this branch, and resolving ahead of the commit turns the review record into a false one.

    Where a thread duplicates another, say so and point at the one carrying the disposition, so the ledger holds one row per distinct concern and a cross-reference for the rest.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && gh api graphql -f query='query { repository(owner:"davdittrich",name:"sota-numerics"){ pullRequest(number:4){ reviewThreads(first:100){ totalCount nodes{ isResolved } } } } }' --jq '"total=\(.data.repository.pullRequest.reviewThreads.totalCount) open=\([.data.repository.pullRequest.reviewThreads.nodes[]|select(.isResolved|not)]|length)"'</automated>
    <fails_when>the printed `total` is larger than the number of rows the ledger carries, which shows a thread exists that the ledger never dispositioned.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads && grep -cE '^\| *T-[0-9]+ *\|' .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/DISPOSITION.md || true</automated>
    <fails_when>the printed count is `0`, or the file is absent, either of which shows no per-thread ledger row was written.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads && awk '/^\| *T-[0-9]+ *\|/ && $0 !~ /(fixed|already|declined|duplicate)/' .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/DISPOSITION.md | wc -l</automated>
    <fails_when>the printed count is greater than `0`, which shows a thread row carries no recognised disposition.</fails_when>
  </verify>
  <acceptance_criteria>
    - The ledger states the live total and the resolved-and-open split, taken from the API.
    - Every thread has a row, and every row states one of: fixed here, already answered by a named commit, declined with a reason, or duplicate of a named row.
    - Every thread resolved by this task has its fix already committed on the branch.
    - Every declined thread has the reason posted on the thread, not only in the ledger.
  </acceptance_criteria>
  <done>No thread on the pull request is unanswered, and none is resolved ahead of its fix.</done>
</task>

<task type="auto">
  <name>Task 2: Give every finding in every internal review artifact a disposition, and a ticket for each deferral</name>
  <read_first>
    - the four review artifacts in the phase directory, in full
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md` decision D-16 and the deferred-ideas section
    - the summaries of plans 24-01 through 24-07
  </read_first>
  <files>.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/DISPOSITION.md</files>
  <action>
    Enumerate every finding in each of the four review artifacts, keeping the identifier each artifact gives it, and the artifact name alongside, so two artifacts numbering from one do not collide in the ledger. Include the findings the artifacts themselves marked non-blocking: the reason those are in the ledger is that non-blocking is a severity, not a disposition, and a phase that ships them unexamined has decided about them without saying so.

    Give each finding one of four dispositions. Fixed, naming the commit. Already handled by an earlier plan in this phase, naming the plan and the commit. Deferred, which requires a bd ticket opened now with the finding's text and a link back to the artifact, and the ticket identifier recorded in the row; a deferral without a ticket is not a disposition. Declined, which requires the reason in the row, stated as what would have to be true for the finding to be right.

    Treat the deletion set one of the reviews proposes as a single decision with a stated line count rather than as a set of independent removals, and record whether it was taken whole, taken in part with the parts named, or declined. A deletion set half-applied without saying which half is the worst outcome available, because it leaves nobody able to tell what remains proposed.

    Where a finding disagrees with a locked decision in the phase context, the decision wins and the row says so, naming the decision. Where a finding disagrees with a deferred idea, the row says the idea is out of scope for this phase and names it.

    Apply the fixes you have marked fixed within the bounded file list. Anything needing a file outside that list stops and is written up in the summary as a scope escalation for the publish decision to weigh.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads && awk '/^\| *(CRIT|PONY|AGY|PROSE)/ && $0 !~ /(fixed|already|deferred|declined)/' .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/DISPOSITION.md | wc -l</automated>
    <fails_when>the printed count is greater than `0`, which shows a finding row carries no recognised disposition.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads && awk '/^\|/ && /deferred/ && $0 !~ /[a-z-]+-[a-z0-9]+\.[0-9]+|[a-z-]+-[a-z0-9]{2,}/' .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/DISPOSITION.md | wc -l</automated>
    <fails_when>the printed count is greater than `0`, which shows a deferral was recorded without a ticket identifier beside it.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && ST=$(git status --porcelain -- .); S=$?; if [ "$S" -ne 0 ]; then echo GITFAIL; else awk 'BEGIN{split(".gsd/capabilities/sota-numerics/scripts/check-alternatives.py .gsd/capabilities/sota-numerics/capability.json .gsd/capabilities/sota-numerics/NOTES.md CHANGELOG.md README.md tests/test-gate-script-resolution.sh", a, " "); for(i in a) ok[a[i]]=1} NF{ if(!($NF in ok)) n++ } END{print n+0}' <<< "$ST"; fi</automated>
    <fails_when>the output is `GITFAIL`, or the printed count is greater than `0`, which shows a fix touched a file outside the bounded list without escalating.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every finding in all four artifacts has exactly one ledger row, identified by artifact and finding identifier.
    - Every row states fixed with a commit, already handled with a plan and commit, deferred with a bd ticket identifier, or declined with a reason.
    - The proposed deletion set is recorded as a single decision with its extent named.
    - No working-tree change touches a file outside the bounded list.
  </acceptance_criteria>
  <done>Every finding raised against this branch has been decided about in writing.</done>
</task>

<task type="auto">
  <name>Task 3: Re-establish the phase's regression evidence after the last accepted fix</name>
  <read_first>
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-03-SUMMARY.md`, the cumulative regression evidence heading and the corpus definitions
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md` decisions D-05 and D-16
  </read_first>
  <files>.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-08-SUMMARY.md</files>
  <precondition>Tasks 1 and 2 have committed every fix they accepted, so the sweep runs against the branch state the publish decision will act on rather than against a dirty tree.</precondition>
  <action>
    The regression evidence the phase rests on was captured before the first gate edit and re-measured after wave 3. Fixes accepted in Tasks 1 and 2 land after that, so the evidence has to be re-established or it no longer describes the branch.

    Re-run both corpora exactly as the earlier plan defined them, using the commands recorded there rather than reconstructed ones, and put the results beside the previous run under the same heading. State which verdicts changed and why each change was intended. A verdict that changed for a reason nobody planned is the finding this whole sweep exists to surface: stop and write it up rather than accepting it because the suite is green.

    Run the shell test suites and the Python suite too, and record the counts. Then confirm the branch is not behind its base and record how far ahead it is, because the publish plan re-checks that at the instant of opening and a first measurement here makes an unexpected change visible.

    Record the post-edit hash value, and compare it with the pre-edit value from Task 1. If they differ, say which command changed it and whether the mirror now holds unreleased bytes; that is the hazard the decision exists for, and the publish plan must know before it decides.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && python3 -m unittest tests/test_check_alternatives.py 2>&1 | tail -3</automated>
    <fails_when>the run exits non-zero, or the trailing output does not read `OK`.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && for t in tests/test-session-start.sh tests/test-capability-auto-install.sh tests/test-gate-script-resolution.sh; do [ -f "$t" ] && bash "$t" >/dev/null 2>&1 && echo "ok $t" || echo "bad $t"; done</automated>
    <fails_when>any printed line begins with `bad`, which names a shell suite that did not exit zero or is missing.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && git fetch origin --quiet && git rev-list --left-right --count origin/main...HEAD</automated>
    <fails_when>the first number is greater than `0`, which shows the branch is behind its base and the publish plan's ordering check would not hold.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads && grep -cE 'cumulative evidence' .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-08-SUMMARY.md || true</automated>
    <fails_when>the printed count is `0`, which shows the re-run was not recorded under the heading the earlier plan established.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both corpora are re-run with the recorded commands and their results sit beside the previous run.
    - Every changed verdict is named with the change that caused it, and any unexplained change stops the task.
    - The Python suite and every shell suite pass, with counts recorded.
    - The behind-count against the base is zero and the ahead-count is recorded.
    - Both hash values appear in the summary with a statement of whether they match.
  </acceptance_criteria>
  <done>The branch the publish decision will act on has current regression evidence rather than evidence from three waves ago.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review record to future reader | A resolved thread is a durable claim that a concern was addressed on this branch. |
| ledger to release decision | The next plan's decision assumes the unhandled-findings set is empty; the ledger is the only thing establishing that. |
| accepted fix to bounded scope | Fixes land after the phase's regression evidence was captured, so an unbounded fix can invalidate it silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-31 | Repudiation | thread resolution ahead of its fix | high | mitigate | Task 1 forbids resolving before the fix commits, and its checks compare the live thread total against the ledger row count so no thread goes unrecorded. |
| T-24-32 | Repudiation | silently dropped non-blocking findings | high | mitigate | Task 2 requires a row per finding across all four artifacts and rejects any row lacking one of the four dispositions; deferrals require a ticket identifier. |
| T-24-33 | Tampering | fixes reaching files outside the bounded list | medium | mitigate | Task 2's third check enumerates the working tree against the bounded list and fails on anything outside it. |
| T-24-34 | Tampering | regression evidence going stale under late fixes | high | mitigate | Task 3 re-runs both corpora after the last accepted fix and requires every changed verdict to be explained. |
| T-24-35 | Elevation of Privilege | global mirror replaced during triage | high | mitigate | Task 3 compares the install hash against the value recorded before the first edit and reports any divergence to the publish decision. |
</threat_model>

## Alternatives Considered

N/A — no mechanism choice

## Artifacts phase produces

- `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/DISPOSITION.md` — one row per review thread and one per internal finding, each with a disposition, a commit, a ticket or a reason.
- `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-08-SUMMARY.md` — the re-run regression evidence, suite counts, the ahead-and-behind measurement, both hash values, and any scope escalation.
- Accepted fixes committed on `feat/extended-sota-definition`, each within the bounded file list.

<verification>
- The live thread total equals the ledger's thread row count, and every row carries a disposition.
- Every finding across the four artifacts has exactly one row, and every deferral names a ticket.
- No working-tree change lies outside the bounded file list.
- Both corpora re-run, all suites pass, the branch is zero behind its base, and both hash values are recorded.
</verification>

<success_criteria>
Nothing known about this branch is undecided, and the evidence describing the branch was measured after the last change to it.
</success_criteria>

<output>
Create `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-08-SUMMARY.md` when done.
</output>
