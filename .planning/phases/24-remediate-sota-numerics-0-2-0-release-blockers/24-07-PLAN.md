---
phase: 24-remediate-sota-numerics-0-2-0-release-blockers
plan: 07
type: execute
wave: 6
depends_on: [24-06]
target_repo: /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013
files_modified:
  - README.md
autonomous: true
requirements: [D-10, D-17]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every behavioural claim the README makes about the gate is traced to the code that decides it, and the trace is recorded rather than asserted (D-10)."
    - "The two claims the reviews named as false state what the code actually does, proved by running the code rather than by reading it."
    - "Every command the README tells a reader to run has been run as written, or is recorded as excluded with the reason it was not run."
  artifacts:
    - README.md
    - .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-07-SUMMARY.md
  key_links:
    - "The gate classifies a plan-shaped name that the strict file-name pattern rejects as misnamed and reports it, so a name the README calls ignored actually fails the phase."
    - "The dirty-bundle guard inspects ignored files as well as tracked ones, so the README's account of what the status check overlooks is inverted."
---

<objective>
Make the README's description of the gate agree with the gate, across the whole file rather than at the two points a review happened to check.

Two claims were caught by review. The reason to trace all of them is that nobody knows how many more there are: the two that were caught were caught by a reader reading closely, not by anything that checks. This plan applies the whole-file claim trace that Phase 23 used on this README to the claims that trace passed over, plus every claim added since.

Purpose: this README is the only description an installer reads before trusting the gate. A false claim in it is worse than no claim, because it is relied on.
Output: a README whose behavioural claims are each traced to code, and a recorded trace an auditor can re-run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/REVIEW-AGY-FINAL.md
@.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-CONTEXT.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-06-SUMMARY.md

All edits land in the worktree named by `target_repo`, on branch
`feat/extended-sota-definition`. Never edit `~/.gsd/capabilities/sota-numerics/...`.

D-17: record `~/.gsd/capability-auto-install-sota-numerics.hash` before the first edit and
after the last one, in `24-07-SUMMARY.md`.

Phase 23 decision D-20 established the claim-trace method this plan reuses: enumerate the
claims, name the code that decides each, and record the pairing, so a later reader can check
the trace rather than repeat it.

Measured in this worktree on 2026-09-07, as the starting point rather than as the answer:
`README.md` is 266 lines, of which 196 sit outside fenced code blocks; 18 of those are table
rows, and 38 carry a verb asserting gate behaviour. Re-measure before relying on any of it;
plans 24-01 through 24-06 may have moved the file.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the two claims the reviews named, and prove each correction by running the gate</name>
  <read_first>
    - `README.md` around lines 55-60, the row about a bundle holding uncommitted or ignored files
    - `README.md` around lines 88-95, the sentences describing which file names the gate looks at
    - `.gsd/capabilities/sota-numerics/scripts/check-alternatives.py` lines 38-70 and 286-310, the two file-name patterns and the discovery function that classifies names against them
    - `.gsd/capabilities/sota-numerics/scripts/check-alternatives.py` around line 613, where the discovery result becomes violations
    - `hooks/capability-auto-install.sh` around line 184, the status invocation the README row describes
  </read_first>
  <files>README.md</files>
  <precondition>`hooks/capability-auto-install.sh` on this branch already passes the ignored-files option to its status invocation, which is the fact the first correction rests on; confirm it before rewriting the row.</precondition>
  <action>
    Record the pre-edit hash value first.

    Take the row about a bundle holding uncommitted or ignored files. Its remedy sentence describes a byte-compiled cache directory as something the status check overlooks while a directory copy would publish it. Read the status invocation the guard actually makes: it asks about ignored files too, so that directory is exactly what the guard reports, and it is the reason the guard fires rather than something the guard misses. Rewrite the sentence to say what makes the guard fire and what the reader should do, and drop the clause asserting the check finds the bundle unproblematic. Keep the row's structure, its condition column and its message column unchanged.

    Then take the sentences about which file names are looked at. The gate holds two name patterns: a strict one deciding which files get checked, and a wider one catching files shaped like plans that the strict one turned down. A name matching the wider pattern but not the strict one is collected as misnamed and becomes a violation, so such a name fails the phase rather than passing unexamined. The README's example name matches the wider pattern. Rewrite the sentence to separate the two outcomes: names reached by neither pattern, and files nested below the directory, genuinely are not looked at, because the directory listing does not recurse; a plan-shaped name the strict pattern rejects is reported. Keep the example name, since it is a good example of the reported case, and change what is claimed about it.

    Do not change either pattern or the discovery function to make the old sentences true. The discovery function's own docstring argues for reporting rather than skipping, and that argument holds: silently skipping would make a phase whose only plan is misnamed indistinguishable from a phase whose plans all passed.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && grep -icE 'calls (it )?clean|considers (it )?clean|treats (it )?as clean' README.md || true</automated>
    <fails_when>the printed count is greater than `0`, which shows the inverted description of the status check survives.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && D=$(mktemp -d) && mkdir -p "$D/p" && printf '# x\n' > "$D/p/draft-PLAN.md" && python3 .gsd/capabilities/sota-numerics/scripts/check-alternatives.py "$D/p" >/dev/null 2>&1; echo "exit=$?"; rm -rf "$D"</automated>
    <fails_when>the output is `exit=0`, which would mean the gate really does pass over that name and the rewritten sentence is the wrong correction.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && sed -n '/draft-PLAN\.md/,+2p' README.md | grep -ci 'misnamed' || true</automated>
    <fails_when>the printed count is `0`, which shows the rewritten sentence still does not say that such a name is reported.</fails_when>
  </verify>
  <acceptance_criteria>
    - The uncommitted-or-ignored row states the guard reports the cache directory, and asserts nothing about the check finding the bundle clean.
    - The file-name sentences distinguish names nothing matches, and files nested below the directory, from plan-shaped names the strict pattern rejects.
    - The empirical run over a directory holding only that example name exits non-zero, and the summary records the exit status observed.
    - Neither name pattern nor the discovery function is modified.
  </acceptance_criteria>
  <done>The two named claims describe what running the code produces.</done>
</task>

<task type="auto">
  <name>Task 2: Trace every remaining behavioural claim in the file to the code that decides it</name>
  <read_first>
    - `README.md` in full
    - `.gsd/capabilities/sota-numerics/scripts/check-alternatives.py`, the pattern definitions, the fence handling, the section-boundary logic and the main entry point
    - `hooks/capability-auto-install.sh` and `hooks/session-start.sh`, for the claims about install and banners
    - `.gsd/capabilities/sota-numerics/capability.json`, for the claims about which lifecycle points dispatch
    - `.planning/phases/23-extend-sota-definition-in-sota-numerics-and-refactor-plugin/23-CONTEXT.md` decision D-20, the trace method
  </read_first>
  <files>README.md, .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-07-SUMMARY.md</files>
  <action>
    Enumerate the file's behavioural claims first, and write the enumeration rule down before applying it, so the coverage number means something. A behavioural claim is any sentence, table cell or list item outside a fenced block that asserts what the gate, the hook or the install does, accepts, rejects, counts, ignores or reports. Measured on 2026-09-07 the file held 196 non-fenced lines of which 38 carry such a verb; treat that as the scale rather than the target, and re-derive it.

    For each claim, name the file and line of the code that decides it, then say whether the code agrees. Where it agrees, record the pairing and move on. Where it disagrees, change the README rather than the code, unless the code is what is wrong, in which case stop and say so in the summary rather than editing the gate under a documentation plan: a behaviour change at this point in the phase would invalidate the regression sweep that waves 1 through 3 established.

    Record the whole trace in the summary as a table, one row per claim, each row carrying the README line, the deciding code location, the verdict, and the edit made if any. A claim that could not be traced to any code location gets a row saying so; that is a finding, not an omission, because a behavioural claim no code decides is either aspirational or about something outside this bundle, and the reader deserves to know which.

    Prefer deleting a claim to weakening it. A sentence hedged until it is safe tells the reader nothing while still occupying the place where a checkable statement would go.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads && grep -cE '^\| *(README\.md)?:?[0-9]+ *\|' .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-07-SUMMARY.md || true</automated>
    <fails_when>the printed count is below `25`, which against the 38 measured claim-carrying lines shows the trace stopped well short of the file.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads && grep -ciE 'enumeration rule|claim is any' .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-07-SUMMARY.md || true</automated>
    <fails_when>the printed count is `0`, which shows the coverage number was reported without the rule that would let anyone reproduce it.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && D=$(git diff --name-only HEAD -- .gsd/capabilities/sota-numerics/scripts/ hooks/); S=$?; if [ "$S" -ne 0 ]; then echo GITFAIL; else awk 'NF{n++} END{print n+0}' <<< "$D"; fi</automated>
    <fails_when>the output is `GITFAIL`, or the printed count is greater than `0`, which shows this documentation plan changed executable code.</fails_when>
  </verify>
  <acceptance_criteria>
    - The summary states the enumeration rule and the count of claims it produced.
    - Every enumerated claim has a trace row naming a code location or saying none was found.
    - Every disagreement is resolved by editing the README, or escalated in the summary if the code is the wrong side.
    - No file under the scripts or hooks directories is modified by this plan.
  </acceptance_criteria>
  <done>Each behavioural claim in the README is paired with the code deciding it, and the pairing is on record.</done>
</task>

<task type="auto">
  <name>Task 3: Run the commands the README tells the reader to run</name>
  <read_first>
    - `README.md`, every fenced block containing a command
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md` decision D-17
  </read_first>
  <files>README.md, .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-07-SUMMARY.md</files>
  <action>
    A command in a README is a claim that running it produces the described result, and it is the one kind of claim that can be checked by doing rather than by reading.

    List every fenced block that holds a command. Classify each as read-only or state-mutating. Run every read-only one exactly as written, from the working directory the surrounding prose implies, and record the exit status and the first line of output. Where the output disagrees with what the prose says the reader will see, correct the prose or correct the command, whichever is wrong, and say which in the summary.

    Do not run the state-mutating ones. Anything that installs a capability at global scope is precisely the hazard D-17 exists for, and running it mid-phase would replace the mirror with an unreleased bundle. Record each excluded block with the reason it was excluded, and say what a reader would have to do to check it safely; an unrun command recorded as unrun is honest, an unrun command silently counted as checked is not.

    Where a block's expected output is quoted in the prose, check the quotation against what the run produced rather than against what it plausibly should be. Where a block carries no expected output, decide whether adding one helps: for a command whose value is that it exits zero, saying so is worth a line; for a command whose output varies by environment, quoting a specimen would create the next stale claim.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads && grep -ciE 'excluded|not run' .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-07-SUMMARY.md || true</automated>
    <fails_when>the printed count is `0`, which shows no block was recorded as deliberately unrun even though the install commands cannot be run under D-17.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads && grep -cE '^\| *`' .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-07-SUMMARY.md || true</automated>
    <fails_when>the printed count is `0`, which shows the summary carries no per-command row recording what was run and what it returned.</fails_when>
  </verify>
  <verify>
    <automated>cat ~/.gsd/capability-auto-install-sota-numerics.hash 2>/dev/null || echo ABSENT</automated>
    <fails_when>the printed value differs from the value Task 1 recorded before its first edit, which shows a command run out of the README re-installed the bundle at global scope.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every fenced command block is listed and classified as read-only or state-mutating.
    - Every read-only block was run as written, with its exit status and first output line recorded.
    - Every state-mutating block is recorded as unrun with its reason and with what a reader would do to check it safely.
    - The recorded install hash is unchanged from before the first edit of this plan.
  </acceptance_criteria>
  <done>No command in the README is untested except the ones recorded as deliberately untested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README to installer | An operator decides whether to trust the gate by reading this file; a false claim here is trusted rather than checked. |
| documentation plan to executable code | This plan reads the gate and the hooks but must not change them, because the phase's regression evidence was established against them. |
| README command to host machine | A command copied out of this file runs on the reader's machine with the reader's privileges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-26 | Spoofing | README claims about what the gate rejects | high | mitigate | A claim that a name is ignored, when the gate reports it, teaches an operator to expect a pass where a failure comes; Task 1 corrects it and proves the correction by running the gate over that exact name. |
| T-24-27 | Repudiation | untraced behavioural claims | high | mitigate | Task 2 enumerates and traces every claim and records the pairing, so a later auditor checks the trace instead of re-deriving it. |
| T-24-28 | Elevation of Privilege | install commands quoted in the README | high | mitigate | Task 3 forbids running the state-mutating blocks and asserts the install hash is unchanged afterwards, so no README command republishes an unreleased bundle at global scope. |
| T-24-29 | Tampering | executable code under a documentation plan | medium | mitigate | Task 2's third check asserts no file under the scripts or hooks directories was modified. |
| T-24-30 | Information Disclosure | specimen command output quoted in the README | low | mitigate | Task 3 declines to quote environment-varying output rather than pasting a local run into the file. |
</threat_model>

## Alternatives Considered

N/A — no mechanism choice

## Artifacts phase produces

- `README.md` — behavioural claims agreeing with the code, with the two review-named claims corrected.
- `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-07-SUMMARY.md` — the enumeration rule, the claim-to-code trace table, the per-command run record with exclusions, and both hash values.

<verification>
- The inverted description of the status check is gone, and the corrected file-name sentence is backed by an observed non-zero exit.
- The trace table covers the enumerated claims and states the rule that produced them.
- No file under the scripts or hooks directories changed.
- Every read-only README command was run; every excluded one is recorded with a reason; the install hash is unchanged.
</verification>

<success_criteria>
Everything the README says the gate does is something the gate was observed to do, and everything it tells a reader to run has been run or recorded as deliberately unrun.
</success_criteria>

<output>
Create `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-07-SUMMARY.md` when done.
</output>
