---
phase: 24-remediate-sota-numerics-0-2-0-release-blockers
plan: 02
type: execute
wave: 1
depends_on: []
target_repo: /home/dd/projects/gsd-beads
files_modified:
  - .claude-plugin/marketplace.json
autonomous: true
requirements: [D-12]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The sota-numerics description an installer reads from the marketplace is byte-identical to the description the plugin manifest on the release branch declares (D-12)."
    - "The change is its own commit in gsd-beads, separable from every worktree commit (D-12)."
    - "The marketplace source stays a bare repository URL with no ref, so merging to main remains the publish (D-19)."
  artifacts:
    - .claude-plugin/marketplace.json
  key_links:
    - "marketplace.json's source entry carries no ref, so what an installer reads is whatever main holds at install time; the description must be correct before the merge, not after."
---

<objective>
Bring this repository's marketplace description for `sota-numerics` back into agreement with the plugin manifest on the release branch.

Purpose: the marketplace entry is what installers read, and its source is a bare repository URL with no ref, so the two repositories must agree at the moment of merge rather than afterwards (D-12, D-19).
Output: one commit in `gsd-beads` changing exactly one string.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md

This is the only plan in the phase that edits this repository. It touches no worktree file, so
it shares no file with any other plan and carries no D-17 exposure.

The authoritative string is the `description` field of
`/home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013/.claude-plugin/plugin.json`
on branch `feat/extended-sota-definition`. Read it at execution time rather than trusting a
transcription. No plan in this phase modifies that manifest, and reading it live is what makes
the comparison a check rather than a copy.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the marketplace description with the manifest description</name>
  <read_first>
    - `/home/dd/projects/gsd-beads/.claude-plugin/marketplace.json`, the `sota-numerics` entry
    - `/home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013/.claude-plugin/plugin.json`, the `description` field
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md` decision D-12
  </read_first>
  <files>.claude-plugin/marketplace.json</files>
  <action>
    Read the description field of the plugin manifest on the release branch, and set the description of the `sota-numerics` entry in this repository's marketplace file to that exact string. Change nothing else in the entry: its name, and its source object with the url kind and repository address, stay as they are, because D-19 depends on the entry pointing at the repository rather than at a tag. Preserve the file's existing indentation and key order so the diff is one line. Commit it alone, with a message naming the drift it closes.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads && A=$(jq -r '.plugins[]|select(.name=="sota-numerics").description' .claude-plugin/marketplace.json) && B=$(jq -r .description .worktrees/sota-numerics-release-013/.claude-plugin/plugin.json) && [ "$A" = "$B" ] && echo MATCH || echo DIFFER</automated>
    <fails_when>the output is `DIFFER`, or the command exits non-zero because either file will not parse as JSON.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads && jq -r '.plugins[]|select(.name=="sota-numerics").source|"\(.source) \(.ref // "no-ref")"' .claude-plugin/marketplace.json</automated>
    <fails_when>the output is anything other than the line `url no-ref`, which shows the source kind or a pinned ref was altered.</fails_when>
  </verify>
  <acceptance_criteria>
    - The marketplace description and the branch manifest description compare equal byte for byte.
    - The entry's source kind is still `url` and it still carries no `ref` key.
    - `git -C /home/dd/projects/gsd-beads show --stat HEAD` lists `.claude-plugin/marketplace.json` and no other path.
  </acceptance_criteria>
  <done>An installer reading the marketplace sees the same description the plugin declares.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the agreement as a re-runnable check for the publish wave</name>
  <read_first>
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md` decisions D-12 and D-19
  </read_first>
  <files>.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-02-SUMMARY.md</files>
  <action>
    Record in the summary the exact command that compares the two descriptions, the commit hash of the marketplace change, and the branch manifest commit the comparison was made against. State that plan 24-08 re-runs this comparison immediately before the merge, because the branch head moves between this wave and the publish wave and D-12 requires agreement at the moment of merge rather than at the moment of this edit.
  </action>
  <verify>
    <automated>grep -c 'moment of merge' /home/dd/projects/gsd-beads/.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-02-SUMMARY.md</automated>
    <fails_when>the command prints `0`, or exits non-zero because the summary file is absent.</fails_when>
  </verify>
  <acceptance_criteria>
    - The summary names the comparison command, the marketplace commit hash, and the branch manifest commit compared against.
    - The summary states that the publish plan re-runs the comparison before merging.
  </acceptance_criteria>
  <done>The cross-repo agreement is recorded with the means to re-check it at merge time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marketplace entry to installer | Every installer reads this description and this source address to decide what to fetch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-06 | Spoofing | `source.url` in the marketplace entry | high | mitigate | Task 1 changes only the description, and Task 1's second check asserts the source kind is still `url` with no `ref`, so the fetch address cannot drift under a description edit. |
| T-24-07 | Repudiation | description drift between the two repositories | medium | mitigate | Task 2 records the comparison command and both commit hashes, and plan 24-08 re-runs the comparison at merge time so agreement is proven at the moment it matters. |
| T-24-08 | Tampering | JSON structure of the marketplace file | low | mitigate | Both checks parse the file with `jq`, so a malformed edit exits non-zero rather than shipping. |
</threat_model>

## Alternatives Considered

N/A — no mechanism choice

## Artifacts phase produces

- `.claude-plugin/marketplace.json` — the `sota-numerics` description matching the branch manifest.
- `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-02-SUMMARY.md` — the comparison command and the two commit hashes it was run against.

<verification>
- The two descriptions compare equal.
- The marketplace source kind is `url` and carries no `ref`.
- The marketplace change is a single-file commit.
</verification>

<success_criteria>
This repository and the release branch state the same thing about what `sota-numerics` is, and the publish wave has a command to re-prove it at merge time.
</success_criteria>

<output>
Create `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-02-SUMMARY.md` when done.
</output>
