---
phase: 24-remediate-sota-numerics-0-2-0-release-blockers
plan: 06
type: execute
wave: 5
depends_on: [24-05]
target_repo: /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013
files_modified:
  - CHANGELOG.md
  - .gsd/capabilities/sota-numerics/scripts/check-alternatives.py
  - .gsd/capabilities/sota-numerics/NOTES.md
  - hooks/session-start.sh
  - .gsd/capabilities/sota-numerics/fragments/executor-numerics.md
autonomous: true
requirements: [D-09, D-11, D-17]
estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "No hand-maintained count survives in the bundle unless something recomputes or checks it (D-09)."
    - "One spelling of the drifted word is used across the whole bundle, and the choice is backed by a measured count rather than a preference (D-11)."
    - "The NOTES residual states the whole coupling, including the configuration key that can stop the gate dispatching at all."
  artifacts:
    - CHANGELOG.md
    - .gsd/capabilities/sota-numerics/NOTES.md
    - .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-06-SUMMARY.md
  key_links:
    - "The gate dispatch sits inside the step gated on the post-planning-gaps configuration key, so a project that turns that key off silently turns this blocking gate off with it."
    - "The test count in CHANGELOG's 0.2.0 prose moves every time this phase adds a fixture, which is why the count goes rather than gets corrected."
---

<objective>
Remove the claims in the bundle that stopped resolving, and complete the residual that understates a real coupling.

Three kinds of drift: hand-maintained counts that are stale the moment a test is added (D-09), one word spelled two ways inside one corpus (D-11), and a documented residual that describes a workflow ordering coupling but not the configuration key that can disable the gate outright.

Purpose: a release whose own changelog miscounts its own tests, and whose gate script comments assert corpus sizes nothing measures, cannot claim the precision the extended SOTA definition asks for.
Output: counts deleted or backed by a mechanism, one spelling, and a residual that names the whole coupling.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/REVIEW-CRITICAL-FINAL.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/REVIEW-PROSE-TOKENS.md
@.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-04-SUMMARY.md

All source edits land in the worktree named by `target_repo`, on branch
`feat/extended-sota-definition`. Never edit `~/.gsd/capabilities/sota-numerics/...`; that is
the install mirror, not source.

D-17: record `~/.gsd/capability-auto-install-sota-numerics.hash` before the first edit and
after the last one, in `24-06-SUMMARY.md`.

Measured 2026-09-07 in the worktree, as the starting point rather than as the answer:
`CHANGELOG.md` line 12 reports a 94-test suite of which 62 pass unchanged and 32 fail, pinned
to commit `253bbdc`; the suite at that time held 97 tests; plans 24-01, 24-03 and 24-04 each
add more. `check-alternatives.py` line 47 claims 76 entries across phase directories with 10
plan-shaped; the live count in this project is 73 entries and 9 plan-shaped. Re-measure both
rather than copying these numbers.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the counts nothing recomputes, in the changelog and in the gate script comment</name>
  <read_first>
    - `CHANGELOG.md` lines 1-20, the 0.2.0 prose carrying the suite counts and the per-category breakdown
    - `.gsd/capabilities/sota-numerics/scripts/check-alternatives.py` lines 40-50, the comment above the plan-file pattern
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md` decisions D-09 and D-24
  </read_first>
  <files>CHANGELOG.md, .gsd/capabilities/sota-numerics/scripts/check-alternatives.py</files>
  <precondition>`24-04-SUMMARY.md` exists, so the gate-script edits of waves 1 through 3 are complete and the suite size this task must not restate is final for the phase.</precondition>
  <action>
    Record the pre-edit hash value first.

    In the changelog's 0.2.0 section, remove the absolute suite size and the pass and fail split, and remove the per-category counts that break that split down. Keep the categories themselves and keep the commit the measurement was taken at, because naming which behaviours changed is the claim readers need and it stays true as the suite grows. Rewrite the affected sentences so they read as prose rather than as a subtraction with the numbers cut out. Do not add a test that checks these numbers: Phase 23 D-21 forbids test infrastructure for prose, which is exactly why the numbers go rather than gain a mechanism.

    In the gate script, delete the corpus measurement from the comment above the plan-file pattern, keeping the reasoning it introduces, which is why the pattern is narrower than any Markdown file. The comment's argument does not depend on the corpus size, so removing the numbers costs it nothing.

    Leave every count that something recomputes or checks. Where a number stays, say in the same sentence what recomputes it, so a later reader can tell a live number from a stale one.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && sed -n '/^## 0\.2\.0/,/^## 0\.1/p' CHANGELOG.md | grep -cE '\b(94|97|62|32|35)\b' || true</automated>
    <fails_when>the printed count is greater than `0`, which shows a suite-size or pass-and-fail number is still stated in the 0.2.0 section.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && sed -n '/^## 0\.2\.0/,/^## 0\.1/p' CHANGELOG.md | grep -ciE '[0-9]+ (phase-resolution|section-boundary|fence|heading|plan|message|discovery)' || true</automated>
    <fails_when>the printed count is greater than `0`, which shows a per-category count still breaks the suite split down.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && sed -n '38,52p' .gsd/capabilities/sota-numerics/scripts/check-alternatives.py | grep -cE '\b(76|73|10|9)\b' || true</automated>
    <fails_when>the printed count is greater than `0`, which shows the corpus measurement is still in the comment.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && python3 -m unittest tests/test_check_alternatives.py 2>&1 | tail -3</automated>
    <fails_when>the run exits non-zero, or the trailing output does not read `OK`, which would show a comment edit disturbed executable code.</fails_when>
  </verify>
  <acceptance_criteria>
    - The 0.2.0 changelog section states which behaviours changed and states no suite size, pass count or fail count.
    - The gate script comment keeps its argument and states no corpus measurement.
    - Any number left anywhere in the bundle is accompanied by what recomputes it.
    - The Python suite still passes, unchanged in count.
  </acceptance_criteria>
  <done>No number in the bundle is waiting to become wrong.</done>
</task>

<task type="auto">
  <name>Task 2: Settle the spelling on the one the corpus already uses, measured rather than assumed</name>
  <read_first>
    - `hooks/session-start.sh` line 34, the executor framing sentence
    - `.gsd/capabilities/sota-numerics/fragments/executor-numerics.md`, the fragment carrying the other spelling
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/REVIEW-PROSE-TOKENS.md`, the spelling finding
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-CONTEXT.md` decision D-11
  </read_first>
  <files>hooks/session-start.sh, .gsd/capabilities/sota-numerics/fragments/executor-numerics.md</files>
  <action>
    Count the spelling variants across the whole tracked bundle before changing anything, and record the counts. Count the drifted word both ways, and count at least one other word whose two spellings separate the same two conventions, because the drifted word itself is split evenly and cannot decide its own case. Measured 2026-09-07 in this worktree: the drifted word stands at one occurrence each way, while the second word stands at seven occurrences to nil, which settles the convention; re-measure rather than trusting that line.

    Apply the winning convention corpus-wide to the drifted word, changing whichever occurrences disagree with it. Change the framing sentence in the hook only in that word; its wording, length and punctuation stay, because that sentence is echoed into a session banner whose token cost the prose review already measured. Do not extend this task into a general spelling sweep of other words: D-11 names one drift, and the corpus shows no other.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && V=$(git grep -hoiwE 'favou?r' -- .); S=$?; if [ "$S" -gt 1 ]; then echo GITFAIL; else awk 'NF{seen[tolower($0)]=1} END{n=0; for(k in seen) n++; print n}' <<< "$V"; fi</automated>
    <fails_when>the output is `GITFAIL`, or the printed count is anything other than `1`, which shows more than one spelling survives.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && bash tests/test-session-start.sh 2>&1 | tail -5</automated>
    <fails_when>the run exits non-zero, or any line of the output begins with `FAIL`.</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one spelling of the drifted word occurs anywhere in the tracked bundle.
    - The chosen spelling matches the convention the measured second word establishes, and both counts are recorded in the summary.
    - The hook's framing sentence differs from its previous text in that word alone.
  </acceptance_criteria>
  <done>The corpus spells the word one way, and the summary shows which measurement chose it.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the NOTES residual with the configuration key that can stop the gate dispatching</name>
  <read_first>
    - `.gsd/capabilities/sota-numerics/NOTES.md` section 6 and its residual subsection, lines 81-148
    - `~/.claude/gsd-core/workflows/plan-phase.md` around lines 1524 and 1556-1566, where the state write and the gate dispatch sit
    - `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/REVIEW-CRITICAL-FINAL.md`, the finding about the post-planning-gaps configuration key
  </read_first>
  <files>.gsd/capabilities/sota-numerics/NOTES.md, .planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-06-SUMMARY.md</files>
  <action>
    Re-derive the two step positions the residual cites against the installed gsd-core rather than trusting the numbers already written there, and record the version the derivation was made against beside them, so the next reader knows what the citation is pinned to. Measured 2026-09-07 against gsd-core 1.13.0: the state write is step 13b and the gate dispatch is step 13e, so the ordering claim holds and the numbers stand.

    Then extend the residual with what it currently omits. The gate dispatch does not merely run after the state write; it runs inside a step whose whole body is conditional on a configuration key that defaults on. A project that turns that key off never dispatches the gate at all, so a gate declared blocking stops blocking without any signal. State that plainly, name the key, say the default, and say what an operator would see, which is nothing. Keep the existing residual text about the ordering coupling and the upstream fix; this adds a second, larger case to the same residual rather than replacing it.

    Open a bd ticket for the upstream fix this new case needs, and record its identifier in the residual next to the existing upstream ticket, so the note points at tracked work rather than at an observation. Do not soften the existing text about what fail-closed does and does not cover; the review's complaint was that the residual undersold the coupling, and an addition that made the rest vaguer would repeat the defect.
  </action>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && grep -c 'post_planning_gaps' .gsd/capabilities/sota-numerics/NOTES.md</automated>
    <fails_when>the command prints `0`, which shows the residual still omits the configuration key.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/dd/projects/gsd-beads/.worktrees/sota-numerics-release-013 && grep -cE 'gsd-beads-[a-z0-9]+' .gsd/capabilities/sota-numerics/NOTES.md</automated>
    <fails_when>the printed count is lower than `2`, which shows the new residual case names no tracked ticket beside the existing one.</fails_when>
  </verify>
  <verify>
    <automated>grep -c 'gsd-core 1.13' /home/dd/projects/gsd-beads/.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-06-SUMMARY.md</automated>
    <fails_when>the command prints `0`, or exits non-zero because the summary file is absent, either of which shows the step-position derivation was not pinned to a version.</fails_when>
  </verify>
  <acceptance_criteria>
    - The residual names the configuration key, its default, and the consequence of turning it off.
    - The residual carries a bd ticket identifier for the new case alongside the existing upstream ticket.
    - The cited step positions are re-derived and the gsd-core version they were derived against is recorded in the summary.
    - The existing residual text about fail-closed coverage is unchanged.
  </acceptance_criteria>
  <done>The residual states the whole coupling, including the case where the gate does not run at all.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capability manifest to host lifecycle | Whether a gate declared blocking actually dispatches is decided by the host's configuration, not by the manifest. |
| release documentation to operator | An operator auditing this release reads the changelog and the notes to decide what it enforces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-22 | Spoofing | blocking gate under a host configuration key | high | mitigate | A project that disables the post-planning-gaps step silently disables this gate while the manifest still claims it blocks; Task 3 records the case, names the key and its default, and files a tracked ticket for the upstream fix. |
| T-24-23 | Repudiation | stale counts in release documentation | medium | mitigate | Counts nothing recomputes make an audit unrepeatable; Task 1 deletes them and requires any surviving number to name what recomputes it. |
| T-24-24 | Tampering | comment edits inside the gate script | medium | mitigate | The Python suite is run after the comment edit, so an edit that reaches executable code is caught rather than shipped. |
| T-24-25 | Information Disclosure | session banner text | low | accept | The banner sentence changes in one word and carries no environment-derived content. |
</threat_model>

## Alternatives Considered

N/A — no mechanism choice

## Artifacts phase produces

- `CHANGELOG.md` — a 0.2.0 section naming which behaviours changed and asserting no stale count.
- `.gsd/capabilities/sota-numerics/scripts/check-alternatives.py` — the plan-file comment keeping its argument without a corpus measurement.
- `hooks/session-start.sh`, `.gsd/capabilities/sota-numerics/fragments/executor-numerics.md` — one spelling across the bundle.
- `.gsd/capabilities/sota-numerics/NOTES.md` — a residual naming the configuration key that can stop the gate dispatching, with a tracked ticket.
- `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-06-SUMMARY.md` — the spelling measurement, the version pin, and both hash values.

<verification>
- The 0.2.0 changelog section and the gate script comment carry no stale count.
- Exactly one spelling of the drifted word occurs in the tracked bundle.
- The NOTES residual names the configuration key and a tracked ticket.
- The Python suite and the session-start suite both pass.
</verification>

<success_criteria>
Every number left in the bundle is one something keeps true, the corpus spells the drifted word one way, and the residual understates nothing.
</success_criteria>

<output>
Create `.planning/phases/24-remediate-sota-numerics-0-2-0-release-blockers/24-06-SUMMARY.md` when done.
</output>
