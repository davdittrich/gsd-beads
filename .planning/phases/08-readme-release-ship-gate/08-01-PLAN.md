---
phase: 08-readme-release-ship-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
  - README.md
autonomous: true
requirements: [PUB-04, PUB-07]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Pushing a tag matching v*.*.* to origin produces a GitHub Release carrying gsd-beads.zip, built by CI, not by hand (D-06)."
    - "Unzipping that asset lists exactly five top-level entries: .agents, .claude-plugin, LICENSE, README.md, hooks — and nothing else (ROADMAP SC2)."
    - "A cold stranger who knows neither gsd-core nor beads can read README.md top to bottom and learn what the plugin does, what it needs, how to install it, how to remove it, what its limits are, its license, and where gsd-core lives (D-01, D-04, ROADMAP SC1)."
    - "Every command printed in README.md was executed verbatim by the author before it was written down (D-02, ROADMAP SC1)."
    - "The rehearsal tag and its release are gone from the remote after the tracer proves the pipeline — the real v1.1.0 release in Plan 02 is the only release a user ever sees."
  artifacts:
    - ".github/workflows/release.yml — tag-triggered allowlist build + gh release create"
    - "README.md — repo root, D-04 section order"
  key_links:
    - "release.yml zip step's explicit path list IS the allowlist boundary — a broad recursive include here is the single failure that leaks dev state to installers (RESEARCH Pitfall 3)."
    - "job-level permissions: contents: write — without it gh release create 403s and no asset ever exists (RESEARCH Pitfall 1)."
    - "README.md is itself a member of the allowlist — it must exist in the tree at tag time or the archive is incomplete."
  prohibitions:
    - "The release archive is built with an exclude/denylist form rather than an explicit include list, so a future top-level directory ships silently. [flagged-unverified]"
    - "A README command is written from documentation or memory rather than transcribed from an execution, so the doc reads correct and fails for a stranger. [flagged-unverified]"
    - "The rehearsal release is left published, so the repo shows a pre-release artifact alongside the real one. [flagged-unverified]"
  flagged_assumptions:
    - "PUB-04 edge-probe row returned unclassified/unresolved — the deterministic probe engine has no category for CI packaging operations. Carried forward unresolved; the allowlist-exactness assertion in Task 1 and Plan 02 Task 2 is the substitute coverage. [flagged-unverified]"
    - "PUB-07 edge-probe row returned unclassified/unresolved — no category for documentation deliverables. Carried forward unresolved; the execute-then-transcribe discipline in Task 3 is the substitute coverage. [flagged-unverified]"
    - "RESEARCH A1: zip is preinstalled on GitHub's ubuntu-latest runner image — not verified against a live runner. Task 1's rehearsal run is exactly the probe that resolves it; if the zip step fails, prepend an apt-get install of zip. [flagged-unverified]"
---

<objective>
Prove the release pipeline end to end on a throwaway tag before the real one exists, then author the
README a stranger reads.

Purpose: a release workflow only fails visibly when a tag is pushed. Rehearsing on a disposable tag
turns a bad `release.yml` into a deleted test release instead of a botched v1.1.0 (same
rehearse-before-the-real-run discipline Phase 7 used for its history rewrite).
Output: `.github/workflows/release.yml`, `README.md`, and a verified-then-deleted rehearsal release.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-readme-release-ship-gate/08-CONTEXT.md
@.planning/phases/08-readme-release-ship-gate/08-RESEARCH.md
@.planning/phases/08-readme-release-ship-gate/08-PATTERNS.md
</context>

## Artifacts this phase produces

New files and objects that do not exist yet — do not verify these against the current tree:

| Artifact | Created by | Notes |
|----------|-----------|-------|
| `README.md` | Plan 01 Task 1 (skeleton), Task 3 (full) | repo root, first README in this repo |
| `.github/workflows/release.yml` | Plan 01 Task 1 | first GitHub Actions workflow in this repo |
| rehearsal tag/release `v0.0.0-rc1` | Plan 01 Task 1 | created and deleted within Task 1 |
| git tag `v1.1.0` | Plan 02 Task 2 | |
| GitHub Release `v1.1.0` + asset `gsd-beads.zip` | Plan 02 Task 2 (via CI) | |

## Source Coverage Audit

Complete audit for the whole Phase 8 plan set (Plans 01 + 02).

### GOAL (ROADMAP Phase 8)

| Item | Status | Where |
|------|--------|-------|
| SC1 README states purpose, requirements, install, uninstall, caveats, license, gsd-core link; commands transcribed from execution | COVERED | 01-T1 (skeleton), 01-T3 (full) |
| SC2 Release carries an allowlist-built archive; unzip lists only the 5 entries | COVERED | 01-T1 (rehearsal proof), 02-T2 (real release proof) |
| SC3 Installing from that release leaves no planning/beads dev-state file behind | COVERED | 01-T1, 02-T2 (archive-content assertion; the archive is the install source) |
| SC4 README's own commands complete a marketplace add → install → uninstall round trip | COVERED | 02-T3 |
| SC5 `claude plugin validate . --strict` clean on the pushed tree at the released tag | COVERED | 02-T3 |

### REQ (REQUIREMENTS.md)

| Item | Status | Where |
|------|--------|-------|
| PUB-04 allowlist archive attached to a GitHub Release | COVERED | 01-T1, 02-T2 |
| PUB-07 README | COVERED | 01-T1, 01-T3 |
| PUB-09 validate --strict + real round trip | COVERED | 02-T3 |

### RESEARCH (08-RESEARCH.md)

| Item | Status | Where |
|------|--------|-------|
| `gh release create` over `softprops/action-gh-release` | COVERED | 01-T1 |
| `actions/checkout@v7` pinned | COVERED | 01-T1 |
| Explicit-include zip, never exclude-list (Pitfall 3) | COVERED | 01-T1, must_haves.prohibitions |
| Job-level `permissions: contents: write` (Pitfall 1) | COVERED | 01-T1 |
| Non-interactive `-y` on install/uninstall; CLI form not slash-command form | COVERED | 02-T3 |
| D-11 resolution: `marketplace.json` `source: "./"` needs no change | COVERED | 02-T3 (empirically re-confirmed by the round trip; no edit planned) |
| Plugin cache copies the whole cloned repo, dev directories included | COVERED | 01-T3 (README caveat), 02-T3 (round trip asserts completion only, not cache contents — Pitfall 2) |
| Fresh-clone-at-tag validate, never the working tree (Pitfall 4) | COVERED | 02-T3 |
| A1 runner `zip` availability | COVERED as flagged assumption | 01 frontmatter |

### CONTEXT (08-CONTEXT.md decisions)

| Item | Status | Where |
|------|--------|-------|
| D-01 cold-stranger audience | COVERED | 01-T3 |
| D-02 verbatim commands, no expected-output blocks | COVERED | 01-T3 |
| D-03 three-part caveats | COVERED | 01-T3 |
| D-04 root path + locked section order | COVERED | 01-T3 |
| D-05 worked `bd` example | COVERED | 01-T3 |
| D-06 GH Actions workflow, tag-triggered, exact allowlist | COVERED | 01-T1 |
| D-07 tag `v1.1.0` | COVERED | 02-T2 |
| D-08 scripted round trip, interactive steps called out | COVERED | 02-T3 |
| D-09 real output embedded in SUMMARY.md | COVERED | 01-T1, 02-T2, 02-T3 |
| D-10 ship gate runs against a fresh clone at the tag | COVERED | 02-T3 |
| D-11 `source: "./"` question | COVERED | 02-T3 |

No MISSING items. No deferred ideas exist for this phase.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end release rehearsal on a throwaway tag</name>
  <files>.github/workflows/release.yml, README.md</files>
  <read_first>
    - `.planning/phases/08-readme-release-ship-gate/08-PATTERNS.md` — the `release.yml` block under "`.github/workflows/release.yml`" is the vetted base to use verbatim; also its "Critical constraints already locked" list
    - `.planning/phases/08-readme-release-ship-gate/08-RESEARCH.md` — Pattern 1, Pitfall 1, Pitfall 3
    - `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` — literal names `beads` and `gsd-beads` used by the install commands
    - `.gitignore` — confirm nothing in it would surprise the CI checkout
  </read_first>
  <precondition>`gh auth status` reports an authenticated account with repo scope, and `git remote -v` shows origin at https://github.com/davdittrich/gsd-beads.git. Halt if either is false — this task pushes a tag to that remote.</precondition>
  <reversibility rating="reversible">A workflow file is editable and a rehearsal release/tag is deleted by this same task; nothing here survives if it is wrong.</reversibility>
  <action>
Wire the one path tag-push → CI → zip → GitHub Release → downloaded asset, end to end, once.

1. Create `.github/workflows/release.yml` from the PATTERNS.md block verbatim: `name: Release`;
   trigger `on: push: tags: ['v*.*.*']`; one job `release` on `runs-on: ubuntu-latest` with job-level
   `permissions: contents: write`; step 1 `uses: actions/checkout@v7`; step 2 named
   `Build allowlisted archive` running `zip -r gsd-beads.zip` over exactly the five allowlist paths
   `.claude-plugin`, `hooks`, `.agents/skills`, `README.md`, `LICENSE`; step 3 named
   `Publish release` running `gh release create "${{ github.ref_name }}" gsd-beads.zip
   --generate-notes` with `env: GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}`. Include-list only — do not
   add any `-x` exclude flag, do not zip `.` recursively, do not substitute a third-party release
   action. Per D-06.
2. Create `README.md` at repo root as a real but thin slice: `# gsd-beads` title, the one-line
   description copied from `plugin.json`'s `description` field, a `## Requirements` section naming
   `bd` on PATH / Python 3 stdlib / gsd-core >= 1.6.0, and an `## Install` section carrying the two
   literal commands `claude plugin marketplace add davdittrich/gsd-beads` and
   `claude plugin install beads@gsd-beads -y`. Task 3 expands this file to the full D-04 section set;
   the thin version exists now because README.md is a member of the allowlist and the archive is
   incomplete without it. No prose about sections that do not exist yet.
3. Commit both files and push to origin/main.
4. Push the rehearsal tag: `git tag v0.0.0-rc1 && git push origin v0.0.0-rc1`.
5. Watch the run to completion (`gh run watch` on the run for that tag, or `gh run list --workflow
   Release`). If the zip step fails on a missing `zip` binary (RESEARCH assumption A1), prepend an
   `apt-get update && apt-get install -y zip` step and retag with `v0.0.0-rc2`.
6. Download and inspect the published asset into a scratch directory:
   `gh release download v0.0.0-rc1 --pattern '*.zip' -O /tmp/gsd-beads-rc.zip`, then
   `unzip -Z1 /tmp/gsd-beads-rc.zip > /tmp/rc-listing.txt`.
7. Assert exactness against the listing, not against the workflow source.
8. Tear down: `gh release delete v0.0.0-rc1 --yes --cleanup-tag`, then `git tag -d v0.0.0-rc1` and
   confirm `git ls-remote --tags origin` no longer lists it.

Capture the real terminal output of steps 4-8 for the phase SUMMARY.md per D-09.
  </action>
  <verify>
    <automated>LC_ALL=C sort -u &lt; &lt;(cut -d/ -f1 /tmp/rc-listing.txt) | tr '\n' ' ' | grep -qx '.agents .claude-plugin LICENSE README.md hooks ' &amp;&amp; grep -cE '^\.(planning|beads)/' /tmp/rc-listing.txt | grep -qx 0 &amp;&amp; ! git ls-remote --tags origin | grep -q 'refs/tags/v0.0.0-rc1'</automated>
  </verify>
  <acceptance_criteria>
    - CLI: the workflow run triggered by `v0.0.0-rc1` reports `completed success` in `gh run list --workflow Release --json conclusion`.
    - CLI: `cut -d/ -f1 /tmp/rc-listing.txt | LC_ALL=C sort -u` emits exactly these five lines in this order: `.agents`, `.claude-plugin`, `LICENSE`, `README.md`, `hooks`.
    - CLI: `grep -cE '^\.(planning|beads)/' /tmp/rc-listing.txt` outputs `0`. <!-- planner-discipline-allow: .planning -->
    - Source: `.github/workflows/release.yml` contains the string `contents: write` and contains no `-x ` exclude flag on the zip invocation.
    - Source: `.github/workflows/release.yml` names all five allowlist paths on the zip command line.
    - CLI: `gh release list` shows no `v0.0.0-rc1` entry and `git ls-remote --tags origin` does not list `refs/tags/v0.0.0-rc1`.
  </acceptance_criteria>
  <done>A tag push builds and publishes an allowlist-exact archive; the rehearsal artifacts are deleted; the workflow file is committed on main.</done>
</task>

<task type="auto">
  <name>Task 2: Execute every command the README will claim</name>
  <files>README.md</files>
  <read_first>
    - `README.md` — the thin version from Task 1
    - `.agents/skills/beads/SKILL.md` — Core CLI Workflow block, source for the D-05 worked example
    - `AGENTS.md` — Quick Reference and the architecture one-liner
    - `hooks/hooks.json` — the exact SessionStart command, for the D-03(3) caveat
  </read_first>
  <action>
Run, verbatim and in a scratch directory outside this repo, every command that Task 3 will print in
README.md, and record the exact invocation and its real output. This is the evidence D-02 and
ROADMAP SC1 require — a command not executed here may not appear in the README.

Cover: the marketplace add / install / uninstall trio using the literal names `davdittrich/gsd-beads`,
`beads@gsd-beads`, `beads`; and the D-05 worked `bd` sequence chosen from SKILL.md's Core CLI
Workflow (a short end-to-end slice — find ready work, claim, close — not the whole block).

Before touching the marketplace, record the current local plugin state with
`claude plugin marketplace list` and `claude plugin list`, because this repo already carries the
`gsd-beads` marketplace installed at local scope as its own dogfooding dependency (see STATE.md,
Phase 6). If the trio would collide with that entry, note it and defer the actual round-trip
execution to Plan 02 Task 3, which owns save-and-restore of that state — in that case transcribe the
README's install commands from Plan 02's transcript rather than re-running them here.

For the `bd` example, run it against a throwaway beads workspace, never against this repo's issues.

Write the transcripts to the phase SUMMARY.md draft per D-09. Do not put output blocks in README.md
itself (D-02).
  </action>
  <verify>
    <automated>test -s /tmp/gsd-beads-cmd-transcript.txt &amp;&amp; grep -q 'claude plugin marketplace add davdittrich/gsd-beads' /tmp/gsd-beads-cmd-transcript.txt &amp;&amp; grep -q 'bd ready' /tmp/gsd-beads-cmd-transcript.txt</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: a transcript file exists containing, for every command Task 3 prints, the literal invocation followed by its captured stdout/stderr.
    - CLI: each `bd` command in the transcript exited 0, or its non-zero exit and reason is recorded and the command is dropped from the README rather than shown.
    - Source: the transcript records the pre-existing local marketplace/plugin state captured before any add/install ran.
  </acceptance_criteria>
  <done>Every command destined for the README has a real execution record; none is aspirational.</done>
</task>

<task type="auto">
  <name>Task 3: Expand README.md to the full locked section set</name>
  <files>README.md</files>
  <read_first>
    - `README.md` — thin version from Task 1
    - the Task 2 transcript — the only permitted source for command text
    - `.planning/phases/08-readme-release-ship-gate/08-PATTERNS.md` — "Pattern Assignments → README.md", Sources 1-5, which name the exact lines to summarize from SKILL.md, AGENTS.md, plugin.json, marketplace.json, hooks.json
    - `.planning/phases/08-readme-release-ship-gate/08-RESEARCH.md` — "D-11 Resolution", second paragraph, for the plugin-cache disclosure
    - `LICENSE` — confirm MIT and the copyright line
  </read_first>
  <action>
Rewrite README.md to carry, in exactly this order and no other (D-04): title plus one-liner; What it
does; Requirements; Install; Uninstall; Caveats; License; Link to gsd-core.

Constraints:
- Audience is a cold stranger who knows neither gsd-core nor beads (D-01) — define both in one
  sentence each in "What it does", sourced from `SKILL.md`'s frontmatter description and `AGENTS.md`'s
  architecture one-liner. Summarize, do not paste those files wholesale.
- Every command block is copy-pasteable and verbatim from the Task 2 transcript (D-02). Show no
  expected-output blocks anywhere in this file (D-02).
- Requirements section names `bd` on PATH, Python 3 stdlib, gsd-core >= 1.6.0.
- Install section: `claude plugin marketplace add davdittrich/gsd-beads` then
  `claude plugin install beads@gsd-beads -y`. Uninstall section: `claude plugin uninstall beads -y`.
- Include the D-05 worked `bd` slice (find ready work → claim → close) under "What it does" or
  immediately after Install, and point to `AGENTS.md` for the full command set rather than
  duplicating it.
- Caveats must carry all three D-03 items plus one more:
  (1) the requirements restated as failure modes — what breaks when `bd` is absent from PATH;
  (2) this repo's beads backend is Dolt-only and carries no `.beads/issues.jsonl` passive export at
      all — say it does not exist here, not merely that it is passive;
  (3) the SessionStart hook runs `bd prime --hook-json` on every session start, per `hooks/hooks.json`,
      and what it needs on first run in a project with no beads workspace yet;
  (4) installing via the marketplace flow copies the entire cloned repository into the installer's
      local plugin cache under `~/.claude/plugins/cache/`, including this project's own planning and
      issue-tracker directories — a documented Claude Code cache behavior, distinct from the release
      archive, which ships only the five allowlisted paths. Disclose it; do not attempt a fix.
- License section: MIT, pointing at `LICENSE`.
- Final section links gsd-core.

The gsd-core link target must be resolved from `PROJECT.md` or the gsd-core install source, not
invented; if no canonical URL is recoverable, halt and ask rather than guessing a URL.
  </action>
  <verify>
    <automated>grep -n '^## ' README.md | cut -d: -f2- | tr '\n' '|' | grep -qiE 'What it does.*Requirements.*Install.*Uninstall.*Caveats.*License' &amp;&amp; grep -q 'claude plugin uninstall beads -y' README.md &amp;&amp; grep -q 'plugins/cache' README.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `README.md` heading order matches D-04 exactly — What it does, Requirements, Install, Uninstall, Caveats, License, gsd-core link — with no additional top-level section interleaved.
    - Source: `README.md` contains the three literal commands `claude plugin marketplace add davdittrich/gsd-beads`, `claude plugin install beads@gsd-beads -y`, `claude plugin uninstall beads -y`.
    - Source: every fenced command in `README.md` appears verbatim in the Task 2 transcript.
    - Source: `README.md` contains no fenced block labelled or presented as expected output.
    - Source: the Caveats section mentions the local plugin cache path and states the release archive ships only the allowlisted paths.
    - Source: the Caveats section states that no `issues.jsonl` export exists in this project.
    - Behavior: the gsd-core link resolves (HTTP 200 or a documented local path), verified once.
  </acceptance_criteria>
  <done>A stranger reading only README.md can evaluate, install, and remove the plugin, and every command shown has been run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo tree → release archive | everything crossing this boundary reaches an installer's machine |
| GitHub Actions job → repo write | the workflow's `GITHUB_TOKEN` can create releases |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `.github/workflows/release.yml` zip step | high | mitigate | explicit five-path include list; asserted against the published asset's own listing in Task 1, not against the workflow source |
| T-08-02 | Elevation of Privilege | `release.yml` job token scope | medium | mitigate | job-level `permissions: contents: write` only; never workflow-wide `write-all`; trigger is tag push, which already requires push access |
| T-08-03 | Information Disclosure | installer's local plugin cache | low | accept | Claude Code's own documented cache-copy behavior, not controllable from this repo; disposed by disclosure in README Caveats (Task 3) |
| T-08-04 | Tampering | third-party GitHub Action supply chain | medium | mitigate | only `actions/checkout@v7` (GitHub first-party, audited in RESEARCH Package Legitimacy Audit); `gh` CLI is runner-preinstalled; no third-party release action |
</threat_model>

<verification>
1. `gh run list --workflow Release` shows the rehearsal run concluded `success`.
2. The rehearsal asset's `unzip -Z1` listing has exactly the five allowlisted top-level entries and zero dev-state paths.
3. The rehearsal release and tag are absent from `gh release list` and `git ls-remote --tags origin`.
4. `README.md` heading order matches D-04 and every fenced command traces to the Task 2 transcript.
</verification>

<success_criteria>
- ROADMAP SC1 satisfied by README.md's content and the execute-then-transcribe evidence.
- ROADMAP SC2/SC3 mechanism proven on a disposable tag, ready for the real one in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/08-readme-release-ship-gate/08-01-SUMMARY.md` when done, embedding the real
command transcripts from Tasks 1 and 2 per D-09.
</output>
