---
phase: 08-readme-release-ship-gate
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .claude-plugin/plugin.json
autonomous: true
requirements: [PUB-04, PUB-09]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A GitHub Release named v1.1.0 exists on davdittrich/gsd-beads carrying gsd-beads.zip built by CI from the allowlist (D-07, ROADMAP SC2)."
    - "Unzipping the published v1.1.0 asset lists exactly .agents, .claude-plugin, LICENSE, README.md, hooks — so nothing an installer unpacks carries this project's dev state (ROADMAP SC2/SC3)."
    - "`claude plugin validate . --strict` runs clean from inside a fresh clone checked out at v1.1.0 — not from this working tree (D-10, ROADMAP SC5)."
    - "The README's own three commands, run verbatim, complete a marketplace add → install → uninstall round trip (D-08, ROADMAP SC4)."
    - "The local dogfooding gsd-beads marketplace/plugin state this repo depends on is restored to exactly what it was before the round trip."
  artifacts:
    - "git tag v1.1.0 on origin"
    - "GitHub Release v1.1.0 with asset gsd-beads.zip"
    - ".planning/phases/08-readme-release-ship-gate/08-02-SUMMARY.md carrying the real transcripts (D-09)"
  key_links:
    - "The fresh clone at the tag is the only evidence of what the tag contains — local tree state is not evidence (Phase 7 precedent, RESEARCH Pitfall 4)."
    - "`marketplace.json`'s `source: \"./\"` resolving under a git-source marketplace add is what makes SC4 possible; the round trip is its empirical confirmation (D-11)."
  prohibitions:
    - "The ship gate re-runs validate against the local working tree instead of a fresh clone at the tag, passing on stale local state. [flagged-unverified]"
    - "The round trip is declared successful from documentation or a prior session's result rather than an execution in this phase. [flagged-unverified]"
    - "The round trip asserts the absence of dev-state files in the plugin cache and fails the phase on a match that is expected Claude Code behavior, not a defect (RESEARCH Pitfall 2). [flagged-unverified]"
    - "The pre-existing local-scope gsd-beads install is torn down for the round trip and never restored, silently breaking this repo's own tooling. [flagged-unverified]"
  flagged_assumptions:
    - "PUB-09 edge-probe rows classified `boundary` and `precision` are engine misfires — this requirement has no numeric threshold, no rounding contract, and no min/max. Both rows carried forward unresolved; no numeric edge coverage is claimed for them. [flagged-unverified]"
    - "PUB-04 edge-probe row unclassified/unresolved; substitute coverage is the archive-exactness assertion in Task 2. [flagged-unverified]"
---

<objective>
Cut the real v1.1.0 release through the pipeline Plan 01 proved, then run the ship gate against the
tag rather than against this machine.

Purpose: PUB-04 and PUB-09 are only satisfied by artifacts that exist on the remote and by checks run
against them. Output: tag v1.1.0, its release asset, and the SC4/SC5 transcripts.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-readme-release-ship-gate/08-CONTEXT.md
@.planning/phases/08-readme-release-ship-gate/08-RESEARCH.md
@.planning/phases/08-readme-release-ship-gate/08-01-SUMMARY.md
</context>

## Artifacts this phase produces

`README.md` and `.github/workflows/release.yml` (Plan 01), the git tag `v1.1.0`, and the GitHub
Release `v1.1.0` with its `gsd-beads.zip` asset (this plan). None of these exist in the tree being
verified against.

<tasks>

<task type="auto">
  <name>Task 1: Align plugin version with the release tag and gate on a clean local validate</name>
  <files>.claude-plugin/plugin.json</files>
  <read_first>
    - `.claude-plugin/plugin.json` — current `version` field is `0.1.0`
    - `.claude-plugin/marketplace.json` — confirm it declares no version of its own that would drift
    - `.planning/ROADMAP.md` §Phase 8 — SC5's wording, which scopes the validate run to the released tag
  </read_first>
  <reversibility rating="reversible">A version string edit before any tag exists; revert is a one-line change.</reversibility>
  <action>
Set `.claude-plugin/plugin.json`'s `version` to `1.1.0` so the version an installer sees matches the
tag this phase publishes (D-07 fixes the tag at v1.1.0; the manifest currently declares 0.1.0, which
would ship as a visible mismatch). Change only that field; leave `name`, `description`, `author`,
`license`, and `skills` untouched. Preserve the file's 2-space indent and key order.

Then confirm `claude plugin validate . --strict` is clean on the local tree — this is a pre-tag
smoke check, not the SC5 gate, which Task 3 owns. The root `CLAUDE.md` warning documented in Phase 5
(ROADMAP Phase 5 SC1) is the one permitted, permanent exception; any other finding halts.

Commit the manifest change together with Plan 01's README and workflow if they are not yet on main,
and push to origin/main. The tag in Task 2 must point at a commit that already contains all of them.
  </action>
  <verify>
    <automated>test "$(node -e 'process.stdout.write(require("./.claude-plugin/plugin.json").version)')" = "1.1.0" &amp;&amp; claude plugin validate . --strict &amp;&amp; git status --porcelain | grep -qv . &amp;&amp; git rev-parse HEAD = "$(git rev-parse origin/main)"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.claude-plugin/plugin.json` has `"version": "1.1.0"` and its other five top-level keys are byte-identical to their pre-edit values.
    - CLI: `claude plugin validate . --strict` exits 0, or emits only the documented root `CLAUDE.md` warning.
    - CLI: `git status --porcelain` is empty and `git rev-parse HEAD` equals `git rev-parse origin/main`.
    - CLI: `git show --name-only HEAD~..HEAD` (or the relevant range) shows `README.md` and `.github/workflows/release.yml` present in the pushed history.
  </acceptance_criteria>
  <done>main carries the README, the workflow, and a 1.1.0 manifest, and the tree validates clean.</done>
</task>

<task type="auto">
  <name>Task 2: Tag v1.1.0 and prove the published archive is allowlist-exact</name>
  <files>(no repo files — creates the tag and release)</files>
  <read_first>
    - `.planning/phases/08-readme-release-ship-gate/08-01-SUMMARY.md` — the rehearsal transcript, including any workflow fix applied during the rehearsal
    - `.github/workflows/release.yml` — the trigger glob, to confirm `v1.1.0` matches it
    - `.planning/phases/08-readme-release-ship-gate/08-RESEARCH.md` — Pitfall 1 (403 signature) and Pitfall 3
  </read_first>
  <precondition>Task 1's commit is on origin/main and `gh release list` shows no existing `v1.1.0` entry. Halt if a v1.1.0 tag or release already exists rather than overwriting it.</precondition>
  <reversibility rating="costly">A published release and tag are deletable (`gh release delete --cleanup-tag`), but a tag that anyone has already fetched is awkward to reuse — prefer bumping to v1.1.1 over retagging if the archive is wrong.</reversibility>
  <action>
Create and push the real tag per D-07: `git tag v1.1.0` on the pushed HEAD, then
`git push origin v1.1.0`. Watch the Release workflow run to completion.

Then verify against the published asset, not against the workflow source or the local tree:
`gh release download v1.1.0 --pattern '*.zip' -O /tmp/gsd-beads-1.1.0.zip`, then
`unzip -Z1 /tmp/gsd-beads-1.1.0.zip > /tmp/release-listing.txt`. Assert the top-level entry set is
exactly the five allowlisted names and that no dev-state path appears anywhere in the listing.

If the workflow fails at the publish step with an HTTP 403, the job is missing
`permissions: contents: write` — fix `release.yml`, push, delete the failed tag/release, and retag.
If the archive is wrong in any way, delete the release with `--cleanup-tag` and re-cut rather than
patching the asset by hand — the archive must be a CI product (D-06).

Capture the tag push, the run conclusion, and the full `unzip -Z1` listing for SUMMARY.md (D-09).
  </action>
  <verify>
    <automated>LC_ALL=C sort -u &lt; &lt;(cut -d/ -f1 /tmp/release-listing.txt) | tr '\n' ' ' | grep -qx '.agents .claude-plugin LICENSE README.md hooks ' &amp;&amp; grep -cE '^\.(planning|beads)/' /tmp/release-listing.txt | grep -qx 0 &amp;&amp; gh release view v1.1.0 --json assets --jq '.assets[].name' | grep -qx 'gsd-beads.zip'</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `gh release view v1.1.0 --json tagName,assets` shows tag `v1.1.0` and exactly one asset named `gsd-beads.zip`.
    - CLI: `cut -d/ -f1 /tmp/release-listing.txt | LC_ALL=C sort -u` emits exactly `.agents`, `.claude-plugin`, `LICENSE`, `README.md`, `hooks`.
    - CLI: `grep -cE '^\.(planning|beads)/' /tmp/release-listing.txt` outputs `0`. <!-- planner-discipline-allow: .planning -->
    - CLI: the workflow run for tag `v1.1.0` reports conclusion `success`.
    - Behavior: the asset was produced by the CI run, not uploaded from a local machine — the release's asset upload timestamp falls inside the workflow run window.
  </acceptance_criteria>
  <done>ROADMAP SC2 and SC3 are proven against the real published artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Ship gate — validate at the tag from a fresh clone, and run the README round trip</name>
  <files>(no repo files — verification only)</files>
  <read_first>
    - `README.md` — the round trip must use the commands as printed there, not as remembered
    - `.planning/phases/08-readme-release-ship-gate/08-RESEARCH.md` — "Fresh-clone ship-gate verification (D-10)" code block, Pattern 2's verified `-y` flag semantics, Pitfall 2 and Pitfall 4
    - `.planning/phases/07-hygiene-publication/07-02-SUMMARY.md` — the fresh-clone transcript pattern D-09 reuses
    - `.claude-plugin/marketplace.json` — `source: "./"`, the subject of D-11
  </read_first>
  <precondition>This repo already has the `gsd-beads` marketplace and `beads` plugin installed at local scope as its own dogfooding dependency (STATE.md, Phase 6). Capture that state before the round trip and restore it afterwards; halt before any removal if the current state cannot be captured.</precondition>
  <action>
Two independent gates. Do not let either one's result stand in for the other (RESEARCH Pitfall 2:
these are different install paths with different contents on disk).

**Gate A — SC5, validate at the tag (D-10).** In a scratch directory, `git clone
https://github.com/davdittrich/gsd-beads.git`, `git checkout v1.1.0` inside it, then run
`claude plugin validate . --strict` with the clone as the working directory. Not `git -C` against
this repo, not a reuse of Task 1's local pass. The documented root `CLAUDE.md` warning is the one
permitted exception; anything else fails the gate.

**Gate B — SC4, the round trip (D-08).** Record the pre-state first:
`claude plugin marketplace list` and `claude plugin list`, saved to the transcript. If a `gsd-beads`
marketplace is already registered from a local path, remove it so the round trip exercises the public
GitHub source rather than the local one, and note the exact command used so it can be reversed.

Then run the three README commands verbatim, in order:
`claude plugin marketplace add davdittrich/gsd-beads`, `claude plugin install beads@gsd-beads -y`,
`claude plugin uninstall beads -y`. All three are non-interactive per RESEARCH Pattern 2; if any of
them nonetheless blocks on a prompt, stop and call that step out explicitly for the user to perform
by hand rather than faking or skipping it (D-08).

Assert only that each step completes and that `claude plugin list` shows `beads` present after
install and absent after uninstall. Do not assert anything about the contents of
`~/.claude/plugins/cache/` — that path legitimately holds a full repo copy and a match there is not
a defect of this phase.

Finally, restore the local dogfooding state captured at the start and confirm with a second
`claude plugin marketplace list` / `claude plugin list` that it matches the pre-state.

Record that `marketplace.json` needed no edit for the round trip to work — this is D-11's empirical
confirmation of the research answer.

Embed both gates' real command output in the SUMMARY.md (D-09); no separate transcript file.
  </action>
  <verify>
    <automated>tmp=$(mktemp -d) &amp;&amp; git clone --quiet https://github.com/davdittrich/gsd-beads.git "$tmp/c" &amp;&amp; git -C "$tmp/c" checkout --quiet v1.1.0 &amp;&amp; (cd "$tmp/c" &amp;&amp; claude plugin validate . --strict)</automated>
    <human-check>Confirm from the SUMMARY.md transcript that the marketplace add → install → uninstall round trip really ran against `davdittrich/gsd-beads` (not a local path), that `beads` appeared then disappeared from `claude plugin list`, and that the pre-existing local dogfooding marketplace/plugin state is back exactly as it was.</human-check>
  </verify>
  <acceptance_criteria>
    - CLI: `claude plugin validate . --strict` executed with the fresh clone at `v1.1.0` as the working directory exits 0, or emits only the documented root `CLAUDE.md` warning.
    - Behavior: the transcript shows the validate command's working directory is a scratch clone path, not `/home/dd/Gemini/gsd-beads`.
    - CLI: `claude plugin marketplace add davdittrich/gsd-beads` exits 0.
    - CLI: after `claude plugin install beads@gsd-beads -y`, `claude plugin list` contains `beads`; after `claude plugin uninstall beads -y`, it does not.
    - Source: no change was made to `.claude-plugin/marketplace.json` during this task — `git status --porcelain .claude-plugin/marketplace.json` is empty.
    - Behavior: post-restore `claude plugin marketplace list` and `claude plugin list` output matches the captured pre-state.
  </acceptance_criteria>
  <done>SC4 and SC5 are proven by execution against the public repo at the released tag, and this machine's own tooling state is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published release asset → installer's machine | the archive contents are what a stranger unpacks |
| public git source → local plugin cache | the marketplace flow copies remote content onto this machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Information Disclosure | published `gsd-beads.zip` | high | mitigate | Task 2 asserts the top-level entry set against the downloaded asset's own listing before the release is accepted |
| T-08-06 | Spoofing | ship gate evidence | medium | mitigate | Gate A runs from a fresh clone at the tag, so a passing result cannot come from uncommitted local state (D-10, Pitfall 4) |
| T-08-07 | Tampering | this machine's local plugin state | low | mitigate | pre-state captured before the round trip and restored after, with a post-restore comparison |
| T-08-08 | Information Disclosure | installer's plugin cache holding a full repo copy | low | accept | documented Claude Code behavior outside this repo's control; disposed by the README caveat authored in Plan 01 Task 3 |
</threat_model>

<verification>
1. `gh release view v1.1.0` shows one asset, `gsd-beads.zip`, from a successful CI run.
2. The asset's listing has exactly the five allowlisted top-level entries and zero dev-state paths.
3. `claude plugin validate . --strict` is clean inside a fresh clone at `v1.1.0`.
4. The three README commands complete in order, with `beads` present then absent from `claude plugin list`.
5. Local marketplace/plugin state matches its pre-round-trip capture.
</verification>

<success_criteria>
- PUB-04 complete: an allowlist-built archive is attached to the v1.1.0 GitHub Release.
- PUB-09 complete: strict validation clean at the tag and a real round trip executed.
- All five ROADMAP Phase 8 success criteria have an execution transcript backing them.
</success_criteria>

<output>
Create `.planning/phases/08-readme-release-ship-gate/08-02-SUMMARY.md` when done, embedding the real
transcripts for the tag push, the archive listing, Gate A, and Gate B per D-09.
</output>
