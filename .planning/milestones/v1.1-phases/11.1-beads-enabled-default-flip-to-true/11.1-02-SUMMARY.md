---
phase: 11.1-beads-enabled-default-flip-to-true
plan: 02
subsystem: docs
tags: [beads, capability, docs, changelog]

requires: ["11.1-01"]
provides:
  - "operator-facing docs (README, both PRIME.md twins, PRD) describe beads.enabled as on-by-default with an opt-out"
  - "CHANGELOG.md at repo root, keyed to capability.json version 0.2.0"
affects: []

actuals:
  tokens: 850
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Keep-a-Changelog-style CHANGELOG.md, versioned against a capability.json rather than a repo-level version"

key-files:
  created:
    - CHANGELOG.md
  modified:
    - README.md
    - .agents/skills/beads/PRIME.md
    - .beads/PRIME.md
    - docs/prd-beads-capability.md

key-decisions:
  - "No column-padding adjustment needed on the PRD's beads.enabled schema row: the plan anticipated the shorter `true` value would need one extra space to preserve brace alignment, but the row's format is identical in shape to the sibling `beads.ship_gate` boolean row (`{ \"type\": \"boolean\", \"default\": true },`), which already matches without adjustment."
  - ".beads/PRIME.md is gitignored by this repo's own .gitignore (line 22) and was never tracked — its edit lives on disk (proven identical to .agents/skills/beads/PRIME.md via the plan's diff check) but is not part of any git commit, consistent with the project's own gitignore intent for this unmanaged twin."

requirements-completed: [D-01, D-03, D-04]

coverage:
  - id: D1
    description: "README states beads is on by default and names the beads.enabled: false opt-out in the same paragraph"
    requirement: D-04
    verification:
      - kind: other
        ref: "grep -cF 'on by default' README.md >=1; grep -cF 'beads.enabled: false' README.md >=1; grep -cF 'off until' README.md ==0"
        status: pass
    human_judgment: false
  - id: D2
    description: "Both PRIME.md twins and the PRD config-schema block report beads.enabled default true; siblings and unrelated rows untouched"
    requirement: D-01
    verification:
      - kind: other
        ref: "Task 2 <verify> block (grep + diff across both twins and the PRD schema block)"
        status: pass
    human_judgment: false
  - id: D3
    description: "CHANGELOG.md created with a 0.2.0 / Changed entry naming the key, opt-out, and no-regression guarantee for explicit values"
    requirement: D-03
    verification:
      - kind: other
        ref: "Task 3 <verify> block (grep for headings, beads.enabled: false, 'explicit', '.planning/config.json', capability.json, absence of 1.2.0 and Unreleased)"
        status: pass
    human_judgment: false

duration: 8min
completed: 2026-08-17
status: complete
---

# Phase 11.1 Plan 02: Beads Default-Flip Doc Sweep Summary

**Reframed README's opt-in paragraph to opt-out, corrected `beads.enabled`'s stated default to `true` in both PRIME.md twins and the PRD config schema, and created a repo-root CHANGELOG.md recording the flip against capability version 0.2.0.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-08-17T12:59:30Z
- **Completed:** 2026-08-17T13:07:00Z
- **Tasks:** 3
- **Files modified:** 5 (1 new: CHANGELOG.md)

## Accomplishments
- `README.md`'s `### Example workflow` intro now leads with "on by default," states `beads.enabled` defaults to `true`, and gives `beads.enabled: false` in `.planning/config.json` as the opt-out — in the same paragraph, per D-04. The numbered 1-4 lifecycle walkthrough below it was untouched.
- Both `.agents/skills/beads/PRIME.md` and `.beads/PRIME.md`'s config-reference lists now read `` `beads.enabled` (default `true`) ``, proven byte-identical to each other on that line; their `sync_mode`, `ship_gate`, `epic_per` neighbor entries were not touched.
- `docs/prd-beads-capability.md`'s embedded config-schema block now reports `"beads.enabled": { "type": "boolean", "default": true }`; the four sibling rows and all nine `"when": "beads.enabled"` step/contribution predicates are unchanged, and the unrelated line-248 remark about the optional milestone epic layer was left alone.
- `CHANGELOG.md` created at the repo root: a `# Changelog` title, an orienting line tying versions to `.gsd/capabilities/beads/capability.json`, and a single `## 0.2.0` / `### Changed` entry stating the new default, the opt-out mechanism, that the four beads skills' gates were inverted to match, and — in its own labelled subsection — that a project with an explicit `beads.enabled` value keeps its current behavior unchanged.

## Task Commits

Each task was committed atomically:

1. **Task 1: Reframe the README example-workflow intro from opt-in to opt-out** - `3ac7a94` (docs)
2. **Task 2: Correct the stated default in both PRIME.md twins and the PRD config schema** - `7e9053b` (docs)
3. **Task 3: Create CHANGELOG.md with the default-flip entry** - `b69b335` (docs)

_No TDD tasks in this plan._

## Files Created/Modified
- `README.md` - Example-workflow intro paragraph reframed opt-in -> opt-out (D-04)
- `.agents/skills/beads/PRIME.md` - `beads.enabled` config-reference default corrected to `true`
- `.beads/PRIME.md` - same correction, kept identical to the tracked twin; file itself is gitignored (see Decisions Made)
- `docs/prd-beads-capability.md` - `beads.enabled` row of the embedded config-schema block corrected to `"default": true`
- `CHANGELOG.md` - **NEW**, repo root: `0.2.0` default-flip entry with opt-out and no-regression guarantee

## Decisions Made
- No column-padding adjustment was applied to the PRD schema row despite the plan's read_first note anticipating one: the row's brace layout for a boolean-typed key is fixed regardless of the default value's text length (confirmed identical to the sibling `beads.ship_gate` row, which already reads `"default": true`), so the plan's anticipated one-space padding fix did not apply once the actual current text was compared.
- `.beads/PRIME.md`'s edit was applied to disk and verified identical to its tracked twin per the plan's `diff` check, but the file itself is excluded from git by this repo's own `.gitignore` (line 22, pre-existing, not modified by this plan) and was never previously tracked — so it is correctly absent from the Task 2 commit. This is expected behavior for an "unmanaged twin," not a deviation.

## Deviations from Plan

None - plan executed exactly as written. All three tasks' `<verify>` blocks and the plan-level `<verification>` block passed without modification.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Every operator-facing document (README, both PRIME.md twins, PRD) now agrees with the shipped `capability.json` default and with each other.
- `CHANGELOG.md` exists and is discoverable by an operator upgrading an existing install, satisfying D-03's no-regression disclosure.
- Phase 11.1's two-plan scope (behavior flip in 11.1-01, doc sweep in 11.1-02) is complete; no blockers carried forward.

---
*Phase: 11.1-beads-enabled-default-flip-to-true*
*Completed: 2026-08-17*

## Self-Check: PASSED

- FOUND: `README.md`, `.agents/skills/beads/PRIME.md`, `.beads/PRIME.md`, `docs/prd-beads-capability.md`, `CHANGELOG.md`
- FOUND: `.planning/phases/11.1-beads-enabled-default-flip-to-true/11.1-02-SUMMARY.md`
- FOUND: commits `3ac7a94`, `7e9053b`, `b69b335`
