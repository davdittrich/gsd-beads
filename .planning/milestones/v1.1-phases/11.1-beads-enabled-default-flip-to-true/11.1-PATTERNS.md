# Phase 11.1: beads.enabled default flip to true - Pattern Map

**Mapped:** 2026-08-17
**Files analyzed:** 9 (8 modified, 1 new)
**Analogs found:** 9 / 9 (self-analog for 7 existing files — edit-in-place text changes; external analog for the 1 new file)

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `.gsd/capabilities/beads/capability.json` | config | transform (in-place edit) | itself (current text below) | exact |
| `README.md` | doc | transform (in-place edit) | itself (current text below) | exact |
| `.agents/skills/beads/PRIME.md` | doc | transform (in-place edit) | itself (current text below) | exact |
| `.beads/PRIME.md` | doc | transform (in-place edit) | itself — near-duplicate of `.agents/skills/beads/PRIME.md` | exact |
| `.gsd/capabilities/beads/skills/beads-recall/SKILL.md` | doc | transform (in-place edit) | itself + 3 sibling SKILL.md files (identical gate paragraph) | exact |
| `.gsd/capabilities/beads/skills/beads-sync/SKILL.md` | doc | transform (in-place edit) | sibling SKILL.md files (same paragraph) | exact |
| `.gsd/capabilities/beads/skills/beads-status/SKILL.md` | doc | transform (in-place edit) | sibling SKILL.md files (same paragraph) | exact |
| `.gsd/capabilities/beads/skills/beads-migrate-todos/SKILL.md` | doc | transform (in-place edit) | sibling SKILL.md files (same paragraph) | exact |
| `docs/prd-beads-capability.md` | doc | transform (in-place edit) | itself (current text below) | exact |
| `CHANGELOG.md` (new, repo root) | doc | event-driven (append-per-release) | `/home/dd/.gemini/config/plugins/metaswarm/CHANGELOG.md` (external, not in this repo) | role-match |

**Note on data flow labeling:** every in-scope file except `CHANGELOG.md` is a static text/config asset being hand-edited (no runtime data flow); "transform (in-place edit)" is used loosely to mean "existing content mutated by this phase," not a code data-flow pattern. `CHANGELOG.md` is genuinely event-driven in the sense that each release appends a new entry going forward.

## Pattern Assignments

### `.gsd/capabilities/beads/capability.json` (config)

**Current text to change** (verbatim, from `Read`):

Line 4 — version field:
```json
  "version": "0.1.0",
```
→ bump to `"0.2.0"` per D-02.

Lines 27-31 — the default block:
```json
    "beads.enabled": {
      "type": "boolean",
      "default": false,
      "description": "Master toggle for the beads issue-tracking capability."
    },
```
→ `"default": true` per D-01. No other field in this block changes; `description` wording is unaffected (does not assert on/off state).

No analog needed — this is the single source-of-truth file, edited directly. Sibling capability.json files (`ponytail`, `sota-numerics`) confirm `"version": "0.1.0"` is the project-wide starting convention for capabilities (grep: all three currently at `0.1.0`), so `0.2.0` for a minor default-behavior bump is consistent with semver usage elsewhere in this repo (no capability has bumped past `0.1.0` yet, so this sets the first precedent — keep it a plain `MAJOR.MINOR.PATCH` bump, no pre-release/build metadata).

---

### `README.md` (doc)

**Current text to change** (lines 52-58, from `Read`):
```markdown
### Example workflow

The capability is off until `beads.enabled` is set `true` in the project's
`.planning/config.json` — every step below checks that gate first and no-ops with a visible
notice otherwise.

With it on, gsd-core's own lifecycle commands drive `bd` state directly:
```

**Required transformation per D-04:** flip polarity to "on by default" and add the opt-out instruction in the same breath (same paragraph, not a separate section) — e.g. state beads is enabled by default via `beads.enabled: true`, and that a project can opt out by setting `beads.enabled: false` in `.planning/config.json`. Keep the "every step below checks that gate first" sentence — it's still true, just now gating an opt-out rather than an opt-in.

---

### `.agents/skills/beads/PRIME.md` and `.beads/PRIME.md` (doc, near-identical twins)

**Current text to change** — both files, same line number and same wording (from `Read`/grep):

Line 47:
```markdown
- `beads.enabled` (default `false`) — master toggle for this whole integration.
```
→ `(default `true`)`.

These two files are byte-for-byte identical at the relevant lines (confirmed via grep — both show `47:- \`beads.enabled\` (default \`false\`) — master toggle...`). Edit both independently; do not assume editing one propagates to the other (no symlink/generation step observed).

Neighboring lines 48-50 (`sync_mode`, `ship_gate`, `epic_per` defaults) are unaffected — only the `enabled` line's default value changes.

---

### 4x `SKILL.md` under `.gsd/capabilities/beads/skills/*/` (doc)

**Shared gate-check paragraph**, identical across `beads-recall`, `beads-sync`, `beads-status`, `beads-migrate-todos` (confirmed via grep, same line numbers in each):

Lines 24-27 pattern (exact text varies only in skill name mentioned around it):
```markdown
Check whether the beads capability is enabled by reading `.planning/config.json` directly with the Read tool.
...
2. If the file does not exist, or `config.beads` is absent, or `config.beads.enabled !== true`: display the disabled message and **STOP**.
```

Line 35 pattern (skill-specific disabled message, same shape in all 4):
```markdown
Beads sync is disabled (beads.enabled).
```
(and the analogous `Beads recall is disabled...`, `Beads status is disabled...` in the other 3 files)

**Assessment:** This gate-check logic (`config.beads.enabled !== true` → STOP) is **not in scope for this phase** — it correctly still gates on the *value* of `beads.enabled`, and that check remains valid whether the shipped default is `true` or `false`. D-01 lists these 4 files as part of the "full doc sweep," but the only textual claim that needs to change is any "default false" / "off until" prose *outside* this gate-check paragraph. Read each SKILL.md in full during planning to confirm whether any of the 4 has separate prose asserting "disabled by default" beyond the gate-check itself — the two greps above show no such standalone "default false" sentence in these 4 files (only the mechanical `!== true` gate, which is behavior-neutral to this flip). Flag this to the planner: these 4 files may need zero content changes, or only a one-line comment/description tweak — verify per-file before writing an edit action.

---

### `docs/prd-beads-capability.md` (doc)

**Current text to change** (line 148, from grep):
```text
    "beads.enabled":      { "type": "boolean", "default": false },
```
→ `"default": true`. This is a PRD's embedded copy of the config schema table (lines ~148-152 list all 5 `beads.*` config keys with their defaults) — only the `beads.enabled` row's default flips; `sync_mode`, `epic_per`, `ship_gate`, `recall_scope` defaults are untouched.

No other "off by default" prose about `beads.enabled` found in this file via grep (line 248's "Off by default" refers to the unrelated `epic (optional)` milestone-layer decision, not `beads.enabled` — do not touch).

---

### `CHANGELOG.md` (new file, repo root)

**No existing CHANGELOG.md in this repo** (confirmed: `find` from repo root and from `/home/dd` turned up none under `gsd-beads`). No other capability or gsd-core-adjacent project in this working tree ships one either.

**External analog used:** `/home/dd/.gemini/config/plugins/metaswarm/CHANGELOG.md` — a sibling Claude-plugin-ecosystem project (metaswarm, not part of this repo) using **Keep a Changelog** convention:

```markdown
# Changelog

## 0.12.0

### Added
- **`/handoff` command and `handoff` skill**: ...

## 0.11.0

### Changed
- **Beads integration updated for standalone plugin (v0.63.3+)**: ...

### Removed
- `scripts/beads-self-reflect.ts` — replaced by `bd compact` in the standalone beads plugin
```

**Recommended shape for this repo's new `CHANGELOG.md`** (per D-03, Claude's Discretion on exact format — metaswarm's simple `## version` + `### Added/Changed` headers is the closest available convention in the Claude-plugin ecosystem and requires no new tooling):

```markdown
# Changelog

## 0.2.0

### Changed
- **`beads.enabled` now defaults to `true`**: fresh installs of the beads capability run with issue tracking on out of the box. Existing projects with an explicit `beads.enabled` value in `.planning/config.json` are unaffected — only installs that never set the key pick up the new default.
```

Use the bumped capability version (`0.2.0` from D-02) as the changelog entry header, not a separate repo-level version — this repo has no other versioned artifact to key the changelog off of.

## Shared Patterns

### "default false" / "off until" wording sweep

**Source of truth for old wording:** `.gsd/capabilities/beads/capability.json:29` (`"default": false`), `README.md:54` ("off until `beads.enabled` is set `true`"), `.agents/skills/beads/PRIME.md:47` / `.beads/PRIME.md:47` ("default `false`"), `docs/prd-beads-capability.md:148` (`"default": false`).
**Apply to:** all 5 files above — flip each occurrence from false/off to true/on, and where the sentence reads as an opt-in gate (README), rephrase as opt-out per D-04.
**Do not touch:** the `!== true` runtime gate-check logic in the 4 SKILL.md files (behavior-neutral to this flip) and any `.planning/milestones/v1.0-phases/**` historical record (explicitly out of scope per Claude's Discretion in CONTEXT.md).

## No Analog Found

None — every file has either an in-repo self-analog (current text is the analog for a text edit) or, for the one genuinely new file (`CHANGELOG.md`), an external analog (`metaswarm`'s Keep-a-Changelog-style file) was located and excerpted above.

## Metadata

**Analog search scope:** repo root, `.gsd/capabilities/beads/**`, `.agents/skills/beads/`, `.beads/`, `docs/`, plus external scan of `/home/dd/**` and `/` (maxdepth 6) for any `CHANGELOG.md` precedent.
**Files scanned:** 9 in-repo target files (read/grepped in full at relevant lines) + 1 external CHANGELOG.md (partial read, 30 lines).
**Pattern extraction date:** 2026-08-17
