---
phase: 11-sota-numerics-capability-plugin-sota-efficiency-numerical-st
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - sota-numerics/.gsd/capabilities/sota-numerics/capability.json
  - sota-numerics/.gsd/capabilities/sota-numerics/fragments/checker-citation-spotcheck.md
  - sota-numerics/.gsd/capabilities/sota-numerics/GSD-CORE-PATCH.md
  - sota-numerics/.gsd/capabilities/sota-numerics/NOTES.md
  - sota-numerics/tests/test-checker-patch.sh
  - .gsd/capabilities/sota-numerics/capability.json
  - .gsd/capabilities/sota-numerics/fragments/checker-citation-spotcheck.md
  - .gsd/capabilities/sota-numerics/GSD-CORE-PATCH.md
  - .gsd/capabilities/sota-numerics/NOTES.md
autonomous: false
requirements: [D-04, D-08]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The route chosen for D-08's citation-plausibility spot-check is recorded with its rationale, rather than silently defaulted (RESEARCH Assumption A2 explicitly demands this be surfaced)."
    - "If the patch route is chosen: `~/.claude/gsd-core/workflows/plan-phase.md` renders `into: \"checker\"` contributions into the gsd-plan-checker prompt, the change is bracketed by a locatable marker, and a patch-loss detector fails loudly when the marker is absent."
    - "If the patch route is chosen: a `checker-citation-spotcheck.md` fragment exists and is declared as a `plan:post` contribution with `into: \"checker\"`."
    - "If the mechanical-only route is chosen: `GSD-CORE-PATCH.md` is NOT created, and NOTES.md records that D-08's LLM-mediated layer was consciously deferred with the deterministic heuristics in `check-alternatives.py` standing in for it."
    - "`NOTES.md` documents that the `plan:post` gate fires AFTER plans are committed and AFTER STATE.md says Ready to execute, so the plan-checker revision loop is the primary enforcement point and the gate is a backstop (RESEARCH Pitfall 1)."
    - "`NOTES.md` documents the `onError: \"halt\"` divergence from every other gate in this repo and why reverting it to `skip` would defeat the gate."
    - "The §13b/§13d/§13e ordering is RE-VERIFIED against the installed `plan-phase.md` during this plan rather than restated from RESEARCH, and the verification outcome is recorded in the SUMMARY (REVIEWS finding 1)."
    - "`NOTES.md` records that `check-alternatives.py` prints the `/gsd-plan-phase <N> --force` remediation to stderr on exit 1, so the recovery path does not depend on a contributor finding NOTES.md (REVIEWS findings 1 and 3)."
    - "`NOTES.md` records the recency rule as at-least-one-year-in-window and warns that tightening it to reject out-of-window years would break every foundational citation (REVIEWS finding 2)."
    - "The full test suite for the plugin passes unchanged after this plan's edits."
  artifacts:
    - .gsd/capabilities/sota-numerics/NOTES.md
    - sota-numerics/.gsd/capabilities/sota-numerics/NOTES.md
  key_links:
    - "`checker-citation-spotcheck.md` (if created) → a `plan:post` contribution with `into: \"checker\"` → the patched render call in `plan-phase.md` step 10 → the gsd-plan-checker prompt. Every link in this chain must exist or the fragment is inert."
    - "patch-loss detector → a marker string inside the patched region of `plan-phase.md`, checked from a call site the GENERIC dispatch already reaches (`plan:pre`), never from inside the patch itself."
---

<objective>
Close D-08's citation-plausibility layer and write the capability's operator documentation.

Purpose: D-08 locks that `gsd-plan-checker` receives a contribution fragment to spot-check citation
plausibility. RESEARCH verified that channel is inert in the installed gsd-core — no call site
renders `into: "checker"` contributions — so honouring D-08 literally requires a small local patch,
the same category as this repo's existing `beads` `ship.md` patch. RESEARCH Assumption A2 states
this choice must be surfaced to the user rather than silently defaulted, which is why this plan
opens with a decision checkpoint instead of picking a route.

Output: D-08 resolved on the record, plus the NOTES.md that stops a future contributor from
"fixing" the two deliberate divergences this capability ships with.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-sota-numerics-capability-plugin-sota-efficiency-numerical-st/11-CONTEXT.md
@.planning/phases/11-sota-numerics-capability-plugin-sota-efficiency-numerical-st/11-RESEARCH.md
@.planning/phases/11-sota-numerics-capability-plugin-sota-efficiency-numerical-st/11-01-SUMMARY.md
@.planning/phases/11-sota-numerics-capability-plugin-sota-efficiency-numerical-st/11-02-SUMMARY.md
</context>

<review_disposition>

## Cross-AI review findings (11-REVIEWS.md, Antigravity) — disposition

The full disposition table for all three actionable findings lives in `11-01-PLAN.md`'s
`<review_disposition>` section. This plan carries the documentation half of two of them.

**[MEDIUM] Late gate execution post-commit — INCORPORATED, split across two plans.** The reviewer
accepted that `NOTES.md` already documents the ordering and asked for two things: that the
documented divergence be double-checked as still accurate after the replan, and that the
remediation path be exercised at execute time rather than only written down. Task 3 now requires
re-verifying the §13b/§13d/§13e ordering against the installed `plan-phase.md` and recording the
outcome in the SUMMARY, so a stale claim cannot be copied forward. The "actually discoverable"
half is Plan 01 Task 1's stderr `remediation: ... --force` line, printed by the script on every
block. NOT deferred, and the gate ordering itself is NOT changed — reordering `plan-phase.md` to
gate before commit would be a second gsd-core patch, strictly larger than the D-08 patch this plan
already puts behind a blocking checkpoint, and the review rated the finding MEDIUM against a
documented-and-now-announced workaround rather than demanding the reorder.

**[LOW] Gate command assumes local capability install — INCORPORATED.** The guard itself is Plan 01
Task 1; Task 3 here records it in `NOTES.md` as a deliberate fail-closed choice so it is not later
softened to exit 0.

**[LOW] Citation recency window** — mechanically fixed in Plan 01, taught in Plan 02, and recorded
as an anti-regression note in Task 3 point 5 here.
</review_disposition>

<artifacts_this_phase_produces>

## Artifacts this phase produces

This plan creates, conditional on the Task 1 decision:
- `fragments/checker-citation-spotcheck.md` (patch route only), in both the plugin bundle and the
  dogfood copy
- a fifth `contributions[]` entry: `point` `plan:post`, `into` `checker` (patch route only)
- `GSD-CORE-PATCH.md` documenting the local `plan-phase.md` patch (patch route only)
- `tests/test-checker-patch.sh`, the patch-loss detector's own smoke test (patch route only)

Unconditionally: `NOTES.md` in both the plugin bundle and the dogfood copy.

No new config key is introduced on either route — D-11 keeps `sota-numerics.enabled` as the sole
key. Full phase-wide artifact list: see `11-01-PLAN.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide D-08's citation-plausibility route</name>
  <decision>How does `gsd-plan-checker` perform D-08's citation-plausibility spot-check: via a local gsd-core patch that renders `into: "checker"` contributions, or via the deterministic heuristics already shipped in `check-alternatives.py`?</decision>
  <context>
D-08 is a locked decision: `gsd-plan-checker` "gets a contribution fragment to spot-check
plausibility before the gate is allowed to pass." RESEARCH verified against the installed gsd-core
1.10.0 source that no workflow call site renders `kind=="contribution"` hooks with `into=="checker"`
into the checker prompt — the role is schema-valid at the `plan` step, but zero capabilities use it
and `plan-phase.md` step 10 constructs the checker prompt without a render call. A fragment declared
today would be schema-valid, installed, and silently inert — exactly the failure mode `beads`'s own
`plan:post` step (`beads-sync`) already exhibits in this repo.

RESEARCH Assumption A2 rates this a MEDIUM-risk judgment call and states explicitly: "flag this
choice explicitly to the user during planning rather than silently defaulting."

Note what is NOT at stake: the deterministic layer ships either way. Plan 01's
`check-alternatives.py` already rejects placeholder hosts, bare TODO/TBD citations, and years
outside the current window. The question is only whether an LLM ALSO reads the citations.
  </context>
  <options>
    <option id="patch">
      <name>Local gsd-core patch + checker fragment (honours D-08 literally)</name>
      <pros>Delivers what D-08 actually locked: genuine LLM judgment on whether a cited URL and date are plausible, which no regex can approximate. Catches a syntactically valid but hallucinated citation — the exact failure the deterministic layer cannot see. Enforcement lands in the checker revision loop, which runs BEFORE plans are committed, making it the cheap enforcement point RESEARCH Pitfall 1 recommends. Precedent exists in this repo (`beads/GSD-CORE-PATCH.md`) with an established documentation, upstream-issue, revert-condition, and patch-loss-detection discipline. The patch is far smaller than beads': one render call in one prompt-construction step, no new gate logic.</pros>
      <cons>Patches a machine-local file outside this repository's git history, shared by every gsd-core project on this machine. Silently lost on any gsd-core upgrade unless the patch-loss detector fires. Adds a second permanent patch to maintain and revert. Violates this project's N2 constraint a second time.</cons>
    </option>
    <option id="mechanical">
      <name>Deterministic heuristics only, defer the LLM layer (RESEARCH's own recommendation)</name>
      <pros>Ships Phase 11 patch-free with zero core-repo risk and no new maintenance surface. Consistent with this repo's ponytail ladder discipline — reach for a deterministic check before an LLM-mediated one. RESEARCH recommends it as the first move, escalating only if dogfood use (D-04) shows an unacceptable false-negative rate. The checker already sees the Alternatives Considered section as ordinary plan content through its generic review instructions, so plausibility problems are not wholly invisible to it.</pros>
      <cons>Does not literally satisfy D-08's "gets a contribution fragment" wording — it delivers D-08's stated intent (a soft, human-adjacent layer) through a different mechanism, and that gap must be recorded, not glossed. Cannot detect a well-formed hallucinated URL.</cons>
    </option>
  </options>
  <resume-signal>Select: patch, or mechanical</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement the chosen D-08 route</name>
  <files>sota-numerics/.gsd/capabilities/sota-numerics/fragments/checker-citation-spotcheck.md, sota-numerics/.gsd/capabilities/sota-numerics/capability.json, sota-numerics/.gsd/capabilities/sota-numerics/GSD-CORE-PATCH.md, sota-numerics/tests/test-checker-patch.sh, .gsd/capabilities/sota-numerics/capability.json, .gsd/capabilities/sota-numerics/fragments/checker-citation-spotcheck.md, .gsd/capabilities/sota-numerics/GSD-CORE-PATCH.md</files>
  <read_first>
    - `.gsd/capabilities/beads/GSD-CORE-PATCH.md` — the documentation shape a local patch must follow: target file, why the patch exists with the constraint-override citation, the upstream issue number, the explicit revert condition listing every artifact that becomes unnecessary, and the note that patch-loss detection must live outside the patch itself.
    - `~/.claude/gsd-core/workflows/plan-phase.md` step 10 (`## 10. Spawn gsd-plan-checker Agent`) — the exact prompt-construction point the render call is inserted into, and step 5.6's existing `plan:pre` contribution render call, which is the pattern to mirror.
    - `~/.claude/gsd-core/bin/lib/loop-host-contract.cjs` — confirms `checker` is a valid `into` value at the `plan` step.
    - `.gsd/capabilities/beads/skills/beads-recall/SKILL.md` Step 3.5 — the patch-loss detector pattern: a cheap marker-presence check run from a call site the generic dispatch already reaches.
    - `sota-numerics/.gsd/capabilities/sota-numerics/capability.json` — the manifest as Plan 02 left it.
    - `sota-numerics/.gsd/capabilities/sota-numerics/scripts/check-alternatives.py` — the deterministic plausibility rules already shipped, so the fragment complements rather than duplicates them.
  </read_first>
  <action>
Implement exactly the route selected in Task 1. Do not implement both, and do not substitute the
other route.

IF `patch` was selected:
Write `fragments/checker-citation-spotcheck.md` as a short prose fragment (4-7 lines, same register
as the Plan 02 fragments, no fenced code blocks) instructing the plan checker to open each
Alternatives Considered entry in the plans under review and judge whether the cited source and date
are plausible: whether the URL's host is a real project, vendor, or documentation domain rather than
an invented one; whether the path shape is consistent with that host's real documentation layout;
whether the stated date is consistent with the cited artifact's actual release history; and whether
the named ranked criterion actually follows from the comparison written. State that the
deterministic gate already rejects placeholder hosts, bare TODO/TBD citations, and out-of-window
years, so the checker's job is only the judgment the regex cannot make. State that a citation the
checker judges implausible is raised as a BLOCKER in the checker's verdict, which forces a planner
revision before anything commits — this is deliberately earlier and cheaper than the `plan:post`
gate.

Add a fifth `contributions[]` entry: `point` `plan:post`, `into` `checker`, `produces: []`,
`consumes: []`, `fragment.path` `fragments/checker-citation-spotcheck.md`, `when`
`sota-numerics.enabled`, `onError` `skip`, no `configValues`.

Patch `~/.claude/gsd-core/workflows/plan-phase.md` step 10 to render `plan:pre` and `plan:post`
`kind=="contribution"` hooks whose `into` equals `checker` into `checker_prompt` before the checker
`Agent()` call, mirroring step 5.6's existing render call for the planner. Bracket the inserted
region with a locatable, greppable marker pair containing the literal token `sota-numerics-checker-render`
so the patch-loss detector has something stable to find.

Write `GSD-CORE-PATCH.md` following beads' structure: target file, why the patch exists (the
verified absence of any `into: "checker"` render call, with the file and step named), the N2
constraint override this represents and who authorised it (the Task 1 checkpoint), the upstream
issue to file and its number once filed, an explicit revert condition (once gsd-core ships a native
`into: "checker"` render call, delete the patch, this file, the detector script, and the
`plan:post`/`checker` contribution entry — do not keep them as permanent duplication), and the exact
patched text.

Write `tests/test-checker-patch.sh` as a stdlib-only bash detector that greps the installed
`plan-phase.md` for the marker token and exits non-zero with an explicit "patch lost — reapply from
GSD-CORE-PATCH.md" message when absent. Per the beads precedent, this detector must NOT be invoked
from inside the patched region — it runs standalone and from the plugin's test suite.

IF `mechanical` was selected:
Create no fragment, no fifth contribution entry, and no `GSD-CORE-PATCH.md` or detector. Instead,
strengthen the deterministic layer in `check-alternatives.py` only if a concrete gap was named
during the checkpoint. Record in the SUMMARY that D-08's LLM-mediated layer is consciously deferred,
that the deterministic heuristics stand in for it, and what dogfood signal (D-04) would trigger
revisiting — a plan passing the gate on a citation later found hallucinated.

On either route, keep the plugin bundle and the `.gsd/capabilities/sota-numerics/` dogfood copy
byte-identical.
  </action>
  <verify>
    <automated>python3 -m unittest discover -s sota-numerics/tests -p 'test_*.py' && bash sota-numerics/tests/test-session-start.sh && diff -r sota-numerics/.gsd/capabilities/sota-numerics .gsd/capabilities/sota-numerics && CK=$(jq '[.contributions[]|select(.into=="checker")]|length' .gsd/capabilities/sota-numerics/capability.json) && if [ -e .gsd/capabilities/sota-numerics/GSD-CORE-PATCH.md ]; then [ "$CK" = "1" ] && echo "route consistent: patch"; else [ "$CK" = "0" ] && echo "route consistent: mechanical"; fi</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one route's artifacts exist: either `GSD-CORE-PATCH.md` plus a `checker-citation-spotcheck.md` fragment plus exactly one contribution with `into` `checker`, or none of the three. The verify command asserts this pairing.
    - Patch route only: `bash sota-numerics/tests/test-checker-patch.sh` exits 0 with the patch applied, and exits non-zero with a message containing `patch lost` when run against a copy of `plan-phase.md` with the marker removed.
    - Patch route only: `grep -c 'sota-numerics-checker-render' ~/.claude/gsd-core/workflows/plan-phase.md` reports at least 2 (the marker pair).
    - Patch route only: `GSD-CORE-PATCH.md` contains a `Revert condition` heading and names every artifact to delete on revert.
    - Patch route only: the fragment contains no fenced code block and is 4-12 lines.
    - Mechanical route only: `test ! -e .gsd/capabilities/sota-numerics/GSD-CORE-PATCH.md` succeeds and `capability.json` has exactly 4 contributions.
    - Either route: `python3 -m unittest discover -s sota-numerics/tests -p 'test_*.py'` still passes and `gates[0].onError` is still `halt`.
    - Either route: `diff -r sota-numerics/.gsd/capabilities/sota-numerics .gsd/capabilities/sota-numerics` reports no differences.
    - Note for the executor: inline interpreter flags (`node -e`, `python3 -c`) are blocked by this environment's shell policy. Use `jq` for JSON assertions and a real script file for anything longer.
  </acceptance_criteria>
  <precondition>`~/.claude/gsd-core/workflows/plan-phase.md` is writable by the current user and its step 10 checker-spawn section is present at the shape RESEARCH recorded — required only on the patch route; halt and report if the section has moved or the file is read-only.</precondition>
  <reversibility rating="costly">On the patch route, the gsd-core patch is a machine-local edit outside this repository's git history — reverting is mechanical but the patch is silently lost on upgrade, which is why the detector and revert condition are mandatory. The mechanical route is fully reversible.</reversibility>
  <done>D-08 is resolved by exactly one implemented route, its artifacts are internally consistent, and on the patch route a working detector will announce the patch's loss.</done>
</task>

<task type="auto">
  <name>Task 3: Write NOTES.md documenting the capability's deliberate divergences and anti-regression rules</name>
  <files>sota-numerics/.gsd/capabilities/sota-numerics/NOTES.md, .gsd/capabilities/sota-numerics/NOTES.md</files>
  <read_first>
    - `ponytail-everywhere/.gsd/capabilities/ponytail/NOTES.md` — the existing per-capability NOTES.md shape and length in this repo.
    - `.planning/phases/11-.../11-RESEARCH.md` Pitfall 1 — the verified `plan-phase.md` step ordering that puts §13e's gate after §13b's STATE.md update and §13d's commit.
    - `~/.claude/gsd-core/workflows/plan-phase.md` steps 13a-13e — read the LIVE file to re-verify that ordering rather than trusting RESEARCH's record of it (REVIEWS finding 1); this is the source of truth for what NOTES.md claims.
    - `.planning/phases/11-.../11-REVIEWS.md` — the three findings whose dispositions this plan's `<review_disposition>` section records.
    - `.planning/phases/11-.../11-01-SUMMARY.md` and `11-02-SUMMARY.md` — the exact gate command string and `into:` values that shipped.
    - `sota-numerics/.gsd/capabilities/sota-numerics/capability.json` — the manifest as it now stands.
  </read_first>
  <action>
Write `NOTES.md` for the capability, covering five things a future contributor will otherwise
"fix" wrongly:

1. **`gates[0].onError` is `halt`, deliberately.** Every other gate, step, and contribution in this
   repo's `beads` and `ponytail` capabilities uses `skip`. `onError` governs the check command
   itself failing to run, not the block decision (which comes from the exit code). A blocking gate
   that silently skips when its checker cannot run defeats its own purpose. Cite CONTEXT.md's
   Established Patterns section. State that `contributions[].onError` correctly stays `skip` and
   that the two values are not inconsistent.

2. **The `plan:post` gate fires late — after commit.** Record the verified step order: §13a decision
   coverage, §13b STATE.md marked Ready to execute, §13d plans committed, then §13e the gate. A
   block at §13e therefore halts with the non-compliant plan already committed and STATE.md already
   claiming readiness. This is expected under the current gsd-core, not a bug in this capability.
   State the consequence: the plan-checker revision loop (steps 10-12) is the PRIMARY enforcement
   point and the gate is a structural backstop, and the remediation is to fix the plan and re-run
   `/gsd-plan-phase N --force`. State that this capability is the first to exercise the
   `blocking: true` branch of §13e, whose own code comment notes it had only ever served a
   non-blocking capability.

   Before writing this section, RE-VERIFY the step ordering against the installed
   `~/.claude/gsd-core/workflows/plan-phase.md` rather than restating RESEARCH's Pitfall 1 from
   memory — the cross-AI review (REVIEWS finding 1) asked specifically whether this documented
   divergence is still accurate, and the answer belongs in the SUMMARY either way. If the installed
   ordering has changed so that the gate now runs before the commit, say so and drop the backstop
   framing instead of copying a stale claim forward.

   Record that the remediation is no longer documentation-only: per REVIEWS finding 1's overlap with
   finding 3, `check-alternatives.py` itself prints `remediation: ... /gsd-plan-phase <N> --force`
   to stderr on every exit-1 run (Plan 01 Task 1), so an operator hitting the gate sees the recovery
   command at the moment of failure without opening this file. NOTES.md explains WHY that line
   exists; the script is what makes it discoverable.

3. **Script path resolution.** Record that the gate command resolves the script through
   `git rev-parse --show-toplevel` rather than `${CLAUDE_PLUGIN_ROOT}`, because the gate-evaluation
   subprocess's environment was not verified to carry that variable while its `cwd` is the project
   root (RESEARCH Open Question 1). State the consequence: the capability's gate only works where
   the bundle is installed at the project root under `.gsd/capabilities/sota-numerics/`, which is
   what `capability install` produces. Record the `test -f` guard the gate command carries (REVIEWS
   finding 3): when the plugin exists only in the global cache and no local install has run, the
   gate exits 1 with a message naming the missing path and the `capability install` remediation
   rather than an opaque `python3` file-not-found. State that this fails closed deliberately —
   removing the guard, or softening it to exit 0, would let an uninstalled capability silently
   stop gating.

4. **The D-08 route actually taken**, with its rationale as decided at this plan's checkpoint, and —
   on the mechanical route — the dogfood signal that would trigger revisiting it.

5. **The recency rule is at-least-one-in-window, not none-outside-window** (REVIEWS finding 2).
   State the rule and the reason: an alternative citing Kahan summation, IEEE 754, or a classic BLAS
   paper alongside a current doc passes, because only one in-window year is required and the
   canonical year is ignored. Warn explicitly that "tightening" this to reject any out-of-window
   year — the obvious-looking reading of D-07 — would make every foundational citation fail, and
   that `plan-foundational.md` plus the generated-year tests exist to catch exactly that regression.
   Name `planner-sota.md` as the place the pairing rule is taught to the planner, so a future edit
   changing one updates the other.

Keep it to roughly a page and a half. Copy it to the dogfood location so both trees stay identical.
  </action>
  <verify>
    <automated>test -s .gsd/capabilities/sota-numerics/NOTES.md && diff sota-numerics/.gsd/capabilities/sota-numerics/NOTES.md .gsd/capabilities/sota-numerics/NOTES.md && for t in onError 13e rev-parse D-08 --force 'capability install' planner-sota.md; do grep -q -- "$t" .gsd/capabilities/sota-numerics/NOTES.md || { echo "NOTES.md missing topic: $t"; exit 1; }; done; echo "NOTES.md covers all five topics"</automated>
  </verify>
  <acceptance_criteria>
    - `NOTES.md` exists in both trees and the two copies are identical.
    - It contains the literal strings `onError`, `13e`, `rev-parse`, `D-08`, `--force`, `capability install`, and `planner-sota.md`.
    - It states that the plan-checker revision loop is the primary enforcement point and the `plan:post` gate is a backstop.
    - It names `/gsd-plan-phase` with the `--force` flag as the remediation after a gate block, AND records that `check-alternatives.py` prints that same remediation to stderr on exit 1 (REVIEWS finding 1).
    - It records the `test -f` missing-script guard and that it fails closed deliberately (REVIEWS finding 3).
    - It states the recency rule is at-least-one-year-in-window and warns against tightening it to reject out-of-window years (REVIEWS finding 2).
    - The SUMMARY records whether the §13b/§13d/§13e ordering was re-verified against the installed `plan-phase.md` and whether it still matches what RESEARCH Pitfall 1 recorded.
    - It states that `contributions[].onError` is `skip` while `gates[0].onError` is `halt`, and that this is intentional.
  </acceptance_criteria>
  <done>The deliberate divergences, the re-verified late-gate ordering, the fail-closed script guard, and the at-least-one-in-window recency rule are documented at the capability, so a future edit that "fixes" any of them has to argue with a written rationale first.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo → `~/.claude/gsd-core/workflows/plan-phase.md` | On the patch route, a repo-driven edit modifies a machine-local file shared by every gsd-core project on this machine. |
| checker fragment → gsd-plan-checker prompt | On the patch route, fragment text is materialised into a reviewing agent's prompt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-11 | Tampering | the local `plan-phase.md` patch affecting every project on this machine | medium | mitigate | Patch route is taken only on explicit approval at the Task 1 blocking checkpoint. The insertion is bracketed by a marker pair, documented in `GSD-CORE-PATCH.md` with an explicit revert condition, and confined to one prompt-construction step — it adds no gate-evaluation logic and changes no other project's behavior unless that project also activates this capability, which is `when`-gated on `sota-numerics.enabled`. |
| T-11-12 | Repudiation | the patch silently vanishing on a gsd-core upgrade, leaving D-08 inert while the manifest still claims coverage | medium | mitigate | `tests/test-checker-patch.sh` greps for the marker and fails loudly; per the beads precedent it lives outside the patched region so it cannot be lost with the patch. Acceptance criteria require it to be demonstrated failing against a marker-stripped copy, not merely to exist. |
| T-11-13 | Information Disclosure | a checker fragment instructing an agent to fetch a cited URL | low | mitigate | The fragment instructs judgment about plausibility from the agent's own knowledge; it must not instruct network fetches of URLs taken from plan bodies, which are untrusted input. Fragment stays plain prose with no tool directives. |
| T-11-14 | Spoofing | a hallucinated but well-formed citation surviving on the mechanical route | medium | accept | Explicitly accepted at the Task 1 checkpoint if that route is chosen, and recorded in `NOTES.md` with the dogfood signal that would trigger revisiting. Consistent with D-08's own "soft, human-adjacent check, not a hard guarantee" framing. |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this plan — markdown, JSON, and shell only. |

No threat is rated `high`; `security_block_on` is `high`, so nothing here blocks.
</threat_model>

<verification>
- The Task 1 checkpoint is answered on the record before any Task 2 edit.
- Exactly one D-08 route's artifacts exist, asserted by the pairing check in Task 2's verify.
- On the patch route, the detector fails against a marker-stripped copy of the workflow.
- `NOTES.md` is identical in both trees and covers all four documented topics.
- The full plugin test suite (`python3 -m unittest discover` plus the two bash smoke tests) passes.
</verification>

<success_criteria>
D-08 is resolved by an explicitly chosen and implemented route rather than a silent default, and the
capability ships with written rationale for its two deliberate divergences (`onError: "halt"`, and
the gate's late position relative to commit) so neither is reverted by a future contributor who
reads only the surrounding code.
</success_criteria>

<output>
Create `.planning/phases/11-sota-numerics-capability-plugin-sota-efficiency-numerical-st/11-03-SUMMARY.md` when done.
Record in it: the route selected at the Task 1 checkpoint and the user's stated reason, and — on the
patch route — the upstream issue filed and the marker token used. Also record the outcome of Task 3's
re-verification of the §13b/§13d/§13e ordering against the installed `plan-phase.md`: whether it
still matches RESEARCH Pitfall 1, and if not, what changed (REVIEWS finding 1).
</output>
