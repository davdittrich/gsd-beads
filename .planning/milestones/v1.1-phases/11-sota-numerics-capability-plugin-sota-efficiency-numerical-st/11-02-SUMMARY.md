---
phase: 11-sota-numerics-capability-plugin-sota-efficiency-numerical-st
plan: 02
subsystem: infra
tags: [claude-code-plugin, capability-loader, fragment-contribution, advisory-steering]

# Dependency graph
requires:
  - phase: 11-01
    provides: "sota-numerics capability plugin's plan:post blocking gate spine, config key (sota-numerics.enabled), and dogfood install pattern"
provides:
  - "sota-numerics capability's full D-12 four-point advisory spread: distinct, stage-tailored steering fragments materialised at plan:pre, execute:wave:pre, execute:wave:post, and ship:pre"
affects: [11-03-checker-spotcheck-decision]

# Actuals (#2632) — pairs with the plan's `estimate` to calibrate future estimates.
actuals:
  tokens: 4512
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Four-point contribution spread on a single config gate (sota-numerics.enabled), no configValues on any contribution (D-11) — mirrors ponytail's contribution shape minus its level knob"
    - "ship:pre contribution's into: orchestrator is the only agentRole the loop-host contract's ship step declares — not reviewer or shipper"

key-files:
  created:
    - sota-numerics/.gsd/capabilities/sota-numerics/fragments/planner-sota.md
    - sota-numerics/.gsd/capabilities/sota-numerics/fragments/executor-numerics.md
    - sota-numerics/.gsd/capabilities/sota-numerics/fragments/verifier-precision.md
    - sota-numerics/.gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md
    - .gsd/capabilities/sota-numerics/fragments/ (dogfood copies, byte-identical)
  modified:
    - sota-numerics/.gsd/capabilities/sota-numerics/capability.json (contributions[] populated, 4 entries)
    - .gsd/capabilities/sota-numerics/capability.json (dogfood copy)
    - sota-numerics/hooks/session-start.sh (per-role FRAMING strings condensed from the new fragments)

key-decisions:
  - "session-start.sh's per-role FRAMING strings, though already fairly complete from Plan 01's packaging commit, were rewritten to explicitly condense the actual fragment text written in Task 1 (first-principles/problem-scale numeric parameters, ranked-criterion order, shipped-vs-justified mechanism drift) rather than leaving Plan 01's anticipatory wording unchanged — keeps the banner and the rendered fragment from drifting apart even though both already satisfied the smoke test's literal substrings."
  - "A stray .gsd/capabilities/sota-numerics/scripts/__pycache__/ directory (gitignored, generated by an earlier interpreter run) was deleted before the dogfood-parity diff -r check — not a task deliverable, just cleanup required for the plan's own literal verify command to pass; nothing was ever staged or committed for it."

requirements-completed: [D-05, D-11, D-12, D-13]

coverage:
  - id: D12
    description: "All four D-12 lifecycle points (plan:pre, execute:wave:pre, execute:wave:post, ship:pre) render a sota-numerics contribution, and gsd_run loop render-hooks materialises each fragment's full body inline, not just a hook listing"
    requirement: D-12
    verification:
      - kind: other
        ref: "node ~/.claude/gsd-core/bin/gsd-tools.cjs loop render-hooks {plan:pre,execute:wave:pre,execute:wave:post,ship:pre} --raw -> each output's rendered field contains a <contribution from=\"sota-numerics\" into=\"...\"> block with the fragment's full inline text, not a placeholder"
        status: pass
      - kind: other
        ref: "jq -e '([.contributions[].point]|sort) == [\"execute:wave:post\",\"execute:wave:pre\",\"plan:pre\",\"ship:pre\"]' capability.json"
        status: pass
    human_judgment: false
  - id: D13
    description: "Four distinct fragment bodies, no shared generic reminder text"
    requirement: D-13
    verification:
      - kind: other
        ref: "md5sum sota-numerics/.gsd/capabilities/sota-numerics/fragments/*.md | awk '{print $1}' | sort -u | wc -l -> 4"
        status: pass
    human_judgment: false
  - id: D11
    description: "Every contribution gated on the single key sota-numerics.enabled; no contribution declares configValues"
    requirement: D-11
    verification:
      - kind: other
        ref: "jq -e '([.contributions[]|has(\"configValues\")]|any|not) and (.config|length)==1' capability.json -> true"
        status: pass
    human_judgment: false
  - id: D5
    description: "planner-sota.md states the SPEC.md-does-not-exempt-PLAN.md's-own-Alternatives-Considered-section rule (D-05)"
    requirement: D-05
    verification:
      - kind: other
        ref: "grep -c 'SPEC.md' sota-numerics/.gsd/capabilities/sota-numerics/fragments/planner-sota.md -> 1"
        status: pass
    human_judgment: false
  - id: TESTSESSION
    description: "bash sota-numerics/tests/test-session-start.sh still passes with the stage-tailored role framings substituted in"
    verification:
      - kind: other
        ref: "bash sota-numerics/tests/test-session-start.sh -> 7/7 PASS, ALL PASS"
        status: pass
    human_judgment: false
  - id: DOGFOODPARITY
    description: "The plugin bundle and the repo-root dogfood copy are identical trees"
    verification:
      - kind: other
        ref: "diff -r sota-numerics/.gsd/capabilities/sota-numerics .gsd/capabilities/sota-numerics -> no differences"
        status: pass
    human_judgment: false

duration: 30min
completed: 2026-08-17
status: complete
---

# Phase 11 Plan 02: sota-numerics Fragment Spread Summary

**Wrote the four stage-tailored sota-numerics steering fragments and wired them as `contributions[]` entries at all four of D-12's lifecycle points, giving `session-start.sh` real per-role framings that condense them.**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-08-17T~10:44Z (continuation from 11-01)
- **Completed:** 2026-08-17T11:14:29Z
- **Tasks:** 2
- **Files modified:** 8 (6 created, 3 modified — 2 files touched by both tasks)

## Accomplishments

- Four distinct, non-shared-text fragments (D-13, confirmed via 4 unique md5 sums): `planner-sota.md` (11 lines — the deliberate longer exception, documenting the exact `Alternatives Considered` format `check-alternatives.py` parses, the D-03 exemption, the D-05 SPEC.md restatement, and the REVIEWS finding 2 foundational-citation pairing rule with `Kahan`/`IEEE 754` examples), `executor-numerics.md` (5 lines — first-principles numeric parameters, stable formulations, no cancellation/error propagation), `verifier-precision.md` (5 lines — shipped-vs-justified mechanism drift and unmeasured claims as findings, not blockers), `ship-precision-advisory.md` (4 lines — advisory pre-ship confirmation, no gate).
- `capability.json`'s `contributions[]` array populated with exactly 4 entries: `plan:pre`->`planner`, `execute:wave:pre`->`executor`, `execute:wave:post`->`verifier`, `ship:pre`->`orchestrator`. `into: "orchestrator"` on the ship entry matches the loop-host contract's ship-step `agentRoles: ["orchestrator"]` constraint exactly. No contribution declares `configValues` (D-11); `gates[0].onError` (`"halt"`) was left untouched.
- `render-hooks --raw` confirmed to **materialise** (not just list) the sota-numerics contribution's full fragment body at all four points — verified against the live gsd-core renderer, not a hand simulation.
- `session-start.sh`'s three role-specific `FRAMING` strings rewritten to condense the actual fragment text now on disk, while keeping every literal substring `test-session-start.sh` asserts on (`ranked criterion`, `blocking plan:post gate`, `avoid cancellation`, `not blockers`); the smoke test's 7 cases still pass, including the injection-guard case.
- Dogfood copy (`D-04`) kept byte-identical to the plugin bundle across both fragments and `capability.json` (`diff -r` clean after removing a stray, gitignored, never-staged `__pycache__` directory left over from an earlier interpreter run).

## Task Commits

Each task was committed atomically:

1. **Task 1: Write the four stage-tailored steering fragments** - `b3cac90` (feat)
2. **Task 2: Wire the four contributions and the per-role session-start framings** - `97f5c2a` (feat)

**Plan metadata:** (this commit)

## Files Created/Modified

- `sota-numerics/.gsd/capabilities/sota-numerics/fragments/planner-sota.md` - planner steering, qualified-advisory (gate follows)
- `sota-numerics/.gsd/capabilities/sota-numerics/fragments/executor-numerics.md` - executor steering, advisory only
- `sota-numerics/.gsd/capabilities/sota-numerics/fragments/verifier-precision.md` - verifier findings-only steering
- `sota-numerics/.gsd/capabilities/sota-numerics/fragments/ship-precision-advisory.md` - ship-time advisory confirmation
- `.gsd/capabilities/sota-numerics/fragments/*.md` - byte-identical dogfood copies of all four
- `sota-numerics/.gsd/capabilities/sota-numerics/capability.json` - `contributions[]` populated (4 entries)
- `.gsd/capabilities/sota-numerics/capability.json` - dogfood copy, kept identical
- `sota-numerics/hooks/session-start.sh` - per-role `FRAMING` strings condensed from the new fragments

## Decisions Made

- Rewrote `session-start.sh`'s FRAMING strings even though Plan 01's anticipatory wording already satisfied every literal substring the smoke test asserts — condensing the *actual* fragment text (rather than leaving text written before the fragments existed) keeps the banner and the rendered fragment from silently drifting apart as either evolves later.
- Deleted a stray, gitignored `__pycache__/` directory under the dogfood `scripts/` path before running the plan's own `diff -r` parity check — pure cleanup of a prior interpreter run's byte-code cache, never staged or committed, not a task deliverable.

## Deviations from Plan

None — plan executed exactly as written. Both items above are within-task cleanup/quality actions, not scope changes: the FRAMING rewrite stays inside Task 2's own instructed action ("replace the placeholder FRAMING strings ... with one-line condensations of the corresponding fragments"), and the `__pycache__` removal is untracked-file hygiene required only to make the plan's own literal verify command pass, with no functional or committed effect.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required. The capability's new contributions render automatically through gsd-core's existing dispatch on the next `plan:pre`/`execute:wave:pre`/`execute:wave:post`/`ship:pre` lifecycle point, gated on the already-live `sota-numerics.enabled` config key.

## `render-hooks` observation (per plan `<output>` spec)

At all four points, `gsd_run loop render-hooks <point> --raw` was observed to **materialise the fragment**, not merely list the hook: each point's JSON `rendered` field contains a full `<contribution from="sota-numerics" into="<role>">...full fragment body...</contribution>` block, with the fragment's inline text present verbatim (matching the source `.md` file byte-for-byte in the body). This was confirmed live against the real renderer for all four points (`plan:pre`, `execute:wave:pre`, `execute:wave:post`, `ship:pre`) — not inferred from the manifest alone.

The four `into:` values used: `planner` (plan:pre), `executor` (execute:wave:pre), `verifier` (execute:wave:post), `orchestrator` (ship:pre).

## Next Phase Readiness

- D-12's full four-point advisory spread is live and proven end-to-end (gate spine from Plan 01 + fragment spread from this plan). No further gate-mechanism or contribution-wiring risk remains for this capability.
- Plan 03's D-08 checker plausibility spot-check decision (patch vs. fold-in) remains unblocked and is the only remaining phase-11 work — this plan added no new surface it depends on beyond the already-live `sota-numerics.enabled` key.
- No blockers.

---
*Phase: 11-sota-numerics-capability-plugin-sota-efficiency-numerical-st*
*Completed: 2026-08-17*

## Self-Check: PASSED

All 12 claimed created/modified files verified present on disk; both task commit hashes (`b3cac90`, `97f5c2a`) verified present in `git log --oneline --all`.
