---
phase: 12-ship-ponytail-everywhere-and-sota-numerics-plugins-publicly
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - /tmp/sota-numerics-extract/tests/test-session-start.sh
  - /tmp/sota-numerics-extract/hooks/capability-auto-install.sh
  - /tmp/sota-numerics-extract/LICENSE
  - /tmp/sota-numerics-extract/.gitignore
  - /tmp/sota-numerics-extract/README.md
  - /tmp/sota-numerics-extract/.github/workflows/ci.yml
autonomous: false
requirements: [D-01, D-03, D-05, D-07, D-09, D-10]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`https://github.com/davdittrich/sota-numerics` is a public repo whose root IS the plugin root (D-01)"
    - "`bash tests/test-session-start.sh` passes when run from the new repo root"
    - "`python3 -m unittest tests/test_check_alternatives.py` passes from the new repo root — the blocking plan:post gate's own test survives relocation unchanged"
    - "`claude plugin validate . --strict` exits 0 from a fresh clone of the pushed repo (D-10)"
    - "The README states that this plugin ships a BLOCKING plan:post gate, not advisory-only behavior (D-09)"
    - "The new repo's git history is a fresh init — no commit imported from gsd-beads (D-03)"
  artifacts:
    - "`/tmp/sota-numerics-extract/` staging tree containing the full plugin, README.md, LICENSE, .gitignore, .github/workflows/ci.yml"
    - "public GitHub repo `davdittrich/sota-numerics` on branch `main`"
    - "`/tmp/sota-numerics-verify/` fresh clone used for the post-push gate"
  key_links:
    - "`tests/test-session-start.sh` REPO_ROOT -> the new repo root (breaks if the `../..` two-level climb survives extraction)"
    - "`tests/test_check_alternatives.py` -> `.gsd/capabilities/sota-numerics/scripts/check-alternatives.py` via `Path(__file__).resolve().parent.parent` (already one level up — must stay unedited)"
    - "capability.json's plan:post gate command -> `$(git rev-parse --show-toplevel)/.gsd/capabilities/sota-numerics/scripts/check-alternatives.py` in the CONSUMING project, not in this repo"
    - "staging location OUTSIDE the gsd-beads working tree -> no embedded/nested git repo is created inside gsd-beads"
---

<objective>
Turn the `sota-numerics/` subdirectory of `gsd-beads` into its own standalone public GitHub repo
(D-01), fresh history (D-03), full README (D-09), full validate proof (D-10) — repeating the exact
sequence Plan 01's tracer proved end-to-end, now with the added Python unit-test gate that covers
this plugin's blocking `plan:post` checker.

This plan runs in wave 2 rather than in parallel with Plan 01 on purpose: the two extractions have
no file conflict, but each ends in a one-way public push, and running the second one only after the
first has been proven start-to-finish means any defect in the sequence is found once, privately,
instead of twice, publicly.

Purpose: `sota-numerics` is reachable by a stranger the way `beads-lifecycle` already is.
Output: a public repo `davdittrich/sota-numerics` that passes `claude plugin validate . --strict`
from a fresh clone.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ship-ponytail-everywhere-and-sota-numerics-plugins-publicly/12-CONTEXT.md
@.planning/phases/12-ship-ponytail-everywhere-and-sota-numerics-plugins-publicly/12-RESEARCH.md
@.planning/phases/12-ship-ponytail-everywhere-and-sota-numerics-plugins-publicly/12-PATTERNS.md
@.planning/phases/12-ship-ponytail-everywhere-and-sota-numerics-plugins-publicly/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage sota-numerics outside the gsd-beads tree and fix the relocation path bugs</name>
  <files>/tmp/sota-numerics-extract/tests/test-session-start.sh, /tmp/sota-numerics-extract/hooks/capability-auto-install.sh, /tmp/sota-numerics-extract/LICENSE, /tmp/sota-numerics-extract/.gitignore</files>
  <read_first>
    - /home/dd/projects/gsd-beads/sota-numerics/tests/test-session-start.sh (the file being modified; lines 8-10 and 33 carry every REPO_ROOT-derived path)
    - /home/dd/projects/gsd-beads/sota-numerics/tests/test_check_alternatives.py (lines 1-30 — confirms `Path(__file__).resolve().parent.parent` is already correct post-relocation and must NOT be edited)
    - /home/dd/projects/gsd-beads/sota-numerics/hooks/capability-auto-install.sh (lines 1-10 and 52-60 carry the two cross-repo comment refs)
    - /home/dd/projects/gsd-beads/sota-numerics/hooks/session-start.sh (confirms PLUGIN_ROOT is already repo-relative and needs NO edit)
    - /home/dd/projects/gsd-beads/LICENSE (the MIT text copied verbatim)
    - /home/dd/projects/gsd-beads/.planning/phases/12-ship-ponytail-everywhere-and-sota-numerics-plugins-publicly/12-01-SUMMARY.md (the sequence Plan 01 proved, including any deviation recorded there)
  </read_first>
  <precondition>Plan 01 completed and `davdittrich/ponytail-everywhere` is public — the extraction sequence used below is the one that plan proved end-to-end.</precondition>
  <action>
    Create the staging tree at the absolute path /tmp/sota-numerics-extract — OUTSIDE the gsd-beads
    working tree. Never run git init inside /home/dd/projects/gsd-beads/sota-numerics.

    Remove any prior staging tree, recreate it, and copy the contents of
    /home/dd/projects/gsd-beads/sota-numerics/ into it so the plugin's own directories
    (.claude-plugin/, hooks/, .gsd/, tests/ including tests/fixtures/) land at the staging-tree ROOT.
    `git ls-files sota-numerics` lists the 22 tracked files the copy must reproduce. Do not copy any
    `__pycache__` directory — it is untracked build output.

    Fix the relocation path bug in the staged tests/test-session-start.sh, exactly as Plan 01 fixed
    the sibling file: REPO_ROOT must climb one level from the script's own directory instead of two,
    SCRIPT must resolve to hooks/session-start.sh directly under REPO_ROOT, and PLUGIN_DIR must be
    REPO_ROOT itself. Leave the `cd "$REPO_ROOT"` line in the cleanup helper as-is. Change nothing
    else — the mk_scratch helper and every config/injection-guard case, including the case that
    asserts the injection payload never creates its sentinel file, are untouched analogs.

    Do NOT edit tests/test_check_alternatives.py. Its SCRIPT constant resolves one level up from
    tests/ already, which is correct both as a subdirectory today and as a repo root after
    extraction; 12-RESEARCH.md verified this explicitly. Its `_project_root()` helper walks up to the
    nearest `.planning/` ancestor, which is a property of the scratch dirs the tests build, not of
    this repo's location.

    Fix the two stale cross-repo path references in the staged hooks/capability-auto-install.sh. Both
    the header comment and the inline gsd_tools resolver comment name a sibling file by a path
    prefixed with the OTHER plugin's directory name — a reference that pointed at a sibling directory
    inside gsd-beads and now points at nothing from inside this repo. Reword each to name the local
    file by its path within THIS repo (hooks/capability-auto-install.sh and hooks/gsd-tools.sh),
    keeping the Phase 10.1 D-05 rationale (vendored copy per plugin, inlined rather than sourced)
    intact. Comment text only — do not alter any executable line, and edit only the staged copy.

    Copy /home/dd/projects/gsd-beads/LICENSE verbatim to the staging root as LICENSE.

    Write a two-line .gitignore at the staging root ignoring __pycache__/ and *.pyc. This one matters
    here: running the Python unit test in this repo creates tests/__pycache__/, and without these
    lines it would be staged into the initial commit.

    Leave .claude-plugin/plugin.json untouched, including its "version": "0.1.0" — per D-05 this repo
    owns its own version sequence and 0.1.0 is its honest start. Cut no tag (D-07 rules out a release
    archive; the marketplace github source Plan 03 writes is unpinned).

    Run both test suites from the staging root to prove the relocation before any git or network
    operation touches the tree.
  </action>
  <verify>
    <automated>cd /tmp/sota-numerics-extract &amp;&amp; bash tests/test-session-start.sh</automated>
    <automated>cd /tmp/sota-numerics-extract &amp;&amp; python3 -m unittest tests/test_check_alternatives.py</automated>
    <automated>test -f /tmp/sota-numerics-extract/LICENSE &amp;&amp; test -f /tmp/sota-numerics-extract/.gsd/capabilities/sota-numerics/scripts/check-alternatives.py &amp;&amp; test -d /tmp/sota-numerics-extract/tests/fixtures</automated>
    <automated>git -C /home/dd/projects/gsd-beads status --porcelain -- sota-numerics | tee /dev/stderr | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/test-session-start.sh` run with cwd = /tmp/sota-numerics-extract exits 0 with a PASS line per case and no "No such file or directory".
    - `python3 -m unittest tests/test_check_alternatives.py` run with cwd = /tmp/sota-numerics-extract exits 0.
    - `grep -c '\.\./\.\.' /tmp/sota-numerics-extract/tests/test-session-start.sh` outputs `0`.
    - `diff /home/dd/projects/gsd-beads/sota-numerics/tests/test_check_alternatives.py /tmp/sota-numerics-extract/tests/test_check_alternatives.py` produces no output — that file is byte-identical, unedited.
    - `diff -r /home/dd/projects/gsd-beads/sota-numerics/tests/fixtures /tmp/sota-numerics-extract/tests/fixtures` produces no output — all eight fixture plans carried over intact.
    - `diff /home/dd/projects/gsd-beads/LICENSE /tmp/sota-numerics-extract/LICENSE` produces no output.
    - `python3 -c "import json; d=json.load(open('/tmp/sota-numerics-extract/.claude-plugin/plugin.json')); assert d['name']=='sota-numerics' and d['version']=='0.1.0' and d['license']=='MIT'"` exits 0.
    - `git -C /home/dd/projects/gsd-beads status --porcelain` lists no change under the sota-numerics path, and `test ! -e /home/dd/projects/gsd-beads/sota-numerics/.git` succeeds.
    - `diff <(grep -v '^#' /home/dd/projects/gsd-beads/sota-numerics/hooks/capability-auto-install.sh) <(grep -v '^#' /tmp/sota-numerics-extract/hooks/capability-auto-install.sh)` produces no output — no executable line changed.
  </acceptance_criteria>
  <done>The staging tree at /tmp/sota-numerics-extract is a complete, path-correct plugin repo root whose bash smoke test and Python unit test both pass from that root, and the gsd-beads working tree is provably unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Write the standalone repo's README and CI, then commit the fresh-init history</name>
  <files>/tmp/sota-numerics-extract/README.md, /tmp/sota-numerics-extract/.github/workflows/ci.yml</files>
  <read_first>
    - /home/dd/projects/gsd-beads/README.md (D-09's structural template: section order and heading text)
    - /tmp/ponytail-everywhere-extract/README.md (the sibling README Plan 01 produced — match its shape so the two published plugins read alike)
    - /home/dd/projects/gsd-beads/sota-numerics/.gsd/capabilities/sota-numerics/capability.json (authoritative source for the config key, its default, engines.gsd, the four contribution points, AND the gates[] entry with its blocking/onError semantics and command — do not invent these)
    - /home/dd/projects/gsd-beads/sota-numerics/.gsd/capabilities/sota-numerics/scripts/check-alternatives.py (lines 1-60 — the rules the gate enforces, for accurate Caveats text)
    - /home/dd/projects/gsd-beads/sota-numerics/hooks/session-start.sh (fail-open behavior of the advisory banner)
  </read_first>
  <action>
    Write /tmp/sota-numerics-extract/README.md following the same seven-section order Plan 01 used and
    gsd-beads' README.md establishes, per D-09: H1 title, one-line description, then What it does,
    Requirements, Install, Uninstall, Caveats, License, gsd-core. Nothing else.

    Title is the plugin name. The one-line description is the "description" string from
    .claude-plugin/plugin.json, verbatim.

    What it does: a gsd-core capability, an installable overlay rather than a fork, with two distinct
    halves that must both be described because D-09 holds this README to the same bar as
    beads-lifecycle's. First half: advisory steering fragments at the four contribution points
    declared in capability.json's contributions array — take the point names and target roles from
    that file. Second half, and it must not be buried: a BLOCKING plan:post gate that runs the
    bundled Alternatives-Considered checker over every plan file in a phase directory and fails the
    plan step when a plan is non-compliant. State plainly what a compliant plan needs, drawn from
    check-alternatives.py: an `## Alternatives Considered` heading, at least two bold-named
    alternatives each carrying a URL or backticked doc-ref citation with a year inside the recency
    window, a `Decided by:` line naming a ranked criterion, and no placeholder host or bare TODO/TBD
    citation — plus the escape hatch that a plan making no real mechanism choice may write the
    documented N/A exemption line as the whole section body. Link gsd-core as
    https://github.com/open-gsd/gsd-core.

    Requirements: Bash (POSIX shell); Python 3, standard library only; git, because the gate resolves
    its own script path through `git rev-parse --show-toplevel` in the consuming project; and gsd-core
    at or above the floor in capability.json's engines.gsd. Do not copy the `bd` bullet from
    gsd-beads' README — this plugin has no beads dependency.

    Install: `claude plugin marketplace add davdittrich/gsd-beads` followed by
    `claude plugin install sota-numerics@gsd-beads -y`. Note in one clause that the marketplace stays
    hosted at davdittrich/gsd-beads (D-02) even though the plugin lives in its own repo.

    Uninstall: `claude plugin uninstall sota-numerics -y`.

    Caveats, all drawn from the files read above rather than invented: the plan:post gate is blocking
    and its onError disposition is halt, deliberately unlike the skip used by the advisory
    contributions — a gate whose own checker cannot run must not silently pass; the gate resolves its
    script from the CONSUMING project's git root under .gsd/capabilities/sota-numerics/scripts/, so
    the capability bundle has to be installed into that project (the SessionStart hook re-grants it
    at user scope on every session start) or the gate halts with the explicit message the gate
    command itself prints; the single config key named in capability.json toggles BOTH the advisory
    fragments and the blocking gate, with the default declared there; the checker reports only the
    first offending alternative per plan, by design, because it is a structural backstop rather than
    a linter; and installing through the marketplace copies the cloned repo into the installer's local
    plugin cache under ~/.claude/plugins/cache/, documented Claude Code behavior this repo does not
    control.

    License: MIT with a relative link to LICENSE. gsd-core: one sentence naming this a capability for
    gsd-core with the same link.

    Write /tmp/sota-numerics-extract/.github/workflows/ci.yml mirroring the file Plan 01 created —
    name CI, push and pull_request triggers, one ubuntu-latest job, actions/checkout@v7, then two
    steps: the bash smoke test and the Python unit test, using the same commands the verify block
    below runs. Same discretion note as Plan 01: D-01..D-10 are silent on CI for the new repos; this
    is included so the published plugin's tests keep running somewhere, and can be struck without
    affecting any locked decision. Note in the summary that unlike ponytail-everywhere's smoke test,
    this plugin's tests were never wired into gsd-beads' own ci.yml, so for this repo the file adds
    coverage rather than relocating it.

    Then create the fresh git history at the staging root: initialize with `main` as the initial
    branch, stage everything, and make exactly one commit with a Conventional Commit subject
    describing the extraction — no AI attribution, no Co-Authored-By trailer. Per D-03 import,
    filter, or graft nothing from gsd-beads.

    Finally run the same pre-publish content audit Plan 01 ran: confirm the tracked file list matches
    an explicit path allowlist and that no secret, dotenv file, or absolute machine-local path is
    about to become public.
  </action>
  <verify>
    <automated>cd /tmp/sota-numerics-extract &amp;&amp; claude plugin validate . --strict</automated>
    <automated>cd /tmp/sota-numerics-extract &amp;&amp; git rev-list --count HEAD | grep -qx 1 &amp;&amp; git symbolic-ref --short HEAD | grep -qx main</automated>
    <automated>cd /tmp/sota-numerics-extract &amp;&amp; git ls-files | tee /dev/stderr | grep -Ev '^(\.claude-plugin/|hooks/|\.gsd/|tests/|\.github/|README\.md$|LICENSE$|\.gitignore$)' | wc -l | grep -qx 0</automated>
    <automated>cd /tmp/sota-numerics-extract &amp;&amp; git ls-files | grep -c '__pycache__' | grep -qx 0</automated>
    <automated>cd /tmp/sota-numerics-extract &amp;&amp; git grep -nE '/home/[a-z]+|BEGIN [A-Z ]*PRIVATE KEY|[A-Za-z0-9_-]*(SECRET|TOKEN|API_KEY)[A-Za-z0-9_-]*=' -- . ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `claude plugin validate . --strict` run with cwd = /tmp/sota-numerics-extract exits 0.
    - `grep -n '^## ' README.md` outputs exactly the seven headings What it does, Requirements, Install, Uninstall, Caveats, License, gsd-core, in that order.
    - README.md's H1 is `# sota-numerics` and the following line matches the `description` value in `.claude-plugin/plugin.json`.
    - README.md names the config key exactly as declared in capability.json (`sota-numerics.enabled`) with its declared default, and names `.planning/config.json` as where it lives.
    - README.md's Caveats section states that the `plan:post` gate is blocking — a reader who greps the README for the word `blocking` finds it in the Caveats or What-it-does section.
    - README.md's Install block contains `claude plugin marketplace add davdittrich/gsd-beads` and `claude plugin install sota-numerics@gsd-beads -y`; its Uninstall block contains `claude plugin uninstall sota-numerics -y`.
    - README.md mentions no `bd` requirement.
    - `git rev-list --count HEAD` outputs `1`; `git symbolic-ref --short HEAD` outputs `main`.
    - `git log -1 --format=%B` contains no `Co-Authored-By` line and no "Claude"/"Anthropic" attribution.
    - `git ls-files` contains no `__pycache__` path and nothing outside the allowlist — both filters in the verify block emit `0`.
    - `git grep` for absolute home paths, private-key headers, and SECRET/TOKEN/API_KEY assignments returns no match (exit 1).
    - `.github/workflows/ci.yml` runs both `bash tests/test-session-start.sh` and `python3 -m unittest tests/test_check_alternatives.py`.
  </acceptance_criteria>
  <done>The staging tree is a validated, documented, single-commit git repo on `main` whose tracked file list has been audited, and whose README describes the blocking gate as prominently as the advisory fragments.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Approve the one-way public repo creation and push</name>
  <reversibility rating="one-way">Creating a public GitHub repo and pushing to it publishes content and a repo name that cannot be reliably un-published; D-01 rates the split permanent.</reversibility>
  <decision>Create the public GitHub repository and push the staged commit to it.</decision>
  <context>
    Task 2 finished a single-commit git repo at /tmp/sota-numerics-extract that passes
    `claude plugin validate . --strict`, whose bash and Python test suites both pass from its root,
    and whose tracked-file list has been audited. The next command creates a PUBLIC repo under the
    `davdittrich` account and pushes that commit. One-way, per D-01. The repo name is also the
    discretion item CONTEXT.md flagged for confirmation, and Plan 03's marketplace.json entry
    hard-codes whatever is chosen here.

    Show the operator, before they decide: `git -C /tmp/sota-numerics-extract ls-files`,
    `git -C /tmp/sota-numerics-extract log --oneline`, and Task 2's `validate --strict` result. If
    Task 3 of Plan 01 selected a non-default name for the sibling repo, surface that choice here too
    so the two names stay consistent.
  </context>
  <options>
    <option id="create-as-planned">
      <name>Create `davdittrich/sota-numerics` public and push</name>
      <pros>Matches CONTEXT.md's stated default, the existing subdirectory name, and the sibling repo created in Plan 01; Plan 03's marketplace entry is written against it.</pros>
      <cons>One-way: the repo name and its contents become public and permanent.</cons>
    </option>
    <option id="create-different-name">
      <name>Create under a different repo name, then push</name>
      <pros>Operator controls the public name.</pros>
      <cons>Plan 03's marketplace.json `repo` value and this repo's README install line must be updated to the chosen name before Plan 03 runs.</cons>
    </option>
    <option id="hold">
      <name>Do not push yet</name>
      <pros>Nothing becomes public; the staging tree is preserved for inspection.</pros>
      <cons>Blocks Plans 03 and 04 — the marketplace repoint needs both repos live.</cons>
    </option>
  </options>
  <resume-signal>Select: create-as-planned, create-different-name (state the name), or hold</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Create the public repo, push, and verify from a fresh clone</name>
  <files>(no files in this repository are modified; this task creates the remote `davdittrich/sota-numerics` and the throwaway clone /tmp/sota-numerics-verify)</files>
  <read_first>
    - /home/dd/projects/gsd-beads/.planning/phases/12-ship-ponytail-everywhere-and-sota-numerics-plugins-publicly/12-01-SUMMARY.md (the exact `gh repo create` invocation Plan 01 used and any deviation it recorded)
    - /home/dd/projects/gsd-beads/.planning/phases/12-ship-ponytail-everywhere-and-sota-numerics-plugins-publicly/12-PATTERNS.md (the Phase 7 precedent and the flags deliberately omitted)
  </read_first>
  <precondition>Task 3's checkpoint was answered with an approval option; the repo name recorded there is the name used below.</precondition>
  <action>
    From the staging root /tmp/sota-numerics-extract, create the remote and push in one step with
    `gh repo create` using the approved name, the public visibility flag, the source flag pointing at
    the current directory, and the push flag — the same invocation Plan 01 used. The source flag
    requires an already-initialized git repo, which Task 2 produced. Do not pass the add-readme,
    gitignore, or license scaffolding flags; Phase 7's D-05 established they force a
    non-fast-forward push, and this repo carries its own.

    Then verify from a throwaway clone rather than from the staging tree, so the check exercises what
    a stranger receives: clone the pushed repo over HTTPS into /tmp/sota-numerics-verify, run
    `claude plugin validate . --strict` there, and run BOTH test suites there. Confirm the remote's
    visibility is public and its default branch is main.

    Do not touch gsd-beads' .claude-plugin/marketplace.json in this plan — that edit is Plan 03's,
    sequenced strictly after both repos have passed this gate.
  </action>
  <verify>
    <automated>gh repo view davdittrich/sota-numerics --json visibility,defaultBranchRef,isPrivate -q '.visibility + " " + .defaultBranchRef.name + " " + (.isPrivate|tostring)' | grep -qi 'public main false'</automated>
    <automated>rm -rf /tmp/sota-numerics-verify &amp;&amp; git clone -q https://github.com/davdittrich/sota-numerics.git /tmp/sota-numerics-verify &amp;&amp; cd /tmp/sota-numerics-verify &amp;&amp; claude plugin validate . --strict</automated>
    <automated>cd /tmp/sota-numerics-verify &amp;&amp; bash tests/test-session-start.sh</automated>
    <automated>cd /tmp/sota-numerics-verify &amp;&amp; python3 -m unittest tests/test_check_alternatives.py</automated>
    <automated>cd /tmp/sota-numerics-verify &amp;&amp; git rev-list --count HEAD | grep -qx 1</automated>
  </verify>
  <acceptance_criteria>
    - `gh repo view davdittrich/sota-numerics` reports visibility public, isPrivate false, and defaultBranchRef.name `main`.
    - A fresh HTTPS clone into /tmp/sota-numerics-verify succeeds without credentials.
    - `claude plugin validate . --strict` in the fresh clone exits 0 (D-10's validate half).
    - `bash tests/test-session-start.sh` in the fresh clone exits 0 with all PASS lines.
    - `python3 -m unittest tests/test_check_alternatives.py` in the fresh clone exits 0 — the blocking gate's checker still behaves identically at a path unrelated to gsd-beads.
    - `git rev-list --count HEAD` in the fresh clone outputs `1` (D-03).
    - `git -C /home/dd/projects/gsd-beads status --porcelain -- .claude-plugin` produces no output.
  </acceptance_criteria>
  <done>`davdittrich/sota-numerics` is a public repo on `main` with exactly one commit, and a fresh clone passes `claude plugin validate . --strict` plus both of the plugin's test suites.</done>
</task>

</tasks>

## Alternatives Considered

- **`cp` to a scratch dir + fresh `git init` + `gh repo create --source=. --push`**: reproduces the
  subdirectory at current file state with no history machinery. `gh repo create --help` (gh 2.97.0,
  verified this session, 2026) documents `--source` as the existing-local-repo path;
  `https://code.claude.com/docs/en/plugins-reference` (2026) confirms a plugin repo needs only
  `.claude-plugin/plugin.json` at its root.
- **`git subtree split -P sota-numerics -b <branch>` then push that branch**: preserves the Phase 11
  commits that built this plugin, at the cost of a synthetic branch and a rewritten root.
  `git subtree --help` (git 2.55.0, verified this session, 2026).
- **`git filter-repo --subdirectory-filter sota-numerics` against a mirror clone**: highest-fidelity
  history extraction; this repo's own filter-repo precedent is
  `.planning/phases/07-hygiene-publication/07-02-PLAN.md` (2026), tool at
  `https://github.com/newren/git-filter-repo` (2026).

Decided by: simplicity/LOC. Performance ties across all three on a 22-file tree, so the next ranked
criterion decides — and locked decision D-03 ("fresh init, no history extraction") independently
rules out both history-preserving options, leaving the copy-and-init path as the shortest and the
mandated one.

## Artifacts this phase produces

Created by this plan:
- Public GitHub repository `davdittrich/sota-numerics`, branch `main`, one commit
- `README.md` at that repo's root (new file, D-09 structure, documents the blocking `plan:post` gate)
- `LICENSE` at that repo's root (new file, MIT, byte-identical to gsd-beads' LICENSE)
- `.gitignore` at that repo's root (new file, two lines — load-bearing here, the Python test creates `tests/__pycache__/`)
- `.github/workflows/ci.yml` at that repo's root (new file, discretion call — runs both test suites)
- Staging tree `/tmp/sota-numerics-extract/` and throwaway clone `/tmp/sota-numerics-verify/` (transient)

Modified by this plan (staged copies only, never the gsd-beads originals):
- `tests/test-session-start.sh` — `REPO_ROOT`, `SCRIPT`, `PLUGIN_DIR` re-anchored to the new repo root
- `hooks/capability-auto-install.sh` — two comment path refs re-anchored (no executable line changed)

Explicitly NOT modified: `tests/test_check_alternatives.py` and `tests/fixtures/**` (already
relocation-safe), `.claude-plugin/plugin.json`, `.gsd/capabilities/sota-numerics/**`.

Not touched by this plan: `.claude-plugin/marketplace.json` (Plan 03), the `sota-numerics/`
subdirectory in gsd-beads (Plan 04), and `.gsd/capabilities/sota-numerics/` at the gsd-beads root
(untouched by the entire phase, per D-04).

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local staging tree -> public GitHub | every tracked byte crosses into permanent public visibility on push |
| gsd-beads working tree -> staging tree | a misplaced `git init` would create an embedded repo inside a tree gsd-beads still tracks |
| operator -> irreversible action | repo creation and push are one-way; only a human may authorize |
| consuming project's git root -> gate script | the published `plan:post` gate executes a Python script resolved from the installer's own repo root |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Information Disclosure | `gh repo create --public` + push of the staging tree | high | mitigate | Task 2 audits `git ls-files` against a path allowlist, asserts no `__pycache__` path was staged, and `git grep`s for absolute home paths, private-key headers, and SECRET/TOKEN/API_KEY assignments before any push; Task 3's blocking checkpoint shows the operator the tracked-file list |
| T-12-07 | Tampering | gsd-beads working tree at `/home/dd/projects/gsd-beads` | medium | mitigate | Staging is pinned to `/tmp/sota-numerics-extract`, outside the gsd-beads tree; Task 1 asserts a clean `git status --porcelain` under the plugin path and that no `.git` exists inside the subdirectory |
| T-12-08 | Spoofing | the public repo name `davdittrich/sota-numerics` | medium | mitigate | Task 3's checkpoint requires the operator to confirm the exact name; Task 4 verifies with `gh repo view` rather than assuming |
| T-12-09 | Elevation of Privilege | the published `plan:post` gate command running `python3` on a path derived from `git rev-parse --show-toplevel` in the installer's project | medium | accept | Pre-existing, unchanged behavior shipped in Phase 11 and covered by `tests/test_check_alternatives.py` including its path-safety cases; this phase relocates the code without altering it, and Task 1's acceptance criteria assert byte-identity of the checker and its fixtures. Accepted because the script runs in the installer's own repo, executes no child process of its own, and the alternative (rewriting the resolution mechanism) is out of this distribution phase's scope |
| T-12-10 | Denial of Service | future installers of the marketplace entry | high | mitigate | This plan does not edit `marketplace.json`; the entry is written in Plan 03 only after this plan's fresh-clone `validate --strict` gate passes |

Supply chain: no npm/pip/cargo install occurs anywhere in this plan (the plugin is Python-stdlib
only), so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- `gh repo view davdittrich/sota-numerics` reports a public repo on `main`.
- A fresh clone passes `claude plugin validate . --strict`, the bash smoke test, and the Python unit test.
- The clone contains exactly one commit and no `__pycache__` path (D-03).
- `git -C /home/dd/projects/gsd-beads status --porcelain` shows no modification caused by this plan.
- D-10's round-trip half is deferred to Plan 03, the first point at which a marketplace entry pointing at this repo exists.
</verification>

<success_criteria>
- D-01 satisfied for `sota-numerics`: it has its own public GitHub repo.
- D-03 satisfied: one fresh commit, nothing imported.
- D-05 satisfied: the repo starts its own version sequence at plugin.json's 0.1.0.
- D-07 satisfied: no GitHub Release archive and no release workflow were created.
- D-09 satisfied: the README carries all seven mandated sections and does not bury the blocking gate.
- D-10 half-satisfied: `claude plugin validate . --strict` is clean on the pushed repo; the round trip lands in Plan 03.
- Both new repos now exist and are verified — Plan 03 can repoint the marketplace without pointing at anything unproven.
</success_criteria>

<output>
Create `.planning/phases/12-ship-ponytail-everywhere-and-sota-numerics-plugins-publicly/12-02-SUMMARY.md` when done.
Record in it: the approved repo name from Task 3, the pushed commit SHA, and whether the discretionary `.github/workflows/ci.yml` was kept or struck.
</output>
