---
phase: 09-beads-content-depth
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - .agents/skills/beads/commands/dep.md
  - .agents/skills/beads/commands/label.md
  - .agents/skills/beads/commands/comments.md
  - .agents/skills/beads/commands/search.md
  - .agents/skills/beads/commands/compact.md
  - .agents/skills/beads/commands/import.md
  - .agents/skills/beads/commands/stats.md
  - .agents/skills/beads/commands/blocked.md
  - .agents/skills/beads/SKILL.md
autonomous: true
requirements: [PUB-11]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the eight bd subcommands PUB-11 names has a shipped reference document an agent can load on demand"
    - "Every command document describes the subcommand as the installed binary actually exposes it, not from recall"
    - "Every adapted command document carries its upstream MIT attribution line, the same assertion plan 09-02 makes on resources/*.md (review finding)"
    - "SKILL.md routes to all eight command documents by resolvable relative path"
    - "The Resources index added in plan 09-02 survives untouched"
  artifacts:
    - path: ".agents/skills/beads/commands/dep.md"
      provides: "bd dep reference with the dependency-type table"
      contains: "bd dep"
      min_lines: 18
    - path: ".agents/skills/beads/commands/label.md"
      provides: "bd label reference and label-based filtering"
      min_lines: 15
    - path: ".agents/skills/beads/commands/comments.md"
      provides: "issue comments as the durable findings channel"
      min_lines: 15
    - path: ".agents/skills/beads/commands/search.md"
      provides: "bd search reference and when it beats listing"
      min_lines: 15
    - path: ".agents/skills/beads/commands/compact.md"
      provides: "bd compact reference and what it does not remove"
      min_lines: 15
    - path: ".agents/skills/beads/commands/import.md"
      provides: "bd import reference and id-collision behaviour"
      min_lines: 15
    - path: ".agents/skills/beads/commands/stats.md"
      provides: "bd stats reference and how to act on the numbers"
      min_lines: 15
    - path: ".agents/skills/beads/commands/blocked.md"
      provides: "bd blocked reference and the ship-gate connection"
      min_lines: 15
  key_links:
    - from: ".agents/skills/beads/SKILL.md"
      to: ".agents/skills/beads/commands/"
      via: "Deeper Topics index lists each command document by relative path"
      pattern: "commands/[a-z]+\\.md"
    - from: ".agents/skills/beads/commands/dep.md"
      to: ".agents/skills/beads/resources/DEPENDENCIES.md"
      via: "command reference points at the conceptual resource for depth"
      pattern: "DEPENDENCIES\\.md"
---

<objective>
Cover the remaining seven PUB-11 topics that map to `bd` subcommands with one reference document each, and route to them from SKILL.md.

Purpose: PUB-11's named command coverage — dependencies, labels, comments, search, `compact`, `import`, `stats`, `blocked`. Plan 09-02 covered the conceptual half; this is the command half.
Output: eight documents under `.agents/skills/beads/commands/`, plus a Commands subsection in SKILL.md's Deeper Topics index.

Scope note: PUB-11's named topic list is the inventory, not upstream's full command directory. Upstream now ships 29 command files including beads-project meta-features outside this requirement's text (09-RESEARCH.md Pitfall 1). D-04's "no curation, no cutting" applies to the named list, and all eight of its command topics are planned here.

These documents are skill-internal reference material, not registered slash commands. `.claude-plugin/plugin.json` declares only `skills`, and registering commands would need a plugin-root directory outside the release allowlist — which would force a `release.yml` edit that D-01 exists to avoid. The upstream frontmatter shape is kept so registration stays a one-line change later.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-beads-content-depth/09-CONTEXT.md
@.planning/phases/09-beads-content-depth/09-PATTERNS.md
@.agents/skills/beads/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: dep, label, comments, search</name>
  <precondition>The `bd` binary is on PATH and answers `bd --version` — each document is written against the installed surface, not from recall.</precondition>
  <files>.agents/skills/beads/commands/dep.md, .agents/skills/beads/commands/label.md, .agents/skills/beads/commands/comments.md, .agents/skills/beads/commands/search.md</files>
  <read_first>
    - Upstream `commands/dep.md`, `commands/label.md`, `commands/comments.md`, `commands/search.md` — the adaptation source and the frontmatter shape.
    - `.planning/phases/09-beads-content-depth/09-PATTERNS.md` §"commands/..." — the two-field frontmatter (`description`, optional `argument-hint`) plus body shape, shown for both the table-carrying and the minimal variants.
    - Live `bd dep --help`, `bd label --help`, `bd search --help`, and the installed binary's help for comments — confirm the real subcommand names and shapes before writing.
  </read_first>
  <action>
    Create four documents under `.agents/skills/beads/commands/`. Each opens with YAML frontmatter carrying a `description` line, plus `argument-hint` where the subcommand takes positional arguments. Immediately below the closing frontmatter delimiter comes the same one-line attribution blockquote plan 09-02 puts on every adapted `resources/*.md` — naming the upstream beads skill and its MIT license — so copyright hygiene is uniform across both trees rather than resources-only (addresses review concern: attribution asserted on resources but not commands). Then a body that states what the subcommand is for, shows two or three realistic invocations in fenced bash, and closes by routing to the binary's own help for the full option surface. Keep each to roughly 20-45 lines. If the installed binary exposes a subcommand under a different name than the upstream filename suggests, document the name the binary actually uses and say so in one line — never write an invocation that was not confirmed against help output.

    dep.md: adding and inspecting dependencies, the four dependency types with a one-line effect each, and a pointer to `resources/DEPENDENCIES.md` for the ready-queue mechanics.

    label.md: applying and removing labels and listing issues by label; note the gsd convention of labelling by phase so a phase's issues can be pulled as a set.

    comments.md: adding and reading issue comments; state that comments are where task findings and handoff notes belong, and that they travel with the issue across sessions and agents.

    search.md: full-text search over issues, combining it with status filters, and when searching beats listing — recovering an issue whose id is unknown, or checking whether work already exists before creating a duplicate.
  </action>
  <verify>
    <automated>
cd .agents/skills/beads/commands
for f in dep.md label.md comments.md search.md; do test -f "$f"; done
for f in dep.md label.md comments.md search.md; do test "$(grep -c . "$f")" -ge 15; done
for f in dep.md label.md comments.md search.md; do head -1 "$f" | grep -q '^---$'; done
for f in dep.md label.md comments.md search.md; do grep -q '^description:' "$f"; done
grep -qF 'bd dep' dep.md
grep -qF 'DEPENDENCIES.md' dep.md
test "$(grep -c . dep.md)" -ge 18
grep -qF 'bd label' label.md
grep -qE 'bd comment' comments.md
grep -qF 'bd search' search.md
for f in dep.md label.md comments.md search.md; do grep -qi 'MIT' "$f"; done
echo COMMANDS_A_OK
    </automated>
  </verify>
  <done>Four command documents exist with upstream-shaped frontmatter, an upstream MIT attribution blockquote, confirmed invocations, and a depth pointer from dep.md into the resources tree.</done>
</task>

<task type="auto">
  <name>Task 2: compact, import, stats, blocked</name>
  <files>.agents/skills/beads/commands/compact.md, .agents/skills/beads/commands/import.md, .agents/skills/beads/commands/stats.md, .agents/skills/beads/commands/blocked.md</files>
  <read_first>
    - Upstream `commands/compact.md`, `commands/import.md`, `commands/stats.md`, `commands/blocked.md`.
    - Live `bd compact --help`, `bd import --help`, `bd stats --help`, `bd blocked --help`.
    - `.gsd/capabilities/beads/capability.json` `gates[]` — the blocking condition blocked.md relates to.
  </read_first>
  <action>
    Create four more documents in the same shape as Task 1 — frontmatter, then the upstream MIT attribution blockquote, then the body — with the same rule about confirming every invocation against live help output.

    compact.md: what compaction does to accumulated issue history, when running it is worthwhile, and — stated explicitly — what it does not remove, so nobody reaches for it expecting a delete.

    import.md: bringing issues in from an exported file, what happens to ids that already exist, and inspecting the result before treating the import as done.

    stats.md: the project metrics the subcommand reports and how to act on them — a high blocked count routes to the blocked document, an empty in-progress set routes to the ready queue.

    blocked.md: listing blocked issues, reading the chain to the blocker that actually needs resolving, and the gsd framing that an open blocker is what makes `BEADS.md` report a non-zero blocking count and hold the ship gate closed.
  </action>
  <verify>
    <automated>
cd .agents/skills/beads/commands
for f in compact.md import.md stats.md blocked.md; do test -f "$f"; done
for f in compact.md import.md stats.md blocked.md; do test "$(grep -c . "$f")" -ge 15; done
for f in compact.md import.md stats.md blocked.md; do head -1 "$f" | grep -q '^---$'; done
for f in compact.md import.md stats.md blocked.md; do grep -q '^description:' "$f"; done
grep -qF 'bd compact' compact.md
grep -qF 'bd import' import.md
grep -qF 'bd stats' stats.md
grep -qF 'bd blocked' blocked.md
grep -qF 'blocking_open' blocked.md
for f in compact.md import.md stats.md blocked.md; do grep -qi 'MIT' "$f"; done
test "$(ls -1 *.md | wc -l)" -eq 8
for f in *.md; do grep -qi 'MIT' "$f"; done
echo COMMANDS_B_OK
    </automated>
  </verify>
  <done>All eight command documents exist, every one carrying upstream MIT attribution; blocked.md connects an open blocker to the gsd ship gate.</done>
</task>

<task type="auto">
  <name>Task 3: Route SKILL.md to the commands</name>
  <files>.agents/skills/beads/SKILL.md</files>
  <action>
    Add a `### Commands` subsection inside the existing `## Deeper Topics` section of `.agents/skills/beads/SKILL.md`, immediately after the `### Resources` subsection created in plan 09-02. Leave the frontmatter, every original section, and the Resources subsection byte-identical. List the eight documents as one bullet per file: the relative path `commands/NAME.md` followed by a one-line statement of what it covers. Keep the entry point short — a path and a clause per bullet, no examples inline (D-03).
  </action>
  <verify>
    <automated>
S=.agents/skills/beads/SKILL.md
grep -q '^## Deeper Topics' "$S"
grep -q '^### Resources' "$S"
grep -q '^### Commands' "$S"
grep -q '^name: beads' "$S"
grep -q '^## Rules' "$S"
CLINKS=$(grep -oE 'commands/[a-z]+\.md' "$S" | sort -u)
test "$(printf '%s\n' "$CLINKS" | wc -l)" -eq 8
MISSING=0; for f in $CLINKS; do test -f ".agents/skills/beads/$f" || MISSING=1; done; test "$MISSING" -eq 0
RLINKS=$(grep -oE 'resources/[A-Z_]+\.md' "$S" | sort -u)
test "$(printf '%s\n' "$RLINKS" | wc -l)" -eq 6
MISSING=0; for f in $RLINKS; do test -f ".agents/skills/beads/$f" || MISSING=1; done; test "$MISSING" -eq 0
echo SKILL_COMMANDS_OK
    </automated>
  </verify>
  <done>SKILL.md indexes all eight command documents and all six resources, every path resolving, with the original entry-point sections intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped command docs → agent behaviour | An agent reads these files and runs the invocations they show against a real project workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-11 | Tampering | `compact.md` / `import.md` showing state-mutating invocations | medium | mitigate | Both documents state what the operation does not remove and require an inspection step before the result is treated as final, so a reader cannot mistake either for a reversible no-op. |
| T-09-12 | Repudiation | Invocations written from recall rather than the installed binary | medium | mitigate | Every command document is written after reading that subcommand's live help; where the binary's name differs from upstream's filename, the document records the binary's name. Task preconditions assert `bd` is present. |
| T-09-06 | Repudiation | Frozen flag tables drifting from the binary | low | mitigate | Each document routes to the binary's own help for the full option surface instead of transcribing it. |
| T-09-10 | Repudiation | Adapted upstream text shipped without attribution | low | mitigate | Every command document carries an MIT attribution blockquote naming the upstream beads skill, checked automatically over the whole directory in Task 2 — the same control plan 09-02 applies to `resources/*.md`. |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No packages are installed by this plan — RESEARCH.md's Package Legitimacy Audit records "Not applicable". |
</threat_model>

<verification>
- Eight files exist under `.agents/skills/beads/commands/`, each with `description` frontmatter, an upstream MIT attribution line, and its subcommand token.
- SKILL.md indexes eight command paths and six resource paths, all resolving.
- SKILL.md's frontmatter and original sections are unchanged.
</verification>

<success_criteria>
- All thirteen PUB-11 topics are now covered across `resources/` and `commands/`.
- D-04's coverage of the named topic set is complete with no topic dropped.
- D-03's entry point remains short and routes to every added file.
</success_criteria>

<output>
Create `.planning/phases/09-beads-content-depth/09-03-SUMMARY.md` when done
</output>
