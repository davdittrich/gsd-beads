---
phase: 09-beads-content-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/beads/PRIME.md
  - hooks/session-start.sh
  - hooks/hooks.json
  - .gitignore
autonomous: true
requirements: [PUB-12]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Starting a session in a bd workspace that lacks .beads/PRIME.md materialises it from the shipped source (D-02)"
    - "bd prime (no --export) prints the gsd-tailored override instead of bd's generic default (PUB-12 SC3)"
    - "The copy happens before bd prime runs, in the same hook invocation (D-09)"
    - "A project directory with no .beads/ is left completely untouched by the hook"
    - "An existing .beads/PRIME.md is never overwritten by the self-heal"
    - ".beads/PRIME.md is invisible to git and absent from the release archive, while .agents/skills/beads/PRIME.md is present in it (D-08, D-01)"
    - "The override documents all six capability.json lifecycle steps — plan:pre through ship:pre — not the five the first draft assumed (review finding)"
    - "The hook resolves its own source path with CLAUDE_PLUGIN_ROOT unset, so a developer or a test script can run it directly without exporting anything (review finding)"
  artifacts:
    - path: ".agents/skills/beads/PRIME.md"
      provides: "gsd-core lifecycle override content that bd prime prints"
      contains: "execute:wave:post"
      min_lines: 35
    - path: "hooks/session-start.sh"
      provides: "copy-if-missing self-heal followed by bd prime --hook-json"
      contains: "CLAUDE_PLUGIN_ROOT"
      min_lines: 10
    - path: "hooks/hooks.json"
      provides: "SessionStart registration pointing at the self-heal wrapper"
      contains: "session-start.sh"
  key_links:
    - from: "hooks/hooks.json"
      to: "hooks/session-start.sh"
      via: "SessionStart command invokes the wrapper under ${CLAUDE_PLUGIN_ROOT}"
      pattern: "session-start\\.sh"
    - from: "hooks/session-start.sh"
      to: ".agents/skills/beads/PRIME.md"
      via: "copies the shipped source into .beads/PRIME.md when absent"
      pattern: "\\.agents/skills/beads/PRIME\\.md"
    - from: "hooks/session-start.sh"
      to: "bd prime"
      via: "wrapper ends by handing off to bd prime --hook-json"
      pattern: "bd prime --hook-json"
---

<objective>
Ship the gsd-tailored `bd prime` override end-to-end: author it once in the allowlisted skill tree, materialise it into `.beads/` whenever it is missing, and prove a plain `bd prime` prints it.

Purpose: PUB-12. Today `bd prime` prints beads' generic default in every gsd-beads install — an agent starting a session learns nothing about phase epics, the lifecycle sync points, or the ship gate.
Output: `.agents/skills/beads/PRIME.md` (shipped source, D-01), `hooks/session-start.sh` (self-heal + handoff, D-02/D-09), `hooks/hooks.json` (rewired), `.gitignore` (D-08).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-beads-content-depth/09-CONTEXT.md
@.planning/phases/09-beads-content-depth/09-PATTERNS.md
@.gsd/capabilities/beads/capability.json
@.gsd/capabilities/beads/skills/beads-recall/SKILL.md
@.gsd/capabilities/beads/skills/beads-sync/SKILL.md
@.gsd/capabilities/beads/skills/beads-status/SKILL.md
@hooks/hooks.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "bd prime prints gsd guidance" — one path only</name>
  <reversibility rating="reversible">A hook wrapper script plus one markdown file; undo is deleting both and restoring the one-line hooks.json command.</reversibility>
  <files>.agents/skills/beads/PRIME.md, hooks/session-start.sh, hooks/hooks.json</files>
  <read_first>
    - `.gsd/capabilities/beads/capability.json` — `steps[]` (**six** lifecycle points, verified at lines 57-140: `plan:pre`, `plan:post`, `execute:wave:pre`, `execute:wave:post`, `verify:post`, `ship:pre`) and `gates[]` (the ship gate's `blocking_open`/`diverged` condition). Quote these values, do not paraphrase from memory.
    - `.gsd/capabilities/beads/skills/beads-recall/SKILL.md` — what `plan:pre` recalls and how `BEADS-RECALL.md` reaches the planner.
    - `.gsd/capabilities/beads/skills/beads-sync/SKILL.md` — what `plan:post` does to a PLAN.md.
    - `.gsd/capabilities/beads/skills/beads-status/SKILL.md` — the branch behaviour behind `execute:wave:pre`, `execute:wave:post`, `verify:post`, `ship:pre`.
    - `hooks/hooks.json` — the single existing SessionStart entry this task rewires.
  </read_first>
  <action>
    Write `.agents/skills/beads/PRIME.md` as the shipped source of truth for the override (D-01: it lives inside the already-allowlisted `.agents/skills/` tree, so `release.yml` needs no edit). Keep it terse per D-05 — bullets and one table, no prose paragraphs, target 40-70 lines. Keep it gsd-integration-only per D-06: assume the reader already knows `bd ready`, `bd show`, `bd update --claim` from the base skill and never restate them. Sections, in order:
    (a) a one-line statement that this project runs gsd-core's plan/execute/verify/ship lifecycle on `bd`, and that bare `bd` usage lives in the `beads` skill;
    (b) Ownership — `bd` owns task state, `.planning/` owns intent (PLAN.md, SUMMARY.md, ROADMAP.md), and task status is never mirrored into markdown checklists;
    (c) Phase epics — one epic per phase, one issue per `&lt;task&gt;` in a PLAN.md, identity bound by the plan's beads id and never by title match, PLAN.md frontmatter carries `beads_epic`;
    (d) a Sync points table with one row per entry in `capability.json`'s `steps[]` — **six** rows, not five, with columns for the point name, the dispatched skill, and a one-line effect drawn from the three dispatch SKILL.md files. The six, in `steps[]` order: `plan:pre` (`beads-recall`, produces `BEADS-RECALL.md`, consumed by the planner through the `contributions[]` fragment), `plan:post` (`beads-sync`), `execute:wave:pre` (`beads-status`), `execute:wave:post` (`beads-status`), `verify:post` (`beads-status`), `ship:pre` (`beads-status`). D-05 requires the four points PUB-12 names; the other two are in the same `steps[]` array and omitting either would misdescribe the lifecycle. Addresses review concern: the earlier draft said "five points" and risked dropping `plan:pre`/`beads-recall` from the table;
    (e) Ship gate — the exact `gates[]` blocking condition and the config key that disables it, plus what an override records;
    (f) Failure mode — every step is `onError: skip`, so a `bd` failure degrades the lifecycle rather than failing a phase.

    Write `hooks/session-start.sh` as a POSIX-sh-compatible bash script: a shebang, a one-line header naming D-02 and D-09, `set -u`, then resolve the plugin root as `${CLAUDE_PLUGIN_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}` and join it to `.agents/skills/beads/PRIME.md` for the source path. The marketplace runner exports the variable; the `:-` default derives the same root from the script's own location, so the wrapper is runnable directly in local dev and by Task 2's matrix without exporting anything, and an unset variable never aborts under `set -u` (addresses review concern: no `CLAUDE_PLUGIN_ROOT` fallback outside the marketplace runner). The default is a command substitution, so it costs nothing when the variable is set. Copy that source to the literal relative path `.beads/PRIME.md` only when all three guards hold: `.beads` is an existing directory, `.beads/PRIME.md` does not already exist, and the source file exists. Send copy failures to `/dev/null` and continue — falling back to bd's default output is exactly the status quo, so no error path is warranted. End the script by handing off to `bd prime --hook-json` via `exec`, so the hook's stdout is bd's JSON envelope and nothing else. Both path expansions must be double-quoted; no `eval`, no path component derived from file content or hook arguments, and no `bd` invocation assembled as a shell string (T-09-01, T-09-02; mirrors the `confined()` discipline in `.gsd/capabilities/beads/scripts/sync.py`). Mark the file executable.

    Rewire `hooks/hooks.json`: replace the single SessionStart command with an invocation of the wrapper via `bash` and a double-quoted `${CLAUDE_PLUGIN_ROOT}` path. Keep the surrounding JSON shape byte-identical — one `SessionStart` entry, one `hooks[]` array, one `command`/`type` object, `matcher` unchanged. Chaining inside one process is what guarantees D-09's ordering; two sibling array entries would not, because matching hooks are not guaranteed to run in array order.

    Verified premise for the source path: `${CLAUDE_PLUGIN_ROOT}` is the documented plugin-root variable and is already used this way by gsd-core's own `hooks/hooks.json` and several other installed marketplace plugins. The marketplace install copies the whole cloned repository into the plugin cache (README "Installing via the marketplace flow"), so `${CLAUDE_PLUGIN_ROOT}/.agents/skills/beads/PRIME.md` resolves in a real install.
  </action>
  <verify>
    <automated>
REPO="$PWD"
chmod +x hooks/session-start.sh
test -x hooks/session-start.sh
grep -q 'session-start.sh' hooks/hooks.json
grep -q 'bd prime --hook-json' hooks/session-start.sh
python3 -c "import json;json.load(open('hooks/hooks.json'))"
test "$(grep -c . .agents/skills/beads/PRIME.md)" -ge 35
grep -qF 'plan:pre' .agents/skills/beads/PRIME.md
grep -qF 'plan:post' .agents/skills/beads/PRIME.md
grep -qF 'execute:wave:pre' .agents/skills/beads/PRIME.md
grep -qF 'execute:wave:post' .agents/skills/beads/PRIME.md
grep -qF 'verify:post' .agents/skills/beads/PRIME.md
grep -qF 'ship:pre' .agents/skills/beads/PRIME.md
grep -qF 'BEADS-RECALL.md' .agents/skills/beads/PRIME.md
rm -f .beads/PRIME.md
env -u CLAUDE_PLUGIN_ROOT bash hooks/session-start.sh >/dev/null
test -f .beads/PRIME.md
cmp .beads/PRIME.md .agents/skills/beads/PRIME.md
rm -f .beads/PRIME.md
CLAUDE_PLUGIN_ROOT="$REPO" bash hooks/session-start.sh >/dev/null
test -f .beads/PRIME.md
cmp .beads/PRIME.md .agents/skills/beads/PRIME.md
bd prime | grep -qF 'execute:wave:post'
test "$(bd prime | cksum)" != "$(bd prime --export | cksum)"
echo TRACER_OK
    </automated>
  </verify>
  <done>A session started in this repo with `.beads/PRIME.md` absent ends with the file present and byte-identical to the shipped source — with the plugin-root variable set or unset — and a plain `bd prime` prints gsd-core lifecycle content naming all six sync points that `bd prime --export` does not.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the self-heal guards and prove the shipping path</name>
  <files>.gitignore, hooks/session-start.sh</files>
  <action>
    Add `.beads/PRIME.md` to the repository root `.gitignore` under its own stanza with a one-line comment stating that the shipped source in `.agents/skills/beads/` is the sole tracked copy and the `.beads/` copy is regenerated by the SessionStart hook (D-08). Place it near the existing `.beads/config.yaml` / `.beads/metadata.json` entries, which established the same untracked-generated-artifact precedent in Phase 7.

    Then exercise the guards written in Task 1 against a scratch-directory matrix and repair `hooks/session-start.sh` if any of them does not hold: a directory with no `.beads/` must be left with no `.beads/` afterwards; a workspace whose `.beads/PRIME.md` already exists must keep its bytes untouched; `bd` must observe the file as already present at the moment it is invoked, which is what proves the ordering D-09 requires; and the wrapper must reach the same shipped source from a scratch cwd with the plugin-root variable removed from the environment, resolving it from the script's own location instead (addresses review concern: no fallback outside the marketplace runner). The matrix uses a stub `bd` on PATH so the guards are exercised without a real Dolt workspace — the stub reports whether the destination existed when it was called.

    Finally reproduce `.github/workflows/release.yml`'s archive step verbatim against the working tree and assert the new files ship while the runtime copy does not (D-01's premise that the allowlist needs no edit).
  </action>
  <verify>
    <automated>
REPO="$PWD"
git check-ignore -q .beads/PRIME.md
test -z "$(git status --porcelain -- .beads/PRIME.md)"
T=$(mktemp -d); mkdir -p "$T/ws/.beads" "$T/bare" "$T/bin"
printf '#!/bin/sh\ntest -f .beads/PRIME.md && echo PRIME_PRESENT_AT_BD_CALL\n' > "$T/bin/bd"
chmod +x "$T/bin/bd"
( cd "$T/ws" && PATH="$T/bin:$PATH" CLAUDE_PLUGIN_ROOT="$REPO" bash "$REPO/hooks/session-start.sh" ) | grep -q PRIME_PRESENT_AT_BD_CALL
cmp "$T/ws/.beads/PRIME.md" "$REPO/.agents/skills/beads/PRIME.md"
rm -f "$T/ws/.beads/PRIME.md"
( cd "$T/ws" && PATH="$T/bin:$PATH" env -u CLAUDE_PLUGIN_ROOT bash "$REPO/hooks/session-start.sh" ) | grep -q PRIME_PRESENT_AT_BD_CALL
cmp "$T/ws/.beads/PRIME.md" "$REPO/.agents/skills/beads/PRIME.md"
printf 'SENTINEL_LOCAL_EDIT\n' >> "$T/ws/.beads/PRIME.md"
( cd "$T/ws" && PATH="$T/bin:$PATH" CLAUDE_PLUGIN_ROOT="$REPO" bash "$REPO/hooks/session-start.sh" >/dev/null )
grep -q SENTINEL_LOCAL_EDIT "$T/ws/.beads/PRIME.md"
( cd "$T/bare" && PATH="$T/bin:$PATH" CLAUDE_PLUGIN_ROOT="$REPO" bash "$REPO/hooks/session-start.sh" >/dev/null )
test ! -e "$T/bare/.beads"
Z="$T/probe.zip"
zip -qr "$Z" .claude-plugin hooks .agents/skills README.md LICENSE
unzip -Z1 "$Z" > "$T/listing.txt"
grep -qx '.agents/skills/beads/PRIME.md' "$T/listing.txt"
grep -qx 'hooks/session-start.sh' "$T/listing.txt"
! grep -qE '^\.(beads|planning|claude)/' "$T/listing.txt"
test "$(cut -d/ -f1 "$T/listing.txt" | LC_ALL=C sort -u | tr '\n' ' ')" = ".agents .claude-plugin LICENSE README.md hooks "
rm -rf "$T"
echo GUARDS_OK
    </automated>
  </verify>
  <done>The runtime copy is gitignored and absent from a working-tree archive build; the hook is a no-op in a project without `.beads/`, never clobbers an existing override, always completes the copy before `bd` runs, and finds its source with the plugin-root variable exported or absent.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin cache → installer's working directory | The SessionStart hook, shipped by this repo, writes a file into whatever project the installer opens |
| `bd` binary → hook stdout | The wrapper's stdout is consumed by Claude Code as a hook JSON envelope |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | `hooks/session-start.sh` copy destination | medium | mitigate | Destination is the literal relative path `.beads/PRIME.md` under the session cwd; source is rooted at `${CLAUDE_PLUGIN_ROOT}`. No path component is derived from file content, hook arguments, or user input — same confinement property `sync.py`'s `confined()` enforces for T-01-02. |
| T-09-02 | Tampering | `hooks/session-start.sh` shell handling | low | mitigate | All expansions double-quoted, `set -u` enabled, no `eval`, and the `bd` handoff is a literal argv line rather than a string assembled from data (N4 / T-01-01). |
| T-09-03 | Tampering | Unsolicited writes into an unrelated project's tree | medium | mitigate | Copy runs only when `.beads/` already exists (the hook never creates it) and only when the destination is absent (never clobbers a user's edited override). Verified by the scratch matrix in Task 2. |
| T-09-04 | Information Disclosure | `.beads/PRIME.md` committed into a downstream repo | low | accept | Content is public plugin documentation with no secrets. This repo gitignores it (D-08); downstream dirty-tree noise is cosmetic and the file is regenerable. |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages in any ecosystem — RESEARCH.md's Package Legitimacy Audit records "Not applicable". No install task exists to gate. |
</threat_model>

<verification>
- `bd prime` in this repo prints the gsd override; `bd prime --export` prints something different.
- The override names all six `capability.json` sync points, `plan:pre` (and its `BEADS-RECALL.md` product) included.
- `hooks/hooks.json` parses as JSON and names the wrapper script.
- The self-heal is idempotent, non-clobbering, inert in a non-bd project, and works with `CLAUDE_PLUGIN_ROOT` unset.
- A working-tree archive build contains `.agents/skills/beads/PRIME.md` and `hooks/session-start.sh`, and contains no `.beads/`, `.planning/`, or `.claude/` entries.
</verification>

<success_criteria>
- PUB-12's override mechanism works end-to-end from shipped source to `bd prime` output.
- D-01, D-02, D-05, D-06, D-08, D-09 are each implemented and covered by an automated check.
- `release.yml` is unmodified, confirming D-01's no-allowlist-change premise.
</success_criteria>

<output>
Create `.planning/phases/09-beads-content-depth/09-01-SUMMARY.md` when done
</output>

---

## Source Coverage Audit (phase 09, all four artifact types)

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Shipped beads guidance matches upstream depth and is gsd-tailored | 09-01, 09-02, 09-03, 09-04 | COVERED | Depth = 09-02/09-03; tailoring = 09-01; shipping = 09-04 |
| REQ | PUB-11 | SKILL.md expanded toward upstream parity across 13 named topics | 09-02, 09-03 | COVERED | 6 resources + 8 commands + SKILL.md index |
| REQ | PUB-12 | gsd-tailored `.beads/PRIME.md` overriding `bd prime` | 09-01 | COVERED | Tracer slice |
| RESEARCH | — | Scope PUB-11 to its named topic list, not all 29 upstream commands (Pitfall 1) | 09-02, 09-03 | COVERED | File inventory fixed to the 13 named topics |
| RESEARCH | — | `.beads/PRIME.md` untracked; resolve git-tracking fork (Pitfall 2) | 09-01 | COVERED | Resolved by D-08; `.gitignore` task |
| RESEARCH | — | Cite 08-01/08-02 (not Phase 7) as the release-recut precedent (Pitfall 3) | 09-04 | COVERED | Context references 08-02-SUMMARY.md |
| RESEARCH | — | `--stealth`/`BEADS_DIR` has no upstream file; write from live `--help` (Pitfall 4) | 09-02 | COVERED | STEALTH_MODE.md sourced from CLI help |
| RESEARCH | — | Do not transcribe `bd --help` flag tables (upstream CLI_REFERENCE discipline) | 09-02, 09-03 | COVERED | Enforced by a negative check in both plans |
| RESEARCH | — | Path confinement + typed-argv discipline for any new script | 09-01 | COVERED | T-09-01/T-09-02 |
| RESEARCH | — | Release archive allowlist needs no edit | 09-01, 09-04 | COVERED | Proven locally in 09-01, at the tag in 09-04 |
| CONTEXT | D-01 | PRIME.md source inside `.agents/skills/beads/` | 09-01 | COVERED | Task 1 |
| CONTEXT | D-02 | Self-healing copy whenever missing | 09-01 | COVERED | Task 1 + Task 2 guards |
| CONTEXT | D-03 | `resources/` + `commands/` split, SKILL.md stays an entry point | 09-02, 09-03 | COVERED | |
| CONTEXT | D-04 | Full coverage of the named upstream topic set | 09-02, 09-03 | COVERED | 13/13 topics mapped to files |
| CONTEXT | D-05 | PRIME.md covers the lifecycle sync points, terse | 09-01 | COVERED | Task 1 |
| CONTEXT | D-06 | PRIME.md is gsd-integration-only | 09-01, 09-03 | COVERED | Non-duplication checked in 09-03 |
| CONTEXT | D-07 | Delete v1.1.0, then cut v1.1.1 | 09-04 | COVERED | Task 2 |
| CONTEXT | D-08 | `.beads/PRIME.md` gitignored | 09-01 | COVERED | Task 2 |
| CONTEXT | D-09 | Copy runs before `bd prime --hook-json` | 09-01 | COVERED | Task 1 wiring, Task 2 ordering probe |

No `⚠ MISSING` rows. CONTEXT.md `## Deferred Ideas` is empty, so nothing is excluded on that basis.

## Cross-AI Review Findings (09-REVIEWS.md, antigravity — all LOW, phase-level record)

| Finding | Plan | Status | Where |
|---|---|---|---|
| No `CLAUDE_PLUGIN_ROOT` fallback outside the marketplace runner | 09-01 | ADDRESSED | Task 1 action (`${CLAUDE_PLUGIN_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}`), asserted by an `env -u` run in both tasks' verify |
| Sync-point table said five points; `capability.json` defines six | 09-01 | ADDRESSED | Task 1 action (d) enumerates all six incl. `plan:pre`/`beads-recall`; Task 1 verify greps each of the six plus `BEADS-RECALL.md` |
| MIT attribution asserted on `resources/*.md` but not `commands/*.md` | 09-03 | ADDRESSED | 09-03 Tasks 1-2 action require the blockquote; both verify blocks assert it |

No finding was deferred or rejected. No HIGH/MEDIUM findings were raised; overall reviewer risk rating LOW.
