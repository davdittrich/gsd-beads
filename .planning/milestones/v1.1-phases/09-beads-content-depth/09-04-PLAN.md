---
phase: 09-beads-content-depth
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - .claude-plugin/plugin.json
autonomous: true
requirements: [PUB-11, PUB-12]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The public release archive contains PRIME.md, the six resources, the eight command documents, and the SessionStart wrapper"
    - "v1.1.0 no longer exists as a GitHub Release or a remote tag, and v1.1.1 has taken its place"
    - "The published archive's top level is exactly the five allowlisted entries — no .beads/, .planning/, or .claude/ content"
    - "The release asset was produced by the Release workflow run for the v1.1.1 tag, not uploaded from a workstation"
    - "A README-driven marketplace install of the plugin yields a bd prime that prints the gsd override (PUB-12 SC3, ROADMAP SC4)"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "plugin version 1.1.1"
      contains: "1.1.1"
  key_links:
    - from: ".claude-plugin/plugin.json"
      to: "the v1.1.1 git tag"
      via: "tag is cut from the commit that carries the bumped version"
      pattern: "\"version\": \"1\\.1\\.1\""
    - from: "the installed plugin copy"
      to: ".beads/PRIME.md"
      via: "the installed copy's SessionStart wrapper materialises the override"
      pattern: "session-start\\.sh"
---

<objective>
Publish the depth work as `v1.1.1` and retire the known-short `v1.1.0`, then prove a stranger following the README ends up with the gsd-tailored `bd prime`.

Purpose: ROADMAP Phase 9 SC4 plus the shipping half of PUB-11 and PUB-12. Everything the previous three plans built is invisible to anyone who installs from the public archive until this runs.
Output: `.claude-plugin/plugin.json` at `1.1.1`, the `v1.1.1` tag and release, `v1.1.0` deleted, and a recorded install round trip.

Process precedent is `08-01-SUMMARY.md` and `08-02-SUMMARY.md` — the throwaway-tag teardown and the real `v1.1.0` cut both happened there. Phase 7 is the precedent only for the general discipline of rehearsing one-way operations, not for this tag-deletion transcript (09-RESEARCH.md Pitfall 3).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-beads-content-depth/09-CONTEXT.md
@.planning/phases/08-readme-release-ship-gate/08-02-SUMMARY.md
@.github/workflows/release.yml
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump to 1.1.1 and prove the working tree ships everything</name>
  <precondition>HEAD is on the repository's default branch — the one `claude plugin marketplace add davdittrich/gsd-beads` clones — and all of plans 09-01 through 09-03 is committed there. If `git rev-parse --abbrev-ref HEAD` reports another branch, halt: a tag cut from a side branch publishes an archive the README's install flow never reaches.</precondition>
  <files>.claude-plugin/plugin.json</files>
  <action>
    Change the `version` field in `.claude-plugin/plugin.json` from `1.1.0` to `1.1.1` and leave every other byte of the file untouched — this is the same single-field edit Phase 8 made when it moved `0.1.0` to `1.1.0`.

    Run `claude plugin validate . --strict` and record the output. Read the result narrowly: `.claude-plugin/marketplace.json` is present, so that command validates the marketplace manifest and does not check skill frontmatter, and only `--strict` promotes field warnings to errors. Assert the skill's own frontmatter keys separately rather than treating a passing validate as coverage of them.

    Rebuild the allowlisted archive locally with the exact command from `.github/workflows/release.yml` and assert the full inventory this phase added is inside it — the override source, the wrapper script, six resource documents, eight command documents — and that the top level is still exactly the five allowlisted entries. This catches an allowlist regression before a tag is pushed, when it is still free to fix.

    Commit the bump and push to the default branch, then confirm HEAD matches the remote.
  </action>
  <verify>
    <automated>
grep -q '"version": "1.1.1"' .claude-plugin/plugin.json
python3 -c "import json;d=json.load(open('.claude-plugin/plugin.json'));assert d['version']=='1.1.1';assert d['name']=='beads';assert d['skills']==['./.agents/skills/beads']"
grep -q '^name: beads' .agents/skills/beads/SKILL.md
grep -q '^description:' .agents/skills/beads/SKILL.md
claude plugin validate . --strict
T=$(mktemp -d)
zip -qr "$T/probe.zip" .claude-plugin hooks .agents/skills README.md LICENSE
unzip -Z1 "$T/probe.zip" > "$T/l.txt"
grep -qx '.agents/skills/beads/PRIME.md' "$T/l.txt"
grep -qx '.agents/skills/beads/SKILL.md' "$T/l.txt"
grep -qx 'hooks/session-start.sh' "$T/l.txt"
test "$(grep -cE '^\.agents/skills/beads/resources/[A-Z_]+\.md$' "$T/l.txt")" -eq 6
test "$(grep -cE '^\.agents/skills/beads/commands/[a-z]+\.md$' "$T/l.txt")" -eq 8
! grep -qE '^\.(beads|planning|claude)/' "$T/l.txt"
test "$(cut -d/ -f1 "$T/l.txt" | LC_ALL=C sort -u | tr '\n' ' ')" = ".agents .claude-plugin LICENSE README.md hooks "
rm -rf "$T"
git fetch origin --quiet
test "$(git rev-parse HEAD)" = "$(git rev-parse origin/$(git rev-parse --abbrev-ref HEAD))"
echo BUMP_OK
    </automated>
  </verify>
  <done>Version is 1.1.1 on the pushed default branch, and a local archive build carries all sixteen files this phase added with the allowlist unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Retire v1.1.0 and cut v1.1.1</name>
  <reversibility rating="costly">GitHub does not restore a deleted release, but v1.1.1 is a content superset cut minutes later from the same workflow; CONTEXT.md D-07 records the developer choosing this teardown, so it is flagged rather than gated.</reversibility>
  <files>(no source files — release operations)</files>
  <action>
    Delete the existing `v1.1.0` GitHub Release together with its tag, then remove the stale local tag so the workstation does not re-push it (D-07). Do this before cutting the new tag, as D-07 specifies: the point is that no window exists in which a stranger can pick the known-short archive off the releases page while a better one is being built.

    Cut `v1.1.1` from the pushed commit, push the tag, find the Release workflow run for that tag, and wait for it to finish successfully. Then download the published asset and verify its contents against the same inventory Task 1 checked locally — this is the artifact a stranger actually receives, so a local build passing is not a substitute.

    Also verify provenance: compare the asset's creation timestamp against the workflow run's start and end timestamps. An asset created outside that window was uploaded by hand rather than produced by CI, which would mean the allowlist never gated it.

    Confirm afterwards that `v1.1.0` is absent from both the releases list and the remote tag list.
  </action>
  <verify>
    <automated>
set -o pipefail
gh release list --json tagName --jq '.[].tagName' > /tmp/rel.txt
grep -qx 'v1.1.1' /tmp/rel.txt
! grep -qx 'v1.1.0' /tmp/rel.txt
! git ls-remote --tags origin | grep -q 'refs/tags/v1\.1\.0$'
git ls-remote --tags origin | grep -q 'refs/tags/v1\.1\.1$'
RUN=$(gh run list --workflow Release --json databaseId,headBranch,conclusion -L 15 --jq '[.[]|select(.headBranch=="v1.1.1")][0].databaseId')
test -n "$RUN"
test "$(gh run view "$RUN" --json conclusion --jq .conclusion)" = "success"
T=$(mktemp -d)
gh release download v1.1.1 --pattern '*.zip' -O "$T/r.zip"
unzip -Z1 "$T/r.zip" > "$T/l.txt"
grep -qx '.agents/skills/beads/PRIME.md' "$T/l.txt"
grep -qx 'hooks/session-start.sh' "$T/l.txt"
test "$(grep -cE '^\.agents/skills/beads/resources/[A-Z_]+\.md$' "$T/l.txt")" -eq 6
test "$(grep -cE '^\.agents/skills/beads/commands/[a-z]+\.md$' "$T/l.txt")" -eq 8
! grep -qE '^\.(beads|planning|claude)/' "$T/l.txt"
test "$(cut -d/ -f1 "$T/l.txt" | LC_ALL=C sort -u | tr '\n' ' ')" = ".agents .claude-plugin LICENSE README.md hooks "
A=$(gh release view v1.1.1 --json assets --jq '.assets[0].createdAt')
S=$(gh run view "$RUN" --json createdAt --jq .createdAt)
E=$(gh run view "$RUN" --json updatedAt --jq .updatedAt)
test "$(printf '%s\n%s\n%s\n' "$S" "$A" "$E" | LC_ALL=C sort | tr '\n' ' ')" = "$S $A $E "
rm -rf "$T"
echo RELEASE_OK
    </automated>
  </verify>
  <done>`v1.1.1` is the only release, its asset came from the CI run for that tag, its contents match the allowlist exactly, and `v1.1.0` is gone from the releases page and the remote.</done>
</task>

<task type="auto">
  <name>Task 3: Install round trip from the README and prove the override in the installed copy</name>
  <files>(no source files — install verification)</files>
  <read_first>
    - `.planning/phases/08-readme-release-ship-gate/08-02-SUMMARY.md` §"Gate A"/"Gate B" — the pre-state capture, add/install/uninstall sequence, and restore steps proven on this machine.
    - `README.md` install section — the exact two commands a stranger runs.
  </read_first>
  <action>
    Gate A: clone the repository fresh into a scratch directory, check out `v1.1.1`, and run `claude plugin validate . --strict` there. Confirm the clone's `plugin.json` reports 1.1.1 and that the resources and commands directories arrived with their full file counts — a fresh clone is where an untracked-file mistake shows up, since a working tree can pass on files git never recorded.

    Gate B: capture the current marketplace and plugin state first so the machine can be returned to it. Then run the README's own two install commands, locate the installed plugin copy under the Claude plugin directory, and prove the override reaches `bd prime` from that installed copy rather than from this working tree: remove this repository's `.beads/PRIME.md`, run the installed copy's SessionStart wrapper with the plugin root pointing at the installed copy, and confirm the file is restored from the installed copy's source and that a plain `bd prime` prints the gsd lifecycle content. Finally uninstall and restore the captured pre-state, and record the full transcript in the SUMMARY.
  </action>
  <verify>
    <automated>
set -o pipefail
G=$(mktemp -d)
git clone --quiet https://github.com/davdittrich/gsd-beads.git "$G/clone"
git -C "$G/clone" checkout --quiet v1.1.1
grep -q '"version": "1.1.1"' "$G/clone/.claude-plugin/plugin.json"
test "$(ls -1 "$G/clone"/.agents/skills/beads/resources/*.md | wc -l)" -eq 6
test "$(ls -1 "$G/clone"/.agents/skills/beads/commands/*.md | wc -l)" -eq 8
test -f "$G/clone/.agents/skills/beads/PRIME.md"
test -f "$G/clone/hooks/session-start.sh"
( cd "$G/clone" && claude plugin validate . --strict )
claude plugin marketplace add davdittrich/gsd-beads
claude plugin install beads@gsd-beads -y
INST=$(find "$HOME/.claude/plugins" -type f -path '*/beads/.agents/skills/beads/PRIME.md' 2>/dev/null | head -1)
test -n "$INST"
ROOT=${INST%/.agents/skills/beads/PRIME.md}
test -f "$ROOT/hooks/session-start.sh"
rm -f .beads/PRIME.md
CLAUDE_PLUGIN_ROOT="$ROOT" bash "$ROOT/hooks/session-start.sh" >/dev/null
test -f .beads/PRIME.md
cmp .beads/PRIME.md "$ROOT/.agents/skills/beads/PRIME.md"
bd prime | grep -qF 'execute:wave:post'
test "$(bd prime | cksum)" != "$(bd prime --export | cksum)"
claude plugin uninstall beads -y
rm -rf "$G"
echo ROUNDTRIP_OK
    </automated>
  </verify>
  <done>A fresh clone at the tag validates and carries all sixteen files; the README install flow produces an installed copy whose wrapper restores the override and whose content is what `bd prime` prints; the machine's marketplace and plugin state is restored.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository working tree → public release archive | The CI zip step decides what leaves this machine's control and reaches strangers |
| GitHub release asset → installer's machine | A stranger executes shipped hooks from the archive or the marketplace clone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Information Disclosure | Release archive carrying non-allowlisted paths | high | mitigate | The published archive's top-level entry set is asserted equal to the five allowlisted entries, and the listing is asserted to contain no `.beads/`, `.planning/`, or `.claude/` entries — checked on the downloaded asset in Task 2, not only on a local build. |
| T-09-08 | Tampering | Release asset uploaded from a workstation instead of built by CI | medium | mitigate | The asset's creation timestamp must fall inside the Release workflow run's start/end window; an out-of-window asset means the allowlist never gated it (Phase 8 provenance pattern, made automatic here). |
| T-09-09 | Denial of Service | Window with no public release between the teardown and the recut | low | accept | The recut follows immediately from the already-pushed commit; D-07 records the developer accepting this in exchange for removing a known-short archive from the releases page. |
| T-09-13 | Tampering | Tag cut from a branch the README install flow never clones | medium | mitigate | Task 1's precondition halts unless HEAD is the default branch that `claude plugin marketplace add` clones, and Task 3 verifies the install round trip from that clone. |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No packages are installed by this plan — RESEARCH.md's Package Legitimacy Audit records "Not applicable". The `claude plugin install` step installs this repository's own plugin from its own marketplace entry. |
</threat_model>

<verification>
- `gh release list` shows `v1.1.1` and no `v1.1.0`; the remote has no `v1.1.0` tag.
- The downloaded `v1.1.1` archive carries PRIME.md, the wrapper, six resources, and eight commands, with an allowlist-exact top level.
- The asset timestamp falls inside its workflow run's window.
- A fresh clone at the tag validates and a README-driven install yields a `bd prime` that prints the override.
</verification>

<success_criteria>
- ROADMAP Phase 9 SC4 satisfied: `v1.1.1` is the public archive a stranger installs from the README, and `v1.1.0` is gone.
- PUB-12 SC3 satisfied from an installed copy of the plugin, not from the development tree.
- D-07 executed in the order it specifies, with the reversibility recorded rather than re-asked.
</success_criteria>

<output>
Create `.planning/phases/09-beads-content-depth/09-04-SUMMARY.md` when done
</output>
