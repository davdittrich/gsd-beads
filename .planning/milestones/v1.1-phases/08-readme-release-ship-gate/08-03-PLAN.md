---
phase: 08-readme-release-ship-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
gap_closure: true
gap_ids: [G-08-1]
requirements: [PUB-07]

estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A reader who knows neither gsd-core nor beads learns from README.md WHY bd is used instead of gsd-core's built-in .planning/-markdown task tracking, not only HOW it is wired."
    - "A reader can see, from README.md alone, how gsd-core's own /gsd-plan-phase -> /gsd-execute-phase -> /gsd-verify-work commands interact with bd state, not just standalone bd CLI usage."
  artifacts:
    - "README.md (modified — What it does section gains a comparison; Example workflow section gains the gsd lifecycle interaction)"
  key_links:
    - "README comparison content traceable to docs/prd-beads-capability.md sections 3.1-3.2 and .planning/PROJECT.md Core Value"
    - "README lifecycle description traceable to .gsd/capabilities/beads/skills/beads-sync/SKILL.md and beads-status/SKILL.md (real mechanism, no invented behavior)"
---

<objective>
Close gap G-08-1 from 08-UAT.md: README.md states the mechanism of gsd-beads but never the
benefit versus gsd-core's built-in `.planning/`-markdown task tracking, and its worked example
shows only bare `bd` CLI commands with no sign of gsd-core's own plan/execute/verify pipeline.

Purpose: a cold stranger (D-01) must be able to *evaluate* gsd-beads, which requires knowing what
problem it solves and how the two systems interoperate.
Output: modified README.md. Documentation content only.

Scope guard: do NOT re-cut a release, touch git tags, GitHub Releases, `.claude-plugin/plugin.json`
version, or `.github/workflows/release.yml`. Fix README.md content and commit. Whether v1.1.0 gets
re-cut is a separate decision the orchestrator raises with the user afterwards.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-readme-release-ship-gate/08-UAT.md
@.planning/phases/08-readme-release-ship-gate/08-CONTEXT.md
</context>

<constraints>
- **D-04 (locked):** README section order is Title/one-liner -> What it does -> Requirements ->
  Install (with the Example workflow subsection) -> Uninstall -> Caveats -> License -> gsd-core
  link. Both edits land *inside* existing sections. Do not add a top-level section, do not reorder.
- **D-02 (locked):** every command shown in the README is real and verbatim-executable, none
  paraphrased. Task 1 adds explanatory prose and a table with no new commands, so no execution
  transcript is required for it. Task 2 names real `/gsd-*` commands and real lifecycle behavior
  read out of the shipped skill files — do not invent a fake terminal transcript, and do not claim
  any capability gsd-beads does not have.
- **D-05 (locked):** the worked example stays short and points to `AGENTS.md` for the full command
  reference; that pointer line stays.
- **D-01 (locked):** audience is a stranger who knows neither gsd-core nor beads.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add the beads-vs-built-in-tracking value proposition to "What it does"</name>
  <files>README.md</files>
  <read_first>
    - README.md (whole file — 78 lines; note the "What it does" section spans lines ~5-18 and ends
      with the mechanism paragraph beginning "`gsd-beads` is a gsd-core capability")
    - docs/prd-beads-capability.md sections 3.1 and 3.2 (the six-row comparison table and the
      drift-cost paragraph) — this is the source content, already written
    - .planning/PROJECT.md "## Core Value" section (the short formulation of the same point)
    - .planning/phases/08-readme-release-ship-gate/08-CONTEXT.md (locked decisions D-01 to D-05)
  </read_first>
  <action>
    Insert new content into the existing "## What it does" section of README.md, immediately AFTER
    the existing mechanism paragraph that begins "`gsd-beads` is a gsd-core capability" and BEFORE
    the "## Requirements" heading. Do not modify the two paragraphs already there and do not add a
    new top-level heading (per D-04 the section order is locked).

    The inserted content has two parts, in this order:

    (a) One short lead-in paragraph (2-4 sentences) answering "why not just use gsd-core's built-in
    tracking?", derived from PRD section 3.2 and PROJECT.md's Core Value. Points it must make:
    gsd-core's `.planning/` markdown is a good fit for plan *content* (what a phase is for, how a
    task should be approached, what done means) but a poor fit for task *state*; without a bridge a
    developer who uses both trackers maintains two hand-written representations of the same work,
    re-typing every planned task as an issue and closing every finished task twice, and the two
    drift silently because nothing compares them. State the resulting value: `bd` becomes the single
    source of truth for task state, so no duplicated task-state bookkeeping survives in
    `.planning/`.

    (b) A condensed markdown comparison table adapted from PRD section 3.1, with the columns
    `Need` / `.planning/ markdown` / `beads`, keeping at least these axes with these exact
    left-column phrasings so they stay traceable to the PRD:
      - `Query "what can I work on now?"` -> read files, reason / `bd ready`
      - `Dependencies and blocking` -> prose ordering / first-class, enforced
      - `Status across phases` -> per-phase files / one query
      - `Survives milestone archival` -> archived away / persists
      - `Visible outside one project` -> no / yes
      - `Machine-updatable without rewriting prose` -> no / yes

    Keep the framing non-triumphalist: `.planning/` markdown is not being called bad, it is being
    described as the wrong store for state. Wrap prose lines at roughly the same width as the rest
    of the file (~95 columns). Add no commands other than the `bd ready` cell already in the table
    (a real command, present in the existing Example workflow section).
  </action>
  <verify>
    <automated>cd /home/dd/Gemini/gsd-beads &amp;&amp; sed -n '/^## What it does/,/^## Requirements/p' README.md &gt; /tmp/wid.md &amp;&amp; test "$(grep -c -e 'what can I work on now' -e 'Dependencies and blocking' -e 'Status across phases' -e 'Survives milestone archival' -e 'Visible outside one project' -e 'Machine-updatable' /tmp/wid.md)" -ge 5 &amp;&amp; grep -qE '^\| *Need *\|' /tmp/wid.md &amp;&amp; grep -q '\.planning/' /tmp/wid.md &amp;&amp; grep -qi 'drift' /tmp/wid.md &amp;&amp; test "$(grep -o '^## .*' README.md | tr '\n' '|')" = "## What it does|## Requirements|## Install|## Uninstall|## Caveats|## License|## gsd-core|" &amp;&amp; echo 'task1 ok'</automated>
  </verify>
  <acceptance_criteria>
    - The "## What it does" section of README.md contains a markdown table with a `Need` column and
      at least 5 of the 6 PRD 3.1 axes (query surface, dependencies, cross-phase status, archival
      survival, external visibility, machine-updatability).
    - That section contains prose naming the silent-drift cost of maintaining two representations.
    - The D-04 section order is unchanged (verified by the ordering assertion above).
    - No new command appears in the section beyond `bd ready`.
  </acceptance_criteria>
  <done>A reader of "What it does" learns both the mechanism (already there) and the problem it solves versus gsd-core's built-in tracking.</done>
</task>

<task type="auto">
  <name>Task 2: Show the gsd-core lifecycle driving beads state in the Example workflow</name>
  <files>README.md</files>
  <read_first>
    - README.md "### Example workflow" subsection (lines ~33-44) — the bare
      `bd ready` / `bd update --claim` / `bd close` block plus the AGENTS.md pointer
    - .gsd/capabilities/beads/skills/beads-sync/SKILL.md — what `plan:post` / `ship:post` sync
      actually does (phase epic, one issue per plan-task block, `beads_epic` frontmatter key,
      per-task `<beads-id>` element, identity bound by `<beads-id>` not title)
    - .gsd/capabilities/beads/skills/beads-status/SKILL.md — the four lifecycle points
      (`execute:wave:pre`, `execute:wave:post`, `verify:post`, `ship:pre`) and what each does
    - .gsd/capabilities/beads/capability.json — the registered `steps[]`, `contributions[]` and the
      two `ship:pre` gates over `BEADS.md` frontmatter (`blocking_open`, `diverged`)
    - .planning/phases/06-runtime-integration/*-SUMMARY.md — check whether a real end-to-end
      transcript of the lifecycle driving bd was captured; if one exists, prefer quoting it
      verbatim (D-02), if not, describe the mechanism from the skill files instead
  </read_first>
  <action>
    Rewrite the "### Example workflow" subsection so it shows the gsd-core lifecycle and beads
    together, not standalone `bd` usage. Keep it short (D-05) and keep the closing
    "See `AGENTS.md` in this repo for the full command reference." pointer line unchanged.

    New structure for the subsection:

    (1) One sentence noting the capability is off until `beads.enabled` is set true in the
    project's `.planning/config.json` (real gate — every skill checks it in its Step 1 config gate
    and prints a disabled message otherwise).

    (2) A short ordered walkthrough tying each gsd-core command to the beads state it produces or
    consumes. Use only behavior that the skill files and capability.json actually declare:
      - `/gsd-plan-phase` — at `plan:pre` beads-recall queries existing issues so already-ticketed
        work is not planned twice; at `plan:post` beads-sync creates one phase epic plus one beads
        issue per plan-task block in the PLAN.md and writes the ids back into the plan (a
        `beads_epic` frontmatter key and a `<beads-id>` element per task).
      - `/gsd-execute-phase` — at `execute:wave:pre` beads-status puts the wave's live issue state
        into the orchestrator's context; at `execute:wave:post` it closes the issues of the tasks
        the wave completed.
      - `/gsd-verify-work` — at `verify:post` beads-status refreshes the projected `BEADS.md` state
        from `bd`.
      - ship — at `ship:pre` two blocking gates read `BEADS.md` frontmatter and refuse the ship
        while `blocking_open` or `diverged` is non-zero.

    (3) Keep the three bare `bd` commands, but reframe them explicitly as the manual escape hatch
    for inspecting or driving the same issues by hand between lifecycle steps — do not delete them.

    If (and only if) Phase 6's SUMMARY.md contains a real captured transcript of this flow, quote it
    verbatim per D-02. Otherwise write it as prose plus the existing `bd` code block; do not
    fabricate terminal output, and do not show a `/gsd-*` command inside a bash code fence as though
    it were a shell command — these are Claude Code slash commands.
  </action>
  <verify>
    <automated>cd /home/dd/Gemini/gsd-beads &amp;&amp; sed -n '/^### Example workflow/,/^## Uninstall/p' README.md &gt; /tmp/exw.md &amp;&amp; grep -q '/gsd-plan-phase' /tmp/exw.md &amp;&amp; grep -q '/gsd-execute-phase' /tmp/exw.md &amp;&amp; grep -q '/gsd-verify-work' /tmp/exw.md &amp;&amp; grep -q 'beads\.enabled' /tmp/exw.md &amp;&amp; grep -q 'BEADS\.md' /tmp/exw.md &amp;&amp; grep -q 'blocking_open' /tmp/exw.md &amp;&amp; grep -q 'diverged' /tmp/exw.md &amp;&amp; grep -qi 'epic' /tmp/exw.md &amp;&amp; grep -q 'bd ready' /tmp/exw.md &amp;&amp; grep -q 'AGENTS\.md' /tmp/exw.md &amp;&amp; echo 'task2 ok'</automated>
  </verify>
  <acceptance_criteria>
    - The Example workflow subsection names all three of `/gsd-plan-phase`, `/gsd-execute-phase`,
      `/gsd-verify-work` and, for each, the beads state it produces or consumes.
    - It names the `beads.enabled` config gate, the phase epic + per-task issue creation, `BEADS.md`,
      and the `blocking_open` / `diverged` ship-gate fields.
    - The three bare `bd` commands and the `AGENTS.md` pointer both survive.
    - Every `/gsd-*` and `bd` reference corresponds to something the shipped skill files or
      capability.json actually declare (checked against the files listed in read_first).
  </acceptance_criteria>
  <done>A stranger can see from the README how gsd-core's plan/execute/verify pipeline and beads interoperate, not just how to run `bd` by hand.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public README readers | README content is the only input crossing; no code, no execution path changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information disclosure | README.md prose | low | accept | Content is drawn from already-public docs (PRD, PROJECT.md Core Value, shipped skill files); no secrets, paths, or credentials introduced. |
| T-08-02 | Tampering (doc/reality drift) | README claims vs shipped behavior | medium | mitigate | Task 2 requires every claim be read out of `.gsd/capabilities/beads/skills/*/SKILL.md` and `capability.json` before writing; verify greps assert the named mechanisms are present, and D-02 forbids fabricated transcripts. |

No package-manager installs in this plan, so no package-legitimacy gate applies.
</threat_model>

<verification>
Both task verify blocks pass, plus:

```bash
cd /home/dd/Gemini/gsd-beads
git diff --name-only            # must list README.md and nothing else
claude plugin validate . --strict
```

`git diff --name-only` listing any file other than `README.md` means the scope guard was violated
(no tags, no release workflow, no plugin.json version bump).
</verification>

<success_criteria>
- G-08-1's two `missing` items are both present in README.md.
- README.md is the only modified file.
- D-04 section order intact; D-02 (no invented commands/output) and D-05 (short example, AGENTS.md
  pointer retained) honored.
</success_criteria>

<output>
Create `.planning/phases/08-readme-release-ship-gate/08-03-SUMMARY.md` when done. Record in it
whether Phase 6 had a reusable transcript for Task 2 or whether the lifecycle was described from
the skill files, and note explicitly that no release artifact was re-cut.
</output>
