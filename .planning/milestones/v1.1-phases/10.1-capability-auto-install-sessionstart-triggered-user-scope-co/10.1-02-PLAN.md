---
phase: 10.1-capability-auto-install
plan: 02
type: execute
wave: 2
depends_on: ["10.1-01"]
files_modified:
  - ponytail-everywhere/.gsd/capabilities/ponytail/capability.json
  - ponytail-everywhere/.gsd/capabilities/ponytail/NOTES.md
  - ponytail-everywhere/.gsd/capabilities/ponytail/fragments/planner-ladder.md
  - ponytail-everywhere/.gsd/capabilities/ponytail/fragments/executor-ladder.md
  - ponytail-everywhere/.gsd/capabilities/ponytail/fragments/verifier-ladder.md
  - ponytail-everywhere/hooks/capability-auto-install.sh
  - ponytail-everywhere/hooks/session-start.sh
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
autonomous: false
requirements: [CAP-01, CAP-02, CAP-06, CAP-07]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`ponytail-everywhere` ships its own `.gsd/capabilities/ponytail/` bundle, so a user who installs only that plugin has something for the auto-install to install (RESEARCH gap #1, CAP-06)."
    - "A session start in the ponytail plugin auto-installs the `ponytail` capability with one notice line, and is silent on subsequent unchanged sessions (D-01/D-02 applied to the second capability)."
    - "The two vendored `capability-auto-install.sh` copies are byte-identical, and CI fails if they ever diverge (D-06, CAP-07)."
    - "The GitHub Release zip carries `.gsd/capabilities/` so the release-artifact install path and the marketplace-cache install path agree on bundle presence (RESEARCH Summary point 2)."
  artifacts:
    - ponytail-everywhere/.gsd/capabilities/ponytail/ (5 files, copied verbatim from the repo-root bundle)
    - ponytail-everywhere/hooks/capability-auto-install.sh (byte-identical vendored copy)
    - .github/workflows/ci.yml
    - "ponytail-everywhere/hooks/session-start.sh (modified: calls auto-install early)"
    - ".github/workflows/release.yml (modified: `.gsd` added to the zip allowlist)"
  key_links:
    - "ponytail-everywhere/hooks/session-start.sh invokes the vendored script with the literal id `ponytail`, before its advisory-banner logic and before its `ponytail.enabled` early exit (an early `exit 0` must not skip the D-02 check)."
    - "CI diffs hooks/capability-auto-install.sh against ponytail-everywhere/hooks/capability-auto-install.sh and runs tests/test-capability-auto-install.sh on every push/PR."
    - "release.yml's zip allowlist and the marketplace clone path must both carry .gsd/capabilities/<id>/, otherwise the auto-install finds no bundle and exits silently."
---

<objective>
Extend the Plan 01 mechanism to the second capability: vendor the `ponytail` bundle into the
`ponytail-everywhere` plugin (it ships none today), vendor a byte-identical copy of the
auto-install script there, wire it into that plugin's SessionStart hook, and add the CI
guards that keep the two copies identical and the release artifact complete.

Purpose: the phase goal requires the mechanism to cover both existing capabilities and to be
reusable verbatim by Phase 11 (`sota-numerics`).

Output: a shipping `ponytail` bundle inside `ponytail-everywhere/`, the second vendored
script copy, and `.github/workflows/ci.yml`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-capability-auto-install-sessionstart-triggered-user-scope-co/10.1-CONTEXT.md
@.planning/phases/10.1-capability-auto-install-sessionstart-triggered-user-scope-co/10.1-RESEARCH.md
@.planning/phases/10.1-capability-auto-install-sessionstart-triggered-user-scope-co/10.1-01-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols/paths created by this plan (not pre-existing — exclude from drift verification):

- `ponytail-everywhere/.gsd/capabilities/ponytail/` and its five files (verbatim copies)
- `ponytail-everywhere/hooks/capability-auto-install.sh` (byte-identical vendored copy of the Plan 01 original)
- `.github/workflows/ci.yml` (new workflow; job name `checks`)
- new step in `.github/workflows/release.yml`'s existing zip allowlist: the `.gsd` path entry

<tasks>

<task type="auto">
  <name>Task 1: Vendor the ponytail bundle and the auto-install script into `ponytail-everywhere`</name>

  <files>ponytail-everywhere/.gsd/capabilities/ponytail/capability.json, ponytail-everywhere/.gsd/capabilities/ponytail/NOTES.md, ponytail-everywhere/.gsd/capabilities/ponytail/fragments/planner-ladder.md, ponytail-everywhere/.gsd/capabilities/ponytail/fragments/executor-ladder.md, ponytail-everywhere/.gsd/capabilities/ponytail/fragments/verifier-ladder.md, ponytail-everywhere/hooks/capability-auto-install.sh, ponytail-everywhere/hooks/session-start.sh</files>

  <read_first>
    - `ponytail-everywhere/hooks/session-start.sh` — the file being modified; note it sets `PLUGIN_ROOT` on line 4, optionally sources `hooks/gsd-tools.sh`, and can `exit 0` early when `ponytail.enabled` is not `true`.
    - `hooks/capability-auto-install.sh` — the authored original from Plan 01; this task copies it verbatim.
    - `.gsd/capabilities/ponytail/capability.json` and the four sibling files under `.gsd/capabilities/ponytail/` — the copy source (repo-root dogfood bundle).
    - `ponytail-everywhere/hooks/hooks.json` — confirms SessionStart AND three SubagentStart matchers all invoke `session-start.sh`; needs no edit.
  </read_first>

  <action>
    Copy the repo-root bundle verbatim: reproduce all five files under
    `.gsd/capabilities/ponytail/` (`capability.json`, `NOTES.md`,
    `fragments/planner-ladder.md`, `fragments/executor-ladder.md`,
    `fragments/verifier-ladder.md`) into `ponytail-everywhere/.gsd/capabilities/ponytail/`,
    preserving the `fragments/` subdirectory layout. This is a byte-for-byte copy, not a
    rewrite or a schema translation — the two bundles must stay identical.

    Copy `hooks/capability-auto-install.sh` verbatim to
    `ponytail-everywhere/hooks/capability-auto-install.sh` and preserve its executable bit.
    Do not edit the copy in any way — the capability id is supplied as `$1` by the caller, so
    the two copies differ in zero bytes (D-05, and the CI check in Task 2 enforces it).

    Modify `ponytail-everywhere/hooks/session-start.sh`: insert
    `bash "$PLUGIN_ROOT/hooks/capability-auto-install.sh" ponytail || true` immediately after
    the `PLUGIN_ROOT=` assignment and BEFORE the `if [ -f "$PLUGIN_ROOT/hooks/gsd-tools.sh" ]`
    block. Placing it there is load-bearing: the existing `ponytail.enabled != true` branch
    exits 0, and D-02 requires the capability check to run on every session regardless of
    whether the advisory banner is enabled. Change nothing else in the file — the banner
    logic, the role handling, and the exit code stay as-is.

    Do not add a session-scoped guard for the SubagentStart re-entry. `hooks.json` invokes
    `session-start.sh` again for each of the three subagent roles, but the auto-install fast
    path on an unchanged bundle is a few `find`/hash calls with no `node` spawn, so re-running
    it costs less than the state-tracking it would take to avoid. Record this as a deliberate
    choice in the SUMMARY rather than building the guard speculatively.
  </action>

  <verify>
    <automated>diff -r .gsd/capabilities/ponytail ponytail-everywhere/.gsd/capabilities/ponytail && cmp hooks/capability-auto-install.sh ponytail-everywhere/hooks/capability-auto-install.sh && bash -n ponytail-everywhere/hooks/session-start.sh && bash ponytail-everywhere/tests/test-session-start.sh</automated>
  </verify>

  <acceptance_criteria>
    - `diff -r .gsd/capabilities/ponytail ponytail-everywhere/.gsd/capabilities/ponytail` exits 0 with no output.
    - `cmp hooks/capability-auto-install.sh ponytail-everywhere/hooks/capability-auto-install.sh` exits 0.
    - `test -x ponytail-everywhere/hooks/capability-auto-install.sh` exits 0.
    - `grep -c 'capability-auto-install.sh' ponytail-everywhere/hooks/session-start.sh` outputs `1`.
    - `grep -n 'capability-auto-install.sh\|hooks/gsd-tools.sh' ponytail-everywhere/hooks/session-start.sh` shows the auto-install line at a strictly lower line number than the `gsd-tools.sh` source line.
    - `bash ponytail-everywhere/tests/test-session-start.sh` still exits 0 (the existing banner test suite is unbroken, including its Case 10 plugin-root parity assertion).
    - `ponytail-everywhere/hooks/hooks.json` is unmodified (`git diff --quiet ponytail-everywhere/hooks/hooks.json` exits 0).
  </acceptance_criteria>

  <reversibility rating="reversible">
    Additive file copies plus one additive line; deleting the copies and the line restores the prior plugin exactly. No published contract changes — `ponytail-everywhere` has never shipped a bundle, so nothing downstream can depend on its absence.
  </reversibility>

  <done>
    `ponytail-everywhere` contains a complete `ponytail` capability bundle and a byte-identical
    auto-install script that its SessionStart hook invokes unconditionally, with the existing
    banner test suite still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: CI parity check and release-artifact bundle parity</name>

  <files>.github/workflows/ci.yml, .github/workflows/release.yml</files>

  <read_first>
    - `.github/workflows/release.yml` — the file being modified; its `Build allowlisted archive` step zips `.claude-plugin`, `hooks`, `.agents/skills`, `README.md`, `LICENSE` and omits `.gsd`.
    - `tests/test-capability-auto-install.sh` and `ponytail-everywhere/tests/test-session-start.sh` — the two commands CI must run.
    - `hooks/capability-auto-install.sh` — the file whose vendored copies CI must diff.
  </read_first>

  <action>
    Create `.github/workflows/ci.yml`: a single job named `checks` on `ubuntu-latest`,
    triggered on `push` and `pull_request`, using `actions/checkout@v7` to match the version
    `release.yml` already pins. Three run steps, no matrix, no caching, no extra actions:

    1. Vendored-copy parity (D-06 / CAP-07):
       `cmp hooks/capability-auto-install.sh ponytail-everywhere/hooks/capability-auto-install.sh`
       and `diff -r .gsd/capabilities/ponytail ponytail-everywhere/.gsd/capabilities/ponytail`.
       A nonzero exit fails the job — this is the mechanism that makes "vendored copies" safe,
       replacing a comment-only convention that depends on every future editor remembering
       to update both copies.
    2. `bash tests/test-capability-auto-install.sh`
    3. `bash ponytail-everywhere/tests/test-session-start.sh`

    Then modify `.github/workflows/release.yml`'s `Build allowlisted archive` step: add `.gsd`
    to the existing `zip -r` allowlist, alongside the current `.claude-plugin`, `hooks`,
    `.agents/skills`, `README.md`, `LICENSE` entries. Without it the release zip carries no
    capability bundle and the auto-install exits silently for anyone installing from the
    release artifact rather than the marketplace clone; the marketplace-cache path already
    carries `.gsd/` (verified live in RESEARCH.md Summary point 2). Change nothing else in
    `release.yml`.
  </action>

  <verify>
    <automated>cmp hooks/capability-auto-install.sh ponytail-everywhere/hooks/capability-auto-install.sh && bash tests/test-capability-auto-install.sh && bash ponytail-everywhere/tests/test-session-start.sh && node -e "const y=require('fs').readFileSync('.github/workflows/release.yml','utf8'); process.exit(/^\s+\.gsd\s*\\\\?$/m.test(y)?0:1)"</automated>
  </verify>

  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists and contains a job whose steps include the strings `cmp hooks/capability-auto-install.sh`, `bash tests/test-capability-auto-install.sh`, and `bash ponytail-everywhere/tests/test-session-start.sh`.
    - `.github/workflows/ci.yml` declares both `push` and `pull_request` triggers.
    - `grep -c 'actions/checkout@v7' .github/workflows/ci.yml` outputs `1` (matches the pin already used by `release.yml`).
    - `.github/workflows/release.yml`'s `zip -r` allowlist includes a `.gsd` entry, and `git diff --stat .github/workflows/release.yml` shows a one-line addition and no deletions.
    - Deliberately breaking parity (e.g. appending a newline to one copy) makes the CI parity command exit nonzero — assert by running the `cmp` command against a temp-modified copy and confirming a nonzero status, then discarding the temp change.
  </acceptance_criteria>

  <reversibility rating="reversible">
    A new workflow file and a one-token allowlist addition; both are deletable with no migration and no consumer contract.
  </reversibility>

  <done>
    CI runs both bash suites and fails on any byte-level divergence between the vendored
    copies; the release zip carries `.gsd/` so both documented install paths ship the bundle.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the real marketplace install of `ponytail-everywhere`</name>

  <what-built>
    A `.gsd/capabilities/ponytail/` bundle vendored inside `ponytail-everywhere/` plus a
    SessionStart auto-install that resolves the bundle relative to `$CLAUDE_PLUGIN_ROOT`.
    RESEARCH.md Assumption A2 flags that Claude Code's caching behavior for a
    *subdirectory-sourced* plugin (`source: "./ponytail-everywhere"`) was never empirically
    verified — only the root-sourced `beads-lifecycle` cache was inspected. Assumption A3
    flags that the `sha256sum` vs `shasum -a 256` fallback was never exercised on macOS.
  </what-built>

  <how-to-verify>
    1. Install the plugin for real via the documented flow:
       `claude plugin marketplace add <this repo>` then
       `claude plugin install ponytail-everywhere@gsd-beads`.
    2. Inspect the resulting cache directory (sibling of the verified
       `~/.claude/plugins/cache/gsd-beads/beads-lifecycle/<version>/`) and confirm
       `.gsd/capabilities/ponytail/capability.json` is present inside the
       `ponytail-everywhere` cache entry.
    3. Start a fresh Claude Code session and confirm exactly one line reading
       `Auto-installed capability: ponytail (user scope)` appears.
    4. Start a second session and confirm no such line appears (silent unchanged path).
    5. Confirm `~/.gsd-capabilities.json` (or `$GSD_HOME/.gsd-capabilities.json`) now lists
       `ponytail`, and that `~/.gsd/capability-auto-install-ponytail.hash` exists.
    6. If a macOS machine is available, run `bash tests/test-capability-auto-install.sh` there
       to exercise the `shasum -a 256` fallback branch (A3). If none is available, say so and
       record it as an accepted, documented gap in the SUMMARY.
  </how-to-verify>

  <acceptance_criteria>
    - The `ponytail-everywhere` cache entry contains `.gsd/capabilities/ponytail/capability.json`.
    - The notice line appears exactly once on the first session and zero times on the second.
    - The global ledger lists the `ponytail` capability after the first session.
    - The macOS result is recorded as either verified-passing or an explicitly accepted gap.
  </acceptance_criteria>

  <resume-signal>Type "approved" or describe what you observed instead</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin distribution artifact → end user's machine | The release zip and the marketplace clone are two distinct content sets; whichever the user installs becomes the source the auto-install trusts. |
| Vendored script copy → authored original | Three separately-shipped copies of the same logic can silently diverge. |
| `ponytail-everywhere` SessionStart/SubagentStart hooks → gsd-core CLI | Same unattended-install boundary as Plan 01, now reached from four registered hook matchers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10.1-07 | Tampering | Divergence between the two vendored `capability-auto-install.sh` copies | high | mitigate | CI `cmp` gate in Task 2 fails the build on any byte difference; a security fix landed in one copy cannot silently miss the other. Asserted by the deliberate-divergence acceptance criterion. |
| T-10.1-08 | Tampering | Divergence between the repo-root `ponytail` bundle and the vendored copy | medium | mitigate | CI `diff -r` gate in Task 2 covers the bundle directory as well as the script. |
| T-10.1-09 | Denial of Service | Release zip lacking `.gsd/`, so auto-install silently finds no bundle | medium | mitigate | Add `.gsd` to the `release.yml` allowlist; the script's "no bundle directory → exit 0 silently" branch means this failure would otherwise be completely invisible. |
| T-10.1-10 | Elevation of Privilege | Auto-install now also fires from three `SubagentStart` matchers, not just SessionStart | medium | accept | Each invocation runs the identical hash-gated path with the identical hard-coded literal id and the identical absolute spec; a subagent spawn grants nothing a session start would not have already granted. No additional attack surface, only repeated evaluation. |
| T-10.1-11 | Information Disclosure | Unverified subdirectory-plugin cache layout (Assumption A2) | low | mitigate | Task 3's blocking human-verify checkpoint inspects the real cache entry before this ships, rather than relying on the unverified assumption. |
| T-10.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs in this phase; RESEARCH.md's Package Legitimacy Audit records "not applicable — zero external packages". |
</threat_model>

<verification>
- `cmp` and `diff -r` parity commands both exit 0 locally.
- `bash tests/test-capability-auto-install.sh` and `bash ponytail-everywhere/tests/test-session-start.sh` both pass.
- `bash -n ponytail-everywhere/hooks/session-start.sh` exits 0.
- Task 3's human checkpoint approved.
</verification>

<success_criteria>
- Both existing capabilities (`beads`, `ponytail`) auto-install with no manual command.
- The vendored copies cannot silently drift — CI enforces byte identity.
- Both documented install paths ship the capability bundle.
- The mechanism is copy-ready for Phase 11 (`sota-numerics`) with no modification beyond the
  bundle it points at and the literal id passed at the call site.
</success_criteria>

<output>
Create `.planning/phases/10.1-capability-auto-install-sessionstart-triggered-user-scope-co/10.1-02-SUMMARY.md` when done
</output>
