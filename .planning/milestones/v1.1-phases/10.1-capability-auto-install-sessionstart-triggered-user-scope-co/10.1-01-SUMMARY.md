---
phase: 10.1-capability-auto-install
plan: 01
subsystem: infra
tags: [bash, sessionstart-hook, capability-install, smoke-test]

requires: []
provides:
  - "hooks/capability-auto-install.sh — vendored auto-install script for a single capability id"
  - "hooks/session-start.sh wired to auto-install `beads` before `exec bd prime --hook-json`"
  - "tests/test-capability-auto-install.sh — 6-case stdlib bash smoke test"
affects: ["10.1-02 (vendors this script verbatim into ponytail-everywhere)"]

actuals:
  tokens: 3460
  tasks: 2
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Whole-bundle-directory hash (paths + file contents) gates a re-install call — sidecar hash file keyed per capability id under ${GSD_HOME:-$HOME}/.gsd/"
    - "Inlined (not sourced) gsd_tools() resolver copy for dependency-free vendored hook scripts"
    - "127-vs-other-nonzero exit-code branching to distinguish 'tool not found' from 'tool ran and failed', per D-04 fail-open-but-loud"

key-files:
  created:
    - hooks/capability-auto-install.sh
    - tests/test-capability-auto-install.sh
  modified:
    - hooks/session-start.sh

key-decisions:
  - "Tracer feedback gate: Task 1 committed as a tracer, checkpoint held for human-verify, user approved as-is before Task 2 expansion — no changes to the Task 1 implementation."
  - "Plugin-root parity case (Task 2, case 6) uses two separate GSD_HOME sidecar dirs against the same fixed plugin/bundle path so both runs are 'first install' and byte-identical, rather than comparing a first run against a state-suppressed second run."

patterns-established:
  - "D-04 stderr warning text is intentionally distinguishable per failure mode (no-tool vs install-failed) so tests and operators can tell the two apart."

requirements-completed: [CAP-01, CAP-02, CAP-03, CAP-04, CAP-05]

coverage:
  - id: D1
    description: "First-run auto-install: installs once at global scope, prints exactly one notice, writes sidecar hash"
    requirement: "CAP-01"
    verification:
      - kind: unit
        ref: "tests/test-capability-auto-install.sh#case1"
        status: pass
    human_judgment: false
  - id: D2
    description: "Unchanged rerun is completely silent and invokes install zero times (D-02 cheap path)"
    requirement: "CAP-03"
    verification:
      - kind: unit
        ref: "tests/test-capability-auto-install.sh#case2"
        status: pass
    human_judgment: false
  - id: D3
    description: "Bundle edit re-grants with a fresh notice (D-03 detect-and-re-grant)"
    requirement: "CAP-03"
    verification:
      - kind: unit
        ref: "tests/test-capability-auto-install.sh#case3"
        status: pass
    human_judgment: false
  - id: D4
    description: "Install-command failure warns once on stderr, exits 0, and does not write the sidecar (retries next session)"
    requirement: "CAP-05"
    verification:
      - kind: unit
        ref: "tests/test-capability-auto-install.sh#case4"
        status: pass
    human_judgment: false
  - id: D5
    description: "gsd-tools unresolvable (exit 127) warns once on stderr with text distinguishable from the install-failure case"
    requirement: "CAP-05"
    verification:
      - kind: unit
        ref: "tests/test-capability-auto-install.sh#case5"
        status: pass
    human_judgment: false
  - id: D6
    description: "CLAUDE_PLUGIN_ROOT exported vs. derived from dirname $0 produce byte-identical stdout/stderr/install-argv"
    requirement: "CAP-02"
    verification:
      - kind: unit
        ref: "tests/test-capability-auto-install.sh#case6"
        status: pass
    human_judgment: false
  - id: D7
    description: "hooks/session-start.sh invokes the auto-install script for `beads` strictly before the trailing `exec bd prime --hook-json`"
    requirement: "CAP-02"
    verification:
      - kind: unit
        ref: "grep -n 'capability-auto-install.sh|exec bd prime' hooks/session-start.sh (auto-install line precedes exec line)"
        status: pass
    human_judgment: false

duration: ~15min (across two executor sessions, checkpoint pause excluded)
completed: 2026-08-17
status: complete
---

# Phase 10.1 Plan 01: Beads Capability Auto-Install Summary

**Vendored SessionStart hook auto-installs the `beads` capability at global scope on bundle-hash drift, with a 6-case stdlib bash smoke test covering happy path, both D-04 failure modes, and plugin-root resolution parity.**

## Performance

- **Duration:** ~15 min total (Task 1 executed in a prior session, paused at a tracer human-verify checkpoint; this session resumed at Task 2)
- **Tasks:** 2 completed
- **Files modified:** 3 (1 new script, 1 new test file, 1 modified hook)

## Accomplishments
- `hooks/capability-auto-install.sh`: id-validated, whole-bundle-hash-gated, fail-open-but-loud auto-install script for a single capability id, with an inlined `gsd_tools()` resolver
- `hooks/session-start.sh` wired to call the auto-install script for `beads` before the trailing `exec bd prime --hook-json`
- `tests/test-capability-auto-install.sh`: 6 passing cases — first-run install, silent unchanged rerun, bundle-edit re-grant, install-failure warning, no-tool warning (text distinguishable from install-failure), and CLAUDE_PLUGIN_ROOT set-vs-derived byte-identity

## Task Commits

Each task was committed atomically (Task 1 as a TDD tracer: RED then GREEN):

1. **Task 1: End-to-end auto-install for `beads`** — `b5df158` (test: RED), `c305941` (feat: GREEN)
2. **Task 2: Failure-path and path-resolution test cases** — `4df5004` (test)

**Plan metadata:** committed alongside this SUMMARY (see final commit)

## Files Created/Modified
- `hooks/capability-auto-install.sh` — vendored auto-install script (D-01..D-05); the byte-identical source Plan 02 will vendor into `ponytail-everywhere/`
- `hooks/session-start.sh` — one added line invoking the auto-install script for `beads`, placed before `exec`
- `tests/test-capability-auto-install.sh` — new `tests/` dir at repo root; stdlib-only bash smoke test, 6 cases, all scratch `GSD_HOME`/plugin dirs, stub `gsd-tools`, never touches the developer's real `$HOME/.gsd/`

## Decisions Made
- Tracer feedback gate (Task 1) was accepted as-is by the user at the human-verify checkpoint; no rework before proceeding to Task 2.
- Task 2's plugin-root parity case (case 6) compares two independent "first install" runs (separate `GSD_HOME` sidecars, same fixed plugin/bundle path) rather than a first-vs-second run, so bundle-hash state transitions cannot make the two runs diverge for reasons unrelated to `CLAUDE_PLUGIN_ROOT` resolution.

## Deviations from Plan

None — plan executed exactly as written. All acceptance criteria for both tasks were met without needing Rule 1-4 fixes.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Plan 02 can vendor `hooks/capability-auto-install.sh` verbatim into `ponytail-everywhere/hooks/` and add the CAP-06/CAP-07 byte-identity check across the two vendored copies.
- No blockers.

---
*Phase: 10.1-capability-auto-install-sessionstart-triggered-user-scope-co*
*Completed: 2026-08-17*

## Self-Check: PASSED

All created/modified files and all 3 task commits (`b5df158`, `c305941`, `4df5004`) verified present on disk / in git log.
