---
phase: 07-hygiene-publication
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .git/config
autonomous: false
requirements: [PUB-10]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`gh repo view davdittrich/gsd-beads --json visibility` reports `PUBLIC` (ROADMAP SC-4)"
    - "`git remote -v` points `origin` at `github.com/davdittrich/gsd-beads` (ROADMAP SC-4)"
    - "The rewritten history is on the remote: `git rev-parse origin/main` equals local `git rev-parse main`, and the `v1.0` tag is present remotely (ROADMAP SC-4)"
    - "A fresh `git clone` of the pushed URL contains none of the 4 target files in its working tree AND `git log -p --all` on those paths prints nothing across full history (ROADMAP SC-5)"
    - "The user explicitly approved the publication before any command touched GitHub"
  artifacts:
    - "github.com/davdittrich/gsd-beads — public GitHub repository, created empty (no auto-README)"
    - "origin remote configured in .git/config"
  key_links:
    - "Plan 01's local verification MUST be green before the checkpoint is presented — publishing unverified history is the failure mode this phase exists to prevent"
    - "`gh repo create` must run WITHOUT `--add-readme`/`--gitignore`/`--license` (D-05), otherwise the remote has a conflicting initial commit and the first push needs a force it would not otherwise need"
    - "The fresh-clone check is the phase gate — local verification alone does not satisfy ROADMAP SC-5"
  prohibitions:
    - "P-07 (flagged-unverified): do not run `gh repo create` or any push before the human checkpoint in Task 1 returns an explicit approval. The push is the phase's stated one-way door — a public repo's history cannot be un-published."
    - "P-08 (flagged-unverified): do not add `--force` to the first push speculatively. Against a genuinely empty remote a plain push succeeds; an unwarranted force trips CLAUDE.md's standing force-push confirmation gate for no reason (RESEARCH Pitfall 5)."
    - "P-09 (flagged-unverified): if the plain push IS rejected, halt and return to the user with the exact `--force-with-lease` command rather than escalating autonomously. A rejection means the remote is not empty — an unexplained state that must be understood before overwriting."
    - "P-10 (flagged-unverified): do not pass `--add-readme`, `--gitignore`, or `--license` to `gh repo create` (D-05). README is Phase 8 scope, written from verified commands; LICENSE already exists in the tree from Phase 5."
    - "P-11 (flagged-unverified): do not push with `--mirror`. That would transmit every ref including any residual local ref namespace; push `main` and `--tags` explicitly (RESEARCH Anti-Patterns)."
    - "P-12 (flagged-unverified): do not delete `/home/dd/Gemini/gsd-beads-backup-pre-filter-repo` in this plan. It stays until the fresh-clone check in Task 3 is green."
  flagged_assumptions:
    - "FA-04 (unresolved): the deterministic edge-probe engine returned `unclassified`/`unresolved` for PUB-10 — it has no category for repo-publication operations. No automated edge coverage was derived; verification rests entirely on the ROADMAP SC-4/SC-5 shell assertions below. Disposition: flagged-unverified, not resolved."
    - "FA-05 (RESEARCH A2): the assumption that a plain `git push -u origin main --tags` to a `gh repo create --source=.` empty remote succeeds without `--force`. If wrong, the push is simply rejected with no data loss and P-09 governs the response."
    - "FA-06: `gh auth status` was verified 2026-08-16 as `davdittrich` with `repo` scope. Task 2 re-verifies it live rather than trusting the recorded value — tokens expire."
---

<objective>
Create the public GitHub repository, push the history Plan 01 rewrote, and prove from a fresh clone
that nothing machine-local survived — satisfying PUB-10 and ROADMAP Phase 7 Success Criteria 4 and 5.

Purpose: this is the phase's actual one-way door. Everything in Plan 01 exists to make this push
safe; nothing after it can be walked back.
Output: `github.com/davdittrich/gsd-beads` public, `origin` configured, rewritten history pushed, and
a fresh-clone verification transcript proving ROADMAP SC-5.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hygiene-publication/07-CONTEXT.md
@.planning/phases/07-hygiene-publication/07-RESEARCH.md
@.planning/phases/07-hygiene-publication/07-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: One-way-door gate — approve publication</name>
  <read_first>
    - /home/dd/Gemini/gsd-beads/.planning/phases/07-hygiene-publication/07-01-SUMMARY.md — Plan 01's verification transcript, including the negative-control probe output and the before/after commit counts. Do NOT present this checkpoint if Plan 01's verification was not green.
    - /home/dd/Gemini/gsd-beads/.planning/ROADMAP.md §"Phase 7" Success Criteria 4 (the force-push confirmation requirement) and §"Notes" (the one-way-door framing)
  </read_first>
  <decision>Publish gsd-beads to `github.com/davdittrich/gsd-beads` as a PUBLIC repository and push the rewritten history. This is irreversible: once a public repo has commits, they are world-readable and third-party archivers may retain them even after a delete.</decision>
  <context>
Before presenting this checkpoint, re-run Plan 01's verification block live and paste the actual
output into the checkpoint body — do not quote the SUMMARY. Specifically show the user:

  - `git log -p --all -- .beads/config.yaml .beads/metadata.json .claude/.headroom_wrap_marker.json .gsd-capabilities.json` → confirm empty
  - `git ls-files | grep -E '\.beads/config\.yaml|\.beads/metadata\.json$|headroom_wrap_marker|\.gsd-capabilities\.json'` → confirm empty
  - `git rev-list --count --all` and `git log --oneline -5`
  - `git ls-files | wc -l` — the total file count about to become public
  - the absolute path of the mirror backup, confirmed still readable

Also state plainly: the repo has no README yet (D-05 — Phase 8 scope), it will be public
immediately on creation, and `LICENSE` (MIT, from Phase 5) is present in the tree.
  </context>
  <options>
    <option id="publish">
      <name>Publish now (public, no auto-README, plain push)</name>
      <pros>Unblocks Phase 8 entirely — README, release archive, and the install round trip all need a public URL. The hygiene window closes cleanly with verified-clean history.</pros>
      <cons>Irreversible. A mistake in Plan 01's strip is now permanent and world-readable.</cons>
    </option>
    <option id="private-first">
      <name>Create private, push, verify, then flip to public</name>
      <pros>The fresh-clone check runs against real GitHub before anything is world-readable; a discovered leak can be fixed by deleting a repo nobody could see.</pros>
      <cons>Departs from D-04's locked `--public` decision; adds a `gh repo edit --visibility public` step. Only choose if the user changes D-04 at the checkpoint.</cons>
    </option>
    <option id="abort">
      <name>Do not publish yet</name>
      <pros>Nothing is lost; Plan 01's work stands and the door stays open.</pros>
      <cons>Phase 7 cannot complete and Phase 8 stays blocked.</cons>
    </option>
  </options>
  <verify>
    <human-check>The user has typed an explicit selection. Absence of objection is NOT approval.</human-check>
  </verify>
  <acceptance_criteria>
    - The live (not quoted) output of both Plan 01 verification probes was shown to the user in the checkpoint body
    - The user returned one of `publish`, `private-first`, or `abort` in their own message
    - No `gh` or `git push` command was executed before that message arrived
  </acceptance_criteria>
  <resume-signal>Select: publish, private-first, or abort</resume-signal>
  <done>An explicit user selection is recorded. On `abort`, stop the plan here and report; do not proceed to Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Create the GitHub repo and push the rewritten history</name>
  <files>.git/config (origin remote added by gh)</files>
  <read_first>
    - /home/dd/Gemini/gsd-beads/.planning/phases/07-hygiene-publication/07-RESEARCH.md §"Code Examples" → "Remote creation and first push" (the exact two commands) and §"Common Pitfalls" Pitfall 5 (why the first push does not need `--force`)
    - /home/dd/Gemini/gsd-beads/.planning/phases/07-hygiene-publication/07-CONTEXT.md §"New GitHub repo creation" (D-04 owner/name/visibility/branch; D-05 no auto-init)
  </read_first>
  <precondition>Task 1 returned `publish` (or `private-first`, in which case substitute `--private` for `--public` below and add the visibility flip after Task 3 passes). Halt on `abort`.</precondition>
  <precondition>`gh auth status` reports an authenticated account with `repo` scope. Halt if the token expired — this is external setup the executor cannot perform.</precondition>
  <reversibility rating="one-way">A public repo's pushed commits are world-readable and may be retained by third-party archivers after deletion. Gated by Task 1.</reversibility>
  <action>
Re-verify auth live: `gh auth status` must show `davdittrich` with `repo` scope (per FA-06 — do not
trust the recorded 2026-08-16 value).

Create the repo with exactly:
`gh repo create davdittrich/gsd-beads --public --source=.`

No `--add-readme`, no `--gitignore`, no `--license`, no `--push` (D-05, P-10). `--source=.` makes
`gh` configure the `origin` remote for this working directory in the same call — Plan 01's
filter-repo run stripped any prior remote, so this is a clean first add. Confirm with `git remote -v`
that `origin` resolves to `github.com/davdittrich/gsd-beads`.

Push with exactly:
`git push -u origin main --tags`

Plain push, no `--force`, no `--mirror` (P-08, P-11). Against the empty remote D-05 guarantees, this
succeeds. `--tags` carries `v1.0`, the repo's only tag. `main` is the only branch (D-04).

If — and only if — the push is REJECTED, halt. Do not retry with `--force`. Report to the user the
rejection output verbatim plus the exact `git push -u origin main --tags --force-with-lease` command
they would need to approve, and stop (P-09). A rejection means the remote already has commits, which
contradicts D-05 and must be explained before anything is overwritten.

Do not delete the mirror backup (P-12) — Task 3 has not run yet.
  </action>
  <verify>
    <automated>gh repo view davdittrich/gsd-beads --json visibility,url --jq '.visibility' | grep -qx 'PUBLIC' && git remote -v | grep -q 'origin.*davdittrich/gsd-beads' && test "$(git rev-parse main)" = "$(git rev-parse origin/main)" && git ls-remote --tags origin | grep -q 'refs/tags/v1.0' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `gh repo view davdittrich/gsd-beads --json visibility,url` returns `visibility` = `PUBLIC` and `url` = `https://github.com/davdittrich/gsd-beads`
    - `git remote -v` shows `origin` fetch and push URLs both pointing at `davdittrich/gsd-beads`
    - `git rev-parse main` equals `git rev-parse origin/main`
    - `git ls-remote --tags origin` includes `refs/tags/v1.0`
    - The push command actually run contains no `--force`, no `--force-with-lease`, and no `--mirror` (record the exact command in the SUMMARY)
    - `gh repo view davdittrich/gsd-beads --json url --jq .url` is recorded in the SUMMARY for Phase 8 to consume
  </acceptance_criteria>
  <done>The public repo exists, `origin` is configured, and `origin/main` plus the `v1.0` tag match local — ROADMAP SC-4 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Fresh-clone verification — the phase gate</name>
  <files>/tmp/gsd-beads-verify (created, disposable)</files>
  <read_first>
    - /home/dd/Gemini/gsd-beads/.planning/phases/07-hygiene-publication/07-RESEARCH.md §"Code Examples" → "Fresh-clone remote verification (Success Criterion 5)" — the exact commands
    - /home/dd/Gemini/gsd-beads/.planning/ROADMAP.md §"Phase 7" Success Criterion 5 (the check must be against the PUSHED copy, not local state)
  </read_first>
  <action>
Clone the pushed repo into a directory that does not exist yet, so nothing local can contaminate the
result: `git clone https://github.com/davdittrich/gsd-beads.git /tmp/gsd-beads-verify` (remove any
prior `/tmp/gsd-beads-verify` first).

In that clone, run the two ROADMAP SC-5 checks verbatim:
  - `git log -p --all -- .beads/config.yaml .beads/metadata.json .claude/.headroom_wrap_marker.json .gsd-capabilities.json` → must print nothing across FULL history, not just HEAD
  - `ls .beads/config.yaml .beads/metadata.json .claude/.headroom_wrap_marker.json .gsd-capabilities.json` → all four must report "No such file or directory"

Also confirm no ROADMAP SC-2 artifact leaked into the pushed tree: `git -C /tmp/gsd-beads-verify
ls-files | grep -E '\.beads\.backup-pre-recovery/|interactions\.jsonl|\.bak$|^\.serena/|dispatch-isolation-sentinel'`
must print nothing. And confirm what MUST be present did survive the push: 17 files under
`.gsd/capabilities/beads/`, 11 `.backup` files under `.beads/hooks/`, plus `LICENSE`,
`.claude-plugin/plugin.json`, and `hooks/hooks.json` (the Phase 5/6 deliverables).

If ANY of these fails, halt immediately and report — the correct response is to delete the GitHub
repo (`gh repo delete davdittrich/gsd-beads`), restore from
`/home/dd/Gemini/gsd-beads-backup-pre-filter-repo`, and re-run Plan 01, NOT to patch forward on a
public repo. Present that as a recommendation to the user; do not delete a public repo unilaterally.

On success, record the full verification transcript in the SUMMARY and remove the two disposable
clones (`/tmp/gsd-beads-verify`, `/tmp/gsd-beads-rehearsal`). Keep
`/home/dd/Gemini/gsd-beads-backup-pre-filter-repo` — it is cheap insurance until Phase 8 ships and
the user decides to remove it.
  </action>
  <verify>
    <automated>rm -rf /tmp/gsd-beads-verify && git clone -q https://github.com/davdittrich/gsd-beads.git /tmp/gsd-beads-verify && test -z "$(git -C /tmp/gsd-beads-verify log -p --all -- .beads/config.yaml .beads/metadata.json .claude/.headroom_wrap_marker.json .gsd-capabilities.json)" && test -z "$(git -C /tmp/gsd-beads-verify ls-files | grep -E '\.beads/config\.yaml|\.beads/metadata\.json$|headroom_wrap_marker|\.gsd-capabilities\.json|interactions\.jsonl|\.bak$|^\.serena/|dispatch-isolation-sentinel')" && git -C /tmp/gsd-beads-verify ls-files .gsd/capabilities/beads/ | wc -l | grep -qx '17' && test -f /tmp/gsd-beads-verify/LICENSE && test -f /tmp/gsd-beads-verify/.claude-plugin/plugin.json && test -f /tmp/gsd-beads-verify/hooks/hooks.json && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /tmp/gsd-beads-verify log -p --all -- <the 4 target paths>` produces zero bytes (ROADMAP SC-5, full history)
    - None of the 4 target paths exists in `/tmp/gsd-beads-verify`'s working tree
    - `git -C /tmp/gsd-beads-verify ls-files | grep -E 'interactions\.jsonl|\.bak$|^\.serena/|dispatch-isolation-sentinel|\.beads\.backup-pre-recovery/'` produces zero bytes (no SC-2 artifact leaked)
    - `git -C /tmp/gsd-beads-verify ls-files .gsd/capabilities/beads/ | wc -l` outputs `17`; `git -C /tmp/gsd-beads-verify ls-files .beads/hooks/ | grep -c '\.backup$'` outputs `11`
    - `/tmp/gsd-beads-verify/LICENSE`, `/tmp/gsd-beads-verify/.claude-plugin/plugin.json`, and `/tmp/gsd-beads-verify/hooks/hooks.json` all exist (Phase 5/6 deliverables survived)
    - The full transcript is recorded in the SUMMARY; `/tmp/gsd-beads-verify` and `/tmp/gsd-beads-rehearsal` are removed afterward; `/home/dd/Gemini/gsd-beads-backup-pre-filter-repo` still exists
  </acceptance_criteria>
  <done>A stranger's-eye-view clone of the public repo contains no machine-local file and no trace of one anywhere in history — ROADMAP SC-5 satisfied and Phase 7 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git object DB → public GitHub | Crossed by Task 2's push. Everything reachable from a pushed ref becomes world-readable and permanently archivable by third parties. This is the boundary the entire phase is built around. |
| `gh` CLI → GitHub API | An authenticated token with `repo` scope creates and configures a world-visible resource. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Information Disclosure | The push itself, if Plan 01's strip was incomplete | critical | mitigate | Task 1's blocking checkpoint re-runs both strip probes LIVE and shows the raw output before approval; Task 3's fresh-clone check independently confirms against the pushed copy rather than local state. |
| T-07-08 | Information Disclosure | Making the repo public before the pushed content is verified | high | mitigate | Ordering is fixed: create → push → fresh-clone verify, with the `private-first` option offered at the checkpoint for a user who wants verification to precede world-visibility. |
| T-07-09 | Tampering | An unwarranted `--force` push overwriting unexpected remote state | medium | mitigate | P-08 forbids speculative `--force`; P-09 requires a halt-and-report on rejection rather than autonomous escalation; CLAUDE.md's standing rule requires explicit user approval for any force-push. |
| T-07-10 | Information Disclosure | Recontamination — a stale clone pushing pre-rewrite history back | low | accept | No other clones and no collaborators exist at push time (RESEARCH §Security Domain). The one stale copy is the deliberately bare, remote-less mirror backup, which cannot push. Re-evaluate if the repo gains collaborators or forks. |
| T-07-11 | Spoofing | `gh` operating under an unexpected account | low | mitigate | Task 2 re-verifies `gh auth status` live (FA-06) and Task 2's acceptance criteria assert the resulting URL is exactly `davdittrich/gsd-beads`. |

**Supply chain:** no `npm`/`pip`/`cargo` install occurs in this plan. No `T-07-SC` threat and no
package-legitimacy checkpoint applies.
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New artifacts introduced by this plan (excluded from drift verification — none exist beforehand):

- `github.com/davdittrich/gsd-beads` — NEW public GitHub repository (external resource, no local file).
- `origin` remote entry in `.git/config` — NEW, created by `gh repo create --source=.`.
- `/tmp/gsd-beads-verify` — NEW disposable verification clone, removed at end of Task 3.

No new source files, functions, classes, or exported symbols. This plan produces no code.
Removed at end of plan: `/tmp/gsd-beads-rehearsal` (created by Plan 01 Task 2).
Retained: `/home/dd/Gemini/gsd-beads-backup-pre-filter-repo`.
</artifacts_this_phase_produces>

<verification>
1. `gh repo view davdittrich/gsd-beads --json visibility,url` → `PUBLIC`, correct URL (SC-4)
2. `git remote -v` → `origin` at `davdittrich/gsd-beads` (SC-4)
3. `git rev-parse main` = `git rev-parse origin/main`; `git ls-remote --tags origin` includes `v1.0` (SC-4)
4. Fresh clone at `/tmp/gsd-beads-verify`: `git log -p --all -- <4 paths>` empty and none of the 4 in the working tree (SC-5)
5. Fresh clone still contains `LICENSE`, `.claude-plugin/plugin.json`, `hooks/hooks.json`, and 17 files under `.gsd/capabilities/beads/`
</verification>

<success_criteria>
ROADMAP Phase 7 Success Criteria 4 and 5 are met, with 1-3 already met by Plan 01. Phase 7 is
complete and Phase 8 (README, release, ship gate) is unblocked with a public URL to work against.
</success_criteria>

<output>
Create `.planning/phases/07-hygiene-publication/07-02-SUMMARY.md` when done. It MUST record: the
user's checkpoint selection verbatim, the exact push command run (proving no `--force`), the public
repo URL for Phase 8 to consume, and the full fresh-clone verification transcript.
</output>
