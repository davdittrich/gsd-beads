---
phase: 07-hygiene-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .planning/phases/06-runtime-integration/06-PATTERNS.md
  - .planning/phases/07-hygiene-publication/07-PATTERNS.md
  - .planning/STATE-ARCHIVE.md
  - .planning/config.json
  - .planning/research/.cache/255dcad363ccf0b782ea7c97a6975466963460aed6ac2b1019bcc2ecc49869fb.json
  - .planning/phases/07-hygiene-publication/07-01-PLAN.md
  - .planning/phases/07-hygiene-publication/07-02-PLAN.md
  - .planning/phases/07-hygiene-publication/07-VALIDATION.md
  - .planning/ROADMAP.md
  - .claude/.headroom_wrap_marker.json
  - .beads/config.yaml
  - .beads/metadata.json
  - .gsd-capabilities.json
autonomous: true
requirements: [PUB-05]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`git ls-files` lists none of `.beads/config.yaml`, `.beads/metadata.json`, `.claude/.headroom_wrap_marker.json`, `.gsd-capabilities.json` (ROADMAP SC-1)"
    - "`git log -p --all --` on all 4 target paths prints nothing — every past commit's tree is stripped, not just HEAD (ROADMAP SC-3)"
    - "`git check-ignore -v` matches `.beads.backup-pre-recovery/`, `.beads/interactions.jsonl`, and a probe `foo.bak` against the ROOT `.gitignore` (ROADMAP SC-2)"
    - "`git stash list` prints nothing before `git filter-repo` runs (RESEARCH Pitfall 1)"
    - "A bare mirror backup of pre-rewrite history exists at a sibling path outside the project root and is readable (D-03)"
    - "`git status --porcelain` is empty after the cleanup commit — no untracked or modified file remains unresolved"
  artifacts:
    - ".gitignore — extended with the 3 ROADMAP-named patterns plus D-01's `.serena/` and `.gsd/dispatch-isolation-sentinel.json`"
    - "/home/dd/Gemini/gsd-beads-backup-pre-filter-repo — bare mirror clone of pre-rewrite history (D-03)"
    - ".planning/phases/06-runtime-integration/06-PATTERNS.md — tracked (D-02)"
  key_links:
    - "The cleanup commit MUST land before `git filter-repo` runs, so the rewritten history's tip already carries the new ignore rules (CONTEXT.md Integration Points)"
    - "The mirror backup MUST exist and be verified before `git filter-repo` runs — it is the only undo path (D-03)"
    - "The scratch rehearsal exercises the exact verification greps used against the real repo, so a green result cannot come from a broken probe"
  prohibitions:
    - "P-01 (flagged-unverified): the `*.bak` ignore entry must be written EXACTLY as `*.bak` — never `*.bak*` or `*.backup` — because 11 tracked files under `.beads/hooks/` end in `.backup` and a widened pattern silently shadows them."
    - "P-02 (flagged-unverified): never add a directory-level `.gsd/` ignore. The `.gsd/capabilities/beads/` subtree is 17 legitimately tracked files that PUB-03 depends on. The sentinel ignore must be the exact filename `.gsd/dispatch-isolation-sentinel.json`."
    - "P-03 (flagged-unverified): do not add ignore patterns for `.planning/` content. `.planning/research/.cache/*.json` is already tracked (8 files) — the new cache file is committed, not ignored, per the existing convention."
    - "P-04 (flagged-unverified): the new `.gitignore` entries go in the ROOT `.gitignore`, never in `.beads/.gitignore` — that file is `bd`-generated, carries a self-warning against edits, and is overwritten on `bd` upgrade (RESEARCH Pitfall 4)."
    - "P-05 (flagged-unverified): do not run `git filter-repo` before the mirror backup exists and has been verified readable. Filter-repo expires reflogs and prunes loose objects immediately — there is no in-repo undo."
    - "P-06 (flagged-unverified): do not add `--path` targets beyond ROADMAP's 4 named files. Scope creep in an irreversible rewrite is unrecoverable without the backup."
  flagged_assumptions:
    - "FA-01 (unresolved): the deterministic edge-probe engine returned `unclassified`/`unresolved` for PUB-05 — it has no category for git-history/repo-publication operations. No automated edge coverage was derived for this requirement; verification rests entirely on the ROADMAP success-criteria shell assertions below. Disposition: flagged-unverified, not resolved."
    - "FA-02 (RESEARCH A1): `git filter-repo`'s handling of `refs/stash` is described as opaque by a community issue thread, not by official docs. Mitigation (drop the stash first) is a strict superset of safety regardless of whether the claim is precise."
    - "FA-03: RESEARCH's live-repo inventory was taken 2026-08-16 and the working tree has since gained `.planning/phases/07-hygiene-publication/07-PATTERNS.md` and one new `.planning/research/.cache/*.json`. Task 2 re-verifies `git status` and `git stash list` immediately before acting rather than trusting the recorded inventory."
---

<objective>
Strip the 4 machine-local files from every commit in this repo's history and extend the root
`.gitignore` so they and their sibling artifacts cannot come back — satisfying PUB-05 and ROADMAP
Phase 7 Success Criteria 1, 2, and 3.

Purpose: this is the last moment the window is open. Once Plan 02 pushes to a public GitHub repo,
`git log -p` on machine-local state becomes permanently recoverable by strangers.
Output: a rewritten local history with no trace of the 4 targets, a bare mirror backup of the
pre-rewrite history, and an extended root `.gitignore`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hygiene-publication/07-CONTEXT.md
@.planning/phases/07-hygiene-publication/07-RESEARCH.md
@.planning/phases/07-hygiene-publication/07-PATTERNS.md
</context>

<tracer_note>
TRACER_MODE maps onto a git-history-rewrite phase as follows: there is no application stack to slice
vertically, so the tracer is the **full strip-and-verify pipeline rehearsed on a throwaway clone of
the D-03 mirror backup** (Task 2). It exercises every layer this phase touches — filter-repo
invocation, empty-commit pruning, the `git log -p --all` probe, the `git ls-files` probe — end to end
against real objects, before the irreversible in-place run in Task 3. The backup clone doubles as the
rehearsal substrate, so the tracer costs one extra `git clone`, not a second artifact.
The one-way door (public push) is deliberately NOT in this plan; it is gated in Plan 02.
</tracer_note>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the working tree into one clean pre-rewrite commit</name>
  <files>.gitignore, .planning/phases/06-runtime-integration/06-PATTERNS.md, .planning/phases/07-hygiene-publication/07-PATTERNS.md, .planning/STATE-ARCHIVE.md, .planning/config.json, .planning/research/.cache/255dcad363ccf0b782ea7c97a6975466963460aed6ac2b1019bcc2ecc49869fb.json, .claude/.headroom_wrap_marker.json</files>
  <read_first>
    - /home/dd/Gemini/gsd-beads/.gitignore (the file being modified — 8 lines, current content and grouping convention)
    - /home/dd/Gemini/gsd-beads/.planning/phases/07-hygiene-publication/07-PATTERNS.md §"`.gitignore` (config, file-I/O)" — the observed grouping convention (blank line between groups, `# Comment` header naming the subsystem, one bare pattern per line, no inline comments)
    - /home/dd/Gemini/gsd-beads/.planning/phases/07-hygiene-publication/07-RESEARCH.md §"Common Pitfalls" Pitfall 4 and §"Runtime State Inventory" — why the root `.gitignore` and not `.beads/.gitignore`, and why the sentinel entry must be filename-scoped
    - /home/dd/Gemini/gsd-beads/.beads/.gitignore (confirm the patterns being added are not already covered there, and confirm it is tool-generated — do NOT edit it)
  </read_first>
  <precondition>The repository has no configured remote (`git remote -v` prints nothing) — this plan assumes a first-time, never-pushed repo. If a remote exists, halt: the rewrite scope changed.</precondition>
  <action>
Append two new groups to the ROOT `/home/dd/Gemini/gsd-beads/.gitignore`, after the existing
`*.gate.lock*` line, matching the file's existing convention (blank line separator, `# Comment`
header, one bare pattern per line).

Group one, headed `# Backup and interaction-log artifacts (local-only, Phase 7 hygiene)`, with
exactly these three patterns, one per line: `.beads.backup-pre-recovery/`,
`.beads/interactions.jsonl`, `*.bak`.

Group two, headed `# Local tool state (per-machine, not shared across clones)`, with exactly these
two patterns, one per line: `.serena/`, `.gsd/dispatch-isolation-sentinel.json`.

The `*.bak` pattern is written exactly as `*.bak` — not `*.bak*`, not `*.backup`. Widening it would
shadow the 11 tracked `.beads/hooks/*.backup` files (per P-01). The sentinel entry is the exact
filename `.gsd/dispatch-isolation-sentinel.json`, never a `.gsd/` directory ignore, which would
silently untrack the 17-file `.gsd/capabilities/beads/` subtree PUB-03 depends on (per P-02).

D-01 resolution: both `.serena/` and `.gsd/dispatch-isolation-sentinel.json` are gitignored, not
deleted. `.serena/` is regenerated by the Serena MCP server on next use so deletion is pointless
churn; the sentinel is a per-dispatch regenerated state file
(`{"isolation":"none","harness_flag":null,"phase":"06",...}`), also regenerated. Both are
non-destructive to ignore.

Then stage and commit, in ONE commit:
  - the modified `.gitignore`
  - `git add` the D-02 file `.planning/phases/06-runtime-integration/06-PATTERNS.md`
  - `git add` `.planning/phases/07-hygiene-publication/07-PATTERNS.md` (same convention as D-02 — a
    phase planning doc under `.planning/phases/`, currently untracked)
  - `git add` the untracked `.planning/research/.cache/255dcad363ccf0b782ea7c97a6975466963460aed6ac2b1019bcc2ecc49869fb.json`
    — the sibling `.planning/research/.cache/*.json` files are ALREADY tracked (8 of them), so this
    one is committed for consistency, not ignored (per P-03)
  - the modified `.planning/STATE-ARCHIVE.md` and `.planning/config.json`
  - any remaining untracked or modified `.planning/` artifact produced by Phase 7 planning itself
    (`07-01-PLAN.md`, `07-02-PLAN.md`, `07-VALIDATION.md`, the ROADMAP plan-list update) — these
    were authored after the RESEARCH inventory was taken and must be tracked, not ignored (P-03)
  - the already-deleted `.claude/.headroom_wrap_marker.json` (stage the deletion with `git rm`)

Commit message: `chore(07): extend gitignore and clean working tree before history rewrite`.
Note the marker-file deletion here is cosmetic — Task 3's filter-repo pass removes it from every
commit's tree regardless; do NOT add a separate `git rm --cached` untrack step for the other 3
targets (RESEARCH Anti-Patterns: redundant, and such a commit can itself be silently pruned as empty).
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain)" && git check-ignore -v .beads.backup-pre-recovery/ .beads/interactions.jsonl foo.bak .serena/ .gsd/dispatch-isolation-sentinel.json && git ls-files .gsd/capabilities/beads/ | wc -l | grep -qx '17' && git ls-files .beads/hooks/ | grep -c '\.backup$' | grep -qx '11' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` prints nothing (empty working tree, nothing untracked, nothing staged)
    - `git check-ignore -v .beads.backup-pre-recovery/ .beads/interactions.jsonl foo.bak .serena/ .gsd/dispatch-isolation-sentinel.json` exits 0 and every one of the 5 paths is reported as matched by `.gitignore` (the root file — the reported source path in column 1 must be `.gitignore`, not `.beads/.gitignore`)
    - `git ls-files .gsd/capabilities/beads/ | wc -l` outputs `17` (P-02 not violated)
    - `git ls-files .beads/hooks/ | grep -c '\.backup$'` outputs `11` (P-01 not violated)
    - `git ls-files .planning/phases/06-runtime-integration/06-PATTERNS.md` outputs that path (D-02 satisfied)
    - `grep -c '^\*\.bak$' .gitignore` outputs `1` and `grep -c '^\.gsd/$' .gitignore` outputs `0`
    - `git log -1 --format=%s` outputs `chore(07): extend gitignore and clean working tree before history rewrite`
  </acceptance_criteria>
  <done>One commit contains the extended root `.gitignore`, the newly tracked planning docs, and the marker deletion; the working tree is clean and every ROADMAP SC-2 pattern is ignored by the root file.</done>
</task>

<task type="tracer">
  <name>Task 2: Mirror backup + full strip-and-verify rehearsal on a throwaway clone</name>
  <files>/home/dd/Gemini/gsd-beads-backup-pre-filter-repo (created), /tmp/gsd-beads-rehearsal (created, disposable)</files>
  <read_first>
    - /home/dd/Gemini/gsd-beads/.planning/phases/07-hygiene-publication/07-RESEARCH.md §"Architecture Patterns" (the 8-step operation sequence diagram and the recommended sibling backup layout), §"Code Examples" (the verbatim filter-repo invocation and the verbatim local verification commands), §"Common Pitfalls" Pitfall 1 (stash) and Pitfall 2 (expected commit-count drop)
    - /home/dd/Gemini/gsd-beads/.planning/phases/07-hygiene-publication/07-CONTEXT.md §"Filter-repo safety" (D-03 — the mirror backup is mandatory, never skipped)
  </read_first>
  <precondition>`git filter-repo --version` succeeds (binary present at /home/dd/.local/bin/git-filter-repo). If absent, halt — this phase has no fallback tool.</precondition>
  <reversibility rating="costly">The mirror backup itself is trivially reversible (a plain clone). Rated `costly` rather than `reversible` because its ABSENCE is what makes Task 3 one-way — this task is the entire undo path for the phase.</reversibility>
  <action>
Re-verify live state first — RESEARCH's inventory is dated and the working tree has changed since
(per FA-03). Run `git status --porcelain` (must be empty after Task 1) and `git stash list`.

Drop the stray stash. `git stash list` currently shows one entry, `stash@{0}: WIP on main: 516581f`,
whose only content is a 1-line `updatedAt` timestamp diff on `.gsd-capabilities.json` — a target
file. `refs/stash` is outside the ref namespace filter-repo reliably processes (RESEARCH Pitfall 1),
so remove the ambiguity with `git stash drop` rather than trusting undocumented behavior. Confirm
`git stash list` prints nothing afterward. The dropped content is a discardable timestamp bump, not
work in progress — but it is captured in the mirror backup taken next, so it remains recoverable.

Take the D-03 mirror backup: `git clone --mirror . /home/dd/Gemini/gsd-beads-backup-pre-filter-repo`.
This sibling path is deliberately OUTSIDE the project root so no later `git add -A` can sweep it and
no `.gitignore` pattern has to cover it. Verify it is readable and complete by running
`git -C /home/dd/Gemini/gsd-beads-backup-pre-filter-repo rev-list --count --all` and confirming it
matches `git rev-list --count --all` in the real repo.

Now the tracer proper — rehearse the entire strip-and-verify pipeline on a throwaway working clone
of that backup, so the irreversible Task 3 run is a repeat of something already proven, and so the
verification probes themselves are proven to be capable of failing:

  1. `git clone /home/dd/Gemini/gsd-beads-backup-pre-filter-repo /tmp/gsd-beads-rehearsal`
  2. In the rehearsal clone, run the NEGATIVE control FIRST: the two verification commands from
     RESEARCH §"Local verification" must produce NON-empty output at this point (the 4 files are
     still present in that clone's history and index). A probe that prints nothing here is broken,
     and a green result in step 4 would be meaningless. Record both outputs.
  3. Run the exact combined invocation from RESEARCH §"Code Examples":
     `git filter-repo --force --path .beads/config.yaml --path .beads/metadata.json --path .claude/.headroom_wrap_marker.json --path .gsd-capabilities.json --invert-paths`
     Exactly these 4 `--path` targets, no more (P-06). `--force` is required and expected — the
     refusal message about "not a fresh clone" was reproduced empirically during research.
  4. Re-run the same two probes: both must now print nothing.
  5. Record `git rev-list --count --all` before and after. A drop is CORRECT, not a defect —
     RESEARCH Pitfall 2 identifies exactly 3 commits (`2075c5f`, `927e9de`, `85aff2a`) that touch
     only target files and are therefore pruned by the default `--prune-empty auto`. Do not write or
     enforce any assertion that the commit count is unchanged.
  6. Confirm the rehearsal clone still contains the things that must survive: `git ls-files
     .gsd/capabilities/beads/ | wc -l` is 17 and `git ls-files .beads/hooks/ | grep -c '\.backup$'`
     is 11.

Report the observed before/after commit counts and the pruned-commit delta in the plan SUMMARY, so
Task 3's identical numbers can be compared against a known-good rehearsal rather than a guess.
Leave `/tmp/gsd-beads-rehearsal` in place until Task 3 passes; it is disposable afterward.
Do NOT run filter-repo against the real repo in this task, and do NOT modify the mirror backup.
  </action>
  <verify>
    <automated>git stash list | grep -q . && exit 1; git -C /home/dd/Gemini/gsd-beads-backup-pre-filter-repo rev-list --count --all >/dev/null && test -z "$(git -C /tmp/gsd-beads-rehearsal log -p --all -- .beads/config.yaml .beads/metadata.json .claude/.headroom_wrap_marker.json .gsd-capabilities.json)" && test -z "$(git -C /tmp/gsd-beads-rehearsal ls-files | grep -E '\.beads/config\.yaml|\.beads/metadata\.json$|headroom_wrap_marker|\.gsd-capabilities\.json')" && git -C /tmp/gsd-beads-rehearsal ls-files .gsd/capabilities/beads/ | wc -l | grep -qx '17' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `git stash list` in the real repo prints nothing
    - `/home/dd/Gemini/gsd-beads-backup-pre-filter-repo` exists, `git -C` it `rev-list --count --all` succeeds, and its count equals the real repo's pre-rewrite `git rev-list --count --all`
    - The negative control was recorded: both probes produced NON-empty output in the rehearsal clone BEFORE filter-repo ran (this is what proves the probes can fail; a run that skipped it does not satisfy this task)
    - After filter-repo in the rehearsal clone: `git log -p --all -- <the 4 paths>` prints nothing AND `git ls-files | grep -E '\.beads/config\.yaml|\.beads/metadata\.json$|headroom_wrap_marker|\.gsd-capabilities\.json'` prints nothing
    - Rehearsal clone still has 17 files under `.gsd/capabilities/beads/` and 11 `.backup` files under `.beads/hooks/`
    - The before/after `git rev-list --count --all` values are recorded in the SUMMARY; no assertion of equality is made
    - The real repo's history is unchanged by this task: `git rev-parse HEAD` in `/home/dd/Gemini/gsd-beads` still equals the Task 1 commit
  </acceptance_criteria>
  <done>The mirror backup exists and is verified complete, the stash is gone, and the exact filter-repo invocation plus both verification probes have been proven end-to-end on a throwaway clone — including a negative control showing the probes detect the files when present.</done>
</task>

<task type="auto">
  <name>Task 3: Run filter-repo in place on the real repo and verify locally</name>
  <files>.beads/config.yaml, .beads/metadata.json, .claude/.headroom_wrap_marker.json, .gsd-capabilities.json (all removed from every commit's tree)</files>
  <read_first>
    - /home/dd/Gemini/gsd-beads/.planning/phases/07-hygiene-publication/07-RESEARCH.md §"Code Examples" (the verbatim invocation and the verbatim local-verification commands — use them as written, do not invent new command forms)
    - /home/dd/Gemini/gsd-beads/.planning/phases/07-hygiene-publication/07-RESEARCH.md §"Common Pitfalls" Pitfall 2 (why the commit count drops) and Pitfall 3 (why `.beads/config.yaml` is in scope even with no live secret in it today)
    - The Task 2 SUMMARY entry recording the rehearsal's before/after commit counts
  </read_first>
  <precondition>`/home/dd/Gemini/gsd-beads-backup-pre-filter-repo` exists and `git -C` it `rev-list --count --all` succeeds. Halt if not — this is the only undo path for this task (P-05).</precondition>
  <reversibility rating="one-way">Filter-repo rewrites commit hashes in place, expires reflogs, and prunes loose objects immediately. Recovery is possible ONLY from the Task 2 mirror backup. Rated one-way for the repo's own object database; the phase's true public one-way door is the push, gated separately in Plan 02.</reversibility>
  <action>
Run the exact combined invocation, in `/home/dd/Gemini/gsd-beads`, with exactly these 4 `--path`
targets and no others (P-06):

`git filter-repo --force --path .beads/config.yaml --path .beads/metadata.json --path .claude/.headroom_wrap_marker.json --path .gsd-capabilities.json --invert-paths`

`--force` is required and expected for this repo (740 loose objects, 2 packs — not freshly packed);
the refusal without it was reproduced during research. Do not work around the refusal any other way.

Then run the two local verification commands from RESEARCH §"Local verification" verbatim:
  - `git log -p --all -- .beads/config.yaml .beads/metadata.json .claude/.headroom_wrap_marker.json .gsd-capabilities.json` → must print nothing (ROADMAP SC-3)
  - `git ls-files | grep -E '\.beads/config\.yaml|\.beads/metadata\.json$|headroom_wrap_marker|\.gsd-capabilities\.json'` → must print nothing (ROADMAP SC-1)

Compare the post-rewrite `git rev-list --count --all` against the value Task 2 observed in the
rehearsal. They should match; a mismatch means the two runs saw different inputs and warrants a halt
before Plan 02 rather than a push.

Also confirm nothing that must survive was collaterally stripped: `git ls-files
.gsd/capabilities/beads/ | wc -l` is 17, `git ls-files .beads/hooks/ | grep -c '\.backup$'` is 11,
and `git tag` still lists `v1.0`. Note filter-repo removes any configured remote as a safety measure
— there is none configured here, so nothing is lost; Plan 02 adds `origin` fresh.

Do NOT create a GitHub repo, add a remote, or push in this task. Publication is Plan 02 and is gated.
  </action>
  <verify>
    <automated>test -z "$(git log -p --all -- .beads/config.yaml .beads/metadata.json .claude/.headroom_wrap_marker.json .gsd-capabilities.json)" && test -z "$(git ls-files | grep -E '\.beads/config\.yaml|\.beads/metadata\.json$|headroom_wrap_marker|\.gsd-capabilities\.json')" && git ls-files .gsd/capabilities/beads/ | wc -l | grep -qx '17' && git ls-files .beads/hooks/ | grep -c '\.backup$' | grep -qx '11' && git tag | grep -qx 'v1.0' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `git log -p --all -- .beads/config.yaml .beads/metadata.json .claude/.headroom_wrap_marker.json .gsd-capabilities.json` produces zero bytes of output (ROADMAP SC-3)
    - `git ls-files | grep -E '\.beads/config\.yaml|\.beads/metadata\.json$|headroom_wrap_marker|\.gsd-capabilities\.json'` produces zero bytes of output and exits 1 (ROADMAP SC-1)
    - None of the 4 paths exists in the working tree: `ls .beads/config.yaml .beads/metadata.json .claude/.headroom_wrap_marker.json .gsd-capabilities.json` reports all four as missing
    - `git ls-files .gsd/capabilities/beads/ | wc -l` outputs `17`; `git ls-files .beads/hooks/ | grep -c '\.backup$'` outputs `11`; `git tag` includes `v1.0`
    - `git ls-files .gitignore` still lists `.gitignore` and `grep -c '^\*\.bak$' .gitignore` outputs `1` (Task 1's commit survived the rewrite)
    - Post-rewrite `git rev-list --count --all` is recorded in the SUMMARY alongside Task 2's rehearsal value; the two match
    - `/home/dd/Gemini/gsd-beads-backup-pre-filter-repo` still exists and is untouched
  </acceptance_criteria>
  <done>Every commit's tree in the real repo is free of the 4 target files, both ROADMAP verification probes are empty, the tracked capability bundle and hook backups are intact, and the mirror backup is preserved for Plan 02's gate.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git object DB → public GitHub | Everything reachable from a pushed ref becomes world-readable and permanently archivable. Crossed in Plan 02, not here — this plan exists to make the crossing safe. |
| `bd`-generated files → git index | Tool-generated config/metadata carrying machine identifiers and secret-capable fields enters the repo without a human review step. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `.beads/config.yaml` in historical commit trees | high | mitigate | Task 3 `git filter-repo --invert-paths`. The file is beads' own template with no live secret today, but its inline comments document `linear.api_key` / `github.token` as fields it is designed to hold — untrack-only leaves any future or past value recoverable via `git log -p` (RESEARCH Pitfall 3). |
| T-07-02 | Information Disclosure | `.beads/metadata.json` (`project_id` UUID), `.gsd-capabilities.json` (absolute checkout path), `.claude/.headroom_wrap_marker.json` | medium | mitigate | Task 3 filter-repo pass, same invocation. Machine-identifying, not credential-bearing. |
| T-07-03 | Information Disclosure | `refs/stash` snapshot of `.gsd-capabilities.json` | low | mitigate | Task 2 `git stash drop` before the rewrite. `refs/stash` is never transmitted by `git push`, so exposure risk is near-zero; dropped anyway to remove reliance on filter-repo's undocumented stash handling (RESEARCH Pitfall 1 / FA-02). |
| T-07-04 | Tampering | `.gsd/capabilities/beads/` (17 tracked files) collaterally untracked by an over-broad `.gsd/` ignore | high | mitigate | P-02 forbids directory-level `.gsd/` ignore; Task 1 and Task 3 acceptance criteria both assert the exact count of 17. |
| T-07-05 | Tampering | Irreversible loss of history from a wrong filter-repo invocation | high | mitigate | D-03 mirror backup taken and verified in Task 2 before any in-place run; Task 3 precondition halts if it is absent; the exact invocation is rehearsed on a throwaway clone first. |
| T-07-06 | Repudiation | A verification probe that never actually ran, indistinguishable from a passing one | medium | mitigate | Task 2's negative control: both probes must produce non-empty output against un-stripped history before they are trusted to prove emptiness. |

**Supply chain:** no `npm`/`pip`/`cargo` install occurs in this phase — `git-filter-repo` and `gh`
are pre-installed system binaries (RESEARCH §Package Legitimacy Audit). No `T-07-SC` threat and no
package-legitimacy checkpoint applies.
</threat_model>

<artifacts_this_phase_produces>

## Artifacts this phase produces

New/changed artifacts introduced by this plan (excluded from drift verification — they do not exist
in the codebase before this plan runs):

- `.gitignore` — five NEW entries: `.beads.backup-pre-recovery/`, `.beads/interactions.jsonl`,
  `*.bak`, `.serena/`, `.gsd/dispatch-isolation-sentinel.json`, plus two new `#` group headers
  (`# Backup and interaction-log artifacts (local-only, Phase 7 hygiene)` and
  `# Local tool state (per-machine, not shared across clones)`).
- `/home/dd/Gemini/gsd-beads-backup-pre-filter-repo` — NEW bare mirror clone, outside the project
  root, untracked by design.
- `/tmp/gsd-beads-rehearsal` — NEW disposable rehearsal clone, deleted after Task 3.
- Newly tracked (previously untracked, no content change):
  `.planning/phases/06-runtime-integration/06-PATTERNS.md`,
  `.planning/phases/07-hygiene-publication/07-PATTERNS.md`,
  `.planning/research/.cache/255dcad363ccf0b782ea7c97a6975466963460aed6ac2b1019bcc2ecc49869fb.json`.

Removed from the repo entirely (every commit's tree): `.beads/config.yaml`,
`.beads/metadata.json`, `.claude/.headroom_wrap_marker.json`, `.gsd-capabilities.json`.

No new source files, functions, classes, or exported symbols. This plan produces no code.
</artifacts_this_phase_produces>

<verification>
Run after all three tasks, from `/home/dd/Gemini/gsd-beads`:

1. `git log -p --all -- .beads/config.yaml .beads/metadata.json .claude/.headroom_wrap_marker.json .gsd-capabilities.json` → empty (ROADMAP SC-3)
2. `git ls-files | grep -E '\.beads/config\.yaml|\.beads/metadata\.json$|headroom_wrap_marker|\.gsd-capabilities\.json'` → empty (ROADMAP SC-1)
3. `git check-ignore -v .beads.backup-pre-recovery/ .beads/interactions.jsonl foo.bak` → all matched by the root `.gitignore` (ROADMAP SC-2)
4. `git ls-files .gsd/capabilities/beads/ | wc -l` → `17`; `git ls-files .beads/hooks/ | grep -c '\.backup$'` → `11` (no collateral damage)
5. `git stash list` → empty
6. `git -C /home/dd/Gemini/gsd-beads-backup-pre-filter-repo rev-list --count --all` → succeeds (backup intact, undo path preserved for Plan 02)
</verification>

<success_criteria>
ROADMAP Phase 7 Success Criteria 1, 2, and 3 are all met locally. Criteria 4 and 5 (public repo,
push, fresh-clone verification) are Plan 02's scope and are deliberately NOT attempted here.
</success_criteria>

<output>
Create `.planning/phases/07-hygiene-publication/07-01-SUMMARY.md` when done. It MUST record: the
pre- and post-rewrite `git rev-list --count --all` values from both the rehearsal and the real run,
the negative-control probe output from Task 2, and the mirror backup's absolute path (Plan 02's
checkpoint reads that path).
</output>
