---
milestone: v1.3
audited: 2026-08-20T10:40:00Z
status: tech_debt
scores:
  requirements: 4/4
  phases: 1/1
  integration: 4/4
  flows: 4/4
gaps: {}
tech_debt:
  - phase: 17-config-code-truth
    items:
      - "RESOLVED (this session, via /gsd-validate-phase 17, commit 373e7fb): WR-04 — SYNC_MODE_VALUES had no direct parity test against capability.json's declared array. Test added, green."
      - "RESOLVED (this session, via /gsd-validate-phase 17, commit 0f8decb): WR-05 — check_native_step_dispatch's detection loop did not exclude fenced-block lines from the kind==\"step\" match, a real latent bug matching the exact T-17-02-01 DoS failure class. RED regression test confirmed first, then sync.py fixed to carry in_fence through a single merged pass. Full suite 246->248 tests, green."
      - "WR-01 (open, confidence 95): check_sync_mode_value's docstring (sync.py:802-804) claims check_patch prints to stderr in a \"benign-skip convention\" opposite to check_sync_mode_value's stdout. False — check_patch (sync.py:2296-2322) prints all four message paths to stdout unconditionally, confirmed by TestCheckShipmdPatch/TestCheckExecutePlanPatch's own stdout-redirect assertions. Current stdout behavior is correct (it's what lets the hook promote warnings into additionalContext); the risk is a future maintainer trusting the docstring and 'fixing' check_patch to route through stderr, silently breaking patch-loss warnings. Fix: correct the docstring or pin the stdout behavior with an explicit comment."
      - "WR-02 (open, confidence 85): not_found_msg/could_not_read_msg (PATCH_CHECKS entries, sync.py:142-179) never carry the \"⚠\" marker that beads-recall/SKILL.md:76 and beads-status/SKILL.md:149 gate surfacing on. Only missing_msg is prefixed. An LLM orchestrator following the SKILL.md literally would surface a missing-marker warning but silently skip a file-not-found or could-not-read case — arguably the more severe, harder-to-self-diagnose failure. Pre-existing (not introduced by phase 17's refactor). Fix: prefix all three non-present messages with \"⚠ \", or broaden the SKILL.md instruction to key off exit code instead of a message substring."
      - "WR-03 (open, non-blocking, docs-only): CHANGELOG.md's 0.4.0 section omits any mention of TRUTH-03 (check_native_step_dispatch, PR #3687, the plan:post/verify:post stand-down mechanism). No functional impact; not required by any plan's files_modified/must_haves."
---

# Milestone v1.3 — Audit Report

**Audited:** 2026-08-20
**Status:** tech_debt (no blockers; all requirements satisfied; 3 open non-blocking debt items, 2 more resolved during this audit)

---

## Requirements Coverage (4/4 satisfied)

3-source cross-reference (REQUIREMENTS.md traceability × phase VERIFICATION.md × SUMMARY.md `requirements-completed` frontmatter):

| Req | Phase | REQUIREMENTS.md | VERIFICATION.md | SUMMARY frontmatter | Final Status |
|-----|-------|------------------|------------------|----------------------|---------------|
| TRUTH-01 | 17 (plan 17-03) | `[x]` Complete | passed | listed (`17-03-SUMMARY.md`) | **satisfied** |
| TRUTH-02 | 17 (plan 17-04) | `[x]` Complete | passed | listed (`17-04-SUMMARY.md`) | **satisfied** |
| TRUTH-03 | 17 (plan 17-02) | `[x]` Complete | passed | listed (`17-02-SUMMARY.md`) | **satisfied** |
| TRUTH-04 | 17 (plan 17-01) | `[x]` Complete | passed | listed (`17-01-SUMMARY.md`) | **satisfied** |

No orphaned requirements (every traceability row has a matching VERIFICATION.md entry). No unsatisfied requirements — the FAIL gate does not trigger.

---

## Phase Verification Summary

| Phase | VERIFICATION.md | SECURITY.md | VALIDATION.md | Plans |
|-------|------------------|--------------|----------------|-------|
| 17-config-code-truth | passed (4/4 must-haves) | verified, threats_open: 0 (21 threats) | validated, nyquist_compliant: true | 4/4 |

---

## Cross-Phase Integration

Spawned `gsd-integration-checker`. Single-phase milestone (v1.3 = Phase 17 only) — checker traced wiring within the phase and against all pre-existing callers. All 4 requirement flows confirmed wired end-to-end, verdict PASS, zero integration gaps:

1. **TRUTH-01** — `capability.json`'s declared `beads.sync_mode` values → `SYNC_MODE_VALUES` (sync.py:759, now test-pinned) → `read_sync_mode()` → `create-issues`'s `allow_strip` (CLI path, config-gated) vs. the hook path's literal `allow_strip=False` (line 965, ungated per D-03) → `check_sync_mode_value()` wired at `plan:pre` (line 942) for out-of-enum notice. Doc sweep (README.md, `docs/prd-beads-capability.md`, `PRIME.md`) confirmed true against shipped code.
2. **TRUTH-02** — Old CLI verbs eliminated (`git grep` zero hits outside `.planning/`); `check-patch <target>` registered (sync.py:2507-2512) and dispatched through `main()` (2560-2561) to `check_patch()` reading the `PATCH_CHECKS` table; both `beads-recall/SKILL.md:72-73` and `beads-status/SKILL.md:146` call the new verb; `check_shipmd_patch`/`check_execute_plan_patch` survive as one-line wrappers, in-file call sites at `lifecycle_dispatch` (936-937) unchanged. Full E2E chain traced (SKILL.md → CLI → argparse → `check_patch` → `PATCH_CHECKS` entry → file read → message) with no broken link.
3. **TRUTH-03** — `lifecycle_dispatch`'s five points confirmed individually: `plan:pre`, `execute:wave:pre`, `execute:wave:post` unconditional (no gating); `plan:post`, `verify:post` gated on `check_native_step_dispatch()` (949-955, 972-978) — no double-dispatch path exists. `allow_strip` asymmetry (CLI config-gated vs. hook literal `False`) confirmed correct per D-03/D-06.
4. **TRUTH-04** — `phase_regex_token`/`phase_dir_prefix` (string-only, no `int()`/`float()`/`Decimal()`) wired at every call site that previously broke: `get_phase_header`, `extract_phase_mentions`, `_resolve_default_phase_dir`, `discover_plan_files`'s `PLAN_FILE_RE`. Runtime mirror (`.gsd/capabilities/beads/`) confirmed byte-identical to tracked source (`diff -rq`, silent).

No TODO/FIXME/stub/placeholder markers found in any phase-17-touched file. No orphaned exports.

---

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 17-config-code-truth | exists, `status: validated` | **true** | none — closed via `/gsd-validate-phase 17` this session (2 gaps found and resolved: gsd-beads-u67.12, gsd-beads-u67.13) |

All milestone phases Nyquist-compliant. No coverage TODOs outstanding.

---

## Tech Debt (5 items found, 2 resolved this session, 3 open — all non-blocking)

**Phase 17-config-code-truth:**
- ~~WR-04: `SYNC_MODE_VALUES` had no parity test against `capability.json`~~ **RESOLVED** (commit `373e7fb`, this session's `/gsd-validate-phase 17` run).
- ~~WR-05: `check_native_step_dispatch` didn't exclude fenced-block lines from its match~~ **RESOLVED** (commit `0f8decb`, real bug fixed with a RED-then-GREEN regression test, this session's `/gsd-validate-phase 17` run).
- WR-01 (open): `check_sync_mode_value`'s docstring misdescribes `check_patch`'s actual stdout-only output convention — see full item above. Real behavior is correct; only the doc comment is wrong, but it's the kind of wrong that could mislead a future fix.
- WR-02 (open): `not_found_msg`/`could_not_read_msg` lack the `"⚠"` marker their own SKILL.md callers gate surfacing on — pre-existing, not introduced by phase 17.
- WR-03 (open, docs-only): `CHANGELOG.md` 0.4.0 section omits TRUTH-03.

---

## Milestone Verdict

**tech_debt** — All 4 requirements satisfied, phase 17 individually verified/secured/Nyquist-validated, all cross-phase integration wired correctly (integration checker: PASS, zero gaps). Two of five tech-debt items surfaced by this milestone's own code review were closed during this audit's Nyquist validation pass (one was a genuine latent bug, not just a missing test). Three non-blocking items remain (two doc/comment-accuracy issues with real future-maintainer risk, one changelog omission) — recorded above for backlog tracking. No unsatisfied requirements, no broken flows, no orphaned requirements.
