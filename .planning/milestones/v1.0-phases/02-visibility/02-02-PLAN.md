---
phase: 02-visibility
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .gsd/capabilities/beads/scripts/sync.py
  - .gsd/capabilities/beads/capability.json
  - .gsd/capabilities/beads/skills/beads-status/SKILL.md
  - .gsd/capabilities/beads/tests/test_sync.py
autonomous: false
requirements: [B8, B11]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "BEADS.md is regenerated from a real bd query at execute:wave:pre (read-only) and at execute:wave:post (regen+close, unchanged from Phase 1); a hand edit to BEADS.md is fully overwritten, never merged, at the next regeneration"
    - "BEADS.md's frontmatter always carries phase, epic, open, closed, blocking_open, diverged, generated_from, generated_at (D-05), with blocking_open/diverged explicitly marked not-yet-computed (D-06)"
    - "The composed prompt passed to each executor's Agent() call at wave dispatch names the wave's issue ids/titles/status, verified by inspecting the prompt text directly (B8's literal acceptance criterion)"
    - "The beads capability is re-installed and re-consented at project scope after every file edit this phase makes, so the new steps are actually active rather than silently inert"
  artifacts:
    - path: ".gsd/capabilities/beads/scripts/sync.py"
      provides: "regenerate_beads_md (full-overwrite BEADS.md regeneration, D-05..D-08 frontmatter/table shape) and render_wave_status_block (wave-scoped issue block for B8)"
      contains: "regenerate-beads-md"
    - path: ".gsd/capabilities/beads/skills/beads-status/SKILL.md"
      provides: "Lifecycle-point branch (D-11): execute:wave:pre regenerates only; execute:wave:post regenerates and closes (unchanged); explicit instruction telling the orchestrator to paste the printed wave-status block into each executor's composed prompt"
      contains: "execute:wave:pre"
    - path: ".gsd/capabilities/beads/capability.json"
      provides: "execute:wave:pre step dispatching beads-status (read-only branch)"
      contains: "execute:wave:pre"
  key_links:
    - from: ".gsd/capabilities/beads/capability.json"
      to: ".gsd/capabilities/beads/skills/beads-status/SKILL.md"
      via: "steps[] entry at execute:wave:pre with ref.skill beads-status (same skill id as execute:wave:post, D-11)"
      pattern: "\"point\": \"execute:wave:pre\""
    - from: ".gsd/capabilities/beads/skills/beads-status/SKILL.md"
      to: ".gsd/capabilities/beads/scripts/sync.py"
      via: "execute:wave:pre branch instructs Bash to run sync.py regenerate-beads-md, then Read the result and compose the wave-status block"
      pattern: "regenerate-beads-md"
---

<objective>
Close Phase 2's remaining two requirements: BEADS.md is regenerated from a real `bd` query at
every step and a hand edit never survives the next regeneration (B11), and the wave's issue state
is visibly named in the composed prompt each executor actually receives (B8) — proven by
inspecting that prompt directly, not by inferring it from behavior.

Purpose: 02-RESEARCH.md's central finding governs this plan's design and revises D-09/D-10:
`execute:wave:pre` has a real, working `steps[]`/skill-dispatch mechanism (same shape as the
`execute:wave:post` entry Phase 1 already proved), but no `contributions[]` template slot that
forwards fragment text into a spawned executor's `Agent()` prompt. B8 is therefore built as a
`steps[]`-only extension of the already-shipped `beads-status` skill (D-11), not as the PRD's
originally-named `claim-and-close.md` `contributions[]` fragment (D-09/D-10, revised) — the skill's own
prose instructs the orchestrator to carry the wave-status text forward itself, the one mechanism
this project has already proven works end-to-end (Phase 1's entire skill-dispatch model).
Output: `regenerate_beads_md`/`render_wave_status_block` in `sync.py`, the `execute:wave:pre`
capability step, the extended `beads-status` skill, and a human-verified re-install/re-consent plus
live prompt trace closing out Phase 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-visibility/02-CONTEXT.md
@.planning/phases/02-visibility/02-RESEARCH.md
@.planning/phases/02-visibility/02-01-SUMMARY.md
</context>

<wave_pre_dispatch_fact>
02-RESEARCH.md verified, by reading `execute-phase.md` verbatim, that `execute:wave:pre`'s
`contributions[]` point has no template slot forwarding fragment text into any executor's `Agent()`
prompt — `WAVE_PRE_HOOKS_JSON` is assigned once and never referenced again except for one narrow
"alternate wave dispatch" branch. The one first-party capability that successfully uses this exact
point (`claude-orchestration`) works by directing the orchestrator's own subsequent manual actions,
not by automatic text substitution. This plan follows that same pattern: `beads-status`'s
`execute:wave:pre` branch (a `steps[]` dispatch, confirmed working at this point) prints a small
`<beads_status>` block and its own SKILL.md prose instructs the orchestrator to paste that block
into each executor's composed prompt before spawning it — verified by grepping the actual
`prompt=` text an `Agent()` call receives, matching B8's literal acceptance criterion.
</wave_pre_dispatch_fact>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BEADS.md full-overwrite regeneration at execute:wave:pre (D-05..D-08 frontmatter/table shape)</name>
  <files>.gsd/capabilities/beads/scripts/sync.py, .gsd/capabilities/beads/capability.json, .gsd/capabilities/beads/skills/beads-status/SKILL.md, .gsd/capabilities/beads/tests/test_sync.py</files>
  <read_first>.planning/phases/02-visibility/02-RESEARCH.md, .gsd/capabilities/beads/scripts/sync.py, .gsd/capabilities/beads/skills/beads-status/SKILL.md, .gsd/capabilities/beads/capability.json</read_first>
  <reversibility rating="costly">D-05 builds BEADS.md's frontmatter to the full future shape now (phase/epic/open/closed/blocking_open/diverged/generated_from/generated_at) because Phase 3's ship gates read this same file via artifact-frontmatter-equals — adding fields later means re-touching every generation call site and every BEADS.md already on disk.</reversibility>
  <behavior>
    - regenerate_beads_md's frontmatter carries phase/epic/open/closed/blocking_open=0/diverged=0/generated_from/generated_at, matching a mocked bd list --parent response's open/closed counts
    - The body text explicitly states blocking_open/diverged are "not yet computed, Phase 3" (D-06)
    - A hand-edited BEADS.md (extra line inserted by the test before regenerating) is fully overwritten -- the hand edit is absent from the file after the next regenerate_beads_md call
    - The issue table's blocked-by column lists only dependencies[] entries with type=="blocks", excluding type=="parent-child" epic edges (D-08)
    - The bd list --parent call passes -n 0 explicitly (Pitfall 3)
  </behavior>
  <action>
Add `regenerate_beads_md(phase_dir_arg)` to `sync.py`, reusing `bd_available`/`run_bd`/
`find_project_root`/`confined`/`append_state_blocker` — same fail-open shape as `create_issues`/
`close_wave`/`beads_recall`: unavailable `bd` prints the one notice, appends the STATE.md bullet,
returns exit 0, and leaves any existing BEADS.md untouched (never partially rewritten).

Resolve the phase's epic id by reading the `beads_epic` frontmatter key (`BEADS_EPIC_RE`) off any
`NN-NN-PLAN.md` discovered in the phase directory via `discover_plan_files` — every plan in a phase
shares the same epic, so the first plan file carrying the key is sufficient; if none carries one
yet (no plan has ever synced), print a "no epic yet -- nothing to regenerate" line and return 0
without writing a file.

Run `bd list --parent <epic_id> --all --json -n 0` (same query `create_issues`'s orphan sweep
already makes, now with `-n 0` per Pitfall 3 -- the existing orphan-sweep call site is a pre-existing
gap out of this plan's scope to fix). Compute `open`/`closed` counts from the returned rows'
`status` field. Build the 5-column table (issue/title/status/plan task/blocked-by): resolve "plan
task" by reusing `discover_plan_files`+`parse_plan` to map each row's id back to its owning plan's
ordinal prefix; resolve "blocked-by" by filtering each row's `dependencies[]` to entries with
`type == "blocks"` and rendering their `depends_on_id` values (D-08 -- zero extra bd calls, this
data already arrives on the one `bd list --parent` response). Escape `|` and strip `\r`/`\n` from
every title cell (same T-02-03-style rule Task 1 of 02-01-PLAN.md established).

**Always fully overwrite** `{phase_dir}/{padded_phase}-BEADS.md` with the freshly computed content
— never read the existing file's body to merge or preserve any prior edit (B11's literal
requirement: "a hand edit is overwritten at the next step"). Frontmatter: `phase`, `epic`, `open`,
`closed`, `blocking_open: 0`, `diverged: 0`, `generated_from` (the literal bd command run),
`generated_at` (current UTC timestamp). Body opens with one line stating "blocking_open/diverged:
not yet computed, Phase 3" (D-06) before the issue table.

Wire a `regenerate-beads-md <phase_dir>` subcommand in `main()`'s argparse. Add the
`execute:wave:pre` `steps[]` entry to `capability.json`: point `execute:wave:pre`, ref.skill
`beads-status` (same skill id as the existing `execute:wave:post` entry, D-11), produces
`["BEADS.md"]`, consumes `["PLAN.md"]`, `when` `"beads.enabled"`, `onError` `"skip"`.

Extend `beads-status/SKILL.md` with an explicit lifecycle-point branch immediately after Step 1's
config gate: **at `execute:wave:pre`**, dispatch only `python3 .gsd/capabilities/beads/scripts/sync.py
regenerate-beads-md <phase_dir>` (read-only, no close) and stop after Step 3's report; **at
`execute:wave:post`**, the existing Step 2 `close-wave` dispatch is unchanged. State plainly which
branch is which so a future editor does not collapse them into one call.

Add a `TestBeadsMdRegeneration` class to `test_sync.py` covering every case in `<behavior>` above,
mocking `subprocess.run` in the existing `_make_bd_side_effect`-style pattern, with a `dependencies[]`
fixture list mixing `"blocks"` and `"parent-child"` types (RESEARCH.md's verified real `bd list
--parent --json` response shape).
  </action>
  <verify>
    <automated>python3 -m unittest discover -s .gsd/capabilities/beads/tests -k TestBeadsMdRegeneration -v</automated>
  </verify>
  <acceptance_criteria>
    - `python3 .gsd/capabilities/beads/scripts/sync.py regenerate-beads-md --help` exits 0
    - `grep -c '"point": "execute:wave:pre"' .gsd/capabilities/beads/capability.json` returns exactly 1
    - `grep -c '"skill": "beads-status"' .gsd/capabilities/beads/capability.json` returns exactly 2
    - `grep -v '^ *#' .gsd/capabilities/beads/scripts/sync.py | grep -c '"-n", "0"'` returns at least 4
    - `grep -c 'not yet computed, Phase 3' .gsd/capabilities/beads/scripts/sync.py` returns at least 1
    - TestBeadsMdRegeneration asserts a hand-inserted line in an existing BEADS.md is absent after the next regenerate_beads_md call
    - TestBeadsMdRegeneration asserts the blocked-by column excludes a `type=="parent-child"` dependency and includes a `type=="blocks"` one
  </acceptance_criteria>
  <done>BEADS.md is produced solely by regenerate_beads_md from a live bd query, full-file overwrite every run, correct D-05..D-08 frontmatter/table shape; TestBeadsMdRegeneration passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-status block and orchestrator-facing instruction (B8)</name>
  <files>.gsd/capabilities/beads/scripts/sync.py, .gsd/capabilities/beads/skills/beads-status/SKILL.md, .gsd/capabilities/beads/tests/test_sync.py</files>
  <read_first>.planning/phases/02-visibility/02-RESEARCH.md, .gsd/capabilities/beads/scripts/sync.py, /home/dd/.claude/gsd-core/capabilities/claude-orchestration/fragments/execute-wave-pre.md</read_first>
  <behavior>
    - render_wave_status_block's stdout contains every synced issue id/title/status for the given plan_ids' tasks
    - It omits issues belonging to other plans in the same phase directory that were not named in this call
    - With zero plan_ids resolving to any synced task (nothing to show), it prints an explicit "no synced issues for this wave" line rather than an empty block
  </behavior>
  <action>
Add `render_wave_status_block(phase_dir_arg, plan_ids)` to `sync.py`: call `regenerate_beads_md`
first (BEADS.md must reflect the current query before the block is derived from it — never build
the block from a stale prior file), then reuse `discover_plan_files`+`parse_plan` over exactly the
given `plan_ids` to collect their tasks' `<beads-id>` values, and print a `<beads_status>` block to
stdout: one line per matched issue naming its id, title, and status, sourced from the just-written
BEADS.md's table (re-read it, do not re-query bd a second time). When no `plan_ids` resolve to any
synced task, print "no synced issues for this wave" instead of an empty block.

Extend `beads-status/SKILL.md`'s `execute:wave:pre` branch (added in Task 1) with one further
instruction, placed immediately after the `regenerate-beads-md` dispatch: run
`python3 .gsd/capabilities/beads/scripts/sync.py wave-status-block <phase_dir> <wave's plan ids>`
and **read its printed `<beads_status>` block**, then explicitly instruct the orchestrator (the
agent that dispatched this skill, per `claude-orchestration`'s own precedent at this exact
lifecycle point) to include that block verbatim inside every executor `Agent()` call's `prompt=`
string for this wave, before spawning any executor. State this as a direct instruction to the
orchestrator's own next action, not as a fact about the skill's own behavior — this is the
mechanism 02-RESEARCH.md verified actually reaches the composed executor prompt at this point
(Pattern 2: skill-mediated, not `contributions[]`-mediated).

Wire a `wave-status-block <phase_dir> <plan_id...>` subcommand in `main()`'s argparse, matching
`close-wave`'s existing multi-plan-id positional-argument shape.

Add a `TestWaveStatusBlock` class to `test_sync.py` covering every `<behavior>` case above, reusing
the existing `plan-wave-a.md`/`plan-wave-b.md` fixtures and the `_make_close_wave_bd_side_effect`
mock pattern (same `bd list --parent`/`--id` shapes it already answers).
  </action>
  <verify>
    <automated>python3 -m unittest discover -s .gsd/capabilities/beads/tests -k TestWaveStatusBlock -v</automated>
  </verify>
  <acceptance_criteria>
    - `python3 .gsd/capabilities/beads/scripts/sync.py wave-status-block --help` exits 0
    - `grep -c 'beads_status' .gsd/capabilities/beads/skills/beads-status/SKILL.md` returns at least 1
    - `grep -c 'prompt=' .gsd/capabilities/beads/skills/beads-status/SKILL.md` returns at least 1
    - `grep -c "contributions\[\]" .gsd/capabilities/beads/skills/beads-status/SKILL.md` returns 0 (B8 is a steps[]-only design per 02-RESEARCH.md's primary recommendation, never a new contributions[] entry)
    - TestWaveStatusBlock asserts the printed block names every issue id belonging to the given plan_ids and none belonging to an un-named plan in the same phase directory
  </acceptance_criteria>
  <done>render_wave_status_block prints a correct, wave-scoped block; beads-status/SKILL.md explicitly instructs the orchestrator to paste it into each executor's composed prompt at this wave's dispatch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-install, re-consent, and confirm the composed executor prompt names the wave's issues</name>
  <what-built>
Phase 2's complete visibility surface: `beads-recall` (plan:pre, BEADS-RECALL.md), the plan:pre
static contribution pointer, and `beads-status`'s extended execute:wave:pre branch (BEADS.md
regeneration plus the wave-status block instruction for B8). Every file inside
`.gsd/capabilities/beads/` was edited across both of this phase's plans, which invalidates the
project-scope consent hash Phase 1 established (T-01-10's pattern, and 02-RESEARCH.md's Pitfall 2 --
the same failure Phase 1's own CR-01 fix already hit once).
  </what-built>
  <how-to-verify>
1. Re-install the capability at project scope, completing the consent prompt (it will now list
   `beads-recall` alongside the two existing skill stems):

   `gsd capability install ./.gsd/capabilities/beads --scope project`

2. Confirm the new steps are active, not silently inert:

   `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" loop render-hooks plan:pre --raw`

   Expected: a step entry with `capId` `beads`, `ref.skill` `beads-recall`, plus a contribution
   entry with `into` `"planner"`.

   `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" loop render-hooks execute:wave:pre --raw`

   Expected: a step entry with `capId` `beads`, `ref.skill` `beads-status`.

3. Confirm the full suite is green:

   `python3 -m unittest discover -s .gsd/capabilities/beads/tests -q`

4. Run `bd init` in a scratch directory (or reuse Phase 1's own verification database) and dispatch
   one real 2-plan wave with `beads.enabled=true`. Grep the actual `prompt=` text passed to each
   executor's `Agent()` call for the synced issue ids named in BEADS.md's table for that wave --
   this is B8's literal acceptance criterion (inspect the composed prompt directly, never infer
   from behavior alone).

5. Confirm BEADS-RECALL.md exists at `{phase_dir}/{padded_phase}-BEADS-RECALL.md` before a real
   `/gsd:plan-phase` run's planner subagent is spawned, even against a project with zero open
   issues (B7/D-04 baseline).
  </how-to-verify>
  <resume-signal>Type "approved" if both render-hooks calls name the beads capability's new entries, the suite is green, and the composed executor prompt names the wave's issue ids, or describe what appeared instead</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `bd` issue title/description text -> BEADS.md table cells / `<beads_status>` block | Same untrusted-text-into-generated-artifact boundary as 02-01-PLAN.md's T-02-03, now covering BEADS.md and the printed wave-status block. |
| Wave plan ids -> filesystem plan lookup | `render_wave_status_block` resolves plan ids against discovered plan files, same shape as Phase 1's `close_wave` (T-01-07). |
| Capability re-install -> user's project | Re-consenting after this phase's edits grants the newly extended instruction surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | BEADS.md regeneration | medium | mitigate | `regenerate_beads_md` always fully overwrites the file's content from a fresh bd query rather than reading and merging the existing file (B11's literal requirement); enforced by TestBeadsMdRegeneration's hand-edit-then-regenerate assertion. |
| T-02-05 | Information Disclosure | `bd` issue title/description echoed into BEADS.md / the wave-status block | low | mitigate | Same escaping rule as T-02-03: strip `\|`/`\r`/`\n` from every title before it enters a markdown table cell or the printed block. |
| T-02-06 | Elevation of Privilege | Project-scope capability re-install/re-consent after this phase's file edits | high | mitigate | Same control as T-01-10: `gsd capability install --scope project` at a blocking human checkpoint (Task 3), never auto-approved, reviewed before the extended instruction surface becomes active. |
| T-02-07 | Tampering | `bd list --parent <epic> --all --json` without `-n 0` (Pitfall 3) | medium | mitigate | Every new `bd list` call this plan adds passes `-n 0` explicitly, enforced by this plan's `-n", "0` grep gate; the pre-existing Phase 1 orphan-sweep call site missing `-n 0` is flagged, not fixed, as out of this plan's scope. |
| T-02-SC | Tampering | Supply chain | low | accept | No package-manager install this plan — N5 unchanged; not applicable per 02-RESEARCH.md's Package Legitimacy Audit. |
</threat_model>

<verification>
- `python3 -m unittest discover -s .gsd/capabilities/beads/tests -q` exits 0
- `python3 .gsd/capabilities/beads/scripts/sync.py --help` exits 0 and lists regenerate-beads-md and wave-status-block
- `python3 -m json.tool .gsd/capabilities/beads/capability.json` exits 0
- `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" loop render-hooks execute:wave:pre --raw` names the beads capability after Task 3's re-install
</verification>

<success_criteria>
- BEADS.md is regenerated from a real bd query at execute:wave:pre and execute:wave:post; a hand edit never survives the next regeneration (B11)
- The composed prompt each executor's Agent() call receives names this wave's issue ids, verified by direct prompt inspection, not inferred behavior (B8)
- The beads capability is re-installed and re-consented after every file this phase edited, so the new steps actually dispatch
</success_criteria>

<output>
Create `.planning/phases/02-visibility/02-02-SUMMARY.md` when done
</output>
